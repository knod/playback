/* skiip-combos-assertions.js
* 
* State skips words exceptions to single call results
*  
* Combos don't always get the same results as singles.
* Certain combos MUST fail, though, and others must NOT fail.
* This makes sure that's happening.
* 
* TODO:
* - Generate file with expected results
*/

'use strict';


var colors = {
  green: '\x1B[32m', red: '\x1B[31m',
  yellow: '\x1B[33m', none: '\x1B[0m'
};


// ------------ export ------------
const MakeAltAsserts = module.exports = function ( plyb ) {
/* ( {} ) -> {}
* 
* TODO:
* Could make this into a function that parses the label handed in and gets
* the right assert ( e.g. /doubles: play(null) + reset/.test(label) ),
* but I think that would be less clear, less easy to look up
* May be necessary, though.
*/

	let plab = plyb;
	var asts = {};


	// ------------ default assertions ------------
	var defaultAsserts = {
		triggered: null,
		not: null,
		frags: null,
		progress: null
	}

	defaultAsserts.not = function ( result ) {

		var passes 	= true;
		var msg 	= 'Combos: Event ' + colors.green + 'NOT' + colors.none + ' triggered';

		// var shouldFail = checkExpectedToFail( testID );
		// if ( shouldFail ) {
		// 	// Nothing is expected, it's failing for good reasons
		// } else {
			if ( result.arg2s.length !== 0 ) {
				passes = false;
				var testID
				msg = 'Combos-' + testID +': Event should not have been triggered but ' + colors.red + 'WAS' + colors.none;
			}
		// }
		return { message: msg, passed: passes };
	};


	asts.assert = function ( label, originalAssertion, result, debug ) {
	// By this point, the first test/assertion was passed and gotten some
	// result, so nothing should be interfereing with the possibility of further results

		// Try to keep these tests as searchable as possible without having a million
		// page doc of each individual label

		if ( debug ) { console.log( label ); }
		var assert = originalAssertion.assertion;


		var testNum = '';
		// If the second function asserts as not, try that first
		if ( result.arg2s.length <= 0 && originalAssertion.type === 'not' ) {
			testNum = '2ndNot-1';

			var testResult = defaultAsserts.not( result, testNum );
			if ( testResult.passed ) { return testResult; }
			// otherwise, if the test failed, continue onward
		}


		const expectFailure = function ( result, evnt ) {
			var testResult = assert( result, 'combos', evnt  )

			var msg, correctlyFailed = !testResult.passed;
			// If correctlyFailed, that was the expected result
			if ( correctlyFailed ) {
				msg = 'Combos: Expected and recieved a failure at combo test.';
			} else {
				msg = 'Combos: Expected test to fail, but it ' + colors.red + 'DIDN\'T FAIL' + colors.none + ':';
				msg += ' Unexpected success: ' + colors.red + testResult.message + colors.none;
			}

			return { passed: correctlyFailed, message: msg }
		};


		if ( asts.expectedFailures[ label ] !== undefined ) {
			return expectFailure( result );
		} else {
			return assert( result );
		}

	};  // End getAssertion() ( func from label)

	asts.expectedFailures = {

		// ============== forceReset ==============
		// #1
		// x + 'resetBegin' > anything except forceReset = not triggered
		'skip-combos: forceReset(null) + resetBegin > reset(null) + resetBegin': true,  // 28: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > reset(null) + resetFinish': true,  // 29: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > restart(null) + restartBegin': true,  // 55: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > restart(null) + restartFinish': true,  // 56: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > restart(null) + loopBegin': true,  // 69: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > restart(null) + loopFinish': true,  // 70: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > restart(null) + newWordFragment': true,  // 71: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > restart(null) + progress': true,  // 72: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > restart(null) + stopBegin': true,  // 73: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > restart(null) + stopFinish': true,  // 74: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > restart(null) + done': true,  // 75: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > play(null) + playBegin': true,  // 76: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > play(null) + playFinish': true,  // 77: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > play(null) + loopBegin': true,  // 94: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > play(null) + loopFinish': true,  // 95: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > play(null) + newWordFragment': true,  // 96: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > play(null) + progress': true,  // 97: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > play(null) + stopBegin': true,  // 98: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > play(null) + stopFinish': true,  // 99: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > play(null) + done': true,  // 100: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > toggle(null) + playBegin': true,  // 101: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > toggle(null) + playFinish': true,  // 102: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > toggle(null) + loopBegin': true,  // 119: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > toggle(null) + loopFinish': true,  // 120: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > toggle(null) + newWordFragment': true,  // 121: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > toggle(null) + progress': true,  // 122: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > toggle(null) + stopBegin': true,  // 123: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > toggle(null) + stopFinish': true,  // 124: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > toggle(null) + done': true,  // 125: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > pause(null) + pauseBegin': true,  // 132: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > pause(null) + pauseFinish': true,  // 133: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > stop(null) + stopBegin': true,  // 173: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > stop(null) + stopFinish': true,  // 174: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > close(null) + closeBegin': true,  // 184: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > close(null) + closeFinish': true,  // 185: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > revert(null) + pauseBegin': true,  // 207: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > revert(null) + pauseFinish': true,  // 208: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > revert(null) + revertBegin': true,  // 213: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > revert(null) + revertFinish': true,  // 214: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > rewind(null) + rewindBegin': true,  // 240: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > rewind(null) + rewindFinish': true,  // 241: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > rewind(null) + loopBegin': true,  // 244: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > rewind(null) + loopFinish': true,  // 245: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > rewind(null) + newWordFragment': true,  // 246: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > rewind(null) + progress': true,  // 247: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > rewind(null) + stopBegin': true,  // 248: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > rewind(null) + stopFinish': true,  // 249: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > rewind(null) + done': true,  // 250: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > fastForward(null) + fastForwardBegin': true,  // 267: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > fastForward(null) + fastForwardFinish': true,  // 268: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > fastForward(null) + loopBegin': true,  // 269: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > fastForward(null) + loopFinish': true,  // 270: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > fastForward(null) + newWordFragment': true,  // 271: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > fastForward(null) + progress': true,  // 272: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > fastForward(null) + stopBegin': true,  // 273: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > fastForward(null) + stopFinish': true,  // 274: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > fastForward(null) + done': true,  // 275: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > once([0,0,-2]) + onceBegin': true,  // 286: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > once([0,0,-2]) + onceFinish': true,  // 287: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > once([0,0,-2]) + loopBegin': true,  // 294: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > once([0,0,-2]) + loopFinish': true,  // 295: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > once([0,0,-2]) + newWordFragment': true,  // 296: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > once([0,0,-2]) + progress': true,  // 297: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > once([0,0,-2]) + stopBegin': true,  // 298: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > once([0,0,-2]) + stopFinish': true,  // 299: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > once([0,0,-2]) + done': true,  // 300: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > once([0,0,2]) + onceBegin': true,  // 311: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > once([0,0,2]) + onceFinish': true,  // 312: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > once([0,0,2]) + loopBegin': true,  // 319: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > once([0,0,2]) + loopFinish': true,  // 320: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > once([0,0,2]) + newWordFragment': true,  // 321: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > once([0,0,2]) + progress': true,  // 322: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > current(null) + onceBegin': true,  // 336: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > current(null) + onceFinish': true,  // 337: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > current(null) + loopBegin': true,  // 344: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > current(null) + loopFinish': true,  // 345: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > current(null) + newWordFragment': true,  // 346: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > current(null) + progress': true,  // 347: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(-3) + onceBegin': true,  // 361: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(-3) + onceFinish': true,  // 362: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(-3) + loopBegin': true,  // 369: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(-3) + loopFinish': true,  // 370: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(-3) + newWordFragment': true,  // 371: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(-3) + progress': true,  // 372: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(-3) + stopBegin': true,  // 373: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(-3) + stopFinish': true,  // 374: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(-3) + done': true,  // 375: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(-1) + onceBegin': true,  // 386: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(-1) + onceFinish': true,  // 387: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(-1) + loopBegin': true,  // 394: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(-1) + loopFinish': true,  // 395: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(-1) + newWordFragment': true,  // 396: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(-1) + progress': true,  // 397: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(-1) + stopBegin': true,  // 398: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(-1) + stopFinish': true,  // 399: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(-1) + done': true,  // 400: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(0) + onceBegin': true,  // 411: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(0) + onceFinish': true,  // 412: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(0) + loopBegin': true,  // 419: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(0) + loopFinish': true,  // 420: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(0) + newWordFragment': true,  // 421: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(0) + progress': true,  // 422: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(6) + onceBegin': true,  // 436: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(6) + onceFinish': true,  // 437: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(6) + loopBegin': true,  // 444: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(6) + loopFinish': true,  // 445: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(6) + newWordFragment': true,  // 446: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(6) + progress': true,  // 447: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(11) + onceBegin': true,  // 461: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(11) + onceFinish': true,  // 462: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(11) + loopBegin': true,  // 469: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(11) + loopFinish': true,  // 470: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(11) + newWordFragment': true,  // 471: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(11) + progress': true,  // 472: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(11) + stopBegin': true,  // 473: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(11) + stopFinish': true,  // 474: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(11) + done': true,  // 475: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(100) + onceBegin': true,  // 486: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(100) + onceFinish': true,  // 487: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(100) + loopBegin': true,  // 494: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(100) + loopFinish': true,  // 495: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(100) + newWordFragment': true,  // 496: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(100) + progress': true,  // 497: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(100) + stopBegin': true,  // 498: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(100) + stopFinish': true,  // 499: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpTo(100) + done': true,  // 500: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(-3) + onceBegin': true,  // 511: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(-3) + onceFinish': true,  // 512: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(-3) + loopBegin': true,  // 519: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(-3) + loopFinish': true,  // 520: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(-3) + newWordFragment': true,  // 521: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(-3) + progress': true,  // 522: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(-3) + stopBegin': true,  // 523: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(-3) + stopFinish': true,  // 524: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(-3) + done': true,  // 525: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(-1) + onceBegin': true,  // 536: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(-1) + onceFinish': true,  // 537: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(-1) + loopBegin': true,  // 544: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(-1) + loopFinish': true,  // 545: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(-1) + newWordFragment': true,  // 546: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(-1) + progress': true,  // 547: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(-1) + stopBegin': true,  // 548: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(-1) + stopFinish': true,  // 549: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(-1) + done': true,  // 550: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(0) + onceBegin': true,  // 561: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(0) + onceFinish': true,  // 562: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(0) + loopBegin': true,  // 569: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(0) + loopFinish': true,  // 570: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(0) + newWordFragment': true,  // 571: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(0) + progress': true,  // 572: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(4) + onceBegin': true,  // 586: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(4) + onceFinish': true,  // 587: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(4) + loopBegin': true,  // 594: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(4) + loopFinish': true,  // 595: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(4) + newWordFragment': true,  // 596: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(4) + progress': true,  // 597: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(11) + onceBegin': true,  // 611: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(11) + onceFinish': true,  // 612: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(11) + loopBegin': true,  // 619: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(11) + loopFinish': true,  // 620: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(11) + newWordFragment': true,  // 621: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(11) + progress': true,  // 622: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(11) + stopBegin': true,  // 623: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(11) + stopFinish': true,  // 624: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(11) + done': true,  // 625: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(100) + onceBegin': true,  // 636: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(100) + onceFinish': true,  // 637: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(100) + loopBegin': true,  // 644: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(100) + loopFinish': true,  // 645: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(100) + newWordFragment': true,  // 646: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(100) + progress': true,  // 647: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(100) + stopBegin': true,  // 648: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(100) + stopFinish': true,  // 649: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpWords(100) + done': true,  // 650: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(-3) + onceBegin': true,  // 661: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(-3) + onceFinish': true,  // 662: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(-3) + loopBegin': true,  // 669: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(-3) + loopFinish': true,  // 670: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(-3) + newWordFragment': true,  // 671: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(-3) + progress': true,  // 672: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(-3) + stopBegin': true,  // 673: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(-3) + stopFinish': true,  // 674: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(-3) + done': true,  // 675: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(-1) + onceBegin': true,  // 686: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(-1) + onceFinish': true,  // 687: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(-1) + loopBegin': true,  // 694: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(-1) + loopFinish': true,  // 695: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(-1) + newWordFragment': true,  // 696: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(-1) + progress': true,  // 697: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(-1) + stopBegin': true,  // 698: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(-1) + stopFinish': true,  // 699: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(-1) + done': true,  // 700: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(0) + onceBegin': true,  // 711: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(0) + onceFinish': true,  // 712: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(0) + loopBegin': true,  // 719: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(0) + loopFinish': true,  // 720: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(0) + newWordFragment': true,  // 721: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(0) + progress': true,  // 722: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(1) + onceBegin': true,  // 736: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(1) + onceFinish': true,  // 737: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(1) + loopBegin': true,  // 744: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(1) + loopFinish': true,  // 745: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(1) + newWordFragment': true,  // 746: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(1) + progress': true,  // 747: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(3) + onceBegin': true,  // 761: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(3) + onceFinish': true,  // 762: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(3) + loopBegin': true,  // 769: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(3) + loopFinish': true,  // 770: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(3) + newWordFragment': true,  // 771: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(3) + progress': true,  // 772: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(100) + onceBegin': true,  // 786: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(100) + onceFinish': true,  // 787: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(100) + loopBegin': true,  // 794: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(100) + loopFinish': true,  // 795: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(100) + newWordFragment': true,  // 796: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(100) + progress': true,  // 797: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(100) + stopBegin': true,  // 798: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(100) + stopFinish': true,  // 799: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(100) + done': true,  // 800: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > nextWord(null) + onceBegin': true,  // 811: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > nextWord(null) + onceFinish': true,  // 812: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > nextWord(null) + loopBegin': true,  // 819: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > nextWord(null) + loopFinish': true,  // 820: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > nextWord(null) + newWordFragment': true,  // 821: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > nextWord(null) + progress': true,  // 822: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > nextSentence(null) + onceBegin': true,  // 836: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > nextSentence(null) + onceFinish': true,  // 837: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > nextSentence(null) + loopBegin': true,  // 844: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > nextSentence(null) + loopFinish': true,  // 845: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > nextSentence(null) + newWordFragment': true,  // 846: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > nextSentence(null) + progress': true,  // 847: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > prevWord(null) + onceBegin': true,  // 861: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > prevWord(null) + onceFinish': true,  // 862: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > prevWord(null) + loopBegin': true,  // 869: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > prevWord(null) + loopFinish': true,  // 870: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > prevWord(null) + newWordFragment': true,  // 871: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > prevWord(null) + progress': true,  // 872: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > prevWord(null) + stopBegin': true,  // 873: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > prevWord(null) + stopFinish': true,  // 874: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > prevWord(null) + done': true,  // 875: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > prevSentence(null) + onceBegin': true,  // 886: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > prevSentence(null) + onceFinish': true,  // 887: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > prevSentence(null) + loopBegin': true,  // 894: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > prevSentence(null) + loopFinish': true,  // 895: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > prevSentence(null) + newWordFragment': true,  // 896: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > prevSentence(null) + progress': true,  // 897: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > prevSentence(null) + stopBegin': true,  // 898: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > prevSentence(null) + stopFinish': true,  // 899: ?? (skip results are different than combos)
		'skip-combos: forceReset(null) + resetBegin > prevSentence(null) + done': true,  // 900: ?? (skip results are different than combos)


		// ============== forceReset as 2nd func ==============
		// #2 A function, once started, finishes its stuff, even if `forceReset()` is used
		// any(any) > forceReset + any relevant events = triggered
		'skip-combos: restart(null) + restartBegin > forceReset(null) + restartFinish': false,  // 1806: ?? (skip results are different than combos)
		'skip-combos: restart(null) + restartBegin > forceReset(null) + loopBegin': false,  // 1819: ?? (skip results are different than combos)
		'skip-combos: restart(null) + restartBegin > forceReset(null) + loopFinish': false,  // 1820: ?? (skip results are different than combos)
		'skip-combos: restart(null) + restartBegin > forceReset(null) + newWordFragment': false,  // 1821: ?? (skip results are different than combos)
		'skip-combos: restart(null) + restartBegin > forceReset(null) + progress': false,  // 1822: ?? (skip results are different than combos)
		'skip-combos: restart(null) + restartBegin > forceReset(null) + stopBegin': false,  // 1823: ?? (skip results are different than combos)
		'skip-combos: restart(null) + restartBegin > forceReset(null) + stopFinish': false,  // 1824: ?? (skip results are different than combos)
		'skip-combos: restart(null) + restartBegin > forceReset(null) + done': false,  // 1825: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopBegin > forceReset(null) + restartFinish': false,  // 16206: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopBegin > forceReset(null) + loopBegin': false,  // 16219: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopBegin > forceReset(null) + loopFinish': false,  // 16220: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopBegin > forceReset(null) + newWordFragment': false,  // 16221: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopBegin > forceReset(null) + progress': false,  // 16222: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopBegin > forceReset(null) + stopBegin': false,  // 16223: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopBegin > forceReset(null) + stopFinish': false,  // 16224: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopBegin > forceReset(null) + done': false,  // 16225: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopFinish > forceReset(null) + restartFinish': false,  // 17106: ?? (skip results are different than combos)
		'skip-combos: restart(null) + newWordFragment > forceReset(null) + restartFinish': false,  // 18006: ?? (skip results are different than combos)
		'skip-combos: restart(null) + newWordFragment > forceReset(null) + loopFinish': false,  // 18020: ?? (skip results are different than combos)
		'skip-combos: restart(null) + newWordFragment > forceReset(null) + progress': false,  // 18022: ?? (skip results are different than combos)
		'skip-combos: restart(null) + progress > forceReset(null) + restartFinish': false,  // 18906: ?? (skip results are different than combos)
		'skip-combos: restart(null) + progress > forceReset(null) + loopFinish': false,  // 18920: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > forceReset(null) + stopFinish': false,  // 19824: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > forceReset(null) + done': false,  // 19825: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > forceReset(null) + done': false,  // 20725: ?? (skip results are different than combos)
		'skip-combos: play(null) + playBegin > forceReset(null) + playFinish': true,  // 3602: ?? (skip results are different than combos)
		'skip-combos: play(null) + playBegin > forceReset(null) + loopBegin': true,  // 3619: ?? (skip results are different than combos)
		'skip-combos: play(null) + playBegin > forceReset(null) + loopFinish': true,  // 3620: ?? (skip results are different than combos)
		'skip-combos: play(null) + playBegin > forceReset(null) + newWordFragment': true,  // 3621: ?? (skip results are different than combos)
		'skip-combos: play(null) + playBegin > forceReset(null) + progress': true,  // 3622: ?? (skip results are different than combos)
		'skip-combos: play(null) + playBegin > forceReset(null) + stopBegin': true,  // 3623: ?? (skip results are different than combos)
		'skip-combos: play(null) + playBegin > forceReset(null) + stopFinish': true,  // 3624: ?? (skip results are different than combos)
		'skip-combos: play(null) + playBegin > forceReset(null) + done': true,  // 3625: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopBegin > forceReset(null) + playFinish': true,  // 16202: ?? (skip results are different than combos)
		// TODO: ??: Why does 'loopBegin' get triggered twice?
		'skip-combos: play(null) + loopBegin > forceReset(null) + loopBegin': true,  // 16219: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopBegin > forceReset(null) + loopFinish': true,  // 16220: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopBegin > forceReset(null) + newWordFragment': true,  // 16221: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopBegin > forceReset(null) + progress': true,  // 16222: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopBegin > forceReset(null) + stopBegin': true,  // 16223: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopBegin > forceReset(null) + stopFinish': true,  // 16224: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopBegin > forceReset(null) + done': true,  // 16225: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopFinish > forceReset(null) + playFinish': true,  // 17102: ?? (skip results are different than combos)
		'skip-combos: play(null) + newWordFragment > forceReset(null) + playFinish': true,  // 18002: ?? (skip results are different than combos)
		'skip-combos: play(null) + newWordFragment > forceReset(null) + loopFinish': true,  // 18020: ?? (skip results are different than combos)
		'skip-combos: play(null) + newWordFragment > forceReset(null) + progress': true,  // 18022: ?? (skip results are different than combos)
		'skip-combos: play(null) + progress > forceReset(null) + playFinish': true,  // 18902: ?? (skip results are different than combos)
		'skip-combos: play(null) + progress > forceReset(null) + loopFinish': true,  // 18920: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > forceReset(null) + stopFinish': true,  // 19824: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > forceReset(null) + done': true,  // 19825: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > forceReset(null) + done': true,  // 20725: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playBegin > forceReset(null) + playFinish': true,  // 3602: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playBegin > forceReset(null) + loopBegin': true,  // 3619: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playBegin > forceReset(null) + loopFinish': true,  // 3620: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playBegin > forceReset(null) + newWordFragment': true,  // 3621: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playBegin > forceReset(null) + progress': true,  // 3622: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playBegin > forceReset(null) + stopBegin': true,  // 3623: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playBegin > forceReset(null) + stopFinish': true,  // 3624: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playBegin > forceReset(null) + done': true,  // 3625: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopBegin > forceReset(null) + playFinish': true,  // 16202: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopBegin > forceReset(null) + loopBegin': true,  // 16219: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopBegin > forceReset(null) + loopFinish': true,  // 16220: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopBegin > forceReset(null) + newWordFragment': true,  // 16221: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopBegin > forceReset(null) + progress': true,  // 16222: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopBegin > forceReset(null) + stopBegin': true,  // 16223: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopBegin > forceReset(null) + stopFinish': true,  // 16224: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopBegin > forceReset(null) + done': true,  // 16225: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopFinish > forceReset(null) + playFinish': true,  // 17102: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + newWordFragment > forceReset(null) + playFinish': true,  // 18002: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + newWordFragment > forceReset(null) + loopFinish': true,  // 18020: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + newWordFragment > forceReset(null) + progress': true,  // 18022: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + progress > forceReset(null) + playFinish': true,  // 18902: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + progress > forceReset(null) + loopFinish': true,  // 18920: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > forceReset(null) + stopFinish': true,  // 19824: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > forceReset(null) + done': true,  // 19825: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > forceReset(null) + done': true,  // 20725: ?? (skip results are different than combos)
		'skip-combos: pause(null) + pauseBegin > forceReset(null) + pauseFinish': true,  // 5408: ?? (skip results are different than combos)
		'skip-combos: stop(null) + stopBegin > forceReset(null) + stopFinish': true,  // 19824: ?? (skip results are different than combos)
		'skip-combos: close(null) + closeBegin > forceReset(null) + closeFinish': true,  // 7210: ?? (skip results are different than combos)
		'skip-combos: revert(null) + pauseBegin > forceReset(null) + pauseFinish': true,  // 5408: ?? (skip results are different than combos)
		'skip-combos: revert(null) + pauseBegin > forceReset(null) + revertFinish': true,  // 5414: ?? (skip results are different than combos)
		'skip-combos: revert(null) + pauseFinish > forceReset(null) + revertFinish': true,  // 6314: ?? (skip results are different than combos)
		'skip-combos: revert(null) + revertBegin > forceReset(null) + pauseBegin': true,  // 10807: ?? (skip results are different than combos)
		'skip-combos: revert(null) + revertBegin > forceReset(null) + pauseFinish': true,  // 10808: ?? (skip results are different than combos)
		'skip-combos: revert(null) + revertBegin > forceReset(null) + revertFinish': true,  // 10814: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + rewindBegin > forceReset(null) + rewindFinish': true,  // 12616: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + rewindBegin > forceReset(null) + loopBegin': true,  // 12619: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + rewindBegin > forceReset(null) + loopFinish': true,  // 12620: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + rewindBegin > forceReset(null) + newWordFragment': true,  // 12621: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + rewindBegin > forceReset(null) + progress': true,  // 12622: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + rewindBegin > forceReset(null) + stopBegin': true,  // 12623: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + rewindBegin > forceReset(null) + stopFinish': true,  // 12624: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + rewindBegin > forceReset(null) + done': true,  // 12625: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + loopBegin > forceReset(null) + rewindFinish': true,  // 16216: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + loopBegin > forceReset(null) + loopBegin': true,  // 16219: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + loopBegin > forceReset(null) + loopFinish': true,  // 16220: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + loopBegin > forceReset(null) + newWordFragment': true,  // 16221: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + loopBegin > forceReset(null) + progress': true,  // 16222: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + loopBegin > forceReset(null) + stopBegin': true,  // 16223: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + loopBegin > forceReset(null) + stopFinish': true,  // 16224: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + loopBegin > forceReset(null) + done': true,  // 16225: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + loopFinish > forceReset(null) + rewindFinish': true,  // 17116: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + newWordFragment > forceReset(null) + rewindFinish': true,  // 18016: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + newWordFragment > forceReset(null) + loopFinish': true,  // 18020: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + newWordFragment > forceReset(null) + progress': true,  // 18022: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + progress > forceReset(null) + rewindFinish': true,  // 18916: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + progress > forceReset(null) + loopFinish': true,  // 18920: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + stopBegin > forceReset(null) + rewindFinish': true,  // 19816: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + stopBegin > forceReset(null) + stopFinish': true,  // 19824: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + stopBegin > forceReset(null) + done': true,  // 19825: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + stopFinish > forceReset(null) + rewindFinish': true,  // 20716: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + stopFinish > forceReset(null) + done': true,  // 20725: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + done > forceReset(null) + rewindFinish': true,  // 21616: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardBegin > forceReset(null) + fastForwardFinish': true,  // 14418: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardBegin > forceReset(null) + loopBegin': true,  // 14419: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardBegin > forceReset(null) + loopFinish': true,  // 14420: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardBegin > forceReset(null) + newWordFragment': true,  // 14421: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardBegin > forceReset(null) + progress': true,  // 14422: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardBegin > forceReset(null) + stopBegin': true,  // 14423: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardBegin > forceReset(null) + stopFinish': true,  // 14424: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardBegin > forceReset(null) + done': true,  // 14425: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopBegin > forceReset(null) + fastForwardFinish': true,  // 16218: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopBegin > forceReset(null) + loopBegin': true,  // 16219: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopBegin > forceReset(null) + loopFinish': true,  // 16220: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopBegin > forceReset(null) + newWordFragment': true,  // 16221: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopBegin > forceReset(null) + progress': true,  // 16222: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopBegin > forceReset(null) + stopBegin': true,  // 16223: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopBegin > forceReset(null) + stopFinish': true,  // 16224: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopBegin > forceReset(null) + done': true,  // 16225: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopFinish > forceReset(null) + fastForwardFinish': true,  // 17118: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + newWordFragment > forceReset(null) + fastForwardFinish': true,  // 18018: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + newWordFragment > forceReset(null) + loopFinish': true,  // 18020: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + newWordFragment > forceReset(null) + progress': true,  // 18022: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + progress > forceReset(null) + fastForwardFinish': true,  // 18918: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + progress > forceReset(null) + loopFinish': true,  // 18920: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > forceReset(null) + stopFinish': true,  // 19824: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > forceReset(null) + done': true,  // 19825: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > forceReset(null) + done': true,  // 20725: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + onceBegin > forceReset(null) + onceFinish': true,  // 9012: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + onceBegin > forceReset(null) + loopBegin': true,  // 9019: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + onceBegin > forceReset(null) + loopFinish': true,  // 9020: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + onceBegin > forceReset(null) + newWordFragment': true,  // 9021: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + onceBegin > forceReset(null) + progress': true,  // 9022: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + onceBegin > forceReset(null) + stopBegin': true,  // 9023: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + onceBegin > forceReset(null) + stopFinish': true,  // 9024: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + onceBegin > forceReset(null) + done': true,  // 9025: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + loopBegin > forceReset(null) + onceFinish': true,  // 16212: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + loopBegin > forceReset(null) + loopFinish': true,  // 16220: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + loopBegin > forceReset(null) + newWordFragment': true,  // 16221: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + loopBegin > forceReset(null) + progress': true,  // 16222: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + loopFinish > forceReset(null) + onceFinish': true,  // 17112: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + newWordFragment > forceReset(null) + onceFinish': true,  // 18012: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + newWordFragment > forceReset(null) + loopFinish': true,  // 18020: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + newWordFragment > forceReset(null) + progress': true,  // 18022: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + progress > forceReset(null) + onceFinish': true,  // 18912: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + progress > forceReset(null) + loopFinish': true,  // 18920: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + stopBegin > forceReset(null) + onceFinish': true,  // 19812: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + stopBegin > forceReset(null) + stopFinish': true,  // 19824: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + stopBegin > forceReset(null) + done': true,  // 19825: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + stopFinish > forceReset(null) + onceFinish': true,  // 20712: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + stopFinish > forceReset(null) + done': true,  // 20725: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + done > forceReset(null) + onceFinish': true,  // 21612: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceBegin > forceReset(null) + onceFinish': true,  // 9012: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceBegin > forceReset(null) + loopBegin': true,  // 9019: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceBegin > forceReset(null) + loopFinish': true,  // 9020: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceBegin > forceReset(null) + newWordFragment': true,  // 9021: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceBegin > forceReset(null) + progress': true,  // 9022: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopBegin > forceReset(null) + onceFinish': true,  // 16212: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopBegin > forceReset(null) + loopFinish': true,  // 16220: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopBegin > forceReset(null) + newWordFragment': true,  // 16221: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopBegin > forceReset(null) + progress': true,  // 16222: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopFinish > forceReset(null) + onceFinish': true,  // 17112: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + newWordFragment > forceReset(null) + onceFinish': true,  // 18012: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + newWordFragment > forceReset(null) + loopFinish': true,  // 18020: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + newWordFragment > forceReset(null) + progress': true,  // 18022: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + progress > forceReset(null) + onceFinish': true,  // 18912: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + progress > forceReset(null) + loopFinish': true,  // 18920: ?? (skip results are different than combos)
		'skip-combos: current(null) + onceBegin > forceReset(null) + onceFinish': true,  // 9012: ?? (skip results are different than combos)
		'skip-combos: current(null) + onceBegin > forceReset(null) + loopBegin': true,  // 9019: ?? (skip results are different than combos)
		'skip-combos: current(null) + onceBegin > forceReset(null) + loopFinish': true,  // 9020: ?? (skip results are different than combos)
		'skip-combos: current(null) + onceBegin > forceReset(null) + newWordFragment': true,  // 9021: ?? (skip results are different than combos)
		'skip-combos: current(null) + onceBegin > forceReset(null) + progress': true,  // 9022: ?? (skip results are different than combos)
		'skip-combos: current(null) + loopBegin > forceReset(null) + onceFinish': true,  // 16212: ?? (skip results are different than combos)
		'skip-combos: current(null) + loopBegin > forceReset(null) + loopFinish': true,  // 16220: ?? (skip results are different than combos)
		'skip-combos: current(null) + loopBegin > forceReset(null) + newWordFragment': true,  // 16221: ?? (skip results are different than combos)
		'skip-combos: current(null) + loopBegin > forceReset(null) + progress': true,  // 16222: ?? (skip results are different than combos)
		'skip-combos: current(null) + loopFinish > forceReset(null) + onceFinish': true,  // 17112: ?? (skip results are different than combos)
		'skip-combos: current(null) + newWordFragment > forceReset(null) + onceFinish': true,  // 18012: ?? (skip results are different than combos)
		'skip-combos: current(null) + newWordFragment > forceReset(null) + loopFinish': true,  // 18020: ?? (skip results are different than combos)
		'skip-combos: current(null) + newWordFragment > forceReset(null) + progress': true,  // 18022: ?? (skip results are different than combos)
		'skip-combos: current(null) + progress > forceReset(null) + onceFinish': true,  // 18912: ?? (skip results are different than combos)
		'skip-combos: current(null) + progress > forceReset(null) + loopFinish': true,  // 18920: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + onceBegin > forceReset(null) + onceFinish': true,  // 9012: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + onceBegin > forceReset(null) + loopBegin': true,  // 9019: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + onceBegin > forceReset(null) + loopFinish': true,  // 9020: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + onceBegin > forceReset(null) + newWordFragment': true,  // 9021: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + onceBegin > forceReset(null) + progress': true,  // 9022: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + onceBegin > forceReset(null) + stopBegin': true,  // 9023: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + onceBegin > forceReset(null) + stopFinish': true,  // 9024: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + onceBegin > forceReset(null) + done': true,  // 9025: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + loopBegin > forceReset(null) + onceFinish': true,  // 16212: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + loopBegin > forceReset(null) + loopFinish': true,  // 16220: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + loopBegin > forceReset(null) + newWordFragment': true,  // 16221: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + loopBegin > forceReset(null) + progress': true,  // 16222: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + loopFinish > forceReset(null) + onceFinish': true,  // 17112: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + newWordFragment > forceReset(null) + onceFinish': true,  // 18012: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + newWordFragment > forceReset(null) + loopFinish': true,  // 18020: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + newWordFragment > forceReset(null) + progress': true,  // 18022: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + progress > forceReset(null) + onceFinish': true,  // 18912: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + progress > forceReset(null) + loopFinish': true,  // 18920: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + stopBegin > forceReset(null) + onceFinish': true,  // 19812: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + stopBegin > forceReset(null) + stopFinish': true,  // 19824: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + stopBegin > forceReset(null) + done': true,  // 19825: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + stopFinish > forceReset(null) + onceFinish': true,  // 20712: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + stopFinish > forceReset(null) + done': true,  // 20725: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + done > forceReset(null) + onceFinish': true,  // 21612: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + onceBegin > forceReset(null) + onceFinish': true,  // 9012: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + onceBegin > forceReset(null) + loopBegin': true,  // 9019: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + onceBegin > forceReset(null) + loopFinish': true,  // 9020: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + onceBegin > forceReset(null) + newWordFragment': true,  // 9021: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + onceBegin > forceReset(null) + progress': true,  // 9022: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + onceBegin > forceReset(null) + stopBegin': true,  // 9023: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + onceBegin > forceReset(null) + stopFinish': true,  // 9024: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + onceBegin > forceReset(null) + done': true,  // 9025: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + loopBegin > forceReset(null) + onceFinish': true,  // 16212: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + loopBegin > forceReset(null) + loopFinish': true,  // 16220: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + loopBegin > forceReset(null) + newWordFragment': true,  // 16221: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + loopBegin > forceReset(null) + progress': true,  // 16222: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + loopFinish > forceReset(null) + onceFinish': true,  // 17112: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + newWordFragment > forceReset(null) + onceFinish': true,  // 18012: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + newWordFragment > forceReset(null) + loopFinish': true,  // 18020: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + newWordFragment > forceReset(null) + progress': true,  // 18022: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + progress > forceReset(null) + onceFinish': true,  // 18912: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + progress > forceReset(null) + loopFinish': true,  // 18920: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + stopBegin > forceReset(null) + onceFinish': true,  // 19812: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + stopBegin > forceReset(null) + stopFinish': true,  // 19824: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + stopBegin > forceReset(null) + done': true,  // 19825: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + stopFinish > forceReset(null) + onceFinish': true,  // 20712: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + stopFinish > forceReset(null) + done': true,  // 20725: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + done > forceReset(null) + onceFinish': true,  // 21612: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + onceBegin > forceReset(null) + onceFinish': true,  // 9012: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + onceBegin > forceReset(null) + loopBegin': true,  // 9019: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + onceBegin > forceReset(null) + loopFinish': true,  // 9020: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + onceBegin > forceReset(null) + newWordFragment': true,  // 9021: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + onceBegin > forceReset(null) + progress': true,  // 9022: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + loopBegin > forceReset(null) + onceFinish': true,  // 16212: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + loopBegin > forceReset(null) + loopFinish': true,  // 16220: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + loopBegin > forceReset(null) + newWordFragment': true,  // 16221: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + loopBegin > forceReset(null) + progress': true,  // 16222: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + loopFinish > forceReset(null) + onceFinish': true,  // 17112: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + newWordFragment > forceReset(null) + onceFinish': true,  // 18012: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + newWordFragment > forceReset(null) + loopFinish': true,  // 18020: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + newWordFragment > forceReset(null) + progress': true,  // 18022: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + progress > forceReset(null) + onceFinish': true,  // 18912: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + progress > forceReset(null) + loopFinish': true,  // 18920: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > forceReset(null) + onceFinish': true,  // 9012: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > forceReset(null) + loopBegin': true,  // 9019: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > forceReset(null) + loopFinish': true,  // 9020: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > forceReset(null) + newWordFragment': true,  // 9021: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > forceReset(null) + progress': true,  // 9022: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > forceReset(null) + onceFinish': true,  // 16212: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > forceReset(null) + loopFinish': true,  // 16220: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > forceReset(null) + newWordFragment': true,  // 16221: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > forceReset(null) + progress': true,  // 16222: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > forceReset(null) + onceFinish': true,  // 17112: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > forceReset(null) + onceFinish': true,  // 18012: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > forceReset(null) + loopFinish': true,  // 18020: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > forceReset(null) + progress': true,  // 18022: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > forceReset(null) + onceFinish': true,  // 18912: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > forceReset(null) + loopFinish': true,  // 18920: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > forceReset(null) + onceFinish': true,  // 9012: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > forceReset(null) + loopBegin': true,  // 9019: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > forceReset(null) + loopFinish': true,  // 9020: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > forceReset(null) + newWordFragment': true,  // 9021: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > forceReset(null) + progress': true,  // 9022: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > forceReset(null) + stopBegin': true,  // 9023: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > forceReset(null) + stopFinish': true,  // 9024: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > forceReset(null) + done': true,  // 9025: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > forceReset(null) + onceFinish': true,  // 16212: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > forceReset(null) + loopFinish': true,  // 16220: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > forceReset(null) + newWordFragment': true,  // 16221: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > forceReset(null) + progress': true,  // 16222: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > forceReset(null) + stopBegin': true,  // 16223: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > forceReset(null) + stopFinish': true,  // 16224: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > forceReset(null) + done': true,  // 16225: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > forceReset(null) + onceFinish': true,  // 17112: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > forceReset(null) + onceFinish': true,  // 18012: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > forceReset(null) + loopFinish': true,  // 18020: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > forceReset(null) + progress': true,  // 18022: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > forceReset(null) + onceFinish': true,  // 18912: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > forceReset(null) + loopFinish': true,  // 18920: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > forceReset(null) + onceFinish': true,  // 19812: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > forceReset(null) + stopFinish': true,  // 19824: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > forceReset(null) + done': true,  // 19825: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > forceReset(null) + onceFinish': true,  // 20712: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > forceReset(null) + done': true,  // 20725: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > forceReset(null) + onceFinish': true,  // 21612: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > forceReset(null) + onceFinish': true,  // 9012: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > forceReset(null) + loopBegin': true,  // 9019: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > forceReset(null) + loopFinish': true,  // 9020: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > forceReset(null) + newWordFragment': true,  // 9021: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > forceReset(null) + progress': true,  // 9022: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > forceReset(null) + stopBegin': true,  // 9023: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > forceReset(null) + stopFinish': true,  // 9024: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > forceReset(null) + done': true,  // 9025: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > forceReset(null) + onceFinish': true,  // 16212: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > forceReset(null) + loopFinish': true,  // 16220: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > forceReset(null) + newWordFragment': true,  // 16221: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > forceReset(null) + progress': true,  // 16222: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > forceReset(null) + stopBegin': true,  // 16223: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > forceReset(null) + stopFinish': true,  // 16224: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > forceReset(null) + done': true,  // 16225: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > forceReset(null) + onceFinish': true,  // 17112: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > forceReset(null) + onceFinish': true,  // 18012: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > forceReset(null) + loopFinish': true,  // 18020: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > forceReset(null) + progress': true,  // 18022: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > forceReset(null) + onceFinish': true,  // 18912: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > forceReset(null) + loopFinish': true,  // 18920: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > forceReset(null) + onceFinish': true,  // 19812: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > forceReset(null) + stopFinish': true,  // 19824: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > forceReset(null) + done': true,  // 19825: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > forceReset(null) + onceFinish': true,  // 20712: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > forceReset(null) + done': true,  // 20725: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > forceReset(null) + onceFinish': true,  // 21612: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + onceBegin > forceReset(null) + onceFinish': true,  // 9012: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + onceBegin > forceReset(null) + loopBegin': true,  // 9019: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + onceBegin > forceReset(null) + loopFinish': true,  // 9020: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + onceBegin > forceReset(null) + newWordFragment': true,  // 9021: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + onceBegin > forceReset(null) + progress': true,  // 9022: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + onceBegin > forceReset(null) + stopBegin': true,  // 9023: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + onceBegin > forceReset(null) + stopFinish': true,  // 9024: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + onceBegin > forceReset(null) + done': true,  // 9025: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + loopBegin > forceReset(null) + onceFinish': true,  // 16212: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + loopBegin > forceReset(null) + loopFinish': true,  // 16220: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + loopBegin > forceReset(null) + newWordFragment': true,  // 16221: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + loopBegin > forceReset(null) + progress': true,  // 16222: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + loopFinish > forceReset(null) + onceFinish': true,  // 17112: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + newWordFragment > forceReset(null) + onceFinish': true,  // 18012: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + newWordFragment > forceReset(null) + loopFinish': true,  // 18020: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + newWordFragment > forceReset(null) + progress': true,  // 18022: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + progress > forceReset(null) + onceFinish': true,  // 18912: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + progress > forceReset(null) + loopFinish': true,  // 18920: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + stopBegin > forceReset(null) + onceFinish': true,  // 19812: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + stopBegin > forceReset(null) + stopFinish': true,  // 19824: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + stopBegin > forceReset(null) + done': true,  // 19825: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + stopFinish > forceReset(null) + onceFinish': true,  // 20712: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + stopFinish > forceReset(null) + done': true,  // 20725: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + done > forceReset(null) + onceFinish': true,  // 21612: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + onceBegin > forceReset(null) + onceFinish': true,  // 9012: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + onceBegin > forceReset(null) + loopBegin': true,  // 9019: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + onceBegin > forceReset(null) + loopFinish': true,  // 9020: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + onceBegin > forceReset(null) + newWordFragment': true,  // 9021: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + onceBegin > forceReset(null) + progress': true,  // 9022: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + onceBegin > forceReset(null) + stopBegin': true,  // 9023: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + onceBegin > forceReset(null) + stopFinish': true,  // 9024: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + onceBegin > forceReset(null) + done': true,  // 9025: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + loopBegin > forceReset(null) + onceFinish': true,  // 16212: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + loopBegin > forceReset(null) + loopFinish': true,  // 16220: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + loopBegin > forceReset(null) + newWordFragment': true,  // 16221: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + loopBegin > forceReset(null) + progress': true,  // 16222: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + loopFinish > forceReset(null) + onceFinish': true,  // 17112: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + newWordFragment > forceReset(null) + onceFinish': true,  // 18012: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + newWordFragment > forceReset(null) + loopFinish': true,  // 18020: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + newWordFragment > forceReset(null) + progress': true,  // 18022: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + progress > forceReset(null) + onceFinish': true,  // 18912: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + progress > forceReset(null) + loopFinish': true,  // 18920: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + stopBegin > forceReset(null) + onceFinish': true,  // 19812: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + stopBegin > forceReset(null) + stopFinish': true,  // 19824: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + stopBegin > forceReset(null) + done': true,  // 19825: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + stopFinish > forceReset(null) + onceFinish': true,  // 20712: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + stopFinish > forceReset(null) + done': true,  // 20725: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + done > forceReset(null) + onceFinish': true,  // 21612: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + onceBegin > forceReset(null) + onceFinish': true,  // 9012: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + onceBegin > forceReset(null) + loopBegin': true,  // 9019: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + onceBegin > forceReset(null) + loopFinish': true,  // 9020: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + onceBegin > forceReset(null) + newWordFragment': true,  // 9021: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + onceBegin > forceReset(null) + progress': true,  // 9022: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + loopBegin > forceReset(null) + onceFinish': true,  // 16212: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + loopBegin > forceReset(null) + loopFinish': true,  // 16220: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + loopBegin > forceReset(null) + newWordFragment': true,  // 16221: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + loopBegin > forceReset(null) + progress': true,  // 16222: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + loopFinish > forceReset(null) + onceFinish': true,  // 17112: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + newWordFragment > forceReset(null) + onceFinish': true,  // 18012: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + newWordFragment > forceReset(null) + loopFinish': true,  // 18020: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + newWordFragment > forceReset(null) + progress': true,  // 18022: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + progress > forceReset(null) + onceFinish': true,  // 18912: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + progress > forceReset(null) + loopFinish': true,  // 18920: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > forceReset(null) + onceFinish': true,  // 9012: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > forceReset(null) + loopBegin': true,  // 9019: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > forceReset(null) + loopFinish': true,  // 9020: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > forceReset(null) + newWordFragment': true,  // 9021: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > forceReset(null) + progress': true,  // 9022: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > forceReset(null) + onceFinish': true,  // 16212: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > forceReset(null) + loopFinish': true,  // 16220: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > forceReset(null) + newWordFragment': true,  // 16221: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > forceReset(null) + progress': true,  // 16222: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > forceReset(null) + onceFinish': true,  // 17112: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > forceReset(null) + onceFinish': true,  // 18012: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > forceReset(null) + loopFinish': true,  // 18020: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > forceReset(null) + progress': true,  // 18022: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > forceReset(null) + onceFinish': true,  // 18912: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > forceReset(null) + loopFinish': true,  // 18920: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > forceReset(null) + onceFinish': true,  // 9012: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > forceReset(null) + loopBegin': true,  // 9019: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > forceReset(null) + loopFinish': true,  // 9020: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > forceReset(null) + newWordFragment': true,  // 9021: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > forceReset(null) + progress': true,  // 9022: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > forceReset(null) + stopBegin': true,  // 9023: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > forceReset(null) + stopFinish': true,  // 9024: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > forceReset(null) + done': true,  // 9025: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > forceReset(null) + onceFinish': true,  // 16212: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > forceReset(null) + loopFinish': true,  // 16220: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > forceReset(null) + newWordFragment': true,  // 16221: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > forceReset(null) + progress': true,  // 16222: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > forceReset(null) + stopBegin': true,  // 16223: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > forceReset(null) + stopFinish': true,  // 16224: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > forceReset(null) + done': true,  // 16225: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > forceReset(null) + onceFinish': true,  // 17112: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > forceReset(null) + onceFinish': true,  // 18012: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > forceReset(null) + loopFinish': true,  // 18020: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > forceReset(null) + progress': true,  // 18022: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > forceReset(null) + onceFinish': true,  // 18912: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > forceReset(null) + loopFinish': true,  // 18920: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > forceReset(null) + onceFinish': true,  // 19812: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > forceReset(null) + stopFinish': true,  // 19824: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > forceReset(null) + done': true,  // 19825: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > forceReset(null) + onceFinish': true,  // 20712: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > forceReset(null) + done': true,  // 20725: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > forceReset(null) + onceFinish': true,  // 21612: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > forceReset(null) + onceFinish': true,  // 9012: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > forceReset(null) + loopBegin': true,  // 9019: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > forceReset(null) + loopFinish': true,  // 9020: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > forceReset(null) + newWordFragment': true,  // 9021: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > forceReset(null) + progress': true,  // 9022: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > forceReset(null) + stopBegin': true,  // 9023: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > forceReset(null) + stopFinish': true,  // 9024: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > forceReset(null) + done': true,  // 9025: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > forceReset(null) + onceFinish': true,  // 16212: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > forceReset(null) + loopFinish': true,  // 16220: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > forceReset(null) + newWordFragment': true,  // 16221: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > forceReset(null) + progress': true,  // 16222: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > forceReset(null) + stopBegin': true,  // 16223: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > forceReset(null) + stopFinish': true,  // 16224: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > forceReset(null) + done': true,  // 16225: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > forceReset(null) + onceFinish': true,  // 17112: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > forceReset(null) + onceFinish': true,  // 18012: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > forceReset(null) + loopFinish': true,  // 18020: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > forceReset(null) + progress': true,  // 18022: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > forceReset(null) + onceFinish': true,  // 18912: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > forceReset(null) + loopFinish': true,  // 18920: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > forceReset(null) + onceFinish': true,  // 19812: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > forceReset(null) + stopFinish': true,  // 19824: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > forceReset(null) + done': true,  // 19825: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > forceReset(null) + onceFinish': true,  // 20712: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > forceReset(null) + done': true,  // 20725: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > forceReset(null) + onceFinish': true,  // 21612: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + onceBegin > forceReset(null) + onceFinish': true,  // 9012: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + onceBegin > forceReset(null) + loopBegin': true,  // 9019: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + onceBegin > forceReset(null) + loopFinish': true,  // 9020: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + onceBegin > forceReset(null) + newWordFragment': true,  // 9021: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + onceBegin > forceReset(null) + progress': true,  // 9022: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + onceBegin > forceReset(null) + stopBegin': true,  // 9023: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + onceBegin > forceReset(null) + stopFinish': true,  // 9024: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + onceBegin > forceReset(null) + done': true,  // 9025: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + loopBegin > forceReset(null) + onceFinish': true,  // 16212: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + loopBegin > forceReset(null) + loopFinish': true,  // 16220: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + loopBegin > forceReset(null) + newWordFragment': true,  // 16221: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + loopBegin > forceReset(null) + progress': true,  // 16222: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + loopFinish > forceReset(null) + onceFinish': true,  // 17112: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + newWordFragment > forceReset(null) + onceFinish': true,  // 18012: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + newWordFragment > forceReset(null) + loopFinish': true,  // 18020: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + newWordFragment > forceReset(null) + progress': true,  // 18022: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + progress > forceReset(null) + onceFinish': true,  // 18912: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + progress > forceReset(null) + loopFinish': true,  // 18920: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + stopBegin > forceReset(null) + onceFinish': true,  // 19812: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + stopBegin > forceReset(null) + stopFinish': true,  // 19824: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + stopBegin > forceReset(null) + done': true,  // 19825: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + stopFinish > forceReset(null) + onceFinish': true,  // 20712: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + stopFinish > forceReset(null) + done': true,  // 20725: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + done > forceReset(null) + onceFinish': true,  // 21612: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + onceBegin > forceReset(null) + onceFinish': true,  // 9012: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + onceBegin > forceReset(null) + loopBegin': true,  // 9019: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + onceBegin > forceReset(null) + loopFinish': true,  // 9020: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + onceBegin > forceReset(null) + newWordFragment': true,  // 9021: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + onceBegin > forceReset(null) + progress': true,  // 9022: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + onceBegin > forceReset(null) + stopBegin': true,  // 9023: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + onceBegin > forceReset(null) + stopFinish': true,  // 9024: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + onceBegin > forceReset(null) + done': true,  // 9025: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + loopBegin > forceReset(null) + onceFinish': true,  // 16212: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + loopBegin > forceReset(null) + loopFinish': true,  // 16220: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + loopBegin > forceReset(null) + newWordFragment': true,  // 16221: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + loopBegin > forceReset(null) + progress': true,  // 16222: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + loopFinish > forceReset(null) + onceFinish': true,  // 17112: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + newWordFragment > forceReset(null) + onceFinish': true,  // 18012: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + newWordFragment > forceReset(null) + loopFinish': true,  // 18020: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + newWordFragment > forceReset(null) + progress': true,  // 18022: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + progress > forceReset(null) + onceFinish': true,  // 18912: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + progress > forceReset(null) + loopFinish': true,  // 18920: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + stopBegin > forceReset(null) + onceFinish': true,  // 19812: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + stopBegin > forceReset(null) + stopFinish': true,  // 19824: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + stopBegin > forceReset(null) + done': true,  // 19825: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + stopFinish > forceReset(null) + onceFinish': true,  // 20712: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + stopFinish > forceReset(null) + done': true,  // 20725: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + done > forceReset(null) + onceFinish': true,  // 21612: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + onceBegin > forceReset(null) + onceFinish': true,  // 9012: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + onceBegin > forceReset(null) + loopBegin': true,  // 9019: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + onceBegin > forceReset(null) + loopFinish': true,  // 9020: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + onceBegin > forceReset(null) + newWordFragment': true,  // 9021: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + onceBegin > forceReset(null) + progress': true,  // 9022: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + loopBegin > forceReset(null) + onceFinish': true,  // 16212: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + loopBegin > forceReset(null) + loopFinish': true,  // 16220: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + loopBegin > forceReset(null) + newWordFragment': true,  // 16221: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + loopBegin > forceReset(null) + progress': true,  // 16222: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + loopFinish > forceReset(null) + onceFinish': true,  // 17112: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + newWordFragment > forceReset(null) + onceFinish': true,  // 18012: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + newWordFragment > forceReset(null) + loopFinish': true,  // 18020: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + newWordFragment > forceReset(null) + progress': true,  // 18022: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + progress > forceReset(null) + onceFinish': true,  // 18912: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + progress > forceReset(null) + loopFinish': true,  // 18920: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > forceReset(null) + onceFinish': true,  // 9012: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > forceReset(null) + loopBegin': true,  // 9019: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > forceReset(null) + loopFinish': true,  // 9020: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > forceReset(null) + newWordFragment': true,  // 9021: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > forceReset(null) + progress': true,  // 9022: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > forceReset(null) + onceFinish': true,  // 16212: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > forceReset(null) + loopFinish': true,  // 16220: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > forceReset(null) + newWordFragment': true,  // 16221: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > forceReset(null) + progress': true,  // 16222: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > forceReset(null) + onceFinish': true,  // 17112: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > forceReset(null) + onceFinish': true,  // 18012: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > forceReset(null) + loopFinish': true,  // 18020: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > forceReset(null) + progress': true,  // 18022: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > forceReset(null) + onceFinish': true,  // 18912: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > forceReset(null) + loopFinish': true,  // 18920: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > forceReset(null) + onceFinish': true,  // 9012: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > forceReset(null) + loopBegin': true,  // 9019: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > forceReset(null) + loopFinish': true,  // 9020: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > forceReset(null) + newWordFragment': true,  // 9021: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > forceReset(null) + progress': true,  // 9022: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > forceReset(null) + onceFinish': true,  // 16212: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > forceReset(null) + loopFinish': true,  // 16220: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > forceReset(null) + newWordFragment': true,  // 16221: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > forceReset(null) + progress': true,  // 16222: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > forceReset(null) + onceFinish': true,  // 17112: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > forceReset(null) + onceFinish': true,  // 18012: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > forceReset(null) + loopFinish': true,  // 18020: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > forceReset(null) + progress': true,  // 18022: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > forceReset(null) + onceFinish': true,  // 18912: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > forceReset(null) + loopFinish': true,  // 18920: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > forceReset(null) + onceFinish': true,  // 9012: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > forceReset(null) + loopBegin': true,  // 9019: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > forceReset(null) + loopFinish': true,  // 9020: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > forceReset(null) + newWordFragment': true,  // 9021: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > forceReset(null) + progress': true,  // 9022: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > forceReset(null) + stopBegin': true,  // 9023: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > forceReset(null) + stopFinish': true,  // 9024: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > forceReset(null) + done': true,  // 9025: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > forceReset(null) + onceFinish': true,  // 16212: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > forceReset(null) + loopFinish': true,  // 16220: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > forceReset(null) + newWordFragment': true,  // 16221: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > forceReset(null) + progress': true,  // 16222: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > forceReset(null) + stopBegin': true,  // 16223: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > forceReset(null) + stopFinish': true,  // 16224: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > forceReset(null) + done': true,  // 16225: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > forceReset(null) + onceFinish': true,  // 17112: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > forceReset(null) + onceFinish': true,  // 18012: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > forceReset(null) + loopFinish': true,  // 18020: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > forceReset(null) + progress': true,  // 18022: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > forceReset(null) + onceFinish': true,  // 18912: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > forceReset(null) + loopFinish': true,  // 18920: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > forceReset(null) + onceFinish': true,  // 19812: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > forceReset(null) + stopFinish': true,  // 19824: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > forceReset(null) + done': true,  // 19825: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > forceReset(null) + onceFinish': true,  // 20712: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > forceReset(null) + done': true,  // 20725: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > forceReset(null) + onceFinish': true,  // 21612: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceBegin > forceReset(null) + onceFinish': true,  // 9012: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceBegin > forceReset(null) + loopBegin': true,  // 9019: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceBegin > forceReset(null) + loopFinish': true,  // 9020: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceBegin > forceReset(null) + newWordFragment': true,  // 9021: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceBegin > forceReset(null) + progress': true,  // 9022: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopBegin > forceReset(null) + onceFinish': true,  // 16212: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopBegin > forceReset(null) + loopFinish': true,  // 16220: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopBegin > forceReset(null) + newWordFragment': true,  // 16221: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopBegin > forceReset(null) + progress': true,  // 16222: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopFinish > forceReset(null) + onceFinish': true,  // 17112: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + newWordFragment > forceReset(null) + onceFinish': true,  // 18012: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + newWordFragment > forceReset(null) + loopFinish': true,  // 18020: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + newWordFragment > forceReset(null) + progress': true,  // 18022: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + progress > forceReset(null) + onceFinish': true,  // 18912: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + progress > forceReset(null) + loopFinish': true,  // 18920: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > forceReset(null) + onceFinish': true,  // 9012: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > forceReset(null) + loopBegin': true,  // 9019: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > forceReset(null) + loopFinish': true,  // 9020: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > forceReset(null) + newWordFragment': true,  // 9021: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > forceReset(null) + progress': true,  // 9022: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > forceReset(null) + onceFinish': true,  // 16212: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > forceReset(null) + loopFinish': true,  // 16220: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > forceReset(null) + newWordFragment': true,  // 16221: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > forceReset(null) + progress': true,  // 16222: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > forceReset(null) + onceFinish': true,  // 17112: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > forceReset(null) + onceFinish': true,  // 18012: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > forceReset(null) + loopFinish': true,  // 18020: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > forceReset(null) + progress': true,  // 18022: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > forceReset(null) + onceFinish': true,  // 18912: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > forceReset(null) + loopFinish': true,  // 18920: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + onceBegin > forceReset(null) + onceFinish': true,  // 9012: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + onceBegin > forceReset(null) + loopBegin': true,  // 9019: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + onceBegin > forceReset(null) + loopFinish': true,  // 9020: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + onceBegin > forceReset(null) + newWordFragment': true,  // 9021: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + onceBegin > forceReset(null) + progress': true,  // 9022: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + onceBegin > forceReset(null) + stopBegin': true,  // 9023: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + onceBegin > forceReset(null) + stopFinish': true,  // 9024: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + onceBegin > forceReset(null) + done': true,  // 9025: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + loopBegin > forceReset(null) + onceFinish': true,  // 16212: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + loopBegin > forceReset(null) + loopFinish': true,  // 16220: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + loopBegin > forceReset(null) + newWordFragment': true,  // 16221: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + loopBegin > forceReset(null) + progress': true,  // 16222: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + loopFinish > forceReset(null) + onceFinish': true,  // 17112: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + newWordFragment > forceReset(null) + onceFinish': true,  // 18012: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + newWordFragment > forceReset(null) + loopFinish': true,  // 18020: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + newWordFragment > forceReset(null) + progress': true,  // 18022: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + progress > forceReset(null) + onceFinish': true,  // 18912: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + progress > forceReset(null) + loopFinish': true,  // 18920: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + stopBegin > forceReset(null) + onceFinish': true,  // 19812: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + stopBegin > forceReset(null) + stopFinish': true,  // 19824: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + stopBegin > forceReset(null) + done': true,  // 19825: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + stopFinish > forceReset(null) + onceFinish': true,  // 20712: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + stopFinish > forceReset(null) + done': true,  // 20725: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + done > forceReset(null) + onceFinish': true,  // 21612: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + onceBegin > forceReset(null) + onceFinish': true,  // 9012: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + onceBegin > forceReset(null) + loopBegin': true,  // 9019: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + onceBegin > forceReset(null) + loopFinish': true,  // 9020: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + onceBegin > forceReset(null) + newWordFragment': true,  // 9021: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + onceBegin > forceReset(null) + progress': true,  // 9022: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + onceBegin > forceReset(null) + stopBegin': true,  // 9023: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + onceBegin > forceReset(null) + stopFinish': true,  // 9024: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + onceBegin > forceReset(null) + done': true,  // 9025: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + loopBegin > forceReset(null) + onceFinish': true,  // 16212: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + loopBegin > forceReset(null) + loopFinish': true,  // 16220: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + loopBegin > forceReset(null) + newWordFragment': true,  // 16221: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + loopBegin > forceReset(null) + progress': true,  // 16222: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + loopFinish > forceReset(null) + onceFinish': true,  // 17112: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + newWordFragment > forceReset(null) + onceFinish': true,  // 18012: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + newWordFragment > forceReset(null) + loopFinish': true,  // 18020: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + newWordFragment > forceReset(null) + progress': true,  // 18022: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + progress > forceReset(null) + onceFinish': true,  // 18912: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + progress > forceReset(null) + loopFinish': true,  // 18920: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + stopBegin > forceReset(null) + onceFinish': true,  // 19812: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + stopBegin > forceReset(null) + stopFinish': true,  // 19824: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + stopBegin > forceReset(null) + done': true,  // 19825: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + stopFinish > forceReset(null) + onceFinish': true,  // 20712: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + stopFinish > forceReset(null) + done': true,  // 20725: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + done > forceReset(null) + onceFinish': true,  // 21612: ?? (skip results are different than combos)



		// ============== reset ==============
		// #1
		'skip-combos: reset(null) + resetBegin > reset(null) + resetBegin': true,  // 28: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > reset(null) + resetFinish': true,  // 29: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > restart(null) + restartBegin': true,  // 55: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > restart(null) + restartFinish': true,  // 56: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > restart(null) + loopBegin': true,  // 69: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > restart(null) + loopFinish': true,  // 70: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > restart(null) + newWordFragment': true,  // 71: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > restart(null) + progress': true,  // 72: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > restart(null) + stopBegin': true,  // 73: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > restart(null) + stopFinish': true,  // 74: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > restart(null) + done': true,  // 75: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > play(null) + playBegin': true,  // 76: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > play(null) + playFinish': true,  // 77: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > play(null) + loopBegin': true,  // 94: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > play(null) + loopFinish': true,  // 95: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > play(null) + newWordFragment': true,  // 96: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > play(null) + progress': true,  // 97: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > play(null) + stopBegin': true,  // 98: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > play(null) + stopFinish': true,  // 99: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > play(null) + done': true,  // 100: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > toggle(null) + playBegin': true,  // 101: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > toggle(null) + playFinish': true,  // 102: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > toggle(null) + loopBegin': true,  // 119: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > toggle(null) + loopFinish': true,  // 120: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > toggle(null) + newWordFragment': true,  // 121: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > toggle(null) + progress': true,  // 122: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > toggle(null) + stopBegin': true,  // 123: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > toggle(null) + stopFinish': true,  // 124: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > toggle(null) + done': true,  // 125: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > pause(null) + pauseBegin': true,  // 132: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > pause(null) + pauseFinish': true,  // 133: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > stop(null) + stopBegin': true,  // 173: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > stop(null) + stopFinish': true,  // 174: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > close(null) + closeBegin': true,  // 184: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > close(null) + closeFinish': true,  // 185: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > revert(null) + pauseBegin': true,  // 207: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > revert(null) + pauseFinish': true,  // 208: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > revert(null) + revertBegin': true,  // 213: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > revert(null) + revertFinish': true,  // 214: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > rewind(null) + rewindBegin': true,  // 240: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > rewind(null) + rewindFinish': true,  // 241: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > rewind(null) + loopBegin': true,  // 244: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > rewind(null) + loopFinish': true,  // 245: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > rewind(null) + newWordFragment': true,  // 246: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > rewind(null) + progress': true,  // 247: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > rewind(null) + stopBegin': true,  // 248: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > rewind(null) + stopFinish': true,  // 249: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > rewind(null) + done': true,  // 250: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > fastForward(null) + fastForwardBegin': true,  // 267: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > fastForward(null) + fastForwardFinish': true,  // 268: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > fastForward(null) + loopBegin': true,  // 269: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > fastForward(null) + loopFinish': true,  // 270: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > fastForward(null) + newWordFragment': true,  // 271: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > fastForward(null) + progress': true,  // 272: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > fastForward(null) + stopBegin': true,  // 273: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > fastForward(null) + stopFinish': true,  // 274: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > fastForward(null) + done': true,  // 275: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > once([0,0,-2]) + onceBegin': false,  // 286: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > once([0,0,-2]) + onceFinish': false,  // 287: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > once([0,0,-2]) + loopBegin': false,  // 294: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > once([0,0,-2]) + loopFinish': false,  // 295: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > once([0,0,-2]) + newWordFragment': false,  // 296: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > once([0,0,-2]) + progress': false,  // 297: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > once([0,0,-2]) + stopBegin': false,  // 298: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > once([0,0,-2]) + stopFinish': false,  // 299: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > once([0,0,-2]) + done': false,  // 300: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > once([0,0,2]) + onceBegin': false,  // 311: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > once([0,0,2]) + onceFinish': false,  // 312: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > once([0,0,2]) + loopBegin': false,  // 319: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > once([0,0,2]) + loopFinish': false,  // 320: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > once([0,0,2]) + newWordFragment': false,  // 321: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > once([0,0,2]) + progress': false,  // 322: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > current(null) + onceBegin': true,  // 336: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > current(null) + onceFinish': true,  // 337: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > current(null) + loopBegin': true,  // 344: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > current(null) + loopFinish': true,  // 345: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > current(null) + newWordFragment': true,  // 346: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > current(null) + progress': true,  // 347: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(-3) + onceBegin': true,  // 361: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(-3) + onceFinish': true,  // 362: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(-3) + loopBegin': true,  // 369: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(-3) + loopFinish': true,  // 370: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(-3) + newWordFragment': true,  // 371: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(-3) + progress': true,  // 372: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(-3) + stopBegin': true,  // 373: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(-3) + stopFinish': true,  // 374: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(-3) + done': true,  // 375: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(-1) + onceBegin': true,  // 386: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(-1) + onceFinish': true,  // 387: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(-1) + loopBegin': true,  // 394: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(-1) + loopFinish': true,  // 395: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(-1) + newWordFragment': true,  // 396: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(-1) + progress': true,  // 397: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(-1) + stopBegin': true,  // 398: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(-1) + stopFinish': true,  // 399: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(-1) + done': true,  // 400: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(0) + onceBegin': true,  // 411: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(0) + onceFinish': true,  // 412: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(0) + loopBegin': true,  // 419: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(0) + loopFinish': true,  // 420: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(0) + newWordFragment': true,  // 421: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(0) + progress': true,  // 422: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(6) + onceBegin': true,  // 436: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(6) + onceFinish': true,  // 437: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(6) + loopBegin': true,  // 444: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(6) + loopFinish': true,  // 445: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(6) + newWordFragment': true,  // 446: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(6) + progress': true,  // 447: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(11) + onceBegin': true,  // 461: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(11) + onceFinish': true,  // 462: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(11) + loopBegin': true,  // 469: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(11) + loopFinish': true,  // 470: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(11) + newWordFragment': true,  // 471: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(11) + progress': true,  // 472: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(11) + stopBegin': true,  // 473: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(11) + stopFinish': true,  // 474: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(11) + done': true,  // 475: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(100) + onceBegin': true,  // 486: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(100) + onceFinish': true,  // 487: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(100) + loopBegin': true,  // 494: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(100) + loopFinish': true,  // 495: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(100) + newWordFragment': true,  // 496: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(100) + progress': true,  // 497: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(100) + stopBegin': true,  // 498: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(100) + stopFinish': true,  // 499: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpTo(100) + done': true,  // 500: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(-3) + onceBegin': true,  // 511: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(-3) + onceFinish': true,  // 512: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(-3) + loopBegin': true,  // 519: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(-3) + loopFinish': true,  // 520: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(-3) + newWordFragment': true,  // 521: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(-3) + progress': true,  // 522: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(-3) + stopBegin': true,  // 523: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(-3) + stopFinish': true,  // 524: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(-3) + done': true,  // 525: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(-1) + onceBegin': true,  // 536: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(-1) + onceFinish': true,  // 537: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(-1) + loopBegin': true,  // 544: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(-1) + loopFinish': true,  // 545: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(-1) + newWordFragment': true,  // 546: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(-1) + progress': true,  // 547: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(-1) + stopBegin': true,  // 548: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(-1) + stopFinish': true,  // 549: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(-1) + done': true,  // 550: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(0) + onceBegin': true,  // 561: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(0) + onceFinish': true,  // 562: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(0) + loopBegin': true,  // 569: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(0) + loopFinish': true,  // 570: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(0) + newWordFragment': true,  // 571: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(0) + progress': true,  // 572: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(4) + onceBegin': true,  // 586: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(4) + onceFinish': true,  // 587: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(4) + loopBegin': true,  // 594: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(4) + loopFinish': true,  // 595: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(4) + newWordFragment': true,  // 596: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(4) + progress': true,  // 597: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(11) + onceBegin': true,  // 611: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(11) + onceFinish': true,  // 612: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(11) + loopBegin': true,  // 619: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(11) + loopFinish': true,  // 620: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(11) + newWordFragment': true,  // 621: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(11) + progress': true,  // 622: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(11) + stopBegin': true,  // 623: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(11) + stopFinish': true,  // 624: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(11) + done': true,  // 625: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(100) + onceBegin': true,  // 636: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(100) + onceFinish': true,  // 637: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(100) + loopBegin': true,  // 644: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(100) + loopFinish': true,  // 645: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(100) + newWordFragment': true,  // 646: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(100) + progress': true,  // 647: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(100) + stopBegin': true,  // 648: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(100) + stopFinish': true,  // 649: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpWords(100) + done': true,  // 650: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(-3) + onceBegin': true,  // 661: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(-3) + onceFinish': true,  // 662: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(-3) + loopBegin': true,  // 669: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(-3) + loopFinish': true,  // 670: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(-3) + newWordFragment': true,  // 671: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(-3) + progress': true,  // 672: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(-3) + stopBegin': true,  // 673: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(-3) + stopFinish': true,  // 674: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(-3) + done': true,  // 675: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(-1) + onceBegin': true,  // 686: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(-1) + onceFinish': true,  // 687: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(-1) + loopBegin': true,  // 694: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(-1) + loopFinish': true,  // 695: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(-1) + newWordFragment': true,  // 696: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(-1) + progress': true,  // 697: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(-1) + stopBegin': true,  // 698: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(-1) + stopFinish': true,  // 699: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(-1) + done': true,  // 700: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(0) + onceBegin': true,  // 711: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(0) + onceFinish': true,  // 712: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(0) + loopBegin': true,  // 719: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(0) + loopFinish': true,  // 720: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(0) + newWordFragment': true,  // 721: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(0) + progress': true,  // 722: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(1) + onceBegin': true,  // 736: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(1) + onceFinish': true,  // 737: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(1) + loopBegin': true,  // 744: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(1) + loopFinish': true,  // 745: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(1) + newWordFragment': true,  // 746: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(1) + progress': true,  // 747: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(3) + onceBegin': true,  // 761: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(3) + onceFinish': true,  // 762: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(3) + loopBegin': true,  // 769: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(3) + loopFinish': true,  // 770: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(3) + newWordFragment': true,  // 771: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(3) + progress': true,  // 772: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(100) + onceBegin': true,  // 786: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(100) + onceFinish': true,  // 787: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(100) + loopBegin': true,  // 794: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(100) + loopFinish': true,  // 795: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(100) + newWordFragment': true,  // 796: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(100) + progress': true,  // 797: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(100) + stopBegin': true,  // 798: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(100) + stopFinish': true,  // 799: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > jumpSentences(100) + done': true,  // 800: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > nextWord(null) + onceBegin': true,  // 811: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > nextWord(null) + onceFinish': true,  // 812: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > nextWord(null) + loopBegin': true,  // 819: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > nextWord(null) + loopFinish': true,  // 820: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > nextWord(null) + newWordFragment': true,  // 821: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > nextWord(null) + progress': true,  // 822: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > nextSentence(null) + onceBegin': true,  // 836: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > nextSentence(null) + onceFinish': true,  // 837: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > nextSentence(null) + loopBegin': true,  // 844: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > nextSentence(null) + loopFinish': true,  // 845: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > nextSentence(null) + newWordFragment': true,  // 846: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > nextSentence(null) + progress': true,  // 847: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > prevWord(null) + onceBegin': true,  // 861: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > prevWord(null) + onceFinish': true,  // 862: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > prevWord(null) + loopBegin': true,  // 869: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > prevWord(null) + loopFinish': true,  // 870: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > prevWord(null) + newWordFragment': true,  // 871: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > prevWord(null) + progress': true,  // 872: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > prevWord(null) + stopBegin': true,  // 873: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > prevWord(null) + stopFinish': true,  // 874: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > prevWord(null) + done': true,  // 875: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > prevSentence(null) + onceBegin': true,  // 886: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > prevSentence(null) + onceFinish': true,  // 887: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > prevSentence(null) + loopBegin': true,  // 894: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > prevSentence(null) + loopFinish': true,  // 895: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > prevSentence(null) + newWordFragment': true,  // 896: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > prevSentence(null) + progress': true,  // 897: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > prevSentence(null) + stopBegin': true,  // 898: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > prevSentence(null) + stopFinish': true,  // 899: ?? (skip results are different than combos)
		'skip-combos: reset(null) + resetBegin > prevSentence(null) + done': true,  // 900: ?? (skip results are different than combos)



		// ============== restart ==============
		// #? just like play?
		'skip-combos: restart(null) + restartBegin > play(null) + progress': false,  // 1897: ?? (skip results are different than combos)
		'skip-combos: restart(null) + restartBegin > toggle(null) + playBegin': true,  // 1901: ?? (skip results are different than combos)
		'skip-combos: restart(null) + restartBegin > toggle(null) + playFinish': true,  // 1902: ?? (skip results are different than combos)
		'skip-combos: restart(null) + restartBegin > toggle(null) + pauseBegin': true,  // 1907: ?? (skip results are different than combos)
		'skip-combos: restart(null) + restartBegin > toggle(null) + pauseFinish': true,  // 1908: ?? (skip results are different than combos)
		'skip-combos: restart(null) + restartBegin > toggle(null) + loopBegin': true,  // 1919: ?? (skip results are different than combos)
		'skip-combos: restart(null) + restartBegin > toggle(null) + loopFinish': true,  // 1920: ?? (skip results are different than combos)
		'skip-combos: restart(null) + restartBegin > toggle(null) + newWordFragment': true,  // 1921: ?? (skip results are different than combos)
		'skip-combos: restart(null) + restartBegin > toggle(null) + progress': true,  // 1922: ?? (skip results are different than combos)
		'skip-combos: restart(null) + restartBegin > toggle(null) + stopBegin': true,  // 1923: ?? (skip results are different than combos)
		'skip-combos: restart(null) + restartBegin > toggle(null) + stopFinish': true,  // 1924: ?? (skip results are different than combos)
		'skip-combos: restart(null) + restartBegin > toggle(null) + done': true,  // 1925: ?? (skip results are different than combos)
		'skip-combos: restart(null) + restartBegin > revert(null) + playBegin': false,  // 2001: ?? (skip results are different than combos)
		'skip-combos: restart(null) + restartBegin > revert(null) + playFinish': false,  // 2002: ?? (skip results are different than combos)
		'skip-combos: restart(null) + restartBegin > revert(null) + pauseBegin': false,  // 2007: ?? (skip results are different than combos)
		'skip-combos: restart(null) + restartBegin > revert(null) + pauseFinish': false,  // 2008: ?? (skip results are different than combos)
		'skip-combos: restart(null) + restartFinish > play(null) + progress': false,  // 2797: ?? (skip results are different than combos)
		'skip-combos: restart(null) + restartFinish > toggle(null) + playBegin': true,  // 2801: ?? (skip results are different than combos)
		'skip-combos: restart(null) + restartFinish > toggle(null) + playFinish': true,  // 2802: ?? (skip results are different than combos)
		'skip-combos: restart(null) + restartFinish > toggle(null) + pauseBegin': true,  // 2807: ?? (skip results are different than combos)
		'skip-combos: restart(null) + restartFinish > toggle(null) + pauseFinish': true,  // 2808: ?? (skip results are different than combos)
		'skip-combos: restart(null) + restartFinish > toggle(null) + loopBegin': true,  // 2819: ?? (skip results are different than combos)
		'skip-combos: restart(null) + restartFinish > toggle(null) + loopFinish': true,  // 2820: ?? (skip results are different than combos)
		'skip-combos: restart(null) + restartFinish > toggle(null) + newWordFragment': true,  // 2821: ?? (skip results are different than combos)
		'skip-combos: restart(null) + restartFinish > toggle(null) + progress': true,  // 2822: ?? (skip results are different than combos)
		'skip-combos: restart(null) + restartFinish > toggle(null) + stopBegin': true,  // 2823: ?? (skip results are different than combos)
		'skip-combos: restart(null) + restartFinish > toggle(null) + stopFinish': true,  // 2824: ?? (skip results are different than combos)
		'skip-combos: restart(null) + restartFinish > toggle(null) + done': true,  // 2825: ?? (skip results are different than combos)
		'skip-combos: restart(null) + restartFinish > revert(null) + playBegin': false,  // 2901: ?? (skip results are different than combos)
		'skip-combos: restart(null) + restartFinish > revert(null) + playFinish': false,  // 2902: ?? (skip results are different than combos)
		'skip-combos: restart(null) + restartFinish > revert(null) + pauseBegin': false,  // 2907: ?? (skip results are different than combos)
		'skip-combos: restart(null) + restartFinish > revert(null) + pauseFinish': false,  // 2908: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopBegin > play(null) + progress': false,  // 16297: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopBegin > toggle(null) + playBegin': true,  // 16301: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopBegin > toggle(null) + playFinish': true,  // 16302: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopBegin > toggle(null) + pauseBegin': true,  // 16307: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopBegin > toggle(null) + pauseFinish': true,  // 16308: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopBegin > toggle(null) + loopBegin': true,  // 16319: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopBegin > toggle(null) + loopFinish': true,  // 16320: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopBegin > toggle(null) + newWordFragment': true,  // 16321: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopBegin > toggle(null) + progress': true,  // 16322: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopBegin > toggle(null) + stopBegin': true,  // 16323: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopBegin > toggle(null) + stopFinish': true,  // 16324: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopBegin > toggle(null) + done': true,  // 16325: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopBegin > revert(null) + playBegin': false,  // 16401: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopBegin > revert(null) + playFinish': false,  // 16402: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopBegin > revert(null) + pauseBegin': false,  // 16407: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopBegin > revert(null) + pauseFinish': false,  // 16408: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopFinish > play(null) + progress': false,  // 17197: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopFinish > toggle(null) + playBegin': true,  // 17201: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopFinish > toggle(null) + playFinish': true,  // 17202: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopFinish > toggle(null) + pauseBegin': true,  // 17207: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopFinish > toggle(null) + pauseFinish': true,  // 17208: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopFinish > toggle(null) + loopBegin': true,  // 17219: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopFinish > toggle(null) + loopFinish': true,  // 17220: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopFinish > toggle(null) + newWordFragment': true,  // 17221: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopFinish > toggle(null) + progress': true,  // 17222: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopFinish > toggle(null) + stopBegin': true,  // 17223: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopFinish > toggle(null) + stopFinish': true,  // 17224: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopFinish > toggle(null) + done': true,  // 17225: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopFinish > revert(null) + playBegin': false,  // 17301: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopFinish > revert(null) + playFinish': false,  // 17302: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopFinish > revert(null) + pauseBegin': false,  // 17307: ?? (skip results are different than combos)
		'skip-combos: restart(null) + loopFinish > revert(null) + pauseFinish': false,  // 17308: ?? (skip results are different than combos)
		'skip-combos: restart(null) + newWordFragment > play(null) + newWordFragment': false,  // 18096: ?? (skip results are different than combos)
		'skip-combos: restart(null) + newWordFragment > play(null) + progress': false,  // 18097: ?? (skip results are different than combos)
		'skip-combos: restart(null) + newWordFragment > toggle(null) + playBegin': true,  // 18101: ?? (skip results are different than combos)
		'skip-combos: restart(null) + newWordFragment > toggle(null) + playFinish': true,  // 18102: ?? (skip results are different than combos)
		'skip-combos: restart(null) + newWordFragment > toggle(null) + pauseBegin': true,  // 18107: ?? (skip results are different than combos)
		'skip-combos: restart(null) + newWordFragment > toggle(null) + pauseFinish': true,  // 18108: ?? (skip results are different than combos)
		'skip-combos: restart(null) + newWordFragment > toggle(null) + loopBegin': true,  // 18119: ?? (skip results are different than combos)
		'skip-combos: restart(null) + newWordFragment > toggle(null) + loopFinish': true,  // 18120: ?? (skip results are different than combos)
		'skip-combos: restart(null) + newWordFragment > toggle(null) + newWordFragment': true,  // 18121: ?? (skip results are different than combos)
		'skip-combos: restart(null) + newWordFragment > toggle(null) + progress': true,  // 18122: ?? (skip results are different than combos)
		'skip-combos: restart(null) + newWordFragment > toggle(null) + stopBegin': true,  // 18123: ?? (skip results are different than combos)
		'skip-combos: restart(null) + newWordFragment > toggle(null) + stopFinish': true,  // 18124: ?? (skip results are different than combos)
		'skip-combos: restart(null) + newWordFragment > toggle(null) + done': true,  // 18125: ?? (skip results are different than combos)
		'skip-combos: restart(null) + newWordFragment > revert(null) + playBegin': false,  // 18201: ?? (skip results are different than combos)
		'skip-combos: restart(null) + newWordFragment > revert(null) + playFinish': false,  // 18202: ?? (skip results are different than combos)
		'skip-combos: restart(null) + newWordFragment > revert(null) + pauseBegin': false,  // 18207: ?? (skip results are different than combos)
		'skip-combos: restart(null) + newWordFragment > revert(null) + pauseFinish': false,  // 18208: ?? (skip results are different than combos)
		'skip-combos: restart(null) + progress > play(null) + progress': false,  // 18997: ?? (skip results are different than combos)
		'skip-combos: restart(null) + progress > toggle(null) + playBegin': true,  // 19001: ?? (skip results are different than combos)
		'skip-combos: restart(null) + progress > toggle(null) + playFinish': true,  // 19002: ?? (skip results are different than combos)
		'skip-combos: restart(null) + progress > toggle(null) + pauseBegin': true,  // 19007: ?? (skip results are different than combos)
		'skip-combos: restart(null) + progress > toggle(null) + pauseFinish': true,  // 19008: ?? (skip results are different than combos)
		'skip-combos: restart(null) + progress > toggle(null) + loopBegin': true,  // 19019: ?? (skip results are different than combos)
		'skip-combos: restart(null) + progress > toggle(null) + loopFinish': true,  // 19020: ?? (skip results are different than combos)
		'skip-combos: restart(null) + progress > toggle(null) + newWordFragment': true,  // 19021: ?? (skip results are different than combos)
		'skip-combos: restart(null) + progress > toggle(null) + progress': true,  // 19022: ?? (skip results are different than combos)
		'skip-combos: restart(null) + progress > toggle(null) + stopBegin': true,  // 19023: ?? (skip results are different than combos)
		'skip-combos: restart(null) + progress > toggle(null) + stopFinish': true,  // 19024: ?? (skip results are different than combos)
		'skip-combos: restart(null) + progress > toggle(null) + done': true,  // 19025: ?? (skip results are different than combos)
		'skip-combos: restart(null) + progress > revert(null) + playBegin': false,  // 19101: ?? (skip results are different than combos)
		'skip-combos: restart(null) + progress > revert(null) + playFinish': false,  // 19102: ?? (skip results are different than combos)
		'skip-combos: restart(null) + progress > revert(null) + pauseBegin': false,  // 19107: ?? (skip results are different than combos)
		'skip-combos: restart(null) + progress > revert(null) + pauseFinish': false,  // 19108: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > play(null) + playBegin': false,  // 19876: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > play(null) + playFinish': false,  // 19877: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > play(null) + restartBegin': false,  // 19880: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > play(null) + restartFinish': false,  // 19881: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > toggle(null) + playBegin': false,  // 19901: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > toggle(null) + playFinish': false,  // 19902: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > toggle(null) + restartBegin': false,  // 19905: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > toggle(null) + restartFinish': false,  // 19906: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > rewind(null) + newWordFragment': false,  // 20046: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > rewind(null) + progress': false,  // 20047: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > fastForward(null) + newWordFragment': false,  // 20071: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > fastForward(null) + progress': false,  // 20072: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > once([0,0,-2]) + newWordFragment': false,  // 20096: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > once([0,0,-2]) + progress': false,  // 20097: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > once([0,0,-2]) + stopBegin': false,  // 20098: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > once([0,0,-2]) + stopFinish': false,  // 20099: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > once([0,0,-2]) + done': false,  // 20100: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > once([0,0,2]) + newWordFragment': false,  // 20121: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > once([0,0,2]) + progress': false,  // 20122: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > once([0,0,2]) + stopBegin': false,  // 20123: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > once([0,0,2]) + stopFinish': false,  // 20124: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > once([0,0,2]) + done': false,  // 20125: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > current(null) + newWordFragment': false,  // 20146: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > current(null) + progress': false,  // 20147: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > current(null) + stopBegin': false,  // 20148: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > current(null) + stopFinish': false,  // 20149: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > current(null) + done': false,  // 20150: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpWords(-3) + newWordFragment': false,  // 20321: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpWords(-3) + progress': false,  // 20322: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpWords(-3) + stopBegin': false,  // 20323: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpWords(-3) + stopFinish': false,  // 20324: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpWords(-3) + done': false,  // 20325: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpWords(-1) + newWordFragment': false,  // 20346: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpWords(-1) + progress': false,  // 20347: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpWords(-1) + stopBegin': false,  // 20348: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpWords(-1) + stopFinish': false,  // 20349: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpWords(-1) + done': false,  // 20350: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpWords(0) + newWordFragment': false,  // 20371: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpWords(0) + progress': false,  // 20372: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpWords(0) + stopBegin': false,  // 20373: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpWords(0) + stopFinish': false,  // 20374: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpWords(0) + done': false,  // 20375: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpWords(4) + newWordFragment': false,  // 20396: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpWords(4) + progress': false,  // 20397: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpWords(4) + stopBegin': false,  // 20398: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpWords(4) + stopFinish': false,  // 20399: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpWords(4) + done': false,  // 20400: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpSentences(-1) + newWordFragment': false,  // 20496: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpSentences(-1) + progress': false,  // 20497: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpSentences(-1) + stopBegin': false,  // 20498: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpSentences(-1) + stopFinish': false,  // 20499: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpSentences(-1) + done': false,  // 20500: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpSentences(0) + newWordFragment': false,  // 20521: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpSentences(0) + progress': false,  // 20522: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpSentences(0) + stopBegin': false,  // 20523: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpSentences(0) + stopFinish': false,  // 20524: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpSentences(0) + done': false,  // 20525: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpSentences(1) + newWordFragment': false,  // 20546: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpSentences(1) + progress': false,  // 20547: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpSentences(1) + stopBegin': false,  // 20548: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpSentences(1) + stopFinish': false,  // 20549: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpSentences(1) + done': false,  // 20550: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpSentences(3) + newWordFragment': false,  // 20571: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpSentences(3) + progress': false,  // 20572: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpSentences(3) + stopBegin': false,  // 20573: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpSentences(3) + stopFinish': false,  // 20574: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > jumpSentences(3) + done': false,  // 20575: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > nextWord(null) + newWordFragment': false,  // 20621: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > nextWord(null) + progress': false,  // 20622: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > nextWord(null) + stopBegin': false,  // 20623: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > nextWord(null) + stopFinish': false,  // 20624: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > nextWord(null) + done': false,  // 20625: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > nextSentence(null) + newWordFragment': false,  // 20646: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > nextSentence(null) + progress': false,  // 20647: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > nextSentence(null) + stopBegin': false,  // 20648: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > nextSentence(null) + stopFinish': false,  // 20649: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > nextSentence(null) + done': false,  // 20650: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > prevWord(null) + newWordFragment': false,  // 20671: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > prevWord(null) + progress': false,  // 20672: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > prevWord(null) + stopBegin': false,  // 20673: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > prevWord(null) + stopFinish': false,  // 20674: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > prevWord(null) + done': false,  // 20675: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > prevSentence(null) + newWordFragment': false,  // 20696: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > prevSentence(null) + progress': false,  // 20697: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > prevSentence(null) + stopBegin': false,  // 20698: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > prevSentence(null) + stopFinish': false,  // 20699: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopBegin > prevSentence(null) + done': false,  // 20700: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > play(null) + playBegin': false,  // 20776: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > play(null) + playFinish': false,  // 20777: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > play(null) + restartBegin': false,  // 20780: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > play(null) + restartFinish': false,  // 20781: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > toggle(null) + playBegin': false,  // 20801: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > toggle(null) + playFinish': false,  // 20802: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > toggle(null) + restartBegin': false,  // 20805: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > toggle(null) + restartFinish': false,  // 20806: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > rewind(null) + newWordFragment': false,  // 20946: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > rewind(null) + progress': false,  // 20947: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > fastForward(null) + newWordFragment': false,  // 20971: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > fastForward(null) + progress': false,  // 20972: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > once([0,0,-2]) + newWordFragment': false,  // 20996: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > once([0,0,-2]) + progress': false,  // 20997: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > once([0,0,-2]) + stopBegin': false,  // 20998: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > once([0,0,-2]) + stopFinish': false,  // 20999: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > once([0,0,-2]) + done': false,  // 21000: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > once([0,0,2]) + newWordFragment': false,  // 21021: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > once([0,0,2]) + progress': false,  // 21022: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > once([0,0,2]) + stopBegin': false,  // 21023: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > once([0,0,2]) + stopFinish': false,  // 21024: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > once([0,0,2]) + done': false,  // 21025: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > current(null) + newWordFragment': false,  // 21046: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > current(null) + progress': false,  // 21047: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > current(null) + stopBegin': false,  // 21048: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > current(null) + stopFinish': false,  // 21049: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > current(null) + done': false,  // 21050: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpWords(-3) + newWordFragment': false,  // 21221: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpWords(-3) + progress': false,  // 21222: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpWords(-3) + stopBegin': false,  // 21223: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpWords(-3) + stopFinish': false,  // 21224: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpWords(-3) + done': false,  // 21225: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpWords(-1) + newWordFragment': false,  // 21246: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpWords(-1) + progress': false,  // 21247: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpWords(-1) + stopBegin': false,  // 21248: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpWords(-1) + stopFinish': false,  // 21249: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpWords(-1) + done': false,  // 21250: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpWords(0) + newWordFragment': false,  // 21271: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpWords(0) + progress': false,  // 21272: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpWords(0) + stopBegin': false,  // 21273: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpWords(0) + stopFinish': false,  // 21274: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpWords(0) + done': false,  // 21275: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpWords(4) + newWordFragment': false,  // 21296: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpWords(4) + progress': false,  // 21297: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpWords(4) + stopBegin': false,  // 21298: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpWords(4) + stopFinish': false,  // 21299: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpWords(4) + done': false,  // 21300: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpSentences(-1) + newWordFragment': false,  // 21396: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpSentences(-1) + progress': false,  // 21397: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpSentences(-1) + stopBegin': false,  // 21398: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpSentences(-1) + stopFinish': false,  // 21399: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpSentences(-1) + done': false,  // 21400: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpSentences(0) + newWordFragment': false,  // 21421: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpSentences(0) + progress': false,  // 21422: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpSentences(0) + stopBegin': false,  // 21423: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpSentences(0) + stopFinish': false,  // 21424: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpSentences(0) + done': false,  // 21425: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpSentences(1) + newWordFragment': false,  // 21446: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpSentences(1) + progress': false,  // 21447: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpSentences(1) + stopBegin': false,  // 21448: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpSentences(1) + stopFinish': false,  // 21449: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpSentences(1) + done': false,  // 21450: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpSentences(3) + newWordFragment': false,  // 21471: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpSentences(3) + progress': false,  // 21472: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpSentences(3) + stopBegin': false,  // 21473: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpSentences(3) + stopFinish': false,  // 21474: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > jumpSentences(3) + done': false,  // 21475: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > nextWord(null) + newWordFragment': false,  // 21521: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > nextWord(null) + progress': false,  // 21522: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > nextWord(null) + stopBegin': false,  // 21523: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > nextWord(null) + stopFinish': false,  // 21524: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > nextWord(null) + done': false,  // 21525: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > nextSentence(null) + newWordFragment': false,  // 21546: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > nextSentence(null) + progress': false,  // 21547: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > nextSentence(null) + stopBegin': false,  // 21548: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > nextSentence(null) + stopFinish': false,  // 21549: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > nextSentence(null) + done': false,  // 21550: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > prevWord(null) + newWordFragment': false,  // 21571: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > prevWord(null) + progress': false,  // 21572: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > prevWord(null) + stopBegin': false,  // 21573: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > prevWord(null) + stopFinish': false,  // 21574: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > prevWord(null) + done': false,  // 21575: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > prevSentence(null) + newWordFragment': false,  // 21596: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > prevSentence(null) + progress': false,  // 21597: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > prevSentence(null) + stopBegin': false,  // 21598: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > prevSentence(null) + stopFinish': false,  // 21599: ?? (skip results are different than combos)
		'skip-combos: restart(null) + stopFinish > prevSentence(null) + done': false,  // 21600: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > play(null) + playBegin': false,  // 21676: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > play(null) + playFinish': false,  // 21677: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > play(null) + restartBegin': false,  // 21680: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > play(null) + restartFinish': false,  // 21681: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > toggle(null) + playBegin': false,  // 21701: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > toggle(null) + playFinish': false,  // 21702: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > toggle(null) + restartBegin': false,  // 21705: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > toggle(null) + restartFinish': false,  // 21706: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > rewind(null) + newWordFragment': false,  // 21846: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > rewind(null) + progress': false,  // 21847: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > fastForward(null) + newWordFragment': false,  // 21871: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > fastForward(null) + progress': false,  // 21872: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > once([0,0,-2]) + newWordFragment': false,  // 21896: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > once([0,0,-2]) + progress': false,  // 21897: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > once([0,0,-2]) + stopBegin': false,  // 21898: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > once([0,0,-2]) + stopFinish': false,  // 21899: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > once([0,0,-2]) + done': false,  // 21900: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > once([0,0,2]) + newWordFragment': false,  // 21921: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > once([0,0,2]) + progress': false,  // 21922: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > once([0,0,2]) + stopBegin': false,  // 21923: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > once([0,0,2]) + stopFinish': false,  // 21924: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > once([0,0,2]) + done': false,  // 21925: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > current(null) + newWordFragment': false,  // 21946: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > current(null) + progress': false,  // 21947: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > current(null) + stopBegin': false,  // 21948: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > current(null) + stopFinish': false,  // 21949: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > current(null) + done': false,  // 21950: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpWords(-3) + newWordFragment': false,  // 22121: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpWords(-3) + progress': false,  // 22122: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpWords(-3) + stopBegin': false,  // 22123: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpWords(-3) + stopFinish': false,  // 22124: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpWords(-3) + done': false,  // 22125: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpWords(-1) + newWordFragment': false,  // 22146: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpWords(-1) + progress': false,  // 22147: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpWords(-1) + stopBegin': false,  // 22148: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpWords(-1) + stopFinish': false,  // 22149: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpWords(-1) + done': false,  // 22150: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpWords(0) + newWordFragment': false,  // 22171: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpWords(0) + progress': false,  // 22172: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpWords(0) + stopBegin': false,  // 22173: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpWords(0) + stopFinish': false,  // 22174: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpWords(0) + done': false,  // 22175: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpWords(4) + newWordFragment': false,  // 22196: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpWords(4) + progress': false,  // 22197: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpWords(4) + stopBegin': false,  // 22198: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpWords(4) + stopFinish': false,  // 22199: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpWords(4) + done': false,  // 22200: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpSentences(-1) + newWordFragment': false,  // 22296: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpSentences(-1) + progress': false,  // 22297: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpSentences(-1) + stopBegin': false,  // 22298: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpSentences(-1) + stopFinish': false,  // 22299: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpSentences(-1) + done': false,  // 22300: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpSentences(0) + newWordFragment': false,  // 22321: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpSentences(0) + progress': false,  // 22322: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpSentences(0) + stopBegin': false,  // 22323: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpSentences(0) + stopFinish': false,  // 22324: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpSentences(0) + done': false,  // 22325: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpSentences(1) + newWordFragment': false,  // 22346: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpSentences(1) + progress': false,  // 22347: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpSentences(1) + stopBegin': false,  // 22348: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpSentences(1) + stopFinish': false,  // 22349: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpSentences(1) + done': false,  // 22350: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpSentences(3) + newWordFragment': false,  // 22371: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpSentences(3) + progress': false,  // 22372: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpSentences(3) + stopBegin': false,  // 22373: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpSentences(3) + stopFinish': false,  // 22374: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > jumpSentences(3) + done': false,  // 22375: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > nextWord(null) + newWordFragment': false,  // 22421: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > nextWord(null) + progress': false,  // 22422: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > nextWord(null) + stopBegin': false,  // 22423: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > nextWord(null) + stopFinish': false,  // 22424: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > nextWord(null) + done': false,  // 22425: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > nextSentence(null) + newWordFragment': false,  // 22446: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > nextSentence(null) + progress': false,  // 22447: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > nextSentence(null) + stopBegin': false,  // 22448: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > nextSentence(null) + stopFinish': false,  // 22449: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > nextSentence(null) + done': false,  // 22450: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > prevWord(null) + newWordFragment': false,  // 22471: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > prevWord(null) + progress': false,  // 22472: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > prevWord(null) + stopBegin': false,  // 22473: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > prevWord(null) + stopFinish': false,  // 22474: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > prevWord(null) + done': false,  // 22475: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > prevSentence(null) + newWordFragment': false,  // 22496: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > prevSentence(null) + progress': false,  // 22497: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > prevSentence(null) + stopBegin': false,  // 22498: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > prevSentence(null) + stopFinish': false,  // 22499: ?? (skip results are different than combos)
		'skip-combos: restart(null) + done > prevSentence(null) + done': false,  // 22500: ?? (skip results are different than combos)



		// ============== play ==============
		// #3 play-like + !stop*|done > some types of relative loopers (just play?) gets different output than usual
		// #4 from 'play' state + !stop*|done > toggle pauses, not plays
		// #5 from 'play' state + !stop*|done > revert plays, not pauses
		// #6 play-like + stop*|done > some more types of relative loopers(+|0|-) get different output than usual
		// #7 play-like + stop*|done > relative single(not rewind) loopers(-) don't trigger stop*|done
		// #8 play-like + stop*|done > relative loopers(+|0) do unexpectedly trigger stop*|done
		// #9 play-like + stop*|done > play|toggle() = triggers `restart` instead of `play`

		'skip-combos: play(null) + playBegin > play(null) + progress': false,  // 3697: ?? (skip results are different than combos)
		'skip-combos: play(null) + playBegin > toggle(null) + playBegin': true,  // 3701: ?? (skip results are different than combos)
		'skip-combos: play(null) + playBegin > toggle(null) + playFinish': true,  // 3702: ?? (skip results are different than combos)
		'skip-combos: play(null) + playBegin > toggle(null) + pauseBegin': true,  // 3707: ?? (skip results are different than combos)
		'skip-combos: play(null) + playBegin > toggle(null) + pauseFinish': true,  // 3708: ?? (skip results are different than combos)
		'skip-combos: play(null) + playBegin > toggle(null) + loopBegin': true,  // 3719: ?? (skip results are different than combos)
		'skip-combos: play(null) + playBegin > toggle(null) + loopFinish': true,  // 3720: ?? (skip results are different than combos)
		'skip-combos: play(null) + playBegin > toggle(null) + newWordFragment': true,  // 3721: ?? (skip results are different than combos)
		'skip-combos: play(null) + playBegin > toggle(null) + progress': true,  // 3722: ?? (skip results are different than combos)
		'skip-combos: play(null) + playBegin > toggle(null) + stopBegin': true,  // 3723: ?? (skip results are different than combos)
		'skip-combos: play(null) + playBegin > toggle(null) + stopFinish': true,  // 3724: ?? (skip results are different than combos)
		'skip-combos: play(null) + playBegin > toggle(null) + done': true,  // 3725: ?? (skip results are different than combos)
		'skip-combos: play(null) + playBegin > revert(null) + playBegin': false,  // 3801: ?? (skip results are different than combos)
		'skip-combos: play(null) + playBegin > revert(null) + playFinish': false,  // 3802: ?? (skip results are different than combos)
		'skip-combos: play(null) + playBegin > revert(null) + pauseBegin': false,  // 3807: ?? (skip results are different than combos)
		'skip-combos: play(null) + playBegin > revert(null) + pauseFinish': false,  // 3808: ?? (skip results are different than combos)
		'skip-combos: play(null) + playFinish > play(null) + progress': false,  // 4597: ?? (skip results are different than combos)
		'skip-combos: play(null) + playFinish > toggle(null) + playBegin': true,  // 4601: ?? (skip results are different than combos)
		'skip-combos: play(null) + playFinish > toggle(null) + playFinish': true,  // 4602: ?? (skip results are different than combos)
		'skip-combos: play(null) + playFinish > toggle(null) + pauseBegin': true,  // 4607: ?? (skip results are different than combos)
		'skip-combos: play(null) + playFinish > toggle(null) + pauseFinish': true,  // 4608: ?? (skip results are different than combos)
		'skip-combos: play(null) + playFinish > toggle(null) + loopBegin': true,  // 4619: ?? (skip results are different than combos)
		'skip-combos: play(null) + playFinish > toggle(null) + loopFinish': true,  // 4620: ?? (skip results are different than combos)
		'skip-combos: play(null) + playFinish > toggle(null) + newWordFragment': true,  // 4621: ?? (skip results are different than combos)
		'skip-combos: play(null) + playFinish > toggle(null) + progress': true,  // 4622: ?? (skip results are different than combos)
		'skip-combos: play(null) + playFinish > toggle(null) + stopBegin': true,  // 4623: ?? (skip results are different than combos)
		'skip-combos: play(null) + playFinish > toggle(null) + stopFinish': true,  // 4624: ?? (skip results are different than combos)
		'skip-combos: play(null) + playFinish > toggle(null) + done': true,  // 4625: ?? (skip results are different than combos)
		'skip-combos: play(null) + playFinish > revert(null) + playBegin': false,  // 4701: ?? (skip results are different than combos)
		'skip-combos: play(null) + playFinish > revert(null) + playFinish': false,  // 4702: ?? (skip results are different than combos)
		'skip-combos: play(null) + playFinish > revert(null) + pauseBegin': false,  // 4707: ?? (skip results are different than combos)
		'skip-combos: play(null) + playFinish > revert(null) + pauseFinish': false,  // 4708: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopBegin > play(null) + progress': false,  // 16297: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopBegin > toggle(null) + playBegin': true,  // 16301: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopBegin > toggle(null) + playFinish': true,  // 16302: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopBegin > toggle(null) + pauseBegin': true,  // 16307: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopBegin > toggle(null) + pauseFinish': true,  // 16308: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopBegin > toggle(null) + loopBegin': true,  // 16319: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopBegin > toggle(null) + loopFinish': true,  // 16320: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopBegin > toggle(null) + newWordFragment': true,  // 16321: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopBegin > toggle(null) + progress': true,  // 16322: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopBegin > toggle(null) + stopBegin': true,  // 16323: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopBegin > toggle(null) + stopFinish': true,  // 16324: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopBegin > toggle(null) + done': true,  // 16325: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopBegin > revert(null) + playBegin': false,  // 16401: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopBegin > revert(null) + playFinish': false,  // 16402: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopBegin > revert(null) + pauseBegin': false,  // 16407: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopBegin > revert(null) + pauseFinish': false,  // 16408: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopFinish > play(null) + progress': false,  // 17197: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopFinish > toggle(null) + playBegin': true,  // 17201: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopFinish > toggle(null) + playFinish': true,  // 17202: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopFinish > toggle(null) + pauseBegin': true,  // 17207: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopFinish > toggle(null) + pauseFinish': true,  // 17208: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopFinish > toggle(null) + loopBegin': true,  // 17219: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopFinish > toggle(null) + loopFinish': true,  // 17220: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopFinish > toggle(null) + newWordFragment': true,  // 17221: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopFinish > toggle(null) + progress': true,  // 17222: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopFinish > toggle(null) + stopBegin': true,  // 17223: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopFinish > toggle(null) + stopFinish': true,  // 17224: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopFinish > toggle(null) + done': true,  // 17225: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopFinish > revert(null) + playBegin': false,  // 17301: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopFinish > revert(null) + playFinish': false,  // 17302: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopFinish > revert(null) + pauseBegin': false,  // 17307: ?? (skip results are different than combos)
		'skip-combos: play(null) + loopFinish > revert(null) + pauseFinish': false,  // 17308: ?? (skip results are different than combos)
		'skip-combos: play(null) + newWordFragment > play(null) + newWordFragment': false,  // 18096: ?? (skip results are different than combos)
		'skip-combos: play(null) + newWordFragment > play(null) + progress': false,  // 18097: ?? (skip results are different than combos)
		'skip-combos: play(null) + newWordFragment > toggle(null) + playBegin': true,  // 18101: ?? (skip results are different than combos)
		'skip-combos: play(null) + newWordFragment > toggle(null) + playFinish': true,  // 18102: ?? (skip results are different than combos)
		'skip-combos: play(null) + newWordFragment > toggle(null) + pauseBegin': true,  // 18107: ?? (skip results are different than combos)
		'skip-combos: play(null) + newWordFragment > toggle(null) + pauseFinish': true,  // 18108: ?? (skip results are different than combos)
		'skip-combos: play(null) + newWordFragment > toggle(null) + loopBegin': true,  // 18119: ?? (skip results are different than combos)
		'skip-combos: play(null) + newWordFragment > toggle(null) + loopFinish': true,  // 18120: ?? (skip results are different than combos)
		'skip-combos: play(null) + newWordFragment > toggle(null) + newWordFragment': true,  // 18121: ?? (skip results are different than combos)
		'skip-combos: play(null) + newWordFragment > toggle(null) + progress': true,  // 18122: ?? (skip results are different than combos)
		'skip-combos: play(null) + newWordFragment > toggle(null) + stopBegin': true,  // 18123: ?? (skip results are different than combos)
		'skip-combos: play(null) + newWordFragment > toggle(null) + stopFinish': true,  // 18124: ?? (skip results are different than combos)
		'skip-combos: play(null) + newWordFragment > toggle(null) + done': true,  // 18125: ?? (skip results are different than combos)
		'skip-combos: play(null) + newWordFragment > revert(null) + playBegin': false,  // 18201: ?? (skip results are different than combos)
		'skip-combos: play(null) + newWordFragment > revert(null) + playFinish': false,  // 18202: ?? (skip results are different than combos)
		'skip-combos: play(null) + newWordFragment > revert(null) + pauseBegin': false,  // 18207: ?? (skip results are different than combos)
		'skip-combos: play(null) + newWordFragment > revert(null) + pauseFinish': false,  // 18208: ?? (skip results are different than combos)
		'skip-combos: play(null) + progress > play(null) + progress': false,  // 18997: ?? (skip results are different than combos)
		'skip-combos: play(null) + progress > toggle(null) + playBegin': true,  // 19001: ?? (skip results are different than combos)
		'skip-combos: play(null) + progress > toggle(null) + playFinish': true,  // 19002: ?? (skip results are different than combos)
		'skip-combos: play(null) + progress > toggle(null) + pauseBegin': true,  // 19007: ?? (skip results are different than combos)
		'skip-combos: play(null) + progress > toggle(null) + pauseFinish': true,  // 19008: ?? (skip results are different than combos)
		'skip-combos: play(null) + progress > toggle(null) + loopBegin': true,  // 19019: ?? (skip results are different than combos)
		'skip-combos: play(null) + progress > toggle(null) + loopFinish': true,  // 19020: ?? (skip results are different than combos)
		'skip-combos: play(null) + progress > toggle(null) + newWordFragment': true,  // 19021: ?? (skip results are different than combos)
		'skip-combos: play(null) + progress > toggle(null) + progress': true,  // 19022: ?? (skip results are different than combos)
		'skip-combos: play(null) + progress > toggle(null) + stopBegin': true,  // 19023: ?? (skip results are different than combos)
		'skip-combos: play(null) + progress > toggle(null) + stopFinish': true,  // 19024: ?? (skip results are different than combos)
		'skip-combos: play(null) + progress > toggle(null) + done': true,  // 19025: ?? (skip results are different than combos)
		'skip-combos: play(null) + progress > revert(null) + playBegin': false,  // 19101: ?? (skip results are different than combos)
		'skip-combos: play(null) + progress > revert(null) + playFinish': false,  // 19102: ?? (skip results are different than combos)
		'skip-combos: play(null) + progress > revert(null) + pauseBegin': false,  // 19107: ?? (skip results are different than combos)
		'skip-combos: play(null) + progress > revert(null) + pauseFinish': false,  // 19108: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > play(null) + playBegin': false,  // 19876: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > play(null) + playFinish': false,  // 19877: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > play(null) + restartBegin': false,  // 19880: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > play(null) + restartFinish': false,  // 19881: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > toggle(null) + playBegin': false,  // 19901: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > toggle(null) + playFinish': false,  // 19902: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > toggle(null) + restartBegin': false,  // 19905: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > toggle(null) + restartFinish': false,  // 19906: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > rewind(null) + newWordFragment': false,  // 20046: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > rewind(null) + progress': false,  // 20047: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > fastForward(null) + newWordFragment': false,  // 20071: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > fastForward(null) + progress': false,  // 20072: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > once([0,0,-2]) + newWordFragment': false,  // 20096: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > once([0,0,-2]) + progress': false,  // 20097: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > once([0,0,-2]) + stopBegin': false,  // 20098: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > once([0,0,-2]) + stopFinish': false,  // 20099: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > once([0,0,-2]) + done': false,  // 20100: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > once([0,0,2]) + newWordFragment': false,  // 20121: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > once([0,0,2]) + progress': false,  // 20122: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > once([0,0,2]) + stopBegin': false,  // 20123: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > once([0,0,2]) + stopFinish': false,  // 20124: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > once([0,0,2]) + done': false,  // 20125: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > current(null) + newWordFragment': false,  // 20146: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > current(null) + progress': false,  // 20147: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > current(null) + stopBegin': false,  // 20148: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > current(null) + stopFinish': false,  // 20149: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > current(null) + done': false,  // 20150: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpWords(-3) + newWordFragment': false,  // 20321: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpWords(-3) + progress': false,  // 20322: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpWords(-3) + stopBegin': false,  // 20323: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpWords(-3) + stopFinish': false,  // 20324: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpWords(-3) + done': false,  // 20325: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpWords(-1) + newWordFragment': false,  // 20346: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpWords(-1) + progress': false,  // 20347: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpWords(-1) + stopBegin': false,  // 20348: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpWords(-1) + stopFinish': false,  // 20349: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpWords(-1) + done': false,  // 20350: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpWords(0) + newWordFragment': false,  // 20371: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpWords(0) + progress': false,  // 20372: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpWords(0) + stopBegin': false,  // 20373: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpWords(0) + stopFinish': false,  // 20374: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpWords(0) + done': false,  // 20375: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpWords(4) + newWordFragment': false,  // 20396: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpWords(4) + progress': false,  // 20397: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpWords(4) + stopBegin': false,  // 20398: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpWords(4) + stopFinish': false,  // 20399: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpWords(4) + done': false,  // 20400: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpSentences(-1) + newWordFragment': false,  // 20496: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpSentences(-1) + progress': false,  // 20497: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpSentences(-1) + stopBegin': false,  // 20498: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpSentences(-1) + stopFinish': false,  // 20499: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpSentences(-1) + done': false,  // 20500: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpSentences(0) + newWordFragment': false,  // 20521: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpSentences(0) + progress': false,  // 20522: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpSentences(0) + stopBegin': false,  // 20523: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpSentences(0) + stopFinish': false,  // 20524: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpSentences(0) + done': false,  // 20525: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpSentences(1) + newWordFragment': false,  // 20546: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpSentences(1) + progress': false,  // 20547: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpSentences(1) + stopBegin': false,  // 20548: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpSentences(1) + stopFinish': false,  // 20549: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpSentences(1) + done': false,  // 20550: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpSentences(3) + newWordFragment': false,  // 20571: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpSentences(3) + progress': false,  // 20572: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpSentences(3) + stopBegin': false,  // 20573: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpSentences(3) + stopFinish': false,  // 20574: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > jumpSentences(3) + done': false,  // 20575: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > nextWord(null) + newWordFragment': false,  // 20621: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > nextWord(null) + progress': false,  // 20622: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > nextWord(null) + stopBegin': false,  // 20623: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > nextWord(null) + stopFinish': false,  // 20624: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > nextWord(null) + done': false,  // 20625: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > nextSentence(null) + newWordFragment': false,  // 20646: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > nextSentence(null) + progress': false,  // 20647: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > nextSentence(null) + stopBegin': false,  // 20648: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > nextSentence(null) + stopFinish': false,  // 20649: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > nextSentence(null) + done': false,  // 20650: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > prevWord(null) + newWordFragment': false,  // 20671: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > prevWord(null) + progress': false,  // 20672: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > prevWord(null) + stopBegin': false,  // 20673: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > prevWord(null) + stopFinish': false,  // 20674: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > prevWord(null) + done': false,  // 20675: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > prevSentence(null) + newWordFragment': false,  // 20696: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > prevSentence(null) + progress': false,  // 20697: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > prevSentence(null) + stopBegin': false,  // 20698: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > prevSentence(null) + stopFinish': false,  // 20699: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopBegin > prevSentence(null) + done': false,  // 20700: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > play(null) + playBegin': false,  // 20776: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > play(null) + playFinish': false,  // 20777: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > play(null) + restartBegin': false,  // 20780: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > play(null) + restartFinish': false,  // 20781: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > toggle(null) + playBegin': false,  // 20801: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > toggle(null) + playFinish': false,  // 20802: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > toggle(null) + restartBegin': false,  // 20805: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > toggle(null) + restartFinish': false,  // 20806: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > rewind(null) + newWordFragment': false,  // 20946: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > rewind(null) + progress': false,  // 20947: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > fastForward(null) + newWordFragment': false,  // 20971: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > fastForward(null) + progress': false,  // 20972: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > once([0,0,-2]) + newWordFragment': false,  // 20996: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > once([0,0,-2]) + progress': false,  // 20997: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > once([0,0,-2]) + stopBegin': false,  // 20998: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > once([0,0,-2]) + stopFinish': false,  // 20999: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > once([0,0,-2]) + done': false,  // 21000: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > once([0,0,2]) + newWordFragment': false,  // 21021: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > once([0,0,2]) + progress': false,  // 21022: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > once([0,0,2]) + stopBegin': false,  // 21023: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > once([0,0,2]) + stopFinish': false,  // 21024: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > once([0,0,2]) + done': false,  // 21025: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > current(null) + newWordFragment': false,  // 21046: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > current(null) + progress': false,  // 21047: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > current(null) + stopBegin': false,  // 21048: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > current(null) + stopFinish': false,  // 21049: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > current(null) + done': false,  // 21050: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpWords(-3) + newWordFragment': false,  // 21221: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpWords(-3) + progress': false,  // 21222: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpWords(-3) + stopBegin': false,  // 21223: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpWords(-3) + stopFinish': false,  // 21224: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpWords(-3) + done': false,  // 21225: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpWords(-1) + newWordFragment': false,  // 21246: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpWords(-1) + progress': false,  // 21247: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpWords(-1) + stopBegin': false,  // 21248: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpWords(-1) + stopFinish': false,  // 21249: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpWords(-1) + done': false,  // 21250: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpWords(0) + newWordFragment': false,  // 21271: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpWords(0) + progress': false,  // 21272: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpWords(0) + stopBegin': false,  // 21273: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpWords(0) + stopFinish': false,  // 21274: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpWords(0) + done': false,  // 21275: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpWords(4) + newWordFragment': false,  // 21296: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpWords(4) + progress': false,  // 21297: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpWords(4) + stopBegin': false,  // 21298: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpWords(4) + stopFinish': false,  // 21299: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpWords(4) + done': false,  // 21300: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpSentences(-1) + newWordFragment': false,  // 21396: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpSentences(-1) + progress': false,  // 21397: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpSentences(-1) + stopBegin': false,  // 21398: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpSentences(-1) + stopFinish': false,  // 21399: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpSentences(-1) + done': false,  // 21400: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpSentences(0) + newWordFragment': false,  // 21421: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpSentences(0) + progress': false,  // 21422: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpSentences(0) + stopBegin': false,  // 21423: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpSentences(0) + stopFinish': false,  // 21424: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpSentences(0) + done': false,  // 21425: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpSentences(1) + newWordFragment': false,  // 21446: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpSentences(1) + progress': false,  // 21447: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpSentences(1) + stopBegin': false,  // 21448: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpSentences(1) + stopFinish': false,  // 21449: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpSentences(1) + done': false,  // 21450: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpSentences(3) + newWordFragment': false,  // 21471: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpSentences(3) + progress': false,  // 21472: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpSentences(3) + stopBegin': false,  // 21473: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpSentences(3) + stopFinish': false,  // 21474: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > jumpSentences(3) + done': false,  // 21475: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > nextWord(null) + newWordFragment': false,  // 21521: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > nextWord(null) + progress': false,  // 21522: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > nextWord(null) + stopBegin': false,  // 21523: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > nextWord(null) + stopFinish': false,  // 21524: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > nextWord(null) + done': false,  // 21525: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > nextSentence(null) + newWordFragment': false,  // 21546: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > nextSentence(null) + progress': false,  // 21547: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > nextSentence(null) + stopBegin': false,  // 21548: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > nextSentence(null) + stopFinish': false,  // 21549: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > nextSentence(null) + done': false,  // 21550: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > prevWord(null) + newWordFragment': false,  // 21571: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > prevWord(null) + progress': false,  // 21572: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > prevWord(null) + stopBegin': false,  // 21573: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > prevWord(null) + stopFinish': false,  // 21574: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > prevWord(null) + done': false,  // 21575: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > prevSentence(null) + newWordFragment': false,  // 21596: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > prevSentence(null) + progress': false,  // 21597: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > prevSentence(null) + stopBegin': false,  // 21598: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > prevSentence(null) + stopFinish': false,  // 21599: ?? (skip results are different than combos)
		'skip-combos: play(null) + stopFinish > prevSentence(null) + done': false,  // 21600: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > play(null) + playBegin': false,  // 21676: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > play(null) + playFinish': false,  // 21677: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > play(null) + restartBegin': false,  // 21680: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > play(null) + restartFinish': false,  // 21681: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > toggle(null) + playBegin': false,  // 21701: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > toggle(null) + playFinish': false,  // 21702: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > toggle(null) + restartBegin': false,  // 21705: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > toggle(null) + restartFinish': false,  // 21706: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > rewind(null) + newWordFragment': false,  // 21846: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > rewind(null) + progress': false,  // 21847: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > fastForward(null) + newWordFragment': false,  // 21871: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > fastForward(null) + progress': false,  // 21872: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > once([0,0,-2]) + newWordFragment': false,  // 21896: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > once([0,0,-2]) + progress': false,  // 21897: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > once([0,0,-2]) + stopBegin': false,  // 21898: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > once([0,0,-2]) + stopFinish': false,  // 21899: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > once([0,0,-2]) + done': false,  // 21900: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > once([0,0,2]) + newWordFragment': false,  // 21921: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > once([0,0,2]) + progress': false,  // 21922: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > once([0,0,2]) + stopBegin': false,  // 21923: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > once([0,0,2]) + stopFinish': false,  // 21924: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > once([0,0,2]) + done': false,  // 21925: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > current(null) + newWordFragment': false,  // 21946: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > current(null) + progress': false,  // 21947: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > current(null) + stopBegin': false,  // 21948: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > current(null) + stopFinish': false,  // 21949: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > current(null) + done': false,  // 21950: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpWords(-3) + newWordFragment': false,  // 22121: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpWords(-3) + progress': false,  // 22122: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpWords(-3) + stopBegin': false,  // 22123: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpWords(-3) + stopFinish': false,  // 22124: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpWords(-3) + done': false,  // 22125: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpWords(-1) + newWordFragment': false,  // 22146: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpWords(-1) + progress': false,  // 22147: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpWords(-1) + stopBegin': false,  // 22148: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpWords(-1) + stopFinish': false,  // 22149: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpWords(-1) + done': false,  // 22150: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpWords(0) + newWordFragment': false,  // 22171: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpWords(0) + progress': false,  // 22172: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpWords(0) + stopBegin': false,  // 22173: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpWords(0) + stopFinish': false,  // 22174: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpWords(0) + done': false,  // 22175: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpWords(4) + newWordFragment': false,  // 22196: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpWords(4) + progress': false,  // 22197: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpWords(4) + stopBegin': false,  // 22198: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpWords(4) + stopFinish': false,  // 22199: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpWords(4) + done': false,  // 22200: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpSentences(-1) + newWordFragment': false,  // 22296: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpSentences(-1) + progress': false,  // 22297: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpSentences(-1) + stopBegin': false,  // 22298: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpSentences(-1) + stopFinish': false,  // 22299: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpSentences(-1) + done': false,  // 22300: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpSentences(0) + newWordFragment': false,  // 22321: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpSentences(0) + progress': false,  // 22322: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpSentences(0) + stopBegin': false,  // 22323: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpSentences(0) + stopFinish': false,  // 22324: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpSentences(0) + done': false,  // 22325: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpSentences(1) + newWordFragment': false,  // 22346: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpSentences(1) + progress': false,  // 22347: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpSentences(1) + stopBegin': false,  // 22348: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpSentences(1) + stopFinish': false,  // 22349: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpSentences(1) + done': false,  // 22350: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpSentences(3) + newWordFragment': false,  // 22371: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpSentences(3) + progress': false,  // 22372: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpSentences(3) + stopBegin': false,  // 22373: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpSentences(3) + stopFinish': false,  // 22374: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > jumpSentences(3) + done': false,  // 22375: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > nextWord(null) + newWordFragment': false,  // 22421: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > nextWord(null) + progress': false,  // 22422: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > nextWord(null) + stopBegin': false,  // 22423: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > nextWord(null) + stopFinish': false,  // 22424: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > nextWord(null) + done': false,  // 22425: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > nextSentence(null) + newWordFragment': false,  // 22446: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > nextSentence(null) + progress': false,  // 22447: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > nextSentence(null) + stopBegin': false,  // 22448: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > nextSentence(null) + stopFinish': false,  // 22449: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > nextSentence(null) + done': false,  // 22450: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > prevWord(null) + newWordFragment': false,  // 22471: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > prevWord(null) + progress': false,  // 22472: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > prevWord(null) + stopBegin': false,  // 22473: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > prevWord(null) + stopFinish': false,  // 22474: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > prevWord(null) + done': false,  // 22475: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > prevSentence(null) + newWordFragment': false,  // 22496: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > prevSentence(null) + progress': false,  // 22497: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > prevSentence(null) + stopBegin': false,  // 22498: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > prevSentence(null) + stopFinish': false,  // 22499: ?? (skip results are different than combos)
		'skip-combos: play(null) + done > prevSentence(null) + done': false,  // 22500: ?? (skip results are different than combos)



		// ============== toggle ==============
		// Identical to `play()` (almost? what are the differences?)
		// #3 play-like + !stop*|done > some types of relative loopers (just play?) gets different output than usual
		// #4 from 'play' state + !stop*|done > toggle pauses, not plays
		// #5 from 'play' state + !stop*|done > revert plays, not pauses
		// #6 play-like + stop*|done > some more types of relative loopers(+|0|-) get different output than usual
		// #7 play-like + stop*|done > relative single(not rewind) loopers(-) don't trigger stop*|done
		// #8 play-like + stop*|done > relative loopers(+|0) do unexpectedly trigger stop*|done
		// #9 play-like + stop*|done > play = triggers `restart` instead of `play`
		'skip-combos: toggle(null) + playBegin > play(null) + progress': false,  // 3697: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playBegin > toggle(null) + playBegin': false,  // 3701: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playBegin > toggle(null) + playFinish': false,  // 3702: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playBegin > toggle(null) + pauseBegin': false,  // 3707: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playBegin > toggle(null) + pauseFinish': false,  // 3708: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playBegin > toggle(null) + loopBegin': false,  // 3719: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playBegin > toggle(null) + loopFinish': false,  // 3720: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playBegin > toggle(null) + newWordFragment': false,  // 3721: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playBegin > toggle(null) + progress': false,  // 3722: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playBegin > toggle(null) + stopBegin': false,  // 3723: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playBegin > toggle(null) + stopFinish': false,  // 3724: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playBegin > toggle(null) + done': false,  // 3725: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playBegin > revert(null) + playBegin': false,  // 3801: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playBegin > revert(null) + playFinish': false,  // 3802: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playBegin > revert(null) + pauseBegin': false,  // 3807: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playBegin > revert(null) + pauseFinish': false,  // 3808: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playFinish > play(null) + progress': false,  // 4597: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playFinish > toggle(null) + playBegin': false,  // 4601: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playFinish > toggle(null) + playFinish': false,  // 4602: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playFinish > toggle(null) + pauseBegin': false,  // 4607: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playFinish > toggle(null) + pauseFinish': false,  // 4608: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playFinish > toggle(null) + loopBegin': false,  // 4619: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playFinish > toggle(null) + loopFinish': false,  // 4620: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playFinish > toggle(null) + newWordFragment': false,  // 4621: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playFinish > toggle(null) + progress': false,  // 4622: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playFinish > toggle(null) + stopBegin': false,  // 4623: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playFinish > toggle(null) + stopFinish': false,  // 4624: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playFinish > toggle(null) + done': false,  // 4625: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playFinish > revert(null) + playBegin': false,  // 4701: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playFinish > revert(null) + playFinish': false,  // 4702: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playFinish > revert(null) + pauseBegin': false,  // 4707: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + playFinish > revert(null) + pauseFinish': false,  // 4708: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopBegin > play(null) + progress': false,  // 16297: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopBegin > toggle(null) + playBegin': false,  // 16301: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopBegin > toggle(null) + playFinish': false,  // 16302: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopBegin > toggle(null) + pauseBegin': false,  // 16307: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopBegin > toggle(null) + pauseFinish': false,  // 16308: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopBegin > toggle(null) + loopBegin': false,  // 16319: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopBegin > toggle(null) + loopFinish': false,  // 16320: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopBegin > toggle(null) + newWordFragment': false,  // 16321: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopBegin > toggle(null) + progress': false,  // 16322: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopBegin > toggle(null) + stopBegin': false,  // 16323: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopBegin > toggle(null) + stopFinish': false,  // 16324: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopBegin > toggle(null) + done': false,  // 16325: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopBegin > revert(null) + playBegin': false,  // 16401: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopBegin > revert(null) + playFinish': false,  // 16402: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopBegin > revert(null) + pauseBegin': false,  // 16407: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopBegin > revert(null) + pauseFinish': false,  // 16408: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopFinish > play(null) + progress': false,  // 17197: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopFinish > toggle(null) + playBegin': false,  // 17201: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopFinish > toggle(null) + playFinish': false,  // 17202: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopFinish > toggle(null) + pauseBegin': false,  // 17207: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopFinish > toggle(null) + pauseFinish': false,  // 17208: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopFinish > toggle(null) + loopBegin': false,  // 17219: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopFinish > toggle(null) + loopFinish': false,  // 17220: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopFinish > toggle(null) + newWordFragment': false,  // 17221: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopFinish > toggle(null) + progress': false,  // 17222: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopFinish > toggle(null) + stopBegin': false,  // 17223: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopFinish > toggle(null) + stopFinish': false,  // 17224: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopFinish > toggle(null) + done': false,  // 17225: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopFinish > revert(null) + playBegin': false,  // 17301: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopFinish > revert(null) + playFinish': false,  // 17302: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopFinish > revert(null) + pauseBegin': false,  // 17307: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + loopFinish > revert(null) + pauseFinish': false,  // 17308: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + newWordFragment > play(null) + newWordFragment': false,  // 18096: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + newWordFragment > play(null) + progress': false,  // 18097: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + newWordFragment > toggle(null) + playBegin': false,  // 18101: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + newWordFragment > toggle(null) + playFinish': false,  // 18102: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + newWordFragment > toggle(null) + pauseBegin': false,  // 18107: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + newWordFragment > toggle(null) + pauseFinish': false,  // 18108: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + newWordFragment > toggle(null) + loopBegin': false,  // 18119: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + newWordFragment > toggle(null) + loopFinish': false,  // 18120: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + newWordFragment > toggle(null) + newWordFragment': false,  // 18121: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + newWordFragment > toggle(null) + progress': false,  // 18122: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + newWordFragment > toggle(null) + stopBegin': false,  // 18123: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + newWordFragment > toggle(null) + stopFinish': false,  // 18124: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + newWordFragment > toggle(null) + done': false,  // 18125: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + newWordFragment > revert(null) + playBegin': false,  // 18201: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + newWordFragment > revert(null) + playFinish': false,  // 18202: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + newWordFragment > revert(null) + pauseBegin': false,  // 18207: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + newWordFragment > revert(null) + pauseFinish': false,  // 18208: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + progress > play(null) + progress': false,  // 18997: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + progress > toggle(null) + playBegin': false,  // 19001: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + progress > toggle(null) + playFinish': false,  // 19002: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + progress > toggle(null) + pauseBegin': false,  // 19007: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + progress > toggle(null) + pauseFinish': false,  // 19008: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + progress > toggle(null) + loopBegin': false,  // 19019: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + progress > toggle(null) + loopFinish': false,  // 19020: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + progress > toggle(null) + newWordFragment': false,  // 19021: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + progress > toggle(null) + progress': false,  // 19022: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + progress > toggle(null) + stopBegin': false,  // 19023: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + progress > toggle(null) + stopFinish': false,  // 19024: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + progress > toggle(null) + done': false,  // 19025: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + progress > revert(null) + playBegin': false,  // 19101: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + progress > revert(null) + playFinish': false,  // 19102: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + progress > revert(null) + pauseBegin': false,  // 19107: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + progress > revert(null) + pauseFinish': false,  // 19108: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > play(null) + playBegin': false,  // 19876: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > play(null) + playFinish': false,  // 19877: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > play(null) + restartBegin': false,  // 19880: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > play(null) + restartFinish': false,  // 19881: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > toggle(null) + playBegin': false,  // 19901: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > toggle(null) + playFinish': false,  // 19902: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > toggle(null) + restartBegin': false,  // 19905: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > toggle(null) + restartFinish': false,  // 19906: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > rewind(null) + newWordFragment': false,  // 20046: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > rewind(null) + progress': false,  // 20047: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > fastForward(null) + newWordFragment': false,  // 20071: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > fastForward(null) + progress': false,  // 20072: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > once([0,0,-2]) + newWordFragment': false,  // 20096: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > once([0,0,-2]) + progress': false,  // 20097: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > once([0,0,-2]) + stopBegin': false,  // 20098: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > once([0,0,-2]) + stopFinish': false,  // 20099: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > once([0,0,-2]) + done': false,  // 20100: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > once([0,0,2]) + newWordFragment': false,  // 20121: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > once([0,0,2]) + progress': false,  // 20122: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > once([0,0,2]) + stopBegin': false,  // 20123: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > once([0,0,2]) + stopFinish': false,  // 20124: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > once([0,0,2]) + done': false,  // 20125: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > current(null) + newWordFragment': false,  // 20146: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > current(null) + progress': false,  // 20147: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > current(null) + stopBegin': false,  // 20148: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > current(null) + stopFinish': false,  // 20149: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > current(null) + done': false,  // 20150: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpWords(-3) + newWordFragment': false,  // 20321: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpWords(-3) + progress': false,  // 20322: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpWords(-3) + stopBegin': false,  // 20323: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpWords(-3) + stopFinish': false,  // 20324: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpWords(-3) + done': false,  // 20325: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpWords(-1) + newWordFragment': false,  // 20346: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpWords(-1) + progress': false,  // 20347: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpWords(-1) + stopBegin': false,  // 20348: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpWords(-1) + stopFinish': false,  // 20349: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpWords(-1) + done': false,  // 20350: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpWords(0) + newWordFragment': false,  // 20371: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpWords(0) + progress': false,  // 20372: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpWords(0) + stopBegin': false,  // 20373: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpWords(0) + stopFinish': false,  // 20374: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpWords(0) + done': false,  // 20375: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpWords(4) + newWordFragment': false,  // 20396: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpWords(4) + progress': false,  // 20397: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpWords(4) + stopBegin': false,  // 20398: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpWords(4) + stopFinish': false,  // 20399: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpWords(4) + done': false,  // 20400: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpSentences(-1) + newWordFragment': false,  // 20496: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpSentences(-1) + progress': false,  // 20497: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpSentences(-1) + stopBegin': false,  // 20498: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpSentences(-1) + stopFinish': false,  // 20499: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpSentences(-1) + done': false,  // 20500: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpSentences(0) + newWordFragment': false,  // 20521: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpSentences(0) + progress': false,  // 20522: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpSentences(0) + stopBegin': false,  // 20523: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpSentences(0) + stopFinish': false,  // 20524: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpSentences(0) + done': false,  // 20525: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpSentences(1) + newWordFragment': false,  // 20546: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpSentences(1) + progress': false,  // 20547: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpSentences(1) + stopBegin': false,  // 20548: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpSentences(1) + stopFinish': false,  // 20549: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpSentences(1) + done': false,  // 20550: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpSentences(3) + newWordFragment': false,  // 20571: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpSentences(3) + progress': false,  // 20572: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpSentences(3) + stopBegin': false,  // 20573: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpSentences(3) + stopFinish': false,  // 20574: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > jumpSentences(3) + done': false,  // 20575: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > nextWord(null) + newWordFragment': false,  // 20621: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > nextWord(null) + progress': false,  // 20622: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > nextWord(null) + stopBegin': false,  // 20623: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > nextWord(null) + stopFinish': false,  // 20624: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > nextWord(null) + done': false,  // 20625: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > nextSentence(null) + newWordFragment': false,  // 20646: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > nextSentence(null) + progress': false,  // 20647: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > nextSentence(null) + stopBegin': false,  // 20648: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > nextSentence(null) + stopFinish': false,  // 20649: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > nextSentence(null) + done': false,  // 20650: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > prevWord(null) + newWordFragment': false,  // 20671: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > prevWord(null) + progress': false,  // 20672: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > prevWord(null) + stopBegin': false,  // 20673: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > prevWord(null) + stopFinish': false,  // 20674: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > prevWord(null) + done': false,  // 20675: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > prevSentence(null) + newWordFragment': false,  // 20696: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > prevSentence(null) + progress': false,  // 20697: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > prevSentence(null) + stopBegin': false,  // 20698: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > prevSentence(null) + stopFinish': false,  // 20699: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopBegin > prevSentence(null) + done': false,  // 20700: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > play(null) + playBegin': false,  // 20776: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > play(null) + playFinish': false,  // 20777: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > play(null) + restartBegin': false,  // 20780: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > play(null) + restartFinish': false,  // 20781: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > toggle(null) + playBegin': false,  // 20801: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > toggle(null) + playFinish': false,  // 20802: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > toggle(null) + restartBegin': false,  // 20805: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > toggle(null) + restartFinish': false,  // 20806: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > rewind(null) + newWordFragment': false,  // 20946: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > rewind(null) + progress': false,  // 20947: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > fastForward(null) + newWordFragment': false,  // 20971: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > fastForward(null) + progress': false,  // 20972: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > once([0,0,-2]) + newWordFragment': false,  // 20996: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > once([0,0,-2]) + progress': false,  // 20997: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > once([0,0,-2]) + stopBegin': false,  // 20998: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > once([0,0,-2]) + stopFinish': false,  // 20999: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > once([0,0,-2]) + done': false,  // 21000: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > once([0,0,2]) + newWordFragment': false,  // 21021: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > once([0,0,2]) + progress': false,  // 21022: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > once([0,0,2]) + stopBegin': false,  // 21023: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > once([0,0,2]) + stopFinish': false,  // 21024: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > once([0,0,2]) + done': false,  // 21025: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > current(null) + newWordFragment': false,  // 21046: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > current(null) + progress': false,  // 21047: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > current(null) + stopBegin': false,  // 21048: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > current(null) + stopFinish': false,  // 21049: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > current(null) + done': false,  // 21050: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpWords(-3) + newWordFragment': false,  // 21221: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpWords(-3) + progress': false,  // 21222: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpWords(-3) + stopBegin': false,  // 21223: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpWords(-3) + stopFinish': false,  // 21224: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpWords(-3) + done': false,  // 21225: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpWords(-1) + newWordFragment': false,  // 21246: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpWords(-1) + progress': false,  // 21247: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpWords(-1) + stopBegin': false,  // 21248: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpWords(-1) + stopFinish': false,  // 21249: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpWords(-1) + done': false,  // 21250: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpWords(0) + newWordFragment': false,  // 21271: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpWords(0) + progress': false,  // 21272: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpWords(0) + stopBegin': false,  // 21273: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpWords(0) + stopFinish': false,  // 21274: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpWords(0) + done': false,  // 21275: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpWords(4) + newWordFragment': false,  // 21296: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpWords(4) + progress': false,  // 21297: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpWords(4) + stopBegin': false,  // 21298: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpWords(4) + stopFinish': false,  // 21299: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpWords(4) + done': false,  // 21300: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpSentences(-1) + newWordFragment': false,  // 21396: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpSentences(-1) + progress': false,  // 21397: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpSentences(-1) + stopBegin': false,  // 21398: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpSentences(-1) + stopFinish': false,  // 21399: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpSentences(-1) + done': false,  // 21400: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpSentences(0) + newWordFragment': false,  // 21421: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpSentences(0) + progress': false,  // 21422: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpSentences(0) + stopBegin': false,  // 21423: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpSentences(0) + stopFinish': false,  // 21424: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpSentences(0) + done': false,  // 21425: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpSentences(1) + newWordFragment': false,  // 21446: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpSentences(1) + progress': false,  // 21447: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpSentences(1) + stopBegin': false,  // 21448: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpSentences(1) + stopFinish': false,  // 21449: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpSentences(1) + done': false,  // 21450: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpSentences(3) + newWordFragment': false,  // 21471: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpSentences(3) + progress': false,  // 21472: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpSentences(3) + stopBegin': false,  // 21473: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpSentences(3) + stopFinish': false,  // 21474: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > jumpSentences(3) + done': false,  // 21475: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > nextWord(null) + newWordFragment': false,  // 21521: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > nextWord(null) + progress': false,  // 21522: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > nextWord(null) + stopBegin': false,  // 21523: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > nextWord(null) + stopFinish': false,  // 21524: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > nextWord(null) + done': false,  // 21525: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > nextSentence(null) + newWordFragment': false,  // 21546: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > nextSentence(null) + progress': false,  // 21547: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > nextSentence(null) + stopBegin': false,  // 21548: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > nextSentence(null) + stopFinish': false,  // 21549: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > nextSentence(null) + done': false,  // 21550: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > prevWord(null) + newWordFragment': false,  // 21571: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > prevWord(null) + progress': false,  // 21572: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > prevWord(null) + stopBegin': false,  // 21573: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > prevWord(null) + stopFinish': false,  // 21574: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > prevWord(null) + done': false,  // 21575: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > prevSentence(null) + newWordFragment': false,  // 21596: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > prevSentence(null) + progress': false,  // 21597: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > prevSentence(null) + stopBegin': false,  // 21598: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > prevSentence(null) + stopFinish': false,  // 21599: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + stopFinish > prevSentence(null) + done': false,  // 21600: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > play(null) + playBegin': false,  // 21676: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > play(null) + playFinish': false,  // 21677: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > play(null) + restartBegin': false,  // 21680: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > play(null) + restartFinish': false,  // 21681: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > toggle(null) + playBegin': false,  // 21701: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > toggle(null) + playFinish': false,  // 21702: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > toggle(null) + restartBegin': false,  // 21705: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > toggle(null) + restartFinish': false,  // 21706: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > rewind(null) + newWordFragment': false,  // 21846: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > rewind(null) + progress': false,  // 21847: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > fastForward(null) + newWordFragment': false,  // 21871: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > fastForward(null) + progress': false,  // 21872: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > once([0,0,-2]) + newWordFragment': false,  // 21896: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > once([0,0,-2]) + progress': false,  // 21897: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > once([0,0,-2]) + stopBegin': false,  // 21898: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > once([0,0,-2]) + stopFinish': false,  // 21899: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > once([0,0,-2]) + done': false,  // 21900: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > once([0,0,2]) + newWordFragment': false,  // 21921: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > once([0,0,2]) + progress': false,  // 21922: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > once([0,0,2]) + stopBegin': false,  // 21923: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > once([0,0,2]) + stopFinish': false,  // 21924: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > once([0,0,2]) + done': false,  // 21925: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > current(null) + newWordFragment': false,  // 21946: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > current(null) + progress': false,  // 21947: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > current(null) + stopBegin': false,  // 21948: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > current(null) + stopFinish': false,  // 21949: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > current(null) + done': false,  // 21950: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpWords(-3) + newWordFragment': false,  // 22121: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpWords(-3) + progress': false,  // 22122: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpWords(-3) + stopBegin': false,  // 22123: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpWords(-3) + stopFinish': false,  // 22124: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpWords(-3) + done': false,  // 22125: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpWords(-1) + newWordFragment': false,  // 22146: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpWords(-1) + progress': false,  // 22147: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpWords(-1) + stopBegin': false,  // 22148: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpWords(-1) + stopFinish': false,  // 22149: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpWords(-1) + done': false,  // 22150: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpWords(0) + newWordFragment': false,  // 22171: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpWords(0) + progress': false,  // 22172: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpWords(0) + stopBegin': false,  // 22173: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpWords(0) + stopFinish': false,  // 22174: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpWords(0) + done': false,  // 22175: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpWords(4) + newWordFragment': false,  // 22196: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpWords(4) + progress': false,  // 22197: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpWords(4) + stopBegin': false,  // 22198: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpWords(4) + stopFinish': false,  // 22199: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpWords(4) + done': false,  // 22200: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpSentences(-1) + newWordFragment': false,  // 22296: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpSentences(-1) + progress': false,  // 22297: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpSentences(-1) + stopBegin': false,  // 22298: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpSentences(-1) + stopFinish': false,  // 22299: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpSentences(-1) + done': false,  // 22300: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpSentences(0) + newWordFragment': false,  // 22321: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpSentences(0) + progress': false,  // 22322: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpSentences(0) + stopBegin': false,  // 22323: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpSentences(0) + stopFinish': false,  // 22324: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpSentences(0) + done': false,  // 22325: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpSentences(1) + newWordFragment': false,  // 22346: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpSentences(1) + progress': false,  // 22347: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpSentences(1) + stopBegin': false,  // 22348: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpSentences(1) + stopFinish': false,  // 22349: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpSentences(1) + done': false,  // 22350: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpSentences(3) + newWordFragment': false,  // 22371: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpSentences(3) + progress': false,  // 22372: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpSentences(3) + stopBegin': false,  // 22373: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpSentences(3) + stopFinish': false,  // 22374: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > jumpSentences(3) + done': false,  // 22375: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > nextWord(null) + newWordFragment': false,  // 22421: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > nextWord(null) + progress': false,  // 22422: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > nextWord(null) + stopBegin': false,  // 22423: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > nextWord(null) + stopFinish': false,  // 22424: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > nextWord(null) + done': false,  // 22425: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > nextSentence(null) + newWordFragment': false,  // 22446: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > nextSentence(null) + progress': false,  // 22447: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > nextSentence(null) + stopBegin': false,  // 22448: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > nextSentence(null) + stopFinish': false,  // 22449: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > nextSentence(null) + done': false,  // 22450: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > prevWord(null) + newWordFragment': false,  // 22471: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > prevWord(null) + progress': false,  // 22472: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > prevWord(null) + stopBegin': false,  // 22473: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > prevWord(null) + stopFinish': false,  // 22474: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > prevWord(null) + done': false,  // 22475: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > prevSentence(null) + newWordFragment': false,  // 22496: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > prevSentence(null) + progress': false,  // 22497: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > prevSentence(null) + stopBegin': false,  // 22498: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > prevSentence(null) + stopFinish': false,  // 22499: ?? (skip results are different than combos)
		'skip-combos: toggle(null) + done > prevSentence(null) + done': false,  // 22500: ?? (skip results are different than combos)

		// ============== pause ==============
		// ============== stop ==============
		// ============== close ==============
		// ============== revert ==============

		// ============== rewind ==============
		// #? 'stop*|done' > play|toggle() = 'restart*', not 'play*'
		// #? Similarly for things that will immediately trigger 'stop*|done'
		// Here one loop of rewind at the start will lead to 'stop*|done', so 'rewind*|loop*|new|prog' all do too
		'skip-combos: rewind(null) + rewindBegin > play(null) + playBegin': true,  // 12676: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + rewindBegin > play(null) + playFinish': true,  // 12677: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + rewindBegin > play(null) + restartBegin': true,  // 12680: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + rewindBegin > play(null) + restartFinish': true,  // 12681: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + rewindBegin > toggle(null) + playBegin': true,  // 12701: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + rewindBegin > toggle(null) + playFinish': true,  // 12702: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + rewindBegin > toggle(null) + restartBegin': true,  // 12705: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + rewindBegin > toggle(null) + restartFinish': true,  // 12706: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + rewindFinish > play(null) + playBegin': true,  // 13576: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + rewindFinish > play(null) + playFinish': true,  // 13577: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + rewindFinish > play(null) + restartBegin': true,  // 13580: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + rewindFinish > play(null) + restartFinish': true,  // 13581: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + rewindFinish > toggle(null) + playBegin': true,  // 13601: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + rewindFinish > toggle(null) + playFinish': true,  // 13602: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + rewindFinish > toggle(null) + restartBegin': true,  // 13605: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + rewindFinish > toggle(null) + restartFinish': true,  // 13606: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + loopBegin > play(null) + playBegin': true,  // 16276: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + loopBegin > play(null) + playFinish': true,  // 16277: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + loopBegin > play(null) + restartBegin': true,  // 16280: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + loopBegin > play(null) + restartFinish': true,  // 16281: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + loopBegin > toggle(null) + playBegin': true,  // 16301: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + loopBegin > toggle(null) + playFinish': true,  // 16302: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + loopBegin > toggle(null) + restartBegin': true,  // 16305: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + loopBegin > toggle(null) + restartFinish': true,  // 16306: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + loopFinish > play(null) + playBegin': true,  // 17176: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + loopFinish > play(null) + playFinish': true,  // 17177: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + loopFinish > play(null) + restartBegin': true,  // 17180: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + loopFinish > play(null) + restartFinish': true,  // 17181: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + loopFinish > toggle(null) + playBegin': true,  // 17201: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + loopFinish > toggle(null) + playFinish': true,  // 17202: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + loopFinish > toggle(null) + restartBegin': true,  // 17205: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + loopFinish > toggle(null) + restartFinish': true,  // 17206: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + newWordFragment > play(null) + playBegin': true,  // 18076: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + newWordFragment > play(null) + playFinish': true,  // 18077: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + newWordFragment > play(null) + restartBegin': true,  // 18080: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + newWordFragment > play(null) + restartFinish': true,  // 18081: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + newWordFragment > toggle(null) + playBegin': true,  // 18101: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + newWordFragment > toggle(null) + playFinish': true,  // 18102: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + newWordFragment > toggle(null) + restartBegin': true,  // 18105: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + newWordFragment > toggle(null) + restartFinish': true,  // 18106: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + progress > play(null) + playBegin': true,  // 18976: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + progress > play(null) + playFinish': true,  // 18977: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + progress > play(null) + restartBegin': true,  // 18980: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + progress > play(null) + restartFinish': true,  // 18981: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + progress > toggle(null) + playBegin': true,  // 19001: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + progress > toggle(null) + playFinish': true,  // 19002: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + progress > toggle(null) + restartBegin': true,  // 19005: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + progress > toggle(null) + restartFinish': true,  // 19006: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + stopBegin > play(null) + playBegin': true,  // 19876: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + stopBegin > play(null) + playFinish': true,  // 19877: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + stopBegin > play(null) + restartBegin': true,  // 19880: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + stopBegin > play(null) + restartFinish': true,  // 19881: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + stopBegin > toggle(null) + playBegin': true,  // 19901: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + stopBegin > toggle(null) + playFinish': true,  // 19902: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + stopBegin > toggle(null) + restartBegin': true,  // 19905: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + stopBegin > toggle(null) + restartFinish': true,  // 19906: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + stopFinish > play(null) + playBegin': true,  // 20776: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + stopFinish > play(null) + playFinish': true,  // 20777: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + stopFinish > play(null) + restartBegin': true,  // 20780: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + stopFinish > play(null) + restartFinish': true,  // 20781: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + stopFinish > toggle(null) + playBegin': true,  // 20801: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + stopFinish > toggle(null) + playFinish': true,  // 20802: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + stopFinish > toggle(null) + restartBegin': true,  // 20805: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + stopFinish > toggle(null) + restartFinish': true,  // 20806: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + done > play(null) + playBegin': true,  // 21676: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + done > play(null) + playFinish': true,  // 21677: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + done > play(null) + restartBegin': true,  // 21680: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + done > play(null) + restartFinish': true,  // 21681: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + done > toggle(null) + playBegin': true,  // 21701: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + done > toggle(null) + playFinish': true,  // 21702: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + done > toggle(null) + restartBegin': true,  // 21705: ?? (skip results are different than combos)
		'skip-combos: rewind(null) + done > toggle(null) + restartFinish': true,  // 21706: ?? (skip results are different than combos)



		// ============== fastForward ==============
		// A bit like `play` or `toggle`, but here `revert` doesn't lead to play, just to pause
		// Other differences?
		'skip-combos: fastForward(null) + fastForwardBegin > play(null) + progress': false,  // 14497: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardBegin > toggle(null) + playBegin': false,  // 14501: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardBegin > toggle(null) + playFinish': false,  // 14502: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardBegin > toggle(null) + pauseBegin': false,  // 14507: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardBegin > toggle(null) + pauseFinish': false,  // 14508: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardBegin > toggle(null) + revertBegin': false,  // 14513: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardBegin > toggle(null) + revertFinish': false,  // 14514: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardBegin > toggle(null) + loopBegin': false,  // 14519: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardBegin > toggle(null) + loopFinish': false,  // 14520: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardBegin > toggle(null) + newWordFragment': false,  // 14521: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardBegin > toggle(null) + progress': false,  // 14522: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardBegin > toggle(null) + stopBegin': false,  // 14523: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardBegin > toggle(null) + stopFinish': false,  // 14524: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardBegin > toggle(null) + done': false,  // 14525: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardBegin > fastForward(null) + newWordFragment': false,  // 14671: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardBegin > fastForward(null) + progress': false,  // 14672: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardBegin > once([0,0,2]) + newWordFragment': false,  // 14721: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardBegin > once([0,0,2]) + progress': false,  // 14722: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardBegin > current(null) + progress': false,  // 14747: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardBegin > jumpWords(0) + progress': false,  // 14972: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardBegin > jumpWords(4) + progress': false,  // 14997: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardBegin > jumpSentences(0) + progress': false,  // 15122: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardBegin > nextWord(null) + newWordFragment': false,  // 15221: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardBegin > nextWord(null) + progress': false,  // 15222: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardFinish > play(null) + progress': false,  // 15397: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardFinish > toggle(null) + playBegin': false,  // 15401: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardFinish > toggle(null) + playFinish': false,  // 15402: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardFinish > toggle(null) + pauseBegin': false,  // 15407: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardFinish > toggle(null) + pauseFinish': false,  // 15408: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardFinish > toggle(null) + revertBegin': false,  // 15413: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardFinish > toggle(null) + revertFinish': false,  // 15414: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardFinish > toggle(null) + loopBegin': false,  // 15419: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardFinish > toggle(null) + loopFinish': false,  // 15420: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardFinish > toggle(null) + newWordFragment': false,  // 15421: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardFinish > toggle(null) + progress': false,  // 15422: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardFinish > toggle(null) + stopBegin': false,  // 15423: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardFinish > toggle(null) + stopFinish': false,  // 15424: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardFinish > toggle(null) + done': false,  // 15425: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardFinish > fastForward(null) + newWordFragment': false,  // 15571: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardFinish > fastForward(null) + progress': false,  // 15572: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardFinish > once([0,0,2]) + newWordFragment': false,  // 15621: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardFinish > once([0,0,2]) + progress': false,  // 15622: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardFinish > current(null) + progress': false,  // 15647: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardFinish > jumpWords(0) + progress': false,  // 15872: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardFinish > jumpWords(4) + progress': false,  // 15897: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardFinish > jumpSentences(0) + progress': false,  // 16022: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardFinish > nextWord(null) + newWordFragment': false,  // 16121: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + fastForwardFinish > nextWord(null) + progress': false,  // 16122: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopBegin > play(null) + progress': false,  // 16297: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopBegin > toggle(null) + playBegin': false,  // 16301: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopBegin > toggle(null) + playFinish': false,  // 16302: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopBegin > toggle(null) + pauseBegin': false,  // 16307: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopBegin > toggle(null) + pauseFinish': false,  // 16308: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopBegin > toggle(null) + revertBegin': false,  // 16313: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopBegin > toggle(null) + revertFinish': false,  // 16314: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopBegin > toggle(null) + loopBegin': false,  // 16319: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopBegin > toggle(null) + loopFinish': false,  // 16320: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopBegin > toggle(null) + newWordFragment': false,  // 16321: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopBegin > toggle(null) + progress': false,  // 16322: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopBegin > toggle(null) + stopBegin': false,  // 16323: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopBegin > toggle(null) + stopFinish': false,  // 16324: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopBegin > toggle(null) + done': false,  // 16325: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopBegin > fastForward(null) + newWordFragment': false,  // 16471: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopBegin > fastForward(null) + progress': false,  // 16472: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopBegin > once([0,0,2]) + newWordFragment': false,  // 16521: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopBegin > once([0,0,2]) + progress': false,  // 16522: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopBegin > current(null) + progress': false,  // 16547: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopBegin > jumpWords(0) + progress': false,  // 16772: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopBegin > jumpWords(4) + progress': false,  // 16797: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopBegin > jumpSentences(0) + progress': false,  // 16922: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopBegin > nextWord(null) + newWordFragment': false,  // 17021: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopBegin > nextWord(null) + progress': false,  // 17022: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopFinish > play(null) + progress': false,  // 17197: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopFinish > toggle(null) + playBegin': false,  // 17201: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopFinish > toggle(null) + playFinish': false,  // 17202: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopFinish > toggle(null) + pauseBegin': false,  // 17207: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopFinish > toggle(null) + pauseFinish': false,  // 17208: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopFinish > toggle(null) + revertBegin': false,  // 17213: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopFinish > toggle(null) + revertFinish': false,  // 17214: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopFinish > toggle(null) + loopBegin': false,  // 17219: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopFinish > toggle(null) + loopFinish': false,  // 17220: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopFinish > toggle(null) + newWordFragment': false,  // 17221: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopFinish > toggle(null) + progress': false,  // 17222: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopFinish > toggle(null) + stopBegin': false,  // 17223: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopFinish > toggle(null) + stopFinish': false,  // 17224: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopFinish > toggle(null) + done': false,  // 17225: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopFinish > fastForward(null) + newWordFragment': false,  // 17371: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopFinish > fastForward(null) + progress': false,  // 17372: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopFinish > once([0,0,2]) + newWordFragment': false,  // 17421: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopFinish > once([0,0,2]) + progress': false,  // 17422: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopFinish > current(null) + progress': false,  // 17447: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopFinish > jumpWords(0) + progress': false,  // 17672: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopFinish > jumpWords(4) + progress': false,  // 17697: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopFinish > jumpSentences(0) + progress': false,  // 17822: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopFinish > nextWord(null) + newWordFragment': false,  // 17921: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + loopFinish > nextWord(null) + progress': false,  // 17922: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + newWordFragment > play(null) + progress': false,  // 18097: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + newWordFragment > toggle(null) + playBegin': false,  // 18101: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + newWordFragment > toggle(null) + playFinish': false,  // 18102: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + newWordFragment > toggle(null) + pauseBegin': false,  // 18107: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + newWordFragment > toggle(null) + pauseFinish': false,  // 18108: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + newWordFragment > toggle(null) + revertBegin': false,  // 18113: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + newWordFragment > toggle(null) + revertFinish': false,  // 18114: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + newWordFragment > toggle(null) + loopBegin': false,  // 18119: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + newWordFragment > toggle(null) + loopFinish': false,  // 18120: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + newWordFragment > toggle(null) + newWordFragment': false,  // 18121: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + newWordFragment > toggle(null) + progress': false,  // 18122: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + newWordFragment > toggle(null) + stopBegin': false,  // 18123: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + newWordFragment > toggle(null) + stopFinish': false,  // 18124: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + newWordFragment > toggle(null) + done': false,  // 18125: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + newWordFragment > fastForward(null) + newWordFragment': false,  // 18271: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + newWordFragment > fastForward(null) + progress': false,  // 18272: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + newWordFragment > once([0,0,2]) + newWordFragment': false,  // 18321: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + newWordFragment > once([0,0,2]) + progress': false,  // 18322: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + newWordFragment > current(null) + progress': false,  // 18347: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + newWordFragment > jumpWords(0) + progress': false,  // 18572: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + newWordFragment > jumpWords(4) + progress': false,  // 18597: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + newWordFragment > jumpSentences(0) + progress': false,  // 18722: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + newWordFragment > nextWord(null) + newWordFragment': false,  // 18821: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + newWordFragment > nextWord(null) + progress': false,  // 18822: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + progress > play(null) + progress': false,  // 18997: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + progress > toggle(null) + playBegin': false,  // 19001: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + progress > toggle(null) + playFinish': false,  // 19002: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + progress > toggle(null) + pauseBegin': false,  // 19007: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + progress > toggle(null) + pauseFinish': false,  // 19008: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + progress > toggle(null) + revertBegin': false,  // 19013: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + progress > toggle(null) + revertFinish': false,  // 19014: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + progress > toggle(null) + loopBegin': false,  // 19019: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + progress > toggle(null) + loopFinish': false,  // 19020: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + progress > toggle(null) + newWordFragment': false,  // 19021: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + progress > toggle(null) + progress': false,  // 19022: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + progress > toggle(null) + stopBegin': false,  // 19023: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + progress > toggle(null) + stopFinish': false,  // 19024: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + progress > toggle(null) + done': false,  // 19025: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + progress > fastForward(null) + newWordFragment': false,  // 19171: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + progress > fastForward(null) + progress': false,  // 19172: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + progress > once([0,0,2]) + newWordFragment': false,  // 19221: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + progress > once([0,0,2]) + progress': false,  // 19222: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + progress > current(null) + progress': false,  // 19247: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + progress > jumpWords(0) + progress': false,  // 19472: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + progress > jumpWords(4) + progress': false,  // 19497: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + progress > jumpSentences(0) + progress': false,  // 19622: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + progress > nextWord(null) + newWordFragment': false,  // 19721: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + progress > nextWord(null) + progress': false,  // 19722: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > play(null) + playBegin': false,  // 19876: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > play(null) + playFinish': false,  // 19877: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > play(null) + restartBegin': false,  // 19880: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > play(null) + restartFinish': false,  // 19881: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > toggle(null) + playBegin': false,  // 19901: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > toggle(null) + playFinish': false,  // 19902: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > toggle(null) + restartBegin': false,  // 19905: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > toggle(null) + restartFinish': false,  // 19906: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > rewind(null) + newWordFragment': false,  // 20046: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > rewind(null) + progress': false,  // 20047: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > fastForward(null) + newWordFragment': false,  // 20071: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > fastForward(null) + progress': false,  // 20072: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > once([0,0,-2]) + newWordFragment': false,  // 20096: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > once([0,0,-2]) + progress': false,  // 20097: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > once([0,0,-2]) + stopBegin': false,  // 20098: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > once([0,0,-2]) + stopFinish': false,  // 20099: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > once([0,0,-2]) + done': false,  // 20100: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > once([0,0,2]) + newWordFragment': false,  // 20121: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > once([0,0,2]) + progress': false,  // 20122: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > once([0,0,2]) + stopBegin': false,  // 20123: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > once([0,0,2]) + stopFinish': false,  // 20124: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > once([0,0,2]) + done': false,  // 20125: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > current(null) + newWordFragment': false,  // 20146: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > current(null) + progress': false,  // 20147: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > current(null) + stopBegin': false,  // 20148: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > current(null) + stopFinish': false,  // 20149: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > current(null) + done': false,  // 20150: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpWords(-3) + newWordFragment': false,  // 20321: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpWords(-3) + progress': false,  // 20322: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpWords(-3) + stopBegin': false,  // 20323: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpWords(-3) + stopFinish': false,  // 20324: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpWords(-3) + done': false,  // 20325: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpWords(-1) + newWordFragment': false,  // 20346: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpWords(-1) + progress': false,  // 20347: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpWords(-1) + stopBegin': false,  // 20348: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpWords(-1) + stopFinish': false,  // 20349: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpWords(-1) + done': false,  // 20350: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpWords(0) + newWordFragment': false,  // 20371: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpWords(0) + progress': false,  // 20372: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpWords(0) + stopBegin': false,  // 20373: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpWords(0) + stopFinish': false,  // 20374: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpWords(0) + done': false,  // 20375: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpWords(4) + newWordFragment': false,  // 20396: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpWords(4) + progress': false,  // 20397: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpWords(4) + stopBegin': false,  // 20398: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpWords(4) + stopFinish': false,  // 20399: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpWords(4) + done': false,  // 20400: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpSentences(-1) + newWordFragment': false,  // 20496: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpSentences(-1) + progress': false,  // 20497: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpSentences(-1) + stopBegin': false,  // 20498: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpSentences(-1) + stopFinish': false,  // 20499: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpSentences(-1) + done': false,  // 20500: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpSentences(0) + newWordFragment': false,  // 20521: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpSentences(0) + progress': false,  // 20522: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpSentences(0) + stopBegin': false,  // 20523: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpSentences(0) + stopFinish': false,  // 20524: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpSentences(0) + done': false,  // 20525: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpSentences(1) + newWordFragment': false,  // 20546: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpSentences(1) + progress': false,  // 20547: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpSentences(1) + stopBegin': false,  // 20548: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpSentences(1) + stopFinish': false,  // 20549: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpSentences(1) + done': false,  // 20550: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpSentences(3) + newWordFragment': false,  // 20571: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpSentences(3) + progress': false,  // 20572: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpSentences(3) + stopBegin': false,  // 20573: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpSentences(3) + stopFinish': false,  // 20574: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > jumpSentences(3) + done': false,  // 20575: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > nextWord(null) + newWordFragment': false,  // 20621: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > nextWord(null) + progress': false,  // 20622: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > nextWord(null) + stopBegin': false,  // 20623: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > nextWord(null) + stopFinish': false,  // 20624: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > nextWord(null) + done': false,  // 20625: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > nextSentence(null) + newWordFragment': false,  // 20646: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > nextSentence(null) + progress': false,  // 20647: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > nextSentence(null) + stopBegin': false,  // 20648: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > nextSentence(null) + stopFinish': false,  // 20649: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > nextSentence(null) + done': false,  // 20650: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > prevWord(null) + newWordFragment': false,  // 20671: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > prevWord(null) + progress': false,  // 20672: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > prevWord(null) + stopBegin': false,  // 20673: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > prevWord(null) + stopFinish': false,  // 20674: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > prevWord(null) + done': false,  // 20675: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > prevSentence(null) + newWordFragment': false,  // 20696: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > prevSentence(null) + progress': false,  // 20697: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > prevSentence(null) + stopBegin': false,  // 20698: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > prevSentence(null) + stopFinish': false,  // 20699: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopBegin > prevSentence(null) + done': false,  // 20700: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > play(null) + playBegin': false,  // 20776: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > play(null) + playFinish': false,  // 20777: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > play(null) + restartBegin': false,  // 20780: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > play(null) + restartFinish': false,  // 20781: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > toggle(null) + playBegin': false,  // 20801: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > toggle(null) + playFinish': false,  // 20802: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > toggle(null) + restartBegin': false,  // 20805: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > toggle(null) + restartFinish': false,  // 20806: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > rewind(null) + newWordFragment': false,  // 20946: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > rewind(null) + progress': false,  // 20947: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > fastForward(null) + newWordFragment': false,  // 20971: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > fastForward(null) + progress': false,  // 20972: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > once([0,0,-2]) + newWordFragment': false,  // 20996: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > once([0,0,-2]) + progress': false,  // 20997: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > once([0,0,-2]) + stopBegin': false,  // 20998: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > once([0,0,-2]) + stopFinish': false,  // 20999: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > once([0,0,-2]) + done': false,  // 21000: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > once([0,0,2]) + newWordFragment': false,  // 21021: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > once([0,0,2]) + progress': false,  // 21022: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > once([0,0,2]) + stopBegin': false,  // 21023: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > once([0,0,2]) + stopFinish': false,  // 21024: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > once([0,0,2]) + done': false,  // 21025: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > current(null) + newWordFragment': false,  // 21046: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > current(null) + progress': false,  // 21047: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > current(null) + stopBegin': false,  // 21048: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > current(null) + stopFinish': false,  // 21049: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > current(null) + done': false,  // 21050: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpWords(-3) + newWordFragment': false,  // 21221: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpWords(-3) + progress': false,  // 21222: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpWords(-3) + stopBegin': false,  // 21223: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpWords(-3) + stopFinish': false,  // 21224: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpWords(-3) + done': false,  // 21225: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpWords(-1) + newWordFragment': false,  // 21246: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpWords(-1) + progress': false,  // 21247: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpWords(-1) + stopBegin': false,  // 21248: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpWords(-1) + stopFinish': false,  // 21249: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpWords(-1) + done': false,  // 21250: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpWords(0) + newWordFragment': false,  // 21271: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpWords(0) + progress': false,  // 21272: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpWords(0) + stopBegin': false,  // 21273: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpWords(0) + stopFinish': false,  // 21274: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpWords(0) + done': false,  // 21275: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpWords(4) + newWordFragment': false,  // 21296: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpWords(4) + progress': false,  // 21297: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpWords(4) + stopBegin': false,  // 21298: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpWords(4) + stopFinish': false,  // 21299: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpWords(4) + done': false,  // 21300: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpSentences(-1) + newWordFragment': false,  // 21396: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpSentences(-1) + progress': false,  // 21397: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpSentences(-1) + stopBegin': false,  // 21398: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpSentences(-1) + stopFinish': false,  // 21399: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpSentences(-1) + done': false,  // 21400: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpSentences(0) + newWordFragment': false,  // 21421: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpSentences(0) + progress': false,  // 21422: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpSentences(0) + stopBegin': false,  // 21423: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpSentences(0) + stopFinish': false,  // 21424: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpSentences(0) + done': false,  // 21425: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpSentences(1) + newWordFragment': false,  // 21446: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpSentences(1) + progress': false,  // 21447: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpSentences(1) + stopBegin': false,  // 21448: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpSentences(1) + stopFinish': false,  // 21449: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpSentences(1) + done': false,  // 21450: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpSentences(3) + newWordFragment': false,  // 21471: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpSentences(3) + progress': false,  // 21472: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpSentences(3) + stopBegin': false,  // 21473: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpSentences(3) + stopFinish': false,  // 21474: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > jumpSentences(3) + done': false,  // 21475: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > nextWord(null) + newWordFragment': false,  // 21521: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > nextWord(null) + progress': false,  // 21522: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > nextWord(null) + stopBegin': false,  // 21523: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > nextWord(null) + stopFinish': false,  // 21524: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > nextWord(null) + done': false,  // 21525: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > nextSentence(null) + newWordFragment': false,  // 21546: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > nextSentence(null) + progress': false,  // 21547: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > nextSentence(null) + stopBegin': false,  // 21548: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > nextSentence(null) + stopFinish': false,  // 21549: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > nextSentence(null) + done': false,  // 21550: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > prevWord(null) + newWordFragment': false,  // 21571: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > prevWord(null) + progress': false,  // 21572: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > prevWord(null) + stopBegin': false,  // 21573: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > prevWord(null) + stopFinish': false,  // 21574: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > prevWord(null) + done': false,  // 21575: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > prevSentence(null) + newWordFragment': false,  // 21596: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > prevSentence(null) + progress': false,  // 21597: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > prevSentence(null) + stopBegin': false,  // 21598: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > prevSentence(null) + stopFinish': false,  // 21599: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + stopFinish > prevSentence(null) + done': false,  // 21600: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > play(null) + playBegin': false,  // 21676: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > play(null) + playFinish': false,  // 21677: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > play(null) + restartBegin': false,  // 21680: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > play(null) + restartFinish': false,  // 21681: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > toggle(null) + playBegin': false,  // 21701: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > toggle(null) + playFinish': false,  // 21702: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > toggle(null) + restartBegin': false,  // 21705: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > toggle(null) + restartFinish': false,  // 21706: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > rewind(null) + newWordFragment': false,  // 21846: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > rewind(null) + progress': false,  // 21847: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > fastForward(null) + newWordFragment': false,  // 21871: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > fastForward(null) + progress': false,  // 21872: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > once([0,0,-2]) + newWordFragment': false,  // 21896: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > once([0,0,-2]) + progress': false,  // 21897: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > once([0,0,-2]) + stopBegin': false,  // 21898: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > once([0,0,-2]) + stopFinish': false,  // 21899: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > once([0,0,-2]) + done': false,  // 21900: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > once([0,0,2]) + newWordFragment': false,  // 21921: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > once([0,0,2]) + progress': false,  // 21922: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > once([0,0,2]) + stopBegin': false,  // 21923: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > once([0,0,2]) + stopFinish': false,  // 21924: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > once([0,0,2]) + done': false,  // 21925: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > current(null) + newWordFragment': false,  // 21946: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > current(null) + progress': false,  // 21947: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > current(null) + stopBegin': false,  // 21948: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > current(null) + stopFinish': false,  // 21949: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > current(null) + done': false,  // 21950: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpWords(-3) + newWordFragment': false,  // 22121: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpWords(-3) + progress': false,  // 22122: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpWords(-3) + stopBegin': false,  // 22123: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpWords(-3) + stopFinish': false,  // 22124: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpWords(-3) + done': false,  // 22125: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpWords(-1) + newWordFragment': false,  // 22146: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpWords(-1) + progress': false,  // 22147: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpWords(-1) + stopBegin': false,  // 22148: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpWords(-1) + stopFinish': false,  // 22149: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpWords(-1) + done': false,  // 22150: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpWords(0) + newWordFragment': false,  // 22171: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpWords(0) + progress': false,  // 22172: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpWords(0) + stopBegin': false,  // 22173: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpWords(0) + stopFinish': false,  // 22174: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpWords(0) + done': false,  // 22175: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpWords(4) + newWordFragment': false,  // 22196: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpWords(4) + progress': false,  // 22197: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpWords(4) + stopBegin': false,  // 22198: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpWords(4) + stopFinish': false,  // 22199: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpWords(4) + done': false,  // 22200: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpSentences(-1) + newWordFragment': false,  // 22296: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpSentences(-1) + progress': false,  // 22297: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpSentences(-1) + stopBegin': false,  // 22298: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpSentences(-1) + stopFinish': false,  // 22299: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpSentences(-1) + done': false,  // 22300: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpSentences(0) + newWordFragment': false,  // 22321: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpSentences(0) + progress': false,  // 22322: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpSentences(0) + stopBegin': false,  // 22323: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpSentences(0) + stopFinish': false,  // 22324: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpSentences(0) + done': false,  // 22325: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpSentences(1) + newWordFragment': false,  // 22346: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpSentences(1) + progress': false,  // 22347: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpSentences(1) + stopBegin': false,  // 22348: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpSentences(1) + stopFinish': false,  // 22349: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpSentences(1) + done': false,  // 22350: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpSentences(3) + newWordFragment': false,  // 22371: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpSentences(3) + progress': false,  // 22372: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpSentences(3) + stopBegin': false,  // 22373: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpSentences(3) + stopFinish': false,  // 22374: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > jumpSentences(3) + done': false,  // 22375: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > nextWord(null) + newWordFragment': false,  // 22421: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > nextWord(null) + progress': false,  // 22422: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > nextWord(null) + stopBegin': false,  // 22423: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > nextWord(null) + stopFinish': false,  // 22424: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > nextWord(null) + done': false,  // 22425: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > nextSentence(null) + newWordFragment': false,  // 22446: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > nextSentence(null) + progress': false,  // 22447: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > nextSentence(null) + stopBegin': false,  // 22448: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > nextSentence(null) + stopFinish': false,  // 22449: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > nextSentence(null) + done': false,  // 22450: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > prevWord(null) + newWordFragment': false,  // 22471: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > prevWord(null) + progress': false,  // 22472: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > prevWord(null) + stopBegin': false,  // 22473: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > prevWord(null) + stopFinish': false,  // 22474: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > prevWord(null) + done': false,  // 22475: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > prevSentence(null) + newWordFragment': false,  // 22496: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > prevSentence(null) + progress': false,  // 22497: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > prevSentence(null) + stopBegin': false,  // 22498: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > prevSentence(null) + stopFinish': false,  // 22499: ?? (skip results are different than combos)
		'skip-combos: fastForward(null) + done > prevSentence(null) + done': false,  // 22500: ?? (skip results are different than combos)



		// ============== once([0,0,-2]) ==============
		// #? anything(-) + 'once*|loop*|new|prog|stop*|done' (that immediately leads to done) > play|toggle() = restart instead of play
		'skip-combos: once([0,0,-2]) + onceBegin > play(null) + playBegin': true,  // 9076: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + onceBegin > play(null) + playFinish': true,  // 9077: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + onceBegin > play(null) + restartBegin': true,  // 9080: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + onceBegin > play(null) + restartFinish': true,  // 9081: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + onceBegin > toggle(null) + playBegin': true,  // 9101: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + onceBegin > toggle(null) + playFinish': true,  // 9102: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + onceBegin > toggle(null) + restartBegin': true,  // 9105: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + onceBegin > toggle(null) + restartFinish': true,  // 9106: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + onceFinish > play(null) + playBegin': true,  // 9976: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + onceFinish > play(null) + playFinish': true,  // 9977: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + onceFinish > play(null) + restartBegin': true,  // 9980: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + onceFinish > play(null) + restartFinish': true,  // 9981: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + onceFinish > toggle(null) + playBegin': true,  // 10001: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + onceFinish > toggle(null) + playFinish': true,  // 10002: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + onceFinish > toggle(null) + restartBegin': true,  // 10005: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + onceFinish > toggle(null) + restartFinish': true,  // 10006: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + loopBegin > play(null) + playBegin': true,  // 16276: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + loopBegin > play(null) + playFinish': true,  // 16277: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + loopBegin > play(null) + restartBegin': true,  // 16280: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + loopBegin > play(null) + restartFinish': true,  // 16281: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + loopBegin > toggle(null) + playBegin': true,  // 16301: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + loopBegin > toggle(null) + playFinish': true,  // 16302: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + loopBegin > toggle(null) + restartBegin': true,  // 16305: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + loopBegin > toggle(null) + restartFinish': true,  // 16306: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + loopFinish > play(null) + playBegin': true,  // 17176: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + loopFinish > play(null) + playFinish': true,  // 17177: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + loopFinish > play(null) + restartBegin': true,  // 17180: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + loopFinish > play(null) + restartFinish': true,  // 17181: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + loopFinish > toggle(null) + playBegin': true,  // 17201: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + loopFinish > toggle(null) + playFinish': true,  // 17202: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + loopFinish > toggle(null) + restartBegin': true,  // 17205: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + loopFinish > toggle(null) + restartFinish': true,  // 17206: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + newWordFragment > play(null) + playBegin': true,  // 18076: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + newWordFragment > play(null) + playFinish': true,  // 18077: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + newWordFragment > play(null) + restartBegin': true,  // 18080: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + newWordFragment > play(null) + restartFinish': true,  // 18081: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + newWordFragment > toggle(null) + playBegin': true,  // 18101: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + newWordFragment > toggle(null) + playFinish': true,  // 18102: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + newWordFragment > toggle(null) + restartBegin': true,  // 18105: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + newWordFragment > toggle(null) + restartFinish': true,  // 18106: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + progress > play(null) + playBegin': true,  // 18976: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + progress > play(null) + playFinish': true,  // 18977: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + progress > play(null) + restartBegin': true,  // 18980: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + progress > play(null) + restartFinish': true,  // 18981: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + progress > toggle(null) + playBegin': true,  // 19001: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + progress > toggle(null) + playFinish': true,  // 19002: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + progress > toggle(null) + restartBegin': true,  // 19005: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + progress > toggle(null) + restartFinish': true,  // 19006: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + stopBegin > play(null) + playBegin': true,  // 19876: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + stopBegin > play(null) + playFinish': true,  // 19877: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + stopBegin > play(null) + restartBegin': true,  // 19880: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + stopBegin > play(null) + restartFinish': true,  // 19881: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + stopBegin > toggle(null) + playBegin': true,  // 19901: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + stopBegin > toggle(null) + playFinish': true,  // 19902: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + stopBegin > toggle(null) + restartBegin': true,  // 19905: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + stopBegin > toggle(null) + restartFinish': true,  // 19906: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + stopFinish > play(null) + playBegin': true,  // 20776: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + stopFinish > play(null) + playFinish': true,  // 20777: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + stopFinish > play(null) + restartBegin': true,  // 20780: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + stopFinish > play(null) + restartFinish': true,  // 20781: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + stopFinish > toggle(null) + playBegin': true,  // 20801: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + stopFinish > toggle(null) + playFinish': true,  // 20802: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + stopFinish > toggle(null) + restartBegin': true,  // 20805: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + stopFinish > toggle(null) + restartFinish': true,  // 20806: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + done > play(null) + playBegin': true,  // 21676: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + done > play(null) + playFinish': true,  // 21677: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + done > play(null) + restartBegin': true,  // 21680: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + done > play(null) + restartFinish': true,  // 21681: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + done > toggle(null) + playBegin': true,  // 21701: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + done > toggle(null) + playFinish': true,  // 21702: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + done > toggle(null) + restartBegin': true,  // 21705: ?? (skip results are different than combos)
		'skip-combos: once([0,0,-2]) + done > toggle(null) + restartFinish': true,  // 21706: ?? (skip results are different than combos)




		// ============== once([0,0,2]) ==============
		// #? once(+) + any > some relative loopers(+|0) + new|prog = different output
		// #? once(+) + any > toggle = reverting and pause stuff, not play stuff
		'skip-combos: once([0,0,2]) + onceBegin > play(null) + progress': false,  // 9097: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceBegin > toggle(null) + playBegin': false,  // 9101: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceBegin > toggle(null) + playFinish': false,  // 9102: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceBegin > toggle(null) + pauseBegin': false,  // 9107: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceBegin > toggle(null) + pauseFinish': false,  // 9108: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceBegin > toggle(null) + revertBegin': false,  // 9113: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceBegin > toggle(null) + revertFinish': false,  // 9114: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceBegin > toggle(null) + loopBegin': false,  // 9119: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceBegin > toggle(null) + loopFinish': false,  // 9120: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceBegin > toggle(null) + newWordFragment': false,  // 9121: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceBegin > toggle(null) + progress': false,  // 9122: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceBegin > toggle(null) + stopBegin': false,  // 9123: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceBegin > toggle(null) + stopFinish': false,  // 9124: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceBegin > toggle(null) + done': false,  // 9125: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceBegin > fastForward(null) + newWordFragment': false,  // 9271: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceBegin > fastForward(null) + progress': false,  // 9272: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceBegin > once([0,0,2]) + newWordFragment': false,  // 9321: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceBegin > once([0,0,2]) + progress': false,  // 9322: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceBegin > current(null) + progress': false,  // 9347: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceBegin > jumpWords(0) + progress': false,  // 9572: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceBegin > jumpWords(4) + progress': false,  // 9597: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceBegin > jumpSentences(0) + progress': false,  // 9722: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceBegin > nextWord(null) + newWordFragment': false,  // 9821: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceBegin > nextWord(null) + progress': false,  // 9822: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceFinish > play(null) + progress': false,  // 9997: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceFinish > toggle(null) + playBegin': false,  // 10001: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceFinish > toggle(null) + playFinish': false,  // 10002: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceFinish > toggle(null) + pauseBegin': false,  // 10007: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceFinish > toggle(null) + pauseFinish': false,  // 10008: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceFinish > toggle(null) + revertBegin': false,  // 10013: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceFinish > toggle(null) + revertFinish': false,  // 10014: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceFinish > toggle(null) + loopBegin': false,  // 10019: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceFinish > toggle(null) + loopFinish': false,  // 10020: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceFinish > toggle(null) + newWordFragment': false,  // 10021: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceFinish > toggle(null) + progress': false,  // 10022: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceFinish > toggle(null) + stopBegin': false,  // 10023: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceFinish > toggle(null) + stopFinish': false,  // 10024: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceFinish > toggle(null) + done': false,  // 10025: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceFinish > fastForward(null) + newWordFragment': false,  // 10171: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceFinish > fastForward(null) + progress': false,  // 10172: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceFinish > once([0,0,2]) + newWordFragment': false,  // 10221: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceFinish > once([0,0,2]) + progress': false,  // 10222: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceFinish > current(null) + progress': false,  // 10247: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceFinish > jumpWords(0) + progress': false,  // 10472: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceFinish > jumpWords(4) + progress': false,  // 10497: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceFinish > jumpSentences(0) + progress': false,  // 10622: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceFinish > nextWord(null) + newWordFragment': false,  // 10721: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + onceFinish > nextWord(null) + progress': false,  // 10722: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopBegin > play(null) + progress': false,  // 16297: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopBegin > toggle(null) + playBegin': false,  // 16301: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopBegin > toggle(null) + playFinish': false,  // 16302: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopBegin > toggle(null) + pauseBegin': false,  // 16307: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopBegin > toggle(null) + pauseFinish': false,  // 16308: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopBegin > toggle(null) + revertBegin': false,  // 16313: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopBegin > toggle(null) + revertFinish': false,  // 16314: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopBegin > toggle(null) + loopBegin': false,  // 16319: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopBegin > toggle(null) + loopFinish': false,  // 16320: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopBegin > toggle(null) + newWordFragment': false,  // 16321: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopBegin > toggle(null) + progress': false,  // 16322: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopBegin > toggle(null) + stopBegin': false,  // 16323: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopBegin > toggle(null) + stopFinish': false,  // 16324: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopBegin > toggle(null) + done': false,  // 16325: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopBegin > fastForward(null) + newWordFragment': false,  // 16471: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopBegin > fastForward(null) + progress': false,  // 16472: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopBegin > once([0,0,2]) + newWordFragment': false,  // 16521: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopBegin > once([0,0,2]) + progress': false,  // 16522: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopBegin > current(null) + progress': false,  // 16547: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopBegin > jumpWords(0) + progress': false,  // 16772: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopBegin > jumpWords(4) + progress': false,  // 16797: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopBegin > jumpSentences(0) + progress': false,  // 16922: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopBegin > nextWord(null) + newWordFragment': false,  // 17021: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopBegin > nextWord(null) + progress': false,  // 17022: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopFinish > play(null) + progress': false,  // 17197: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopFinish > toggle(null) + playBegin': false,  // 17201: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopFinish > toggle(null) + playFinish': false,  // 17202: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopFinish > toggle(null) + pauseBegin': false,  // 17207: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopFinish > toggle(null) + pauseFinish': false,  // 17208: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopFinish > toggle(null) + revertBegin': false,  // 17213: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopFinish > toggle(null) + revertFinish': false,  // 17214: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopFinish > toggle(null) + loopBegin': false,  // 17219: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopFinish > toggle(null) + loopFinish': false,  // 17220: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopFinish > toggle(null) + newWordFragment': false,  // 17221: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopFinish > toggle(null) + progress': false,  // 17222: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopFinish > toggle(null) + stopBegin': false,  // 17223: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopFinish > toggle(null) + stopFinish': false,  // 17224: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopFinish > toggle(null) + done': false,  // 17225: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopFinish > fastForward(null) + newWordFragment': false,  // 17371: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopFinish > fastForward(null) + progress': false,  // 17372: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopFinish > once([0,0,2]) + newWordFragment': false,  // 17421: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopFinish > once([0,0,2]) + progress': false,  // 17422: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopFinish > current(null) + progress': false,  // 17447: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopFinish > jumpWords(0) + progress': false,  // 17672: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopFinish > jumpWords(4) + progress': false,  // 17697: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopFinish > jumpSentences(0) + progress': false,  // 17822: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopFinish > nextWord(null) + newWordFragment': false,  // 17921: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + loopFinish > nextWord(null) + progress': false,  // 17922: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + newWordFragment > play(null) + progress': false,  // 18097: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + newWordFragment > toggle(null) + playBegin': false,  // 18101: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + newWordFragment > toggle(null) + playFinish': false,  // 18102: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + newWordFragment > toggle(null) + pauseBegin': false,  // 18107: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + newWordFragment > toggle(null) + pauseFinish': false,  // 18108: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + newWordFragment > toggle(null) + revertBegin': false,  // 18113: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + newWordFragment > toggle(null) + revertFinish': false,  // 18114: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + newWordFragment > toggle(null) + loopBegin': false,  // 18119: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + newWordFragment > toggle(null) + loopFinish': false,  // 18120: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + newWordFragment > toggle(null) + newWordFragment': false,  // 18121: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + newWordFragment > toggle(null) + progress': false,  // 18122: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + newWordFragment > toggle(null) + stopBegin': false,  // 18123: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + newWordFragment > toggle(null) + stopFinish': false,  // 18124: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + newWordFragment > toggle(null) + done': false,  // 18125: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + newWordFragment > fastForward(null) + newWordFragment': false,  // 18271: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + newWordFragment > fastForward(null) + progress': false,  // 18272: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + newWordFragment > once([0,0,2]) + newWordFragment': false,  // 18321: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + newWordFragment > once([0,0,2]) + progress': false,  // 18322: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + newWordFragment > current(null) + progress': false,  // 18347: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + newWordFragment > jumpWords(0) + progress': false,  // 18572: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + newWordFragment > jumpWords(4) + progress': false,  // 18597: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + newWordFragment > jumpSentences(0) + progress': false,  // 18722: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + newWordFragment > nextWord(null) + newWordFragment': false,  // 18821: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + newWordFragment > nextWord(null) + progress': false,  // 18822: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + progress > play(null) + progress': false,  // 18997: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + progress > toggle(null) + playBegin': false,  // 19001: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + progress > toggle(null) + playFinish': false,  // 19002: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + progress > toggle(null) + pauseBegin': false,  // 19007: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + progress > toggle(null) + pauseFinish': false,  // 19008: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + progress > toggle(null) + revertBegin': false,  // 19013: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + progress > toggle(null) + revertFinish': false,  // 19014: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + progress > toggle(null) + loopBegin': false,  // 19019: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + progress > toggle(null) + loopFinish': false,  // 19020: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + progress > toggle(null) + newWordFragment': false,  // 19021: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + progress > toggle(null) + progress': false,  // 19022: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + progress > toggle(null) + stopBegin': false,  // 19023: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + progress > toggle(null) + stopFinish': false,  // 19024: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + progress > toggle(null) + done': false,  // 19025: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + progress > fastForward(null) + newWordFragment': false,  // 19171: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + progress > fastForward(null) + progress': false,  // 19172: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + progress > once([0,0,2]) + newWordFragment': false,  // 19221: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + progress > once([0,0,2]) + progress': false,  // 19222: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + progress > current(null) + progress': false,  // 19247: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + progress > jumpWords(0) + progress': false,  // 19472: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + progress > jumpWords(4) + progress': false,  // 19497: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + progress > jumpSentences(0) + progress': false,  // 19622: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + progress > nextWord(null) + newWordFragment': false,  // 19721: ?? (skip results are different than combos)
		'skip-combos: once([0,0,2]) + progress > nextWord(null) + progress': false,  // 19722: ?? (skip results are different than combos)



		// ============== current ==============
		// #? current() reverts to 'pause', so toggle = 'revert*|pause*' stuff not 'play*|...' stuff
		'skip-combos: current(null) + onceBegin > toggle(null) + playBegin': true,  // 9101: ?? (skip results are different than combos)
		'skip-combos: current(null) + onceBegin > toggle(null) + playFinish': true,  // 9102: ?? (skip results are different than combos)
		'skip-combos: current(null) + onceBegin > toggle(null) + pauseBegin': true,  // 9107: ?? (skip results are different than combos)
		'skip-combos: current(null) + onceBegin > toggle(null) + pauseFinish': true,  // 9108: ?? (skip results are different than combos)
		'skip-combos: current(null) + onceBegin > toggle(null) + revertBegin': true,  // 9113: ?? (skip results are different than combos)
		'skip-combos: current(null) + onceBegin > toggle(null) + revertFinish': true,  // 9114: ?? (skip results are different than combos)
		'skip-combos: current(null) + onceBegin > toggle(null) + loopBegin': true,  // 9119: ?? (skip results are different than combos)
		'skip-combos: current(null) + onceBegin > toggle(null) + loopFinish': true,  // 9120: ?? (skip results are different than combos)
		'skip-combos: current(null) + onceBegin > toggle(null) + newWordFragment': true,  // 9121: ?? (skip results are different than combos)
		'skip-combos: current(null) + onceBegin > toggle(null) + progress': true,  // 9122: ?? (skip results are different than combos)
		'skip-combos: current(null) + onceBegin > toggle(null) + stopBegin': true,  // 9123: ?? (skip results are different than combos)
		'skip-combos: current(null) + onceBegin > toggle(null) + stopFinish': true,  // 9124: ?? (skip results are different than combos)
		'skip-combos: current(null) + onceBegin > toggle(null) + done': true,  // 9125: ?? (skip results are different than combos)
		'skip-combos: current(null) + onceFinish > toggle(null) + playBegin': true,  // 10001: ?? (skip results are different than combos)
		'skip-combos: current(null) + onceFinish > toggle(null) + playFinish': true,  // 10002: ?? (skip results are different than combos)
		'skip-combos: current(null) + onceFinish > toggle(null) + pauseBegin': true,  // 10007: ?? (skip results are different than combos)
		'skip-combos: current(null) + onceFinish > toggle(null) + pauseFinish': true,  // 10008: ?? (skip results are different than combos)
		'skip-combos: current(null) + onceFinish > toggle(null) + revertBegin': true,  // 10013: ?? (skip results are different than combos)
		'skip-combos: current(null) + onceFinish > toggle(null) + revertFinish': true,  // 10014: ?? (skip results are different than combos)
		'skip-combos: current(null) + onceFinish > toggle(null) + loopBegin': true,  // 10019: ?? (skip results are different than combos)
		'skip-combos: current(null) + onceFinish > toggle(null) + loopFinish': true,  // 10020: ?? (skip results are different than combos)
		'skip-combos: current(null) + onceFinish > toggle(null) + newWordFragment': true,  // 10021: ?? (skip results are different than combos)
		'skip-combos: current(null) + onceFinish > toggle(null) + progress': true,  // 10022: ?? (skip results are different than combos)
		'skip-combos: current(null) + onceFinish > toggle(null) + stopBegin': true,  // 10023: ?? (skip results are different than combos)
		'skip-combos: current(null) + onceFinish > toggle(null) + stopFinish': true,  // 10024: ?? (skip results are different than combos)
		'skip-combos: current(null) + onceFinish > toggle(null) + done': true,  // 10025: ?? (skip results are different than combos)
		'skip-combos: current(null) + loopBegin > toggle(null) + playBegin': true,  // 16301: ?? (skip results are different than combos)
		'skip-combos: current(null) + loopBegin > toggle(null) + playFinish': true,  // 16302: ?? (skip results are different than combos)
		'skip-combos: current(null) + loopBegin > toggle(null) + pauseBegin': true,  // 16307: ?? (skip results are different than combos)
		'skip-combos: current(null) + loopBegin > toggle(null) + pauseFinish': true,  // 16308: ?? (skip results are different than combos)
		'skip-combos: current(null) + loopBegin > toggle(null) + revertBegin': true,  // 16313: ?? (skip results are different than combos)
		'skip-combos: current(null) + loopBegin > toggle(null) + revertFinish': true,  // 16314: ?? (skip results are different than combos)
		'skip-combos: current(null) + loopBegin > toggle(null) + loopBegin': true,  // 16319: ?? (skip results are different than combos)
		'skip-combos: current(null) + loopBegin > toggle(null) + loopFinish': true,  // 16320: ?? (skip results are different than combos)
		'skip-combos: current(null) + loopBegin > toggle(null) + newWordFragment': true,  // 16321: ?? (skip results are different than combos)
		'skip-combos: current(null) + loopBegin > toggle(null) + progress': true,  // 16322: ?? (skip results are different than combos)
		'skip-combos: current(null) + loopBegin > toggle(null) + stopBegin': true,  // 16323: ?? (skip results are different than combos)
		'skip-combos: current(null) + loopBegin > toggle(null) + stopFinish': true,  // 16324: ?? (skip results are different than combos)
		'skip-combos: current(null) + loopBegin > toggle(null) + done': true,  // 16325: ?? (skip results are different than combos)
		'skip-combos: current(null) + loopFinish > toggle(null) + playBegin': true,  // 17201: ?? (skip results are different than combos)
		'skip-combos: current(null) + loopFinish > toggle(null) + playFinish': true,  // 17202: ?? (skip results are different than combos)
		'skip-combos: current(null) + loopFinish > toggle(null) + pauseBegin': true,  // 17207: ?? (skip results are different than combos)
		'skip-combos: current(null) + loopFinish > toggle(null) + pauseFinish': true,  // 17208: ?? (skip results are different than combos)
		'skip-combos: current(null) + loopFinish > toggle(null) + revertBegin': true,  // 17213: ?? (skip results are different than combos)
		'skip-combos: current(null) + loopFinish > toggle(null) + revertFinish': true,  // 17214: ?? (skip results are different than combos)
		'skip-combos: current(null) + loopFinish > toggle(null) + loopBegin': true,  // 17219: ?? (skip results are different than combos)
		'skip-combos: current(null) + loopFinish > toggle(null) + loopFinish': true,  // 17220: ?? (skip results are different than combos)
		'skip-combos: current(null) + loopFinish > toggle(null) + newWordFragment': true,  // 17221: ?? (skip results are different than combos)
		'skip-combos: current(null) + loopFinish > toggle(null) + progress': true,  // 17222: ?? (skip results are different than combos)
		'skip-combos: current(null) + loopFinish > toggle(null) + stopBegin': true,  // 17223: ?? (skip results are different than combos)
		'skip-combos: current(null) + loopFinish > toggle(null) + stopFinish': true,  // 17224: ?? (skip results are different than combos)
		'skip-combos: current(null) + loopFinish > toggle(null) + done': true,  // 17225: ?? (skip results are different than combos)
		'skip-combos: current(null) + newWordFragment > toggle(null) + playBegin': true,  // 18101: ?? (skip results are different than combos)
		'skip-combos: current(null) + newWordFragment > toggle(null) + playFinish': true,  // 18102: ?? (skip results are different than combos)
		'skip-combos: current(null) + newWordFragment > toggle(null) + pauseBegin': true,  // 18107: ?? (skip results are different than combos)
		'skip-combos: current(null) + newWordFragment > toggle(null) + pauseFinish': true,  // 18108: ?? (skip results are different than combos)
		'skip-combos: current(null) + newWordFragment > toggle(null) + revertBegin': true,  // 18113: ?? (skip results are different than combos)
		'skip-combos: current(null) + newWordFragment > toggle(null) + revertFinish': true,  // 18114: ?? (skip results are different than combos)
		'skip-combos: current(null) + newWordFragment > toggle(null) + loopBegin': true,  // 18119: ?? (skip results are different than combos)
		'skip-combos: current(null) + newWordFragment > toggle(null) + loopFinish': true,  // 18120: ?? (skip results are different than combos)
		'skip-combos: current(null) + newWordFragment > toggle(null) + newWordFragment': true,  // 18121: ?? (skip results are different than combos)
		'skip-combos: current(null) + newWordFragment > toggle(null) + progress': true,  // 18122: ?? (skip results are different than combos)
		'skip-combos: current(null) + newWordFragment > toggle(null) + stopBegin': true,  // 18123: ?? (skip results are different than combos)
		'skip-combos: current(null) + newWordFragment > toggle(null) + stopFinish': true,  // 18124: ?? (skip results are different than combos)
		'skip-combos: current(null) + newWordFragment > toggle(null) + done': true,  // 18125: ?? (skip results are different than combos)
		'skip-combos: current(null) + progress > toggle(null) + playBegin': true,  // 19001: ?? (skip results are different than combos)
		'skip-combos: current(null) + progress > toggle(null) + playFinish': true,  // 19002: ?? (skip results are different than combos)
		'skip-combos: current(null) + progress > toggle(null) + pauseBegin': true,  // 19007: ?? (skip results are different than combos)
		'skip-combos: current(null) + progress > toggle(null) + pauseFinish': true,  // 19008: ?? (skip results are different than combos)
		'skip-combos: current(null) + progress > toggle(null) + revertBegin': true,  // 19013: ?? (skip results are different than combos)
		'skip-combos: current(null) + progress > toggle(null) + revertFinish': true,  // 19014: ?? (skip results are different than combos)
		'skip-combos: current(null) + progress > toggle(null) + loopBegin': true,  // 19019: ?? (skip results are different than combos)
		'skip-combos: current(null) + progress > toggle(null) + loopFinish': true,  // 19020: ?? (skip results are different than combos)
		'skip-combos: current(null) + progress > toggle(null) + newWordFragment': true,  // 19021: ?? (skip results are different than combos)
		'skip-combos: current(null) + progress > toggle(null) + progress': true,  // 19022: ?? (skip results are different than combos)
		'skip-combos: current(null) + progress > toggle(null) + stopBegin': true,  // 19023: ?? (skip results are different than combos)
		'skip-combos: current(null) + progress > toggle(null) + stopFinish': true,  // 19024: ?? (skip results are different than combos)
		'skip-combos: current(null) + progress > toggle(null) + done': true,  // 19025: ?? (skip results are different than combos)




		// ============== jumpTo(-3) ==============
		// #? like prev
		'skip-combos: jumpTo(-3) + onceBegin > play(null) + playBegin': true,  // 9076: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + onceBegin > play(null) + playFinish': true,  // 9077: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + onceBegin > play(null) + restartBegin': true,  // 9080: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + onceBegin > play(null) + restartFinish': true,  // 9081: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + onceBegin > toggle(null) + playBegin': true,  // 9101: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + onceBegin > toggle(null) + playFinish': true,  // 9102: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + onceBegin > toggle(null) + restartBegin': true,  // 9105: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + onceBegin > toggle(null) + restartFinish': true,  // 9106: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + onceFinish > play(null) + playBegin': true,  // 9976: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + onceFinish > play(null) + playFinish': true,  // 9977: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + onceFinish > play(null) + restartBegin': true,  // 9980: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + onceFinish > play(null) + restartFinish': true,  // 9981: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + onceFinish > toggle(null) + playBegin': true,  // 10001: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + onceFinish > toggle(null) + playFinish': true,  // 10002: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + onceFinish > toggle(null) + restartBegin': true,  // 10005: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + onceFinish > toggle(null) + restartFinish': true,  // 10006: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + loopBegin > play(null) + playBegin': true,  // 16276: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + loopBegin > play(null) + playFinish': true,  // 16277: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + loopBegin > play(null) + restartBegin': true,  // 16280: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + loopBegin > play(null) + restartFinish': true,  // 16281: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + loopBegin > toggle(null) + playBegin': true,  // 16301: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + loopBegin > toggle(null) + playFinish': true,  // 16302: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + loopBegin > toggle(null) + restartBegin': true,  // 16305: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + loopBegin > toggle(null) + restartFinish': true,  // 16306: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + loopFinish > play(null) + playBegin': true,  // 17176: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + loopFinish > play(null) + playFinish': true,  // 17177: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + loopFinish > play(null) + restartBegin': true,  // 17180: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + loopFinish > play(null) + restartFinish': true,  // 17181: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + loopFinish > toggle(null) + playBegin': true,  // 17201: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + loopFinish > toggle(null) + playFinish': true,  // 17202: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + loopFinish > toggle(null) + restartBegin': true,  // 17205: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + loopFinish > toggle(null) + restartFinish': true,  // 17206: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + newWordFragment > play(null) + playBegin': true,  // 18076: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + newWordFragment > play(null) + playFinish': true,  // 18077: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + newWordFragment > play(null) + restartBegin': true,  // 18080: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + newWordFragment > play(null) + restartFinish': true,  // 18081: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + newWordFragment > toggle(null) + playBegin': true,  // 18101: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + newWordFragment > toggle(null) + playFinish': true,  // 18102: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + newWordFragment > toggle(null) + restartBegin': true,  // 18105: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + newWordFragment > toggle(null) + restartFinish': true,  // 18106: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + progress > play(null) + playBegin': true,  // 18976: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + progress > play(null) + playFinish': true,  // 18977: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + progress > play(null) + restartBegin': true,  // 18980: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + progress > play(null) + restartFinish': true,  // 18981: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + progress > toggle(null) + playBegin': true,  // 19001: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + progress > toggle(null) + playFinish': true,  // 19002: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + progress > toggle(null) + restartBegin': true,  // 19005: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + progress > toggle(null) + restartFinish': true,  // 19006: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + stopBegin > play(null) + playBegin': true,  // 19876: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + stopBegin > play(null) + playFinish': true,  // 19877: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + stopBegin > play(null) + restartBegin': true,  // 19880: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + stopBegin > play(null) + restartFinish': true,  // 19881: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + stopBegin > toggle(null) + playBegin': true,  // 19901: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + stopBegin > toggle(null) + playFinish': true,  // 19902: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + stopBegin > toggle(null) + restartBegin': true,  // 19905: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + stopBegin > toggle(null) + restartFinish': true,  // 19906: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + stopFinish > play(null) + playBegin': true,  // 20776: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + stopFinish > play(null) + playFinish': true,  // 20777: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + stopFinish > play(null) + restartBegin': true,  // 20780: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + stopFinish > play(null) + restartFinish': true,  // 20781: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + stopFinish > toggle(null) + playBegin': true,  // 20801: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + stopFinish > toggle(null) + playFinish': true,  // 20802: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + stopFinish > toggle(null) + restartBegin': true,  // 20805: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + stopFinish > toggle(null) + restartFinish': true,  // 20806: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + done > play(null) + playBegin': true,  // 21676: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + done > play(null) + playFinish': true,  // 21677: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + done > play(null) + restartBegin': true,  // 21680: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + done > play(null) + restartFinish': true,  // 21681: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + done > toggle(null) + playBegin': true,  // 21701: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + done > toggle(null) + playFinish': true,  // 21702: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + done > toggle(null) + restartBegin': true,  // 21705: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-3) + done > toggle(null) + restartFinish': true,  // 21706: ?? (skip results are different than combos)




		// ============== jumpTo(-1) ==============
		// #? like prev
		'skip-combos: jumpTo(-1) + onceBegin > play(null) + playBegin': true,  // 9076: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + onceBegin > play(null) + playFinish': true,  // 9077: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + onceBegin > play(null) + restartBegin': true,  // 9080: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + onceBegin > play(null) + restartFinish': true,  // 9081: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + onceBegin > toggle(null) + playBegin': true,  // 9101: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + onceBegin > toggle(null) + playFinish': true,  // 9102: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + onceBegin > toggle(null) + restartBegin': true,  // 9105: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + onceBegin > toggle(null) + restartFinish': true,  // 9106: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + onceFinish > play(null) + playBegin': true,  // 9976: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + onceFinish > play(null) + playFinish': true,  // 9977: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + onceFinish > play(null) + restartBegin': true,  // 9980: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + onceFinish > play(null) + restartFinish': true,  // 9981: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + onceFinish > toggle(null) + playBegin': true,  // 10001: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + onceFinish > toggle(null) + playFinish': true,  // 10002: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + onceFinish > toggle(null) + restartBegin': true,  // 10005: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + onceFinish > toggle(null) + restartFinish': true,  // 10006: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + loopBegin > play(null) + playBegin': true,  // 16276: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + loopBegin > play(null) + playFinish': true,  // 16277: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + loopBegin > play(null) + restartBegin': true,  // 16280: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + loopBegin > play(null) + restartFinish': true,  // 16281: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + loopBegin > toggle(null) + playBegin': true,  // 16301: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + loopBegin > toggle(null) + playFinish': true,  // 16302: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + loopBegin > toggle(null) + restartBegin': true,  // 16305: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + loopBegin > toggle(null) + restartFinish': true,  // 16306: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + loopFinish > play(null) + playBegin': true,  // 17176: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + loopFinish > play(null) + playFinish': true,  // 17177: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + loopFinish > play(null) + restartBegin': true,  // 17180: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + loopFinish > play(null) + restartFinish': true,  // 17181: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + loopFinish > toggle(null) + playBegin': true,  // 17201: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + loopFinish > toggle(null) + playFinish': true,  // 17202: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + loopFinish > toggle(null) + restartBegin': true,  // 17205: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + loopFinish > toggle(null) + restartFinish': true,  // 17206: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + newWordFragment > play(null) + playBegin': true,  // 18076: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + newWordFragment > play(null) + playFinish': true,  // 18077: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + newWordFragment > play(null) + restartBegin': true,  // 18080: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + newWordFragment > play(null) + restartFinish': true,  // 18081: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + newWordFragment > toggle(null) + playBegin': true,  // 18101: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + newWordFragment > toggle(null) + playFinish': true,  // 18102: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + newWordFragment > toggle(null) + restartBegin': true,  // 18105: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + newWordFragment > toggle(null) + restartFinish': true,  // 18106: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + progress > play(null) + playBegin': true,  // 18976: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + progress > play(null) + playFinish': true,  // 18977: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + progress > play(null) + restartBegin': true,  // 18980: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + progress > play(null) + restartFinish': true,  // 18981: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + progress > toggle(null) + playBegin': true,  // 19001: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + progress > toggle(null) + playFinish': true,  // 19002: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + progress > toggle(null) + restartBegin': true,  // 19005: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + progress > toggle(null) + restartFinish': true,  // 19006: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + stopBegin > play(null) + playBegin': true,  // 19876: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + stopBegin > play(null) + playFinish': true,  // 19877: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + stopBegin > play(null) + restartBegin': true,  // 19880: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + stopBegin > play(null) + restartFinish': true,  // 19881: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + stopBegin > toggle(null) + playBegin': true,  // 19901: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + stopBegin > toggle(null) + playFinish': true,  // 19902: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + stopBegin > toggle(null) + restartBegin': true,  // 19905: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + stopBegin > toggle(null) + restartFinish': true,  // 19906: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + stopFinish > play(null) + playBegin': true,  // 20776: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + stopFinish > play(null) + playFinish': true,  // 20777: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + stopFinish > play(null) + restartBegin': true,  // 20780: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + stopFinish > play(null) + restartFinish': true,  // 20781: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + stopFinish > toggle(null) + playBegin': true,  // 20801: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + stopFinish > toggle(null) + playFinish': true,  // 20802: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + stopFinish > toggle(null) + restartBegin': true,  // 20805: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + stopFinish > toggle(null) + restartFinish': true,  // 20806: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + done > play(null) + playBegin': true,  // 21676: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + done > play(null) + playFinish': true,  // 21677: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + done > play(null) + restartBegin': true,  // 21680: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + done > play(null) + restartFinish': true,  // 21681: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + done > toggle(null) + playBegin': true,  // 21701: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + done > toggle(null) + playFinish': true,  // 21702: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + done > toggle(null) + restartBegin': true,  // 21705: ?? (skip results are different than combos)
		'skip-combos: jumpTo(-1) + done > toggle(null) + restartFinish': true,  // 21706: ?? (skip results are different than combos)



		// ============== jumpTo(0) ==============
		// #? jumpTo(0) reverts to 'pause', so toggle = 'revert*|pause*' stuff not 'play*|...' stuff
		// Like current
		'skip-combos: jumpTo(0) + onceBegin > toggle(null) + playBegin': true,  // 9101: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + onceBegin > toggle(null) + playFinish': true,  // 9102: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + onceBegin > toggle(null) + pauseBegin': true,  // 9107: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + onceBegin > toggle(null) + pauseFinish': true,  // 9108: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + onceBegin > toggle(null) + revertBegin': true,  // 9113: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + onceBegin > toggle(null) + revertFinish': true,  // 9114: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + onceBegin > toggle(null) + loopBegin': true,  // 9119: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + onceBegin > toggle(null) + loopFinish': true,  // 9120: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + onceBegin > toggle(null) + newWordFragment': true,  // 9121: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + onceBegin > toggle(null) + progress': true,  // 9122: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + onceBegin > toggle(null) + stopBegin': true,  // 9123: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + onceBegin > toggle(null) + stopFinish': true,  // 9124: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + onceBegin > toggle(null) + done': true,  // 9125: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + onceFinish > toggle(null) + playBegin': true,  // 10001: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + onceFinish > toggle(null) + playFinish': true,  // 10002: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + onceFinish > toggle(null) + pauseBegin': true,  // 10007: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + onceFinish > toggle(null) + pauseFinish': true,  // 10008: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + onceFinish > toggle(null) + revertBegin': true,  // 10013: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + onceFinish > toggle(null) + revertFinish': true,  // 10014: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + onceFinish > toggle(null) + loopBegin': true,  // 10019: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + onceFinish > toggle(null) + loopFinish': true,  // 10020: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + onceFinish > toggle(null) + newWordFragment': true,  // 10021: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + onceFinish > toggle(null) + progress': true,  // 10022: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + onceFinish > toggle(null) + stopBegin': true,  // 10023: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + onceFinish > toggle(null) + stopFinish': true,  // 10024: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + onceFinish > toggle(null) + done': true,  // 10025: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + loopBegin > toggle(null) + playBegin': true,  // 16301: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + loopBegin > toggle(null) + playFinish': true,  // 16302: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + loopBegin > toggle(null) + pauseBegin': true,  // 16307: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + loopBegin > toggle(null) + pauseFinish': true,  // 16308: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + loopBegin > toggle(null) + revertBegin': true,  // 16313: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + loopBegin > toggle(null) + revertFinish': true,  // 16314: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + loopBegin > toggle(null) + loopBegin': true,  // 16319: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + loopBegin > toggle(null) + loopFinish': true,  // 16320: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + loopBegin > toggle(null) + newWordFragment': true,  // 16321: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + loopBegin > toggle(null) + progress': true,  // 16322: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + loopBegin > toggle(null) + stopBegin': true,  // 16323: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + loopBegin > toggle(null) + stopFinish': true,  // 16324: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + loopBegin > toggle(null) + done': true,  // 16325: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + loopFinish > toggle(null) + playBegin': true,  // 17201: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + loopFinish > toggle(null) + playFinish': true,  // 17202: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + loopFinish > toggle(null) + pauseBegin': true,  // 17207: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + loopFinish > toggle(null) + pauseFinish': true,  // 17208: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + loopFinish > toggle(null) + revertBegin': true,  // 17213: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + loopFinish > toggle(null) + revertFinish': true,  // 17214: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + loopFinish > toggle(null) + loopBegin': true,  // 17219: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + loopFinish > toggle(null) + loopFinish': true,  // 17220: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + loopFinish > toggle(null) + newWordFragment': true,  // 17221: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + loopFinish > toggle(null) + progress': true,  // 17222: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + loopFinish > toggle(null) + stopBegin': true,  // 17223: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + loopFinish > toggle(null) + stopFinish': true,  // 17224: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + loopFinish > toggle(null) + done': true,  // 17225: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + newWordFragment > toggle(null) + playBegin': true,  // 18101: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + newWordFragment > toggle(null) + playFinish': true,  // 18102: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + newWordFragment > toggle(null) + pauseBegin': true,  // 18107: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + newWordFragment > toggle(null) + pauseFinish': true,  // 18108: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + newWordFragment > toggle(null) + revertBegin': true,  // 18113: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + newWordFragment > toggle(null) + revertFinish': true,  // 18114: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + newWordFragment > toggle(null) + loopBegin': true,  // 18119: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + newWordFragment > toggle(null) + loopFinish': true,  // 18120: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + newWordFragment > toggle(null) + newWordFragment': true,  // 18121: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + newWordFragment > toggle(null) + progress': true,  // 18122: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + newWordFragment > toggle(null) + stopBegin': true,  // 18123: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + newWordFragment > toggle(null) + stopFinish': true,  // 18124: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + newWordFragment > toggle(null) + done': true,  // 18125: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + progress > toggle(null) + playBegin': true,  // 19001: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + progress > toggle(null) + playFinish': true,  // 19002: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + progress > toggle(null) + pauseBegin': true,  // 19007: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + progress > toggle(null) + pauseFinish': true,  // 19008: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + progress > toggle(null) + revertBegin': true,  // 19013: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + progress > toggle(null) + revertFinish': true,  // 19014: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + progress > toggle(null) + loopBegin': true,  // 19019: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + progress > toggle(null) + loopFinish': true,  // 19020: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + progress > toggle(null) + newWordFragment': true,  // 19021: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + progress > toggle(null) + progress': true,  // 19022: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + progress > toggle(null) + stopBegin': true,  // 19023: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + progress > toggle(null) + stopFinish': true,  // 19024: ?? (skip results are different than combos)
		'skip-combos: jumpTo(0) + progress > toggle(null) + done': true,  // 19025: ?? (skip results are different than combos)



		// ============== jumpTo(6) ==============
		// like once(+) in some ways, but moves forward more words and sentences, so a bit different
		// #? jumpTo(6) + any > some relative loopers(+|0) + new|prog = different output
		// #? jumpTo(6) + any > toggle = reverting and pause stuff, not play stuff
		// #? jumpTo(6) + any > some relative loopers(-) + new|prog = different output (even sentence(-1) loopers because they lead to start of this sentence instead of to first sentence)
		// #? jumpTo(6) + any > some relative loopers(-) + stop*|done = not triggered
		// #? jumpTo(6) + any > some relative loopers(+) + stop*|done = triggered
		'skip-combos: jumpTo(6) + onceBegin > play(null) + newWordFragment': false,  // 9096: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > play(null) + progress': false,  // 9097: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > toggle(null) + playBegin': false,  // 9101: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > toggle(null) + playFinish': false,  // 9102: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > toggle(null) + pauseBegin': false,  // 9107: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > toggle(null) + pauseFinish': false,  // 9108: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > toggle(null) + revertBegin': false,  // 9113: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > toggle(null) + revertFinish': false,  // 9114: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > toggle(null) + loopBegin': false,  // 9119: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > toggle(null) + loopFinish': false,  // 9120: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > toggle(null) + newWordFragment': false,  // 9121: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > toggle(null) + progress': false,  // 9122: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > toggle(null) + stopBegin': false,  // 9123: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > toggle(null) + stopFinish': false,  // 9124: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > toggle(null) + done': false,  // 9125: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > rewind(null) + newWordFragment': false,  // 9246: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > rewind(null) + progress': false,  // 9247: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > fastForward(null) + newWordFragment': false,  // 9271: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > fastForward(null) + progress': false,  // 9272: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > once([0,0,-2]) + newWordFragment': false,  // 9296: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > once([0,0,-2]) + progress': false,  // 9297: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > once([0,0,-2]) + stopBegin': false,  // 9298: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > once([0,0,-2]) + stopFinish': false,  // 9299: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > once([0,0,-2]) + done': false,  // 9300: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > once([0,0,2]) + newWordFragment': false,  // 9321: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > once([0,0,2]) + progress': false,  // 9322: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > current(null) + newWordFragment': false,  // 9346: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > current(null) + progress': false,  // 9347: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > jumpWords(-3) + newWordFragment': false,  // 9521: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > jumpWords(-3) + progress': false,  // 9522: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > jumpWords(-3) + stopBegin': false,  // 9523: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > jumpWords(-3) + stopFinish': false,  // 9524: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > jumpWords(-3) + done': false,  // 9525: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > jumpWords(-1) + newWordFragment': false,  // 9546: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > jumpWords(-1) + progress': false,  // 9547: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > jumpWords(-1) + stopBegin': false,  // 9548: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > jumpWords(-1) + stopFinish': false,  // 9549: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > jumpWords(-1) + done': false,  // 9550: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > jumpWords(0) + newWordFragment': false,  // 9571: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > jumpWords(0) + progress': false,  // 9572: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > jumpWords(4) + newWordFragment': false,  // 9596: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > jumpWords(4) + progress': false,  // 9597: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > jumpSentences(-1) + newWordFragment': false,  // 9696: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > jumpSentences(-1) + progress': false,  // 9697: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > jumpSentences(-1) + stopBegin': false,  // 9698: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > jumpSentences(-1) + stopFinish': false,  // 9699: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > jumpSentences(-1) + done': false,  // 9700: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > jumpSentences(0) + newWordFragment': false,  // 9721: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > jumpSentences(0) + progress': false,  // 9722: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > jumpSentences(1) + newWordFragment': false,  // 9746: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > jumpSentences(1) + progress': false,  // 9747: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > jumpSentences(3) + newWordFragment': false,  // 9771: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > jumpSentences(3) + progress': false,  // 9772: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > jumpSentences(3) + stopBegin': false,  // 9773: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > jumpSentences(3) + stopFinish': false,  // 9774: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > jumpSentences(3) + done': false,  // 9775: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > nextWord(null) + newWordFragment': false,  // 9821: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > nextWord(null) + progress': false,  // 9822: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > nextSentence(null) + newWordFragment': false,  // 9846: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > nextSentence(null) + progress': false,  // 9847: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > prevWord(null) + newWordFragment': false,  // 9871: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > prevWord(null) + progress': false,  // 9872: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > prevWord(null) + stopBegin': false,  // 9873: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > prevWord(null) + stopFinish': false,  // 9874: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > prevWord(null) + done': false,  // 9875: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > prevSentence(null) + newWordFragment': false,  // 9896: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > prevSentence(null) + progress': false,  // 9897: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > prevSentence(null) + stopBegin': false,  // 9898: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > prevSentence(null) + stopFinish': false,  // 9899: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceBegin > prevSentence(null) + done': false,  // 9900: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > play(null) + newWordFragment': false,  // 9996: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > play(null) + progress': false,  // 9997: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > toggle(null) + playBegin': false,  // 10001: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > toggle(null) + playFinish': false,  // 10002: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > toggle(null) + pauseBegin': false,  // 10007: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > toggle(null) + pauseFinish': false,  // 10008: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > toggle(null) + revertBegin': false,  // 10013: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > toggle(null) + revertFinish': false,  // 10014: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > toggle(null) + loopBegin': false,  // 10019: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > toggle(null) + loopFinish': false,  // 10020: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > toggle(null) + newWordFragment': false,  // 10021: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > toggle(null) + progress': false,  // 10022: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > toggle(null) + stopBegin': false,  // 10023: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > toggle(null) + stopFinish': false,  // 10024: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > toggle(null) + done': false,  // 10025: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > rewind(null) + newWordFragment': false,  // 10146: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > rewind(null) + progress': false,  // 10147: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > fastForward(null) + newWordFragment': false,  // 10171: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > fastForward(null) + progress': false,  // 10172: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > once([0,0,-2]) + newWordFragment': false,  // 10196: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > once([0,0,-2]) + progress': false,  // 10197: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > once([0,0,-2]) + stopBegin': false,  // 10198: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > once([0,0,-2]) + stopFinish': false,  // 10199: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > once([0,0,-2]) + done': false,  // 10200: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > once([0,0,2]) + newWordFragment': false,  // 10221: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > once([0,0,2]) + progress': false,  // 10222: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > current(null) + newWordFragment': false,  // 10246: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > current(null) + progress': false,  // 10247: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > jumpWords(-3) + newWordFragment': false,  // 10421: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > jumpWords(-3) + progress': false,  // 10422: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > jumpWords(-3) + stopBegin': false,  // 10423: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > jumpWords(-3) + stopFinish': false,  // 10424: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > jumpWords(-3) + done': false,  // 10425: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > jumpWords(-1) + newWordFragment': false,  // 10446: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > jumpWords(-1) + progress': false,  // 10447: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > jumpWords(-1) + stopBegin': false,  // 10448: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > jumpWords(-1) + stopFinish': false,  // 10449: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > jumpWords(-1) + done': false,  // 10450: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > jumpWords(0) + newWordFragment': false,  // 10471: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > jumpWords(0) + progress': false,  // 10472: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > jumpWords(4) + newWordFragment': false,  // 10496: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > jumpWords(4) + progress': false,  // 10497: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > jumpSentences(-1) + newWordFragment': false,  // 10596: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > jumpSentences(-1) + progress': false,  // 10597: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > jumpSentences(-1) + stopBegin': false,  // 10598: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > jumpSentences(-1) + stopFinish': false,  // 10599: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > jumpSentences(-1) + done': false,  // 10600: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > jumpSentences(0) + newWordFragment': false,  // 10621: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > jumpSentences(0) + progress': false,  // 10622: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > jumpSentences(1) + newWordFragment': false,  // 10646: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > jumpSentences(1) + progress': false,  // 10647: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > jumpSentences(3) + newWordFragment': false,  // 10671: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > jumpSentences(3) + progress': false,  // 10672: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > jumpSentences(3) + stopBegin': false,  // 10673: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > jumpSentences(3) + stopFinish': false,  // 10674: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > jumpSentences(3) + done': false,  // 10675: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > nextWord(null) + newWordFragment': false,  // 10721: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > nextWord(null) + progress': false,  // 10722: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > nextSentence(null) + newWordFragment': false,  // 10746: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > nextSentence(null) + progress': false,  // 10747: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > prevWord(null) + newWordFragment': false,  // 10771: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > prevWord(null) + progress': false,  // 10772: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > prevWord(null) + stopBegin': false,  // 10773: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > prevWord(null) + stopFinish': false,  // 10774: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > prevWord(null) + done': false,  // 10775: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > prevSentence(null) + newWordFragment': false,  // 10796: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > prevSentence(null) + progress': false,  // 10797: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > prevSentence(null) + stopBegin': false,  // 10798: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > prevSentence(null) + stopFinish': false,  // 10799: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + onceFinish > prevSentence(null) + done': false,  // 10800: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > play(null) + newWordFragment': false,  // 16296: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > play(null) + progress': false,  // 16297: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > toggle(null) + playBegin': false,  // 16301: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > toggle(null) + playFinish': false,  // 16302: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > toggle(null) + pauseBegin': false,  // 16307: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > toggle(null) + pauseFinish': false,  // 16308: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > toggle(null) + revertBegin': false,  // 16313: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > toggle(null) + revertFinish': false,  // 16314: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > toggle(null) + loopBegin': false,  // 16319: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > toggle(null) + loopFinish': false,  // 16320: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > toggle(null) + newWordFragment': false,  // 16321: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > toggle(null) + progress': false,  // 16322: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > toggle(null) + stopBegin': false,  // 16323: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > toggle(null) + stopFinish': false,  // 16324: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > toggle(null) + done': false,  // 16325: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > rewind(null) + newWordFragment': false,  // 16446: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > rewind(null) + progress': false,  // 16447: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > fastForward(null) + newWordFragment': false,  // 16471: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > fastForward(null) + progress': false,  // 16472: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > once([0,0,-2]) + newWordFragment': false,  // 16496: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > once([0,0,-2]) + progress': false,  // 16497: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > once([0,0,-2]) + stopBegin': false,  // 16498: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > once([0,0,-2]) + stopFinish': false,  // 16499: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > once([0,0,-2]) + done': false,  // 16500: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > once([0,0,2]) + newWordFragment': false,  // 16521: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > once([0,0,2]) + progress': false,  // 16522: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > current(null) + newWordFragment': false,  // 16546: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > current(null) + progress': false,  // 16547: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > jumpWords(-3) + newWordFragment': false,  // 16721: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > jumpWords(-3) + progress': false,  // 16722: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > jumpWords(-3) + stopBegin': false,  // 16723: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > jumpWords(-3) + stopFinish': false,  // 16724: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > jumpWords(-3) + done': false,  // 16725: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > jumpWords(-1) + newWordFragment': false,  // 16746: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > jumpWords(-1) + progress': false,  // 16747: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > jumpWords(-1) + stopBegin': false,  // 16748: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > jumpWords(-1) + stopFinish': false,  // 16749: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > jumpWords(-1) + done': false,  // 16750: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > jumpWords(0) + newWordFragment': false,  // 16771: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > jumpWords(0) + progress': false,  // 16772: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > jumpWords(4) + newWordFragment': false,  // 16796: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > jumpWords(4) + progress': false,  // 16797: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > jumpSentences(-1) + newWordFragment': false,  // 16896: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > jumpSentences(-1) + progress': false,  // 16897: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > jumpSentences(-1) + stopBegin': false,  // 16898: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > jumpSentences(-1) + stopFinish': false,  // 16899: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > jumpSentences(-1) + done': false,  // 16900: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > jumpSentences(0) + newWordFragment': false,  // 16921: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > jumpSentences(0) + progress': false,  // 16922: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > jumpSentences(1) + newWordFragment': false,  // 16946: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > jumpSentences(1) + progress': false,  // 16947: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > jumpSentences(3) + newWordFragment': false,  // 16971: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > jumpSentences(3) + progress': false,  // 16972: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > jumpSentences(3) + stopBegin': false,  // 16973: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > jumpSentences(3) + stopFinish': false,  // 16974: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > jumpSentences(3) + done': false,  // 16975: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > nextWord(null) + newWordFragment': false,  // 17021: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > nextWord(null) + progress': false,  // 17022: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > nextSentence(null) + newWordFragment': false,  // 17046: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > nextSentence(null) + progress': false,  // 17047: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > prevWord(null) + newWordFragment': false,  // 17071: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > prevWord(null) + progress': false,  // 17072: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > prevWord(null) + stopBegin': false,  // 17073: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > prevWord(null) + stopFinish': false,  // 17074: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > prevWord(null) + done': false,  // 17075: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > prevSentence(null) + newWordFragment': false,  // 17096: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > prevSentence(null) + progress': false,  // 17097: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > prevSentence(null) + stopBegin': false,  // 17098: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > prevSentence(null) + stopFinish': false,  // 17099: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopBegin > prevSentence(null) + done': false,  // 17100: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > play(null) + newWordFragment': false,  // 17196: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > play(null) + progress': false,  // 17197: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > toggle(null) + playBegin': false,  // 17201: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > toggle(null) + playFinish': false,  // 17202: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > toggle(null) + pauseBegin': false,  // 17207: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > toggle(null) + pauseFinish': false,  // 17208: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > toggle(null) + revertBegin': false,  // 17213: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > toggle(null) + revertFinish': false,  // 17214: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > toggle(null) + loopBegin': false,  // 17219: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > toggle(null) + loopFinish': false,  // 17220: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > toggle(null) + newWordFragment': false,  // 17221: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > toggle(null) + progress': false,  // 17222: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > toggle(null) + stopBegin': false,  // 17223: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > toggle(null) + stopFinish': false,  // 17224: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > toggle(null) + done': false,  // 17225: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > rewind(null) + newWordFragment': false,  // 17346: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > rewind(null) + progress': false,  // 17347: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > fastForward(null) + newWordFragment': false,  // 17371: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > fastForward(null) + progress': false,  // 17372: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > once([0,0,-2]) + newWordFragment': false,  // 17396: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > once([0,0,-2]) + progress': false,  // 17397: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > once([0,0,-2]) + stopBegin': false,  // 17398: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > once([0,0,-2]) + stopFinish': false,  // 17399: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > once([0,0,-2]) + done': false,  // 17400: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > once([0,0,2]) + newWordFragment': false,  // 17421: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > once([0,0,2]) + progress': false,  // 17422: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > current(null) + newWordFragment': false,  // 17446: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > current(null) + progress': false,  // 17447: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > jumpWords(-3) + newWordFragment': false,  // 17621: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > jumpWords(-3) + progress': false,  // 17622: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > jumpWords(-3) + stopBegin': false,  // 17623: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > jumpWords(-3) + stopFinish': false,  // 17624: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > jumpWords(-3) + done': false,  // 17625: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > jumpWords(-1) + newWordFragment': false,  // 17646: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > jumpWords(-1) + progress': false,  // 17647: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > jumpWords(-1) + stopBegin': false,  // 17648: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > jumpWords(-1) + stopFinish': false,  // 17649: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > jumpWords(-1) + done': false,  // 17650: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > jumpWords(0) + newWordFragment': false,  // 17671: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > jumpWords(0) + progress': false,  // 17672: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > jumpWords(4) + newWordFragment': false,  // 17696: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > jumpWords(4) + progress': false,  // 17697: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > jumpSentences(-1) + newWordFragment': false,  // 17796: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > jumpSentences(-1) + progress': false,  // 17797: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > jumpSentences(-1) + stopBegin': false,  // 17798: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > jumpSentences(-1) + stopFinish': false,  // 17799: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > jumpSentences(-1) + done': false,  // 17800: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > jumpSentences(0) + newWordFragment': false,  // 17821: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > jumpSentences(0) + progress': false,  // 17822: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > jumpSentences(1) + newWordFragment': false,  // 17846: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > jumpSentences(1) + progress': false,  // 17847: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > jumpSentences(3) + newWordFragment': false,  // 17871: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > jumpSentences(3) + progress': false,  // 17872: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > jumpSentences(3) + stopBegin': false,  // 17873: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > jumpSentences(3) + stopFinish': false,  // 17874: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > jumpSentences(3) + done': false,  // 17875: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > nextWord(null) + newWordFragment': false,  // 17921: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > nextWord(null) + progress': false,  // 17922: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > nextSentence(null) + newWordFragment': false,  // 17946: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > nextSentence(null) + progress': false,  // 17947: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > prevWord(null) + newWordFragment': false,  // 17971: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > prevWord(null) + progress': false,  // 17972: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > prevWord(null) + stopBegin': false,  // 17973: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > prevWord(null) + stopFinish': false,  // 17974: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > prevWord(null) + done': false,  // 17975: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > prevSentence(null) + newWordFragment': false,  // 17996: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > prevSentence(null) + progress': false,  // 17997: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > prevSentence(null) + stopBegin': false,  // 17998: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > prevSentence(null) + stopFinish': false,  // 17999: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + loopFinish > prevSentence(null) + done': false,  // 18000: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > play(null) + newWordFragment': false,  // 18096: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > play(null) + progress': false,  // 18097: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > toggle(null) + playBegin': false,  // 18101: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > toggle(null) + playFinish': false,  // 18102: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > toggle(null) + pauseBegin': false,  // 18107: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > toggle(null) + pauseFinish': false,  // 18108: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > toggle(null) + revertBegin': false,  // 18113: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > toggle(null) + revertFinish': false,  // 18114: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > toggle(null) + loopBegin': false,  // 18119: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > toggle(null) + loopFinish': false,  // 18120: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > toggle(null) + newWordFragment': false,  // 18121: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > toggle(null) + progress': false,  // 18122: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > toggle(null) + stopBegin': false,  // 18123: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > toggle(null) + stopFinish': false,  // 18124: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > toggle(null) + done': false,  // 18125: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > rewind(null) + newWordFragment': false,  // 18246: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > rewind(null) + progress': false,  // 18247: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > fastForward(null) + newWordFragment': false,  // 18271: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > fastForward(null) + progress': false,  // 18272: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > once([0,0,-2]) + newWordFragment': false,  // 18296: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > once([0,0,-2]) + progress': false,  // 18297: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > once([0,0,-2]) + stopBegin': false,  // 18298: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > once([0,0,-2]) + stopFinish': false,  // 18299: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > once([0,0,-2]) + done': false,  // 18300: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > once([0,0,2]) + newWordFragment': false,  // 18321: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > once([0,0,2]) + progress': false,  // 18322: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > current(null) + newWordFragment': false,  // 18346: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > current(null) + progress': false,  // 18347: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > jumpWords(-3) + newWordFragment': false,  // 18521: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > jumpWords(-3) + progress': false,  // 18522: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > jumpWords(-3) + stopBegin': false,  // 18523: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > jumpWords(-3) + stopFinish': false,  // 18524: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > jumpWords(-3) + done': false,  // 18525: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > jumpWords(-1) + newWordFragment': false,  // 18546: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > jumpWords(-1) + progress': false,  // 18547: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > jumpWords(-1) + stopBegin': false,  // 18548: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > jumpWords(-1) + stopFinish': false,  // 18549: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > jumpWords(-1) + done': false,  // 18550: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > jumpWords(0) + newWordFragment': false,  // 18571: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > jumpWords(0) + progress': false,  // 18572: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > jumpWords(4) + newWordFragment': false,  // 18596: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > jumpWords(4) + progress': false,  // 18597: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > jumpSentences(-1) + newWordFragment': false,  // 18696: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > jumpSentences(-1) + progress': false,  // 18697: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > jumpSentences(-1) + stopBegin': false,  // 18698: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > jumpSentences(-1) + stopFinish': false,  // 18699: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > jumpSentences(-1) + done': false,  // 18700: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > jumpSentences(0) + newWordFragment': false,  // 18721: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > jumpSentences(0) + progress': false,  // 18722: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > jumpSentences(1) + newWordFragment': false,  // 18746: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > jumpSentences(1) + progress': false,  // 18747: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > jumpSentences(3) + newWordFragment': false,  // 18771: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > jumpSentences(3) + progress': false,  // 18772: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > jumpSentences(3) + stopBegin': false,  // 18773: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > jumpSentences(3) + stopFinish': false,  // 18774: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > jumpSentences(3) + done': false,  // 18775: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > nextWord(null) + newWordFragment': false,  // 18821: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > nextWord(null) + progress': false,  // 18822: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > nextSentence(null) + newWordFragment': false,  // 18846: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > nextSentence(null) + progress': false,  // 18847: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > prevWord(null) + newWordFragment': false,  // 18871: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > prevWord(null) + progress': false,  // 18872: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > prevWord(null) + stopBegin': false,  // 18873: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > prevWord(null) + stopFinish': false,  // 18874: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > prevWord(null) + done': false,  // 18875: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > prevSentence(null) + newWordFragment': false,  // 18896: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > prevSentence(null) + progress': false,  // 18897: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > prevSentence(null) + stopBegin': false,  // 18898: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > prevSentence(null) + stopFinish': false,  // 18899: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + newWordFragment > prevSentence(null) + done': false,  // 18900: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > play(null) + newWordFragment': false,  // 18996: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > play(null) + progress': false,  // 18997: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > toggle(null) + playBegin': false,  // 19001: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > toggle(null) + playFinish': false,  // 19002: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > toggle(null) + pauseBegin': false,  // 19007: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > toggle(null) + pauseFinish': false,  // 19008: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > toggle(null) + revertBegin': false,  // 19013: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > toggle(null) + revertFinish': false,  // 19014: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > toggle(null) + loopBegin': false,  // 19019: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > toggle(null) + loopFinish': false,  // 19020: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > toggle(null) + newWordFragment': false,  // 19021: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > toggle(null) + progress': false,  // 19022: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > toggle(null) + stopBegin': false,  // 19023: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > toggle(null) + stopFinish': false,  // 19024: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > toggle(null) + done': false,  // 19025: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > rewind(null) + newWordFragment': false,  // 19146: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > rewind(null) + progress': false,  // 19147: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > fastForward(null) + newWordFragment': false,  // 19171: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > fastForward(null) + progress': false,  // 19172: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > once([0,0,-2]) + newWordFragment': false,  // 19196: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > once([0,0,-2]) + progress': false,  // 19197: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > once([0,0,-2]) + stopBegin': false,  // 19198: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > once([0,0,-2]) + stopFinish': false,  // 19199: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > once([0,0,-2]) + done': false,  // 19200: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > once([0,0,2]) + newWordFragment': false,  // 19221: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > once([0,0,2]) + progress': false,  // 19222: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > current(null) + newWordFragment': false,  // 19246: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > current(null) + progress': false,  // 19247: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > jumpWords(-3) + newWordFragment': false,  // 19421: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > jumpWords(-3) + progress': false,  // 19422: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > jumpWords(-3) + stopBegin': false,  // 19423: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > jumpWords(-3) + stopFinish': false,  // 19424: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > jumpWords(-3) + done': false,  // 19425: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > jumpWords(-1) + newWordFragment': false,  // 19446: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > jumpWords(-1) + progress': false,  // 19447: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > jumpWords(-1) + stopBegin': false,  // 19448: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > jumpWords(-1) + stopFinish': false,  // 19449: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > jumpWords(-1) + done': false,  // 19450: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > jumpWords(0) + newWordFragment': false,  // 19471: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > jumpWords(0) + progress': false,  // 19472: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > jumpWords(4) + newWordFragment': false,  // 19496: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > jumpWords(4) + progress': false,  // 19497: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > jumpSentences(-1) + newWordFragment': false,  // 19596: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > jumpSentences(-1) + progress': false,  // 19597: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > jumpSentences(-1) + stopBegin': false,  // 19598: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > jumpSentences(-1) + stopFinish': false,  // 19599: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > jumpSentences(-1) + done': false,  // 19600: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > jumpSentences(0) + newWordFragment': false,  // 19621: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > jumpSentences(0) + progress': false,  // 19622: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > jumpSentences(1) + newWordFragment': false,  // 19646: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > jumpSentences(1) + progress': false,  // 19647: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > jumpSentences(3) + newWordFragment': false,  // 19671: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > jumpSentences(3) + progress': false,  // 19672: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > jumpSentences(3) + stopBegin': false,  // 19673: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > jumpSentences(3) + stopFinish': false,  // 19674: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > jumpSentences(3) + done': false,  // 19675: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > nextWord(null) + newWordFragment': false,  // 19721: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > nextWord(null) + progress': false,  // 19722: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > nextSentence(null) + newWordFragment': false,  // 19746: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > nextSentence(null) + progress': false,  // 19747: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > prevWord(null) + newWordFragment': false,  // 19771: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > prevWord(null) + progress': false,  // 19772: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > prevWord(null) + stopBegin': false,  // 19773: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > prevWord(null) + stopFinish': false,  // 19774: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > prevWord(null) + done': false,  // 19775: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > prevSentence(null) + newWordFragment': false,  // 19796: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > prevSentence(null) + progress': false,  // 19797: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > prevSentence(null) + stopBegin': false,  // 19798: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > prevSentence(null) + stopFinish': false,  // 19799: ?? (skip results are different than combos)
		'skip-combos: jumpTo(6) + progress > prevSentence(null) + done': false,  // 19800: ?? (skip results are different than combos)



		// ============== jumpTo(11) ==============
		// #? like play in some ways (other differences?)
		// #6 immediately leading to stop*|done > some more types of relative loopers(+|0|-) get different output than usual
		// #7 immediately leading to stop*|done > relative single(not rewind) loopers(-) don't trigger stop*|done
		// #8 immediately leading to stop*|done > relative loopers(+|0) do unexpectedly trigger stop*|done
		// #9 immediately leading to stop*|done > play|toggle = triggers `restart` instead of `play`
		'skip-combos: jumpTo(11) + onceBegin > play(null) + playBegin': false,  // 9076: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > play(null) + playFinish': false,  // 9077: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > play(null) + restartBegin': false,  // 9080: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > play(null) + restartFinish': false,  // 9081: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > toggle(null) + playBegin': false,  // 9101: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > toggle(null) + playFinish': false,  // 9102: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > toggle(null) + restartBegin': false,  // 9105: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > toggle(null) + restartFinish': false,  // 9106: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > rewind(null) + newWordFragment': false,  // 9246: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > rewind(null) + progress': false,  // 9247: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > fastForward(null) + newWordFragment': false,  // 9271: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > fastForward(null) + progress': false,  // 9272: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > once([0,0,-2]) + newWordFragment': false,  // 9296: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > once([0,0,-2]) + progress': false,  // 9297: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > once([0,0,-2]) + stopBegin': false,  // 9298: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > once([0,0,-2]) + stopFinish': false,  // 9299: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > once([0,0,-2]) + done': false,  // 9300: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > once([0,0,2]) + newWordFragment': false,  // 9321: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > once([0,0,2]) + progress': false,  // 9322: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > once([0,0,2]) + stopBegin': false,  // 9323: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > once([0,0,2]) + stopFinish': false,  // 9324: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > once([0,0,2]) + done': false,  // 9325: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > current(null) + newWordFragment': false,  // 9346: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > current(null) + progress': false,  // 9347: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > current(null) + stopBegin': false,  // 9348: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > current(null) + stopFinish': false,  // 9349: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > current(null) + done': false,  // 9350: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpWords(-3) + newWordFragment': false,  // 9521: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpWords(-3) + progress': false,  // 9522: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpWords(-3) + stopBegin': false,  // 9523: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpWords(-3) + stopFinish': false,  // 9524: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpWords(-3) + done': false,  // 9525: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpWords(-1) + newWordFragment': false,  // 9546: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpWords(-1) + progress': false,  // 9547: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpWords(-1) + stopBegin': false,  // 9548: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpWords(-1) + stopFinish': false,  // 9549: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpWords(-1) + done': false,  // 9550: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpWords(0) + newWordFragment': false,  // 9571: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpWords(0) + progress': false,  // 9572: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpWords(0) + stopBegin': false,  // 9573: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpWords(0) + stopFinish': false,  // 9574: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpWords(0) + done': false,  // 9575: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpWords(4) + newWordFragment': false,  // 9596: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpWords(4) + progress': false,  // 9597: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpWords(4) + stopBegin': false,  // 9598: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpWords(4) + stopFinish': false,  // 9599: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpWords(4) + done': false,  // 9600: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpSentences(-1) + newWordFragment': false,  // 9696: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpSentences(-1) + progress': false,  // 9697: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpSentences(-1) + stopBegin': false,  // 9698: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpSentences(-1) + stopFinish': false,  // 9699: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpSentences(-1) + done': false,  // 9700: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpSentences(0) + newWordFragment': false,  // 9721: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpSentences(0) + progress': false,  // 9722: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpSentences(0) + stopBegin': false,  // 9723: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpSentences(0) + stopFinish': false,  // 9724: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpSentences(0) + done': false,  // 9725: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpSentences(1) + newWordFragment': false,  // 9746: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpSentences(1) + progress': false,  // 9747: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpSentences(1) + stopBegin': false,  // 9748: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpSentences(1) + stopFinish': false,  // 9749: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpSentences(1) + done': false,  // 9750: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpSentences(3) + newWordFragment': false,  // 9771: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpSentences(3) + progress': false,  // 9772: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpSentences(3) + stopBegin': false,  // 9773: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpSentences(3) + stopFinish': false,  // 9774: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > jumpSentences(3) + done': false,  // 9775: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > nextWord(null) + newWordFragment': false,  // 9821: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > nextWord(null) + progress': false,  // 9822: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > nextWord(null) + stopBegin': false,  // 9823: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > nextWord(null) + stopFinish': false,  // 9824: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > nextWord(null) + done': false,  // 9825: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > nextSentence(null) + newWordFragment': false,  // 9846: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > nextSentence(null) + progress': false,  // 9847: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > nextSentence(null) + stopBegin': false,  // 9848: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > nextSentence(null) + stopFinish': false,  // 9849: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > nextSentence(null) + done': false,  // 9850: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > prevWord(null) + newWordFragment': false,  // 9871: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > prevWord(null) + progress': false,  // 9872: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > prevWord(null) + stopBegin': false,  // 9873: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > prevWord(null) + stopFinish': false,  // 9874: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > prevWord(null) + done': false,  // 9875: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > prevSentence(null) + newWordFragment': false,  // 9896: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > prevSentence(null) + progress': false,  // 9897: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > prevSentence(null) + stopBegin': false,  // 9898: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > prevSentence(null) + stopFinish': false,  // 9899: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceBegin > prevSentence(null) + done': false,  // 9900: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > play(null) + playBegin': false,  // 9976: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > play(null) + playFinish': false,  // 9977: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > play(null) + restartBegin': false,  // 9980: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > play(null) + restartFinish': false,  // 9981: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > toggle(null) + playBegin': false,  // 10001: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > toggle(null) + playFinish': false,  // 10002: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > toggle(null) + restartBegin': false,  // 10005: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > toggle(null) + restartFinish': false,  // 10006: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > rewind(null) + newWordFragment': false,  // 10146: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > rewind(null) + progress': false,  // 10147: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > fastForward(null) + newWordFragment': false,  // 10171: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > fastForward(null) + progress': false,  // 10172: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > once([0,0,-2]) + newWordFragment': false,  // 10196: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > once([0,0,-2]) + progress': false,  // 10197: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > once([0,0,-2]) + stopBegin': false,  // 10198: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > once([0,0,-2]) + stopFinish': false,  // 10199: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > once([0,0,-2]) + done': false,  // 10200: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > once([0,0,2]) + newWordFragment': false,  // 10221: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > once([0,0,2]) + progress': false,  // 10222: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > once([0,0,2]) + stopBegin': false,  // 10223: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > once([0,0,2]) + stopFinish': false,  // 10224: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > once([0,0,2]) + done': false,  // 10225: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > current(null) + newWordFragment': false,  // 10246: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > current(null) + progress': false,  // 10247: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > current(null) + stopBegin': false,  // 10248: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > current(null) + stopFinish': false,  // 10249: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > current(null) + done': false,  // 10250: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpWords(-3) + newWordFragment': false,  // 10421: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpWords(-3) + progress': false,  // 10422: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpWords(-3) + stopBegin': false,  // 10423: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpWords(-3) + stopFinish': false,  // 10424: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpWords(-3) + done': false,  // 10425: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpWords(-1) + newWordFragment': false,  // 10446: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpWords(-1) + progress': false,  // 10447: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpWords(-1) + stopBegin': false,  // 10448: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpWords(-1) + stopFinish': false,  // 10449: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpWords(-1) + done': false,  // 10450: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpWords(0) + newWordFragment': false,  // 10471: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpWords(0) + progress': false,  // 10472: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpWords(0) + stopBegin': false,  // 10473: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpWords(0) + stopFinish': false,  // 10474: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpWords(0) + done': false,  // 10475: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpWords(4) + newWordFragment': false,  // 10496: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpWords(4) + progress': false,  // 10497: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpWords(4) + stopBegin': false,  // 10498: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpWords(4) + stopFinish': false,  // 10499: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpWords(4) + done': false,  // 10500: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpSentences(-1) + newWordFragment': false,  // 10596: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpSentences(-1) + progress': false,  // 10597: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpSentences(-1) + stopBegin': false,  // 10598: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpSentences(-1) + stopFinish': false,  // 10599: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpSentences(-1) + done': false,  // 10600: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpSentences(0) + newWordFragment': false,  // 10621: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpSentences(0) + progress': false,  // 10622: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpSentences(0) + stopBegin': false,  // 10623: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpSentences(0) + stopFinish': false,  // 10624: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpSentences(0) + done': false,  // 10625: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpSentences(1) + newWordFragment': false,  // 10646: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpSentences(1) + progress': false,  // 10647: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpSentences(1) + stopBegin': false,  // 10648: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpSentences(1) + stopFinish': false,  // 10649: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpSentences(1) + done': false,  // 10650: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpSentences(3) + newWordFragment': false,  // 10671: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpSentences(3) + progress': false,  // 10672: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpSentences(3) + stopBegin': false,  // 10673: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpSentences(3) + stopFinish': false,  // 10674: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > jumpSentences(3) + done': false,  // 10675: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > nextWord(null) + newWordFragment': false,  // 10721: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > nextWord(null) + progress': false,  // 10722: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > nextWord(null) + stopBegin': false,  // 10723: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > nextWord(null) + stopFinish': false,  // 10724: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > nextWord(null) + done': false,  // 10725: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > nextSentence(null) + newWordFragment': false,  // 10746: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > nextSentence(null) + progress': false,  // 10747: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > nextSentence(null) + stopBegin': false,  // 10748: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > nextSentence(null) + stopFinish': false,  // 10749: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > nextSentence(null) + done': false,  // 10750: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > prevWord(null) + newWordFragment': false,  // 10771: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > prevWord(null) + progress': false,  // 10772: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > prevWord(null) + stopBegin': false,  // 10773: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > prevWord(null) + stopFinish': false,  // 10774: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > prevWord(null) + done': false,  // 10775: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > prevSentence(null) + newWordFragment': false,  // 10796: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > prevSentence(null) + progress': false,  // 10797: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > prevSentence(null) + stopBegin': false,  // 10798: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > prevSentence(null) + stopFinish': false,  // 10799: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + onceFinish > prevSentence(null) + done': false,  // 10800: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > play(null) + playBegin': false,  // 16276: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > play(null) + playFinish': false,  // 16277: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > play(null) + restartBegin': false,  // 16280: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > play(null) + restartFinish': false,  // 16281: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > toggle(null) + playBegin': false,  // 16301: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > toggle(null) + playFinish': false,  // 16302: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > toggle(null) + restartBegin': false,  // 16305: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > toggle(null) + restartFinish': false,  // 16306: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > rewind(null) + newWordFragment': false,  // 16446: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > rewind(null) + progress': false,  // 16447: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > fastForward(null) + newWordFragment': false,  // 16471: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > fastForward(null) + progress': false,  // 16472: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > once([0,0,-2]) + newWordFragment': false,  // 16496: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > once([0,0,-2]) + progress': false,  // 16497: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > once([0,0,-2]) + stopBegin': false,  // 16498: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > once([0,0,-2]) + stopFinish': false,  // 16499: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > once([0,0,-2]) + done': false,  // 16500: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > once([0,0,2]) + newWordFragment': false,  // 16521: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > once([0,0,2]) + progress': false,  // 16522: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > once([0,0,2]) + stopBegin': false,  // 16523: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > once([0,0,2]) + stopFinish': false,  // 16524: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > once([0,0,2]) + done': false,  // 16525: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > current(null) + newWordFragment': false,  // 16546: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > current(null) + progress': false,  // 16547: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > current(null) + stopBegin': false,  // 16548: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > current(null) + stopFinish': false,  // 16549: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > current(null) + done': false,  // 16550: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpWords(-3) + newWordFragment': false,  // 16721: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpWords(-3) + progress': false,  // 16722: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpWords(-3) + stopBegin': false,  // 16723: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpWords(-3) + stopFinish': false,  // 16724: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpWords(-3) + done': false,  // 16725: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpWords(-1) + newWordFragment': false,  // 16746: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpWords(-1) + progress': false,  // 16747: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpWords(-1) + stopBegin': false,  // 16748: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpWords(-1) + stopFinish': false,  // 16749: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpWords(-1) + done': false,  // 16750: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpWords(0) + newWordFragment': false,  // 16771: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpWords(0) + progress': false,  // 16772: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpWords(0) + stopBegin': false,  // 16773: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpWords(0) + stopFinish': false,  // 16774: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpWords(0) + done': false,  // 16775: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpWords(4) + newWordFragment': false,  // 16796: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpWords(4) + progress': false,  // 16797: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpWords(4) + stopBegin': false,  // 16798: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpWords(4) + stopFinish': false,  // 16799: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpWords(4) + done': false,  // 16800: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpSentences(-1) + newWordFragment': false,  // 16896: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpSentences(-1) + progress': false,  // 16897: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpSentences(-1) + stopBegin': false,  // 16898: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpSentences(-1) + stopFinish': false,  // 16899: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpSentences(-1) + done': false,  // 16900: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpSentences(0) + newWordFragment': false,  // 16921: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpSentences(0) + progress': false,  // 16922: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpSentences(0) + stopBegin': false,  // 16923: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpSentences(0) + stopFinish': false,  // 16924: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpSentences(0) + done': false,  // 16925: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpSentences(1) + newWordFragment': false,  // 16946: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpSentences(1) + progress': false,  // 16947: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpSentences(1) + stopBegin': false,  // 16948: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpSentences(1) + stopFinish': false,  // 16949: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpSentences(1) + done': false,  // 16950: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpSentences(3) + newWordFragment': false,  // 16971: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpSentences(3) + progress': false,  // 16972: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpSentences(3) + stopBegin': false,  // 16973: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpSentences(3) + stopFinish': false,  // 16974: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > jumpSentences(3) + done': false,  // 16975: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > nextWord(null) + newWordFragment': false,  // 17021: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > nextWord(null) + progress': false,  // 17022: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > nextWord(null) + stopBegin': false,  // 17023: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > nextWord(null) + stopFinish': false,  // 17024: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > nextWord(null) + done': false,  // 17025: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > nextSentence(null) + newWordFragment': false,  // 17046: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > nextSentence(null) + progress': false,  // 17047: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > nextSentence(null) + stopBegin': false,  // 17048: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > nextSentence(null) + stopFinish': false,  // 17049: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > nextSentence(null) + done': false,  // 17050: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > prevWord(null) + newWordFragment': false,  // 17071: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > prevWord(null) + progress': false,  // 17072: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > prevWord(null) + stopBegin': false,  // 17073: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > prevWord(null) + stopFinish': false,  // 17074: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > prevWord(null) + done': false,  // 17075: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > prevSentence(null) + newWordFragment': false,  // 17096: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > prevSentence(null) + progress': false,  // 17097: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > prevSentence(null) + stopBegin': false,  // 17098: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > prevSentence(null) + stopFinish': false,  // 17099: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopBegin > prevSentence(null) + done': false,  // 17100: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > play(null) + playBegin': false,  // 17176: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > play(null) + playFinish': false,  // 17177: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > play(null) + restartBegin': false,  // 17180: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > play(null) + restartFinish': false,  // 17181: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > toggle(null) + playBegin': false,  // 17201: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > toggle(null) + playFinish': false,  // 17202: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > toggle(null) + restartBegin': false,  // 17205: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > toggle(null) + restartFinish': false,  // 17206: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > rewind(null) + newWordFragment': false,  // 17346: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > rewind(null) + progress': false,  // 17347: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > fastForward(null) + newWordFragment': false,  // 17371: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > fastForward(null) + progress': false,  // 17372: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > once([0,0,-2]) + newWordFragment': false,  // 17396: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > once([0,0,-2]) + progress': false,  // 17397: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > once([0,0,-2]) + stopBegin': false,  // 17398: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > once([0,0,-2]) + stopFinish': false,  // 17399: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > once([0,0,-2]) + done': false,  // 17400: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > once([0,0,2]) + newWordFragment': false,  // 17421: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > once([0,0,2]) + progress': false,  // 17422: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > once([0,0,2]) + stopBegin': false,  // 17423: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > once([0,0,2]) + stopFinish': false,  // 17424: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > once([0,0,2]) + done': false,  // 17425: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > current(null) + newWordFragment': false,  // 17446: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > current(null) + progress': false,  // 17447: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > current(null) + stopBegin': false,  // 17448: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > current(null) + stopFinish': false,  // 17449: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > current(null) + done': false,  // 17450: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpWords(-3) + newWordFragment': false,  // 17621: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpWords(-3) + progress': false,  // 17622: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpWords(-3) + stopBegin': false,  // 17623: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpWords(-3) + stopFinish': false,  // 17624: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpWords(-3) + done': false,  // 17625: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpWords(-1) + newWordFragment': false,  // 17646: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpWords(-1) + progress': false,  // 17647: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpWords(-1) + stopBegin': false,  // 17648: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpWords(-1) + stopFinish': false,  // 17649: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpWords(-1) + done': false,  // 17650: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpWords(0) + newWordFragment': false,  // 17671: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpWords(0) + progress': false,  // 17672: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpWords(0) + stopBegin': false,  // 17673: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpWords(0) + stopFinish': false,  // 17674: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpWords(0) + done': false,  // 17675: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpWords(4) + newWordFragment': false,  // 17696: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpWords(4) + progress': false,  // 17697: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpWords(4) + stopBegin': false,  // 17698: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpWords(4) + stopFinish': false,  // 17699: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpWords(4) + done': false,  // 17700: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpSentences(-1) + newWordFragment': false,  // 17796: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpSentences(-1) + progress': false,  // 17797: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpSentences(-1) + stopBegin': false,  // 17798: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpSentences(-1) + stopFinish': false,  // 17799: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpSentences(-1) + done': false,  // 17800: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpSentences(0) + newWordFragment': false,  // 17821: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpSentences(0) + progress': false,  // 17822: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpSentences(0) + stopBegin': false,  // 17823: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpSentences(0) + stopFinish': false,  // 17824: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpSentences(0) + done': false,  // 17825: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpSentences(1) + newWordFragment': false,  // 17846: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpSentences(1) + progress': false,  // 17847: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpSentences(1) + stopBegin': false,  // 17848: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpSentences(1) + stopFinish': false,  // 17849: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpSentences(1) + done': false,  // 17850: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpSentences(3) + newWordFragment': false,  // 17871: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpSentences(3) + progress': false,  // 17872: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpSentences(3) + stopBegin': false,  // 17873: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpSentences(3) + stopFinish': false,  // 17874: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > jumpSentences(3) + done': false,  // 17875: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > nextWord(null) + newWordFragment': false,  // 17921: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > nextWord(null) + progress': false,  // 17922: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > nextWord(null) + stopBegin': false,  // 17923: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > nextWord(null) + stopFinish': false,  // 17924: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > nextWord(null) + done': false,  // 17925: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > nextSentence(null) + newWordFragment': false,  // 17946: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > nextSentence(null) + progress': false,  // 17947: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > nextSentence(null) + stopBegin': false,  // 17948: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > nextSentence(null) + stopFinish': false,  // 17949: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > nextSentence(null) + done': false,  // 17950: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > prevWord(null) + newWordFragment': false,  // 17971: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > prevWord(null) + progress': false,  // 17972: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > prevWord(null) + stopBegin': false,  // 17973: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > prevWord(null) + stopFinish': false,  // 17974: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > prevWord(null) + done': false,  // 17975: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > prevSentence(null) + newWordFragment': false,  // 17996: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > prevSentence(null) + progress': false,  // 17997: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > prevSentence(null) + stopBegin': false,  // 17998: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > prevSentence(null) + stopFinish': false,  // 17999: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + loopFinish > prevSentence(null) + done': false,  // 18000: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > play(null) + playBegin': false,  // 18076: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > play(null) + playFinish': false,  // 18077: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > play(null) + restartBegin': false,  // 18080: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > play(null) + restartFinish': false,  // 18081: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > toggle(null) + playBegin': false,  // 18101: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > toggle(null) + playFinish': false,  // 18102: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > toggle(null) + restartBegin': false,  // 18105: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > toggle(null) + restartFinish': false,  // 18106: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > rewind(null) + newWordFragment': false,  // 18246: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > rewind(null) + progress': false,  // 18247: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > fastForward(null) + newWordFragment': false,  // 18271: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > fastForward(null) + progress': false,  // 18272: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > once([0,0,-2]) + newWordFragment': false,  // 18296: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > once([0,0,-2]) + progress': false,  // 18297: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > once([0,0,-2]) + stopBegin': false,  // 18298: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > once([0,0,-2]) + stopFinish': false,  // 18299: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > once([0,0,-2]) + done': false,  // 18300: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > once([0,0,2]) + newWordFragment': false,  // 18321: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > once([0,0,2]) + progress': false,  // 18322: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > once([0,0,2]) + stopBegin': false,  // 18323: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > once([0,0,2]) + stopFinish': false,  // 18324: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > once([0,0,2]) + done': false,  // 18325: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > current(null) + newWordFragment': false,  // 18346: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > current(null) + progress': false,  // 18347: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > current(null) + stopBegin': false,  // 18348: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > current(null) + stopFinish': false,  // 18349: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > current(null) + done': false,  // 18350: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpWords(-3) + newWordFragment': false,  // 18521: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpWords(-3) + progress': false,  // 18522: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpWords(-3) + stopBegin': false,  // 18523: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpWords(-3) + stopFinish': false,  // 18524: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpWords(-3) + done': false,  // 18525: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpWords(-1) + newWordFragment': false,  // 18546: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpWords(-1) + progress': false,  // 18547: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpWords(-1) + stopBegin': false,  // 18548: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpWords(-1) + stopFinish': false,  // 18549: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpWords(-1) + done': false,  // 18550: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpWords(0) + newWordFragment': false,  // 18571: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpWords(0) + progress': false,  // 18572: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpWords(0) + stopBegin': false,  // 18573: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpWords(0) + stopFinish': false,  // 18574: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpWords(0) + done': false,  // 18575: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpWords(4) + newWordFragment': false,  // 18596: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpWords(4) + progress': false,  // 18597: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpWords(4) + stopBegin': false,  // 18598: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpWords(4) + stopFinish': false,  // 18599: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpWords(4) + done': false,  // 18600: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpSentences(-1) + newWordFragment': false,  // 18696: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpSentences(-1) + progress': false,  // 18697: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpSentences(-1) + stopBegin': false,  // 18698: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpSentences(-1) + stopFinish': false,  // 18699: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpSentences(-1) + done': false,  // 18700: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpSentences(0) + newWordFragment': false,  // 18721: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpSentences(0) + progress': false,  // 18722: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpSentences(0) + stopBegin': false,  // 18723: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpSentences(0) + stopFinish': false,  // 18724: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpSentences(0) + done': false,  // 18725: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpSentences(1) + newWordFragment': false,  // 18746: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpSentences(1) + progress': false,  // 18747: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpSentences(1) + stopBegin': false,  // 18748: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpSentences(1) + stopFinish': false,  // 18749: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpSentences(1) + done': false,  // 18750: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpSentences(3) + newWordFragment': false,  // 18771: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpSentences(3) + progress': false,  // 18772: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpSentences(3) + stopBegin': false,  // 18773: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpSentences(3) + stopFinish': false,  // 18774: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > jumpSentences(3) + done': false,  // 18775: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > nextWord(null) + newWordFragment': false,  // 18821: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > nextWord(null) + progress': false,  // 18822: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > nextWord(null) + stopBegin': false,  // 18823: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > nextWord(null) + stopFinish': false,  // 18824: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > nextWord(null) + done': false,  // 18825: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > nextSentence(null) + newWordFragment': false,  // 18846: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > nextSentence(null) + progress': false,  // 18847: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > nextSentence(null) + stopBegin': false,  // 18848: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > nextSentence(null) + stopFinish': false,  // 18849: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > nextSentence(null) + done': false,  // 18850: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > prevWord(null) + newWordFragment': false,  // 18871: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > prevWord(null) + progress': false,  // 18872: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > prevWord(null) + stopBegin': false,  // 18873: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > prevWord(null) + stopFinish': false,  // 18874: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > prevWord(null) + done': false,  // 18875: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > prevSentence(null) + newWordFragment': false,  // 18896: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > prevSentence(null) + progress': false,  // 18897: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > prevSentence(null) + stopBegin': false,  // 18898: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > prevSentence(null) + stopFinish': false,  // 18899: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + newWordFragment > prevSentence(null) + done': false,  // 18900: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > play(null) + playBegin': false,  // 18976: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > play(null) + playFinish': false,  // 18977: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > play(null) + restartBegin': false,  // 18980: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > play(null) + restartFinish': false,  // 18981: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > toggle(null) + playBegin': false,  // 19001: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > toggle(null) + playFinish': false,  // 19002: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > toggle(null) + restartBegin': false,  // 19005: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > toggle(null) + restartFinish': false,  // 19006: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > rewind(null) + newWordFragment': false,  // 19146: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > rewind(null) + progress': false,  // 19147: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > fastForward(null) + newWordFragment': false,  // 19171: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > fastForward(null) + progress': false,  // 19172: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > once([0,0,-2]) + newWordFragment': false,  // 19196: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > once([0,0,-2]) + progress': false,  // 19197: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > once([0,0,-2]) + stopBegin': false,  // 19198: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > once([0,0,-2]) + stopFinish': false,  // 19199: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > once([0,0,-2]) + done': false,  // 19200: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > once([0,0,2]) + newWordFragment': false,  // 19221: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > once([0,0,2]) + progress': false,  // 19222: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > once([0,0,2]) + stopBegin': false,  // 19223: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > once([0,0,2]) + stopFinish': false,  // 19224: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > once([0,0,2]) + done': false,  // 19225: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > current(null) + newWordFragment': false,  // 19246: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > current(null) + progress': false,  // 19247: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > current(null) + stopBegin': false,  // 19248: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > current(null) + stopFinish': false,  // 19249: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > current(null) + done': false,  // 19250: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpWords(-3) + newWordFragment': false,  // 19421: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpWords(-3) + progress': false,  // 19422: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpWords(-3) + stopBegin': false,  // 19423: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpWords(-3) + stopFinish': false,  // 19424: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpWords(-3) + done': false,  // 19425: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpWords(-1) + newWordFragment': false,  // 19446: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpWords(-1) + progress': false,  // 19447: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpWords(-1) + stopBegin': false,  // 19448: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpWords(-1) + stopFinish': false,  // 19449: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpWords(-1) + done': false,  // 19450: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpWords(0) + newWordFragment': false,  // 19471: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpWords(0) + progress': false,  // 19472: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpWords(0) + stopBegin': false,  // 19473: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpWords(0) + stopFinish': false,  // 19474: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpWords(0) + done': false,  // 19475: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpWords(4) + newWordFragment': false,  // 19496: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpWords(4) + progress': false,  // 19497: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpWords(4) + stopBegin': false,  // 19498: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpWords(4) + stopFinish': false,  // 19499: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpWords(4) + done': false,  // 19500: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpSentences(-1) + newWordFragment': false,  // 19596: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpSentences(-1) + progress': false,  // 19597: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpSentences(-1) + stopBegin': false,  // 19598: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpSentences(-1) + stopFinish': false,  // 19599: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpSentences(-1) + done': false,  // 19600: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpSentences(0) + newWordFragment': false,  // 19621: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpSentences(0) + progress': false,  // 19622: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpSentences(0) + stopBegin': false,  // 19623: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpSentences(0) + stopFinish': false,  // 19624: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpSentences(0) + done': false,  // 19625: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpSentences(1) + newWordFragment': false,  // 19646: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpSentences(1) + progress': false,  // 19647: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpSentences(1) + stopBegin': false,  // 19648: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpSentences(1) + stopFinish': false,  // 19649: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpSentences(1) + done': false,  // 19650: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpSentences(3) + newWordFragment': false,  // 19671: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpSentences(3) + progress': false,  // 19672: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpSentences(3) + stopBegin': false,  // 19673: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpSentences(3) + stopFinish': false,  // 19674: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > jumpSentences(3) + done': false,  // 19675: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > nextWord(null) + newWordFragment': false,  // 19721: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > nextWord(null) + progress': false,  // 19722: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > nextWord(null) + stopBegin': false,  // 19723: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > nextWord(null) + stopFinish': false,  // 19724: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > nextWord(null) + done': false,  // 19725: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > nextSentence(null) + newWordFragment': false,  // 19746: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > nextSentence(null) + progress': false,  // 19747: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > nextSentence(null) + stopBegin': false,  // 19748: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > nextSentence(null) + stopFinish': false,  // 19749: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > nextSentence(null) + done': false,  // 19750: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > prevWord(null) + newWordFragment': false,  // 19771: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > prevWord(null) + progress': false,  // 19772: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > prevWord(null) + stopBegin': false,  // 19773: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > prevWord(null) + stopFinish': false,  // 19774: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > prevWord(null) + done': false,  // 19775: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > prevSentence(null) + newWordFragment': false,  // 19796: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > prevSentence(null) + progress': false,  // 19797: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > prevSentence(null) + stopBegin': false,  // 19798: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > prevSentence(null) + stopFinish': false,  // 19799: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + progress > prevSentence(null) + done': false,  // 19800: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > play(null) + playBegin': false,  // 19876: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > play(null) + playFinish': false,  // 19877: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > play(null) + restartBegin': false,  // 19880: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > play(null) + restartFinish': false,  // 19881: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > toggle(null) + playBegin': false,  // 19901: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > toggle(null) + playFinish': false,  // 19902: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > toggle(null) + restartBegin': false,  // 19905: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > toggle(null) + restartFinish': false,  // 19906: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > rewind(null) + newWordFragment': false,  // 20046: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > rewind(null) + progress': false,  // 20047: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > fastForward(null) + newWordFragment': false,  // 20071: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > fastForward(null) + progress': false,  // 20072: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > once([0,0,-2]) + newWordFragment': false,  // 20096: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > once([0,0,-2]) + progress': false,  // 20097: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > once([0,0,-2]) + stopBegin': false,  // 20098: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > once([0,0,-2]) + stopFinish': false,  // 20099: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > once([0,0,-2]) + done': false,  // 20100: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > once([0,0,2]) + newWordFragment': false,  // 20121: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > once([0,0,2]) + progress': false,  // 20122: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > once([0,0,2]) + stopBegin': false,  // 20123: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > once([0,0,2]) + stopFinish': false,  // 20124: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > once([0,0,2]) + done': false,  // 20125: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > current(null) + newWordFragment': false,  // 20146: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > current(null) + progress': false,  // 20147: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > current(null) + stopBegin': false,  // 20148: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > current(null) + stopFinish': false,  // 20149: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > current(null) + done': false,  // 20150: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpWords(-3) + newWordFragment': false,  // 20321: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpWords(-3) + progress': false,  // 20322: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpWords(-3) + stopBegin': false,  // 20323: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpWords(-3) + stopFinish': false,  // 20324: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpWords(-3) + done': false,  // 20325: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpWords(-1) + newWordFragment': false,  // 20346: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpWords(-1) + progress': false,  // 20347: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpWords(-1) + stopBegin': false,  // 20348: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpWords(-1) + stopFinish': false,  // 20349: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpWords(-1) + done': false,  // 20350: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpWords(0) + newWordFragment': false,  // 20371: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpWords(0) + progress': false,  // 20372: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpWords(0) + stopBegin': false,  // 20373: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpWords(0) + stopFinish': false,  // 20374: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpWords(0) + done': false,  // 20375: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpWords(4) + newWordFragment': false,  // 20396: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpWords(4) + progress': false,  // 20397: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpWords(4) + stopBegin': false,  // 20398: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpWords(4) + stopFinish': false,  // 20399: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpWords(4) + done': false,  // 20400: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpSentences(-1) + newWordFragment': false,  // 20496: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpSentences(-1) + progress': false,  // 20497: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpSentences(-1) + stopBegin': false,  // 20498: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpSentences(-1) + stopFinish': false,  // 20499: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpSentences(-1) + done': false,  // 20500: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpSentences(0) + newWordFragment': false,  // 20521: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpSentences(0) + progress': false,  // 20522: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpSentences(0) + stopBegin': false,  // 20523: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpSentences(0) + stopFinish': false,  // 20524: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpSentences(0) + done': false,  // 20525: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpSentences(1) + newWordFragment': false,  // 20546: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpSentences(1) + progress': false,  // 20547: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpSentences(1) + stopBegin': false,  // 20548: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpSentences(1) + stopFinish': false,  // 20549: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpSentences(1) + done': false,  // 20550: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpSentences(3) + newWordFragment': false,  // 20571: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpSentences(3) + progress': false,  // 20572: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpSentences(3) + stopBegin': false,  // 20573: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpSentences(3) + stopFinish': false,  // 20574: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > jumpSentences(3) + done': false,  // 20575: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > nextWord(null) + newWordFragment': false,  // 20621: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > nextWord(null) + progress': false,  // 20622: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > nextWord(null) + stopBegin': false,  // 20623: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > nextWord(null) + stopFinish': false,  // 20624: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > nextWord(null) + done': false,  // 20625: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > nextSentence(null) + newWordFragment': false,  // 20646: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > nextSentence(null) + progress': false,  // 20647: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > nextSentence(null) + stopBegin': false,  // 20648: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > nextSentence(null) + stopFinish': false,  // 20649: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > nextSentence(null) + done': false,  // 20650: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > prevWord(null) + newWordFragment': false,  // 20671: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > prevWord(null) + progress': false,  // 20672: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > prevWord(null) + stopBegin': false,  // 20673: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > prevWord(null) + stopFinish': false,  // 20674: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > prevWord(null) + done': false,  // 20675: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > prevSentence(null) + newWordFragment': false,  // 20696: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > prevSentence(null) + progress': false,  // 20697: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > prevSentence(null) + stopBegin': false,  // 20698: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > prevSentence(null) + stopFinish': false,  // 20699: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopBegin > prevSentence(null) + done': false,  // 20700: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > play(null) + playBegin': false,  // 20776: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > play(null) + playFinish': false,  // 20777: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > play(null) + restartBegin': false,  // 20780: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > play(null) + restartFinish': false,  // 20781: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > toggle(null) + playBegin': false,  // 20801: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > toggle(null) + playFinish': false,  // 20802: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > toggle(null) + restartBegin': false,  // 20805: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > toggle(null) + restartFinish': false,  // 20806: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > rewind(null) + newWordFragment': false,  // 20946: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > rewind(null) + progress': false,  // 20947: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > fastForward(null) + newWordFragment': false,  // 20971: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > fastForward(null) + progress': false,  // 20972: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > once([0,0,-2]) + newWordFragment': false,  // 20996: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > once([0,0,-2]) + progress': false,  // 20997: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > once([0,0,-2]) + stopBegin': false,  // 20998: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > once([0,0,-2]) + stopFinish': false,  // 20999: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > once([0,0,-2]) + done': false,  // 21000: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > once([0,0,2]) + newWordFragment': false,  // 21021: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > once([0,0,2]) + progress': false,  // 21022: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > once([0,0,2]) + stopBegin': false,  // 21023: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > once([0,0,2]) + stopFinish': false,  // 21024: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > once([0,0,2]) + done': false,  // 21025: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > current(null) + newWordFragment': false,  // 21046: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > current(null) + progress': false,  // 21047: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > current(null) + stopBegin': false,  // 21048: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > current(null) + stopFinish': false,  // 21049: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > current(null) + done': false,  // 21050: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpWords(-3) + newWordFragment': false,  // 21221: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpWords(-3) + progress': false,  // 21222: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpWords(-3) + stopBegin': false,  // 21223: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpWords(-3) + stopFinish': false,  // 21224: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpWords(-3) + done': false,  // 21225: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpWords(-1) + newWordFragment': false,  // 21246: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpWords(-1) + progress': false,  // 21247: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpWords(-1) + stopBegin': false,  // 21248: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpWords(-1) + stopFinish': false,  // 21249: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpWords(-1) + done': false,  // 21250: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpWords(0) + newWordFragment': false,  // 21271: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpWords(0) + progress': false,  // 21272: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpWords(0) + stopBegin': false,  // 21273: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpWords(0) + stopFinish': false,  // 21274: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpWords(0) + done': false,  // 21275: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpWords(4) + newWordFragment': false,  // 21296: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpWords(4) + progress': false,  // 21297: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpWords(4) + stopBegin': false,  // 21298: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpWords(4) + stopFinish': false,  // 21299: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpWords(4) + done': false,  // 21300: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpSentences(-1) + newWordFragment': false,  // 21396: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpSentences(-1) + progress': false,  // 21397: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpSentences(-1) + stopBegin': false,  // 21398: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpSentences(-1) + stopFinish': false,  // 21399: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpSentences(-1) + done': false,  // 21400: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpSentences(0) + newWordFragment': false,  // 21421: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpSentences(0) + progress': false,  // 21422: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpSentences(0) + stopBegin': false,  // 21423: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpSentences(0) + stopFinish': false,  // 21424: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpSentences(0) + done': false,  // 21425: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpSentences(1) + newWordFragment': false,  // 21446: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpSentences(1) + progress': false,  // 21447: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpSentences(1) + stopBegin': false,  // 21448: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpSentences(1) + stopFinish': false,  // 21449: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpSentences(1) + done': false,  // 21450: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpSentences(3) + newWordFragment': false,  // 21471: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpSentences(3) + progress': false,  // 21472: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpSentences(3) + stopBegin': false,  // 21473: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpSentences(3) + stopFinish': false,  // 21474: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > jumpSentences(3) + done': false,  // 21475: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > nextWord(null) + newWordFragment': false,  // 21521: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > nextWord(null) + progress': false,  // 21522: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > nextWord(null) + stopBegin': false,  // 21523: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > nextWord(null) + stopFinish': false,  // 21524: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > nextWord(null) + done': false,  // 21525: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > nextSentence(null) + newWordFragment': false,  // 21546: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > nextSentence(null) + progress': false,  // 21547: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > nextSentence(null) + stopBegin': false,  // 21548: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > nextSentence(null) + stopFinish': false,  // 21549: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > nextSentence(null) + done': false,  // 21550: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > prevWord(null) + newWordFragment': false,  // 21571: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > prevWord(null) + progress': false,  // 21572: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > prevWord(null) + stopBegin': false,  // 21573: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > prevWord(null) + stopFinish': false,  // 21574: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > prevWord(null) + done': false,  // 21575: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > prevSentence(null) + newWordFragment': false,  // 21596: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > prevSentence(null) + progress': false,  // 21597: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > prevSentence(null) + stopBegin': false,  // 21598: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > prevSentence(null) + stopFinish': false,  // 21599: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + stopFinish > prevSentence(null) + done': false,  // 21600: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > play(null) + playBegin': false,  // 21676: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > play(null) + playFinish': false,  // 21677: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > play(null) + restartBegin': false,  // 21680: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > play(null) + restartFinish': false,  // 21681: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > toggle(null) + playBegin': false,  // 21701: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > toggle(null) + playFinish': false,  // 21702: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > toggle(null) + restartBegin': false,  // 21705: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > toggle(null) + restartFinish': false,  // 21706: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > rewind(null) + newWordFragment': false,  // 21846: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > rewind(null) + progress': false,  // 21847: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > fastForward(null) + newWordFragment': false,  // 21871: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > fastForward(null) + progress': false,  // 21872: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > once([0,0,-2]) + newWordFragment': false,  // 21896: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > once([0,0,-2]) + progress': false,  // 21897: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > once([0,0,-2]) + stopBegin': false,  // 21898: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > once([0,0,-2]) + stopFinish': false,  // 21899: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > once([0,0,-2]) + done': false,  // 21900: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > once([0,0,2]) + newWordFragment': false,  // 21921: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > once([0,0,2]) + progress': false,  // 21922: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > once([0,0,2]) + stopBegin': false,  // 21923: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > once([0,0,2]) + stopFinish': false,  // 21924: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > once([0,0,2]) + done': false,  // 21925: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > current(null) + newWordFragment': false,  // 21946: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > current(null) + progress': false,  // 21947: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > current(null) + stopBegin': false,  // 21948: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > current(null) + stopFinish': false,  // 21949: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > current(null) + done': false,  // 21950: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpWords(-3) + newWordFragment': false,  // 22121: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpWords(-3) + progress': false,  // 22122: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpWords(-3) + stopBegin': false,  // 22123: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpWords(-3) + stopFinish': false,  // 22124: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpWords(-3) + done': false,  // 22125: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpWords(-1) + newWordFragment': false,  // 22146: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpWords(-1) + progress': false,  // 22147: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpWords(-1) + stopBegin': false,  // 22148: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpWords(-1) + stopFinish': false,  // 22149: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpWords(-1) + done': false,  // 22150: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpWords(0) + newWordFragment': false,  // 22171: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpWords(0) + progress': false,  // 22172: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpWords(0) + stopBegin': false,  // 22173: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpWords(0) + stopFinish': false,  // 22174: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpWords(0) + done': false,  // 22175: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpWords(4) + newWordFragment': false,  // 22196: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpWords(4) + progress': false,  // 22197: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpWords(4) + stopBegin': false,  // 22198: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpWords(4) + stopFinish': false,  // 22199: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpWords(4) + done': false,  // 22200: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpSentences(-1) + newWordFragment': false,  // 22296: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpSentences(-1) + progress': false,  // 22297: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpSentences(-1) + stopBegin': false,  // 22298: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpSentences(-1) + stopFinish': false,  // 22299: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpSentences(-1) + done': false,  // 22300: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpSentences(0) + newWordFragment': false,  // 22321: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpSentences(0) + progress': false,  // 22322: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpSentences(0) + stopBegin': false,  // 22323: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpSentences(0) + stopFinish': false,  // 22324: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpSentences(0) + done': false,  // 22325: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpSentences(1) + newWordFragment': false,  // 22346: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpSentences(1) + progress': false,  // 22347: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpSentences(1) + stopBegin': false,  // 22348: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpSentences(1) + stopFinish': false,  // 22349: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpSentences(1) + done': false,  // 22350: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpSentences(3) + newWordFragment': false,  // 22371: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpSentences(3) + progress': false,  // 22372: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpSentences(3) + stopBegin': false,  // 22373: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpSentences(3) + stopFinish': false,  // 22374: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > jumpSentences(3) + done': false,  // 22375: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > nextWord(null) + newWordFragment': false,  // 22421: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > nextWord(null) + progress': false,  // 22422: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > nextWord(null) + stopBegin': false,  // 22423: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > nextWord(null) + stopFinish': false,  // 22424: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > nextWord(null) + done': false,  // 22425: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > nextSentence(null) + newWordFragment': false,  // 22446: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > nextSentence(null) + progress': false,  // 22447: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > nextSentence(null) + stopBegin': false,  // 22448: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > nextSentence(null) + stopFinish': false,  // 22449: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > nextSentence(null) + done': false,  // 22450: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > prevWord(null) + newWordFragment': false,  // 22471: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > prevWord(null) + progress': false,  // 22472: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > prevWord(null) + stopBegin': false,  // 22473: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > prevWord(null) + stopFinish': false,  // 22474: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > prevWord(null) + done': false,  // 22475: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > prevSentence(null) + newWordFragment': false,  // 22496: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > prevSentence(null) + progress': false,  // 22497: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > prevSentence(null) + stopBegin': false,  // 22498: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > prevSentence(null) + stopFinish': false,  // 22499: ?? (skip results are different than combos)
		'skip-combos: jumpTo(11) + done > prevSentence(null) + done': false,  // 22500: ?? (skip results are different than combos)



		// ============== jumpTo(100) ==============
		// #? like jumpTo(11)
		// #6 immediately leading to stop*|done > some more types of relative loopers(+|0|-) get different output than usual
		// #7 immediately leading to stop*|done > relative single(not rewind) loopers(-) don't trigger stop*|done
		// #8 immediately leading to stop*|done > relative loopers(+|0) do unexpectedly trigger stop*|done
		// #9 immediately leading to stop*|done > play|toggle = triggers `restart` instead of `play`
		'skip-combos: jumpTo(100) + onceBegin > play(null) + playBegin': false,  // 9076: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > play(null) + playFinish': false,  // 9077: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > play(null) + restartBegin': false,  // 9080: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > play(null) + restartFinish': false,  // 9081: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > toggle(null) + playBegin': false,  // 9101: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > toggle(null) + playFinish': false,  // 9102: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > toggle(null) + restartBegin': false,  // 9105: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > toggle(null) + restartFinish': false,  // 9106: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > rewind(null) + newWordFragment': false,  // 9246: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > rewind(null) + progress': false,  // 9247: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > fastForward(null) + newWordFragment': false,  // 9271: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > fastForward(null) + progress': false,  // 9272: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > once([0,0,-2]) + newWordFragment': false,  // 9296: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > once([0,0,-2]) + progress': false,  // 9297: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > once([0,0,-2]) + stopBegin': false,  // 9298: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > once([0,0,-2]) + stopFinish': false,  // 9299: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > once([0,0,-2]) + done': false,  // 9300: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > once([0,0,2]) + newWordFragment': false,  // 9321: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > once([0,0,2]) + progress': false,  // 9322: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > once([0,0,2]) + stopBegin': false,  // 9323: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > once([0,0,2]) + stopFinish': false,  // 9324: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > once([0,0,2]) + done': false,  // 9325: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > current(null) + newWordFragment': false,  // 9346: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > current(null) + progress': false,  // 9347: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > current(null) + stopBegin': false,  // 9348: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > current(null) + stopFinish': false,  // 9349: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > current(null) + done': false,  // 9350: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpWords(-3) + newWordFragment': false,  // 9521: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpWords(-3) + progress': false,  // 9522: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpWords(-3) + stopBegin': false,  // 9523: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpWords(-3) + stopFinish': false,  // 9524: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpWords(-3) + done': false,  // 9525: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpWords(-1) + newWordFragment': false,  // 9546: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpWords(-1) + progress': false,  // 9547: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpWords(-1) + stopBegin': false,  // 9548: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpWords(-1) + stopFinish': false,  // 9549: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpWords(-1) + done': false,  // 9550: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpWords(0) + newWordFragment': false,  // 9571: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpWords(0) + progress': false,  // 9572: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpWords(0) + stopBegin': false,  // 9573: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpWords(0) + stopFinish': false,  // 9574: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpWords(0) + done': false,  // 9575: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpWords(4) + newWordFragment': false,  // 9596: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpWords(4) + progress': false,  // 9597: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpWords(4) + stopBegin': false,  // 9598: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpWords(4) + stopFinish': false,  // 9599: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpWords(4) + done': false,  // 9600: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpSentences(-1) + newWordFragment': false,  // 9696: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpSentences(-1) + progress': false,  // 9697: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpSentences(-1) + stopBegin': false,  // 9698: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpSentences(-1) + stopFinish': false,  // 9699: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpSentences(-1) + done': false,  // 9700: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpSentences(0) + newWordFragment': false,  // 9721: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpSentences(0) + progress': false,  // 9722: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpSentences(0) + stopBegin': false,  // 9723: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpSentences(0) + stopFinish': false,  // 9724: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpSentences(0) + done': false,  // 9725: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpSentences(1) + newWordFragment': false,  // 9746: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpSentences(1) + progress': false,  // 9747: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpSentences(1) + stopBegin': false,  // 9748: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpSentences(1) + stopFinish': false,  // 9749: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpSentences(1) + done': false,  // 9750: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpSentences(3) + newWordFragment': false,  // 9771: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpSentences(3) + progress': false,  // 9772: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpSentences(3) + stopBegin': false,  // 9773: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpSentences(3) + stopFinish': false,  // 9774: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > jumpSentences(3) + done': false,  // 9775: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > nextWord(null) + newWordFragment': false,  // 9821: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > nextWord(null) + progress': false,  // 9822: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > nextWord(null) + stopBegin': false,  // 9823: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > nextWord(null) + stopFinish': false,  // 9824: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > nextWord(null) + done': false,  // 9825: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > nextSentence(null) + newWordFragment': false,  // 9846: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > nextSentence(null) + progress': false,  // 9847: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > nextSentence(null) + stopBegin': false,  // 9848: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > nextSentence(null) + stopFinish': false,  // 9849: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > nextSentence(null) + done': false,  // 9850: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > prevWord(null) + newWordFragment': false,  // 9871: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > prevWord(null) + progress': false,  // 9872: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > prevWord(null) + stopBegin': false,  // 9873: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > prevWord(null) + stopFinish': false,  // 9874: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > prevWord(null) + done': false,  // 9875: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > prevSentence(null) + newWordFragment': false,  // 9896: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > prevSentence(null) + progress': false,  // 9897: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > prevSentence(null) + stopBegin': false,  // 9898: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > prevSentence(null) + stopFinish': false,  // 9899: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceBegin > prevSentence(null) + done': false,  // 9900: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > play(null) + playBegin': false,  // 9976: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > play(null) + playFinish': false,  // 9977: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > play(null) + restartBegin': false,  // 9980: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > play(null) + restartFinish': false,  // 9981: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > toggle(null) + playBegin': false,  // 10001: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > toggle(null) + playFinish': false,  // 10002: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > toggle(null) + restartBegin': false,  // 10005: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > toggle(null) + restartFinish': false,  // 10006: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > rewind(null) + newWordFragment': false,  // 10146: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > rewind(null) + progress': false,  // 10147: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > fastForward(null) + newWordFragment': false,  // 10171: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > fastForward(null) + progress': false,  // 10172: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > once([0,0,-2]) + newWordFragment': false,  // 10196: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > once([0,0,-2]) + progress': false,  // 10197: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > once([0,0,-2]) + stopBegin': false,  // 10198: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > once([0,0,-2]) + stopFinish': false,  // 10199: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > once([0,0,-2]) + done': false,  // 10200: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > once([0,0,2]) + newWordFragment': false,  // 10221: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > once([0,0,2]) + progress': false,  // 10222: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > once([0,0,2]) + stopBegin': false,  // 10223: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > once([0,0,2]) + stopFinish': false,  // 10224: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > once([0,0,2]) + done': false,  // 10225: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > current(null) + newWordFragment': false,  // 10246: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > current(null) + progress': false,  // 10247: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > current(null) + stopBegin': false,  // 10248: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > current(null) + stopFinish': false,  // 10249: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > current(null) + done': false,  // 10250: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpWords(-3) + newWordFragment': false,  // 10421: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpWords(-3) + progress': false,  // 10422: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpWords(-3) + stopBegin': false,  // 10423: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpWords(-3) + stopFinish': false,  // 10424: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpWords(-3) + done': false,  // 10425: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpWords(-1) + newWordFragment': false,  // 10446: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpWords(-1) + progress': false,  // 10447: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpWords(-1) + stopBegin': false,  // 10448: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpWords(-1) + stopFinish': false,  // 10449: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpWords(-1) + done': false,  // 10450: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpWords(0) + newWordFragment': false,  // 10471: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpWords(0) + progress': false,  // 10472: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpWords(0) + stopBegin': false,  // 10473: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpWords(0) + stopFinish': false,  // 10474: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpWords(0) + done': false,  // 10475: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpWords(4) + newWordFragment': false,  // 10496: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpWords(4) + progress': false,  // 10497: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpWords(4) + stopBegin': false,  // 10498: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpWords(4) + stopFinish': false,  // 10499: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpWords(4) + done': false,  // 10500: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpSentences(-1) + newWordFragment': false,  // 10596: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpSentences(-1) + progress': false,  // 10597: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpSentences(-1) + stopBegin': false,  // 10598: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpSentences(-1) + stopFinish': false,  // 10599: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpSentences(-1) + done': false,  // 10600: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpSentences(0) + newWordFragment': false,  // 10621: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpSentences(0) + progress': false,  // 10622: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpSentences(0) + stopBegin': false,  // 10623: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpSentences(0) + stopFinish': false,  // 10624: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpSentences(0) + done': false,  // 10625: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpSentences(1) + newWordFragment': false,  // 10646: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpSentences(1) + progress': false,  // 10647: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpSentences(1) + stopBegin': false,  // 10648: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpSentences(1) + stopFinish': false,  // 10649: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpSentences(1) + done': false,  // 10650: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpSentences(3) + newWordFragment': false,  // 10671: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpSentences(3) + progress': false,  // 10672: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpSentences(3) + stopBegin': false,  // 10673: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpSentences(3) + stopFinish': false,  // 10674: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > jumpSentences(3) + done': false,  // 10675: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > nextWord(null) + newWordFragment': false,  // 10721: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > nextWord(null) + progress': false,  // 10722: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > nextWord(null) + stopBegin': false,  // 10723: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > nextWord(null) + stopFinish': false,  // 10724: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > nextWord(null) + done': false,  // 10725: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > nextSentence(null) + newWordFragment': false,  // 10746: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > nextSentence(null) + progress': false,  // 10747: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > nextSentence(null) + stopBegin': false,  // 10748: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > nextSentence(null) + stopFinish': false,  // 10749: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > nextSentence(null) + done': false,  // 10750: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > prevWord(null) + newWordFragment': false,  // 10771: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > prevWord(null) + progress': false,  // 10772: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > prevWord(null) + stopBegin': false,  // 10773: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > prevWord(null) + stopFinish': false,  // 10774: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > prevWord(null) + done': false,  // 10775: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > prevSentence(null) + newWordFragment': false,  // 10796: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > prevSentence(null) + progress': false,  // 10797: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > prevSentence(null) + stopBegin': false,  // 10798: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > prevSentence(null) + stopFinish': false,  // 10799: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + onceFinish > prevSentence(null) + done': false,  // 10800: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > play(null) + playBegin': false,  // 16276: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > play(null) + playFinish': false,  // 16277: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > play(null) + restartBegin': false,  // 16280: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > play(null) + restartFinish': false,  // 16281: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > toggle(null) + playBegin': false,  // 16301: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > toggle(null) + playFinish': false,  // 16302: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > toggle(null) + restartBegin': false,  // 16305: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > toggle(null) + restartFinish': false,  // 16306: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > rewind(null) + newWordFragment': false,  // 16446: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > rewind(null) + progress': false,  // 16447: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > fastForward(null) + newWordFragment': false,  // 16471: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > fastForward(null) + progress': false,  // 16472: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > once([0,0,-2]) + newWordFragment': false,  // 16496: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > once([0,0,-2]) + progress': false,  // 16497: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > once([0,0,-2]) + stopBegin': false,  // 16498: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > once([0,0,-2]) + stopFinish': false,  // 16499: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > once([0,0,-2]) + done': false,  // 16500: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > once([0,0,2]) + newWordFragment': false,  // 16521: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > once([0,0,2]) + progress': false,  // 16522: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > once([0,0,2]) + stopBegin': false,  // 16523: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > once([0,0,2]) + stopFinish': false,  // 16524: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > once([0,0,2]) + done': false,  // 16525: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > current(null) + newWordFragment': false,  // 16546: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > current(null) + progress': false,  // 16547: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > current(null) + stopBegin': false,  // 16548: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > current(null) + stopFinish': false,  // 16549: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > current(null) + done': false,  // 16550: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpWords(-3) + newWordFragment': false,  // 16721: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpWords(-3) + progress': false,  // 16722: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpWords(-3) + stopBegin': false,  // 16723: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpWords(-3) + stopFinish': false,  // 16724: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpWords(-3) + done': false,  // 16725: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpWords(-1) + newWordFragment': false,  // 16746: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpWords(-1) + progress': false,  // 16747: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpWords(-1) + stopBegin': false,  // 16748: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpWords(-1) + stopFinish': false,  // 16749: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpWords(-1) + done': false,  // 16750: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpWords(0) + newWordFragment': false,  // 16771: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpWords(0) + progress': false,  // 16772: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpWords(0) + stopBegin': false,  // 16773: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpWords(0) + stopFinish': false,  // 16774: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpWords(0) + done': false,  // 16775: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpWords(4) + newWordFragment': false,  // 16796: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpWords(4) + progress': false,  // 16797: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpWords(4) + stopBegin': false,  // 16798: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpWords(4) + stopFinish': false,  // 16799: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpWords(4) + done': false,  // 16800: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpSentences(-1) + newWordFragment': false,  // 16896: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpSentences(-1) + progress': false,  // 16897: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpSentences(-1) + stopBegin': false,  // 16898: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpSentences(-1) + stopFinish': false,  // 16899: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpSentences(-1) + done': false,  // 16900: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpSentences(0) + newWordFragment': false,  // 16921: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpSentences(0) + progress': false,  // 16922: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpSentences(0) + stopBegin': false,  // 16923: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpSentences(0) + stopFinish': false,  // 16924: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpSentences(0) + done': false,  // 16925: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpSentences(1) + newWordFragment': false,  // 16946: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpSentences(1) + progress': false,  // 16947: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpSentences(1) + stopBegin': false,  // 16948: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpSentences(1) + stopFinish': false,  // 16949: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpSentences(1) + done': false,  // 16950: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpSentences(3) + newWordFragment': false,  // 16971: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpSentences(3) + progress': false,  // 16972: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpSentences(3) + stopBegin': false,  // 16973: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpSentences(3) + stopFinish': false,  // 16974: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > jumpSentences(3) + done': false,  // 16975: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > nextWord(null) + newWordFragment': false,  // 17021: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > nextWord(null) + progress': false,  // 17022: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > nextWord(null) + stopBegin': false,  // 17023: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > nextWord(null) + stopFinish': false,  // 17024: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > nextWord(null) + done': false,  // 17025: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > nextSentence(null) + newWordFragment': false,  // 17046: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > nextSentence(null) + progress': false,  // 17047: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > nextSentence(null) + stopBegin': false,  // 17048: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > nextSentence(null) + stopFinish': false,  // 17049: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > nextSentence(null) + done': false,  // 17050: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > prevWord(null) + newWordFragment': false,  // 17071: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > prevWord(null) + progress': false,  // 17072: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > prevWord(null) + stopBegin': false,  // 17073: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > prevWord(null) + stopFinish': false,  // 17074: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > prevWord(null) + done': false,  // 17075: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > prevSentence(null) + newWordFragment': false,  // 17096: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > prevSentence(null) + progress': false,  // 17097: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > prevSentence(null) + stopBegin': false,  // 17098: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > prevSentence(null) + stopFinish': false,  // 17099: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopBegin > prevSentence(null) + done': false,  // 17100: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > play(null) + playBegin': false,  // 17176: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > play(null) + playFinish': false,  // 17177: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > play(null) + restartBegin': false,  // 17180: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > play(null) + restartFinish': false,  // 17181: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > toggle(null) + playBegin': false,  // 17201: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > toggle(null) + playFinish': false,  // 17202: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > toggle(null) + restartBegin': false,  // 17205: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > toggle(null) + restartFinish': false,  // 17206: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > rewind(null) + newWordFragment': false,  // 17346: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > rewind(null) + progress': false,  // 17347: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > fastForward(null) + newWordFragment': false,  // 17371: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > fastForward(null) + progress': false,  // 17372: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > once([0,0,-2]) + newWordFragment': false,  // 17396: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > once([0,0,-2]) + progress': false,  // 17397: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > once([0,0,-2]) + stopBegin': false,  // 17398: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > once([0,0,-2]) + stopFinish': false,  // 17399: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > once([0,0,-2]) + done': false,  // 17400: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > once([0,0,2]) + newWordFragment': false,  // 17421: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > once([0,0,2]) + progress': false,  // 17422: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > once([0,0,2]) + stopBegin': false,  // 17423: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > once([0,0,2]) + stopFinish': false,  // 17424: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > once([0,0,2]) + done': false,  // 17425: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > current(null) + newWordFragment': false,  // 17446: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > current(null) + progress': false,  // 17447: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > current(null) + stopBegin': false,  // 17448: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > current(null) + stopFinish': false,  // 17449: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > current(null) + done': false,  // 17450: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpWords(-3) + newWordFragment': false,  // 17621: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpWords(-3) + progress': false,  // 17622: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpWords(-3) + stopBegin': false,  // 17623: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpWords(-3) + stopFinish': false,  // 17624: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpWords(-3) + done': false,  // 17625: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpWords(-1) + newWordFragment': false,  // 17646: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpWords(-1) + progress': false,  // 17647: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpWords(-1) + stopBegin': false,  // 17648: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpWords(-1) + stopFinish': false,  // 17649: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpWords(-1) + done': false,  // 17650: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpWords(0) + newWordFragment': false,  // 17671: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpWords(0) + progress': false,  // 17672: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpWords(0) + stopBegin': false,  // 17673: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpWords(0) + stopFinish': false,  // 17674: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpWords(0) + done': false,  // 17675: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpWords(4) + newWordFragment': false,  // 17696: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpWords(4) + progress': false,  // 17697: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpWords(4) + stopBegin': false,  // 17698: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpWords(4) + stopFinish': false,  // 17699: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpWords(4) + done': false,  // 17700: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpSentences(-1) + newWordFragment': false,  // 17796: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpSentences(-1) + progress': false,  // 17797: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpSentences(-1) + stopBegin': false,  // 17798: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpSentences(-1) + stopFinish': false,  // 17799: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpSentences(-1) + done': false,  // 17800: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpSentences(0) + newWordFragment': false,  // 17821: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpSentences(0) + progress': false,  // 17822: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpSentences(0) + stopBegin': false,  // 17823: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpSentences(0) + stopFinish': false,  // 17824: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpSentences(0) + done': false,  // 17825: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpSentences(1) + newWordFragment': false,  // 17846: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpSentences(1) + progress': false,  // 17847: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpSentences(1) + stopBegin': false,  // 17848: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpSentences(1) + stopFinish': false,  // 17849: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpSentences(1) + done': false,  // 17850: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpSentences(3) + newWordFragment': false,  // 17871: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpSentences(3) + progress': false,  // 17872: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpSentences(3) + stopBegin': false,  // 17873: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpSentences(3) + stopFinish': false,  // 17874: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > jumpSentences(3) + done': false,  // 17875: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > nextWord(null) + newWordFragment': false,  // 17921: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > nextWord(null) + progress': false,  // 17922: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > nextWord(null) + stopBegin': false,  // 17923: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > nextWord(null) + stopFinish': false,  // 17924: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > nextWord(null) + done': false,  // 17925: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > nextSentence(null) + newWordFragment': false,  // 17946: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > nextSentence(null) + progress': false,  // 17947: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > nextSentence(null) + stopBegin': false,  // 17948: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > nextSentence(null) + stopFinish': false,  // 17949: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > nextSentence(null) + done': false,  // 17950: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > prevWord(null) + newWordFragment': false,  // 17971: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > prevWord(null) + progress': false,  // 17972: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > prevWord(null) + stopBegin': false,  // 17973: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > prevWord(null) + stopFinish': false,  // 17974: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > prevWord(null) + done': false,  // 17975: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > prevSentence(null) + newWordFragment': false,  // 17996: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > prevSentence(null) + progress': false,  // 17997: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > prevSentence(null) + stopBegin': false,  // 17998: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > prevSentence(null) + stopFinish': false,  // 17999: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + loopFinish > prevSentence(null) + done': false,  // 18000: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > play(null) + playBegin': false,  // 18076: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > play(null) + playFinish': false,  // 18077: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > play(null) + restartBegin': false,  // 18080: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > play(null) + restartFinish': false,  // 18081: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > toggle(null) + playBegin': false,  // 18101: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > toggle(null) + playFinish': false,  // 18102: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > toggle(null) + restartBegin': false,  // 18105: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > toggle(null) + restartFinish': false,  // 18106: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > rewind(null) + newWordFragment': false,  // 18246: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > rewind(null) + progress': false,  // 18247: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > fastForward(null) + newWordFragment': false,  // 18271: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > fastForward(null) + progress': false,  // 18272: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > once([0,0,-2]) + newWordFragment': false,  // 18296: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > once([0,0,-2]) + progress': false,  // 18297: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > once([0,0,-2]) + stopBegin': false,  // 18298: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > once([0,0,-2]) + stopFinish': false,  // 18299: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > once([0,0,-2]) + done': false,  // 18300: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > once([0,0,2]) + newWordFragment': false,  // 18321: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > once([0,0,2]) + progress': false,  // 18322: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > once([0,0,2]) + stopBegin': false,  // 18323: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > once([0,0,2]) + stopFinish': false,  // 18324: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > once([0,0,2]) + done': false,  // 18325: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > current(null) + newWordFragment': false,  // 18346: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > current(null) + progress': false,  // 18347: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > current(null) + stopBegin': false,  // 18348: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > current(null) + stopFinish': false,  // 18349: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > current(null) + done': false,  // 18350: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpWords(-3) + newWordFragment': false,  // 18521: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpWords(-3) + progress': false,  // 18522: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpWords(-3) + stopBegin': false,  // 18523: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpWords(-3) + stopFinish': false,  // 18524: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpWords(-3) + done': false,  // 18525: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpWords(-1) + newWordFragment': false,  // 18546: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpWords(-1) + progress': false,  // 18547: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpWords(-1) + stopBegin': false,  // 18548: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpWords(-1) + stopFinish': false,  // 18549: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpWords(-1) + done': false,  // 18550: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpWords(0) + newWordFragment': false,  // 18571: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpWords(0) + progress': false,  // 18572: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpWords(0) + stopBegin': false,  // 18573: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpWords(0) + stopFinish': false,  // 18574: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpWords(0) + done': false,  // 18575: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpWords(4) + newWordFragment': false,  // 18596: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpWords(4) + progress': false,  // 18597: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpWords(4) + stopBegin': false,  // 18598: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpWords(4) + stopFinish': false,  // 18599: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpWords(4) + done': false,  // 18600: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpSentences(-1) + newWordFragment': false,  // 18696: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpSentences(-1) + progress': false,  // 18697: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpSentences(-1) + stopBegin': false,  // 18698: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpSentences(-1) + stopFinish': false,  // 18699: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpSentences(-1) + done': false,  // 18700: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpSentences(0) + newWordFragment': false,  // 18721: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpSentences(0) + progress': false,  // 18722: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpSentences(0) + stopBegin': false,  // 18723: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpSentences(0) + stopFinish': false,  // 18724: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpSentences(0) + done': false,  // 18725: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpSentences(1) + newWordFragment': false,  // 18746: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpSentences(1) + progress': false,  // 18747: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpSentences(1) + stopBegin': false,  // 18748: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpSentences(1) + stopFinish': false,  // 18749: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpSentences(1) + done': false,  // 18750: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpSentences(3) + newWordFragment': false,  // 18771: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpSentences(3) + progress': false,  // 18772: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpSentences(3) + stopBegin': false,  // 18773: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpSentences(3) + stopFinish': false,  // 18774: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > jumpSentences(3) + done': false,  // 18775: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > nextWord(null) + newWordFragment': false,  // 18821: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > nextWord(null) + progress': false,  // 18822: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > nextWord(null) + stopBegin': false,  // 18823: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > nextWord(null) + stopFinish': false,  // 18824: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > nextWord(null) + done': false,  // 18825: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > nextSentence(null) + newWordFragment': false,  // 18846: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > nextSentence(null) + progress': false,  // 18847: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > nextSentence(null) + stopBegin': false,  // 18848: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > nextSentence(null) + stopFinish': false,  // 18849: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > nextSentence(null) + done': false,  // 18850: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > prevWord(null) + newWordFragment': false,  // 18871: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > prevWord(null) + progress': false,  // 18872: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > prevWord(null) + stopBegin': false,  // 18873: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > prevWord(null) + stopFinish': false,  // 18874: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > prevWord(null) + done': false,  // 18875: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > prevSentence(null) + newWordFragment': false,  // 18896: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > prevSentence(null) + progress': false,  // 18897: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > prevSentence(null) + stopBegin': false,  // 18898: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > prevSentence(null) + stopFinish': false,  // 18899: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + newWordFragment > prevSentence(null) + done': false,  // 18900: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > play(null) + playBegin': false,  // 18976: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > play(null) + playFinish': false,  // 18977: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > play(null) + restartBegin': false,  // 18980: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > play(null) + restartFinish': false,  // 18981: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > toggle(null) + playBegin': false,  // 19001: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > toggle(null) + playFinish': false,  // 19002: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > toggle(null) + restartBegin': false,  // 19005: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > toggle(null) + restartFinish': false,  // 19006: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > rewind(null) + newWordFragment': false,  // 19146: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > rewind(null) + progress': false,  // 19147: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > fastForward(null) + newWordFragment': false,  // 19171: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > fastForward(null) + progress': false,  // 19172: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > once([0,0,-2]) + newWordFragment': false,  // 19196: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > once([0,0,-2]) + progress': false,  // 19197: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > once([0,0,-2]) + stopBegin': false,  // 19198: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > once([0,0,-2]) + stopFinish': false,  // 19199: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > once([0,0,-2]) + done': false,  // 19200: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > once([0,0,2]) + newWordFragment': false,  // 19221: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > once([0,0,2]) + progress': false,  // 19222: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > once([0,0,2]) + stopBegin': false,  // 19223: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > once([0,0,2]) + stopFinish': false,  // 19224: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > once([0,0,2]) + done': false,  // 19225: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > current(null) + newWordFragment': false,  // 19246: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > current(null) + progress': false,  // 19247: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > current(null) + stopBegin': false,  // 19248: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > current(null) + stopFinish': false,  // 19249: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > current(null) + done': false,  // 19250: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpWords(-3) + newWordFragment': false,  // 19421: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpWords(-3) + progress': false,  // 19422: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpWords(-3) + stopBegin': false,  // 19423: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpWords(-3) + stopFinish': false,  // 19424: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpWords(-3) + done': false,  // 19425: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpWords(-1) + newWordFragment': false,  // 19446: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpWords(-1) + progress': false,  // 19447: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpWords(-1) + stopBegin': false,  // 19448: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpWords(-1) + stopFinish': false,  // 19449: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpWords(-1) + done': false,  // 19450: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpWords(0) + newWordFragment': false,  // 19471: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpWords(0) + progress': false,  // 19472: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpWords(0) + stopBegin': false,  // 19473: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpWords(0) + stopFinish': false,  // 19474: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpWords(0) + done': false,  // 19475: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpWords(4) + newWordFragment': false,  // 19496: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpWords(4) + progress': false,  // 19497: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpWords(4) + stopBegin': false,  // 19498: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpWords(4) + stopFinish': false,  // 19499: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpWords(4) + done': false,  // 19500: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpSentences(-1) + newWordFragment': false,  // 19596: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpSentences(-1) + progress': false,  // 19597: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpSentences(-1) + stopBegin': false,  // 19598: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpSentences(-1) + stopFinish': false,  // 19599: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpSentences(-1) + done': false,  // 19600: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpSentences(0) + newWordFragment': false,  // 19621: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpSentences(0) + progress': false,  // 19622: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpSentences(0) + stopBegin': false,  // 19623: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpSentences(0) + stopFinish': false,  // 19624: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpSentences(0) + done': false,  // 19625: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpSentences(1) + newWordFragment': false,  // 19646: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpSentences(1) + progress': false,  // 19647: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpSentences(1) + stopBegin': false,  // 19648: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpSentences(1) + stopFinish': false,  // 19649: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpSentences(1) + done': false,  // 19650: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpSentences(3) + newWordFragment': false,  // 19671: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpSentences(3) + progress': false,  // 19672: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpSentences(3) + stopBegin': false,  // 19673: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpSentences(3) + stopFinish': false,  // 19674: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > jumpSentences(3) + done': false,  // 19675: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > nextWord(null) + newWordFragment': false,  // 19721: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > nextWord(null) + progress': false,  // 19722: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > nextWord(null) + stopBegin': false,  // 19723: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > nextWord(null) + stopFinish': false,  // 19724: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > nextWord(null) + done': false,  // 19725: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > nextSentence(null) + newWordFragment': false,  // 19746: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > nextSentence(null) + progress': false,  // 19747: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > nextSentence(null) + stopBegin': false,  // 19748: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > nextSentence(null) + stopFinish': false,  // 19749: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > nextSentence(null) + done': false,  // 19750: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > prevWord(null) + newWordFragment': false,  // 19771: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > prevWord(null) + progress': false,  // 19772: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > prevWord(null) + stopBegin': false,  // 19773: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > prevWord(null) + stopFinish': false,  // 19774: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > prevWord(null) + done': false,  // 19775: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > prevSentence(null) + newWordFragment': false,  // 19796: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > prevSentence(null) + progress': false,  // 19797: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > prevSentence(null) + stopBegin': false,  // 19798: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > prevSentence(null) + stopFinish': false,  // 19799: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + progress > prevSentence(null) + done': false,  // 19800: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > play(null) + playBegin': false,  // 19876: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > play(null) + playFinish': false,  // 19877: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > play(null) + restartBegin': false,  // 19880: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > play(null) + restartFinish': false,  // 19881: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > toggle(null) + playBegin': false,  // 19901: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > toggle(null) + playFinish': false,  // 19902: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > toggle(null) + restartBegin': false,  // 19905: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > toggle(null) + restartFinish': false,  // 19906: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > rewind(null) + newWordFragment': false,  // 20046: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > rewind(null) + progress': false,  // 20047: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > fastForward(null) + newWordFragment': false,  // 20071: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > fastForward(null) + progress': false,  // 20072: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > once([0,0,-2]) + newWordFragment': false,  // 20096: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > once([0,0,-2]) + progress': false,  // 20097: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > once([0,0,-2]) + stopBegin': false,  // 20098: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > once([0,0,-2]) + stopFinish': false,  // 20099: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > once([0,0,-2]) + done': false,  // 20100: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > once([0,0,2]) + newWordFragment': false,  // 20121: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > once([0,0,2]) + progress': false,  // 20122: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > once([0,0,2]) + stopBegin': false,  // 20123: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > once([0,0,2]) + stopFinish': false,  // 20124: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > once([0,0,2]) + done': false,  // 20125: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > current(null) + newWordFragment': false,  // 20146: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > current(null) + progress': false,  // 20147: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > current(null) + stopBegin': false,  // 20148: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > current(null) + stopFinish': false,  // 20149: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > current(null) + done': false,  // 20150: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpWords(-3) + newWordFragment': false,  // 20321: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpWords(-3) + progress': false,  // 20322: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpWords(-3) + stopBegin': false,  // 20323: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpWords(-3) + stopFinish': false,  // 20324: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpWords(-3) + done': false,  // 20325: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpWords(-1) + newWordFragment': false,  // 20346: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpWords(-1) + progress': false,  // 20347: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpWords(-1) + stopBegin': false,  // 20348: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpWords(-1) + stopFinish': false,  // 20349: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpWords(-1) + done': false,  // 20350: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpWords(0) + newWordFragment': false,  // 20371: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpWords(0) + progress': false,  // 20372: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpWords(0) + stopBegin': false,  // 20373: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpWords(0) + stopFinish': false,  // 20374: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpWords(0) + done': false,  // 20375: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpWords(4) + newWordFragment': false,  // 20396: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpWords(4) + progress': false,  // 20397: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpWords(4) + stopBegin': false,  // 20398: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpWords(4) + stopFinish': false,  // 20399: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpWords(4) + done': false,  // 20400: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpSentences(-1) + newWordFragment': false,  // 20496: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpSentences(-1) + progress': false,  // 20497: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpSentences(-1) + stopBegin': false,  // 20498: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpSentences(-1) + stopFinish': false,  // 20499: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpSentences(-1) + done': false,  // 20500: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpSentences(0) + newWordFragment': false,  // 20521: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpSentences(0) + progress': false,  // 20522: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpSentences(0) + stopBegin': false,  // 20523: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpSentences(0) + stopFinish': false,  // 20524: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpSentences(0) + done': false,  // 20525: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpSentences(1) + newWordFragment': false,  // 20546: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpSentences(1) + progress': false,  // 20547: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpSentences(1) + stopBegin': false,  // 20548: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpSentences(1) + stopFinish': false,  // 20549: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpSentences(1) + done': false,  // 20550: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpSentences(3) + newWordFragment': false,  // 20571: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpSentences(3) + progress': false,  // 20572: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpSentences(3) + stopBegin': false,  // 20573: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpSentences(3) + stopFinish': false,  // 20574: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > jumpSentences(3) + done': false,  // 20575: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > nextWord(null) + newWordFragment': false,  // 20621: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > nextWord(null) + progress': false,  // 20622: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > nextWord(null) + stopBegin': false,  // 20623: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > nextWord(null) + stopFinish': false,  // 20624: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > nextWord(null) + done': false,  // 20625: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > nextSentence(null) + newWordFragment': false,  // 20646: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > nextSentence(null) + progress': false,  // 20647: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > nextSentence(null) + stopBegin': false,  // 20648: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > nextSentence(null) + stopFinish': false,  // 20649: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > nextSentence(null) + done': false,  // 20650: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > prevWord(null) + newWordFragment': false,  // 20671: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > prevWord(null) + progress': false,  // 20672: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > prevWord(null) + stopBegin': false,  // 20673: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > prevWord(null) + stopFinish': false,  // 20674: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > prevWord(null) + done': false,  // 20675: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > prevSentence(null) + newWordFragment': false,  // 20696: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > prevSentence(null) + progress': false,  // 20697: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > prevSentence(null) + stopBegin': false,  // 20698: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > prevSentence(null) + stopFinish': false,  // 20699: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopBegin > prevSentence(null) + done': false,  // 20700: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > play(null) + playBegin': false,  // 20776: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > play(null) + playFinish': false,  // 20777: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > play(null) + restartBegin': false,  // 20780: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > play(null) + restartFinish': false,  // 20781: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > toggle(null) + playBegin': false,  // 20801: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > toggle(null) + playFinish': false,  // 20802: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > toggle(null) + restartBegin': false,  // 20805: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > toggle(null) + restartFinish': false,  // 20806: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > rewind(null) + newWordFragment': false,  // 20946: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > rewind(null) + progress': false,  // 20947: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > fastForward(null) + newWordFragment': false,  // 20971: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > fastForward(null) + progress': false,  // 20972: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > once([0,0,-2]) + newWordFragment': false,  // 20996: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > once([0,0,-2]) + progress': false,  // 20997: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > once([0,0,-2]) + stopBegin': false,  // 20998: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > once([0,0,-2]) + stopFinish': false,  // 20999: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > once([0,0,-2]) + done': false,  // 21000: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > once([0,0,2]) + newWordFragment': false,  // 21021: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > once([0,0,2]) + progress': false,  // 21022: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > once([0,0,2]) + stopBegin': false,  // 21023: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > once([0,0,2]) + stopFinish': false,  // 21024: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > once([0,0,2]) + done': false,  // 21025: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > current(null) + newWordFragment': false,  // 21046: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > current(null) + progress': false,  // 21047: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > current(null) + stopBegin': false,  // 21048: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > current(null) + stopFinish': false,  // 21049: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > current(null) + done': false,  // 21050: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpWords(-3) + newWordFragment': false,  // 21221: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpWords(-3) + progress': false,  // 21222: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpWords(-3) + stopBegin': false,  // 21223: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpWords(-3) + stopFinish': false,  // 21224: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpWords(-3) + done': false,  // 21225: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpWords(-1) + newWordFragment': false,  // 21246: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpWords(-1) + progress': false,  // 21247: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpWords(-1) + stopBegin': false,  // 21248: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpWords(-1) + stopFinish': false,  // 21249: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpWords(-1) + done': false,  // 21250: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpWords(0) + newWordFragment': false,  // 21271: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpWords(0) + progress': false,  // 21272: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpWords(0) + stopBegin': false,  // 21273: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpWords(0) + stopFinish': false,  // 21274: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpWords(0) + done': false,  // 21275: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpWords(4) + newWordFragment': false,  // 21296: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpWords(4) + progress': false,  // 21297: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpWords(4) + stopBegin': false,  // 21298: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpWords(4) + stopFinish': false,  // 21299: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpWords(4) + done': false,  // 21300: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpSentences(-1) + newWordFragment': false,  // 21396: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpSentences(-1) + progress': false,  // 21397: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpSentences(-1) + stopBegin': false,  // 21398: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpSentences(-1) + stopFinish': false,  // 21399: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpSentences(-1) + done': false,  // 21400: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpSentences(0) + newWordFragment': false,  // 21421: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpSentences(0) + progress': false,  // 21422: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpSentences(0) + stopBegin': false,  // 21423: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpSentences(0) + stopFinish': false,  // 21424: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpSentences(0) + done': false,  // 21425: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpSentences(1) + newWordFragment': false,  // 21446: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpSentences(1) + progress': false,  // 21447: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpSentences(1) + stopBegin': false,  // 21448: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpSentences(1) + stopFinish': false,  // 21449: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpSentences(1) + done': false,  // 21450: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpSentences(3) + newWordFragment': false,  // 21471: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpSentences(3) + progress': false,  // 21472: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpSentences(3) + stopBegin': false,  // 21473: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpSentences(3) + stopFinish': false,  // 21474: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > jumpSentences(3) + done': false,  // 21475: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > nextWord(null) + newWordFragment': false,  // 21521: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > nextWord(null) + progress': false,  // 21522: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > nextWord(null) + stopBegin': false,  // 21523: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > nextWord(null) + stopFinish': false,  // 21524: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > nextWord(null) + done': false,  // 21525: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > nextSentence(null) + newWordFragment': false,  // 21546: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > nextSentence(null) + progress': false,  // 21547: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > nextSentence(null) + stopBegin': false,  // 21548: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > nextSentence(null) + stopFinish': false,  // 21549: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > nextSentence(null) + done': false,  // 21550: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > prevWord(null) + newWordFragment': false,  // 21571: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > prevWord(null) + progress': false,  // 21572: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > prevWord(null) + stopBegin': false,  // 21573: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > prevWord(null) + stopFinish': false,  // 21574: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > prevWord(null) + done': false,  // 21575: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > prevSentence(null) + newWordFragment': false,  // 21596: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > prevSentence(null) + progress': false,  // 21597: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > prevSentence(null) + stopBegin': false,  // 21598: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > prevSentence(null) + stopFinish': false,  // 21599: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + stopFinish > prevSentence(null) + done': false,  // 21600: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > play(null) + playBegin': false,  // 21676: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > play(null) + playFinish': false,  // 21677: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > play(null) + restartBegin': false,  // 21680: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > play(null) + restartFinish': false,  // 21681: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > toggle(null) + playBegin': false,  // 21701: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > toggle(null) + playFinish': false,  // 21702: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > toggle(null) + restartBegin': false,  // 21705: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > toggle(null) + restartFinish': false,  // 21706: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > rewind(null) + newWordFragment': false,  // 21846: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > rewind(null) + progress': false,  // 21847: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > fastForward(null) + newWordFragment': false,  // 21871: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > fastForward(null) + progress': false,  // 21872: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > once([0,0,-2]) + newWordFragment': false,  // 21896: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > once([0,0,-2]) + progress': false,  // 21897: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > once([0,0,-2]) + stopBegin': false,  // 21898: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > once([0,0,-2]) + stopFinish': false,  // 21899: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > once([0,0,-2]) + done': false,  // 21900: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > once([0,0,2]) + newWordFragment': false,  // 21921: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > once([0,0,2]) + progress': false,  // 21922: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > once([0,0,2]) + stopBegin': false,  // 21923: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > once([0,0,2]) + stopFinish': false,  // 21924: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > once([0,0,2]) + done': false,  // 21925: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > current(null) + newWordFragment': false,  // 21946: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > current(null) + progress': false,  // 21947: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > current(null) + stopBegin': false,  // 21948: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > current(null) + stopFinish': false,  // 21949: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > current(null) + done': false,  // 21950: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpWords(-3) + newWordFragment': false,  // 22121: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpWords(-3) + progress': false,  // 22122: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpWords(-3) + stopBegin': false,  // 22123: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpWords(-3) + stopFinish': false,  // 22124: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpWords(-3) + done': false,  // 22125: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpWords(-1) + newWordFragment': false,  // 22146: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpWords(-1) + progress': false,  // 22147: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpWords(-1) + stopBegin': false,  // 22148: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpWords(-1) + stopFinish': false,  // 22149: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpWords(-1) + done': false,  // 22150: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpWords(0) + newWordFragment': false,  // 22171: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpWords(0) + progress': false,  // 22172: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpWords(0) + stopBegin': false,  // 22173: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpWords(0) + stopFinish': false,  // 22174: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpWords(0) + done': false,  // 22175: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpWords(4) + newWordFragment': false,  // 22196: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpWords(4) + progress': false,  // 22197: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpWords(4) + stopBegin': false,  // 22198: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpWords(4) + stopFinish': false,  // 22199: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpWords(4) + done': false,  // 22200: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpSentences(-1) + newWordFragment': false,  // 22296: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpSentences(-1) + progress': false,  // 22297: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpSentences(-1) + stopBegin': false,  // 22298: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpSentences(-1) + stopFinish': false,  // 22299: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpSentences(-1) + done': false,  // 22300: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpSentences(0) + newWordFragment': false,  // 22321: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpSentences(0) + progress': false,  // 22322: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpSentences(0) + stopBegin': false,  // 22323: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpSentences(0) + stopFinish': false,  // 22324: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpSentences(0) + done': false,  // 22325: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpSentences(1) + newWordFragment': false,  // 22346: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpSentences(1) + progress': false,  // 22347: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpSentences(1) + stopBegin': false,  // 22348: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpSentences(1) + stopFinish': false,  // 22349: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpSentences(1) + done': false,  // 22350: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpSentences(3) + newWordFragment': false,  // 22371: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpSentences(3) + progress': false,  // 22372: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpSentences(3) + stopBegin': false,  // 22373: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpSentences(3) + stopFinish': false,  // 22374: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > jumpSentences(3) + done': false,  // 22375: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > nextWord(null) + newWordFragment': false,  // 22421: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > nextWord(null) + progress': false,  // 22422: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > nextWord(null) + stopBegin': false,  // 22423: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > nextWord(null) + stopFinish': false,  // 22424: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > nextWord(null) + done': false,  // 22425: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > nextSentence(null) + newWordFragment': false,  // 22446: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > nextSentence(null) + progress': false,  // 22447: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > nextSentence(null) + stopBegin': false,  // 22448: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > nextSentence(null) + stopFinish': false,  // 22449: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > nextSentence(null) + done': false,  // 22450: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > prevWord(null) + newWordFragment': false,  // 22471: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > prevWord(null) + progress': false,  // 22472: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > prevWord(null) + stopBegin': false,  // 22473: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > prevWord(null) + stopFinish': false,  // 22474: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > prevWord(null) + done': false,  // 22475: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > prevSentence(null) + newWordFragment': false,  // 22496: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > prevSentence(null) + progress': false,  // 22497: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > prevSentence(null) + stopBegin': false,  // 22498: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > prevSentence(null) + stopFinish': false,  // 22499: ?? (skip results are different than combos)
		'skip-combos: jumpTo(100) + done > prevSentence(null) + done': false,  // 22500: ?? (skip results are different than combos)



		// ============== jumpWords(-3) ==============
		// #? like prev
		'skip-combos: jumpWords(-3) + onceBegin > play(null) + playBegin': true,  // 9076: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + onceBegin > play(null) + playFinish': true,  // 9077: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + onceBegin > play(null) + restartBegin': true,  // 9080: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + onceBegin > play(null) + restartFinish': true,  // 9081: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + onceBegin > toggle(null) + playBegin': true,  // 9101: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + onceBegin > toggle(null) + playFinish': true,  // 9102: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + onceBegin > toggle(null) + restartBegin': true,  // 9105: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + onceBegin > toggle(null) + restartFinish': true,  // 9106: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + onceFinish > play(null) + playBegin': true,  // 9976: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + onceFinish > play(null) + playFinish': true,  // 9977: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + onceFinish > play(null) + restartBegin': true,  // 9980: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + onceFinish > play(null) + restartFinish': true,  // 9981: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + onceFinish > toggle(null) + playBegin': true,  // 10001: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + onceFinish > toggle(null) + playFinish': true,  // 10002: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + onceFinish > toggle(null) + restartBegin': true,  // 10005: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + onceFinish > toggle(null) + restartFinish': true,  // 10006: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + loopBegin > play(null) + playBegin': true,  // 16276: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + loopBegin > play(null) + playFinish': true,  // 16277: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + loopBegin > play(null) + restartBegin': true,  // 16280: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + loopBegin > play(null) + restartFinish': true,  // 16281: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + loopBegin > toggle(null) + playBegin': true,  // 16301: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + loopBegin > toggle(null) + playFinish': true,  // 16302: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + loopBegin > toggle(null) + restartBegin': true,  // 16305: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + loopBegin > toggle(null) + restartFinish': true,  // 16306: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + loopFinish > play(null) + playBegin': true,  // 17176: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + loopFinish > play(null) + playFinish': true,  // 17177: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + loopFinish > play(null) + restartBegin': true,  // 17180: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + loopFinish > play(null) + restartFinish': true,  // 17181: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + loopFinish > toggle(null) + playBegin': true,  // 17201: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + loopFinish > toggle(null) + playFinish': true,  // 17202: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + loopFinish > toggle(null) + restartBegin': true,  // 17205: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + loopFinish > toggle(null) + restartFinish': true,  // 17206: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + newWordFragment > play(null) + playBegin': true,  // 18076: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + newWordFragment > play(null) + playFinish': true,  // 18077: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + newWordFragment > play(null) + restartBegin': true,  // 18080: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + newWordFragment > play(null) + restartFinish': true,  // 18081: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + newWordFragment > toggle(null) + playBegin': true,  // 18101: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + newWordFragment > toggle(null) + playFinish': true,  // 18102: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + newWordFragment > toggle(null) + restartBegin': true,  // 18105: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + newWordFragment > toggle(null) + restartFinish': true,  // 18106: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + progress > play(null) + playBegin': true,  // 18976: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + progress > play(null) + playFinish': true,  // 18977: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + progress > play(null) + restartBegin': true,  // 18980: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + progress > play(null) + restartFinish': true,  // 18981: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + progress > toggle(null) + playBegin': true,  // 19001: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + progress > toggle(null) + playFinish': true,  // 19002: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + progress > toggle(null) + restartBegin': true,  // 19005: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + progress > toggle(null) + restartFinish': true,  // 19006: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + stopBegin > play(null) + playBegin': true,  // 19876: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + stopBegin > play(null) + playFinish': true,  // 19877: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + stopBegin > play(null) + restartBegin': true,  // 19880: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + stopBegin > play(null) + restartFinish': true,  // 19881: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + stopBegin > toggle(null) + playBegin': true,  // 19901: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + stopBegin > toggle(null) + playFinish': true,  // 19902: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + stopBegin > toggle(null) + restartBegin': true,  // 19905: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + stopBegin > toggle(null) + restartFinish': true,  // 19906: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + stopFinish > play(null) + playBegin': true,  // 20776: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + stopFinish > play(null) + playFinish': true,  // 20777: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + stopFinish > play(null) + restartBegin': true,  // 20780: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + stopFinish > play(null) + restartFinish': true,  // 20781: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + stopFinish > toggle(null) + playBegin': true,  // 20801: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + stopFinish > toggle(null) + playFinish': true,  // 20802: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + stopFinish > toggle(null) + restartBegin': true,  // 20805: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + stopFinish > toggle(null) + restartFinish': true,  // 20806: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + done > play(null) + playBegin': true,  // 21676: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + done > play(null) + playFinish': true,  // 21677: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + done > play(null) + restartBegin': true,  // 21680: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + done > play(null) + restartFinish': true,  // 21681: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + done > toggle(null) + playBegin': true,  // 21701: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + done > toggle(null) + playFinish': true,  // 21702: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + done > toggle(null) + restartBegin': true,  // 21705: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-3) + done > toggle(null) + restartFinish': true,  // 21706: ?? (skip results are different than combos)



		// ============== jumpWords(-1) ==============
		// #? like prev
		'skip-combos: jumpWords(-1) + onceBegin > play(null) + playBegin': true,  // 9076: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + onceBegin > play(null) + playFinish': true,  // 9077: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + onceBegin > play(null) + restartBegin': true,  // 9080: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + onceBegin > play(null) + restartFinish': true,  // 9081: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + onceBegin > toggle(null) + playBegin': true,  // 9101: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + onceBegin > toggle(null) + playFinish': true,  // 9102: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + onceBegin > toggle(null) + restartBegin': true,  // 9105: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + onceBegin > toggle(null) + restartFinish': true,  // 9106: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + onceFinish > play(null) + playBegin': true,  // 9976: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + onceFinish > play(null) + playFinish': true,  // 9977: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + onceFinish > play(null) + restartBegin': true,  // 9980: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + onceFinish > play(null) + restartFinish': true,  // 9981: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + onceFinish > toggle(null) + playBegin': true,  // 10001: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + onceFinish > toggle(null) + playFinish': true,  // 10002: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + onceFinish > toggle(null) + restartBegin': true,  // 10005: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + onceFinish > toggle(null) + restartFinish': true,  // 10006: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + loopBegin > play(null) + playBegin': true,  // 16276: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + loopBegin > play(null) + playFinish': true,  // 16277: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + loopBegin > play(null) + restartBegin': true,  // 16280: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + loopBegin > play(null) + restartFinish': true,  // 16281: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + loopBegin > toggle(null) + playBegin': true,  // 16301: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + loopBegin > toggle(null) + playFinish': true,  // 16302: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + loopBegin > toggle(null) + restartBegin': true,  // 16305: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + loopBegin > toggle(null) + restartFinish': true,  // 16306: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + loopFinish > play(null) + playBegin': true,  // 17176: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + loopFinish > play(null) + playFinish': true,  // 17177: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + loopFinish > play(null) + restartBegin': true,  // 17180: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + loopFinish > play(null) + restartFinish': true,  // 17181: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + loopFinish > toggle(null) + playBegin': true,  // 17201: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + loopFinish > toggle(null) + playFinish': true,  // 17202: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + loopFinish > toggle(null) + restartBegin': true,  // 17205: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + loopFinish > toggle(null) + restartFinish': true,  // 17206: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + newWordFragment > play(null) + playBegin': true,  // 18076: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + newWordFragment > play(null) + playFinish': true,  // 18077: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + newWordFragment > play(null) + restartBegin': true,  // 18080: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + newWordFragment > play(null) + restartFinish': true,  // 18081: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + newWordFragment > toggle(null) + playBegin': true,  // 18101: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + newWordFragment > toggle(null) + playFinish': true,  // 18102: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + newWordFragment > toggle(null) + restartBegin': true,  // 18105: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + newWordFragment > toggle(null) + restartFinish': true,  // 18106: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + progress > play(null) + playBegin': true,  // 18976: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + progress > play(null) + playFinish': true,  // 18977: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + progress > play(null) + restartBegin': true,  // 18980: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + progress > play(null) + restartFinish': true,  // 18981: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + progress > toggle(null) + playBegin': true,  // 19001: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + progress > toggle(null) + playFinish': true,  // 19002: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + progress > toggle(null) + restartBegin': true,  // 19005: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + progress > toggle(null) + restartFinish': true,  // 19006: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + stopBegin > play(null) + playBegin': true,  // 19876: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + stopBegin > play(null) + playFinish': true,  // 19877: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + stopBegin > play(null) + restartBegin': true,  // 19880: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + stopBegin > play(null) + restartFinish': true,  // 19881: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + stopBegin > toggle(null) + playBegin': true,  // 19901: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + stopBegin > toggle(null) + playFinish': true,  // 19902: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + stopBegin > toggle(null) + restartBegin': true,  // 19905: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + stopBegin > toggle(null) + restartFinish': true,  // 19906: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + stopFinish > play(null) + playBegin': true,  // 20776: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + stopFinish > play(null) + playFinish': true,  // 20777: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + stopFinish > play(null) + restartBegin': true,  // 20780: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + stopFinish > play(null) + restartFinish': true,  // 20781: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + stopFinish > toggle(null) + playBegin': true,  // 20801: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + stopFinish > toggle(null) + playFinish': true,  // 20802: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + stopFinish > toggle(null) + restartBegin': true,  // 20805: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + stopFinish > toggle(null) + restartFinish': true,  // 20806: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + done > play(null) + playBegin': true,  // 21676: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + done > play(null) + playFinish': true,  // 21677: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + done > play(null) + restartBegin': true,  // 21680: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + done > play(null) + restartFinish': true,  // 21681: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + done > toggle(null) + playBegin': true,  // 21701: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + done > toggle(null) + playFinish': true,  // 21702: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + done > toggle(null) + restartBegin': true,  // 21705: ?? (skip results are different than combos)
		'skip-combos: jumpWords(-1) + done > toggle(null) + restartFinish': true,  // 21706: ?? (skip results are different than combos)


		// ============== jumpWords(0) ==============
		// #? jumpWords(0) reverts to 'pause', so toggle = 'revert*|pause*' stuff not 'play*|...' stuff
		// Like current
		'skip-combos: jumpWords(0) + onceBegin > toggle(null) + playBegin': true,  // 9101: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + onceBegin > toggle(null) + playFinish': true,  // 9102: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + onceBegin > toggle(null) + pauseBegin': true,  // 9107: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + onceBegin > toggle(null) + pauseFinish': true,  // 9108: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + onceBegin > toggle(null) + revertBegin': true,  // 9113: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + onceBegin > toggle(null) + revertFinish': true,  // 9114: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + onceBegin > toggle(null) + loopBegin': true,  // 9119: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + onceBegin > toggle(null) + loopFinish': true,  // 9120: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + onceBegin > toggle(null) + newWordFragment': true,  // 9121: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + onceBegin > toggle(null) + progress': true,  // 9122: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + onceBegin > toggle(null) + stopBegin': true,  // 9123: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + onceBegin > toggle(null) + stopFinish': true,  // 9124: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + onceBegin > toggle(null) + done': true,  // 9125: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + onceFinish > toggle(null) + playBegin': true,  // 10001: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + onceFinish > toggle(null) + playFinish': true,  // 10002: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + onceFinish > toggle(null) + pauseBegin': true,  // 10007: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + onceFinish > toggle(null) + pauseFinish': true,  // 10008: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + onceFinish > toggle(null) + revertBegin': true,  // 10013: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + onceFinish > toggle(null) + revertFinish': true,  // 10014: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + onceFinish > toggle(null) + loopBegin': true,  // 10019: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + onceFinish > toggle(null) + loopFinish': true,  // 10020: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + onceFinish > toggle(null) + newWordFragment': true,  // 10021: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + onceFinish > toggle(null) + progress': true,  // 10022: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + onceFinish > toggle(null) + stopBegin': true,  // 10023: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + onceFinish > toggle(null) + stopFinish': true,  // 10024: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + onceFinish > toggle(null) + done': true,  // 10025: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + loopBegin > toggle(null) + playBegin': true,  // 16301: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + loopBegin > toggle(null) + playFinish': true,  // 16302: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + loopBegin > toggle(null) + pauseBegin': true,  // 16307: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + loopBegin > toggle(null) + pauseFinish': true,  // 16308: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + loopBegin > toggle(null) + revertBegin': true,  // 16313: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + loopBegin > toggle(null) + revertFinish': true,  // 16314: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + loopBegin > toggle(null) + loopBegin': true,  // 16319: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + loopBegin > toggle(null) + loopFinish': true,  // 16320: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + loopBegin > toggle(null) + newWordFragment': true,  // 16321: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + loopBegin > toggle(null) + progress': true,  // 16322: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + loopBegin > toggle(null) + stopBegin': true,  // 16323: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + loopBegin > toggle(null) + stopFinish': true,  // 16324: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + loopBegin > toggle(null) + done': true,  // 16325: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + loopFinish > toggle(null) + playBegin': true,  // 17201: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + loopFinish > toggle(null) + playFinish': true,  // 17202: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + loopFinish > toggle(null) + pauseBegin': true,  // 17207: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + loopFinish > toggle(null) + pauseFinish': true,  // 17208: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + loopFinish > toggle(null) + revertBegin': true,  // 17213: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + loopFinish > toggle(null) + revertFinish': true,  // 17214: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + loopFinish > toggle(null) + loopBegin': true,  // 17219: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + loopFinish > toggle(null) + loopFinish': true,  // 17220: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + loopFinish > toggle(null) + newWordFragment': true,  // 17221: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + loopFinish > toggle(null) + progress': true,  // 17222: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + loopFinish > toggle(null) + stopBegin': true,  // 17223: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + loopFinish > toggle(null) + stopFinish': true,  // 17224: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + loopFinish > toggle(null) + done': true,  // 17225: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + newWordFragment > toggle(null) + playBegin': true,  // 18101: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + newWordFragment > toggle(null) + playFinish': true,  // 18102: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + newWordFragment > toggle(null) + pauseBegin': true,  // 18107: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + newWordFragment > toggle(null) + pauseFinish': true,  // 18108: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + newWordFragment > toggle(null) + revertBegin': true,  // 18113: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + newWordFragment > toggle(null) + revertFinish': true,  // 18114: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + newWordFragment > toggle(null) + loopBegin': true,  // 18119: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + newWordFragment > toggle(null) + loopFinish': true,  // 18120: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + newWordFragment > toggle(null) + newWordFragment': true,  // 18121: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + newWordFragment > toggle(null) + progress': true,  // 18122: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + newWordFragment > toggle(null) + stopBegin': true,  // 18123: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + newWordFragment > toggle(null) + stopFinish': true,  // 18124: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + newWordFragment > toggle(null) + done': true,  // 18125: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + progress > toggle(null) + playBegin': true,  // 19001: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + progress > toggle(null) + playFinish': true,  // 19002: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + progress > toggle(null) + pauseBegin': true,  // 19007: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + progress > toggle(null) + pauseFinish': true,  // 19008: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + progress > toggle(null) + revertBegin': true,  // 19013: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + progress > toggle(null) + revertFinish': true,  // 19014: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + progress > toggle(null) + loopBegin': true,  // 19019: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + progress > toggle(null) + loopFinish': true,  // 19020: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + progress > toggle(null) + newWordFragment': true,  // 19021: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + progress > toggle(null) + progress': true,  // 19022: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + progress > toggle(null) + stopBegin': true,  // 19023: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + progress > toggle(null) + stopFinish': true,  // 19024: ?? (skip results are different than combos)
		'skip-combos: jumpWords(0) + progress > toggle(null) + done': true,  // 19025: ?? (skip results are different than combos)



		// ============== jumpWords(4) ==============
		// #? like jumpTo(6) in some (all?) ways
		// #? This one (both?) don't change jumpSentence(-) or prevSentence(), since still goes to first sentence
		'skip-combos: jumpWords(4) + onceBegin > play(null) + newWordFragment': false,  // 9096: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > play(null) + progress': false,  // 9097: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > toggle(null) + playBegin': false,  // 9101: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > toggle(null) + playFinish': false,  // 9102: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > toggle(null) + pauseBegin': false,  // 9107: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > toggle(null) + pauseFinish': false,  // 9108: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > toggle(null) + revertBegin': false,  // 9113: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > toggle(null) + revertFinish': false,  // 9114: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > toggle(null) + loopBegin': false,  // 9119: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > toggle(null) + loopFinish': false,  // 9120: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > toggle(null) + newWordFragment': false,  // 9121: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > toggle(null) + progress': false,  // 9122: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > toggle(null) + stopBegin': false,  // 9123: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > toggle(null) + stopFinish': false,  // 9124: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > toggle(null) + done': false,  // 9125: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > rewind(null) + newWordFragment': false,  // 9246: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > rewind(null) + progress': false,  // 9247: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > fastForward(null) + newWordFragment': false,  // 9271: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > fastForward(null) + progress': false,  // 9272: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > once([0,0,-2]) + newWordFragment': false,  // 9296: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > once([0,0,-2]) + progress': false,  // 9297: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > once([0,0,-2]) + stopBegin': false,  // 9298: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > once([0,0,-2]) + stopFinish': false,  // 9299: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > once([0,0,-2]) + done': false,  // 9300: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > once([0,0,2]) + newWordFragment': false,  // 9321: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > once([0,0,2]) + progress': false,  // 9322: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > current(null) + newWordFragment': false,  // 9346: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > current(null) + progress': false,  // 9347: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > jumpWords(-3) + newWordFragment': false,  // 9521: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > jumpWords(-3) + progress': false,  // 9522: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > jumpWords(-3) + stopBegin': false,  // 9523: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > jumpWords(-3) + stopFinish': false,  // 9524: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > jumpWords(-3) + done': false,  // 9525: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > jumpWords(-1) + newWordFragment': false,  // 9546: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > jumpWords(-1) + progress': false,  // 9547: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > jumpWords(-1) + stopBegin': false,  // 9548: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > jumpWords(-1) + stopFinish': false,  // 9549: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > jumpWords(-1) + done': false,  // 9550: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > jumpWords(0) + newWordFragment': false,  // 9571: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > jumpWords(0) + progress': false,  // 9572: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > jumpWords(4) + newWordFragment': false,  // 9596: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > jumpWords(4) + progress': false,  // 9597: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > jumpSentences(0) + newWordFragment': false,  // 9721: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > jumpSentences(0) + progress': false,  // 9722: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > jumpSentences(1) + newWordFragment': false,  // 9746: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > jumpSentences(1) + progress': false,  // 9747: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > jumpSentences(3) + newWordFragment': false,  // 9771: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > jumpSentences(3) + progress': false,  // 9772: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > jumpSentences(3) + stopBegin': false,  // 9773: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > jumpSentences(3) + stopFinish': false,  // 9774: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > jumpSentences(3) + done': false,  // 9775: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > nextWord(null) + newWordFragment': false,  // 9821: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > nextWord(null) + progress': false,  // 9822: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > nextSentence(null) + newWordFragment': false,  // 9846: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > nextSentence(null) + progress': false,  // 9847: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > prevWord(null) + newWordFragment': false,  // 9871: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > prevWord(null) + progress': false,  // 9872: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > prevWord(null) + stopBegin': false,  // 9873: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > prevWord(null) + stopFinish': false,  // 9874: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceBegin > prevWord(null) + done': false,  // 9875: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > play(null) + newWordFragment': false,  // 9996: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > play(null) + progress': false,  // 9997: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > toggle(null) + playBegin': false,  // 10001: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > toggle(null) + playFinish': false,  // 10002: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > toggle(null) + pauseBegin': false,  // 10007: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > toggle(null) + pauseFinish': false,  // 10008: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > toggle(null) + revertBegin': false,  // 10013: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > toggle(null) + revertFinish': false,  // 10014: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > toggle(null) + loopBegin': false,  // 10019: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > toggle(null) + loopFinish': false,  // 10020: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > toggle(null) + newWordFragment': false,  // 10021: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > toggle(null) + progress': false,  // 10022: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > toggle(null) + stopBegin': false,  // 10023: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > toggle(null) + stopFinish': false,  // 10024: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > toggle(null) + done': false,  // 10025: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > rewind(null) + newWordFragment': false,  // 10146: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > rewind(null) + progress': false,  // 10147: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > fastForward(null) + newWordFragment': false,  // 10171: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > fastForward(null) + progress': false,  // 10172: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > once([0,0,-2]) + newWordFragment': false,  // 10196: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > once([0,0,-2]) + progress': false,  // 10197: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > once([0,0,-2]) + stopBegin': false,  // 10198: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > once([0,0,-2]) + stopFinish': false,  // 10199: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > once([0,0,-2]) + done': false,  // 10200: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > once([0,0,2]) + newWordFragment': false,  // 10221: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > once([0,0,2]) + progress': false,  // 10222: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > current(null) + newWordFragment': false,  // 10246: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > current(null) + progress': false,  // 10247: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > jumpWords(-3) + newWordFragment': false,  // 10421: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > jumpWords(-3) + progress': false,  // 10422: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > jumpWords(-3) + stopBegin': false,  // 10423: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > jumpWords(-3) + stopFinish': false,  // 10424: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > jumpWords(-3) + done': false,  // 10425: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > jumpWords(-1) + newWordFragment': false,  // 10446: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > jumpWords(-1) + progress': false,  // 10447: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > jumpWords(-1) + stopBegin': false,  // 10448: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > jumpWords(-1) + stopFinish': false,  // 10449: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > jumpWords(-1) + done': false,  // 10450: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > jumpWords(0) + newWordFragment': false,  // 10471: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > jumpWords(0) + progress': false,  // 10472: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > jumpWords(4) + newWordFragment': false,  // 10496: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > jumpWords(4) + progress': false,  // 10497: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > jumpSentences(0) + newWordFragment': false,  // 10621: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > jumpSentences(0) + progress': false,  // 10622: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > jumpSentences(1) + newWordFragment': false,  // 10646: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > jumpSentences(1) + progress': false,  // 10647: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > jumpSentences(3) + newWordFragment': false,  // 10671: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > jumpSentences(3) + progress': false,  // 10672: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > jumpSentences(3) + stopBegin': false,  // 10673: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > jumpSentences(3) + stopFinish': false,  // 10674: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > jumpSentences(3) + done': false,  // 10675: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > nextWord(null) + newWordFragment': false,  // 10721: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > nextWord(null) + progress': false,  // 10722: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > nextSentence(null) + newWordFragment': false,  // 10746: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > nextSentence(null) + progress': false,  // 10747: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > prevWord(null) + newWordFragment': false,  // 10771: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > prevWord(null) + progress': false,  // 10772: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > prevWord(null) + stopBegin': false,  // 10773: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > prevWord(null) + stopFinish': false,  // 10774: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + onceFinish > prevWord(null) + done': false,  // 10775: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > play(null) + newWordFragment': false,  // 16296: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > play(null) + progress': false,  // 16297: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > toggle(null) + playBegin': false,  // 16301: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > toggle(null) + playFinish': false,  // 16302: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > toggle(null) + pauseBegin': false,  // 16307: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > toggle(null) + pauseFinish': false,  // 16308: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > toggle(null) + revertBegin': false,  // 16313: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > toggle(null) + revertFinish': false,  // 16314: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > toggle(null) + loopBegin': false,  // 16319: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > toggle(null) + loopFinish': false,  // 16320: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > toggle(null) + newWordFragment': false,  // 16321: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > toggle(null) + progress': false,  // 16322: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > toggle(null) + stopBegin': false,  // 16323: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > toggle(null) + stopFinish': false,  // 16324: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > toggle(null) + done': false,  // 16325: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > rewind(null) + newWordFragment': false,  // 16446: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > rewind(null) + progress': false,  // 16447: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > fastForward(null) + newWordFragment': false,  // 16471: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > fastForward(null) + progress': false,  // 16472: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > once([0,0,-2]) + newWordFragment': false,  // 16496: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > once([0,0,-2]) + progress': false,  // 16497: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > once([0,0,-2]) + stopBegin': false,  // 16498: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > once([0,0,-2]) + stopFinish': false,  // 16499: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > once([0,0,-2]) + done': false,  // 16500: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > once([0,0,2]) + newWordFragment': false,  // 16521: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > once([0,0,2]) + progress': false,  // 16522: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > current(null) + newWordFragment': false,  // 16546: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > current(null) + progress': false,  // 16547: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > jumpWords(-3) + newWordFragment': false,  // 16721: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > jumpWords(-3) + progress': false,  // 16722: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > jumpWords(-3) + stopBegin': false,  // 16723: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > jumpWords(-3) + stopFinish': false,  // 16724: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > jumpWords(-3) + done': false,  // 16725: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > jumpWords(-1) + newWordFragment': false,  // 16746: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > jumpWords(-1) + progress': false,  // 16747: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > jumpWords(-1) + stopBegin': false,  // 16748: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > jumpWords(-1) + stopFinish': false,  // 16749: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > jumpWords(-1) + done': false,  // 16750: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > jumpWords(0) + newWordFragment': false,  // 16771: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > jumpWords(0) + progress': false,  // 16772: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > jumpWords(4) + newWordFragment': false,  // 16796: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > jumpWords(4) + progress': false,  // 16797: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > jumpSentences(0) + newWordFragment': false,  // 16921: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > jumpSentences(0) + progress': false,  // 16922: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > jumpSentences(1) + newWordFragment': false,  // 16946: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > jumpSentences(1) + progress': false,  // 16947: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > jumpSentences(3) + newWordFragment': false,  // 16971: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > jumpSentences(3) + progress': false,  // 16972: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > jumpSentences(3) + stopBegin': false,  // 16973: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > jumpSentences(3) + stopFinish': false,  // 16974: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > jumpSentences(3) + done': false,  // 16975: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > nextWord(null) + newWordFragment': false,  // 17021: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > nextWord(null) + progress': false,  // 17022: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > nextSentence(null) + newWordFragment': false,  // 17046: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > nextSentence(null) + progress': false,  // 17047: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > prevWord(null) + newWordFragment': false,  // 17071: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > prevWord(null) + progress': false,  // 17072: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > prevWord(null) + stopBegin': false,  // 17073: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > prevWord(null) + stopFinish': false,  // 17074: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopBegin > prevWord(null) + done': false,  // 17075: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > play(null) + newWordFragment': false,  // 17196: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > play(null) + progress': false,  // 17197: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > toggle(null) + playBegin': false,  // 17201: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > toggle(null) + playFinish': false,  // 17202: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > toggle(null) + pauseBegin': false,  // 17207: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > toggle(null) + pauseFinish': false,  // 17208: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > toggle(null) + revertBegin': false,  // 17213: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > toggle(null) + revertFinish': false,  // 17214: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > toggle(null) + loopBegin': false,  // 17219: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > toggle(null) + loopFinish': false,  // 17220: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > toggle(null) + newWordFragment': false,  // 17221: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > toggle(null) + progress': false,  // 17222: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > toggle(null) + stopBegin': false,  // 17223: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > toggle(null) + stopFinish': false,  // 17224: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > toggle(null) + done': false,  // 17225: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > rewind(null) + newWordFragment': false,  // 17346: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > rewind(null) + progress': false,  // 17347: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > fastForward(null) + newWordFragment': false,  // 17371: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > fastForward(null) + progress': false,  // 17372: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > once([0,0,-2]) + newWordFragment': false,  // 17396: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > once([0,0,-2]) + progress': false,  // 17397: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > once([0,0,-2]) + stopBegin': false,  // 17398: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > once([0,0,-2]) + stopFinish': false,  // 17399: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > once([0,0,-2]) + done': false,  // 17400: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > once([0,0,2]) + newWordFragment': false,  // 17421: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > once([0,0,2]) + progress': false,  // 17422: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > current(null) + newWordFragment': false,  // 17446: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > current(null) + progress': false,  // 17447: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > jumpWords(-3) + newWordFragment': false,  // 17621: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > jumpWords(-3) + progress': false,  // 17622: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > jumpWords(-3) + stopBegin': false,  // 17623: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > jumpWords(-3) + stopFinish': false,  // 17624: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > jumpWords(-3) + done': false,  // 17625: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > jumpWords(-1) + newWordFragment': false,  // 17646: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > jumpWords(-1) + progress': false,  // 17647: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > jumpWords(-1) + stopBegin': false,  // 17648: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > jumpWords(-1) + stopFinish': false,  // 17649: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > jumpWords(-1) + done': false,  // 17650: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > jumpWords(0) + newWordFragment': false,  // 17671: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > jumpWords(0) + progress': false,  // 17672: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > jumpWords(4) + newWordFragment': false,  // 17696: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > jumpWords(4) + progress': false,  // 17697: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > jumpSentences(0) + newWordFragment': false,  // 17821: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > jumpSentences(0) + progress': false,  // 17822: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > jumpSentences(1) + newWordFragment': false,  // 17846: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > jumpSentences(1) + progress': false,  // 17847: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > jumpSentences(3) + newWordFragment': false,  // 17871: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > jumpSentences(3) + progress': false,  // 17872: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > jumpSentences(3) + stopBegin': false,  // 17873: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > jumpSentences(3) + stopFinish': false,  // 17874: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > jumpSentences(3) + done': false,  // 17875: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > nextWord(null) + newWordFragment': false,  // 17921: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > nextWord(null) + progress': false,  // 17922: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > nextSentence(null) + newWordFragment': false,  // 17946: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > nextSentence(null) + progress': false,  // 17947: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > prevWord(null) + newWordFragment': false,  // 17971: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > prevWord(null) + progress': false,  // 17972: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > prevWord(null) + stopBegin': false,  // 17973: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > prevWord(null) + stopFinish': false,  // 17974: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + loopFinish > prevWord(null) + done': false,  // 17975: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > play(null) + newWordFragment': false,  // 18096: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > play(null) + progress': false,  // 18097: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > toggle(null) + playBegin': false,  // 18101: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > toggle(null) + playFinish': false,  // 18102: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > toggle(null) + pauseBegin': false,  // 18107: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > toggle(null) + pauseFinish': false,  // 18108: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > toggle(null) + revertBegin': false,  // 18113: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > toggle(null) + revertFinish': false,  // 18114: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > toggle(null) + loopBegin': false,  // 18119: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > toggle(null) + loopFinish': false,  // 18120: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > toggle(null) + newWordFragment': false,  // 18121: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > toggle(null) + progress': false,  // 18122: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > toggle(null) + stopBegin': false,  // 18123: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > toggle(null) + stopFinish': false,  // 18124: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > toggle(null) + done': false,  // 18125: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > rewind(null) + newWordFragment': false,  // 18246: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > rewind(null) + progress': false,  // 18247: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > fastForward(null) + newWordFragment': false,  // 18271: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > fastForward(null) + progress': false,  // 18272: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > once([0,0,-2]) + newWordFragment': false,  // 18296: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > once([0,0,-2]) + progress': false,  // 18297: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > once([0,0,-2]) + stopBegin': false,  // 18298: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > once([0,0,-2]) + stopFinish': false,  // 18299: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > once([0,0,-2]) + done': false,  // 18300: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > once([0,0,2]) + newWordFragment': false,  // 18321: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > once([0,0,2]) + progress': false,  // 18322: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > current(null) + newWordFragment': false,  // 18346: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > current(null) + progress': false,  // 18347: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > jumpWords(-3) + newWordFragment': false,  // 18521: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > jumpWords(-3) + progress': false,  // 18522: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > jumpWords(-3) + stopBegin': false,  // 18523: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > jumpWords(-3) + stopFinish': false,  // 18524: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > jumpWords(-3) + done': false,  // 18525: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > jumpWords(-1) + newWordFragment': false,  // 18546: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > jumpWords(-1) + progress': false,  // 18547: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > jumpWords(-1) + stopBegin': false,  // 18548: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > jumpWords(-1) + stopFinish': false,  // 18549: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > jumpWords(-1) + done': false,  // 18550: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > jumpWords(0) + newWordFragment': false,  // 18571: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > jumpWords(0) + progress': false,  // 18572: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > jumpWords(4) + newWordFragment': false,  // 18596: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > jumpWords(4) + progress': false,  // 18597: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > jumpSentences(0) + newWordFragment': false,  // 18721: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > jumpSentences(0) + progress': false,  // 18722: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > jumpSentences(1) + newWordFragment': false,  // 18746: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > jumpSentences(1) + progress': false,  // 18747: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > jumpSentences(3) + newWordFragment': false,  // 18771: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > jumpSentences(3) + progress': false,  // 18772: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > jumpSentences(3) + stopBegin': false,  // 18773: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > jumpSentences(3) + stopFinish': false,  // 18774: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > jumpSentences(3) + done': false,  // 18775: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > nextWord(null) + newWordFragment': false,  // 18821: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > nextWord(null) + progress': false,  // 18822: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > nextSentence(null) + newWordFragment': false,  // 18846: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > nextSentence(null) + progress': false,  // 18847: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > prevWord(null) + newWordFragment': false,  // 18871: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > prevWord(null) + progress': false,  // 18872: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > prevWord(null) + stopBegin': false,  // 18873: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > prevWord(null) + stopFinish': false,  // 18874: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + newWordFragment > prevWord(null) + done': false,  // 18875: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > play(null) + newWordFragment': false,  // 18996: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > play(null) + progress': false,  // 18997: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > toggle(null) + playBegin': false,  // 19001: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > toggle(null) + playFinish': false,  // 19002: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > toggle(null) + pauseBegin': false,  // 19007: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > toggle(null) + pauseFinish': false,  // 19008: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > toggle(null) + revertBegin': false,  // 19013: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > toggle(null) + revertFinish': false,  // 19014: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > toggle(null) + loopBegin': false,  // 19019: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > toggle(null) + loopFinish': false,  // 19020: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > toggle(null) + newWordFragment': false,  // 19021: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > toggle(null) + progress': false,  // 19022: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > toggle(null) + stopBegin': false,  // 19023: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > toggle(null) + stopFinish': false,  // 19024: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > toggle(null) + done': false,  // 19025: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > rewind(null) + newWordFragment': false,  // 19146: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > rewind(null) + progress': false,  // 19147: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > fastForward(null) + newWordFragment': false,  // 19171: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > fastForward(null) + progress': false,  // 19172: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > once([0,0,-2]) + newWordFragment': false,  // 19196: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > once([0,0,-2]) + progress': false,  // 19197: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > once([0,0,-2]) + stopBegin': false,  // 19198: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > once([0,0,-2]) + stopFinish': false,  // 19199: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > once([0,0,-2]) + done': false,  // 19200: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > once([0,0,2]) + newWordFragment': false,  // 19221: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > once([0,0,2]) + progress': false,  // 19222: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > current(null) + newWordFragment': false,  // 19246: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > current(null) + progress': false,  // 19247: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > jumpWords(-3) + newWordFragment': false,  // 19421: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > jumpWords(-3) + progress': false,  // 19422: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > jumpWords(-3) + stopBegin': false,  // 19423: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > jumpWords(-3) + stopFinish': false,  // 19424: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > jumpWords(-3) + done': false,  // 19425: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > jumpWords(-1) + newWordFragment': false,  // 19446: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > jumpWords(-1) + progress': false,  // 19447: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > jumpWords(-1) + stopBegin': false,  // 19448: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > jumpWords(-1) + stopFinish': false,  // 19449: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > jumpWords(-1) + done': false,  // 19450: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > jumpWords(0) + newWordFragment': false,  // 19471: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > jumpWords(0) + progress': false,  // 19472: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > jumpWords(4) + newWordFragment': false,  // 19496: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > jumpWords(4) + progress': false,  // 19497: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > jumpSentences(0) + newWordFragment': false,  // 19621: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > jumpSentences(0) + progress': false,  // 19622: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > jumpSentences(1) + newWordFragment': false,  // 19646: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > jumpSentences(1) + progress': false,  // 19647: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > jumpSentences(3) + newWordFragment': false,  // 19671: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > jumpSentences(3) + progress': false,  // 19672: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > jumpSentences(3) + stopBegin': false,  // 19673: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > jumpSentences(3) + stopFinish': false,  // 19674: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > jumpSentences(3) + done': false,  // 19675: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > nextWord(null) + newWordFragment': false,  // 19721: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > nextWord(null) + progress': false,  // 19722: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > nextSentence(null) + newWordFragment': false,  // 19746: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > nextSentence(null) + progress': false,  // 19747: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > prevWord(null) + newWordFragment': false,  // 19771: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > prevWord(null) + progress': false,  // 19772: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > prevWord(null) + stopBegin': false,  // 19773: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > prevWord(null) + stopFinish': false,  // 19774: ?? (skip results are different than combos)
		'skip-combos: jumpWords(4) + progress > prevWord(null) + done': false,  // 19775: ?? (skip results are different than combos)



		// ============== jumpWords(11) ==============
		// #? like jumpTo(11)
		// #6 immediately leading to stop*|done > some more types of relative loopers(+|0|-) get different output than usual
		// #7 immediately leading to stop*|done > relative single(not rewind) loopers(-) don't trigger stop*|done
		// #8 immediately leading to stop*|done > relative loopers(+|0) do unexpectedly trigger stop*|done
		// #9 immediately leading to stop*|done > play|toggle = triggers `restart` instead of `play`
		'skip-combos: jumpWords(11) + onceBegin > play(null) + playBegin': false,  // 9076: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > play(null) + playFinish': false,  // 9077: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > play(null) + restartBegin': false,  // 9080: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > play(null) + restartFinish': false,  // 9081: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > toggle(null) + playBegin': false,  // 9101: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > toggle(null) + playFinish': false,  // 9102: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > toggle(null) + restartBegin': false,  // 9105: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > toggle(null) + restartFinish': false,  // 9106: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > rewind(null) + newWordFragment': false,  // 9246: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > rewind(null) + progress': false,  // 9247: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > fastForward(null) + newWordFragment': false,  // 9271: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > fastForward(null) + progress': false,  // 9272: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > once([0,0,-2]) + newWordFragment': false,  // 9296: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > once([0,0,-2]) + progress': false,  // 9297: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > once([0,0,-2]) + stopBegin': false,  // 9298: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > once([0,0,-2]) + stopFinish': false,  // 9299: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > once([0,0,-2]) + done': false,  // 9300: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > once([0,0,2]) + newWordFragment': false,  // 9321: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > once([0,0,2]) + progress': false,  // 9322: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > once([0,0,2]) + stopBegin': false,  // 9323: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > once([0,0,2]) + stopFinish': false,  // 9324: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > once([0,0,2]) + done': false,  // 9325: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > current(null) + newWordFragment': false,  // 9346: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > current(null) + progress': false,  // 9347: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > current(null) + stopBegin': false,  // 9348: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > current(null) + stopFinish': false,  // 9349: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > current(null) + done': false,  // 9350: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpWords(-3) + newWordFragment': false,  // 9521: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpWords(-3) + progress': false,  // 9522: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpWords(-3) + stopBegin': false,  // 9523: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpWords(-3) + stopFinish': false,  // 9524: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpWords(-3) + done': false,  // 9525: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpWords(-1) + newWordFragment': false,  // 9546: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpWords(-1) + progress': false,  // 9547: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpWords(-1) + stopBegin': false,  // 9548: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpWords(-1) + stopFinish': false,  // 9549: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpWords(-1) + done': false,  // 9550: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpWords(0) + newWordFragment': false,  // 9571: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpWords(0) + progress': false,  // 9572: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpWords(0) + stopBegin': false,  // 9573: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpWords(0) + stopFinish': false,  // 9574: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpWords(0) + done': false,  // 9575: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpWords(4) + newWordFragment': false,  // 9596: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpWords(4) + progress': false,  // 9597: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpWords(4) + stopBegin': false,  // 9598: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpWords(4) + stopFinish': false,  // 9599: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpWords(4) + done': false,  // 9600: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpSentences(-1) + newWordFragment': false,  // 9696: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpSentences(-1) + progress': false,  // 9697: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpSentences(-1) + stopBegin': false,  // 9698: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpSentences(-1) + stopFinish': false,  // 9699: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpSentences(-1) + done': false,  // 9700: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpSentences(0) + newWordFragment': false,  // 9721: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpSentences(0) + progress': false,  // 9722: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpSentences(0) + stopBegin': false,  // 9723: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpSentences(0) + stopFinish': false,  // 9724: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpSentences(0) + done': false,  // 9725: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpSentences(1) + newWordFragment': false,  // 9746: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpSentences(1) + progress': false,  // 9747: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpSentences(1) + stopBegin': false,  // 9748: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpSentences(1) + stopFinish': false,  // 9749: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpSentences(1) + done': false,  // 9750: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpSentences(3) + newWordFragment': false,  // 9771: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpSentences(3) + progress': false,  // 9772: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpSentences(3) + stopBegin': false,  // 9773: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpSentences(3) + stopFinish': false,  // 9774: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > jumpSentences(3) + done': false,  // 9775: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > nextWord(null) + newWordFragment': false,  // 9821: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > nextWord(null) + progress': false,  // 9822: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > nextWord(null) + stopBegin': false,  // 9823: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > nextWord(null) + stopFinish': false,  // 9824: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > nextWord(null) + done': false,  // 9825: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > nextSentence(null) + newWordFragment': false,  // 9846: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > nextSentence(null) + progress': false,  // 9847: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > nextSentence(null) + stopBegin': false,  // 9848: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > nextSentence(null) + stopFinish': false,  // 9849: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > nextSentence(null) + done': false,  // 9850: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > prevWord(null) + newWordFragment': false,  // 9871: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > prevWord(null) + progress': false,  // 9872: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > prevWord(null) + stopBegin': false,  // 9873: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > prevWord(null) + stopFinish': false,  // 9874: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > prevWord(null) + done': false,  // 9875: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > prevSentence(null) + newWordFragment': false,  // 9896: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > prevSentence(null) + progress': false,  // 9897: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > prevSentence(null) + stopBegin': false,  // 9898: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > prevSentence(null) + stopFinish': false,  // 9899: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceBegin > prevSentence(null) + done': false,  // 9900: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > play(null) + playBegin': false,  // 9976: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > play(null) + playFinish': false,  // 9977: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > play(null) + restartBegin': false,  // 9980: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > play(null) + restartFinish': false,  // 9981: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > toggle(null) + playBegin': false,  // 10001: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > toggle(null) + playFinish': false,  // 10002: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > toggle(null) + restartBegin': false,  // 10005: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > toggle(null) + restartFinish': false,  // 10006: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > rewind(null) + newWordFragment': false,  // 10146: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > rewind(null) + progress': false,  // 10147: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > fastForward(null) + newWordFragment': false,  // 10171: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > fastForward(null) + progress': false,  // 10172: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > once([0,0,-2]) + newWordFragment': false,  // 10196: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > once([0,0,-2]) + progress': false,  // 10197: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > once([0,0,-2]) + stopBegin': false,  // 10198: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > once([0,0,-2]) + stopFinish': false,  // 10199: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > once([0,0,-2]) + done': false,  // 10200: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > once([0,0,2]) + newWordFragment': false,  // 10221: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > once([0,0,2]) + progress': false,  // 10222: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > once([0,0,2]) + stopBegin': false,  // 10223: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > once([0,0,2]) + stopFinish': false,  // 10224: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > once([0,0,2]) + done': false,  // 10225: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > current(null) + newWordFragment': false,  // 10246: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > current(null) + progress': false,  // 10247: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > current(null) + stopBegin': false,  // 10248: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > current(null) + stopFinish': false,  // 10249: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > current(null) + done': false,  // 10250: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpWords(-3) + newWordFragment': false,  // 10421: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpWords(-3) + progress': false,  // 10422: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpWords(-3) + stopBegin': false,  // 10423: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpWords(-3) + stopFinish': false,  // 10424: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpWords(-3) + done': false,  // 10425: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpWords(-1) + newWordFragment': false,  // 10446: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpWords(-1) + progress': false,  // 10447: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpWords(-1) + stopBegin': false,  // 10448: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpWords(-1) + stopFinish': false,  // 10449: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpWords(-1) + done': false,  // 10450: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpWords(0) + newWordFragment': false,  // 10471: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpWords(0) + progress': false,  // 10472: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpWords(0) + stopBegin': false,  // 10473: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpWords(0) + stopFinish': false,  // 10474: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpWords(0) + done': false,  // 10475: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpWords(4) + newWordFragment': false,  // 10496: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpWords(4) + progress': false,  // 10497: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpWords(4) + stopBegin': false,  // 10498: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpWords(4) + stopFinish': false,  // 10499: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpWords(4) + done': false,  // 10500: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpSentences(-1) + newWordFragment': false,  // 10596: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpSentences(-1) + progress': false,  // 10597: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpSentences(-1) + stopBegin': false,  // 10598: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpSentences(-1) + stopFinish': false,  // 10599: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpSentences(-1) + done': false,  // 10600: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpSentences(0) + newWordFragment': false,  // 10621: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpSentences(0) + progress': false,  // 10622: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpSentences(0) + stopBegin': false,  // 10623: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpSentences(0) + stopFinish': false,  // 10624: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpSentences(0) + done': false,  // 10625: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpSentences(1) + newWordFragment': false,  // 10646: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpSentences(1) + progress': false,  // 10647: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpSentences(1) + stopBegin': false,  // 10648: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpSentences(1) + stopFinish': false,  // 10649: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpSentences(1) + done': false,  // 10650: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpSentences(3) + newWordFragment': false,  // 10671: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpSentences(3) + progress': false,  // 10672: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpSentences(3) + stopBegin': false,  // 10673: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpSentences(3) + stopFinish': false,  // 10674: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > jumpSentences(3) + done': false,  // 10675: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > nextWord(null) + newWordFragment': false,  // 10721: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > nextWord(null) + progress': false,  // 10722: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > nextWord(null) + stopBegin': false,  // 10723: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > nextWord(null) + stopFinish': false,  // 10724: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > nextWord(null) + done': false,  // 10725: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > nextSentence(null) + newWordFragment': false,  // 10746: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > nextSentence(null) + progress': false,  // 10747: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > nextSentence(null) + stopBegin': false,  // 10748: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > nextSentence(null) + stopFinish': false,  // 10749: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > nextSentence(null) + done': false,  // 10750: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > prevWord(null) + newWordFragment': false,  // 10771: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > prevWord(null) + progress': false,  // 10772: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > prevWord(null) + stopBegin': false,  // 10773: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > prevWord(null) + stopFinish': false,  // 10774: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > prevWord(null) + done': false,  // 10775: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > prevSentence(null) + newWordFragment': false,  // 10796: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > prevSentence(null) + progress': false,  // 10797: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > prevSentence(null) + stopBegin': false,  // 10798: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > prevSentence(null) + stopFinish': false,  // 10799: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + onceFinish > prevSentence(null) + done': false,  // 10800: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > play(null) + playBegin': false,  // 16276: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > play(null) + playFinish': false,  // 16277: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > play(null) + restartBegin': false,  // 16280: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > play(null) + restartFinish': false,  // 16281: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > toggle(null) + playBegin': false,  // 16301: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > toggle(null) + playFinish': false,  // 16302: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > toggle(null) + restartBegin': false,  // 16305: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > toggle(null) + restartFinish': false,  // 16306: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > rewind(null) + newWordFragment': false,  // 16446: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > rewind(null) + progress': false,  // 16447: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > fastForward(null) + newWordFragment': false,  // 16471: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > fastForward(null) + progress': false,  // 16472: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > once([0,0,-2]) + newWordFragment': false,  // 16496: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > once([0,0,-2]) + progress': false,  // 16497: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > once([0,0,-2]) + stopBegin': false,  // 16498: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > once([0,0,-2]) + stopFinish': false,  // 16499: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > once([0,0,-2]) + done': false,  // 16500: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > once([0,0,2]) + newWordFragment': false,  // 16521: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > once([0,0,2]) + progress': false,  // 16522: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > once([0,0,2]) + stopBegin': false,  // 16523: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > once([0,0,2]) + stopFinish': false,  // 16524: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > once([0,0,2]) + done': false,  // 16525: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > current(null) + newWordFragment': false,  // 16546: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > current(null) + progress': false,  // 16547: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > current(null) + stopBegin': false,  // 16548: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > current(null) + stopFinish': false,  // 16549: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > current(null) + done': false,  // 16550: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpWords(-3) + newWordFragment': false,  // 16721: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpWords(-3) + progress': false,  // 16722: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpWords(-3) + stopBegin': false,  // 16723: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpWords(-3) + stopFinish': false,  // 16724: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpWords(-3) + done': false,  // 16725: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpWords(-1) + newWordFragment': false,  // 16746: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpWords(-1) + progress': false,  // 16747: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpWords(-1) + stopBegin': false,  // 16748: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpWords(-1) + stopFinish': false,  // 16749: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpWords(-1) + done': false,  // 16750: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpWords(0) + newWordFragment': false,  // 16771: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpWords(0) + progress': false,  // 16772: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpWords(0) + stopBegin': false,  // 16773: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpWords(0) + stopFinish': false,  // 16774: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpWords(0) + done': false,  // 16775: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpWords(4) + newWordFragment': false,  // 16796: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpWords(4) + progress': false,  // 16797: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpWords(4) + stopBegin': false,  // 16798: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpWords(4) + stopFinish': false,  // 16799: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpWords(4) + done': false,  // 16800: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpSentences(-1) + newWordFragment': false,  // 16896: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpSentences(-1) + progress': false,  // 16897: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpSentences(-1) + stopBegin': false,  // 16898: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpSentences(-1) + stopFinish': false,  // 16899: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpSentences(-1) + done': false,  // 16900: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpSentences(0) + newWordFragment': false,  // 16921: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpSentences(0) + progress': false,  // 16922: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpSentences(0) + stopBegin': false,  // 16923: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpSentences(0) + stopFinish': false,  // 16924: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpSentences(0) + done': false,  // 16925: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpSentences(1) + newWordFragment': false,  // 16946: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpSentences(1) + progress': false,  // 16947: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpSentences(1) + stopBegin': false,  // 16948: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpSentences(1) + stopFinish': false,  // 16949: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpSentences(1) + done': false,  // 16950: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpSentences(3) + newWordFragment': false,  // 16971: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpSentences(3) + progress': false,  // 16972: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpSentences(3) + stopBegin': false,  // 16973: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpSentences(3) + stopFinish': false,  // 16974: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > jumpSentences(3) + done': false,  // 16975: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > nextWord(null) + newWordFragment': false,  // 17021: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > nextWord(null) + progress': false,  // 17022: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > nextWord(null) + stopBegin': false,  // 17023: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > nextWord(null) + stopFinish': false,  // 17024: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > nextWord(null) + done': false,  // 17025: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > nextSentence(null) + newWordFragment': false,  // 17046: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > nextSentence(null) + progress': false,  // 17047: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > nextSentence(null) + stopBegin': false,  // 17048: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > nextSentence(null) + stopFinish': false,  // 17049: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > nextSentence(null) + done': false,  // 17050: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > prevWord(null) + newWordFragment': false,  // 17071: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > prevWord(null) + progress': false,  // 17072: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > prevWord(null) + stopBegin': false,  // 17073: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > prevWord(null) + stopFinish': false,  // 17074: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > prevWord(null) + done': false,  // 17075: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > prevSentence(null) + newWordFragment': false,  // 17096: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > prevSentence(null) + progress': false,  // 17097: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > prevSentence(null) + stopBegin': false,  // 17098: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > prevSentence(null) + stopFinish': false,  // 17099: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopBegin > prevSentence(null) + done': false,  // 17100: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > play(null) + playBegin': false,  // 17176: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > play(null) + playFinish': false,  // 17177: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > play(null) + restartBegin': false,  // 17180: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > play(null) + restartFinish': false,  // 17181: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > toggle(null) + playBegin': false,  // 17201: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > toggle(null) + playFinish': false,  // 17202: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > toggle(null) + restartBegin': false,  // 17205: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > toggle(null) + restartFinish': false,  // 17206: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > rewind(null) + newWordFragment': false,  // 17346: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > rewind(null) + progress': false,  // 17347: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > fastForward(null) + newWordFragment': false,  // 17371: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > fastForward(null) + progress': false,  // 17372: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > once([0,0,-2]) + newWordFragment': false,  // 17396: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > once([0,0,-2]) + progress': false,  // 17397: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > once([0,0,-2]) + stopBegin': false,  // 17398: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > once([0,0,-2]) + stopFinish': false,  // 17399: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > once([0,0,-2]) + done': false,  // 17400: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > once([0,0,2]) + newWordFragment': false,  // 17421: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > once([0,0,2]) + progress': false,  // 17422: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > once([0,0,2]) + stopBegin': false,  // 17423: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > once([0,0,2]) + stopFinish': false,  // 17424: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > once([0,0,2]) + done': false,  // 17425: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > current(null) + newWordFragment': false,  // 17446: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > current(null) + progress': false,  // 17447: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > current(null) + stopBegin': false,  // 17448: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > current(null) + stopFinish': false,  // 17449: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > current(null) + done': false,  // 17450: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpWords(-3) + newWordFragment': false,  // 17621: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpWords(-3) + progress': false,  // 17622: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpWords(-3) + stopBegin': false,  // 17623: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpWords(-3) + stopFinish': false,  // 17624: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpWords(-3) + done': false,  // 17625: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpWords(-1) + newWordFragment': false,  // 17646: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpWords(-1) + progress': false,  // 17647: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpWords(-1) + stopBegin': false,  // 17648: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpWords(-1) + stopFinish': false,  // 17649: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpWords(-1) + done': false,  // 17650: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpWords(0) + newWordFragment': false,  // 17671: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpWords(0) + progress': false,  // 17672: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpWords(0) + stopBegin': false,  // 17673: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpWords(0) + stopFinish': false,  // 17674: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpWords(0) + done': false,  // 17675: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpWords(4) + newWordFragment': false,  // 17696: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpWords(4) + progress': false,  // 17697: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpWords(4) + stopBegin': false,  // 17698: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpWords(4) + stopFinish': false,  // 17699: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpWords(4) + done': false,  // 17700: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpSentences(-1) + newWordFragment': false,  // 17796: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpSentences(-1) + progress': false,  // 17797: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpSentences(-1) + stopBegin': false,  // 17798: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpSentences(-1) + stopFinish': false,  // 17799: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpSentences(-1) + done': false,  // 17800: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpSentences(0) + newWordFragment': false,  // 17821: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpSentences(0) + progress': false,  // 17822: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpSentences(0) + stopBegin': false,  // 17823: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpSentences(0) + stopFinish': false,  // 17824: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpSentences(0) + done': false,  // 17825: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpSentences(1) + newWordFragment': false,  // 17846: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpSentences(1) + progress': false,  // 17847: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpSentences(1) + stopBegin': false,  // 17848: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpSentences(1) + stopFinish': false,  // 17849: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpSentences(1) + done': false,  // 17850: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpSentences(3) + newWordFragment': false,  // 17871: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpSentences(3) + progress': false,  // 17872: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpSentences(3) + stopBegin': false,  // 17873: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpSentences(3) + stopFinish': false,  // 17874: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > jumpSentences(3) + done': false,  // 17875: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > nextWord(null) + newWordFragment': false,  // 17921: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > nextWord(null) + progress': false,  // 17922: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > nextWord(null) + stopBegin': false,  // 17923: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > nextWord(null) + stopFinish': false,  // 17924: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > nextWord(null) + done': false,  // 17925: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > nextSentence(null) + newWordFragment': false,  // 17946: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > nextSentence(null) + progress': false,  // 17947: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > nextSentence(null) + stopBegin': false,  // 17948: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > nextSentence(null) + stopFinish': false,  // 17949: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > nextSentence(null) + done': false,  // 17950: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > prevWord(null) + newWordFragment': false,  // 17971: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > prevWord(null) + progress': false,  // 17972: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > prevWord(null) + stopBegin': false,  // 17973: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > prevWord(null) + stopFinish': false,  // 17974: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > prevWord(null) + done': false,  // 17975: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > prevSentence(null) + newWordFragment': false,  // 17996: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > prevSentence(null) + progress': false,  // 17997: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > prevSentence(null) + stopBegin': false,  // 17998: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > prevSentence(null) + stopFinish': false,  // 17999: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + loopFinish > prevSentence(null) + done': false,  // 18000: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > play(null) + playBegin': false,  // 18076: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > play(null) + playFinish': false,  // 18077: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > play(null) + restartBegin': false,  // 18080: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > play(null) + restartFinish': false,  // 18081: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > toggle(null) + playBegin': false,  // 18101: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > toggle(null) + playFinish': false,  // 18102: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > toggle(null) + restartBegin': false,  // 18105: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > toggle(null) + restartFinish': false,  // 18106: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > rewind(null) + newWordFragment': false,  // 18246: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > rewind(null) + progress': false,  // 18247: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > fastForward(null) + newWordFragment': false,  // 18271: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > fastForward(null) + progress': false,  // 18272: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > once([0,0,-2]) + newWordFragment': false,  // 18296: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > once([0,0,-2]) + progress': false,  // 18297: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > once([0,0,-2]) + stopBegin': false,  // 18298: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > once([0,0,-2]) + stopFinish': false,  // 18299: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > once([0,0,-2]) + done': false,  // 18300: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > once([0,0,2]) + newWordFragment': false,  // 18321: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > once([0,0,2]) + progress': false,  // 18322: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > once([0,0,2]) + stopBegin': false,  // 18323: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > once([0,0,2]) + stopFinish': false,  // 18324: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > once([0,0,2]) + done': false,  // 18325: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > current(null) + newWordFragment': false,  // 18346: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > current(null) + progress': false,  // 18347: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > current(null) + stopBegin': false,  // 18348: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > current(null) + stopFinish': false,  // 18349: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > current(null) + done': false,  // 18350: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpWords(-3) + newWordFragment': false,  // 18521: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpWords(-3) + progress': false,  // 18522: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpWords(-3) + stopBegin': false,  // 18523: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpWords(-3) + stopFinish': false,  // 18524: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpWords(-3) + done': false,  // 18525: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpWords(-1) + newWordFragment': false,  // 18546: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpWords(-1) + progress': false,  // 18547: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpWords(-1) + stopBegin': false,  // 18548: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpWords(-1) + stopFinish': false,  // 18549: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpWords(-1) + done': false,  // 18550: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpWords(0) + newWordFragment': false,  // 18571: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpWords(0) + progress': false,  // 18572: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpWords(0) + stopBegin': false,  // 18573: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpWords(0) + stopFinish': false,  // 18574: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpWords(0) + done': false,  // 18575: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpWords(4) + newWordFragment': false,  // 18596: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpWords(4) + progress': false,  // 18597: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpWords(4) + stopBegin': false,  // 18598: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpWords(4) + stopFinish': false,  // 18599: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpWords(4) + done': false,  // 18600: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpSentences(-1) + newWordFragment': false,  // 18696: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpSentences(-1) + progress': false,  // 18697: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpSentences(-1) + stopBegin': false,  // 18698: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpSentences(-1) + stopFinish': false,  // 18699: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpSentences(-1) + done': false,  // 18700: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpSentences(0) + newWordFragment': false,  // 18721: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpSentences(0) + progress': false,  // 18722: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpSentences(0) + stopBegin': false,  // 18723: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpSentences(0) + stopFinish': false,  // 18724: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpSentences(0) + done': false,  // 18725: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpSentences(1) + newWordFragment': false,  // 18746: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpSentences(1) + progress': false,  // 18747: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpSentences(1) + stopBegin': false,  // 18748: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpSentences(1) + stopFinish': false,  // 18749: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpSentences(1) + done': false,  // 18750: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpSentences(3) + newWordFragment': false,  // 18771: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpSentences(3) + progress': false,  // 18772: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpSentences(3) + stopBegin': false,  // 18773: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpSentences(3) + stopFinish': false,  // 18774: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > jumpSentences(3) + done': false,  // 18775: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > nextWord(null) + newWordFragment': false,  // 18821: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > nextWord(null) + progress': false,  // 18822: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > nextWord(null) + stopBegin': false,  // 18823: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > nextWord(null) + stopFinish': false,  // 18824: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > nextWord(null) + done': false,  // 18825: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > nextSentence(null) + newWordFragment': false,  // 18846: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > nextSentence(null) + progress': false,  // 18847: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > nextSentence(null) + stopBegin': false,  // 18848: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > nextSentence(null) + stopFinish': false,  // 18849: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > nextSentence(null) + done': false,  // 18850: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > prevWord(null) + newWordFragment': false,  // 18871: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > prevWord(null) + progress': false,  // 18872: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > prevWord(null) + stopBegin': false,  // 18873: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > prevWord(null) + stopFinish': false,  // 18874: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > prevWord(null) + done': false,  // 18875: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > prevSentence(null) + newWordFragment': false,  // 18896: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > prevSentence(null) + progress': false,  // 18897: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > prevSentence(null) + stopBegin': false,  // 18898: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > prevSentence(null) + stopFinish': false,  // 18899: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + newWordFragment > prevSentence(null) + done': false,  // 18900: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > play(null) + playBegin': false,  // 18976: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > play(null) + playFinish': false,  // 18977: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > play(null) + restartBegin': false,  // 18980: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > play(null) + restartFinish': false,  // 18981: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > toggle(null) + playBegin': false,  // 19001: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > toggle(null) + playFinish': false,  // 19002: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > toggle(null) + restartBegin': false,  // 19005: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > toggle(null) + restartFinish': false,  // 19006: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > rewind(null) + newWordFragment': false,  // 19146: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > rewind(null) + progress': false,  // 19147: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > fastForward(null) + newWordFragment': false,  // 19171: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > fastForward(null) + progress': false,  // 19172: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > once([0,0,-2]) + newWordFragment': false,  // 19196: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > once([0,0,-2]) + progress': false,  // 19197: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > once([0,0,-2]) + stopBegin': false,  // 19198: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > once([0,0,-2]) + stopFinish': false,  // 19199: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > once([0,0,-2]) + done': false,  // 19200: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > once([0,0,2]) + newWordFragment': false,  // 19221: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > once([0,0,2]) + progress': false,  // 19222: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > once([0,0,2]) + stopBegin': false,  // 19223: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > once([0,0,2]) + stopFinish': false,  // 19224: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > once([0,0,2]) + done': false,  // 19225: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > current(null) + newWordFragment': false,  // 19246: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > current(null) + progress': false,  // 19247: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > current(null) + stopBegin': false,  // 19248: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > current(null) + stopFinish': false,  // 19249: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > current(null) + done': false,  // 19250: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpWords(-3) + newWordFragment': false,  // 19421: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpWords(-3) + progress': false,  // 19422: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpWords(-3) + stopBegin': false,  // 19423: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpWords(-3) + stopFinish': false,  // 19424: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpWords(-3) + done': false,  // 19425: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpWords(-1) + newWordFragment': false,  // 19446: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpWords(-1) + progress': false,  // 19447: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpWords(-1) + stopBegin': false,  // 19448: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpWords(-1) + stopFinish': false,  // 19449: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpWords(-1) + done': false,  // 19450: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpWords(0) + newWordFragment': false,  // 19471: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpWords(0) + progress': false,  // 19472: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpWords(0) + stopBegin': false,  // 19473: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpWords(0) + stopFinish': false,  // 19474: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpWords(0) + done': false,  // 19475: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpWords(4) + newWordFragment': false,  // 19496: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpWords(4) + progress': false,  // 19497: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpWords(4) + stopBegin': false,  // 19498: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpWords(4) + stopFinish': false,  // 19499: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpWords(4) + done': false,  // 19500: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpSentences(-1) + newWordFragment': false,  // 19596: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpSentences(-1) + progress': false,  // 19597: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpSentences(-1) + stopBegin': false,  // 19598: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpSentences(-1) + stopFinish': false,  // 19599: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpSentences(-1) + done': false,  // 19600: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpSentences(0) + newWordFragment': false,  // 19621: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpSentences(0) + progress': false,  // 19622: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpSentences(0) + stopBegin': false,  // 19623: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpSentences(0) + stopFinish': false,  // 19624: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpSentences(0) + done': false,  // 19625: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpSentences(1) + newWordFragment': false,  // 19646: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpSentences(1) + progress': false,  // 19647: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpSentences(1) + stopBegin': false,  // 19648: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpSentences(1) + stopFinish': false,  // 19649: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpSentences(1) + done': false,  // 19650: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpSentences(3) + newWordFragment': false,  // 19671: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpSentences(3) + progress': false,  // 19672: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpSentences(3) + stopBegin': false,  // 19673: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpSentences(3) + stopFinish': false,  // 19674: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > jumpSentences(3) + done': false,  // 19675: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > nextWord(null) + newWordFragment': false,  // 19721: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > nextWord(null) + progress': false,  // 19722: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > nextWord(null) + stopBegin': false,  // 19723: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > nextWord(null) + stopFinish': false,  // 19724: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > nextWord(null) + done': false,  // 19725: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > nextSentence(null) + newWordFragment': false,  // 19746: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > nextSentence(null) + progress': false,  // 19747: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > nextSentence(null) + stopBegin': false,  // 19748: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > nextSentence(null) + stopFinish': false,  // 19749: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > nextSentence(null) + done': false,  // 19750: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > prevWord(null) + newWordFragment': false,  // 19771: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > prevWord(null) + progress': false,  // 19772: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > prevWord(null) + stopBegin': false,  // 19773: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > prevWord(null) + stopFinish': false,  // 19774: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > prevWord(null) + done': false,  // 19775: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > prevSentence(null) + newWordFragment': false,  // 19796: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > prevSentence(null) + progress': false,  // 19797: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > prevSentence(null) + stopBegin': false,  // 19798: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > prevSentence(null) + stopFinish': false,  // 19799: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + progress > prevSentence(null) + done': false,  // 19800: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > play(null) + playBegin': false,  // 19876: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > play(null) + playFinish': false,  // 19877: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > play(null) + restartBegin': false,  // 19880: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > play(null) + restartFinish': false,  // 19881: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > toggle(null) + playBegin': false,  // 19901: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > toggle(null) + playFinish': false,  // 19902: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > toggle(null) + restartBegin': false,  // 19905: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > toggle(null) + restartFinish': false,  // 19906: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > rewind(null) + newWordFragment': false,  // 20046: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > rewind(null) + progress': false,  // 20047: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > fastForward(null) + newWordFragment': false,  // 20071: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > fastForward(null) + progress': false,  // 20072: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > once([0,0,-2]) + newWordFragment': false,  // 20096: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > once([0,0,-2]) + progress': false,  // 20097: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > once([0,0,-2]) + stopBegin': false,  // 20098: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > once([0,0,-2]) + stopFinish': false,  // 20099: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > once([0,0,-2]) + done': false,  // 20100: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > once([0,0,2]) + newWordFragment': false,  // 20121: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > once([0,0,2]) + progress': false,  // 20122: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > once([0,0,2]) + stopBegin': false,  // 20123: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > once([0,0,2]) + stopFinish': false,  // 20124: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > once([0,0,2]) + done': false,  // 20125: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > current(null) + newWordFragment': false,  // 20146: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > current(null) + progress': false,  // 20147: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > current(null) + stopBegin': false,  // 20148: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > current(null) + stopFinish': false,  // 20149: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > current(null) + done': false,  // 20150: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpWords(-3) + newWordFragment': false,  // 20321: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpWords(-3) + progress': false,  // 20322: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpWords(-3) + stopBegin': false,  // 20323: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpWords(-3) + stopFinish': false,  // 20324: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpWords(-3) + done': false,  // 20325: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpWords(-1) + newWordFragment': false,  // 20346: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpWords(-1) + progress': false,  // 20347: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpWords(-1) + stopBegin': false,  // 20348: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpWords(-1) + stopFinish': false,  // 20349: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpWords(-1) + done': false,  // 20350: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpWords(0) + newWordFragment': false,  // 20371: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpWords(0) + progress': false,  // 20372: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpWords(0) + stopBegin': false,  // 20373: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpWords(0) + stopFinish': false,  // 20374: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpWords(0) + done': false,  // 20375: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpWords(4) + newWordFragment': false,  // 20396: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpWords(4) + progress': false,  // 20397: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpWords(4) + stopBegin': false,  // 20398: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpWords(4) + stopFinish': false,  // 20399: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpWords(4) + done': false,  // 20400: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpSentences(-1) + newWordFragment': false,  // 20496: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpSentences(-1) + progress': false,  // 20497: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpSentences(-1) + stopBegin': false,  // 20498: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpSentences(-1) + stopFinish': false,  // 20499: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpSentences(-1) + done': false,  // 20500: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpSentences(0) + newWordFragment': false,  // 20521: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpSentences(0) + progress': false,  // 20522: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpSentences(0) + stopBegin': false,  // 20523: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpSentences(0) + stopFinish': false,  // 20524: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpSentences(0) + done': false,  // 20525: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpSentences(1) + newWordFragment': false,  // 20546: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpSentences(1) + progress': false,  // 20547: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpSentences(1) + stopBegin': false,  // 20548: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpSentences(1) + stopFinish': false,  // 20549: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpSentences(1) + done': false,  // 20550: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpSentences(3) + newWordFragment': false,  // 20571: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpSentences(3) + progress': false,  // 20572: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpSentences(3) + stopBegin': false,  // 20573: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpSentences(3) + stopFinish': false,  // 20574: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > jumpSentences(3) + done': false,  // 20575: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > nextWord(null) + newWordFragment': false,  // 20621: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > nextWord(null) + progress': false,  // 20622: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > nextWord(null) + stopBegin': false,  // 20623: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > nextWord(null) + stopFinish': false,  // 20624: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > nextWord(null) + done': false,  // 20625: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > nextSentence(null) + newWordFragment': false,  // 20646: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > nextSentence(null) + progress': false,  // 20647: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > nextSentence(null) + stopBegin': false,  // 20648: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > nextSentence(null) + stopFinish': false,  // 20649: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > nextSentence(null) + done': false,  // 20650: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > prevWord(null) + newWordFragment': false,  // 20671: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > prevWord(null) + progress': false,  // 20672: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > prevWord(null) + stopBegin': false,  // 20673: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > prevWord(null) + stopFinish': false,  // 20674: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > prevWord(null) + done': false,  // 20675: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > prevSentence(null) + newWordFragment': false,  // 20696: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > prevSentence(null) + progress': false,  // 20697: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > prevSentence(null) + stopBegin': false,  // 20698: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > prevSentence(null) + stopFinish': false,  // 20699: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopBegin > prevSentence(null) + done': false,  // 20700: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > play(null) + playBegin': false,  // 20776: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > play(null) + playFinish': false,  // 20777: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > play(null) + restartBegin': false,  // 20780: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > play(null) + restartFinish': false,  // 20781: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > toggle(null) + playBegin': false,  // 20801: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > toggle(null) + playFinish': false,  // 20802: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > toggle(null) + restartBegin': false,  // 20805: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > toggle(null) + restartFinish': false,  // 20806: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > rewind(null) + newWordFragment': false,  // 20946: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > rewind(null) + progress': false,  // 20947: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > fastForward(null) + newWordFragment': false,  // 20971: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > fastForward(null) + progress': false,  // 20972: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > once([0,0,-2]) + newWordFragment': false,  // 20996: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > once([0,0,-2]) + progress': false,  // 20997: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > once([0,0,-2]) + stopBegin': false,  // 20998: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > once([0,0,-2]) + stopFinish': false,  // 20999: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > once([0,0,-2]) + done': false,  // 21000: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > once([0,0,2]) + newWordFragment': false,  // 21021: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > once([0,0,2]) + progress': false,  // 21022: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > once([0,0,2]) + stopBegin': false,  // 21023: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > once([0,0,2]) + stopFinish': false,  // 21024: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > once([0,0,2]) + done': false,  // 21025: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > current(null) + newWordFragment': false,  // 21046: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > current(null) + progress': false,  // 21047: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > current(null) + stopBegin': false,  // 21048: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > current(null) + stopFinish': false,  // 21049: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > current(null) + done': false,  // 21050: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpWords(-3) + newWordFragment': false,  // 21221: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpWords(-3) + progress': false,  // 21222: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpWords(-3) + stopBegin': false,  // 21223: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpWords(-3) + stopFinish': false,  // 21224: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpWords(-3) + done': false,  // 21225: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpWords(-1) + newWordFragment': false,  // 21246: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpWords(-1) + progress': false,  // 21247: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpWords(-1) + stopBegin': false,  // 21248: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpWords(-1) + stopFinish': false,  // 21249: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpWords(-1) + done': false,  // 21250: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpWords(0) + newWordFragment': false,  // 21271: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpWords(0) + progress': false,  // 21272: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpWords(0) + stopBegin': false,  // 21273: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpWords(0) + stopFinish': false,  // 21274: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpWords(0) + done': false,  // 21275: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpWords(4) + newWordFragment': false,  // 21296: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpWords(4) + progress': false,  // 21297: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpWords(4) + stopBegin': false,  // 21298: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpWords(4) + stopFinish': false,  // 21299: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpWords(4) + done': false,  // 21300: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpSentences(-1) + newWordFragment': false,  // 21396: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpSentences(-1) + progress': false,  // 21397: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpSentences(-1) + stopBegin': false,  // 21398: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpSentences(-1) + stopFinish': false,  // 21399: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpSentences(-1) + done': false,  // 21400: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpSentences(0) + newWordFragment': false,  // 21421: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpSentences(0) + progress': false,  // 21422: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpSentences(0) + stopBegin': false,  // 21423: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpSentences(0) + stopFinish': false,  // 21424: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpSentences(0) + done': false,  // 21425: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpSentences(1) + newWordFragment': false,  // 21446: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpSentences(1) + progress': false,  // 21447: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpSentences(1) + stopBegin': false,  // 21448: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpSentences(1) + stopFinish': false,  // 21449: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpSentences(1) + done': false,  // 21450: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpSentences(3) + newWordFragment': false,  // 21471: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpSentences(3) + progress': false,  // 21472: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpSentences(3) + stopBegin': false,  // 21473: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpSentences(3) + stopFinish': false,  // 21474: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > jumpSentences(3) + done': false,  // 21475: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > nextWord(null) + newWordFragment': false,  // 21521: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > nextWord(null) + progress': false,  // 21522: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > nextWord(null) + stopBegin': false,  // 21523: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > nextWord(null) + stopFinish': false,  // 21524: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > nextWord(null) + done': false,  // 21525: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > nextSentence(null) + newWordFragment': false,  // 21546: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > nextSentence(null) + progress': false,  // 21547: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > nextSentence(null) + stopBegin': false,  // 21548: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > nextSentence(null) + stopFinish': false,  // 21549: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > nextSentence(null) + done': false,  // 21550: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > prevWord(null) + newWordFragment': false,  // 21571: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > prevWord(null) + progress': false,  // 21572: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > prevWord(null) + stopBegin': false,  // 21573: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > prevWord(null) + stopFinish': false,  // 21574: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > prevWord(null) + done': false,  // 21575: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > prevSentence(null) + newWordFragment': false,  // 21596: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > prevSentence(null) + progress': false,  // 21597: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > prevSentence(null) + stopBegin': false,  // 21598: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > prevSentence(null) + stopFinish': false,  // 21599: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + stopFinish > prevSentence(null) + done': false,  // 21600: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > play(null) + playBegin': false,  // 21676: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > play(null) + playFinish': false,  // 21677: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > play(null) + restartBegin': false,  // 21680: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > play(null) + restartFinish': false,  // 21681: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > toggle(null) + playBegin': false,  // 21701: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > toggle(null) + playFinish': false,  // 21702: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > toggle(null) + restartBegin': false,  // 21705: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > toggle(null) + restartFinish': false,  // 21706: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > rewind(null) + newWordFragment': false,  // 21846: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > rewind(null) + progress': false,  // 21847: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > fastForward(null) + newWordFragment': false,  // 21871: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > fastForward(null) + progress': false,  // 21872: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > once([0,0,-2]) + newWordFragment': false,  // 21896: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > once([0,0,-2]) + progress': false,  // 21897: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > once([0,0,-2]) + stopBegin': false,  // 21898: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > once([0,0,-2]) + stopFinish': false,  // 21899: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > once([0,0,-2]) + done': false,  // 21900: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > once([0,0,2]) + newWordFragment': false,  // 21921: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > once([0,0,2]) + progress': false,  // 21922: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > once([0,0,2]) + stopBegin': false,  // 21923: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > once([0,0,2]) + stopFinish': false,  // 21924: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > once([0,0,2]) + done': false,  // 21925: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > current(null) + newWordFragment': false,  // 21946: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > current(null) + progress': false,  // 21947: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > current(null) + stopBegin': false,  // 21948: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > current(null) + stopFinish': false,  // 21949: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > current(null) + done': false,  // 21950: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpWords(-3) + newWordFragment': false,  // 22121: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpWords(-3) + progress': false,  // 22122: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpWords(-3) + stopBegin': false,  // 22123: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpWords(-3) + stopFinish': false,  // 22124: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpWords(-3) + done': false,  // 22125: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpWords(-1) + newWordFragment': false,  // 22146: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpWords(-1) + progress': false,  // 22147: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpWords(-1) + stopBegin': false,  // 22148: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpWords(-1) + stopFinish': false,  // 22149: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpWords(-1) + done': false,  // 22150: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpWords(0) + newWordFragment': false,  // 22171: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpWords(0) + progress': false,  // 22172: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpWords(0) + stopBegin': false,  // 22173: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpWords(0) + stopFinish': false,  // 22174: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpWords(0) + done': false,  // 22175: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpWords(4) + newWordFragment': false,  // 22196: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpWords(4) + progress': false,  // 22197: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpWords(4) + stopBegin': false,  // 22198: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpWords(4) + stopFinish': false,  // 22199: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpWords(4) + done': false,  // 22200: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpSentences(-1) + newWordFragment': false,  // 22296: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpSentences(-1) + progress': false,  // 22297: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpSentences(-1) + stopBegin': false,  // 22298: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpSentences(-1) + stopFinish': false,  // 22299: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpSentences(-1) + done': false,  // 22300: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpSentences(0) + newWordFragment': false,  // 22321: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpSentences(0) + progress': false,  // 22322: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpSentences(0) + stopBegin': false,  // 22323: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpSentences(0) + stopFinish': false,  // 22324: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpSentences(0) + done': false,  // 22325: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpSentences(1) + newWordFragment': false,  // 22346: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpSentences(1) + progress': false,  // 22347: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpSentences(1) + stopBegin': false,  // 22348: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpSentences(1) + stopFinish': false,  // 22349: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpSentences(1) + done': false,  // 22350: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpSentences(3) + newWordFragment': false,  // 22371: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpSentences(3) + progress': false,  // 22372: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpSentences(3) + stopBegin': false,  // 22373: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpSentences(3) + stopFinish': false,  // 22374: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > jumpSentences(3) + done': false,  // 22375: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > nextWord(null) + newWordFragment': false,  // 22421: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > nextWord(null) + progress': false,  // 22422: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > nextWord(null) + stopBegin': false,  // 22423: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > nextWord(null) + stopFinish': false,  // 22424: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > nextWord(null) + done': false,  // 22425: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > nextSentence(null) + newWordFragment': false,  // 22446: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > nextSentence(null) + progress': false,  // 22447: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > nextSentence(null) + stopBegin': false,  // 22448: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > nextSentence(null) + stopFinish': false,  // 22449: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > nextSentence(null) + done': false,  // 22450: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > prevWord(null) + newWordFragment': false,  // 22471: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > prevWord(null) + progress': false,  // 22472: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > prevWord(null) + stopBegin': false,  // 22473: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > prevWord(null) + stopFinish': false,  // 22474: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > prevWord(null) + done': false,  // 22475: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > prevSentence(null) + newWordFragment': false,  // 22496: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > prevSentence(null) + progress': false,  // 22497: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > prevSentence(null) + stopBegin': false,  // 22498: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > prevSentence(null) + stopFinish': false,  // 22499: ?? (skip results are different than combos)
		'skip-combos: jumpWords(11) + done > prevSentence(null) + done': false,  // 22500: ?? (skip results are different than combos)



		// ============== jumpWords(100) ==============
		// #? like jumpTo(11)
		// #6 immediately leading to stop*|done > some more types of relative loopers(+|0|-) get different output than usual
		// #7 immediately leading to stop*|done > relative single(not rewind) loopers(-) don't trigger stop*|done
		// #8 immediately leading to stop*|done > relative loopers(+|0) do unexpectedly trigger stop*|done
		// #9 immediately leading to stop*|done > play|toggle = triggers `restart` instead of `play`
		'skip-combos: jumpWords(100) + onceBegin > play(null) + playBegin': false,  // 9076: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > play(null) + playFinish': false,  // 9077: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > play(null) + restartBegin': false,  // 9080: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > play(null) + restartFinish': false,  // 9081: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > toggle(null) + playBegin': false,  // 9101: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > toggle(null) + playFinish': false,  // 9102: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > toggle(null) + restartBegin': false,  // 9105: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > toggle(null) + restartFinish': false,  // 9106: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > rewind(null) + newWordFragment': false,  // 9246: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > rewind(null) + progress': false,  // 9247: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > fastForward(null) + newWordFragment': false,  // 9271: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > fastForward(null) + progress': false,  // 9272: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > once([0,0,-2]) + newWordFragment': false,  // 9296: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > once([0,0,-2]) + progress': false,  // 9297: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > once([0,0,-2]) + stopBegin': false,  // 9298: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > once([0,0,-2]) + stopFinish': false,  // 9299: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > once([0,0,-2]) + done': false,  // 9300: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > once([0,0,2]) + newWordFragment': false,  // 9321: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > once([0,0,2]) + progress': false,  // 9322: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > once([0,0,2]) + stopBegin': false,  // 9323: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > once([0,0,2]) + stopFinish': false,  // 9324: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > once([0,0,2]) + done': false,  // 9325: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > current(null) + newWordFragment': false,  // 9346: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > current(null) + progress': false,  // 9347: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > current(null) + stopBegin': false,  // 9348: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > current(null) + stopFinish': false,  // 9349: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > current(null) + done': false,  // 9350: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpWords(-3) + newWordFragment': false,  // 9521: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpWords(-3) + progress': false,  // 9522: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpWords(-3) + stopBegin': false,  // 9523: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpWords(-3) + stopFinish': false,  // 9524: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpWords(-3) + done': false,  // 9525: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpWords(-1) + newWordFragment': false,  // 9546: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpWords(-1) + progress': false,  // 9547: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpWords(-1) + stopBegin': false,  // 9548: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpWords(-1) + stopFinish': false,  // 9549: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpWords(-1) + done': false,  // 9550: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpWords(0) + newWordFragment': false,  // 9571: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpWords(0) + progress': false,  // 9572: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpWords(0) + stopBegin': false,  // 9573: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpWords(0) + stopFinish': false,  // 9574: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpWords(0) + done': false,  // 9575: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpWords(4) + newWordFragment': false,  // 9596: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpWords(4) + progress': false,  // 9597: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpWords(4) + stopBegin': false,  // 9598: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpWords(4) + stopFinish': false,  // 9599: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpWords(4) + done': false,  // 9600: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpSentences(-1) + newWordFragment': false,  // 9696: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpSentences(-1) + progress': false,  // 9697: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpSentences(-1) + stopBegin': false,  // 9698: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpSentences(-1) + stopFinish': false,  // 9699: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpSentences(-1) + done': false,  // 9700: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpSentences(0) + newWordFragment': false,  // 9721: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpSentences(0) + progress': false,  // 9722: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpSentences(0) + stopBegin': false,  // 9723: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpSentences(0) + stopFinish': false,  // 9724: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpSentences(0) + done': false,  // 9725: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpSentences(1) + newWordFragment': false,  // 9746: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpSentences(1) + progress': false,  // 9747: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpSentences(1) + stopBegin': false,  // 9748: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpSentences(1) + stopFinish': false,  // 9749: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpSentences(1) + done': false,  // 9750: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpSentences(3) + newWordFragment': false,  // 9771: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpSentences(3) + progress': false,  // 9772: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpSentences(3) + stopBegin': false,  // 9773: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpSentences(3) + stopFinish': false,  // 9774: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > jumpSentences(3) + done': false,  // 9775: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > nextWord(null) + newWordFragment': false,  // 9821: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > nextWord(null) + progress': false,  // 9822: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > nextWord(null) + stopBegin': false,  // 9823: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > nextWord(null) + stopFinish': false,  // 9824: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > nextWord(null) + done': false,  // 9825: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > nextSentence(null) + newWordFragment': false,  // 9846: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > nextSentence(null) + progress': false,  // 9847: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > nextSentence(null) + stopBegin': false,  // 9848: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > nextSentence(null) + stopFinish': false,  // 9849: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > nextSentence(null) + done': false,  // 9850: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > prevWord(null) + newWordFragment': false,  // 9871: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > prevWord(null) + progress': false,  // 9872: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > prevWord(null) + stopBegin': false,  // 9873: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > prevWord(null) + stopFinish': false,  // 9874: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > prevWord(null) + done': false,  // 9875: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > prevSentence(null) + newWordFragment': false,  // 9896: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > prevSentence(null) + progress': false,  // 9897: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > prevSentence(null) + stopBegin': false,  // 9898: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > prevSentence(null) + stopFinish': false,  // 9899: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceBegin > prevSentence(null) + done': false,  // 9900: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > play(null) + playBegin': false,  // 9976: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > play(null) + playFinish': false,  // 9977: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > play(null) + restartBegin': false,  // 9980: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > play(null) + restartFinish': false,  // 9981: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > toggle(null) + playBegin': false,  // 10001: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > toggle(null) + playFinish': false,  // 10002: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > toggle(null) + restartBegin': false,  // 10005: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > toggle(null) + restartFinish': false,  // 10006: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > rewind(null) + newWordFragment': false,  // 10146: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > rewind(null) + progress': false,  // 10147: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > fastForward(null) + newWordFragment': false,  // 10171: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > fastForward(null) + progress': false,  // 10172: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > once([0,0,-2]) + newWordFragment': false,  // 10196: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > once([0,0,-2]) + progress': false,  // 10197: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > once([0,0,-2]) + stopBegin': false,  // 10198: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > once([0,0,-2]) + stopFinish': false,  // 10199: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > once([0,0,-2]) + done': false,  // 10200: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > once([0,0,2]) + newWordFragment': false,  // 10221: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > once([0,0,2]) + progress': false,  // 10222: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > once([0,0,2]) + stopBegin': false,  // 10223: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > once([0,0,2]) + stopFinish': false,  // 10224: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > once([0,0,2]) + done': false,  // 10225: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > current(null) + newWordFragment': false,  // 10246: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > current(null) + progress': false,  // 10247: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > current(null) + stopBegin': false,  // 10248: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > current(null) + stopFinish': false,  // 10249: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > current(null) + done': false,  // 10250: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpWords(-3) + newWordFragment': false,  // 10421: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpWords(-3) + progress': false,  // 10422: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpWords(-3) + stopBegin': false,  // 10423: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpWords(-3) + stopFinish': false,  // 10424: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpWords(-3) + done': false,  // 10425: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpWords(-1) + newWordFragment': false,  // 10446: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpWords(-1) + progress': false,  // 10447: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpWords(-1) + stopBegin': false,  // 10448: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpWords(-1) + stopFinish': false,  // 10449: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpWords(-1) + done': false,  // 10450: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpWords(0) + newWordFragment': false,  // 10471: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpWords(0) + progress': false,  // 10472: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpWords(0) + stopBegin': false,  // 10473: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpWords(0) + stopFinish': false,  // 10474: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpWords(0) + done': false,  // 10475: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpWords(4) + newWordFragment': false,  // 10496: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpWords(4) + progress': false,  // 10497: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpWords(4) + stopBegin': false,  // 10498: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpWords(4) + stopFinish': false,  // 10499: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpWords(4) + done': false,  // 10500: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpSentences(-1) + newWordFragment': false,  // 10596: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpSentences(-1) + progress': false,  // 10597: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpSentences(-1) + stopBegin': false,  // 10598: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpSentences(-1) + stopFinish': false,  // 10599: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpSentences(-1) + done': false,  // 10600: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpSentences(0) + newWordFragment': false,  // 10621: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpSentences(0) + progress': false,  // 10622: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpSentences(0) + stopBegin': false,  // 10623: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpSentences(0) + stopFinish': false,  // 10624: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpSentences(0) + done': false,  // 10625: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpSentences(1) + newWordFragment': false,  // 10646: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpSentences(1) + progress': false,  // 10647: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpSentences(1) + stopBegin': false,  // 10648: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpSentences(1) + stopFinish': false,  // 10649: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpSentences(1) + done': false,  // 10650: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpSentences(3) + newWordFragment': false,  // 10671: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpSentences(3) + progress': false,  // 10672: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpSentences(3) + stopBegin': false,  // 10673: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpSentences(3) + stopFinish': false,  // 10674: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > jumpSentences(3) + done': false,  // 10675: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > nextWord(null) + newWordFragment': false,  // 10721: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > nextWord(null) + progress': false,  // 10722: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > nextWord(null) + stopBegin': false,  // 10723: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > nextWord(null) + stopFinish': false,  // 10724: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > nextWord(null) + done': false,  // 10725: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > nextSentence(null) + newWordFragment': false,  // 10746: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > nextSentence(null) + progress': false,  // 10747: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > nextSentence(null) + stopBegin': false,  // 10748: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > nextSentence(null) + stopFinish': false,  // 10749: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > nextSentence(null) + done': false,  // 10750: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > prevWord(null) + newWordFragment': false,  // 10771: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > prevWord(null) + progress': false,  // 10772: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > prevWord(null) + stopBegin': false,  // 10773: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > prevWord(null) + stopFinish': false,  // 10774: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > prevWord(null) + done': false,  // 10775: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > prevSentence(null) + newWordFragment': false,  // 10796: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > prevSentence(null) + progress': false,  // 10797: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > prevSentence(null) + stopBegin': false,  // 10798: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > prevSentence(null) + stopFinish': false,  // 10799: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + onceFinish > prevSentence(null) + done': false,  // 10800: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > play(null) + playBegin': false,  // 16276: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > play(null) + playFinish': false,  // 16277: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > play(null) + restartBegin': false,  // 16280: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > play(null) + restartFinish': false,  // 16281: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > toggle(null) + playBegin': false,  // 16301: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > toggle(null) + playFinish': false,  // 16302: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > toggle(null) + restartBegin': false,  // 16305: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > toggle(null) + restartFinish': false,  // 16306: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > rewind(null) + newWordFragment': false,  // 16446: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > rewind(null) + progress': false,  // 16447: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > fastForward(null) + newWordFragment': false,  // 16471: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > fastForward(null) + progress': false,  // 16472: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > once([0,0,-2]) + newWordFragment': false,  // 16496: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > once([0,0,-2]) + progress': false,  // 16497: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > once([0,0,-2]) + stopBegin': false,  // 16498: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > once([0,0,-2]) + stopFinish': false,  // 16499: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > once([0,0,-2]) + done': false,  // 16500: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > once([0,0,2]) + newWordFragment': false,  // 16521: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > once([0,0,2]) + progress': false,  // 16522: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > once([0,0,2]) + stopBegin': false,  // 16523: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > once([0,0,2]) + stopFinish': false,  // 16524: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > once([0,0,2]) + done': false,  // 16525: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > current(null) + newWordFragment': false,  // 16546: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > current(null) + progress': false,  // 16547: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > current(null) + stopBegin': false,  // 16548: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > current(null) + stopFinish': false,  // 16549: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > current(null) + done': false,  // 16550: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpWords(-3) + newWordFragment': false,  // 16721: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpWords(-3) + progress': false,  // 16722: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpWords(-3) + stopBegin': false,  // 16723: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpWords(-3) + stopFinish': false,  // 16724: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpWords(-3) + done': false,  // 16725: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpWords(-1) + newWordFragment': false,  // 16746: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpWords(-1) + progress': false,  // 16747: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpWords(-1) + stopBegin': false,  // 16748: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpWords(-1) + stopFinish': false,  // 16749: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpWords(-1) + done': false,  // 16750: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpWords(0) + newWordFragment': false,  // 16771: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpWords(0) + progress': false,  // 16772: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpWords(0) + stopBegin': false,  // 16773: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpWords(0) + stopFinish': false,  // 16774: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpWords(0) + done': false,  // 16775: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpWords(4) + newWordFragment': false,  // 16796: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpWords(4) + progress': false,  // 16797: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpWords(4) + stopBegin': false,  // 16798: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpWords(4) + stopFinish': false,  // 16799: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpWords(4) + done': false,  // 16800: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpSentences(-1) + newWordFragment': false,  // 16896: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpSentences(-1) + progress': false,  // 16897: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpSentences(-1) + stopBegin': false,  // 16898: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpSentences(-1) + stopFinish': false,  // 16899: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpSentences(-1) + done': false,  // 16900: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpSentences(0) + newWordFragment': false,  // 16921: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpSentences(0) + progress': false,  // 16922: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpSentences(0) + stopBegin': false,  // 16923: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpSentences(0) + stopFinish': false,  // 16924: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpSentences(0) + done': false,  // 16925: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpSentences(1) + newWordFragment': false,  // 16946: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpSentences(1) + progress': false,  // 16947: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpSentences(1) + stopBegin': false,  // 16948: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpSentences(1) + stopFinish': false,  // 16949: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpSentences(1) + done': false,  // 16950: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpSentences(3) + newWordFragment': false,  // 16971: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpSentences(3) + progress': false,  // 16972: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpSentences(3) + stopBegin': false,  // 16973: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpSentences(3) + stopFinish': false,  // 16974: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > jumpSentences(3) + done': false,  // 16975: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > nextWord(null) + newWordFragment': false,  // 17021: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > nextWord(null) + progress': false,  // 17022: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > nextWord(null) + stopBegin': false,  // 17023: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > nextWord(null) + stopFinish': false,  // 17024: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > nextWord(null) + done': false,  // 17025: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > nextSentence(null) + newWordFragment': false,  // 17046: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > nextSentence(null) + progress': false,  // 17047: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > nextSentence(null) + stopBegin': false,  // 17048: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > nextSentence(null) + stopFinish': false,  // 17049: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > nextSentence(null) + done': false,  // 17050: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > prevWord(null) + newWordFragment': false,  // 17071: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > prevWord(null) + progress': false,  // 17072: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > prevWord(null) + stopBegin': false,  // 17073: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > prevWord(null) + stopFinish': false,  // 17074: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > prevWord(null) + done': false,  // 17075: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > prevSentence(null) + newWordFragment': false,  // 17096: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > prevSentence(null) + progress': false,  // 17097: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > prevSentence(null) + stopBegin': false,  // 17098: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > prevSentence(null) + stopFinish': false,  // 17099: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopBegin > prevSentence(null) + done': false,  // 17100: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > play(null) + playBegin': false,  // 17176: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > play(null) + playFinish': false,  // 17177: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > play(null) + restartBegin': false,  // 17180: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > play(null) + restartFinish': false,  // 17181: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > toggle(null) + playBegin': false,  // 17201: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > toggle(null) + playFinish': false,  // 17202: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > toggle(null) + restartBegin': false,  // 17205: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > toggle(null) + restartFinish': false,  // 17206: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > rewind(null) + newWordFragment': false,  // 17346: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > rewind(null) + progress': false,  // 17347: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > fastForward(null) + newWordFragment': false,  // 17371: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > fastForward(null) + progress': false,  // 17372: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > once([0,0,-2]) + newWordFragment': false,  // 17396: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > once([0,0,-2]) + progress': false,  // 17397: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > once([0,0,-2]) + stopBegin': false,  // 17398: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > once([0,0,-2]) + stopFinish': false,  // 17399: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > once([0,0,-2]) + done': false,  // 17400: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > once([0,0,2]) + newWordFragment': false,  // 17421: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > once([0,0,2]) + progress': false,  // 17422: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > once([0,0,2]) + stopBegin': false,  // 17423: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > once([0,0,2]) + stopFinish': false,  // 17424: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > once([0,0,2]) + done': false,  // 17425: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > current(null) + newWordFragment': false,  // 17446: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > current(null) + progress': false,  // 17447: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > current(null) + stopBegin': false,  // 17448: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > current(null) + stopFinish': false,  // 17449: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > current(null) + done': false,  // 17450: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpWords(-3) + newWordFragment': false,  // 17621: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpWords(-3) + progress': false,  // 17622: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpWords(-3) + stopBegin': false,  // 17623: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpWords(-3) + stopFinish': false,  // 17624: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpWords(-3) + done': false,  // 17625: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpWords(-1) + newWordFragment': false,  // 17646: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpWords(-1) + progress': false,  // 17647: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpWords(-1) + stopBegin': false,  // 17648: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpWords(-1) + stopFinish': false,  // 17649: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpWords(-1) + done': false,  // 17650: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpWords(0) + newWordFragment': false,  // 17671: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpWords(0) + progress': false,  // 17672: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpWords(0) + stopBegin': false,  // 17673: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpWords(0) + stopFinish': false,  // 17674: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpWords(0) + done': false,  // 17675: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpWords(4) + newWordFragment': false,  // 17696: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpWords(4) + progress': false,  // 17697: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpWords(4) + stopBegin': false,  // 17698: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpWords(4) + stopFinish': false,  // 17699: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpWords(4) + done': false,  // 17700: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpSentences(-1) + newWordFragment': false,  // 17796: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpSentences(-1) + progress': false,  // 17797: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpSentences(-1) + stopBegin': false,  // 17798: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpSentences(-1) + stopFinish': false,  // 17799: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpSentences(-1) + done': false,  // 17800: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpSentences(0) + newWordFragment': false,  // 17821: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpSentences(0) + progress': false,  // 17822: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpSentences(0) + stopBegin': false,  // 17823: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpSentences(0) + stopFinish': false,  // 17824: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpSentences(0) + done': false,  // 17825: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpSentences(1) + newWordFragment': false,  // 17846: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpSentences(1) + progress': false,  // 17847: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpSentences(1) + stopBegin': false,  // 17848: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpSentences(1) + stopFinish': false,  // 17849: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpSentences(1) + done': false,  // 17850: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpSentences(3) + newWordFragment': false,  // 17871: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpSentences(3) + progress': false,  // 17872: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpSentences(3) + stopBegin': false,  // 17873: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpSentences(3) + stopFinish': false,  // 17874: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > jumpSentences(3) + done': false,  // 17875: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > nextWord(null) + newWordFragment': false,  // 17921: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > nextWord(null) + progress': false,  // 17922: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > nextWord(null) + stopBegin': false,  // 17923: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > nextWord(null) + stopFinish': false,  // 17924: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > nextWord(null) + done': false,  // 17925: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > nextSentence(null) + newWordFragment': false,  // 17946: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > nextSentence(null) + progress': false,  // 17947: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > nextSentence(null) + stopBegin': false,  // 17948: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > nextSentence(null) + stopFinish': false,  // 17949: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > nextSentence(null) + done': false,  // 17950: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > prevWord(null) + newWordFragment': false,  // 17971: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > prevWord(null) + progress': false,  // 17972: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > prevWord(null) + stopBegin': false,  // 17973: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > prevWord(null) + stopFinish': false,  // 17974: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > prevWord(null) + done': false,  // 17975: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > prevSentence(null) + newWordFragment': false,  // 17996: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > prevSentence(null) + progress': false,  // 17997: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > prevSentence(null) + stopBegin': false,  // 17998: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > prevSentence(null) + stopFinish': false,  // 17999: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + loopFinish > prevSentence(null) + done': false,  // 18000: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > play(null) + playBegin': false,  // 18076: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > play(null) + playFinish': false,  // 18077: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > play(null) + restartBegin': false,  // 18080: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > play(null) + restartFinish': false,  // 18081: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > toggle(null) + playBegin': false,  // 18101: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > toggle(null) + playFinish': false,  // 18102: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > toggle(null) + restartBegin': false,  // 18105: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > toggle(null) + restartFinish': false,  // 18106: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > rewind(null) + newWordFragment': false,  // 18246: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > rewind(null) + progress': false,  // 18247: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > fastForward(null) + newWordFragment': false,  // 18271: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > fastForward(null) + progress': false,  // 18272: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > once([0,0,-2]) + newWordFragment': false,  // 18296: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > once([0,0,-2]) + progress': false,  // 18297: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > once([0,0,-2]) + stopBegin': false,  // 18298: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > once([0,0,-2]) + stopFinish': false,  // 18299: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > once([0,0,-2]) + done': false,  // 18300: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > once([0,0,2]) + newWordFragment': false,  // 18321: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > once([0,0,2]) + progress': false,  // 18322: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > once([0,0,2]) + stopBegin': false,  // 18323: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > once([0,0,2]) + stopFinish': false,  // 18324: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > once([0,0,2]) + done': false,  // 18325: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > current(null) + newWordFragment': false,  // 18346: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > current(null) + progress': false,  // 18347: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > current(null) + stopBegin': false,  // 18348: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > current(null) + stopFinish': false,  // 18349: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > current(null) + done': false,  // 18350: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpWords(-3) + newWordFragment': false,  // 18521: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpWords(-3) + progress': false,  // 18522: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpWords(-3) + stopBegin': false,  // 18523: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpWords(-3) + stopFinish': false,  // 18524: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpWords(-3) + done': false,  // 18525: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpWords(-1) + newWordFragment': false,  // 18546: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpWords(-1) + progress': false,  // 18547: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpWords(-1) + stopBegin': false,  // 18548: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpWords(-1) + stopFinish': false,  // 18549: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpWords(-1) + done': false,  // 18550: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpWords(0) + newWordFragment': false,  // 18571: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpWords(0) + progress': false,  // 18572: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpWords(0) + stopBegin': false,  // 18573: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpWords(0) + stopFinish': false,  // 18574: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpWords(0) + done': false,  // 18575: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpWords(4) + newWordFragment': false,  // 18596: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpWords(4) + progress': false,  // 18597: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpWords(4) + stopBegin': false,  // 18598: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpWords(4) + stopFinish': false,  // 18599: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpWords(4) + done': false,  // 18600: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpSentences(-1) + newWordFragment': false,  // 18696: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpSentences(-1) + progress': false,  // 18697: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpSentences(-1) + stopBegin': false,  // 18698: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpSentences(-1) + stopFinish': false,  // 18699: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpSentences(-1) + done': false,  // 18700: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpSentences(0) + newWordFragment': false,  // 18721: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpSentences(0) + progress': false,  // 18722: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpSentences(0) + stopBegin': false,  // 18723: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpSentences(0) + stopFinish': false,  // 18724: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpSentences(0) + done': false,  // 18725: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpSentences(1) + newWordFragment': false,  // 18746: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpSentences(1) + progress': false,  // 18747: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpSentences(1) + stopBegin': false,  // 18748: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpSentences(1) + stopFinish': false,  // 18749: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpSentences(1) + done': false,  // 18750: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpSentences(3) + newWordFragment': false,  // 18771: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpSentences(3) + progress': false,  // 18772: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpSentences(3) + stopBegin': false,  // 18773: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpSentences(3) + stopFinish': false,  // 18774: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > jumpSentences(3) + done': false,  // 18775: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > nextWord(null) + newWordFragment': false,  // 18821: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > nextWord(null) + progress': false,  // 18822: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > nextWord(null) + stopBegin': false,  // 18823: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > nextWord(null) + stopFinish': false,  // 18824: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > nextWord(null) + done': false,  // 18825: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > nextSentence(null) + newWordFragment': false,  // 18846: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > nextSentence(null) + progress': false,  // 18847: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > nextSentence(null) + stopBegin': false,  // 18848: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > nextSentence(null) + stopFinish': false,  // 18849: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > nextSentence(null) + done': false,  // 18850: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > prevWord(null) + newWordFragment': false,  // 18871: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > prevWord(null) + progress': false,  // 18872: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > prevWord(null) + stopBegin': false,  // 18873: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > prevWord(null) + stopFinish': false,  // 18874: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > prevWord(null) + done': false,  // 18875: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > prevSentence(null) + newWordFragment': false,  // 18896: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > prevSentence(null) + progress': false,  // 18897: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > prevSentence(null) + stopBegin': false,  // 18898: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > prevSentence(null) + stopFinish': false,  // 18899: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + newWordFragment > prevSentence(null) + done': false,  // 18900: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > play(null) + playBegin': false,  // 18976: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > play(null) + playFinish': false,  // 18977: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > play(null) + restartBegin': false,  // 18980: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > play(null) + restartFinish': false,  // 18981: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > toggle(null) + playBegin': false,  // 19001: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > toggle(null) + playFinish': false,  // 19002: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > toggle(null) + restartBegin': false,  // 19005: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > toggle(null) + restartFinish': false,  // 19006: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > rewind(null) + newWordFragment': false,  // 19146: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > rewind(null) + progress': false,  // 19147: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > fastForward(null) + newWordFragment': false,  // 19171: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > fastForward(null) + progress': false,  // 19172: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > once([0,0,-2]) + newWordFragment': false,  // 19196: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > once([0,0,-2]) + progress': false,  // 19197: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > once([0,0,-2]) + stopBegin': false,  // 19198: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > once([0,0,-2]) + stopFinish': false,  // 19199: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > once([0,0,-2]) + done': false,  // 19200: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > once([0,0,2]) + newWordFragment': false,  // 19221: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > once([0,0,2]) + progress': false,  // 19222: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > once([0,0,2]) + stopBegin': false,  // 19223: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > once([0,0,2]) + stopFinish': false,  // 19224: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > once([0,0,2]) + done': false,  // 19225: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > current(null) + newWordFragment': false,  // 19246: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > current(null) + progress': false,  // 19247: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > current(null) + stopBegin': false,  // 19248: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > current(null) + stopFinish': false,  // 19249: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > current(null) + done': false,  // 19250: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpWords(-3) + newWordFragment': false,  // 19421: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpWords(-3) + progress': false,  // 19422: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpWords(-3) + stopBegin': false,  // 19423: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpWords(-3) + stopFinish': false,  // 19424: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpWords(-3) + done': false,  // 19425: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpWords(-1) + newWordFragment': false,  // 19446: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpWords(-1) + progress': false,  // 19447: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpWords(-1) + stopBegin': false,  // 19448: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpWords(-1) + stopFinish': false,  // 19449: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpWords(-1) + done': false,  // 19450: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpWords(0) + newWordFragment': false,  // 19471: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpWords(0) + progress': false,  // 19472: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpWords(0) + stopBegin': false,  // 19473: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpWords(0) + stopFinish': false,  // 19474: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpWords(0) + done': false,  // 19475: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpWords(4) + newWordFragment': false,  // 19496: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpWords(4) + progress': false,  // 19497: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpWords(4) + stopBegin': false,  // 19498: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpWords(4) + stopFinish': false,  // 19499: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpWords(4) + done': false,  // 19500: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpSentences(-1) + newWordFragment': false,  // 19596: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpSentences(-1) + progress': false,  // 19597: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpSentences(-1) + stopBegin': false,  // 19598: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpSentences(-1) + stopFinish': false,  // 19599: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpSentences(-1) + done': false,  // 19600: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpSentences(0) + newWordFragment': false,  // 19621: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpSentences(0) + progress': false,  // 19622: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpSentences(0) + stopBegin': false,  // 19623: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpSentences(0) + stopFinish': false,  // 19624: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpSentences(0) + done': false,  // 19625: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpSentences(1) + newWordFragment': false,  // 19646: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpSentences(1) + progress': false,  // 19647: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpSentences(1) + stopBegin': false,  // 19648: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpSentences(1) + stopFinish': false,  // 19649: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpSentences(1) + done': false,  // 19650: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpSentences(3) + newWordFragment': false,  // 19671: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpSentences(3) + progress': false,  // 19672: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpSentences(3) + stopBegin': false,  // 19673: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpSentences(3) + stopFinish': false,  // 19674: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > jumpSentences(3) + done': false,  // 19675: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > nextWord(null) + newWordFragment': false,  // 19721: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > nextWord(null) + progress': false,  // 19722: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > nextWord(null) + stopBegin': false,  // 19723: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > nextWord(null) + stopFinish': false,  // 19724: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > nextWord(null) + done': false,  // 19725: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > nextSentence(null) + newWordFragment': false,  // 19746: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > nextSentence(null) + progress': false,  // 19747: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > nextSentence(null) + stopBegin': false,  // 19748: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > nextSentence(null) + stopFinish': false,  // 19749: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > nextSentence(null) + done': false,  // 19750: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > prevWord(null) + newWordFragment': false,  // 19771: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > prevWord(null) + progress': false,  // 19772: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > prevWord(null) + stopBegin': false,  // 19773: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > prevWord(null) + stopFinish': false,  // 19774: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > prevWord(null) + done': false,  // 19775: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > prevSentence(null) + newWordFragment': false,  // 19796: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > prevSentence(null) + progress': false,  // 19797: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > prevSentence(null) + stopBegin': false,  // 19798: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > prevSentence(null) + stopFinish': false,  // 19799: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + progress > prevSentence(null) + done': false,  // 19800: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > play(null) + playBegin': false,  // 19876: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > play(null) + playFinish': false,  // 19877: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > play(null) + restartBegin': false,  // 19880: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > play(null) + restartFinish': false,  // 19881: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > toggle(null) + playBegin': false,  // 19901: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > toggle(null) + playFinish': false,  // 19902: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > toggle(null) + restartBegin': false,  // 19905: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > toggle(null) + restartFinish': false,  // 19906: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > rewind(null) + newWordFragment': false,  // 20046: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > rewind(null) + progress': false,  // 20047: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > fastForward(null) + newWordFragment': false,  // 20071: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > fastForward(null) + progress': false,  // 20072: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > once([0,0,-2]) + newWordFragment': false,  // 20096: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > once([0,0,-2]) + progress': false,  // 20097: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > once([0,0,-2]) + stopBegin': false,  // 20098: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > once([0,0,-2]) + stopFinish': false,  // 20099: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > once([0,0,-2]) + done': false,  // 20100: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > once([0,0,2]) + newWordFragment': false,  // 20121: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > once([0,0,2]) + progress': false,  // 20122: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > once([0,0,2]) + stopBegin': false,  // 20123: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > once([0,0,2]) + stopFinish': false,  // 20124: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > once([0,0,2]) + done': false,  // 20125: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > current(null) + newWordFragment': false,  // 20146: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > current(null) + progress': false,  // 20147: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > current(null) + stopBegin': false,  // 20148: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > current(null) + stopFinish': false,  // 20149: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > current(null) + done': false,  // 20150: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpWords(-3) + newWordFragment': false,  // 20321: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpWords(-3) + progress': false,  // 20322: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpWords(-3) + stopBegin': false,  // 20323: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpWords(-3) + stopFinish': false,  // 20324: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpWords(-3) + done': false,  // 20325: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpWords(-1) + newWordFragment': false,  // 20346: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpWords(-1) + progress': false,  // 20347: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpWords(-1) + stopBegin': false,  // 20348: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpWords(-1) + stopFinish': false,  // 20349: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpWords(-1) + done': false,  // 20350: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpWords(0) + newWordFragment': false,  // 20371: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpWords(0) + progress': false,  // 20372: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpWords(0) + stopBegin': false,  // 20373: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpWords(0) + stopFinish': false,  // 20374: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpWords(0) + done': false,  // 20375: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpWords(4) + newWordFragment': false,  // 20396: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpWords(4) + progress': false,  // 20397: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpWords(4) + stopBegin': false,  // 20398: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpWords(4) + stopFinish': false,  // 20399: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpWords(4) + done': false,  // 20400: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpSentences(-1) + newWordFragment': false,  // 20496: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpSentences(-1) + progress': false,  // 20497: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpSentences(-1) + stopBegin': false,  // 20498: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpSentences(-1) + stopFinish': false,  // 20499: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpSentences(-1) + done': false,  // 20500: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpSentences(0) + newWordFragment': false,  // 20521: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpSentences(0) + progress': false,  // 20522: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpSentences(0) + stopBegin': false,  // 20523: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpSentences(0) + stopFinish': false,  // 20524: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpSentences(0) + done': false,  // 20525: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpSentences(1) + newWordFragment': false,  // 20546: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpSentences(1) + progress': false,  // 20547: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpSentences(1) + stopBegin': false,  // 20548: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpSentences(1) + stopFinish': false,  // 20549: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpSentences(1) + done': false,  // 20550: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpSentences(3) + newWordFragment': false,  // 20571: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpSentences(3) + progress': false,  // 20572: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpSentences(3) + stopBegin': false,  // 20573: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpSentences(3) + stopFinish': false,  // 20574: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > jumpSentences(3) + done': false,  // 20575: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > nextWord(null) + newWordFragment': false,  // 20621: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > nextWord(null) + progress': false,  // 20622: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > nextWord(null) + stopBegin': false,  // 20623: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > nextWord(null) + stopFinish': false,  // 20624: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > nextWord(null) + done': false,  // 20625: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > nextSentence(null) + newWordFragment': false,  // 20646: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > nextSentence(null) + progress': false,  // 20647: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > nextSentence(null) + stopBegin': false,  // 20648: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > nextSentence(null) + stopFinish': false,  // 20649: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > nextSentence(null) + done': false,  // 20650: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > prevWord(null) + newWordFragment': false,  // 20671: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > prevWord(null) + progress': false,  // 20672: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > prevWord(null) + stopBegin': false,  // 20673: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > prevWord(null) + stopFinish': false,  // 20674: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > prevWord(null) + done': false,  // 20675: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > prevSentence(null) + newWordFragment': false,  // 20696: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > prevSentence(null) + progress': false,  // 20697: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > prevSentence(null) + stopBegin': false,  // 20698: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > prevSentence(null) + stopFinish': false,  // 20699: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopBegin > prevSentence(null) + done': false,  // 20700: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > play(null) + playBegin': false,  // 20776: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > play(null) + playFinish': false,  // 20777: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > play(null) + restartBegin': false,  // 20780: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > play(null) + restartFinish': false,  // 20781: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > toggle(null) + playBegin': false,  // 20801: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > toggle(null) + playFinish': false,  // 20802: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > toggle(null) + restartBegin': false,  // 20805: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > toggle(null) + restartFinish': false,  // 20806: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > rewind(null) + newWordFragment': false,  // 20946: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > rewind(null) + progress': false,  // 20947: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > fastForward(null) + newWordFragment': false,  // 20971: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > fastForward(null) + progress': false,  // 20972: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > once([0,0,-2]) + newWordFragment': false,  // 20996: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > once([0,0,-2]) + progress': false,  // 20997: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > once([0,0,-2]) + stopBegin': false,  // 20998: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > once([0,0,-2]) + stopFinish': false,  // 20999: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > once([0,0,-2]) + done': false,  // 21000: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > once([0,0,2]) + newWordFragment': false,  // 21021: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > once([0,0,2]) + progress': false,  // 21022: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > once([0,0,2]) + stopBegin': false,  // 21023: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > once([0,0,2]) + stopFinish': false,  // 21024: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > once([0,0,2]) + done': false,  // 21025: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > current(null) + newWordFragment': false,  // 21046: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > current(null) + progress': false,  // 21047: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > current(null) + stopBegin': false,  // 21048: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > current(null) + stopFinish': false,  // 21049: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > current(null) + done': false,  // 21050: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpWords(-3) + newWordFragment': false,  // 21221: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpWords(-3) + progress': false,  // 21222: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpWords(-3) + stopBegin': false,  // 21223: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpWords(-3) + stopFinish': false,  // 21224: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpWords(-3) + done': false,  // 21225: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpWords(-1) + newWordFragment': false,  // 21246: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpWords(-1) + progress': false,  // 21247: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpWords(-1) + stopBegin': false,  // 21248: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpWords(-1) + stopFinish': false,  // 21249: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpWords(-1) + done': false,  // 21250: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpWords(0) + newWordFragment': false,  // 21271: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpWords(0) + progress': false,  // 21272: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpWords(0) + stopBegin': false,  // 21273: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpWords(0) + stopFinish': false,  // 21274: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpWords(0) + done': false,  // 21275: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpWords(4) + newWordFragment': false,  // 21296: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpWords(4) + progress': false,  // 21297: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpWords(4) + stopBegin': false,  // 21298: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpWords(4) + stopFinish': false,  // 21299: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpWords(4) + done': false,  // 21300: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpSentences(-1) + newWordFragment': false,  // 21396: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpSentences(-1) + progress': false,  // 21397: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpSentences(-1) + stopBegin': false,  // 21398: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpSentences(-1) + stopFinish': false,  // 21399: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpSentences(-1) + done': false,  // 21400: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpSentences(0) + newWordFragment': false,  // 21421: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpSentences(0) + progress': false,  // 21422: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpSentences(0) + stopBegin': false,  // 21423: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpSentences(0) + stopFinish': false,  // 21424: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpSentences(0) + done': false,  // 21425: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpSentences(1) + newWordFragment': false,  // 21446: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpSentences(1) + progress': false,  // 21447: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpSentences(1) + stopBegin': false,  // 21448: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpSentences(1) + stopFinish': false,  // 21449: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpSentences(1) + done': false,  // 21450: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpSentences(3) + newWordFragment': false,  // 21471: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpSentences(3) + progress': false,  // 21472: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpSentences(3) + stopBegin': false,  // 21473: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpSentences(3) + stopFinish': false,  // 21474: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > jumpSentences(3) + done': false,  // 21475: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > nextWord(null) + newWordFragment': false,  // 21521: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > nextWord(null) + progress': false,  // 21522: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > nextWord(null) + stopBegin': false,  // 21523: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > nextWord(null) + stopFinish': false,  // 21524: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > nextWord(null) + done': false,  // 21525: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > nextSentence(null) + newWordFragment': false,  // 21546: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > nextSentence(null) + progress': false,  // 21547: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > nextSentence(null) + stopBegin': false,  // 21548: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > nextSentence(null) + stopFinish': false,  // 21549: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > nextSentence(null) + done': false,  // 21550: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > prevWord(null) + newWordFragment': false,  // 21571: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > prevWord(null) + progress': false,  // 21572: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > prevWord(null) + stopBegin': false,  // 21573: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > prevWord(null) + stopFinish': false,  // 21574: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > prevWord(null) + done': false,  // 21575: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > prevSentence(null) + newWordFragment': false,  // 21596: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > prevSentence(null) + progress': false,  // 21597: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > prevSentence(null) + stopBegin': false,  // 21598: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > prevSentence(null) + stopFinish': false,  // 21599: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + stopFinish > prevSentence(null) + done': false,  // 21600: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > play(null) + playBegin': false,  // 21676: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > play(null) + playFinish': false,  // 21677: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > play(null) + restartBegin': false,  // 21680: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > play(null) + restartFinish': false,  // 21681: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > toggle(null) + playBegin': false,  // 21701: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > toggle(null) + playFinish': false,  // 21702: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > toggle(null) + restartBegin': false,  // 21705: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > toggle(null) + restartFinish': false,  // 21706: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > rewind(null) + newWordFragment': false,  // 21846: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > rewind(null) + progress': false,  // 21847: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > fastForward(null) + newWordFragment': false,  // 21871: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > fastForward(null) + progress': false,  // 21872: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > once([0,0,-2]) + newWordFragment': false,  // 21896: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > once([0,0,-2]) + progress': false,  // 21897: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > once([0,0,-2]) + stopBegin': false,  // 21898: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > once([0,0,-2]) + stopFinish': false,  // 21899: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > once([0,0,-2]) + done': false,  // 21900: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > once([0,0,2]) + newWordFragment': false,  // 21921: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > once([0,0,2]) + progress': false,  // 21922: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > once([0,0,2]) + stopBegin': false,  // 21923: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > once([0,0,2]) + stopFinish': false,  // 21924: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > once([0,0,2]) + done': false,  // 21925: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > current(null) + newWordFragment': false,  // 21946: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > current(null) + progress': false,  // 21947: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > current(null) + stopBegin': false,  // 21948: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > current(null) + stopFinish': false,  // 21949: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > current(null) + done': false,  // 21950: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpWords(-3) + newWordFragment': false,  // 22121: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpWords(-3) + progress': false,  // 22122: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpWords(-3) + stopBegin': false,  // 22123: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpWords(-3) + stopFinish': false,  // 22124: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpWords(-3) + done': false,  // 22125: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpWords(-1) + newWordFragment': false,  // 22146: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpWords(-1) + progress': false,  // 22147: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpWords(-1) + stopBegin': false,  // 22148: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpWords(-1) + stopFinish': false,  // 22149: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpWords(-1) + done': false,  // 22150: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpWords(0) + newWordFragment': false,  // 22171: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpWords(0) + progress': false,  // 22172: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpWords(0) + stopBegin': false,  // 22173: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpWords(0) + stopFinish': false,  // 22174: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpWords(0) + done': false,  // 22175: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpWords(4) + newWordFragment': false,  // 22196: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpWords(4) + progress': false,  // 22197: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpWords(4) + stopBegin': false,  // 22198: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpWords(4) + stopFinish': false,  // 22199: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpWords(4) + done': false,  // 22200: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpSentences(-1) + newWordFragment': false,  // 22296: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpSentences(-1) + progress': false,  // 22297: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpSentences(-1) + stopBegin': false,  // 22298: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpSentences(-1) + stopFinish': false,  // 22299: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpSentences(-1) + done': false,  // 22300: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpSentences(0) + newWordFragment': false,  // 22321: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpSentences(0) + progress': false,  // 22322: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpSentences(0) + stopBegin': false,  // 22323: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpSentences(0) + stopFinish': false,  // 22324: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpSentences(0) + done': false,  // 22325: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpSentences(1) + newWordFragment': false,  // 22346: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpSentences(1) + progress': false,  // 22347: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpSentences(1) + stopBegin': false,  // 22348: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpSentences(1) + stopFinish': false,  // 22349: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpSentences(1) + done': false,  // 22350: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpSentences(3) + newWordFragment': false,  // 22371: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpSentences(3) + progress': false,  // 22372: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpSentences(3) + stopBegin': false,  // 22373: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpSentences(3) + stopFinish': false,  // 22374: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > jumpSentences(3) + done': false,  // 22375: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > nextWord(null) + newWordFragment': false,  // 22421: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > nextWord(null) + progress': false,  // 22422: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > nextWord(null) + stopBegin': false,  // 22423: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > nextWord(null) + stopFinish': false,  // 22424: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > nextWord(null) + done': false,  // 22425: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > nextSentence(null) + newWordFragment': false,  // 22446: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > nextSentence(null) + progress': false,  // 22447: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > nextSentence(null) + stopBegin': false,  // 22448: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > nextSentence(null) + stopFinish': false,  // 22449: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > nextSentence(null) + done': false,  // 22450: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > prevWord(null) + newWordFragment': false,  // 22471: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > prevWord(null) + progress': false,  // 22472: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > prevWord(null) + stopBegin': false,  // 22473: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > prevWord(null) + stopFinish': false,  // 22474: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > prevWord(null) + done': false,  // 22475: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > prevSentence(null) + newWordFragment': false,  // 22496: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > prevSentence(null) + progress': false,  // 22497: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > prevSentence(null) + stopBegin': false,  // 22498: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > prevSentence(null) + stopFinish': false,  // 22499: ?? (skip results are different than combos)
		'skip-combos: jumpWords(100) + done > prevSentence(null) + done': false,  // 22500: ?? (skip results are different than combos)



		// ============== jumpSentences(-3) ==============
		// #? like prev
		'skip-combos: jumpSentences(-3) + onceBegin > play(null) + playBegin': true,  // 9076: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + onceBegin > play(null) + playFinish': true,  // 9077: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + onceBegin > play(null) + restartBegin': true,  // 9080: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + onceBegin > play(null) + restartFinish': true,  // 9081: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + onceBegin > toggle(null) + playBegin': true,  // 9101: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + onceBegin > toggle(null) + playFinish': true,  // 9102: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + onceBegin > toggle(null) + restartBegin': true,  // 9105: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + onceBegin > toggle(null) + restartFinish': true,  // 9106: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + onceFinish > play(null) + playBegin': true,  // 9976: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + onceFinish > play(null) + playFinish': true,  // 9977: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + onceFinish > play(null) + restartBegin': true,  // 9980: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + onceFinish > play(null) + restartFinish': true,  // 9981: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + onceFinish > toggle(null) + playBegin': true,  // 10001: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + onceFinish > toggle(null) + playFinish': true,  // 10002: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + onceFinish > toggle(null) + restartBegin': true,  // 10005: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + onceFinish > toggle(null) + restartFinish': true,  // 10006: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + loopBegin > play(null) + playBegin': true,  // 16276: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + loopBegin > play(null) + playFinish': true,  // 16277: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + loopBegin > play(null) + restartBegin': true,  // 16280: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + loopBegin > play(null) + restartFinish': true,  // 16281: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + loopBegin > toggle(null) + playBegin': true,  // 16301: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + loopBegin > toggle(null) + playFinish': true,  // 16302: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + loopBegin > toggle(null) + restartBegin': true,  // 16305: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + loopBegin > toggle(null) + restartFinish': true,  // 16306: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + loopFinish > play(null) + playBegin': true,  // 17176: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + loopFinish > play(null) + playFinish': true,  // 17177: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + loopFinish > play(null) + restartBegin': true,  // 17180: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + loopFinish > play(null) + restartFinish': true,  // 17181: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + loopFinish > toggle(null) + playBegin': true,  // 17201: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + loopFinish > toggle(null) + playFinish': true,  // 17202: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + loopFinish > toggle(null) + restartBegin': true,  // 17205: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + loopFinish > toggle(null) + restartFinish': true,  // 17206: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + newWordFragment > play(null) + playBegin': true,  // 18076: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + newWordFragment > play(null) + playFinish': true,  // 18077: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + newWordFragment > play(null) + restartBegin': true,  // 18080: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + newWordFragment > play(null) + restartFinish': true,  // 18081: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + newWordFragment > toggle(null) + playBegin': true,  // 18101: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + newWordFragment > toggle(null) + playFinish': true,  // 18102: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + newWordFragment > toggle(null) + restartBegin': true,  // 18105: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + newWordFragment > toggle(null) + restartFinish': true,  // 18106: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + progress > play(null) + playBegin': true,  // 18976: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + progress > play(null) + playFinish': true,  // 18977: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + progress > play(null) + restartBegin': true,  // 18980: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + progress > play(null) + restartFinish': true,  // 18981: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + progress > toggle(null) + playBegin': true,  // 19001: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + progress > toggle(null) + playFinish': true,  // 19002: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + progress > toggle(null) + restartBegin': true,  // 19005: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + progress > toggle(null) + restartFinish': true,  // 19006: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + stopBegin > play(null) + playBegin': true,  // 19876: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + stopBegin > play(null) + playFinish': true,  // 19877: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + stopBegin > play(null) + restartBegin': true,  // 19880: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + stopBegin > play(null) + restartFinish': true,  // 19881: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + stopBegin > toggle(null) + playBegin': true,  // 19901: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + stopBegin > toggle(null) + playFinish': true,  // 19902: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + stopBegin > toggle(null) + restartBegin': true,  // 19905: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + stopBegin > toggle(null) + restartFinish': true,  // 19906: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + stopFinish > play(null) + playBegin': true,  // 20776: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + stopFinish > play(null) + playFinish': true,  // 20777: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + stopFinish > play(null) + restartBegin': true,  // 20780: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + stopFinish > play(null) + restartFinish': true,  // 20781: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + stopFinish > toggle(null) + playBegin': true,  // 20801: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + stopFinish > toggle(null) + playFinish': true,  // 20802: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + stopFinish > toggle(null) + restartBegin': true,  // 20805: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + stopFinish > toggle(null) + restartFinish': true,  // 20806: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + done > play(null) + playBegin': true,  // 21676: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + done > play(null) + playFinish': true,  // 21677: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + done > play(null) + restartBegin': true,  // 21680: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + done > play(null) + restartFinish': true,  // 21681: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + done > toggle(null) + playBegin': true,  // 21701: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + done > toggle(null) + playFinish': true,  // 21702: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + done > toggle(null) + restartBegin': true,  // 21705: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-3) + done > toggle(null) + restartFinish': true,  // 21706: ?? (skip results are different than combos)



		// ============== jumpSentences(-1) ==============
		// #? like prev
		'skip-combos: jumpSentences(-1) + onceBegin > play(null) + playBegin': true,  // 9076: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + onceBegin > play(null) + playFinish': true,  // 9077: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + onceBegin > play(null) + restartBegin': true,  // 9080: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + onceBegin > play(null) + restartFinish': true,  // 9081: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + onceBegin > toggle(null) + playBegin': true,  // 9101: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + onceBegin > toggle(null) + playFinish': true,  // 9102: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + onceBegin > toggle(null) + restartBegin': true,  // 9105: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + onceBegin > toggle(null) + restartFinish': true,  // 9106: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + onceFinish > play(null) + playBegin': true,  // 9976: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + onceFinish > play(null) + playFinish': true,  // 9977: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + onceFinish > play(null) + restartBegin': true,  // 9980: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + onceFinish > play(null) + restartFinish': true,  // 9981: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + onceFinish > toggle(null) + playBegin': true,  // 10001: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + onceFinish > toggle(null) + playFinish': true,  // 10002: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + onceFinish > toggle(null) + restartBegin': true,  // 10005: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + onceFinish > toggle(null) + restartFinish': true,  // 10006: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + loopBegin > play(null) + playBegin': true,  // 16276: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + loopBegin > play(null) + playFinish': true,  // 16277: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + loopBegin > play(null) + restartBegin': true,  // 16280: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + loopBegin > play(null) + restartFinish': true,  // 16281: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + loopBegin > toggle(null) + playBegin': true,  // 16301: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + loopBegin > toggle(null) + playFinish': true,  // 16302: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + loopBegin > toggle(null) + restartBegin': true,  // 16305: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + loopBegin > toggle(null) + restartFinish': true,  // 16306: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + loopFinish > play(null) + playBegin': true,  // 17176: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + loopFinish > play(null) + playFinish': true,  // 17177: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + loopFinish > play(null) + restartBegin': true,  // 17180: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + loopFinish > play(null) + restartFinish': true,  // 17181: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + loopFinish > toggle(null) + playBegin': true,  // 17201: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + loopFinish > toggle(null) + playFinish': true,  // 17202: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + loopFinish > toggle(null) + restartBegin': true,  // 17205: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + loopFinish > toggle(null) + restartFinish': true,  // 17206: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + newWordFragment > play(null) + playBegin': true,  // 18076: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + newWordFragment > play(null) + playFinish': true,  // 18077: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + newWordFragment > play(null) + restartBegin': true,  // 18080: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + newWordFragment > play(null) + restartFinish': true,  // 18081: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + newWordFragment > toggle(null) + playBegin': true,  // 18101: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + newWordFragment > toggle(null) + playFinish': true,  // 18102: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + newWordFragment > toggle(null) + restartBegin': true,  // 18105: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + newWordFragment > toggle(null) + restartFinish': true,  // 18106: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + progress > play(null) + playBegin': true,  // 18976: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + progress > play(null) + playFinish': true,  // 18977: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + progress > play(null) + restartBegin': true,  // 18980: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + progress > play(null) + restartFinish': true,  // 18981: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + progress > toggle(null) + playBegin': true,  // 19001: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + progress > toggle(null) + playFinish': true,  // 19002: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + progress > toggle(null) + restartBegin': true,  // 19005: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + progress > toggle(null) + restartFinish': true,  // 19006: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + stopBegin > play(null) + playBegin': true,  // 19876: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + stopBegin > play(null) + playFinish': true,  // 19877: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + stopBegin > play(null) + restartBegin': true,  // 19880: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + stopBegin > play(null) + restartFinish': true,  // 19881: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + stopBegin > toggle(null) + playBegin': true,  // 19901: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + stopBegin > toggle(null) + playFinish': true,  // 19902: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + stopBegin > toggle(null) + restartBegin': true,  // 19905: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + stopBegin > toggle(null) + restartFinish': true,  // 19906: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + stopFinish > play(null) + playBegin': true,  // 20776: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + stopFinish > play(null) + playFinish': true,  // 20777: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + stopFinish > play(null) + restartBegin': true,  // 20780: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + stopFinish > play(null) + restartFinish': true,  // 20781: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + stopFinish > toggle(null) + playBegin': true,  // 20801: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + stopFinish > toggle(null) + playFinish': true,  // 20802: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + stopFinish > toggle(null) + restartBegin': true,  // 20805: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + stopFinish > toggle(null) + restartFinish': true,  // 20806: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + done > play(null) + playBegin': true,  // 21676: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + done > play(null) + playFinish': true,  // 21677: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + done > play(null) + restartBegin': true,  // 21680: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + done > play(null) + restartFinish': true,  // 21681: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + done > toggle(null) + playBegin': true,  // 21701: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + done > toggle(null) + playFinish': true,  // 21702: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + done > toggle(null) + restartBegin': true,  // 21705: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(-1) + done > toggle(null) + restartFinish': true,  // 21706: ?? (skip results are different than combos)



		// ============== jumpSentences(0) ==============
		// #? jumpSentences(0) reverts to 'pause', so toggle = 'revert*|pause*' stuff not 'play*|...' stuff
		// Like current
		'skip-combos: jumpSentences(0) + onceBegin > toggle(null) + playBegin': true,  // 9101: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + onceBegin > toggle(null) + playFinish': true,  // 9102: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + onceBegin > toggle(null) + pauseBegin': true,  // 9107: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + onceBegin > toggle(null) + pauseFinish': true,  // 9108: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + onceBegin > toggle(null) + revertBegin': true,  // 9113: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + onceBegin > toggle(null) + revertFinish': true,  // 9114: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + onceBegin > toggle(null) + loopBegin': true,  // 9119: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + onceBegin > toggle(null) + loopFinish': true,  // 9120: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + onceBegin > toggle(null) + newWordFragment': true,  // 9121: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + onceBegin > toggle(null) + progress': true,  // 9122: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + onceBegin > toggle(null) + stopBegin': true,  // 9123: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + onceBegin > toggle(null) + stopFinish': true,  // 9124: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + onceBegin > toggle(null) + done': true,  // 9125: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + onceFinish > toggle(null) + playBegin': true,  // 10001: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + onceFinish > toggle(null) + playFinish': true,  // 10002: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + onceFinish > toggle(null) + pauseBegin': true,  // 10007: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + onceFinish > toggle(null) + pauseFinish': true,  // 10008: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + onceFinish > toggle(null) + revertBegin': true,  // 10013: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + onceFinish > toggle(null) + revertFinish': true,  // 10014: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + onceFinish > toggle(null) + loopBegin': true,  // 10019: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + onceFinish > toggle(null) + loopFinish': true,  // 10020: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + onceFinish > toggle(null) + newWordFragment': true,  // 10021: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + onceFinish > toggle(null) + progress': true,  // 10022: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + onceFinish > toggle(null) + stopBegin': true,  // 10023: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + onceFinish > toggle(null) + stopFinish': true,  // 10024: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + onceFinish > toggle(null) + done': true,  // 10025: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + loopBegin > toggle(null) + playBegin': true,  // 16301: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + loopBegin > toggle(null) + playFinish': true,  // 16302: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + loopBegin > toggle(null) + pauseBegin': true,  // 16307: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + loopBegin > toggle(null) + pauseFinish': true,  // 16308: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + loopBegin > toggle(null) + revertBegin': true,  // 16313: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + loopBegin > toggle(null) + revertFinish': true,  // 16314: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + loopBegin > toggle(null) + loopBegin': true,  // 16319: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + loopBegin > toggle(null) + loopFinish': true,  // 16320: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + loopBegin > toggle(null) + newWordFragment': true,  // 16321: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + loopBegin > toggle(null) + progress': true,  // 16322: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + loopBegin > toggle(null) + stopBegin': true,  // 16323: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + loopBegin > toggle(null) + stopFinish': true,  // 16324: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + loopBegin > toggle(null) + done': true,  // 16325: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + loopFinish > toggle(null) + playBegin': true,  // 17201: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + loopFinish > toggle(null) + playFinish': true,  // 17202: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + loopFinish > toggle(null) + pauseBegin': true,  // 17207: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + loopFinish > toggle(null) + pauseFinish': true,  // 17208: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + loopFinish > toggle(null) + revertBegin': true,  // 17213: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + loopFinish > toggle(null) + revertFinish': true,  // 17214: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + loopFinish > toggle(null) + loopBegin': true,  // 17219: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + loopFinish > toggle(null) + loopFinish': true,  // 17220: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + loopFinish > toggle(null) + newWordFragment': true,  // 17221: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + loopFinish > toggle(null) + progress': true,  // 17222: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + loopFinish > toggle(null) + stopBegin': true,  // 17223: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + loopFinish > toggle(null) + stopFinish': true,  // 17224: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + loopFinish > toggle(null) + done': true,  // 17225: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + newWordFragment > toggle(null) + playBegin': true,  // 18101: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + newWordFragment > toggle(null) + playFinish': true,  // 18102: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + newWordFragment > toggle(null) + pauseBegin': true,  // 18107: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + newWordFragment > toggle(null) + pauseFinish': true,  // 18108: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + newWordFragment > toggle(null) + revertBegin': true,  // 18113: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + newWordFragment > toggle(null) + revertFinish': true,  // 18114: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + newWordFragment > toggle(null) + loopBegin': true,  // 18119: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + newWordFragment > toggle(null) + loopFinish': true,  // 18120: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + newWordFragment > toggle(null) + newWordFragment': true,  // 18121: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + newWordFragment > toggle(null) + progress': true,  // 18122: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + newWordFragment > toggle(null) + stopBegin': true,  // 18123: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + newWordFragment > toggle(null) + stopFinish': true,  // 18124: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + newWordFragment > toggle(null) + done': true,  // 18125: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + progress > toggle(null) + playBegin': true,  // 19001: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + progress > toggle(null) + playFinish': true,  // 19002: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + progress > toggle(null) + pauseBegin': true,  // 19007: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + progress > toggle(null) + pauseFinish': true,  // 19008: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + progress > toggle(null) + revertBegin': true,  // 19013: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + progress > toggle(null) + revertFinish': true,  // 19014: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + progress > toggle(null) + loopBegin': true,  // 19019: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + progress > toggle(null) + loopFinish': true,  // 19020: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + progress > toggle(null) + newWordFragment': true,  // 19021: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + progress > toggle(null) + progress': true,  // 19022: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + progress > toggle(null) + stopBegin': true,  // 19023: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + progress > toggle(null) + stopFinish': true,  // 19024: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(0) + progress > toggle(null) + done': true,  // 19025: ?? (skip results are different than combos)



		// ============== jumpSentences(1) ==============
		// #? like jumpTo(6)? Except that one also changes prevSentence and jumpSentences(-1), because they have different output for that one.
		// #? Also will be (exactly?) like nextSentence()
		'skip-combos: jumpSentences(1) + onceBegin > play(null) + newWordFragment': false,  // 9096: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > play(null) + progress': false,  // 9097: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > toggle(null) + playBegin': false,  // 9101: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > toggle(null) + playFinish': false,  // 9102: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > toggle(null) + pauseBegin': false,  // 9107: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > toggle(null) + pauseFinish': false,  // 9108: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > toggle(null) + revertBegin': false,  // 9113: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > toggle(null) + revertFinish': false,  // 9114: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > toggle(null) + loopBegin': false,  // 9119: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > toggle(null) + loopFinish': false,  // 9120: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > toggle(null) + newWordFragment': false,  // 9121: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > toggle(null) + progress': false,  // 9122: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > toggle(null) + stopBegin': false,  // 9123: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > toggle(null) + stopFinish': false,  // 9124: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > toggle(null) + done': false,  // 9125: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > rewind(null) + newWordFragment': false,  // 9246: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > rewind(null) + progress': false,  // 9247: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > fastForward(null) + newWordFragment': false,  // 9271: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > fastForward(null) + progress': false,  // 9272: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > once([0,0,-2]) + newWordFragment': false,  // 9296: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > once([0,0,-2]) + progress': false,  // 9297: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > once([0,0,-2]) + stopBegin': false,  // 9298: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > once([0,0,-2]) + stopFinish': false,  // 9299: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > once([0,0,-2]) + done': false,  // 9300: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > once([0,0,2]) + newWordFragment': false,  // 9321: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > once([0,0,2]) + progress': false,  // 9322: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > current(null) + newWordFragment': false,  // 9346: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > current(null) + progress': false,  // 9347: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > jumpWords(-3) + newWordFragment': false,  // 9521: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > jumpWords(-3) + progress': false,  // 9522: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > jumpWords(-3) + stopBegin': false,  // 9523: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > jumpWords(-3) + stopFinish': false,  // 9524: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > jumpWords(-3) + done': false,  // 9525: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > jumpWords(-1) + newWordFragment': false,  // 9546: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > jumpWords(-1) + progress': false,  // 9547: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > jumpWords(-1) + stopBegin': false,  // 9548: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > jumpWords(-1) + stopFinish': false,  // 9549: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > jumpWords(-1) + done': false,  // 9550: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > jumpWords(0) + newWordFragment': false,  // 9571: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > jumpWords(0) + progress': false,  // 9572: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > jumpWords(4) + newWordFragment': false,  // 9596: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > jumpWords(4) + progress': false,  // 9597: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > jumpSentences(0) + newWordFragment': false,  // 9721: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > jumpSentences(0) + progress': false,  // 9722: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > jumpSentences(1) + newWordFragment': false,  // 9746: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > jumpSentences(1) + progress': false,  // 9747: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > jumpSentences(3) + newWordFragment': false,  // 9771: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > jumpSentences(3) + progress': false,  // 9772: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > jumpSentences(3) + stopBegin': false,  // 9773: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > jumpSentences(3) + stopFinish': false,  // 9774: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > jumpSentences(3) + done': false,  // 9775: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > nextWord(null) + newWordFragment': false,  // 9821: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > nextWord(null) + progress': false,  // 9822: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > nextSentence(null) + newWordFragment': false,  // 9846: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > nextSentence(null) + progress': false,  // 9847: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > prevWord(null) + newWordFragment': false,  // 9871: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > prevWord(null) + progress': false,  // 9872: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > prevWord(null) + stopBegin': false,  // 9873: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > prevWord(null) + stopFinish': false,  // 9874: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceBegin > prevWord(null) + done': false,  // 9875: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > play(null) + newWordFragment': false,  // 9996: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > play(null) + progress': false,  // 9997: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > toggle(null) + playBegin': false,  // 10001: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > toggle(null) + playFinish': false,  // 10002: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > toggle(null) + pauseBegin': false,  // 10007: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > toggle(null) + pauseFinish': false,  // 10008: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > toggle(null) + revertBegin': false,  // 10013: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > toggle(null) + revertFinish': false,  // 10014: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > toggle(null) + loopBegin': false,  // 10019: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > toggle(null) + loopFinish': false,  // 10020: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > toggle(null) + newWordFragment': false,  // 10021: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > toggle(null) + progress': false,  // 10022: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > toggle(null) + stopBegin': false,  // 10023: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > toggle(null) + stopFinish': false,  // 10024: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > toggle(null) + done': false,  // 10025: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > rewind(null) + newWordFragment': false,  // 10146: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > rewind(null) + progress': false,  // 10147: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > fastForward(null) + newWordFragment': false,  // 10171: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > fastForward(null) + progress': false,  // 10172: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > once([0,0,-2]) + newWordFragment': false,  // 10196: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > once([0,0,-2]) + progress': false,  // 10197: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > once([0,0,-2]) + stopBegin': false,  // 10198: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > once([0,0,-2]) + stopFinish': false,  // 10199: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > once([0,0,-2]) + done': false,  // 10200: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > once([0,0,2]) + newWordFragment': false,  // 10221: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > once([0,0,2]) + progress': false,  // 10222: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > current(null) + newWordFragment': false,  // 10246: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > current(null) + progress': false,  // 10247: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > jumpWords(-3) + newWordFragment': false,  // 10421: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > jumpWords(-3) + progress': false,  // 10422: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > jumpWords(-3) + stopBegin': false,  // 10423: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > jumpWords(-3) + stopFinish': false,  // 10424: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > jumpWords(-3) + done': false,  // 10425: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > jumpWords(-1) + newWordFragment': false,  // 10446: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > jumpWords(-1) + progress': false,  // 10447: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > jumpWords(-1) + stopBegin': false,  // 10448: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > jumpWords(-1) + stopFinish': false,  // 10449: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > jumpWords(-1) + done': false,  // 10450: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > jumpWords(0) + newWordFragment': false,  // 10471: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > jumpWords(0) + progress': false,  // 10472: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > jumpWords(4) + newWordFragment': false,  // 10496: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > jumpWords(4) + progress': false,  // 10497: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > jumpSentences(0) + newWordFragment': false,  // 10621: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > jumpSentences(0) + progress': false,  // 10622: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > jumpSentences(1) + newWordFragment': false,  // 10646: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > jumpSentences(1) + progress': false,  // 10647: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > jumpSentences(3) + newWordFragment': false,  // 10671: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > jumpSentences(3) + progress': false,  // 10672: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > jumpSentences(3) + stopBegin': false,  // 10673: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > jumpSentences(3) + stopFinish': false,  // 10674: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > jumpSentences(3) + done': false,  // 10675: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > nextWord(null) + newWordFragment': false,  // 10721: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > nextWord(null) + progress': false,  // 10722: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > nextSentence(null) + newWordFragment': false,  // 10746: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > nextSentence(null) + progress': false,  // 10747: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > prevWord(null) + newWordFragment': false,  // 10771: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > prevWord(null) + progress': false,  // 10772: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > prevWord(null) + stopBegin': false,  // 10773: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > prevWord(null) + stopFinish': false,  // 10774: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + onceFinish > prevWord(null) + done': false,  // 10775: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > play(null) + newWordFragment': false,  // 16296: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > play(null) + progress': false,  // 16297: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > toggle(null) + playBegin': false,  // 16301: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > toggle(null) + playFinish': false,  // 16302: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > toggle(null) + pauseBegin': false,  // 16307: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > toggle(null) + pauseFinish': false,  // 16308: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > toggle(null) + revertBegin': false,  // 16313: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > toggle(null) + revertFinish': false,  // 16314: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > toggle(null) + loopBegin': false,  // 16319: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > toggle(null) + loopFinish': false,  // 16320: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > toggle(null) + newWordFragment': false,  // 16321: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > toggle(null) + progress': false,  // 16322: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > toggle(null) + stopBegin': false,  // 16323: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > toggle(null) + stopFinish': false,  // 16324: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > toggle(null) + done': false,  // 16325: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > rewind(null) + newWordFragment': false,  // 16446: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > rewind(null) + progress': false,  // 16447: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > fastForward(null) + newWordFragment': false,  // 16471: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > fastForward(null) + progress': false,  // 16472: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > once([0,0,-2]) + newWordFragment': false,  // 16496: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > once([0,0,-2]) + progress': false,  // 16497: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > once([0,0,-2]) + stopBegin': false,  // 16498: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > once([0,0,-2]) + stopFinish': false,  // 16499: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > once([0,0,-2]) + done': false,  // 16500: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > once([0,0,2]) + newWordFragment': false,  // 16521: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > once([0,0,2]) + progress': false,  // 16522: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > current(null) + newWordFragment': false,  // 16546: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > current(null) + progress': false,  // 16547: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > jumpWords(-3) + newWordFragment': false,  // 16721: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > jumpWords(-3) + progress': false,  // 16722: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > jumpWords(-3) + stopBegin': false,  // 16723: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > jumpWords(-3) + stopFinish': false,  // 16724: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > jumpWords(-3) + done': false,  // 16725: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > jumpWords(-1) + newWordFragment': false,  // 16746: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > jumpWords(-1) + progress': false,  // 16747: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > jumpWords(-1) + stopBegin': false,  // 16748: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > jumpWords(-1) + stopFinish': false,  // 16749: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > jumpWords(-1) + done': false,  // 16750: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > jumpWords(0) + newWordFragment': false,  // 16771: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > jumpWords(0) + progress': false,  // 16772: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > jumpWords(4) + newWordFragment': false,  // 16796: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > jumpWords(4) + progress': false,  // 16797: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > jumpSentences(0) + newWordFragment': false,  // 16921: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > jumpSentences(0) + progress': false,  // 16922: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > jumpSentences(1) + newWordFragment': false,  // 16946: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > jumpSentences(1) + progress': false,  // 16947: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > jumpSentences(3) + newWordFragment': false,  // 16971: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > jumpSentences(3) + progress': false,  // 16972: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > jumpSentences(3) + stopBegin': false,  // 16973: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > jumpSentences(3) + stopFinish': false,  // 16974: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > jumpSentences(3) + done': false,  // 16975: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > nextWord(null) + newWordFragment': false,  // 17021: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > nextWord(null) + progress': false,  // 17022: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > nextSentence(null) + newWordFragment': false,  // 17046: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > nextSentence(null) + progress': false,  // 17047: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > prevWord(null) + newWordFragment': false,  // 17071: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > prevWord(null) + progress': false,  // 17072: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > prevWord(null) + stopBegin': false,  // 17073: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > prevWord(null) + stopFinish': false,  // 17074: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopBegin > prevWord(null) + done': false,  // 17075: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > play(null) + newWordFragment': false,  // 17196: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > play(null) + progress': false,  // 17197: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > toggle(null) + playBegin': false,  // 17201: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > toggle(null) + playFinish': false,  // 17202: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > toggle(null) + pauseBegin': false,  // 17207: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > toggle(null) + pauseFinish': false,  // 17208: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > toggle(null) + revertBegin': false,  // 17213: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > toggle(null) + revertFinish': false,  // 17214: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > toggle(null) + loopBegin': false,  // 17219: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > toggle(null) + loopFinish': false,  // 17220: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > toggle(null) + newWordFragment': false,  // 17221: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > toggle(null) + progress': false,  // 17222: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > toggle(null) + stopBegin': false,  // 17223: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > toggle(null) + stopFinish': false,  // 17224: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > toggle(null) + done': false,  // 17225: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > rewind(null) + newWordFragment': false,  // 17346: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > rewind(null) + progress': false,  // 17347: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > fastForward(null) + newWordFragment': false,  // 17371: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > fastForward(null) + progress': false,  // 17372: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > once([0,0,-2]) + newWordFragment': false,  // 17396: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > once([0,0,-2]) + progress': false,  // 17397: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > once([0,0,-2]) + stopBegin': false,  // 17398: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > once([0,0,-2]) + stopFinish': false,  // 17399: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > once([0,0,-2]) + done': false,  // 17400: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > once([0,0,2]) + newWordFragment': false,  // 17421: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > once([0,0,2]) + progress': false,  // 17422: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > current(null) + newWordFragment': false,  // 17446: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > current(null) + progress': false,  // 17447: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > jumpWords(-3) + newWordFragment': false,  // 17621: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > jumpWords(-3) + progress': false,  // 17622: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > jumpWords(-3) + stopBegin': false,  // 17623: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > jumpWords(-3) + stopFinish': false,  // 17624: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > jumpWords(-3) + done': false,  // 17625: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > jumpWords(-1) + newWordFragment': false,  // 17646: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > jumpWords(-1) + progress': false,  // 17647: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > jumpWords(-1) + stopBegin': false,  // 17648: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > jumpWords(-1) + stopFinish': false,  // 17649: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > jumpWords(-1) + done': false,  // 17650: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > jumpWords(0) + newWordFragment': false,  // 17671: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > jumpWords(0) + progress': false,  // 17672: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > jumpWords(4) + newWordFragment': false,  // 17696: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > jumpWords(4) + progress': false,  // 17697: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > jumpSentences(0) + newWordFragment': false,  // 17821: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > jumpSentences(0) + progress': false,  // 17822: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > jumpSentences(1) + newWordFragment': false,  // 17846: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > jumpSentences(1) + progress': false,  // 17847: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > jumpSentences(3) + newWordFragment': false,  // 17871: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > jumpSentences(3) + progress': false,  // 17872: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > jumpSentences(3) + stopBegin': false,  // 17873: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > jumpSentences(3) + stopFinish': false,  // 17874: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > jumpSentences(3) + done': false,  // 17875: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > nextWord(null) + newWordFragment': false,  // 17921: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > nextWord(null) + progress': false,  // 17922: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > nextSentence(null) + newWordFragment': false,  // 17946: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > nextSentence(null) + progress': false,  // 17947: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > prevWord(null) + newWordFragment': false,  // 17971: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > prevWord(null) + progress': false,  // 17972: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > prevWord(null) + stopBegin': false,  // 17973: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > prevWord(null) + stopFinish': false,  // 17974: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + loopFinish > prevWord(null) + done': false,  // 17975: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > play(null) + newWordFragment': false,  // 18096: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > play(null) + progress': false,  // 18097: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > toggle(null) + playBegin': false,  // 18101: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > toggle(null) + playFinish': false,  // 18102: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > toggle(null) + pauseBegin': false,  // 18107: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > toggle(null) + pauseFinish': false,  // 18108: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > toggle(null) + revertBegin': false,  // 18113: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > toggle(null) + revertFinish': false,  // 18114: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > toggle(null) + loopBegin': false,  // 18119: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > toggle(null) + loopFinish': false,  // 18120: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > toggle(null) + newWordFragment': false,  // 18121: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > toggle(null) + progress': false,  // 18122: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > toggle(null) + stopBegin': false,  // 18123: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > toggle(null) + stopFinish': false,  // 18124: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > toggle(null) + done': false,  // 18125: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > rewind(null) + newWordFragment': false,  // 18246: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > rewind(null) + progress': false,  // 18247: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > fastForward(null) + newWordFragment': false,  // 18271: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > fastForward(null) + progress': false,  // 18272: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > once([0,0,-2]) + newWordFragment': false,  // 18296: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > once([0,0,-2]) + progress': false,  // 18297: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > once([0,0,-2]) + stopBegin': false,  // 18298: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > once([0,0,-2]) + stopFinish': false,  // 18299: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > once([0,0,-2]) + done': false,  // 18300: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > once([0,0,2]) + newWordFragment': false,  // 18321: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > once([0,0,2]) + progress': false,  // 18322: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > current(null) + newWordFragment': false,  // 18346: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > current(null) + progress': false,  // 18347: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > jumpWords(-3) + newWordFragment': false,  // 18521: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > jumpWords(-3) + progress': false,  // 18522: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > jumpWords(-3) + stopBegin': false,  // 18523: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > jumpWords(-3) + stopFinish': false,  // 18524: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > jumpWords(-3) + done': false,  // 18525: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > jumpWords(-1) + newWordFragment': false,  // 18546: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > jumpWords(-1) + progress': false,  // 18547: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > jumpWords(-1) + stopBegin': false,  // 18548: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > jumpWords(-1) + stopFinish': false,  // 18549: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > jumpWords(-1) + done': false,  // 18550: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > jumpWords(0) + newWordFragment': false,  // 18571: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > jumpWords(0) + progress': false,  // 18572: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > jumpWords(4) + newWordFragment': false,  // 18596: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > jumpWords(4) + progress': false,  // 18597: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > jumpSentences(0) + newWordFragment': false,  // 18721: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > jumpSentences(0) + progress': false,  // 18722: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > jumpSentences(1) + newWordFragment': false,  // 18746: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > jumpSentences(1) + progress': false,  // 18747: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > jumpSentences(3) + newWordFragment': false,  // 18771: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > jumpSentences(3) + progress': false,  // 18772: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > jumpSentences(3) + stopBegin': false,  // 18773: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > jumpSentences(3) + stopFinish': false,  // 18774: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > jumpSentences(3) + done': false,  // 18775: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > nextWord(null) + newWordFragment': false,  // 18821: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > nextWord(null) + progress': false,  // 18822: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > nextSentence(null) + newWordFragment': false,  // 18846: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > nextSentence(null) + progress': false,  // 18847: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > prevWord(null) + newWordFragment': false,  // 18871: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > prevWord(null) + progress': false,  // 18872: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > prevWord(null) + stopBegin': false,  // 18873: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > prevWord(null) + stopFinish': false,  // 18874: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + newWordFragment > prevWord(null) + done': false,  // 18875: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > play(null) + newWordFragment': false,  // 18996: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > play(null) + progress': false,  // 18997: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > toggle(null) + playBegin': false,  // 19001: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > toggle(null) + playFinish': false,  // 19002: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > toggle(null) + pauseBegin': false,  // 19007: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > toggle(null) + pauseFinish': false,  // 19008: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > toggle(null) + revertBegin': false,  // 19013: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > toggle(null) + revertFinish': false,  // 19014: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > toggle(null) + loopBegin': false,  // 19019: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > toggle(null) + loopFinish': false,  // 19020: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > toggle(null) + newWordFragment': false,  // 19021: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > toggle(null) + progress': false,  // 19022: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > toggle(null) + stopBegin': false,  // 19023: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > toggle(null) + stopFinish': false,  // 19024: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > toggle(null) + done': false,  // 19025: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > rewind(null) + newWordFragment': false,  // 19146: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > rewind(null) + progress': false,  // 19147: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > fastForward(null) + newWordFragment': false,  // 19171: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > fastForward(null) + progress': false,  // 19172: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > once([0,0,-2]) + newWordFragment': false,  // 19196: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > once([0,0,-2]) + progress': false,  // 19197: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > once([0,0,-2]) + stopBegin': false,  // 19198: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > once([0,0,-2]) + stopFinish': false,  // 19199: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > once([0,0,-2]) + done': false,  // 19200: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > once([0,0,2]) + newWordFragment': false,  // 19221: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > once([0,0,2]) + progress': false,  // 19222: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > current(null) + newWordFragment': false,  // 19246: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > current(null) + progress': false,  // 19247: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > jumpWords(-3) + newWordFragment': false,  // 19421: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > jumpWords(-3) + progress': false,  // 19422: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > jumpWords(-3) + stopBegin': false,  // 19423: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > jumpWords(-3) + stopFinish': false,  // 19424: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > jumpWords(-3) + done': false,  // 19425: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > jumpWords(-1) + newWordFragment': false,  // 19446: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > jumpWords(-1) + progress': false,  // 19447: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > jumpWords(-1) + stopBegin': false,  // 19448: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > jumpWords(-1) + stopFinish': false,  // 19449: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > jumpWords(-1) + done': false,  // 19450: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > jumpWords(0) + newWordFragment': false,  // 19471: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > jumpWords(0) + progress': false,  // 19472: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > jumpWords(4) + newWordFragment': false,  // 19496: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > jumpWords(4) + progress': false,  // 19497: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > jumpSentences(0) + newWordFragment': false,  // 19621: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > jumpSentences(0) + progress': false,  // 19622: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > jumpSentences(1) + newWordFragment': false,  // 19646: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > jumpSentences(1) + progress': false,  // 19647: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > jumpSentences(3) + newWordFragment': false,  // 19671: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > jumpSentences(3) + progress': false,  // 19672: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > jumpSentences(3) + stopBegin': false,  // 19673: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > jumpSentences(3) + stopFinish': false,  // 19674: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > jumpSentences(3) + done': false,  // 19675: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > nextWord(null) + newWordFragment': false,  // 19721: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > nextWord(null) + progress': false,  // 19722: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > nextSentence(null) + newWordFragment': false,  // 19746: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > nextSentence(null) + progress': false,  // 19747: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > prevWord(null) + newWordFragment': false,  // 19771: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > prevWord(null) + progress': false,  // 19772: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > prevWord(null) + stopBegin': false,  // 19773: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > prevWord(null) + stopFinish': false,  // 19774: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(1) + progress > prevWord(null) + done': false,  // 19775: ?? (skip results are different than combos)



		// ============== jumpSentences(3) ==============
		// #? like jumpTo(6) in some (all?) ways
		// #? This one DOES change jumpSentence(-1) (not -3 still) or prevSentence(), since DON'T go to first sentence (which is like jumpTo(6))
		'skip-combos: jumpSentences(3) + onceBegin > play(null) + newWordFragment': false,  // 9096: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > play(null) + progress': false,  // 9097: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > toggle(null) + playBegin': false,  // 9101: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > toggle(null) + playFinish': false,  // 9102: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > toggle(null) + pauseBegin': false,  // 9107: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > toggle(null) + pauseFinish': false,  // 9108: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > toggle(null) + revertBegin': false,  // 9113: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > toggle(null) + revertFinish': false,  // 9114: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > toggle(null) + loopBegin': false,  // 9119: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > toggle(null) + loopFinish': false,  // 9120: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > toggle(null) + newWordFragment': false,  // 9121: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > toggle(null) + progress': false,  // 9122: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > toggle(null) + stopBegin': false,  // 9123: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > toggle(null) + stopFinish': false,  // 9124: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > toggle(null) + done': false,  // 9125: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > rewind(null) + newWordFragment': false,  // 9246: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > rewind(null) + progress': false,  // 9247: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > fastForward(null) + newWordFragment': false,  // 9271: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > fastForward(null) + progress': false,  // 9272: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > once([0,0,-2]) + newWordFragment': false,  // 9296: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > once([0,0,-2]) + progress': false,  // 9297: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > once([0,0,-2]) + stopBegin': false,  // 9298: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > once([0,0,-2]) + stopFinish': false,  // 9299: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > once([0,0,-2]) + done': false,  // 9300: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > once([0,0,2]) + newWordFragment': false,  // 9321: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > once([0,0,2]) + progress': false,  // 9322: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > current(null) + newWordFragment': false,  // 9346: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > current(null) + progress': false,  // 9347: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > jumpWords(-3) + newWordFragment': false,  // 9521: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > jumpWords(-3) + progress': false,  // 9522: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > jumpWords(-3) + stopBegin': false,  // 9523: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > jumpWords(-3) + stopFinish': false,  // 9524: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > jumpWords(-3) + done': false,  // 9525: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > jumpWords(-1) + newWordFragment': false,  // 9546: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > jumpWords(-1) + progress': false,  // 9547: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > jumpWords(-1) + stopBegin': false,  // 9548: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > jumpWords(-1) + stopFinish': false,  // 9549: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > jumpWords(-1) + done': false,  // 9550: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > jumpWords(0) + newWordFragment': false,  // 9571: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > jumpWords(0) + progress': false,  // 9572: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > jumpWords(4) + newWordFragment': false,  // 9596: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > jumpWords(4) + progress': false,  // 9597: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > jumpWords(4) + stopBegin': false,  // 9598: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > jumpWords(4) + stopFinish': false,  // 9599: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > jumpWords(4) + done': false,  // 9600: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > jumpSentences(-1) + newWordFragment': false,  // 9696: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > jumpSentences(-1) + progress': false,  // 9697: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > jumpSentences(-1) + stopBegin': false,  // 9698: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > jumpSentences(-1) + stopFinish': false,  // 9699: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > jumpSentences(-1) + done': false,  // 9700: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > jumpSentences(0) + newWordFragment': false,  // 9721: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > jumpSentences(0) + progress': false,  // 9722: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > jumpSentences(1) + newWordFragment': false,  // 9746: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > jumpSentences(1) + progress': false,  // 9747: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > jumpSentences(1) + stopBegin': false,  // 9748: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > jumpSentences(1) + stopFinish': false,  // 9749: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > jumpSentences(1) + done': false,  // 9750: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > jumpSentences(3) + newWordFragment': false,  // 9771: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > jumpSentences(3) + progress': false,  // 9772: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > jumpSentences(3) + stopBegin': false,  // 9773: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > jumpSentences(3) + stopFinish': false,  // 9774: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > jumpSentences(3) + done': false,  // 9775: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > nextWord(null) + newWordFragment': false,  // 9821: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > nextWord(null) + progress': false,  // 9822: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > nextSentence(null) + newWordFragment': false,  // 9846: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > nextSentence(null) + progress': false,  // 9847: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > nextSentence(null) + stopBegin': false,  // 9848: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > nextSentence(null) + stopFinish': false,  // 9849: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > nextSentence(null) + done': false,  // 9850: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > prevWord(null) + newWordFragment': false,  // 9871: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > prevWord(null) + progress': false,  // 9872: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > prevWord(null) + stopBegin': false,  // 9873: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > prevWord(null) + stopFinish': false,  // 9874: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > prevWord(null) + done': false,  // 9875: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > prevSentence(null) + newWordFragment': false,  // 9896: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > prevSentence(null) + progress': false,  // 9897: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > prevSentence(null) + stopBegin': false,  // 9898: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > prevSentence(null) + stopFinish': false,  // 9899: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceBegin > prevSentence(null) + done': false,  // 9900: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > play(null) + newWordFragment': false,  // 9996: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > play(null) + progress': false,  // 9997: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > toggle(null) + playBegin': false,  // 10001: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > toggle(null) + playFinish': false,  // 10002: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > toggle(null) + pauseBegin': false,  // 10007: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > toggle(null) + pauseFinish': false,  // 10008: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > toggle(null) + revertBegin': false,  // 10013: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > toggle(null) + revertFinish': false,  // 10014: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > toggle(null) + loopBegin': false,  // 10019: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > toggle(null) + loopFinish': false,  // 10020: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > toggle(null) + newWordFragment': false,  // 10021: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > toggle(null) + progress': false,  // 10022: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > toggle(null) + stopBegin': false,  // 10023: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > toggle(null) + stopFinish': false,  // 10024: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > toggle(null) + done': false,  // 10025: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > rewind(null) + newWordFragment': false,  // 10146: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > rewind(null) + progress': false,  // 10147: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > fastForward(null) + newWordFragment': false,  // 10171: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > fastForward(null) + progress': false,  // 10172: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > once([0,0,-2]) + newWordFragment': false,  // 10196: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > once([0,0,-2]) + progress': false,  // 10197: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > once([0,0,-2]) + stopBegin': false,  // 10198: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > once([0,0,-2]) + stopFinish': false,  // 10199: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > once([0,0,-2]) + done': false,  // 10200: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > once([0,0,2]) + newWordFragment': false,  // 10221: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > once([0,0,2]) + progress': false,  // 10222: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > current(null) + newWordFragment': false,  // 10246: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > current(null) + progress': false,  // 10247: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > jumpWords(-3) + newWordFragment': false,  // 10421: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > jumpWords(-3) + progress': false,  // 10422: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > jumpWords(-3) + stopBegin': false,  // 10423: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > jumpWords(-3) + stopFinish': false,  // 10424: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > jumpWords(-3) + done': false,  // 10425: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > jumpWords(-1) + newWordFragment': false,  // 10446: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > jumpWords(-1) + progress': false,  // 10447: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > jumpWords(-1) + stopBegin': false,  // 10448: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > jumpWords(-1) + stopFinish': false,  // 10449: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > jumpWords(-1) + done': false,  // 10450: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > jumpWords(0) + newWordFragment': false,  // 10471: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > jumpWords(0) + progress': false,  // 10472: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > jumpWords(4) + newWordFragment': false,  // 10496: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > jumpWords(4) + progress': false,  // 10497: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > jumpWords(4) + stopBegin': false,  // 10498: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > jumpWords(4) + stopFinish': false,  // 10499: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > jumpWords(4) + done': false,  // 10500: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > jumpSentences(-1) + newWordFragment': false,  // 10596: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > jumpSentences(-1) + progress': false,  // 10597: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > jumpSentences(-1) + stopBegin': false,  // 10598: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > jumpSentences(-1) + stopFinish': false,  // 10599: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > jumpSentences(-1) + done': false,  // 10600: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > jumpSentences(0) + newWordFragment': false,  // 10621: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > jumpSentences(0) + progress': false,  // 10622: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > jumpSentences(1) + newWordFragment': false,  // 10646: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > jumpSentences(1) + progress': false,  // 10647: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > jumpSentences(1) + stopBegin': false,  // 10648: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > jumpSentences(1) + stopFinish': false,  // 10649: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > jumpSentences(1) + done': false,  // 10650: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > jumpSentences(3) + newWordFragment': false,  // 10671: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > jumpSentences(3) + progress': false,  // 10672: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > jumpSentences(3) + stopBegin': false,  // 10673: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > jumpSentences(3) + stopFinish': false,  // 10674: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > jumpSentences(3) + done': false,  // 10675: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > nextWord(null) + newWordFragment': false,  // 10721: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > nextWord(null) + progress': false,  // 10722: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > nextSentence(null) + newWordFragment': false,  // 10746: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > nextSentence(null) + progress': false,  // 10747: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > nextSentence(null) + stopBegin': false,  // 10748: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > nextSentence(null) + stopFinish': false,  // 10749: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > nextSentence(null) + done': false,  // 10750: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > prevWord(null) + newWordFragment': false,  // 10771: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > prevWord(null) + progress': false,  // 10772: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > prevWord(null) + stopBegin': false,  // 10773: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > prevWord(null) + stopFinish': false,  // 10774: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > prevWord(null) + done': false,  // 10775: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > prevSentence(null) + newWordFragment': false,  // 10796: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > prevSentence(null) + progress': false,  // 10797: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > prevSentence(null) + stopBegin': false,  // 10798: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > prevSentence(null) + stopFinish': false,  // 10799: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + onceFinish > prevSentence(null) + done': false,  // 10800: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > play(null) + newWordFragment': false,  // 16296: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > play(null) + progress': false,  // 16297: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > toggle(null) + playBegin': false,  // 16301: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > toggle(null) + playFinish': false,  // 16302: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > toggle(null) + pauseBegin': false,  // 16307: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > toggle(null) + pauseFinish': false,  // 16308: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > toggle(null) + revertBegin': false,  // 16313: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > toggle(null) + revertFinish': false,  // 16314: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > toggle(null) + loopBegin': false,  // 16319: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > toggle(null) + loopFinish': false,  // 16320: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > toggle(null) + newWordFragment': false,  // 16321: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > toggle(null) + progress': false,  // 16322: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > toggle(null) + stopBegin': false,  // 16323: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > toggle(null) + stopFinish': false,  // 16324: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > toggle(null) + done': false,  // 16325: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > rewind(null) + newWordFragment': false,  // 16446: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > rewind(null) + progress': false,  // 16447: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > fastForward(null) + newWordFragment': false,  // 16471: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > fastForward(null) + progress': false,  // 16472: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > once([0,0,-2]) + newWordFragment': false,  // 16496: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > once([0,0,-2]) + progress': false,  // 16497: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > once([0,0,-2]) + stopBegin': false,  // 16498: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > once([0,0,-2]) + stopFinish': false,  // 16499: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > once([0,0,-2]) + done': false,  // 16500: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > once([0,0,2]) + newWordFragment': false,  // 16521: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > once([0,0,2]) + progress': false,  // 16522: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > current(null) + newWordFragment': false,  // 16546: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > current(null) + progress': false,  // 16547: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > jumpWords(-3) + newWordFragment': false,  // 16721: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > jumpWords(-3) + progress': false,  // 16722: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > jumpWords(-3) + stopBegin': false,  // 16723: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > jumpWords(-3) + stopFinish': false,  // 16724: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > jumpWords(-3) + done': false,  // 16725: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > jumpWords(-1) + newWordFragment': false,  // 16746: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > jumpWords(-1) + progress': false,  // 16747: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > jumpWords(-1) + stopBegin': false,  // 16748: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > jumpWords(-1) + stopFinish': false,  // 16749: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > jumpWords(-1) + done': false,  // 16750: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > jumpWords(0) + newWordFragment': false,  // 16771: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > jumpWords(0) + progress': false,  // 16772: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > jumpWords(4) + newWordFragment': false,  // 16796: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > jumpWords(4) + progress': false,  // 16797: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > jumpWords(4) + stopBegin': false,  // 16798: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > jumpWords(4) + stopFinish': false,  // 16799: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > jumpWords(4) + done': false,  // 16800: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > jumpSentences(-1) + newWordFragment': false,  // 16896: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > jumpSentences(-1) + progress': false,  // 16897: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > jumpSentences(-1) + stopBegin': false,  // 16898: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > jumpSentences(-1) + stopFinish': false,  // 16899: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > jumpSentences(-1) + done': false,  // 16900: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > jumpSentences(0) + newWordFragment': false,  // 16921: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > jumpSentences(0) + progress': false,  // 16922: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > jumpSentences(1) + newWordFragment': false,  // 16946: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > jumpSentences(1) + progress': false,  // 16947: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > jumpSentences(1) + stopBegin': false,  // 16948: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > jumpSentences(1) + stopFinish': false,  // 16949: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > jumpSentences(1) + done': false,  // 16950: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > jumpSentences(3) + newWordFragment': false,  // 16971: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > jumpSentences(3) + progress': false,  // 16972: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > jumpSentences(3) + stopBegin': false,  // 16973: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > jumpSentences(3) + stopFinish': false,  // 16974: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > jumpSentences(3) + done': false,  // 16975: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > nextWord(null) + newWordFragment': false,  // 17021: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > nextWord(null) + progress': false,  // 17022: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > nextSentence(null) + newWordFragment': false,  // 17046: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > nextSentence(null) + progress': false,  // 17047: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > nextSentence(null) + stopBegin': false,  // 17048: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > nextSentence(null) + stopFinish': false,  // 17049: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > nextSentence(null) + done': false,  // 17050: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > prevWord(null) + newWordFragment': false,  // 17071: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > prevWord(null) + progress': false,  // 17072: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > prevWord(null) + stopBegin': false,  // 17073: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > prevWord(null) + stopFinish': false,  // 17074: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > prevWord(null) + done': false,  // 17075: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > prevSentence(null) + newWordFragment': false,  // 17096: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > prevSentence(null) + progress': false,  // 17097: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > prevSentence(null) + stopBegin': false,  // 17098: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > prevSentence(null) + stopFinish': false,  // 17099: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopBegin > prevSentence(null) + done': false,  // 17100: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > play(null) + newWordFragment': false,  // 17196: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > play(null) + progress': false,  // 17197: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > toggle(null) + playBegin': false,  // 17201: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > toggle(null) + playFinish': false,  // 17202: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > toggle(null) + pauseBegin': false,  // 17207: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > toggle(null) + pauseFinish': false,  // 17208: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > toggle(null) + revertBegin': false,  // 17213: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > toggle(null) + revertFinish': false,  // 17214: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > toggle(null) + loopBegin': false,  // 17219: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > toggle(null) + loopFinish': false,  // 17220: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > toggle(null) + newWordFragment': false,  // 17221: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > toggle(null) + progress': false,  // 17222: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > toggle(null) + stopBegin': false,  // 17223: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > toggle(null) + stopFinish': false,  // 17224: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > toggle(null) + done': false,  // 17225: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > rewind(null) + newWordFragment': false,  // 17346: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > rewind(null) + progress': false,  // 17347: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > fastForward(null) + newWordFragment': false,  // 17371: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > fastForward(null) + progress': false,  // 17372: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > once([0,0,-2]) + newWordFragment': false,  // 17396: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > once([0,0,-2]) + progress': false,  // 17397: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > once([0,0,-2]) + stopBegin': false,  // 17398: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > once([0,0,-2]) + stopFinish': false,  // 17399: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > once([0,0,-2]) + done': false,  // 17400: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > once([0,0,2]) + newWordFragment': false,  // 17421: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > once([0,0,2]) + progress': false,  // 17422: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > current(null) + newWordFragment': false,  // 17446: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > current(null) + progress': false,  // 17447: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > jumpWords(-3) + newWordFragment': false,  // 17621: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > jumpWords(-3) + progress': false,  // 17622: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > jumpWords(-3) + stopBegin': false,  // 17623: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > jumpWords(-3) + stopFinish': false,  // 17624: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > jumpWords(-3) + done': false,  // 17625: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > jumpWords(-1) + newWordFragment': false,  // 17646: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > jumpWords(-1) + progress': false,  // 17647: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > jumpWords(-1) + stopBegin': false,  // 17648: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > jumpWords(-1) + stopFinish': false,  // 17649: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > jumpWords(-1) + done': false,  // 17650: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > jumpWords(0) + newWordFragment': false,  // 17671: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > jumpWords(0) + progress': false,  // 17672: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > jumpWords(4) + newWordFragment': false,  // 17696: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > jumpWords(4) + progress': false,  // 17697: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > jumpWords(4) + stopBegin': false,  // 17698: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > jumpWords(4) + stopFinish': false,  // 17699: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > jumpWords(4) + done': false,  // 17700: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > jumpSentences(-1) + newWordFragment': false,  // 17796: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > jumpSentences(-1) + progress': false,  // 17797: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > jumpSentences(-1) + stopBegin': false,  // 17798: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > jumpSentences(-1) + stopFinish': false,  // 17799: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > jumpSentences(-1) + done': false,  // 17800: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > jumpSentences(0) + newWordFragment': false,  // 17821: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > jumpSentences(0) + progress': false,  // 17822: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > jumpSentences(1) + newWordFragment': false,  // 17846: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > jumpSentences(1) + progress': false,  // 17847: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > jumpSentences(1) + stopBegin': false,  // 17848: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > jumpSentences(1) + stopFinish': false,  // 17849: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > jumpSentences(1) + done': false,  // 17850: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > jumpSentences(3) + newWordFragment': false,  // 17871: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > jumpSentences(3) + progress': false,  // 17872: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > jumpSentences(3) + stopBegin': false,  // 17873: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > jumpSentences(3) + stopFinish': false,  // 17874: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > jumpSentences(3) + done': false,  // 17875: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > nextWord(null) + newWordFragment': false,  // 17921: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > nextWord(null) + progress': false,  // 17922: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > nextSentence(null) + newWordFragment': false,  // 17946: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > nextSentence(null) + progress': false,  // 17947: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > nextSentence(null) + stopBegin': false,  // 17948: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > nextSentence(null) + stopFinish': false,  // 17949: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > nextSentence(null) + done': false,  // 17950: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > prevWord(null) + newWordFragment': false,  // 17971: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > prevWord(null) + progress': false,  // 17972: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > prevWord(null) + stopBegin': false,  // 17973: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > prevWord(null) + stopFinish': false,  // 17974: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > prevWord(null) + done': false,  // 17975: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > prevSentence(null) + newWordFragment': false,  // 17996: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > prevSentence(null) + progress': false,  // 17997: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > prevSentence(null) + stopBegin': false,  // 17998: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > prevSentence(null) + stopFinish': false,  // 17999: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + loopFinish > prevSentence(null) + done': false,  // 18000: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > play(null) + newWordFragment': false,  // 18096: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > play(null) + progress': false,  // 18097: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > toggle(null) + playBegin': false,  // 18101: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > toggle(null) + playFinish': false,  // 18102: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > toggle(null) + pauseBegin': false,  // 18107: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > toggle(null) + pauseFinish': false,  // 18108: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > toggle(null) + revertBegin': false,  // 18113: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > toggle(null) + revertFinish': false,  // 18114: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > toggle(null) + loopBegin': false,  // 18119: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > toggle(null) + loopFinish': false,  // 18120: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > toggle(null) + newWordFragment': false,  // 18121: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > toggle(null) + progress': false,  // 18122: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > toggle(null) + stopBegin': false,  // 18123: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > toggle(null) + stopFinish': false,  // 18124: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > toggle(null) + done': false,  // 18125: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > rewind(null) + newWordFragment': false,  // 18246: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > rewind(null) + progress': false,  // 18247: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > fastForward(null) + newWordFragment': false,  // 18271: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > fastForward(null) + progress': false,  // 18272: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > once([0,0,-2]) + newWordFragment': false,  // 18296: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > once([0,0,-2]) + progress': false,  // 18297: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > once([0,0,-2]) + stopBegin': false,  // 18298: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > once([0,0,-2]) + stopFinish': false,  // 18299: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > once([0,0,-2]) + done': false,  // 18300: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > once([0,0,2]) + newWordFragment': false,  // 18321: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > once([0,0,2]) + progress': false,  // 18322: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > current(null) + newWordFragment': false,  // 18346: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > current(null) + progress': false,  // 18347: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > jumpWords(-3) + newWordFragment': false,  // 18521: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > jumpWords(-3) + progress': false,  // 18522: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > jumpWords(-3) + stopBegin': false,  // 18523: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > jumpWords(-3) + stopFinish': false,  // 18524: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > jumpWords(-3) + done': false,  // 18525: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > jumpWords(-1) + newWordFragment': false,  // 18546: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > jumpWords(-1) + progress': false,  // 18547: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > jumpWords(-1) + stopBegin': false,  // 18548: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > jumpWords(-1) + stopFinish': false,  // 18549: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > jumpWords(-1) + done': false,  // 18550: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > jumpWords(0) + newWordFragment': false,  // 18571: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > jumpWords(0) + progress': false,  // 18572: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > jumpWords(4) + newWordFragment': false,  // 18596: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > jumpWords(4) + progress': false,  // 18597: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > jumpWords(4) + stopBegin': false,  // 18598: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > jumpWords(4) + stopFinish': false,  // 18599: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > jumpWords(4) + done': false,  // 18600: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > jumpSentences(-1) + newWordFragment': false,  // 18696: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > jumpSentences(-1) + progress': false,  // 18697: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > jumpSentences(-1) + stopBegin': false,  // 18698: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > jumpSentences(-1) + stopFinish': false,  // 18699: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > jumpSentences(-1) + done': false,  // 18700: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > jumpSentences(0) + newWordFragment': false,  // 18721: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > jumpSentences(0) + progress': false,  // 18722: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > jumpSentences(1) + newWordFragment': false,  // 18746: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > jumpSentences(1) + progress': false,  // 18747: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > jumpSentences(1) + stopBegin': false,  // 18748: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > jumpSentences(1) + stopFinish': false,  // 18749: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > jumpSentences(1) + done': false,  // 18750: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > jumpSentences(3) + newWordFragment': false,  // 18771: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > jumpSentences(3) + progress': false,  // 18772: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > jumpSentences(3) + stopBegin': false,  // 18773: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > jumpSentences(3) + stopFinish': false,  // 18774: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > jumpSentences(3) + done': false,  // 18775: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > nextWord(null) + newWordFragment': false,  // 18821: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > nextWord(null) + progress': false,  // 18822: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > nextSentence(null) + newWordFragment': false,  // 18846: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > nextSentence(null) + progress': false,  // 18847: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > nextSentence(null) + stopBegin': false,  // 18848: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > nextSentence(null) + stopFinish': false,  // 18849: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > nextSentence(null) + done': false,  // 18850: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > prevWord(null) + newWordFragment': false,  // 18871: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > prevWord(null) + progress': false,  // 18872: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > prevWord(null) + stopBegin': false,  // 18873: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > prevWord(null) + stopFinish': false,  // 18874: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > prevWord(null) + done': false,  // 18875: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > prevSentence(null) + newWordFragment': false,  // 18896: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > prevSentence(null) + progress': false,  // 18897: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > prevSentence(null) + stopBegin': false,  // 18898: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > prevSentence(null) + stopFinish': false,  // 18899: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + newWordFragment > prevSentence(null) + done': false,  // 18900: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > play(null) + newWordFragment': false,  // 18996: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > play(null) + progress': false,  // 18997: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > toggle(null) + playBegin': false,  // 19001: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > toggle(null) + playFinish': false,  // 19002: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > toggle(null) + pauseBegin': false,  // 19007: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > toggle(null) + pauseFinish': false,  // 19008: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > toggle(null) + revertBegin': false,  // 19013: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > toggle(null) + revertFinish': false,  // 19014: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > toggle(null) + loopBegin': false,  // 19019: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > toggle(null) + loopFinish': false,  // 19020: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > toggle(null) + newWordFragment': false,  // 19021: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > toggle(null) + progress': false,  // 19022: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > toggle(null) + stopBegin': false,  // 19023: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > toggle(null) + stopFinish': false,  // 19024: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > toggle(null) + done': false,  // 19025: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > rewind(null) + newWordFragment': false,  // 19146: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > rewind(null) + progress': false,  // 19147: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > fastForward(null) + newWordFragment': false,  // 19171: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > fastForward(null) + progress': false,  // 19172: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > once([0,0,-2]) + newWordFragment': false,  // 19196: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > once([0,0,-2]) + progress': false,  // 19197: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > once([0,0,-2]) + stopBegin': false,  // 19198: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > once([0,0,-2]) + stopFinish': false,  // 19199: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > once([0,0,-2]) + done': false,  // 19200: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > once([0,0,2]) + newWordFragment': false,  // 19221: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > once([0,0,2]) + progress': false,  // 19222: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > current(null) + newWordFragment': false,  // 19246: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > current(null) + progress': false,  // 19247: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > jumpWords(-3) + newWordFragment': false,  // 19421: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > jumpWords(-3) + progress': false,  // 19422: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > jumpWords(-3) + stopBegin': false,  // 19423: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > jumpWords(-3) + stopFinish': false,  // 19424: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > jumpWords(-3) + done': false,  // 19425: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > jumpWords(-1) + newWordFragment': false,  // 19446: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > jumpWords(-1) + progress': false,  // 19447: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > jumpWords(-1) + stopBegin': false,  // 19448: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > jumpWords(-1) + stopFinish': false,  // 19449: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > jumpWords(-1) + done': false,  // 19450: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > jumpWords(0) + newWordFragment': false,  // 19471: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > jumpWords(0) + progress': false,  // 19472: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > jumpWords(4) + newWordFragment': false,  // 19496: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > jumpWords(4) + progress': false,  // 19497: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > jumpWords(4) + stopBegin': false,  // 19498: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > jumpWords(4) + stopFinish': false,  // 19499: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > jumpWords(4) + done': false,  // 19500: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > jumpSentences(-1) + newWordFragment': false,  // 19596: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > jumpSentences(-1) + progress': false,  // 19597: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > jumpSentences(-1) + stopBegin': false,  // 19598: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > jumpSentences(-1) + stopFinish': false,  // 19599: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > jumpSentences(-1) + done': false,  // 19600: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > jumpSentences(0) + newWordFragment': false,  // 19621: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > jumpSentences(0) + progress': false,  // 19622: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > jumpSentences(1) + newWordFragment': false,  // 19646: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > jumpSentences(1) + progress': false,  // 19647: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > jumpSentences(1) + stopBegin': false,  // 19648: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > jumpSentences(1) + stopFinish': false,  // 19649: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > jumpSentences(1) + done': false,  // 19650: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > jumpSentences(3) + newWordFragment': false,  // 19671: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > jumpSentences(3) + progress': false,  // 19672: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > jumpSentences(3) + stopBegin': false,  // 19673: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > jumpSentences(3) + stopFinish': false,  // 19674: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > jumpSentences(3) + done': false,  // 19675: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > nextWord(null) + newWordFragment': false,  // 19721: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > nextWord(null) + progress': false,  // 19722: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > nextSentence(null) + newWordFragment': false,  // 19746: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > nextSentence(null) + progress': false,  // 19747: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > nextSentence(null) + stopBegin': false,  // 19748: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > nextSentence(null) + stopFinish': false,  // 19749: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > nextSentence(null) + done': false,  // 19750: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > prevWord(null) + newWordFragment': false,  // 19771: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > prevWord(null) + progress': false,  // 19772: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > prevWord(null) + stopBegin': false,  // 19773: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > prevWord(null) + stopFinish': false,  // 19774: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > prevWord(null) + done': false,  // 19775: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > prevSentence(null) + newWordFragment': false,  // 19796: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > prevSentence(null) + progress': false,  // 19797: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > prevSentence(null) + stopBegin': false,  // 19798: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > prevSentence(null) + stopFinish': false,  // 19799: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(3) + progress > prevSentence(null) + done': false,  // 19800: ?? (skip results are different than combos)



		// ============== jumpSentences(100) ==============
		// #? like jumpTo(11)
		// #6 immediately leading to stop*|done > some more types of relative loopers(+|0|-) get different output than usual
		// #7 immediately leading to stop*|done > relative single(not rewind) loopers(-) don't trigger stop*|done
		// #8 immediately leading to stop*|done > relative loopers(+|0) do unexpectedly trigger stop*|done
		// #9 immediately leading to stop*|done > play|toggle = triggers `restart` instead of `play`
		'skip-combos: jumpSentences(100) + onceBegin > play(null) + playBegin': false,  // 9076: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > play(null) + playFinish': false,  // 9077: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > play(null) + restartBegin': false,  // 9080: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > play(null) + restartFinish': false,  // 9081: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > toggle(null) + playBegin': false,  // 9101: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > toggle(null) + playFinish': false,  // 9102: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > toggle(null) + restartBegin': false,  // 9105: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > toggle(null) + restartFinish': false,  // 9106: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > rewind(null) + newWordFragment': false,  // 9246: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > rewind(null) + progress': false,  // 9247: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > fastForward(null) + newWordFragment': false,  // 9271: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > fastForward(null) + progress': false,  // 9272: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > once([0,0,-2]) + newWordFragment': false,  // 9296: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > once([0,0,-2]) + progress': false,  // 9297: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > once([0,0,-2]) + stopBegin': false,  // 9298: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > once([0,0,-2]) + stopFinish': false,  // 9299: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > once([0,0,-2]) + done': false,  // 9300: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > once([0,0,2]) + newWordFragment': false,  // 9321: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > once([0,0,2]) + progress': false,  // 9322: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > once([0,0,2]) + stopBegin': false,  // 9323: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > once([0,0,2]) + stopFinish': false,  // 9324: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > once([0,0,2]) + done': false,  // 9325: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > current(null) + newWordFragment': false,  // 9346: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > current(null) + progress': false,  // 9347: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > current(null) + stopBegin': false,  // 9348: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > current(null) + stopFinish': false,  // 9349: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > current(null) + done': false,  // 9350: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpWords(-3) + newWordFragment': false,  // 9521: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpWords(-3) + progress': false,  // 9522: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpWords(-3) + stopBegin': false,  // 9523: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpWords(-3) + stopFinish': false,  // 9524: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpWords(-3) + done': false,  // 9525: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpWords(-1) + newWordFragment': false,  // 9546: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpWords(-1) + progress': false,  // 9547: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpWords(-1) + stopBegin': false,  // 9548: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpWords(-1) + stopFinish': false,  // 9549: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpWords(-1) + done': false,  // 9550: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpWords(0) + newWordFragment': false,  // 9571: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpWords(0) + progress': false,  // 9572: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpWords(0) + stopBegin': false,  // 9573: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpWords(0) + stopFinish': false,  // 9574: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpWords(0) + done': false,  // 9575: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpWords(4) + newWordFragment': false,  // 9596: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpWords(4) + progress': false,  // 9597: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpWords(4) + stopBegin': false,  // 9598: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpWords(4) + stopFinish': false,  // 9599: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpWords(4) + done': false,  // 9600: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpSentences(-1) + newWordFragment': false,  // 9696: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpSentences(-1) + progress': false,  // 9697: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpSentences(-1) + stopBegin': false,  // 9698: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpSentences(-1) + stopFinish': false,  // 9699: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpSentences(-1) + done': false,  // 9700: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpSentences(0) + newWordFragment': false,  // 9721: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpSentences(0) + progress': false,  // 9722: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpSentences(0) + stopBegin': false,  // 9723: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpSentences(0) + stopFinish': false,  // 9724: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpSentences(0) + done': false,  // 9725: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpSentences(1) + newWordFragment': false,  // 9746: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpSentences(1) + progress': false,  // 9747: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpSentences(1) + stopBegin': false,  // 9748: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpSentences(1) + stopFinish': false,  // 9749: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpSentences(1) + done': false,  // 9750: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpSentences(3) + newWordFragment': false,  // 9771: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpSentences(3) + progress': false,  // 9772: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpSentences(3) + stopBegin': false,  // 9773: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpSentences(3) + stopFinish': false,  // 9774: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > jumpSentences(3) + done': false,  // 9775: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > nextWord(null) + newWordFragment': false,  // 9821: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > nextWord(null) + progress': false,  // 9822: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > nextWord(null) + stopBegin': false,  // 9823: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > nextWord(null) + stopFinish': false,  // 9824: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > nextWord(null) + done': false,  // 9825: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > nextSentence(null) + newWordFragment': false,  // 9846: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > nextSentence(null) + progress': false,  // 9847: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > nextSentence(null) + stopBegin': false,  // 9848: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > nextSentence(null) + stopFinish': false,  // 9849: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > nextSentence(null) + done': false,  // 9850: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > prevWord(null) + newWordFragment': false,  // 9871: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > prevWord(null) + progress': false,  // 9872: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > prevWord(null) + stopBegin': false,  // 9873: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > prevWord(null) + stopFinish': false,  // 9874: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > prevWord(null) + done': false,  // 9875: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > prevSentence(null) + newWordFragment': false,  // 9896: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > prevSentence(null) + progress': false,  // 9897: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > prevSentence(null) + stopBegin': false,  // 9898: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > prevSentence(null) + stopFinish': false,  // 9899: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceBegin > prevSentence(null) + done': false,  // 9900: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > play(null) + playBegin': false,  // 9976: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > play(null) + playFinish': false,  // 9977: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > play(null) + restartBegin': false,  // 9980: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > play(null) + restartFinish': false,  // 9981: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > toggle(null) + playBegin': false,  // 10001: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > toggle(null) + playFinish': false,  // 10002: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > toggle(null) + restartBegin': false,  // 10005: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > toggle(null) + restartFinish': false,  // 10006: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > rewind(null) + newWordFragment': false,  // 10146: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > rewind(null) + progress': false,  // 10147: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > fastForward(null) + newWordFragment': false,  // 10171: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > fastForward(null) + progress': false,  // 10172: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > once([0,0,-2]) + newWordFragment': false,  // 10196: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > once([0,0,-2]) + progress': false,  // 10197: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > once([0,0,-2]) + stopBegin': false,  // 10198: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > once([0,0,-2]) + stopFinish': false,  // 10199: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > once([0,0,-2]) + done': false,  // 10200: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > once([0,0,2]) + newWordFragment': false,  // 10221: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > once([0,0,2]) + progress': false,  // 10222: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > once([0,0,2]) + stopBegin': false,  // 10223: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > once([0,0,2]) + stopFinish': false,  // 10224: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > once([0,0,2]) + done': false,  // 10225: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > current(null) + newWordFragment': false,  // 10246: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > current(null) + progress': false,  // 10247: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > current(null) + stopBegin': false,  // 10248: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > current(null) + stopFinish': false,  // 10249: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > current(null) + done': false,  // 10250: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpWords(-3) + newWordFragment': false,  // 10421: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpWords(-3) + progress': false,  // 10422: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpWords(-3) + stopBegin': false,  // 10423: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpWords(-3) + stopFinish': false,  // 10424: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpWords(-3) + done': false,  // 10425: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpWords(-1) + newWordFragment': false,  // 10446: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpWords(-1) + progress': false,  // 10447: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpWords(-1) + stopBegin': false,  // 10448: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpWords(-1) + stopFinish': false,  // 10449: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpWords(-1) + done': false,  // 10450: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpWords(0) + newWordFragment': false,  // 10471: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpWords(0) + progress': false,  // 10472: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpWords(0) + stopBegin': false,  // 10473: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpWords(0) + stopFinish': false,  // 10474: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpWords(0) + done': false,  // 10475: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpWords(4) + newWordFragment': false,  // 10496: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpWords(4) + progress': false,  // 10497: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpWords(4) + stopBegin': false,  // 10498: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpWords(4) + stopFinish': false,  // 10499: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpWords(4) + done': false,  // 10500: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpSentences(-1) + newWordFragment': false,  // 10596: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpSentences(-1) + progress': false,  // 10597: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpSentences(-1) + stopBegin': false,  // 10598: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpSentences(-1) + stopFinish': false,  // 10599: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpSentences(-1) + done': false,  // 10600: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpSentences(0) + newWordFragment': false,  // 10621: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpSentences(0) + progress': false,  // 10622: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpSentences(0) + stopBegin': false,  // 10623: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpSentences(0) + stopFinish': false,  // 10624: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpSentences(0) + done': false,  // 10625: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpSentences(1) + newWordFragment': false,  // 10646: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpSentences(1) + progress': false,  // 10647: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpSentences(1) + stopBegin': false,  // 10648: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpSentences(1) + stopFinish': false,  // 10649: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpSentences(1) + done': false,  // 10650: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpSentences(3) + newWordFragment': false,  // 10671: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpSentences(3) + progress': false,  // 10672: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpSentences(3) + stopBegin': false,  // 10673: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpSentences(3) + stopFinish': false,  // 10674: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > jumpSentences(3) + done': false,  // 10675: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > nextWord(null) + newWordFragment': false,  // 10721: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > nextWord(null) + progress': false,  // 10722: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > nextWord(null) + stopBegin': false,  // 10723: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > nextWord(null) + stopFinish': false,  // 10724: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > nextWord(null) + done': false,  // 10725: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > nextSentence(null) + newWordFragment': false,  // 10746: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > nextSentence(null) + progress': false,  // 10747: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > nextSentence(null) + stopBegin': false,  // 10748: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > nextSentence(null) + stopFinish': false,  // 10749: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > nextSentence(null) + done': false,  // 10750: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > prevWord(null) + newWordFragment': false,  // 10771: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > prevWord(null) + progress': false,  // 10772: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > prevWord(null) + stopBegin': false,  // 10773: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > prevWord(null) + stopFinish': false,  // 10774: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > prevWord(null) + done': false,  // 10775: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > prevSentence(null) + newWordFragment': false,  // 10796: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > prevSentence(null) + progress': false,  // 10797: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > prevSentence(null) + stopBegin': false,  // 10798: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > prevSentence(null) + stopFinish': false,  // 10799: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + onceFinish > prevSentence(null) + done': false,  // 10800: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > play(null) + playBegin': false,  // 16276: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > play(null) + playFinish': false,  // 16277: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > play(null) + restartBegin': false,  // 16280: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > play(null) + restartFinish': false,  // 16281: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > toggle(null) + playBegin': false,  // 16301: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > toggle(null) + playFinish': false,  // 16302: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > toggle(null) + restartBegin': false,  // 16305: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > toggle(null) + restartFinish': false,  // 16306: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > rewind(null) + newWordFragment': false,  // 16446: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > rewind(null) + progress': false,  // 16447: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > fastForward(null) + newWordFragment': false,  // 16471: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > fastForward(null) + progress': false,  // 16472: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > once([0,0,-2]) + newWordFragment': false,  // 16496: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > once([0,0,-2]) + progress': false,  // 16497: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > once([0,0,-2]) + stopBegin': false,  // 16498: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > once([0,0,-2]) + stopFinish': false,  // 16499: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > once([0,0,-2]) + done': false,  // 16500: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > once([0,0,2]) + newWordFragment': false,  // 16521: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > once([0,0,2]) + progress': false,  // 16522: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > once([0,0,2]) + stopBegin': false,  // 16523: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > once([0,0,2]) + stopFinish': false,  // 16524: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > once([0,0,2]) + done': false,  // 16525: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > current(null) + newWordFragment': false,  // 16546: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > current(null) + progress': false,  // 16547: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > current(null) + stopBegin': false,  // 16548: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > current(null) + stopFinish': false,  // 16549: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > current(null) + done': false,  // 16550: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpWords(-3) + newWordFragment': false,  // 16721: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpWords(-3) + progress': false,  // 16722: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpWords(-3) + stopBegin': false,  // 16723: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpWords(-3) + stopFinish': false,  // 16724: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpWords(-3) + done': false,  // 16725: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpWords(-1) + newWordFragment': false,  // 16746: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpWords(-1) + progress': false,  // 16747: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpWords(-1) + stopBegin': false,  // 16748: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpWords(-1) + stopFinish': false,  // 16749: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpWords(-1) + done': false,  // 16750: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpWords(0) + newWordFragment': false,  // 16771: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpWords(0) + progress': false,  // 16772: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpWords(0) + stopBegin': false,  // 16773: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpWords(0) + stopFinish': false,  // 16774: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpWords(0) + done': false,  // 16775: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpWords(4) + newWordFragment': false,  // 16796: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpWords(4) + progress': false,  // 16797: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpWords(4) + stopBegin': false,  // 16798: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpWords(4) + stopFinish': false,  // 16799: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpWords(4) + done': false,  // 16800: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpSentences(-1) + newWordFragment': false,  // 16896: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpSentences(-1) + progress': false,  // 16897: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpSentences(-1) + stopBegin': false,  // 16898: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpSentences(-1) + stopFinish': false,  // 16899: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpSentences(-1) + done': false,  // 16900: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpSentences(0) + newWordFragment': false,  // 16921: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpSentences(0) + progress': false,  // 16922: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpSentences(0) + stopBegin': false,  // 16923: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpSentences(0) + stopFinish': false,  // 16924: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpSentences(0) + done': false,  // 16925: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpSentences(1) + newWordFragment': false,  // 16946: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpSentences(1) + progress': false,  // 16947: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpSentences(1) + stopBegin': false,  // 16948: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpSentences(1) + stopFinish': false,  // 16949: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpSentences(1) + done': false,  // 16950: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpSentences(3) + newWordFragment': false,  // 16971: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpSentences(3) + progress': false,  // 16972: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpSentences(3) + stopBegin': false,  // 16973: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpSentences(3) + stopFinish': false,  // 16974: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > jumpSentences(3) + done': false,  // 16975: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > nextWord(null) + newWordFragment': false,  // 17021: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > nextWord(null) + progress': false,  // 17022: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > nextWord(null) + stopBegin': false,  // 17023: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > nextWord(null) + stopFinish': false,  // 17024: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > nextWord(null) + done': false,  // 17025: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > nextSentence(null) + newWordFragment': false,  // 17046: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > nextSentence(null) + progress': false,  // 17047: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > nextSentence(null) + stopBegin': false,  // 17048: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > nextSentence(null) + stopFinish': false,  // 17049: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > nextSentence(null) + done': false,  // 17050: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > prevWord(null) + newWordFragment': false,  // 17071: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > prevWord(null) + progress': false,  // 17072: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > prevWord(null) + stopBegin': false,  // 17073: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > prevWord(null) + stopFinish': false,  // 17074: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > prevWord(null) + done': false,  // 17075: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > prevSentence(null) + newWordFragment': false,  // 17096: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > prevSentence(null) + progress': false,  // 17097: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > prevSentence(null) + stopBegin': false,  // 17098: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > prevSentence(null) + stopFinish': false,  // 17099: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopBegin > prevSentence(null) + done': false,  // 17100: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > play(null) + playBegin': false,  // 17176: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > play(null) + playFinish': false,  // 17177: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > play(null) + restartBegin': false,  // 17180: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > play(null) + restartFinish': false,  // 17181: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > toggle(null) + playBegin': false,  // 17201: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > toggle(null) + playFinish': false,  // 17202: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > toggle(null) + restartBegin': false,  // 17205: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > toggle(null) + restartFinish': false,  // 17206: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > rewind(null) + newWordFragment': false,  // 17346: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > rewind(null) + progress': false,  // 17347: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > fastForward(null) + newWordFragment': false,  // 17371: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > fastForward(null) + progress': false,  // 17372: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > once([0,0,-2]) + newWordFragment': false,  // 17396: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > once([0,0,-2]) + progress': false,  // 17397: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > once([0,0,-2]) + stopBegin': false,  // 17398: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > once([0,0,-2]) + stopFinish': false,  // 17399: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > once([0,0,-2]) + done': false,  // 17400: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > once([0,0,2]) + newWordFragment': false,  // 17421: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > once([0,0,2]) + progress': false,  // 17422: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > once([0,0,2]) + stopBegin': false,  // 17423: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > once([0,0,2]) + stopFinish': false,  // 17424: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > once([0,0,2]) + done': false,  // 17425: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > current(null) + newWordFragment': false,  // 17446: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > current(null) + progress': false,  // 17447: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > current(null) + stopBegin': false,  // 17448: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > current(null) + stopFinish': false,  // 17449: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > current(null) + done': false,  // 17450: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpWords(-3) + newWordFragment': false,  // 17621: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpWords(-3) + progress': false,  // 17622: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpWords(-3) + stopBegin': false,  // 17623: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpWords(-3) + stopFinish': false,  // 17624: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpWords(-3) + done': false,  // 17625: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpWords(-1) + newWordFragment': false,  // 17646: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpWords(-1) + progress': false,  // 17647: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpWords(-1) + stopBegin': false,  // 17648: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpWords(-1) + stopFinish': false,  // 17649: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpWords(-1) + done': false,  // 17650: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpWords(0) + newWordFragment': false,  // 17671: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpWords(0) + progress': false,  // 17672: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpWords(0) + stopBegin': false,  // 17673: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpWords(0) + stopFinish': false,  // 17674: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpWords(0) + done': false,  // 17675: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpWords(4) + newWordFragment': false,  // 17696: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpWords(4) + progress': false,  // 17697: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpWords(4) + stopBegin': false,  // 17698: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpWords(4) + stopFinish': false,  // 17699: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpWords(4) + done': false,  // 17700: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpSentences(-1) + newWordFragment': false,  // 17796: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpSentences(-1) + progress': false,  // 17797: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpSentences(-1) + stopBegin': false,  // 17798: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpSentences(-1) + stopFinish': false,  // 17799: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpSentences(-1) + done': false,  // 17800: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpSentences(0) + newWordFragment': false,  // 17821: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpSentences(0) + progress': false,  // 17822: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpSentences(0) + stopBegin': false,  // 17823: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpSentences(0) + stopFinish': false,  // 17824: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpSentences(0) + done': false,  // 17825: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpSentences(1) + newWordFragment': false,  // 17846: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpSentences(1) + progress': false,  // 17847: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpSentences(1) + stopBegin': false,  // 17848: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpSentences(1) + stopFinish': false,  // 17849: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpSentences(1) + done': false,  // 17850: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpSentences(3) + newWordFragment': false,  // 17871: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpSentences(3) + progress': false,  // 17872: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpSentences(3) + stopBegin': false,  // 17873: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpSentences(3) + stopFinish': false,  // 17874: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > jumpSentences(3) + done': false,  // 17875: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > nextWord(null) + newWordFragment': false,  // 17921: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > nextWord(null) + progress': false,  // 17922: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > nextWord(null) + stopBegin': false,  // 17923: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > nextWord(null) + stopFinish': false,  // 17924: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > nextWord(null) + done': false,  // 17925: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > nextSentence(null) + newWordFragment': false,  // 17946: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > nextSentence(null) + progress': false,  // 17947: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > nextSentence(null) + stopBegin': false,  // 17948: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > nextSentence(null) + stopFinish': false,  // 17949: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > nextSentence(null) + done': false,  // 17950: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > prevWord(null) + newWordFragment': false,  // 17971: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > prevWord(null) + progress': false,  // 17972: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > prevWord(null) + stopBegin': false,  // 17973: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > prevWord(null) + stopFinish': false,  // 17974: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > prevWord(null) + done': false,  // 17975: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > prevSentence(null) + newWordFragment': false,  // 17996: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > prevSentence(null) + progress': false,  // 17997: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > prevSentence(null) + stopBegin': false,  // 17998: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > prevSentence(null) + stopFinish': false,  // 17999: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + loopFinish > prevSentence(null) + done': false,  // 18000: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > play(null) + playBegin': false,  // 18076: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > play(null) + playFinish': false,  // 18077: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > play(null) + restartBegin': false,  // 18080: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > play(null) + restartFinish': false,  // 18081: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > toggle(null) + playBegin': false,  // 18101: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > toggle(null) + playFinish': false,  // 18102: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > toggle(null) + restartBegin': false,  // 18105: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > toggle(null) + restartFinish': false,  // 18106: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > rewind(null) + newWordFragment': false,  // 18246: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > rewind(null) + progress': false,  // 18247: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > fastForward(null) + newWordFragment': false,  // 18271: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > fastForward(null) + progress': false,  // 18272: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > once([0,0,-2]) + newWordFragment': false,  // 18296: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > once([0,0,-2]) + progress': false,  // 18297: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > once([0,0,-2]) + stopBegin': false,  // 18298: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > once([0,0,-2]) + stopFinish': false,  // 18299: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > once([0,0,-2]) + done': false,  // 18300: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > once([0,0,2]) + newWordFragment': false,  // 18321: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > once([0,0,2]) + progress': false,  // 18322: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > once([0,0,2]) + stopBegin': false,  // 18323: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > once([0,0,2]) + stopFinish': false,  // 18324: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > once([0,0,2]) + done': false,  // 18325: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > current(null) + newWordFragment': false,  // 18346: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > current(null) + progress': false,  // 18347: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > current(null) + stopBegin': false,  // 18348: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > current(null) + stopFinish': false,  // 18349: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > current(null) + done': false,  // 18350: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpWords(-3) + newWordFragment': false,  // 18521: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpWords(-3) + progress': false,  // 18522: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpWords(-3) + stopBegin': false,  // 18523: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpWords(-3) + stopFinish': false,  // 18524: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpWords(-3) + done': false,  // 18525: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpWords(-1) + newWordFragment': false,  // 18546: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpWords(-1) + progress': false,  // 18547: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpWords(-1) + stopBegin': false,  // 18548: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpWords(-1) + stopFinish': false,  // 18549: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpWords(-1) + done': false,  // 18550: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpWords(0) + newWordFragment': false,  // 18571: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpWords(0) + progress': false,  // 18572: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpWords(0) + stopBegin': false,  // 18573: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpWords(0) + stopFinish': false,  // 18574: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpWords(0) + done': false,  // 18575: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpWords(4) + newWordFragment': false,  // 18596: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpWords(4) + progress': false,  // 18597: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpWords(4) + stopBegin': false,  // 18598: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpWords(4) + stopFinish': false,  // 18599: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpWords(4) + done': false,  // 18600: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpSentences(-1) + newWordFragment': false,  // 18696: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpSentences(-1) + progress': false,  // 18697: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpSentences(-1) + stopBegin': false,  // 18698: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpSentences(-1) + stopFinish': false,  // 18699: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpSentences(-1) + done': false,  // 18700: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpSentences(0) + newWordFragment': false,  // 18721: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpSentences(0) + progress': false,  // 18722: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpSentences(0) + stopBegin': false,  // 18723: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpSentences(0) + stopFinish': false,  // 18724: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpSentences(0) + done': false,  // 18725: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpSentences(1) + newWordFragment': false,  // 18746: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpSentences(1) + progress': false,  // 18747: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpSentences(1) + stopBegin': false,  // 18748: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpSentences(1) + stopFinish': false,  // 18749: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpSentences(1) + done': false,  // 18750: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpSentences(3) + newWordFragment': false,  // 18771: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpSentences(3) + progress': false,  // 18772: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpSentences(3) + stopBegin': false,  // 18773: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpSentences(3) + stopFinish': false,  // 18774: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > jumpSentences(3) + done': false,  // 18775: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > nextWord(null) + newWordFragment': false,  // 18821: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > nextWord(null) + progress': false,  // 18822: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > nextWord(null) + stopBegin': false,  // 18823: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > nextWord(null) + stopFinish': false,  // 18824: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > nextWord(null) + done': false,  // 18825: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > nextSentence(null) + newWordFragment': false,  // 18846: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > nextSentence(null) + progress': false,  // 18847: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > nextSentence(null) + stopBegin': false,  // 18848: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > nextSentence(null) + stopFinish': false,  // 18849: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > nextSentence(null) + done': false,  // 18850: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > prevWord(null) + newWordFragment': false,  // 18871: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > prevWord(null) + progress': false,  // 18872: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > prevWord(null) + stopBegin': false,  // 18873: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > prevWord(null) + stopFinish': false,  // 18874: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > prevWord(null) + done': false,  // 18875: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > prevSentence(null) + newWordFragment': false,  // 18896: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > prevSentence(null) + progress': false,  // 18897: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > prevSentence(null) + stopBegin': false,  // 18898: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > prevSentence(null) + stopFinish': false,  // 18899: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + newWordFragment > prevSentence(null) + done': false,  // 18900: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > play(null) + playBegin': false,  // 18976: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > play(null) + playFinish': false,  // 18977: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > play(null) + restartBegin': false,  // 18980: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > play(null) + restartFinish': false,  // 18981: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > toggle(null) + playBegin': false,  // 19001: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > toggle(null) + playFinish': false,  // 19002: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > toggle(null) + restartBegin': false,  // 19005: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > toggle(null) + restartFinish': false,  // 19006: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > rewind(null) + newWordFragment': false,  // 19146: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > rewind(null) + progress': false,  // 19147: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > fastForward(null) + newWordFragment': false,  // 19171: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > fastForward(null) + progress': false,  // 19172: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > once([0,0,-2]) + newWordFragment': false,  // 19196: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > once([0,0,-2]) + progress': false,  // 19197: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > once([0,0,-2]) + stopBegin': false,  // 19198: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > once([0,0,-2]) + stopFinish': false,  // 19199: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > once([0,0,-2]) + done': false,  // 19200: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > once([0,0,2]) + newWordFragment': false,  // 19221: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > once([0,0,2]) + progress': false,  // 19222: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > once([0,0,2]) + stopBegin': false,  // 19223: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > once([0,0,2]) + stopFinish': false,  // 19224: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > once([0,0,2]) + done': false,  // 19225: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > current(null) + newWordFragment': false,  // 19246: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > current(null) + progress': false,  // 19247: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > current(null) + stopBegin': false,  // 19248: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > current(null) + stopFinish': false,  // 19249: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > current(null) + done': false,  // 19250: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpWords(-3) + newWordFragment': false,  // 19421: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpWords(-3) + progress': false,  // 19422: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpWords(-3) + stopBegin': false,  // 19423: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpWords(-3) + stopFinish': false,  // 19424: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpWords(-3) + done': false,  // 19425: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpWords(-1) + newWordFragment': false,  // 19446: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpWords(-1) + progress': false,  // 19447: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpWords(-1) + stopBegin': false,  // 19448: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpWords(-1) + stopFinish': false,  // 19449: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpWords(-1) + done': false,  // 19450: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpWords(0) + newWordFragment': false,  // 19471: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpWords(0) + progress': false,  // 19472: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpWords(0) + stopBegin': false,  // 19473: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpWords(0) + stopFinish': false,  // 19474: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpWords(0) + done': false,  // 19475: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpWords(4) + newWordFragment': false,  // 19496: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpWords(4) + progress': false,  // 19497: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpWords(4) + stopBegin': false,  // 19498: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpWords(4) + stopFinish': false,  // 19499: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpWords(4) + done': false,  // 19500: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpSentences(-1) + newWordFragment': false,  // 19596: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpSentences(-1) + progress': false,  // 19597: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpSentences(-1) + stopBegin': false,  // 19598: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpSentences(-1) + stopFinish': false,  // 19599: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpSentences(-1) + done': false,  // 19600: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpSentences(0) + newWordFragment': false,  // 19621: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpSentences(0) + progress': false,  // 19622: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpSentences(0) + stopBegin': false,  // 19623: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpSentences(0) + stopFinish': false,  // 19624: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpSentences(0) + done': false,  // 19625: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpSentences(1) + newWordFragment': false,  // 19646: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpSentences(1) + progress': false,  // 19647: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpSentences(1) + stopBegin': false,  // 19648: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpSentences(1) + stopFinish': false,  // 19649: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpSentences(1) + done': false,  // 19650: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpSentences(3) + newWordFragment': false,  // 19671: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpSentences(3) + progress': false,  // 19672: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpSentences(3) + stopBegin': false,  // 19673: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpSentences(3) + stopFinish': false,  // 19674: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > jumpSentences(3) + done': false,  // 19675: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > nextWord(null) + newWordFragment': false,  // 19721: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > nextWord(null) + progress': false,  // 19722: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > nextWord(null) + stopBegin': false,  // 19723: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > nextWord(null) + stopFinish': false,  // 19724: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > nextWord(null) + done': false,  // 19725: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > nextSentence(null) + newWordFragment': false,  // 19746: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > nextSentence(null) + progress': false,  // 19747: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > nextSentence(null) + stopBegin': false,  // 19748: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > nextSentence(null) + stopFinish': false,  // 19749: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > nextSentence(null) + done': false,  // 19750: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > prevWord(null) + newWordFragment': false,  // 19771: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > prevWord(null) + progress': false,  // 19772: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > prevWord(null) + stopBegin': false,  // 19773: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > prevWord(null) + stopFinish': false,  // 19774: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > prevWord(null) + done': false,  // 19775: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > prevSentence(null) + newWordFragment': false,  // 19796: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > prevSentence(null) + progress': false,  // 19797: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > prevSentence(null) + stopBegin': false,  // 19798: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > prevSentence(null) + stopFinish': false,  // 19799: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + progress > prevSentence(null) + done': false,  // 19800: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > play(null) + playBegin': false,  // 19876: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > play(null) + playFinish': false,  // 19877: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > play(null) + restartBegin': false,  // 19880: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > play(null) + restartFinish': false,  // 19881: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > toggle(null) + playBegin': false,  // 19901: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > toggle(null) + playFinish': false,  // 19902: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > toggle(null) + restartBegin': false,  // 19905: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > toggle(null) + restartFinish': false,  // 19906: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > rewind(null) + newWordFragment': false,  // 20046: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > rewind(null) + progress': false,  // 20047: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > fastForward(null) + newWordFragment': false,  // 20071: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > fastForward(null) + progress': false,  // 20072: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > once([0,0,-2]) + newWordFragment': false,  // 20096: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > once([0,0,-2]) + progress': false,  // 20097: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > once([0,0,-2]) + stopBegin': false,  // 20098: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > once([0,0,-2]) + stopFinish': false,  // 20099: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > once([0,0,-2]) + done': false,  // 20100: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > once([0,0,2]) + newWordFragment': false,  // 20121: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > once([0,0,2]) + progress': false,  // 20122: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > once([0,0,2]) + stopBegin': false,  // 20123: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > once([0,0,2]) + stopFinish': false,  // 20124: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > once([0,0,2]) + done': false,  // 20125: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > current(null) + newWordFragment': false,  // 20146: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > current(null) + progress': false,  // 20147: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > current(null) + stopBegin': false,  // 20148: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > current(null) + stopFinish': false,  // 20149: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > current(null) + done': false,  // 20150: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpWords(-3) + newWordFragment': false,  // 20321: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpWords(-3) + progress': false,  // 20322: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpWords(-3) + stopBegin': false,  // 20323: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpWords(-3) + stopFinish': false,  // 20324: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpWords(-3) + done': false,  // 20325: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpWords(-1) + newWordFragment': false,  // 20346: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpWords(-1) + progress': false,  // 20347: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpWords(-1) + stopBegin': false,  // 20348: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpWords(-1) + stopFinish': false,  // 20349: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpWords(-1) + done': false,  // 20350: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpWords(0) + newWordFragment': false,  // 20371: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpWords(0) + progress': false,  // 20372: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpWords(0) + stopBegin': false,  // 20373: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpWords(0) + stopFinish': false,  // 20374: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpWords(0) + done': false,  // 20375: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpWords(4) + newWordFragment': false,  // 20396: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpWords(4) + progress': false,  // 20397: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpWords(4) + stopBegin': false,  // 20398: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpWords(4) + stopFinish': false,  // 20399: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpWords(4) + done': false,  // 20400: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpSentences(-1) + newWordFragment': false,  // 20496: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpSentences(-1) + progress': false,  // 20497: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpSentences(-1) + stopBegin': false,  // 20498: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpSentences(-1) + stopFinish': false,  // 20499: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpSentences(-1) + done': false,  // 20500: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpSentences(0) + newWordFragment': false,  // 20521: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpSentences(0) + progress': false,  // 20522: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpSentences(0) + stopBegin': false,  // 20523: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpSentences(0) + stopFinish': false,  // 20524: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpSentences(0) + done': false,  // 20525: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpSentences(1) + newWordFragment': false,  // 20546: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpSentences(1) + progress': false,  // 20547: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpSentences(1) + stopBegin': false,  // 20548: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpSentences(1) + stopFinish': false,  // 20549: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpSentences(1) + done': false,  // 20550: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpSentences(3) + newWordFragment': false,  // 20571: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpSentences(3) + progress': false,  // 20572: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpSentences(3) + stopBegin': false,  // 20573: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpSentences(3) + stopFinish': false,  // 20574: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > jumpSentences(3) + done': false,  // 20575: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > nextWord(null) + newWordFragment': false,  // 20621: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > nextWord(null) + progress': false,  // 20622: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > nextWord(null) + stopBegin': false,  // 20623: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > nextWord(null) + stopFinish': false,  // 20624: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > nextWord(null) + done': false,  // 20625: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > nextSentence(null) + newWordFragment': false,  // 20646: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > nextSentence(null) + progress': false,  // 20647: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > nextSentence(null) + stopBegin': false,  // 20648: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > nextSentence(null) + stopFinish': false,  // 20649: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > nextSentence(null) + done': false,  // 20650: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > prevWord(null) + newWordFragment': false,  // 20671: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > prevWord(null) + progress': false,  // 20672: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > prevWord(null) + stopBegin': false,  // 20673: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > prevWord(null) + stopFinish': false,  // 20674: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > prevWord(null) + done': false,  // 20675: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > prevSentence(null) + newWordFragment': false,  // 20696: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > prevSentence(null) + progress': false,  // 20697: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > prevSentence(null) + stopBegin': false,  // 20698: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > prevSentence(null) + stopFinish': false,  // 20699: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopBegin > prevSentence(null) + done': false,  // 20700: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > play(null) + playBegin': false,  // 20776: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > play(null) + playFinish': false,  // 20777: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > play(null) + restartBegin': false,  // 20780: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > play(null) + restartFinish': false,  // 20781: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > toggle(null) + playBegin': false,  // 20801: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > toggle(null) + playFinish': false,  // 20802: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > toggle(null) + restartBegin': false,  // 20805: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > toggle(null) + restartFinish': false,  // 20806: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > rewind(null) + newWordFragment': false,  // 20946: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > rewind(null) + progress': false,  // 20947: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > fastForward(null) + newWordFragment': false,  // 20971: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > fastForward(null) + progress': false,  // 20972: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > once([0,0,-2]) + newWordFragment': false,  // 20996: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > once([0,0,-2]) + progress': false,  // 20997: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > once([0,0,-2]) + stopBegin': false,  // 20998: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > once([0,0,-2]) + stopFinish': false,  // 20999: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > once([0,0,-2]) + done': false,  // 21000: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > once([0,0,2]) + newWordFragment': false,  // 21021: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > once([0,0,2]) + progress': false,  // 21022: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > once([0,0,2]) + stopBegin': false,  // 21023: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > once([0,0,2]) + stopFinish': false,  // 21024: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > once([0,0,2]) + done': false,  // 21025: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > current(null) + newWordFragment': false,  // 21046: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > current(null) + progress': false,  // 21047: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > current(null) + stopBegin': false,  // 21048: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > current(null) + stopFinish': false,  // 21049: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > current(null) + done': false,  // 21050: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpWords(-3) + newWordFragment': false,  // 21221: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpWords(-3) + progress': false,  // 21222: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpWords(-3) + stopBegin': false,  // 21223: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpWords(-3) + stopFinish': false,  // 21224: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpWords(-3) + done': false,  // 21225: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpWords(-1) + newWordFragment': false,  // 21246: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpWords(-1) + progress': false,  // 21247: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpWords(-1) + stopBegin': false,  // 21248: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpWords(-1) + stopFinish': false,  // 21249: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpWords(-1) + done': false,  // 21250: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpWords(0) + newWordFragment': false,  // 21271: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpWords(0) + progress': false,  // 21272: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpWords(0) + stopBegin': false,  // 21273: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpWords(0) + stopFinish': false,  // 21274: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpWords(0) + done': false,  // 21275: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpWords(4) + newWordFragment': false,  // 21296: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpWords(4) + progress': false,  // 21297: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpWords(4) + stopBegin': false,  // 21298: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpWords(4) + stopFinish': false,  // 21299: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpWords(4) + done': false,  // 21300: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpSentences(-1) + newWordFragment': false,  // 21396: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpSentences(-1) + progress': false,  // 21397: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpSentences(-1) + stopBegin': false,  // 21398: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpSentences(-1) + stopFinish': false,  // 21399: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpSentences(-1) + done': false,  // 21400: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpSentences(0) + newWordFragment': false,  // 21421: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpSentences(0) + progress': false,  // 21422: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpSentences(0) + stopBegin': false,  // 21423: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpSentences(0) + stopFinish': false,  // 21424: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpSentences(0) + done': false,  // 21425: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpSentences(1) + newWordFragment': false,  // 21446: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpSentences(1) + progress': false,  // 21447: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpSentences(1) + stopBegin': false,  // 21448: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpSentences(1) + stopFinish': false,  // 21449: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpSentences(1) + done': false,  // 21450: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpSentences(3) + newWordFragment': false,  // 21471: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpSentences(3) + progress': false,  // 21472: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpSentences(3) + stopBegin': false,  // 21473: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpSentences(3) + stopFinish': false,  // 21474: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > jumpSentences(3) + done': false,  // 21475: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > nextWord(null) + newWordFragment': false,  // 21521: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > nextWord(null) + progress': false,  // 21522: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > nextWord(null) + stopBegin': false,  // 21523: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > nextWord(null) + stopFinish': false,  // 21524: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > nextWord(null) + done': false,  // 21525: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > nextSentence(null) + newWordFragment': false,  // 21546: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > nextSentence(null) + progress': false,  // 21547: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > nextSentence(null) + stopBegin': false,  // 21548: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > nextSentence(null) + stopFinish': false,  // 21549: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > nextSentence(null) + done': false,  // 21550: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > prevWord(null) + newWordFragment': false,  // 21571: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > prevWord(null) + progress': false,  // 21572: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > prevWord(null) + stopBegin': false,  // 21573: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > prevWord(null) + stopFinish': false,  // 21574: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > prevWord(null) + done': false,  // 21575: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > prevSentence(null) + newWordFragment': false,  // 21596: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > prevSentence(null) + progress': false,  // 21597: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > prevSentence(null) + stopBegin': false,  // 21598: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > prevSentence(null) + stopFinish': false,  // 21599: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + stopFinish > prevSentence(null) + done': false,  // 21600: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > play(null) + playBegin': false,  // 21676: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > play(null) + playFinish': false,  // 21677: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > play(null) + restartBegin': false,  // 21680: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > play(null) + restartFinish': false,  // 21681: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > toggle(null) + playBegin': false,  // 21701: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > toggle(null) + playFinish': false,  // 21702: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > toggle(null) + restartBegin': false,  // 21705: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > toggle(null) + restartFinish': false,  // 21706: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > rewind(null) + newWordFragment': false,  // 21846: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > rewind(null) + progress': false,  // 21847: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > fastForward(null) + newWordFragment': false,  // 21871: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > fastForward(null) + progress': false,  // 21872: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > once([0,0,-2]) + newWordFragment': false,  // 21896: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > once([0,0,-2]) + progress': false,  // 21897: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > once([0,0,-2]) + stopBegin': false,  // 21898: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > once([0,0,-2]) + stopFinish': false,  // 21899: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > once([0,0,-2]) + done': false,  // 21900: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > once([0,0,2]) + newWordFragment': false,  // 21921: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > once([0,0,2]) + progress': false,  // 21922: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > once([0,0,2]) + stopBegin': false,  // 21923: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > once([0,0,2]) + stopFinish': false,  // 21924: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > once([0,0,2]) + done': false,  // 21925: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > current(null) + newWordFragment': false,  // 21946: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > current(null) + progress': false,  // 21947: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > current(null) + stopBegin': false,  // 21948: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > current(null) + stopFinish': false,  // 21949: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > current(null) + done': false,  // 21950: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpWords(-3) + newWordFragment': false,  // 22121: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpWords(-3) + progress': false,  // 22122: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpWords(-3) + stopBegin': false,  // 22123: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpWords(-3) + stopFinish': false,  // 22124: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpWords(-3) + done': false,  // 22125: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpWords(-1) + newWordFragment': false,  // 22146: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpWords(-1) + progress': false,  // 22147: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpWords(-1) + stopBegin': false,  // 22148: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpWords(-1) + stopFinish': false,  // 22149: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpWords(-1) + done': false,  // 22150: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpWords(0) + newWordFragment': false,  // 22171: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpWords(0) + progress': false,  // 22172: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpWords(0) + stopBegin': false,  // 22173: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpWords(0) + stopFinish': false,  // 22174: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpWords(0) + done': false,  // 22175: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpWords(4) + newWordFragment': false,  // 22196: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpWords(4) + progress': false,  // 22197: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpWords(4) + stopBegin': false,  // 22198: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpWords(4) + stopFinish': false,  // 22199: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpWords(4) + done': false,  // 22200: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpSentences(-1) + newWordFragment': false,  // 22296: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpSentences(-1) + progress': false,  // 22297: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpSentences(-1) + stopBegin': false,  // 22298: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpSentences(-1) + stopFinish': false,  // 22299: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpSentences(-1) + done': false,  // 22300: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpSentences(0) + newWordFragment': false,  // 22321: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpSentences(0) + progress': false,  // 22322: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpSentences(0) + stopBegin': false,  // 22323: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpSentences(0) + stopFinish': false,  // 22324: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpSentences(0) + done': false,  // 22325: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpSentences(1) + newWordFragment': false,  // 22346: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpSentences(1) + progress': false,  // 22347: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpSentences(1) + stopBegin': false,  // 22348: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpSentences(1) + stopFinish': false,  // 22349: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpSentences(1) + done': false,  // 22350: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpSentences(3) + newWordFragment': false,  // 22371: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpSentences(3) + progress': false,  // 22372: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpSentences(3) + stopBegin': false,  // 22373: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpSentences(3) + stopFinish': false,  // 22374: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > jumpSentences(3) + done': false,  // 22375: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > nextWord(null) + newWordFragment': false,  // 22421: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > nextWord(null) + progress': false,  // 22422: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > nextWord(null) + stopBegin': false,  // 22423: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > nextWord(null) + stopFinish': false,  // 22424: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > nextWord(null) + done': false,  // 22425: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > nextSentence(null) + newWordFragment': false,  // 22446: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > nextSentence(null) + progress': false,  // 22447: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > nextSentence(null) + stopBegin': false,  // 22448: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > nextSentence(null) + stopFinish': false,  // 22449: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > nextSentence(null) + done': false,  // 22450: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > prevWord(null) + newWordFragment': false,  // 22471: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > prevWord(null) + progress': false,  // 22472: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > prevWord(null) + stopBegin': false,  // 22473: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > prevWord(null) + stopFinish': false,  // 22474: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > prevWord(null) + done': false,  // 22475: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > prevSentence(null) + newWordFragment': false,  // 22496: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > prevSentence(null) + progress': false,  // 22497: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > prevSentence(null) + stopBegin': false,  // 22498: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > prevSentence(null) + stopFinish': false,  // 22499: ?? (skip results are different than combos)
		'skip-combos: jumpSentences(100) + done > prevSentence(null) + done': false,  // 22500: ?? (skip results are different than combos)



		// ============== nextWord ==============
		// #?: Almost like jumpWord(3), but prevWord, jump*(-), rewind(), etc. (what others?) won't be different
		'skip-combos: nextWord(null) + onceBegin > play(null) + progress': false,  // 9097: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceBegin > toggle(null) + playBegin': false,  // 9101: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceBegin > toggle(null) + playFinish': false,  // 9102: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceBegin > toggle(null) + pauseBegin': false,  // 9107: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceBegin > toggle(null) + pauseFinish': false,  // 9108: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceBegin > toggle(null) + revertBegin': false,  // 9113: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceBegin > toggle(null) + revertFinish': false,  // 9114: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceBegin > toggle(null) + loopBegin': false,  // 9119: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceBegin > toggle(null) + loopFinish': false,  // 9120: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceBegin > toggle(null) + newWordFragment': false,  // 9121: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceBegin > toggle(null) + progress': false,  // 9122: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceBegin > toggle(null) + stopBegin': false,  // 9123: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceBegin > toggle(null) + stopFinish': false,  // 9124: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceBegin > toggle(null) + done': false,  // 9125: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceBegin > fastForward(null) + newWordFragment': false,  // 9271: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceBegin > fastForward(null) + progress': false,  // 9272: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceBegin > once([0,0,2]) + newWordFragment': false,  // 9321: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceBegin > once([0,0,2]) + progress': false,  // 9322: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceBegin > current(null) + progress': false,  // 9347: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceBegin > jumpWords(0) + progress': false,  // 9572: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceBegin > jumpWords(4) + progress': false,  // 9597: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceBegin > jumpSentences(0) + progress': false,  // 9722: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceBegin > nextWord(null) + newWordFragment': false,  // 9821: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceBegin > nextWord(null) + progress': false,  // 9822: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceFinish > play(null) + progress': false,  // 9997: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceFinish > toggle(null) + playBegin': false,  // 10001: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceFinish > toggle(null) + playFinish': false,  // 10002: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceFinish > toggle(null) + pauseBegin': false,  // 10007: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceFinish > toggle(null) + pauseFinish': false,  // 10008: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceFinish > toggle(null) + revertBegin': false,  // 10013: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceFinish > toggle(null) + revertFinish': false,  // 10014: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceFinish > toggle(null) + loopBegin': false,  // 10019: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceFinish > toggle(null) + loopFinish': false,  // 10020: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceFinish > toggle(null) + newWordFragment': false,  // 10021: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceFinish > toggle(null) + progress': false,  // 10022: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceFinish > toggle(null) + stopBegin': false,  // 10023: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceFinish > toggle(null) + stopFinish': false,  // 10024: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceFinish > toggle(null) + done': false,  // 10025: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceFinish > fastForward(null) + newWordFragment': false,  // 10171: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceFinish > fastForward(null) + progress': false,  // 10172: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceFinish > once([0,0,2]) + newWordFragment': false,  // 10221: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceFinish > once([0,0,2]) + progress': false,  // 10222: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceFinish > current(null) + progress': false,  // 10247: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceFinish > jumpWords(0) + progress': false,  // 10472: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceFinish > jumpWords(4) + progress': false,  // 10497: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceFinish > jumpSentences(0) + progress': false,  // 10622: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceFinish > nextWord(null) + newWordFragment': false,  // 10721: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + onceFinish > nextWord(null) + progress': false,  // 10722: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopBegin > play(null) + progress': false,  // 16297: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopBegin > toggle(null) + playBegin': false,  // 16301: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopBegin > toggle(null) + playFinish': false,  // 16302: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopBegin > toggle(null) + pauseBegin': false,  // 16307: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopBegin > toggle(null) + pauseFinish': false,  // 16308: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopBegin > toggle(null) + revertBegin': false,  // 16313: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopBegin > toggle(null) + revertFinish': false,  // 16314: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopBegin > toggle(null) + loopBegin': false,  // 16319: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopBegin > toggle(null) + loopFinish': false,  // 16320: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopBegin > toggle(null) + newWordFragment': false,  // 16321: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopBegin > toggle(null) + progress': false,  // 16322: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopBegin > toggle(null) + stopBegin': false,  // 16323: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopBegin > toggle(null) + stopFinish': false,  // 16324: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopBegin > toggle(null) + done': false,  // 16325: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopBegin > fastForward(null) + newWordFragment': false,  // 16471: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopBegin > fastForward(null) + progress': false,  // 16472: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopBegin > once([0,0,2]) + newWordFragment': false,  // 16521: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopBegin > once([0,0,2]) + progress': false,  // 16522: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopBegin > current(null) + progress': false,  // 16547: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopBegin > jumpWords(0) + progress': false,  // 16772: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopBegin > jumpWords(4) + progress': false,  // 16797: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopBegin > jumpSentences(0) + progress': false,  // 16922: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopBegin > nextWord(null) + newWordFragment': false,  // 17021: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopBegin > nextWord(null) + progress': false,  // 17022: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopFinish > play(null) + progress': false,  // 17197: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopFinish > toggle(null) + playBegin': false,  // 17201: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopFinish > toggle(null) + playFinish': false,  // 17202: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopFinish > toggle(null) + pauseBegin': false,  // 17207: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopFinish > toggle(null) + pauseFinish': false,  // 17208: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopFinish > toggle(null) + revertBegin': false,  // 17213: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopFinish > toggle(null) + revertFinish': false,  // 17214: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopFinish > toggle(null) + loopBegin': false,  // 17219: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopFinish > toggle(null) + loopFinish': false,  // 17220: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopFinish > toggle(null) + newWordFragment': false,  // 17221: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopFinish > toggle(null) + progress': false,  // 17222: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopFinish > toggle(null) + stopBegin': false,  // 17223: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopFinish > toggle(null) + stopFinish': false,  // 17224: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopFinish > toggle(null) + done': false,  // 17225: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopFinish > fastForward(null) + newWordFragment': false,  // 17371: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopFinish > fastForward(null) + progress': false,  // 17372: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopFinish > once([0,0,2]) + newWordFragment': false,  // 17421: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopFinish > once([0,0,2]) + progress': false,  // 17422: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopFinish > current(null) + progress': false,  // 17447: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopFinish > jumpWords(0) + progress': false,  // 17672: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopFinish > jumpWords(4) + progress': false,  // 17697: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopFinish > jumpSentences(0) + progress': false,  // 17822: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopFinish > nextWord(null) + newWordFragment': false,  // 17921: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + loopFinish > nextWord(null) + progress': false,  // 17922: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + newWordFragment > play(null) + progress': false,  // 18097: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + newWordFragment > toggle(null) + playBegin': false,  // 18101: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + newWordFragment > toggle(null) + playFinish': false,  // 18102: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + newWordFragment > toggle(null) + pauseBegin': false,  // 18107: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + newWordFragment > toggle(null) + pauseFinish': false,  // 18108: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + newWordFragment > toggle(null) + revertBegin': false,  // 18113: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + newWordFragment > toggle(null) + revertFinish': false,  // 18114: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + newWordFragment > toggle(null) + loopBegin': false,  // 18119: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + newWordFragment > toggle(null) + loopFinish': false,  // 18120: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + newWordFragment > toggle(null) + newWordFragment': false,  // 18121: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + newWordFragment > toggle(null) + progress': false,  // 18122: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + newWordFragment > toggle(null) + stopBegin': false,  // 18123: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + newWordFragment > toggle(null) + stopFinish': false,  // 18124: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + newWordFragment > toggle(null) + done': false,  // 18125: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + newWordFragment > fastForward(null) + newWordFragment': false,  // 18271: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + newWordFragment > fastForward(null) + progress': false,  // 18272: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + newWordFragment > once([0,0,2]) + newWordFragment': false,  // 18321: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + newWordFragment > once([0,0,2]) + progress': false,  // 18322: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + newWordFragment > current(null) + progress': false,  // 18347: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + newWordFragment > jumpWords(0) + progress': false,  // 18572: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + newWordFragment > jumpWords(4) + progress': false,  // 18597: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + newWordFragment > jumpSentences(0) + progress': false,  // 18722: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + newWordFragment > nextWord(null) + newWordFragment': false,  // 18821: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + newWordFragment > nextWord(null) + progress': false,  // 18822: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + progress > play(null) + progress': false,  // 18997: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + progress > toggle(null) + playBegin': false,  // 19001: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + progress > toggle(null) + playFinish': false,  // 19002: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + progress > toggle(null) + pauseBegin': false,  // 19007: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + progress > toggle(null) + pauseFinish': false,  // 19008: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + progress > toggle(null) + revertBegin': false,  // 19013: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + progress > toggle(null) + revertFinish': false,  // 19014: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + progress > toggle(null) + loopBegin': false,  // 19019: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + progress > toggle(null) + loopFinish': false,  // 19020: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + progress > toggle(null) + newWordFragment': false,  // 19021: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + progress > toggle(null) + progress': false,  // 19022: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + progress > toggle(null) + stopBegin': false,  // 19023: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + progress > toggle(null) + stopFinish': false,  // 19024: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + progress > toggle(null) + done': false,  // 19025: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + progress > fastForward(null) + newWordFragment': false,  // 19171: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + progress > fastForward(null) + progress': false,  // 19172: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + progress > once([0,0,2]) + newWordFragment': false,  // 19221: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + progress > once([0,0,2]) + progress': false,  // 19222: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + progress > current(null) + progress': false,  // 19247: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + progress > jumpWords(0) + progress': false,  // 19472: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + progress > jumpWords(4) + progress': false,  // 19497: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + progress > jumpSentences(0) + progress': false,  // 19622: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + progress > nextWord(null) + newWordFragment': false,  // 19721: ?? (skip results are different than combos)
		'skip-combos: nextWord(null) + progress > nextWord(null) + progress': false,  // 19722: ?? (skip results are different than combos)


		// ============== nextSentence ==============
		// #?: like jumpSentence(1)
		'skip-combos: nextSentence(null) + onceBegin > play(null) + newWordFragment': false,  // 9096: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > play(null) + progress': false,  // 9097: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > toggle(null) + playBegin': false,  // 9101: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > toggle(null) + playFinish': false,  // 9102: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > toggle(null) + pauseBegin': false,  // 9107: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > toggle(null) + pauseFinish': false,  // 9108: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > toggle(null) + revertBegin': false,  // 9113: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > toggle(null) + revertFinish': false,  // 9114: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > toggle(null) + loopBegin': false,  // 9119: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > toggle(null) + loopFinish': false,  // 9120: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > toggle(null) + newWordFragment': false,  // 9121: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > toggle(null) + progress': false,  // 9122: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > toggle(null) + stopBegin': false,  // 9123: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > toggle(null) + stopFinish': false,  // 9124: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > toggle(null) + done': false,  // 9125: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > rewind(null) + newWordFragment': false,  // 9246: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > rewind(null) + progress': false,  // 9247: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > fastForward(null) + newWordFragment': false,  // 9271: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > fastForward(null) + progress': false,  // 9272: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > once([0,0,-2]) + newWordFragment': false,  // 9296: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > once([0,0,-2]) + progress': false,  // 9297: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > once([0,0,-2]) + stopBegin': false,  // 9298: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > once([0,0,-2]) + stopFinish': false,  // 9299: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > once([0,0,-2]) + done': false,  // 9300: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > once([0,0,2]) + newWordFragment': false,  // 9321: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > once([0,0,2]) + progress': false,  // 9322: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > current(null) + newWordFragment': false,  // 9346: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > current(null) + progress': false,  // 9347: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > jumpWords(-3) + newWordFragment': false,  // 9521: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > jumpWords(-3) + progress': false,  // 9522: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > jumpWords(-3) + stopBegin': false,  // 9523: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > jumpWords(-3) + stopFinish': false,  // 9524: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > jumpWords(-3) + done': false,  // 9525: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > jumpWords(-1) + newWordFragment': false,  // 9546: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > jumpWords(-1) + progress': false,  // 9547: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > jumpWords(-1) + stopBegin': false,  // 9548: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > jumpWords(-1) + stopFinish': false,  // 9549: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > jumpWords(-1) + done': false,  // 9550: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > jumpWords(0) + newWordFragment': false,  // 9571: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > jumpWords(0) + progress': false,  // 9572: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > jumpWords(4) + newWordFragment': false,  // 9596: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > jumpWords(4) + progress': false,  // 9597: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > jumpSentences(0) + newWordFragment': false,  // 9721: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > jumpSentences(0) + progress': false,  // 9722: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > jumpSentences(1) + newWordFragment': false,  // 9746: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > jumpSentences(1) + progress': false,  // 9747: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > jumpSentences(3) + newWordFragment': false,  // 9771: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > jumpSentences(3) + progress': false,  // 9772: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > jumpSentences(3) + stopBegin': false,  // 9773: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > jumpSentences(3) + stopFinish': false,  // 9774: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > jumpSentences(3) + done': false,  // 9775: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > nextWord(null) + newWordFragment': false,  // 9821: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > nextWord(null) + progress': false,  // 9822: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > nextSentence(null) + newWordFragment': false,  // 9846: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > nextSentence(null) + progress': false,  // 9847: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > prevWord(null) + newWordFragment': false,  // 9871: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > prevWord(null) + progress': false,  // 9872: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > prevWord(null) + stopBegin': false,  // 9873: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > prevWord(null) + stopFinish': false,  // 9874: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceBegin > prevWord(null) + done': false,  // 9875: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > play(null) + newWordFragment': false,  // 9996: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > play(null) + progress': false,  // 9997: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > toggle(null) + playBegin': false,  // 10001: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > toggle(null) + playFinish': false,  // 10002: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > toggle(null) + pauseBegin': false,  // 10007: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > toggle(null) + pauseFinish': false,  // 10008: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > toggle(null) + revertBegin': false,  // 10013: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > toggle(null) + revertFinish': false,  // 10014: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > toggle(null) + loopBegin': false,  // 10019: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > toggle(null) + loopFinish': false,  // 10020: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > toggle(null) + newWordFragment': false,  // 10021: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > toggle(null) + progress': false,  // 10022: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > toggle(null) + stopBegin': false,  // 10023: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > toggle(null) + stopFinish': false,  // 10024: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > toggle(null) + done': false,  // 10025: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > rewind(null) + newWordFragment': false,  // 10146: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > rewind(null) + progress': false,  // 10147: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > fastForward(null) + newWordFragment': false,  // 10171: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > fastForward(null) + progress': false,  // 10172: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > once([0,0,-2]) + newWordFragment': false,  // 10196: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > once([0,0,-2]) + progress': false,  // 10197: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > once([0,0,-2]) + stopBegin': false,  // 10198: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > once([0,0,-2]) + stopFinish': false,  // 10199: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > once([0,0,-2]) + done': false,  // 10200: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > once([0,0,2]) + newWordFragment': false,  // 10221: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > once([0,0,2]) + progress': false,  // 10222: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > current(null) + newWordFragment': false,  // 10246: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > current(null) + progress': false,  // 10247: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > jumpWords(-3) + newWordFragment': false,  // 10421: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > jumpWords(-3) + progress': false,  // 10422: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > jumpWords(-3) + stopBegin': false,  // 10423: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > jumpWords(-3) + stopFinish': false,  // 10424: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > jumpWords(-3) + done': false,  // 10425: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > jumpWords(-1) + newWordFragment': false,  // 10446: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > jumpWords(-1) + progress': false,  // 10447: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > jumpWords(-1) + stopBegin': false,  // 10448: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > jumpWords(-1) + stopFinish': false,  // 10449: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > jumpWords(-1) + done': false,  // 10450: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > jumpWords(0) + newWordFragment': false,  // 10471: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > jumpWords(0) + progress': false,  // 10472: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > jumpWords(4) + newWordFragment': false,  // 10496: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > jumpWords(4) + progress': false,  // 10497: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > jumpSentences(0) + newWordFragment': false,  // 10621: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > jumpSentences(0) + progress': false,  // 10622: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > jumpSentences(1) + newWordFragment': false,  // 10646: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > jumpSentences(1) + progress': false,  // 10647: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > jumpSentences(3) + newWordFragment': false,  // 10671: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > jumpSentences(3) + progress': false,  // 10672: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > jumpSentences(3) + stopBegin': false,  // 10673: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > jumpSentences(3) + stopFinish': false,  // 10674: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > jumpSentences(3) + done': false,  // 10675: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > nextWord(null) + newWordFragment': false,  // 10721: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > nextWord(null) + progress': false,  // 10722: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > nextSentence(null) + newWordFragment': false,  // 10746: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > nextSentence(null) + progress': false,  // 10747: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > prevWord(null) + newWordFragment': false,  // 10771: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > prevWord(null) + progress': false,  // 10772: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > prevWord(null) + stopBegin': false,  // 10773: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > prevWord(null) + stopFinish': false,  // 10774: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + onceFinish > prevWord(null) + done': false,  // 10775: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > play(null) + newWordFragment': false,  // 16296: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > play(null) + progress': false,  // 16297: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > toggle(null) + playBegin': false,  // 16301: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > toggle(null) + playFinish': false,  // 16302: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > toggle(null) + pauseBegin': false,  // 16307: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > toggle(null) + pauseFinish': false,  // 16308: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > toggle(null) + revertBegin': false,  // 16313: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > toggle(null) + revertFinish': false,  // 16314: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > toggle(null) + loopBegin': false,  // 16319: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > toggle(null) + loopFinish': false,  // 16320: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > toggle(null) + newWordFragment': false,  // 16321: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > toggle(null) + progress': false,  // 16322: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > toggle(null) + stopBegin': false,  // 16323: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > toggle(null) + stopFinish': false,  // 16324: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > toggle(null) + done': false,  // 16325: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > rewind(null) + newWordFragment': false,  // 16446: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > rewind(null) + progress': false,  // 16447: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > fastForward(null) + newWordFragment': false,  // 16471: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > fastForward(null) + progress': false,  // 16472: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > once([0,0,-2]) + newWordFragment': false,  // 16496: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > once([0,0,-2]) + progress': false,  // 16497: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > once([0,0,-2]) + stopBegin': false,  // 16498: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > once([0,0,-2]) + stopFinish': false,  // 16499: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > once([0,0,-2]) + done': false,  // 16500: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > once([0,0,2]) + newWordFragment': false,  // 16521: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > once([0,0,2]) + progress': false,  // 16522: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > current(null) + newWordFragment': false,  // 16546: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > current(null) + progress': false,  // 16547: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > jumpWords(-3) + newWordFragment': false,  // 16721: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > jumpWords(-3) + progress': false,  // 16722: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > jumpWords(-3) + stopBegin': false,  // 16723: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > jumpWords(-3) + stopFinish': false,  // 16724: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > jumpWords(-3) + done': false,  // 16725: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > jumpWords(-1) + newWordFragment': false,  // 16746: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > jumpWords(-1) + progress': false,  // 16747: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > jumpWords(-1) + stopBegin': false,  // 16748: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > jumpWords(-1) + stopFinish': false,  // 16749: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > jumpWords(-1) + done': false,  // 16750: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > jumpWords(0) + newWordFragment': false,  // 16771: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > jumpWords(0) + progress': false,  // 16772: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > jumpWords(4) + newWordFragment': false,  // 16796: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > jumpWords(4) + progress': false,  // 16797: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > jumpSentences(0) + newWordFragment': false,  // 16921: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > jumpSentences(0) + progress': false,  // 16922: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > jumpSentences(1) + newWordFragment': false,  // 16946: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > jumpSentences(1) + progress': false,  // 16947: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > jumpSentences(3) + newWordFragment': false,  // 16971: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > jumpSentences(3) + progress': false,  // 16972: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > jumpSentences(3) + stopBegin': false,  // 16973: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > jumpSentences(3) + stopFinish': false,  // 16974: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > jumpSentences(3) + done': false,  // 16975: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > nextWord(null) + newWordFragment': false,  // 17021: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > nextWord(null) + progress': false,  // 17022: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > nextSentence(null) + newWordFragment': false,  // 17046: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > nextSentence(null) + progress': false,  // 17047: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > prevWord(null) + newWordFragment': false,  // 17071: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > prevWord(null) + progress': false,  // 17072: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > prevWord(null) + stopBegin': false,  // 17073: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > prevWord(null) + stopFinish': false,  // 17074: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopBegin > prevWord(null) + done': false,  // 17075: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > play(null) + newWordFragment': false,  // 17196: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > play(null) + progress': false,  // 17197: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > toggle(null) + playBegin': false,  // 17201: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > toggle(null) + playFinish': false,  // 17202: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > toggle(null) + pauseBegin': false,  // 17207: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > toggle(null) + pauseFinish': false,  // 17208: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > toggle(null) + revertBegin': false,  // 17213: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > toggle(null) + revertFinish': false,  // 17214: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > toggle(null) + loopBegin': false,  // 17219: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > toggle(null) + loopFinish': false,  // 17220: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > toggle(null) + newWordFragment': false,  // 17221: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > toggle(null) + progress': false,  // 17222: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > toggle(null) + stopBegin': false,  // 17223: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > toggle(null) + stopFinish': false,  // 17224: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > toggle(null) + done': false,  // 17225: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > rewind(null) + newWordFragment': false,  // 17346: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > rewind(null) + progress': false,  // 17347: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > fastForward(null) + newWordFragment': false,  // 17371: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > fastForward(null) + progress': false,  // 17372: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > once([0,0,-2]) + newWordFragment': false,  // 17396: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > once([0,0,-2]) + progress': false,  // 17397: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > once([0,0,-2]) + stopBegin': false,  // 17398: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > once([0,0,-2]) + stopFinish': false,  // 17399: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > once([0,0,-2]) + done': false,  // 17400: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > once([0,0,2]) + newWordFragment': false,  // 17421: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > once([0,0,2]) + progress': false,  // 17422: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > current(null) + newWordFragment': false,  // 17446: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > current(null) + progress': false,  // 17447: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > jumpWords(-3) + newWordFragment': false,  // 17621: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > jumpWords(-3) + progress': false,  // 17622: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > jumpWords(-3) + stopBegin': false,  // 17623: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > jumpWords(-3) + stopFinish': false,  // 17624: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > jumpWords(-3) + done': false,  // 17625: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > jumpWords(-1) + newWordFragment': false,  // 17646: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > jumpWords(-1) + progress': false,  // 17647: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > jumpWords(-1) + stopBegin': false,  // 17648: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > jumpWords(-1) + stopFinish': false,  // 17649: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > jumpWords(-1) + done': false,  // 17650: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > jumpWords(0) + newWordFragment': false,  // 17671: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > jumpWords(0) + progress': false,  // 17672: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > jumpWords(4) + newWordFragment': false,  // 17696: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > jumpWords(4) + progress': false,  // 17697: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > jumpSentences(0) + newWordFragment': false,  // 17821: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > jumpSentences(0) + progress': false,  // 17822: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > jumpSentences(1) + newWordFragment': false,  // 17846: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > jumpSentences(1) + progress': false,  // 17847: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > jumpSentences(3) + newWordFragment': false,  // 17871: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > jumpSentences(3) + progress': false,  // 17872: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > jumpSentences(3) + stopBegin': false,  // 17873: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > jumpSentences(3) + stopFinish': false,  // 17874: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > jumpSentences(3) + done': false,  // 17875: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > nextWord(null) + newWordFragment': false,  // 17921: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > nextWord(null) + progress': false,  // 17922: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > nextSentence(null) + newWordFragment': false,  // 17946: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > nextSentence(null) + progress': false,  // 17947: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > prevWord(null) + newWordFragment': false,  // 17971: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > prevWord(null) + progress': false,  // 17972: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > prevWord(null) + stopBegin': false,  // 17973: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > prevWord(null) + stopFinish': false,  // 17974: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + loopFinish > prevWord(null) + done': false,  // 17975: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > play(null) + newWordFragment': false,  // 18096: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > play(null) + progress': false,  // 18097: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > toggle(null) + playBegin': false,  // 18101: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > toggle(null) + playFinish': false,  // 18102: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > toggle(null) + pauseBegin': false,  // 18107: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > toggle(null) + pauseFinish': false,  // 18108: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > toggle(null) + revertBegin': false,  // 18113: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > toggle(null) + revertFinish': false,  // 18114: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > toggle(null) + loopBegin': false,  // 18119: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > toggle(null) + loopFinish': false,  // 18120: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > toggle(null) + newWordFragment': false,  // 18121: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > toggle(null) + progress': false,  // 18122: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > toggle(null) + stopBegin': false,  // 18123: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > toggle(null) + stopFinish': false,  // 18124: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > toggle(null) + done': false,  // 18125: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > rewind(null) + newWordFragment': false,  // 18246: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > rewind(null) + progress': false,  // 18247: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > fastForward(null) + newWordFragment': false,  // 18271: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > fastForward(null) + progress': false,  // 18272: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > once([0,0,-2]) + newWordFragment': false,  // 18296: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > once([0,0,-2]) + progress': false,  // 18297: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > once([0,0,-2]) + stopBegin': false,  // 18298: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > once([0,0,-2]) + stopFinish': false,  // 18299: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > once([0,0,-2]) + done': false,  // 18300: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > once([0,0,2]) + newWordFragment': false,  // 18321: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > once([0,0,2]) + progress': false,  // 18322: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > current(null) + newWordFragment': false,  // 18346: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > current(null) + progress': false,  // 18347: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > jumpWords(-3) + newWordFragment': false,  // 18521: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > jumpWords(-3) + progress': false,  // 18522: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > jumpWords(-3) + stopBegin': false,  // 18523: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > jumpWords(-3) + stopFinish': false,  // 18524: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > jumpWords(-3) + done': false,  // 18525: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > jumpWords(-1) + newWordFragment': false,  // 18546: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > jumpWords(-1) + progress': false,  // 18547: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > jumpWords(-1) + stopBegin': false,  // 18548: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > jumpWords(-1) + stopFinish': false,  // 18549: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > jumpWords(-1) + done': false,  // 18550: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > jumpWords(0) + newWordFragment': false,  // 18571: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > jumpWords(0) + progress': false,  // 18572: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > jumpWords(4) + newWordFragment': false,  // 18596: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > jumpWords(4) + progress': false,  // 18597: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > jumpSentences(0) + newWordFragment': false,  // 18721: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > jumpSentences(0) + progress': false,  // 18722: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > jumpSentences(1) + newWordFragment': false,  // 18746: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > jumpSentences(1) + progress': false,  // 18747: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > jumpSentences(3) + newWordFragment': false,  // 18771: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > jumpSentences(3) + progress': false,  // 18772: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > jumpSentences(3) + stopBegin': false,  // 18773: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > jumpSentences(3) + stopFinish': false,  // 18774: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > jumpSentences(3) + done': false,  // 18775: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > nextWord(null) + newWordFragment': false,  // 18821: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > nextWord(null) + progress': false,  // 18822: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > nextSentence(null) + newWordFragment': false,  // 18846: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > nextSentence(null) + progress': false,  // 18847: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > prevWord(null) + newWordFragment': false,  // 18871: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > prevWord(null) + progress': false,  // 18872: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > prevWord(null) + stopBegin': false,  // 18873: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > prevWord(null) + stopFinish': false,  // 18874: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + newWordFragment > prevWord(null) + done': false,  // 18875: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > play(null) + newWordFragment': false,  // 18996: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > play(null) + progress': false,  // 18997: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > toggle(null) + playBegin': false,  // 19001: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > toggle(null) + playFinish': false,  // 19002: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > toggle(null) + pauseBegin': false,  // 19007: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > toggle(null) + pauseFinish': false,  // 19008: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > toggle(null) + revertBegin': false,  // 19013: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > toggle(null) + revertFinish': false,  // 19014: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > toggle(null) + loopBegin': false,  // 19019: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > toggle(null) + loopFinish': false,  // 19020: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > toggle(null) + newWordFragment': false,  // 19021: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > toggle(null) + progress': false,  // 19022: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > toggle(null) + stopBegin': false,  // 19023: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > toggle(null) + stopFinish': false,  // 19024: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > toggle(null) + done': false,  // 19025: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > rewind(null) + newWordFragment': false,  // 19146: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > rewind(null) + progress': false,  // 19147: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > fastForward(null) + newWordFragment': false,  // 19171: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > fastForward(null) + progress': false,  // 19172: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > once([0,0,-2]) + newWordFragment': false,  // 19196: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > once([0,0,-2]) + progress': false,  // 19197: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > once([0,0,-2]) + stopBegin': false,  // 19198: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > once([0,0,-2]) + stopFinish': false,  // 19199: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > once([0,0,-2]) + done': false,  // 19200: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > once([0,0,2]) + newWordFragment': false,  // 19221: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > once([0,0,2]) + progress': false,  // 19222: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > current(null) + newWordFragment': false,  // 19246: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > current(null) + progress': false,  // 19247: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > jumpWords(-3) + newWordFragment': false,  // 19421: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > jumpWords(-3) + progress': false,  // 19422: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > jumpWords(-3) + stopBegin': false,  // 19423: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > jumpWords(-3) + stopFinish': false,  // 19424: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > jumpWords(-3) + done': false,  // 19425: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > jumpWords(-1) + newWordFragment': false,  // 19446: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > jumpWords(-1) + progress': false,  // 19447: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > jumpWords(-1) + stopBegin': false,  // 19448: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > jumpWords(-1) + stopFinish': false,  // 19449: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > jumpWords(-1) + done': false,  // 19450: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > jumpWords(0) + newWordFragment': false,  // 19471: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > jumpWords(0) + progress': false,  // 19472: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > jumpWords(4) + newWordFragment': false,  // 19496: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > jumpWords(4) + progress': false,  // 19497: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > jumpSentences(0) + newWordFragment': false,  // 19621: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > jumpSentences(0) + progress': false,  // 19622: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > jumpSentences(1) + newWordFragment': false,  // 19646: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > jumpSentences(1) + progress': false,  // 19647: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > jumpSentences(3) + newWordFragment': false,  // 19671: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > jumpSentences(3) + progress': false,  // 19672: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > jumpSentences(3) + stopBegin': false,  // 19673: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > jumpSentences(3) + stopFinish': false,  // 19674: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > jumpSentences(3) + done': false,  // 19675: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > nextWord(null) + newWordFragment': false,  // 19721: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > nextWord(null) + progress': false,  // 19722: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > nextSentence(null) + newWordFragment': false,  // 19746: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > nextSentence(null) + progress': false,  // 19747: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > prevWord(null) + newWordFragment': false,  // 19771: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > prevWord(null) + progress': false,  // 19772: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > prevWord(null) + stopBegin': false,  // 19773: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > prevWord(null) + stopFinish': false,  // 19774: ?? (skip results are different than combos)
		'skip-combos: nextSentence(null) + progress > prevWord(null) + done': false,  // 19775: ?? (skip results are different than combos)


		// ============== prevWord ==============
		// #? functions and events that immediately lead to stop*|done > play|toggle() = restart, not play
		'skip-combos: prevWord(null) + onceBegin > play(null) + playBegin': true,  // 9076: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + onceBegin > play(null) + playFinish': true,  // 9077: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + onceBegin > play(null) + restartBegin': true,  // 9080: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + onceBegin > play(null) + restartFinish': true,  // 9081: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + onceBegin > toggle(null) + playBegin': true,  // 9101: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + onceBegin > toggle(null) + playFinish': true,  // 9102: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + onceBegin > toggle(null) + restartBegin': true,  // 9105: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + onceBegin > toggle(null) + restartFinish': true,  // 9106: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + onceFinish > play(null) + playBegin': true,  // 9976: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + onceFinish > play(null) + playFinish': true,  // 9977: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + onceFinish > play(null) + restartBegin': true,  // 9980: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + onceFinish > play(null) + restartFinish': true,  // 9981: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + onceFinish > toggle(null) + playBegin': true,  // 10001: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + onceFinish > toggle(null) + playFinish': true,  // 10002: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + onceFinish > toggle(null) + restartBegin': true,  // 10005: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + onceFinish > toggle(null) + restartFinish': true,  // 10006: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + loopBegin > play(null) + playBegin': true,  // 16276: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + loopBegin > play(null) + playFinish': true,  // 16277: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + loopBegin > play(null) + restartBegin': true,  // 16280: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + loopBegin > play(null) + restartFinish': true,  // 16281: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + loopBegin > toggle(null) + playBegin': true,  // 16301: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + loopBegin > toggle(null) + playFinish': true,  // 16302: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + loopBegin > toggle(null) + restartBegin': true,  // 16305: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + loopBegin > toggle(null) + restartFinish': true,  // 16306: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + loopFinish > play(null) + playBegin': true,  // 17176: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + loopFinish > play(null) + playFinish': true,  // 17177: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + loopFinish > play(null) + restartBegin': true,  // 17180: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + loopFinish > play(null) + restartFinish': true,  // 17181: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + loopFinish > toggle(null) + playBegin': true,  // 17201: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + loopFinish > toggle(null) + playFinish': true,  // 17202: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + loopFinish > toggle(null) + restartBegin': true,  // 17205: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + loopFinish > toggle(null) + restartFinish': true,  // 17206: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + newWordFragment > play(null) + playBegin': true,  // 18076: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + newWordFragment > play(null) + playFinish': true,  // 18077: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + newWordFragment > play(null) + restartBegin': true,  // 18080: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + newWordFragment > play(null) + restartFinish': true,  // 18081: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + newWordFragment > toggle(null) + playBegin': true,  // 18101: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + newWordFragment > toggle(null) + playFinish': true,  // 18102: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + newWordFragment > toggle(null) + restartBegin': true,  // 18105: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + newWordFragment > toggle(null) + restartFinish': true,  // 18106: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + progress > play(null) + playBegin': true,  // 18976: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + progress > play(null) + playFinish': true,  // 18977: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + progress > play(null) + restartBegin': true,  // 18980: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + progress > play(null) + restartFinish': true,  // 18981: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + progress > toggle(null) + playBegin': true,  // 19001: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + progress > toggle(null) + playFinish': true,  // 19002: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + progress > toggle(null) + restartBegin': true,  // 19005: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + progress > toggle(null) + restartFinish': true,  // 19006: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + stopBegin > play(null) + playBegin': true,  // 19876: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + stopBegin > play(null) + playFinish': true,  // 19877: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + stopBegin > play(null) + restartBegin': true,  // 19880: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + stopBegin > play(null) + restartFinish': true,  // 19881: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + stopBegin > toggle(null) + playBegin': true,  // 19901: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + stopBegin > toggle(null) + playFinish': true,  // 19902: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + stopBegin > toggle(null) + restartBegin': true,  // 19905: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + stopBegin > toggle(null) + restartFinish': true,  // 19906: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + stopFinish > play(null) + playBegin': true,  // 20776: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + stopFinish > play(null) + playFinish': true,  // 20777: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + stopFinish > play(null) + restartBegin': true,  // 20780: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + stopFinish > play(null) + restartFinish': true,  // 20781: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + stopFinish > toggle(null) + playBegin': true,  // 20801: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + stopFinish > toggle(null) + playFinish': true,  // 20802: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + stopFinish > toggle(null) + restartBegin': true,  // 20805: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + stopFinish > toggle(null) + restartFinish': true,  // 20806: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + done > play(null) + playBegin': true,  // 21676: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + done > play(null) + playFinish': true,  // 21677: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + done > play(null) + restartBegin': true,  // 21680: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + done > play(null) + restartFinish': true,  // 21681: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + done > toggle(null) + playBegin': true,  // 21701: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + done > toggle(null) + playFinish': true,  // 21702: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + done > toggle(null) + restartBegin': true,  // 21705: ?? (skip results are different than combos)
		'skip-combos: prevWord(null) + done > toggle(null) + restartFinish': true,  // 21706: ?? (skip results are different than combos)



		// ============== prevSentence ==============
		// #? like prevWord
		'skip-combos: prevSentence(null) + onceBegin > play(null) + playBegin': true,  // 9076: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + onceBegin > play(null) + playFinish': true,  // 9077: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + onceBegin > play(null) + restartBegin': true,  // 9080: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + onceBegin > play(null) + restartFinish': true,  // 9081: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + onceBegin > toggle(null) + playBegin': true,  // 9101: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + onceBegin > toggle(null) + playFinish': true,  // 9102: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + onceBegin > toggle(null) + restartBegin': true,  // 9105: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + onceBegin > toggle(null) + restartFinish': true,  // 9106: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + onceFinish > play(null) + playBegin': true,  // 9976: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + onceFinish > play(null) + playFinish': true,  // 9977: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + onceFinish > play(null) + restartBegin': true,  // 9980: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + onceFinish > play(null) + restartFinish': true,  // 9981: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + onceFinish > toggle(null) + playBegin': true,  // 10001: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + onceFinish > toggle(null) + playFinish': true,  // 10002: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + onceFinish > toggle(null) + restartBegin': true,  // 10005: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + onceFinish > toggle(null) + restartFinish': true,  // 10006: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + loopBegin > play(null) + playBegin': true,  // 16276: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + loopBegin > play(null) + playFinish': true,  // 16277: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + loopBegin > play(null) + restartBegin': true,  // 16280: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + loopBegin > play(null) + restartFinish': true,  // 16281: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + loopBegin > toggle(null) + playBegin': true,  // 16301: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + loopBegin > toggle(null) + playFinish': true,  // 16302: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + loopBegin > toggle(null) + restartBegin': true,  // 16305: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + loopBegin > toggle(null) + restartFinish': true,  // 16306: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + loopFinish > play(null) + playBegin': true,  // 17176: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + loopFinish > play(null) + playFinish': true,  // 17177: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + loopFinish > play(null) + restartBegin': true,  // 17180: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + loopFinish > play(null) + restartFinish': true,  // 17181: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + loopFinish > toggle(null) + playBegin': true,  // 17201: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + loopFinish > toggle(null) + playFinish': true,  // 17202: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + loopFinish > toggle(null) + restartBegin': true,  // 17205: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + loopFinish > toggle(null) + restartFinish': true,  // 17206: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + newWordFragment > play(null) + playBegin': true,  // 18076: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + newWordFragment > play(null) + playFinish': true,  // 18077: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + newWordFragment > play(null) + restartBegin': true,  // 18080: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + newWordFragment > play(null) + restartFinish': true,  // 18081: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + newWordFragment > toggle(null) + playBegin': true,  // 18101: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + newWordFragment > toggle(null) + playFinish': true,  // 18102: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + newWordFragment > toggle(null) + restartBegin': true,  // 18105: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + newWordFragment > toggle(null) + restartFinish': true,  // 18106: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + progress > play(null) + playBegin': true,  // 18976: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + progress > play(null) + playFinish': true,  // 18977: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + progress > play(null) + restartBegin': true,  // 18980: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + progress > play(null) + restartFinish': true,  // 18981: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + progress > toggle(null) + playBegin': true,  // 19001: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + progress > toggle(null) + playFinish': true,  // 19002: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + progress > toggle(null) + restartBegin': true,  // 19005: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + progress > toggle(null) + restartFinish': true,  // 19006: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + stopBegin > play(null) + playBegin': true,  // 19876: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + stopBegin > play(null) + playFinish': true,  // 19877: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + stopBegin > play(null) + restartBegin': true,  // 19880: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + stopBegin > play(null) + restartFinish': true,  // 19881: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + stopBegin > toggle(null) + playBegin': true,  // 19901: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + stopBegin > toggle(null) + playFinish': true,  // 19902: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + stopBegin > toggle(null) + restartBegin': true,  // 19905: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + stopBegin > toggle(null) + restartFinish': true,  // 19906: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + stopFinish > play(null) + playBegin': true,  // 20776: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + stopFinish > play(null) + playFinish': true,  // 20777: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + stopFinish > play(null) + restartBegin': true,  // 20780: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + stopFinish > play(null) + restartFinish': true,  // 20781: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + stopFinish > toggle(null) + playBegin': true,  // 20801: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + stopFinish > toggle(null) + playFinish': true,  // 20802: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + stopFinish > toggle(null) + restartBegin': true,  // 20805: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + stopFinish > toggle(null) + restartFinish': true,  // 20806: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + done > play(null) + playBegin': true,  // 21676: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + done > play(null) + playFinish': true,  // 21677: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + done > play(null) + restartBegin': true,  // 21680: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + done > play(null) + restartFinish': true,  // 21681: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + done > toggle(null) + playBegin': true,  // 21701: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + done > toggle(null) + playFinish': true,  // 21702: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + done > toggle(null) + restartBegin': true,  // 21705: ?? (skip results are different than combos)
		'skip-combos: prevSentence(null) + done > toggle(null) + restartFinish': true,  // 21706: ?? (skip results are different than combos)


		// ================================================================
		// ================================================================
		// tests/skip-combos.js (specifically skip combo expected failures)
		// ================================================================
		// ================================================================
		// forceReset(null)
		'skip-combos: forceReset(null) + resetBegin > restart(null) + loopSkip': false,  // 78: event should have been triggerd but was NOT
		'skip-combos: forceReset(null) + resetBegin > play(null) + loopSkip': false,  // 104: event should have been triggerd but was NOT
		'skip-combos: forceReset(null) + resetBegin > toggle(null) + loopSkip': false,  // 130: event should have been triggerd but was NOT
		'skip-combos: forceReset(null) + resetBegin > rewind(null) + loopSkip': false,  // 260: event should have been triggerd but was NOT
		'skip-combos: forceReset(null) + resetBegin > fastForward(null) + loopSkip': false,  // 286: event should have been triggerd but was NOT
		'skip-combos: forceReset(null) + resetBegin > once([0,0,-2]) + loopSkip': false,  // 312: event should have been triggerd but was NOT
		'skip-combos: forceReset(null) + resetBegin > current(null) + loopSkip': false,  // 364: event should have been triggerd but was NOT
		'skip-combos: forceReset(null) + resetBegin > jumpTo(-3) + loopSkip': false,  // 390: event should have been triggerd but was NOT
		'skip-combos: forceReset(null) + resetBegin > jumpTo(-1) + loopSkip': false,  // 416: event should have been triggerd but was NOT
		'skip-combos: forceReset(null) + resetBegin > jumpTo(0) + loopSkip': false,  // 442: event should have been triggerd but was NOT
		'skip-combos: forceReset(null) + resetBegin > jumpTo(4) + onceBegin': false,  // 453: event should have been triggerd but was NOT
		'skip-combos: forceReset(null) + resetBegin > jumpTo(4) + onceFinish': false,  // 454: event should have been triggerd but was NOT
		'skip-combos: forceReset(null) + resetBegin > jumpTo(4) + loopBegin': false,  // 461: event should have been triggerd but was NOT
		'skip-combos: forceReset(null) + resetBegin > jumpTo(4) + loopFinish': false,  // 462: event should have been triggerd but was NOT
		'skip-combos: forceReset(null) + resetBegin > jumpTo(4) + newWordFragment': false,  // 463: event should have been triggerd but was NOT
		'skip-combos: forceReset(null) + resetBegin > jumpTo(4) + progress': false,  // 464: event should have been triggerd but was NOT
		'skip-combos: forceReset(null) + resetBegin > jumpTo(4) + loopSkip': false,  // 468: event should have been triggerd but was NOT
		'skip-combos: forceReset(null) + resetBegin > jumpTo(11) + loopSkip': false,  // 494: event should have been triggerd but was NOT
		'skip-combos: forceReset(null) + resetBegin > jumpTo(100) + loopSkip': false,  // 520: event should have been triggerd but was NOT
		'skip-combos: forceReset(null) + resetBegin > jumpWords(-3) + loopSkip': false,  // 546: event should have been triggerd but was NOT
		'skip-combos: forceReset(null) + resetBegin > jumpWords(-1) + loopSkip': false,  // 572: event should have been triggerd but was NOT
		'skip-combos: forceReset(null) + resetBegin > jumpWords(0) + loopSkip': false,  // 598: event should have been triggerd but was NOT
		'skip-combos: forceReset(null) + resetBegin > jumpWords(4) + loopSkip': false,  // 624: event should have been triggerd but was NOT
		'skip-combos: forceReset(null) + resetBegin > jumpWords(11) + loopSkip': false,  // 650: event should have been triggerd but was NOT
		'skip-combos: forceReset(null) + resetBegin > jumpWords(100) + loopSkip': false,  // 676: event should have been triggerd but was NOT
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(-3) + loopSkip': false,  // 702: event should have been triggerd but was NOT
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(-1) + loopSkip': false,  // 728: event should have been triggerd but was NOT
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(0) + loopSkip': false,  // 754: event should have been triggerd but was NOT
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(1) + loopSkip': false,  // 780: event should have been triggerd but was NOT
		'skip-combos: forceReset(null) + resetBegin > jumpSentences(100) + loopSkip': false,  // 832: event should have been triggerd but was NOT
		'skip-combos: forceReset(null) + resetBegin > nextSentence(null) + loopSkip': false,  // 884: event should have been triggerd but was NOT
		'skip-combos: forceReset(null) + resetBegin > prevWord(null) + loopSkip': false,  // 910: event should have been triggerd but was NOT
		'skip-combos: forceReset(null) + resetBegin > prevSentence(null) + loopSkip': false,  // 936: event should have been triggerd but was NOT

		// > forceReset(null)
		'skip-combos: restart(null) + restartBegin > forceReset(null) + loopSkip': false,  // 1898: event should not have been triggerd but WAS
		'skip-combos: restart(null) + loopBegin > forceReset(null) + loopSkip': false,  // 16874: event should not have been triggerd but WAS
		'skip-combos: restart(null) + loopSkip > forceReset(null) + restartFinish': false,  // 23406: event should not have been triggerd but WAS
		'skip-combos: restart(null) + loopSkip > forceReset(null) + loopBegin': false,  // 23419: event should not have been triggerd but WAS
		'skip-combos: restart(null) + loopSkip > forceReset(null) + loopFinish': false,  // 23420: event should not have been triggerd but WAS
		'skip-combos: restart(null) + loopSkip > forceReset(null) + newWordFragment': false,  // 23421: event should not have been triggerd but WAS
		'skip-combos: restart(null) + loopSkip > forceReset(null) + progress': false,  // 23422: event should not have been triggerd but WAS
		'skip-combos: restart(null) + loopSkip > forceReset(null) + stopBegin': false,  // 23423: event should not have been triggerd but WAS
		'skip-combos: restart(null) + loopSkip > forceReset(null) + stopFinish': false,  // 23424: event should not have been triggerd but WAS
		'skip-combos: restart(null) + loopSkip > forceReset(null) + done': false,  // 23425: event should not have been triggerd but WAS
		'skip-combos: restart(null) + loopSkip > forceReset(null) + loopSkip': false,  // 23426: event should not have been triggerd but WAS
		'skip-combos: play(null) + playBegin > forceReset(null) + loopSkip': false,  // 28106: event should not have been triggerd but WAS
		'skip-combos: play(null) + loopBegin > forceReset(null) + loopSkip': false,  // 41210: event should not have been triggerd but WAS
		'skip-combos: play(null) + loopSkip > forceReset(null) + playFinish': false,  // 47738: event should not have been triggerd but WAS
		'skip-combos: play(null) + loopSkip > forceReset(null) + loopBegin': false,  // 47755: event should not have been triggerd but WAS
		'skip-combos: play(null) + loopSkip > forceReset(null) + loopFinish': false,  // 47756: event should not have been triggerd but WAS
		'skip-combos: play(null) + loopSkip > forceReset(null) + newWordFragment': false,  // 47757: event should not have been triggerd but WAS
		'skip-combos: play(null) + loopSkip > forceReset(null) + progress': false,  // 47758: event should not have been triggerd but WAS
		'skip-combos: play(null) + loopSkip > forceReset(null) + stopBegin': false,  // 47759: event should not have been triggerd but WAS
		'skip-combos: play(null) + loopSkip > forceReset(null) + stopFinish': false,  // 47760: event should not have been triggerd but WAS
		'skip-combos: play(null) + loopSkip > forceReset(null) + done': false,  // 47761: event should not have been triggerd but WAS
		'skip-combos: play(null) + loopSkip > forceReset(null) + loopSkip': false,  // 47762: event should not have been triggerd but WAS
		'skip-combos: toggle(null) + playBegin > forceReset(null) + loopSkip': false,  // 3770: event should not have been triggerd but WAS
		'skip-combos: toggle(null) + loopBegin > forceReset(null) + loopSkip': false,  // 16874: event should not have been triggerd but WAS
		'skip-combos: toggle(null) + loopSkip > forceReset(null) + playFinish': false,  // 23402: event should not have been triggerd but WAS
		'skip-combos: toggle(null) + loopSkip > forceReset(null) + loopBegin': false,  // 23419: event should not have been triggerd but WAS
		'skip-combos: toggle(null) + loopSkip > forceReset(null) + loopFinish': false,  // 23420: event should not have been triggerd but WAS
		'skip-combos: toggle(null) + loopSkip > forceReset(null) + newWordFragment': false,  // 23421: event should not have been triggerd but WAS
		'skip-combos: toggle(null) + loopSkip > forceReset(null) + progress': false,  // 23422: event should not have been triggerd but WAS
		'skip-combos: toggle(null) + loopSkip > forceReset(null) + stopBegin': false,  // 23423: event should not have been triggerd but WAS
		'skip-combos: toggle(null) + loopSkip > forceReset(null) + stopFinish': false,  // 23424: event should not have been triggerd but WAS
		'skip-combos: toggle(null) + loopSkip > forceReset(null) + done': false,  // 23425: event should not have been triggerd but WAS
		'skip-combos: toggle(null) + loopSkip > forceReset(null) + loopSkip': false,  // 23426: event should not have been triggerd but WAS
		'skip-combos: rewind(null) + rewindBegin > forceReset(null) + loopSkip': false,  // 37466: event should not have been triggerd but WAS
		'skip-combos: rewind(null) + loopBegin > forceReset(null) + loopSkip': false,  // 41210: event should not have been triggerd but WAS
		'skip-combos: rewind(null) + loopSkip > forceReset(null) + rewindFinish': false,  // 47752: event should not have been triggerd but WAS
		'skip-combos: rewind(null) + loopSkip > forceReset(null) + loopBegin': false,  // 47755: event should not have been triggerd but WAS
		'skip-combos: rewind(null) + loopSkip > forceReset(null) + loopFinish': false,  // 47756: event should not have been triggerd but WAS
		'skip-combos: rewind(null) + loopSkip > forceReset(null) + progress': false,  // 47758: event should not have been triggerd but WAS
		'skip-combos: rewind(null) + loopSkip > forceReset(null) + loopSkip': false,  // 47762: event should not have been triggerd but WAS
		'skip-combos: fastForward(null) + fastForwardBegin > forceReset(null) + loopSkip': false,  // 15002: event should not have been triggerd but WAS
		'skip-combos: fastForward(null) + loopBegin > forceReset(null) + loopSkip': false,  // 16874: event should not have been triggerd but WAS
		'skip-combos: fastForward(null) + loopSkip > forceReset(null) + loopBegin': false,  // 23419: event should not have been triggerd but WAS
		'skip-combos: fastForward(null) + loopSkip > forceReset(null) + loopFinish': false,  // 23420: event should not have been triggerd but WAS
		'skip-combos: fastForward(null) + loopSkip > forceReset(null) + newWordFragment': false,  // 23421: event should not have been triggerd but WAS
		'skip-combos: fastForward(null) + loopSkip > forceReset(null) + progress': false,  // 23422: event should not have been triggerd but WAS
		'skip-combos: fastForward(null) + loopSkip > forceReset(null) + stopBegin': false,  // 23423: event should not have been triggerd but WAS
		'skip-combos: fastForward(null) + loopSkip > forceReset(null) + stopFinish': false,  // 23424: event should not have been triggerd but WAS
		'skip-combos: fastForward(null) + loopSkip > forceReset(null) + done': false,  // 23425: event should not have been triggerd but WAS
		'skip-combos: fastForward(null) + loopSkip > forceReset(null) + loopSkip': false,  // 23426: event should not have been triggerd but WAS
		'skip-combos: once([0,0,-2]) + onceBegin > forceReset(null) + loopSkip': false,  // 33722: event should not have been triggerd but WAS
		'skip-combos: once([0,0,-2]) + loopBegin > forceReset(null) + loopBegin': false,  // 41203: event should not have been triggerd but WAS
		'skip-combos: once([0,0,-2]) + loopBegin > forceReset(null) + loopSkip': false,  // 41210: event should not have been triggerd but WAS
		'skip-combos: once([0,0,-2]) + loopSkip > forceReset(null) + onceFinish': false,  // 47748: event should not have been triggerd but WAS
		'skip-combos: once([0,0,-2]) + loopSkip > forceReset(null) + loopBegin': false,  // 47755: event should not have been triggerd but WAS
		'skip-combos: once([0,0,-2]) + loopSkip > forceReset(null) + loopFinish': false,  // 47756: event should not have been triggerd but WAS
		'skip-combos: once([0,0,-2]) + loopSkip > forceReset(null) + progress': false,  // 47758: event should not have been triggerd but WAS
		'skip-combos: once([0,0,-2]) + loopSkip > forceReset(null) + loopSkip': false,  // 47762: event should not have been triggerd but WAS
		'skip-combos: current(null) + onceBegin > forceReset(null) + loopSkip': false,  // 33722: event should not have been triggerd but WAS
		'skip-combos: current(null) + loopBegin > forceReset(null) + loopBegin': false,  // 41203: event should not have been triggerd but WAS
		'skip-combos: current(null) + loopBegin > forceReset(null) + loopSkip': false,  // 41210: event should not have been triggerd but WAS
		'skip-combos: current(null) + loopSkip > forceReset(null) + onceFinish': false,  // 47748: event should not have been triggerd but WAS
		'skip-combos: current(null) + loopSkip > forceReset(null) + loopBegin': false,  // 47755: event should not have been triggerd but WAS
		'skip-combos: current(null) + loopSkip > forceReset(null) + loopFinish': false,  // 47756: event should not have been triggerd but WAS
		'skip-combos: current(null) + loopSkip > forceReset(null) + newWordFragment': false,  // 47757: event should not have been triggerd but WAS
		'skip-combos: current(null) + loopSkip > forceReset(null) + progress': false,  // 47758: event should not have been triggerd but WAS
		'skip-combos: jumpTo(-3) + onceBegin > forceReset(null) + loopSkip': false,  // 9386: event should not have been triggerd but WAS
		'skip-combos: jumpTo(-3) + loopBegin > forceReset(null) + loopBegin': false,  // 16867: event should not have been triggerd but WAS
		'skip-combos: jumpTo(-3) + loopBegin > forceReset(null) + loopSkip': false,  // 16874: event should not have been triggerd but WAS
		'skip-combos: jumpTo(-3) + loopSkip > forceReset(null) + onceFinish': false,  // 23412: event should not have been triggerd but WAS
		'skip-combos: jumpTo(-3) + loopSkip > forceReset(null) + loopBegin': false,  // 23419: event should not have been triggerd but WAS
		'skip-combos: jumpTo(-3) + loopSkip > forceReset(null) + loopFinish': false,  // 23420: event should not have been triggerd but WAS
		'skip-combos: jumpTo(-3) + loopSkip > forceReset(null) + progress': false,  // 23422: event should not have been triggerd but WAS
		'skip-combos: jumpTo(-3) + loopSkip > forceReset(null) + loopSkip': false,  // 23426: event should not have been triggerd but WAS
		'skip-combos: jumpTo(-1) + onceBegin > forceReset(null) + loopSkip': false,  // 33722: event should not have been triggerd but WAS
		'skip-combos: jumpTo(-1) + loopBegin > forceReset(null) + loopBegin': false,  // 41203: event should not have been triggerd but WAS
		'skip-combos: jumpTo(-1) + loopBegin > forceReset(null) + loopSkip': false,  // 41210: event should not have been triggerd but WAS
		'skip-combos: jumpTo(-1) + loopSkip > forceReset(null) + onceFinish': false,  // 47748: event should not have been triggerd but WAS
		'skip-combos: jumpTo(-1) + loopSkip > forceReset(null) + loopBegin': false,  // 47755: event should not have been triggerd but WAS
		'skip-combos: jumpTo(-1) + loopSkip > forceReset(null) + loopFinish': false,  // 47756: event should not have been triggerd but WAS
		'skip-combos: jumpTo(-1) + loopSkip > forceReset(null) + progress': false,  // 47758: event should not have been triggerd but WAS
		'skip-combos: jumpTo(-1) + loopSkip > forceReset(null) + loopSkip': false,  // 47762: event should not have been triggerd but WAS
		'skip-combos: jumpTo(0) + onceBegin > forceReset(null) + loopSkip': false,  // 9386: event should not have been triggerd but WAS
		'skip-combos: jumpTo(0) + loopBegin > forceReset(null) + loopBegin': false,  // 16867: event should not have been triggerd but WAS
		'skip-combos: jumpTo(0) + loopBegin > forceReset(null) + loopSkip': false,  // 16874: event should not have been triggerd but WAS
		'skip-combos: jumpTo(0) + loopSkip > forceReset(null) + onceFinish': false,  // 23412: event should not have been triggerd but WAS
		'skip-combos: jumpTo(0) + loopSkip > forceReset(null) + loopBegin': false,  // 23419: event should not have been triggerd but WAS
		'skip-combos: jumpTo(0) + loopSkip > forceReset(null) + loopFinish': false,  // 23420: event should not have been triggerd but WAS
		'skip-combos: jumpTo(0) + loopSkip > forceReset(null) + newWordFragment': false,  // 23421: event should not have been triggerd but WAS
		'skip-combos: jumpTo(0) + loopSkip > forceReset(null) + progress': false,  // 23422: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + onceBegin > forceReset(null) + onceFinish': false,  // 33708: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + onceBegin > forceReset(null) + loopBegin': false,  // 33715: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + onceBegin > forceReset(null) + loopFinish': false,  // 33716: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + onceBegin > forceReset(null) + newWordFragment': false,  // 33717: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + onceBegin > forceReset(null) + progress': false,  // 33718: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + onceBegin > forceReset(null) + loopSkip': false,  // 33722: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopBegin > forceReset(null) + onceFinish': false,  // 41196: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopBegin > forceReset(null) + loopBegin': false,  // 41203: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopBegin > forceReset(null) + loopFinish': false,  // 41204: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopBegin > forceReset(null) + newWordFragment': false,  // 41205: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopBegin > forceReset(null) + progress': false,  // 41206: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopBegin > forceReset(null) + loopSkip': false,  // 41210: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopFinish > forceReset(null) + onceFinish': false,  // 42132: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + newWordFragment > forceReset(null) + loopFinish': false,  // 43076: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + newWordFragment > forceReset(null) + progress': false,  // 43078: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + progress > forceReset(null) + onceFinish': false,  // 44004: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + progress > forceReset(null) + loopFinish': false,  // 44012: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopSkip > forceReset(null) + onceFinish': false,  // 47748: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopSkip > forceReset(null) + loopBegin': false,  // 47755: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopSkip > forceReset(null) + loopFinish': false,  // 47756: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopSkip > forceReset(null) + newWordFragment': false,  // 47757: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopSkip > forceReset(null) + progress': false,  // 47758: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + onceBegin > forceReset(null) + loopSkip': false,  // 9386: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopBegin > forceReset(null) + loopSkip': false,  // 16874: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > forceReset(null) + onceFinish': false,  // 23412: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > forceReset(null) + loopBegin': false,  // 23419: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > forceReset(null) + loopFinish': false,  // 23420: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > forceReset(null) + newWordFragment': false,  // 23421: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > forceReset(null) + progress': false,  // 23422: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + onceBegin > forceReset(null) + loopSkip': false,  // 33722: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopBegin > forceReset(null) + loopSkip': false,  // 41210: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > forceReset(null) + onceFinish': false,  // 47748: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > forceReset(null) + loopBegin': false,  // 47755: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > forceReset(null) + loopFinish': false,  // 47756: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > forceReset(null) + newWordFragment': false,  // 47757: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > forceReset(null) + progress': false,  // 47758: event should not have been triggerd but WAS
		'skip-combos: jumpWords(-3) + onceBegin > forceReset(null) + loopSkip': false,  // 9386: event should not have been triggerd but WAS
		'skip-combos: jumpWords(-3) + loopBegin > forceReset(null) + loopBegin': false,  // 16867: event should not have been triggerd but WAS
		'skip-combos: jumpWords(-3) + loopBegin > forceReset(null) + loopSkip': false,  // 16874: event should not have been triggerd but WAS
		'skip-combos: jumpWords(-3) + loopSkip > forceReset(null) + onceFinish': false,  // 23412: event should not have been triggerd but WAS
		'skip-combos: jumpWords(-3) + loopSkip > forceReset(null) + loopBegin': false,  // 23419: event should not have been triggerd but WAS
		'skip-combos: jumpWords(-3) + loopSkip > forceReset(null) + loopFinish': false,  // 23420: event should not have been triggerd but WAS
		'skip-combos: jumpWords(-3) + loopSkip > forceReset(null) + progress': false,  // 23422: event should not have been triggerd but WAS
		'skip-combos: jumpWords(-3) + loopSkip > forceReset(null) + loopSkip': false,  // 23426: event should not have been triggerd but WAS
		'skip-combos: jumpWords(-1) + onceBegin > forceReset(null) + loopSkip': false,  // 33722: event should not have been triggerd but WAS
		'skip-combos: jumpWords(-1) + loopBegin > forceReset(null) + loopBegin': false,  // 41203: event should not have been triggerd but WAS
		'skip-combos: jumpWords(-1) + loopBegin > forceReset(null) + loopSkip': false,  // 41210: event should not have been triggerd but WAS
		'skip-combos: jumpWords(-1) + loopSkip > forceReset(null) + onceFinish': false,  // 47748: event should not have been triggerd but WAS
		'skip-combos: jumpWords(-1) + loopSkip > forceReset(null) + loopBegin': false,  // 47755: event should not have been triggerd but WAS
		'skip-combos: jumpWords(-1) + loopSkip > forceReset(null) + loopFinish': false,  // 47756: event should not have been triggerd but WAS
		'skip-combos: jumpWords(-1) + loopSkip > forceReset(null) + progress': false,  // 47758: event should not have been triggerd but WAS
		'skip-combos: jumpWords(-1) + loopSkip > forceReset(null) + loopSkip': false,  // 47762: event should not have been triggerd but WAS
		'skip-combos: jumpWords(0) + onceBegin > forceReset(null) + loopSkip': false,  // 9386: event should not have been triggerd but WAS
		'skip-combos: jumpWords(0) + loopBegin > forceReset(null) + loopBegin': false,  // 16867: event should not have been triggerd but WAS
		'skip-combos: jumpWords(0) + loopBegin > forceReset(null) + loopSkip': false,  // 16874: event should not have been triggerd but WAS
		'skip-combos: jumpWords(0) + loopSkip > forceReset(null) + onceFinish': false,  // 23412: event should not have been triggerd but WAS
		'skip-combos: jumpWords(0) + loopSkip > forceReset(null) + loopBegin': false,  // 23419: event should not have been triggerd but WAS
		'skip-combos: jumpWords(0) + loopSkip > forceReset(null) + loopFinish': false,  // 23420: event should not have been triggerd but WAS
		'skip-combos: jumpWords(0) + loopSkip > forceReset(null) + newWordFragment': false,  // 23421: event should not have been triggerd but WAS
		'skip-combos: jumpWords(0) + loopSkip > forceReset(null) + progress': false,  // 23422: event should not have been triggerd but WAS
		'skip-combos: jumpWords(4) + onceBegin > forceReset(null) + loopSkip': false,  // 33722: event should not have been triggerd but WAS
		
		'skip-combos: jumpWords(4) + loopBegin > forceReset(null) + loopBegin': false,  // 41203: event should not have been triggerd but WAS
		'skip-combos: jumpWords(4) + loopBegin > forceReset(null) + loopSkip': false,  // 41210: event should not have been triggerd but WAS
		'skip-combos: jumpWords(4) + loopSkip > forceReset(null) + onceFinish': false,  // 47748: event should not have been triggerd but WAS
		'skip-combos: jumpWords(4) + loopSkip > forceReset(null) + loopBegin': false,  // 47755: event should not have been triggerd but WAS
		'skip-combos: jumpWords(4) + loopSkip > forceReset(null) + loopFinish': false,  // 47756: event should not have been triggerd but WAS
		'skip-combos: jumpWords(4) + loopSkip > forceReset(null) + newWordFragment': false,  // 47757: event should not have been triggerd but WAS
		'skip-combos: jumpWords(4) + loopSkip > forceReset(null) + progress': false,  // 47758: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + onceBegin > forceReset(null) + loopSkip': false,  // 9386: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopBegin > forceReset(null) + loopSkip': false,  // 16874: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > forceReset(null) + onceFinish': false,  // 23412: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > forceReset(null) + loopBegin': false,  // 23419: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > forceReset(null) + loopFinish': false,  // 23420: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > forceReset(null) + newWordFragment': false,  // 23421: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > forceReset(null) + progress': false,  // 23422: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + onceBegin > forceReset(null) + loopSkip': false,  // 33722: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopBegin > forceReset(null) + loopSkip': false,  // 41210: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > forceReset(null) + onceFinish': false,  // 47748: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > forceReset(null) + loopBegin': false,  // 47755: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > forceReset(null) + loopFinish': false,  // 47756: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > forceReset(null) + newWordFragment': false,  // 47757: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > forceReset(null) + progress': false,  // 47758: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(-3) + onceBegin > forceReset(null) + loopSkip': false,  // 9386: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(-3) + loopBegin > forceReset(null) + loopBegin': false,  // 16867: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(-3) + loopBegin > forceReset(null) + loopSkip': false,  // 16874: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(-3) + loopSkip > forceReset(null) + onceFinish': false,  // 23412: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(-3) + loopSkip > forceReset(null) + loopBegin': false,  // 23419: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(-3) + loopSkip > forceReset(null) + loopFinish': false,  // 23420: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(-3) + loopSkip > forceReset(null) + progress': false,  // 23422: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(-3) + loopSkip > forceReset(null) + loopSkip': false,  // 23426: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(-1) + onceBegin > forceReset(null) + loopSkip': false,  // 33722: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(-1) + loopBegin > forceReset(null) + loopBegin': false,  // 41203: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(-1) + loopBegin > forceReset(null) + loopSkip': false,  // 41210: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(-1) + loopSkip > forceReset(null) + onceFinish': false,  // 47748: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(-1) + loopSkip > forceReset(null) + loopBegin': false,  // 47755: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(-1) + loopSkip > forceReset(null) + loopFinish': false,  // 47756: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(-1) + loopSkip > forceReset(null) + progress': false,  // 47758: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(-1) + loopSkip > forceReset(null) + loopSkip': false,  // 47762: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(0) + onceBegin > forceReset(null) + loopSkip': false,  // 9386: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(0) + loopBegin > forceReset(null) + loopBegin': false,  // 16867: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(0) + loopBegin > forceReset(null) + loopSkip': false,  // 16874: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(0) + loopSkip > forceReset(null) + onceFinish': false,  // 23412: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(0) + loopSkip > forceReset(null) + loopBegin': false,  // 23419: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(0) + loopSkip > forceReset(null) + loopFinish': false,  // 23420: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(0) + loopSkip > forceReset(null) + newWordFragment': false,  // 23421: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(0) + loopSkip > forceReset(null) + progress': false,  // 23422: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(1) + onceBegin > forceReset(null) + loopSkip': false,  // 33722: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(1) + loopBegin > forceReset(null) + loopBegin': false,  // 41203: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(1) + loopBegin > forceReset(null) + loopSkip': false,  // 41210: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(1) + loopSkip > forceReset(null) + onceFinish': false,  // 47748: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(1) + loopSkip > forceReset(null) + loopBegin': false,  // 47755: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(1) + loopSkip > forceReset(null) + loopFinish': false,  // 47756: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(1) + loopSkip > forceReset(null) + newWordFragment': false,  // 47757: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(1) + loopSkip > forceReset(null) + progress': false,  // 47758: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + onceBegin > forceReset(null) + loopSkip': false,  // 33722: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopBegin > forceReset(null) + loopSkip': false,  // 41210: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > forceReset(null) + onceFinish': false,  // 47748: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > forceReset(null) + loopBegin': false,  // 47755: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > forceReset(null) + loopFinish': false,  // 47756: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > forceReset(null) + newWordFragment': false,  // 47757: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > forceReset(null) + progress': false,  // 47758: event should not have been triggerd but WAS
		'skip-combos: nextSentence(null) + onceBegin > forceReset(null) + loopSkip': false,  // 33722: event should not have been triggerd but WAS
		'skip-combos: nextSentence(null) + loopBegin > forceReset(null) + loopBegin': false,  // 41203: event should not have been triggerd but WAS
		'skip-combos: nextSentence(null) + loopBegin > forceReset(null) + loopSkip': false,  // 41210: event should not have been triggerd but WAS
		'skip-combos: nextSentence(null) + loopSkip > forceReset(null) + onceFinish': false,  // 47748: event should not have been triggerd but WAS
		'skip-combos: nextSentence(null) + loopSkip > forceReset(null) + loopBegin': false,  // 47755: event should not have been triggerd but WAS
		'skip-combos: nextSentence(null) + loopSkip > forceReset(null) + loopFinish': false,  // 47756: event should not have been triggerd but WAS
		'skip-combos: nextSentence(null) + loopSkip > forceReset(null) + newWordFragment': false,  // 47757: event should not have been triggerd but WAS
		'skip-combos: nextSentence(null) + loopSkip > forceReset(null) + progress': false,  // 47758: event should not have been triggerd but WAS
		'skip-combos: prevWord(null) + onceBegin > forceReset(null) + loopSkip': false,  // 9386: event should not have been triggerd but WAS
		'skip-combos: prevWord(null) + loopBegin > forceReset(null) + loopBegin': false,  // 16867: event should not have been triggerd but WAS
		'skip-combos: prevWord(null) + loopBegin > forceReset(null) + loopSkip': false,  // 16874: event should not have been triggerd but WAS
		'skip-combos: prevWord(null) + loopSkip > forceReset(null) + onceFinish': false,  // 23412: event should not have been triggerd but WAS
		'skip-combos: prevWord(null) + loopSkip > forceReset(null) + loopBegin': false,  // 23419: event should not have been triggerd but WAS
		'skip-combos: prevWord(null) + loopSkip > forceReset(null) + loopFinish': false,  // 23420: event should not have been triggerd but WAS
		'skip-combos: prevWord(null) + loopSkip > forceReset(null) + progress': false,  // 23422: event should not have been triggerd but WAS
		'skip-combos: prevWord(null) + loopSkip > forceReset(null) + loopSkip': false,  // 23426: event should not have been triggerd but WAS
		'skip-combos: prevSentence(null) + onceBegin > forceReset(null) + loopSkip': false,  // 33722: event should not have been triggerd but WAS
		'skip-combos: prevSentence(null) + loopBegin > forceReset(null) + loopBegin': false,  // 41203: event should not have been triggerd but WAS
		'skip-combos: prevSentence(null) + loopBegin > forceReset(null) + loopSkip': false,  // 41210: event should not have been triggerd but WAS
		'skip-combos: prevSentence(null) + loopSkip > forceReset(null) + onceFinish': false,  // 47748: event should not have been triggerd but WAS
		'skip-combos: prevSentence(null) + loopSkip > forceReset(null) + loopBegin': false,  // 47755: event should not have been triggerd but WAS
		'skip-combos: prevSentence(null) + loopSkip > forceReset(null) + loopFinish': false,  // 47756: event should not have been triggerd but WAS
		'skip-combos: prevSentence(null) + loopSkip > forceReset(null) + progress': false,  // 47758: event should not have been triggerd but WAS
		'skip-combos: prevSentence(null) + loopSkip > forceReset(null) + loopSkip': false,  // 47762: event should not have been triggerd but WAS

		// reset(null)
		'skip-combos: reset(null) + resetBegin > restart(null) + loopSkip': false,  // 24414: event should have been triggerd but was NOT
		'skip-combos: reset(null) + resetBegin > play(null) + loopSkip': false,  // 24440: event should have been triggerd but was NOT
		'skip-combos: reset(null) + resetBegin > toggle(null) + loopSkip': false,  // 24466: event should have been triggerd but was NOT
		'skip-combos: reset(null) + resetBegin > rewind(null) + loopSkip': false,  // 24596: event should have been triggerd but was NOT
		'skip-combos: reset(null) + resetBegin > fastForward(null) + loopSkip': false,  // 24622: event should have been triggerd but was NOT
		'skip-combos: reset(null) + resetBegin > once([0,0,-2]) + loopSkip': false,  // 24648: event should have been triggerd but was NOT
		'skip-combos: reset(null) + resetBegin > current(null) + loopSkip': false,  // 24700: event should have been triggerd but was NOT
		'skip-combos: reset(null) + resetBegin > jumpTo(-3) + loopSkip': false,  // 24726: event should have been triggerd but was NOT
		'skip-combos: reset(null) + resetBegin > jumpTo(-1) + loopSkip': false,  // 24752: event should have been triggerd but was NOT
		'skip-combos: reset(null) + resetBegin > jumpTo(0) + loopSkip': false,  // 24778: event should have been triggerd but was NOT
		'skip-combos: reset(null) + resetBegin > jumpTo(4) + onceBegin': false,  // 24789: event should have been triggerd but was NOT
		'skip-combos: reset(null) + resetBegin > jumpTo(4) + onceFinish': false,  // 24790: event should have been triggerd but was NOT
		'skip-combos: reset(null) + resetBegin > jumpTo(4) + loopBegin': false,  // 24797: event should have been triggerd but was NOT
		'skip-combos: reset(null) + resetBegin > jumpTo(4) + loopFinish': false,  // 24798: event should have been triggerd but was NOT
		'skip-combos: reset(null) + resetBegin > jumpTo(4) + newWordFragment': false,  // 24799: event should have been triggerd but was NOT
		'skip-combos: reset(null) + resetBegin > jumpTo(4) + progress': false,  // 24800: event should have been triggerd but was NOT
		'skip-combos: reset(null) + resetBegin > jumpTo(4) + loopSkip': false,  // 24804: event should have been triggerd but was NOT
		'skip-combos: reset(null) + resetBegin > jumpTo(11) + loopSkip': false,  // 24830: event should have been triggerd but was NOT
		'skip-combos: reset(null) + resetBegin > jumpTo(100) + loopSkip': false,  // 24856: event should have been triggerd but was NOT
		'skip-combos: reset(null) + resetBegin > jumpWords(-3) + loopSkip': false,  // 24882: event should have been triggerd but was NOT
		'skip-combos: reset(null) + resetBegin > jumpWords(-1) + loopSkip': false,  // 24908: event should have been triggerd but was NOT
		'skip-combos: reset(null) + resetBegin > jumpWords(0) + loopSkip': false,  // 24934: event should have been triggerd but was NOT
		'skip-combos: reset(null) + resetBegin > jumpWords(4) + loopSkip': false,  // 24960: event should have been triggerd but was NOT
		'skip-combos: reset(null) + resetBegin > jumpWords(11) + loopSkip': false,  // 24986: event should have been triggerd but was NOT
		'skip-combos: reset(null) + resetBegin > jumpWords(100) + loopSkip': false,  // 25012: event should have been triggerd but was NOT
		'skip-combos: reset(null) + resetBegin > jumpSentences(-3) + loopSkip': false,  // 25038: event should have been triggerd but was NOT
		'skip-combos: reset(null) + resetBegin > jumpSentences(-1) + loopSkip': false,  // 25064: event should have been triggerd but was NOT
		'skip-combos: reset(null) + resetBegin > jumpSentences(0) + loopSkip': false,  // 25090: event should have been triggerd but was NOT
		'skip-combos: reset(null) + resetBegin > jumpSentences(1) + loopSkip': false,  // 25116: event should have been triggerd but was NOT
		'skip-combos: reset(null) + resetBegin > jumpSentences(100) + loopSkip': false,  // 25168: event should have been triggerd but was NOT
		'skip-combos: reset(null) + resetBegin > nextSentence(null) + loopSkip': false,  // 25220: event should have been triggerd but was NOT
		'skip-combos: reset(null) + resetBegin > prevWord(null) + loopSkip': false,  // 25246: event should have been triggerd but was NOT
		'skip-combos: reset(null) + resetBegin > prevSentence(null) + loopSkip': false,  // 25272: event should have been triggerd but was NOT

		// restart(null)
		'skip-combos: restart(null) + restartBegin > play(null) + loopSkip': false,  // 1976: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?"]
		'skip-combos: restart(null) + restartBegin > toggle(null) + loopSkip': false,  // 2002: event should have been triggerd but was NOT
		'skip-combos: restart(null) + restartFinish > play(null) + loopSkip': false,  // 2912: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?"]
		'skip-combos: restart(null) + restartFinish > toggle(null) + loopSkip': false,  // 2938: event should have been triggerd but was NOT
		'skip-combos: restart(null) + loopBegin > play(null) + loopSkip': false,  // 16952: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?"]
		'skip-combos: restart(null) + loopBegin > toggle(null) + loopSkip': false,  // 16978: event should have been triggerd but was NOT
		'skip-combos: restart(null) + loopFinish > play(null) + loopSkip': false,  // 17888: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?"]
		'skip-combos: restart(null) + loopFinish > toggle(null) + loopSkip': false,  // 17914: event should have been triggerd but was NOT
		'skip-combos: restart(null) + newWordFragment > play(null) + loopSkip': false,  // 18824: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?"]
		'skip-combos: restart(null) + newWordFragment > toggle(null) + loopSkip': false,  // 18850: event should have been triggerd but was NOT
		'skip-combos: restart(null) + newWordFragment > fastForward(null) + newWordFragment': false,  // 19001: frags expected ["you","brave","I","back.","Why,","oh"], but got ["brave","I","back.","Why,","oh"]
		'skip-combos: restart(null) + newWordFragment > fastForward(null) + progress': false,  // 19002: 'progress' expected [0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1]
		'skip-combos: restart(null) + newWordFragment > once([0,0,2]) + newWordFragment': false,  // 19053: frags expected ["you"], but got ["brave"]
		'skip-combos: restart(null) + newWordFragment > once([0,0,2]) + progress': false,  // 19054: 'progress' expected [0.16666666666666666], but got [0.25]
		'skip-combos: restart(null) + newWordFragment > current(null) + progress': false,  // 19080: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.16666666666666666]
		'skip-combos: restart(null) + newWordFragment > current(null) + loopSkip': false,  // 19084: event should have been triggerd but was NOT
		'skip-combos: restart(null) + newWordFragment > jumpWords(0) + progress': false,  // 19314: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.16666666666666666]
		'skip-combos: restart(null) + newWordFragment > jumpWords(0) + loopSkip': false,  // 19318: event should have been triggerd but was NOT
		'skip-combos: restart(null) + newWordFragment > jumpWords(4) + progress': false,  // 19340: 'progress' expected [0.4166666666666667,0.5], but got [0.5]
		'skip-combos: restart(null) + newWordFragment > jumpWords(4) + loopSkip': false,  // 19344: event should have been triggerd but was NOT
		'skip-combos: restart(null) + newWordFragment > jumpSentences(0) + progress': false,  // 19470: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.16666666666666666]
		'skip-combos: restart(null) + newWordFragment > jumpSentences(0) + loopSkip': false,  // 19474: event should have been triggerd but was NOT
		'skip-combos: restart(null) + newWordFragment > nextWord(null) + newWordFragment': false,  // 19573: frags expected ["you"], but got ["brave"]
		'skip-combos: restart(null) + newWordFragment > nextWord(null) + progress': false,  // 19574: 'progress' expected [0.16666666666666666], but got [0.25]
		'skip-combos: restart(null) + progress > play(null) + loopSkip': false,  // 19760: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?"]
		'skip-combos: restart(null) + progress > toggle(null) + loopSkip': false,  // 19786: event should have been triggerd but was NOT
		'skip-combos: restart(null) + stopBegin > rewind(null) + loopSkip': false,  // 20852: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: restart(null) + stopBegin > fastForward(null) + loopSkip': false,  // 20878: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: restart(null) + stopBegin > once([0,0,-2]) + loopSkip': false,  // 20904: event should have been triggerd but was NOT
		'skip-combos: restart(null) + stopBegin > once([0,0,2]) + loopSkip': false,  // 20930: event should not have been triggerd but WAS
		'skip-combos: restart(null) + stopBegin > current(null) + loopSkip': false,  // 20956: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: restart(null) + stopBegin > jumpWords(-3) + loopSkip': false,  // 21138: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: restart(null) + stopBegin > jumpWords(-1) + loopSkip': false,  // 21164: event should have been triggerd but was NOT
		'skip-combos: restart(null) + stopBegin > jumpWords(0) + loopSkip': false,  // 21190: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: restart(null) + stopBegin > jumpWords(4) + loopSkip': false,  // 21216: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: restart(null) + stopBegin > jumpSentences(-1) + loopSkip': false,  // 21320: event should have been triggerd but was NOT
		'skip-combos: restart(null) + stopBegin > jumpSentences(0) + loopSkip': false,  // 21346: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: restart(null) + stopBegin > jumpSentences(1) + loopSkip': false,  // 21372: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: restart(null) + stopBegin > jumpSentences(3) + loopSkip': false,  // 21398: event should not have been triggerd but WAS
		'skip-combos: restart(null) + stopBegin > nextWord(null) + loopSkip': false,  // 21450: event should not have been triggerd but WAS
		'skip-combos: restart(null) + stopBegin > nextSentence(null) + loopSkip': false,  // 21476: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: restart(null) + stopBegin > prevWord(null) + loopSkip': false,  // 21502: event should have been triggerd but was NOT
		'skip-combos: restart(null) + stopBegin > prevSentence(null) + loopSkip': false,  // 21528: event should have been triggerd but was NOT
		'skip-combos: restart(null) + stopFinish > rewind(null) + loopSkip': false,  // 21788: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: restart(null) + stopFinish > fastForward(null) + loopSkip': false,  // 21814: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: restart(null) + stopFinish > once([0,0,-2]) + loopSkip': false,  // 21840: event should have been triggerd but was NOT
		'skip-combos: restart(null) + stopFinish > once([0,0,2]) + loopSkip': false,  // 21866: event should not have been triggerd but WAS
		'skip-combos: restart(null) + stopFinish > current(null) + loopSkip': false,  // 21892: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: restart(null) + stopFinish > jumpWords(-3) + loopSkip': false,  // 22074: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: restart(null) + stopFinish > jumpWords(-1) + loopSkip': false,  // 22100: event should have been triggerd but was NOT
		'skip-combos: restart(null) + stopFinish > jumpWords(0) + loopSkip': false,  // 22126: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: restart(null) + stopFinish > jumpWords(4) + loopSkip': false,  // 22152: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: restart(null) + stopFinish > jumpSentences(-1) + loopSkip': false,  // 22256: event should have been triggerd but was NOT
		'skip-combos: restart(null) + stopFinish > jumpSentences(0) + loopSkip': false,  // 22282: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: restart(null) + stopFinish > jumpSentences(1) + loopSkip': false,  // 22308: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: restart(null) + stopFinish > jumpSentences(3) + loopSkip': false,  // 22334: event should not have been triggerd but WAS
		'skip-combos: restart(null) + stopFinish > nextWord(null) + loopSkip': false,  // 22386: event should not have been triggerd but WAS
		'skip-combos: restart(null) + stopFinish > nextSentence(null) + loopSkip': false,  // 22412: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: restart(null) + stopFinish > prevWord(null) + loopSkip': false,  // 22438: event should have been triggerd but was NOT
		'skip-combos: restart(null) + stopFinish > prevSentence(null) + loopSkip': false,  // 22464: event should have been triggerd but was NOT
		'skip-combos: restart(null) + done > rewind(null) + loopSkip': false,  // 22724: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: restart(null) + done > fastForward(null) + loopSkip': false,  // 22750: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: restart(null) + done > once([0,0,-2]) + loopSkip': false,  // 22776: event should have been triggerd but was NOT
		'skip-combos: restart(null) + done > once([0,0,2]) + loopSkip': false,  // 22802: event should not have been triggerd but WAS
		'skip-combos: restart(null) + done > current(null) + loopSkip': false,  // 22828: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: restart(null) + done > jumpWords(-3) + loopSkip': false,  // 23010: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: restart(null) + done > jumpWords(-1) + loopSkip': false,  // 23036: event should have been triggerd but was NOT
		'skip-combos: restart(null) + done > jumpWords(0) + loopSkip': false,  // 23062: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: restart(null) + done > jumpWords(4) + loopSkip': false,  // 23088: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: restart(null) + done > jumpSentences(-1) + loopSkip': false,  // 23192: event should have been triggerd but was NOT
		'skip-combos: restart(null) + done > jumpSentences(0) + loopSkip': false,  // 23218: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: restart(null) + done > jumpSentences(1) + loopSkip': false,  // 23244: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: restart(null) + done > jumpSentences(3) + loopSkip': false,  // 23270: event should not have been triggerd but WAS
		'skip-combos: restart(null) + done > nextWord(null) + loopSkip': false,  // 23322: event should not have been triggerd but WAS
		'skip-combos: restart(null) + done > nextSentence(null) + loopSkip': false,  // 23348: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: restart(null) + done > prevWord(null) + loopSkip': false,  // 23374: event should have been triggerd but was NOT
		'skip-combos: restart(null) + done > prevSentence(null) + loopSkip': false,  // 23400: event should have been triggerd but was NOT
		'skip-combos: restart(null) + loopSkip > play(null) + progress': false,  // 23500: 'progress' expected [0.08333333333333333,0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1]
		'skip-combos: restart(null) + loopSkip > play(null) + loopSkip': false,  // 23504: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?"]
		'skip-combos: restart(null) + loopSkip > toggle(null) + playBegin': false,  // 23505: event should have been triggerd but was NOT
		'skip-combos: restart(null) + loopSkip > toggle(null) + playFinish': false,  // 23506: event should have been triggerd but was NOT
		'skip-combos: restart(null) + loopSkip > toggle(null) + pauseBegin': false,  // 23511: event should not have been triggerd but WAS
		'skip-combos: restart(null) + loopSkip > toggle(null) + pauseFinish': false,  // 23512: event should not have been triggerd but WAS
		'skip-combos: restart(null) + loopSkip > toggle(null) + loopBegin': false,  // 23523: event should have been triggerd but was NOT
		'skip-combos: restart(null) + loopSkip > toggle(null) + loopFinish': false,  // 23524: event should have been triggerd but was NOT
		'skip-combos: restart(null) + loopSkip > toggle(null) + newWordFragment': false,  // 23525: event should have been triggerd but was NOT
		'skip-combos: restart(null) + loopSkip > toggle(null) + progress': false,  // 23526: event should have been triggerd but was NOT
		'skip-combos: restart(null) + loopSkip > toggle(null) + stopBegin': false,  // 23527: event should have been triggerd but was NOT
		'skip-combos: restart(null) + loopSkip > toggle(null) + stopFinish': false,  // 23528: event should have been triggerd but was NOT
		'skip-combos: restart(null) + loopSkip > toggle(null) + done': false,  // 23529: event should have been triggerd but was NOT
		'skip-combos: restart(null) + loopSkip > toggle(null) + loopSkip': false,  // 23530: event should have been triggerd but was NOT
		'skip-combos: restart(null) + loopSkip > revert(null) + playBegin': false,  // 23609: event should not have been triggerd but WAS
		'skip-combos: restart(null) + loopSkip > revert(null) + playFinish': false,  // 23610: event should not have been triggerd but WAS
		'skip-combos: restart(null) + loopSkip > revert(null) + pauseBegin': false,  // 23615: event should have been triggerd but was NOT
		'skip-combos: restart(null) + loopSkip > revert(null) + pauseFinish': false,  // 23616: event should have been triggerd but was NOT

		// play(null)
		'skip-combos: play(null) + playBegin > play(null) + loopSkip': false,  // 28184: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?"]
		'skip-combos: play(null) + playBegin > toggle(null) + loopSkip': false,  // 28210: event should have been triggerd but was NOT
		'skip-combos: play(null) + playFinish > play(null) + loopSkip': false,  // 29120: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?"]
		'skip-combos: play(null) + playFinish > toggle(null) + loopSkip': false,  // 29146: event should have been triggerd but was NOT
		'skip-combos: play(null) + loopBegin > play(null) + loopSkip': false,  // 41288: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?"]
		'skip-combos: play(null) + loopBegin > toggle(null) + loopSkip': false,  // 41314: event should have been triggerd but was NOT
		'skip-combos: play(null) + loopFinish > play(null) + loopSkip': false,  // 42224: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?"]
		'skip-combos: play(null) + loopFinish > toggle(null) + loopSkip': false,  // 42250: event should have been triggerd but was NOT
		'skip-combos: play(null) + newWordFragment > play(null) + loopSkip': false,  // 43160: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?"]
		'skip-combos: play(null) + newWordFragment > toggle(null) + loopSkip': false,  // 43186: event should have been triggerd but was NOT
		'skip-combos: play(null) + newWordFragment > fastForward(null) + newWordFragment': false,  // 43337: frags expected ["you","brave","I","back.","Why,","oh"], but got ["brave","I","back.","Why,","oh"]
		'skip-combos: play(null) + newWordFragment > fastForward(null) + progress': false,  // 43338: 'progress' expected [0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1]
		'skip-combos: play(null) + newWordFragment > once([0,0,2]) + newWordFragment': false,  // 43389: frags expected ["you"], but got ["brave"]
		'skip-combos: play(null) + newWordFragment > once([0,0,2]) + progress': false,  // 43390: 'progress' expected [0.16666666666666666], but got [0.25]
		'skip-combos: play(null) + newWordFragment > current(null) + progress': false,  // 43416: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.16666666666666666]
		'skip-combos: play(null) + newWordFragment > current(null) + loopSkip': false,  // 43420: event should have been triggerd but was NOT
		'skip-combos: play(null) + newWordFragment > jumpWords(0) + progress': false,  // 43650: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.16666666666666666]
		'skip-combos: play(null) + newWordFragment > jumpWords(0) + loopSkip': false,  // 43654: event should have been triggerd but was NOT
		'skip-combos: play(null) + newWordFragment > jumpWords(4) + progress': false,  // 43676: 'progress' expected [0.4166666666666667,0.5], but got [0.5]
		'skip-combos: play(null) + newWordFragment > jumpWords(4) + loopSkip': false,  // 43680: event should have been triggerd but was NOT
		'skip-combos: play(null) + newWordFragment > jumpSentences(0) + progress': false,  // 43806: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.16666666666666666]
		'skip-combos: play(null) + newWordFragment > jumpSentences(0) + loopSkip': false,  // 43810: event should have been triggerd but was NOT
		'skip-combos: play(null) + newWordFragment > nextWord(null) + newWordFragment': false,  // 43909: frags expected ["you"], but got ["brave"]
		'skip-combos: play(null) + newWordFragment > nextWord(null) + progress': false,  // 43910: 'progress' expected [0.16666666666666666], but got [0.25]
		'skip-combos: play(null) + progress > play(null) + loopSkip': false,  // 44096: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?"]
		'skip-combos: play(null) + progress > toggle(null) + loopSkip': false,  // 44122: event should have been triggerd but was NOT
		'skip-combos: play(null) + stopBegin > rewind(null) + loopSkip': false,  // 45188: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: play(null) + stopBegin > fastForward(null) + loopSkip': false,  // 45214: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: play(null) + stopBegin > once([0,0,-2]) + loopSkip': false,  // 45240: event should have been triggerd but was NOT
		'skip-combos: play(null) + stopBegin > once([0,0,2]) + loopSkip': false,  // 45266: event should not have been triggerd but WAS
		'skip-combos: play(null) + stopBegin > current(null) + loopSkip': false,  // 45292: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: play(null) + stopBegin > jumpWords(-3) + loopSkip': false,  // 45474: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: play(null) + stopBegin > jumpWords(-1) + loopSkip': false,  // 45500: event should have been triggerd but was NOT
		'skip-combos: play(null) + stopBegin > jumpWords(0) + loopSkip': false,  // 45526: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: play(null) + stopBegin > jumpWords(4) + loopSkip': false,  // 45552: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: play(null) + stopBegin > jumpSentences(-1) + loopSkip': false,  // 45656: event should have been triggerd but was NOT
		'skip-combos: play(null) + stopBegin > jumpSentences(0) + loopSkip': false,  // 45682: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: play(null) + stopBegin > jumpSentences(1) + loopSkip': false,  // 45708: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: play(null) + stopBegin > jumpSentences(3) + loopSkip': false,  // 45734: event should not have been triggerd but WAS
		'skip-combos: play(null) + stopBegin > nextWord(null) + loopSkip': false,  // 45786: event should not have been triggerd but WAS
		'skip-combos: play(null) + stopBegin > nextSentence(null) + loopSkip': false,  // 45812: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: play(null) + stopBegin > prevWord(null) + loopSkip': false,  // 45838: event should have been triggerd but was NOT
		'skip-combos: play(null) + stopBegin > prevSentence(null) + loopSkip': false,  // 45864: event should have been triggerd but was NOT
		'skip-combos: play(null) + stopFinish > rewind(null) + loopSkip': false,  // 46124: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: play(null) + stopFinish > fastForward(null) + loopSkip': false,  // 46150: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: play(null) + stopFinish > once([0,0,-2]) + loopSkip': false,  // 46176: event should have been triggerd but was NOT
		'skip-combos: play(null) + stopFinish > once([0,0,2]) + loopSkip': false,  // 46202: event should not have been triggerd but WAS
		'skip-combos: play(null) + stopFinish > current(null) + loopSkip': false,  // 46228: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: play(null) + stopFinish > jumpWords(-3) + loopSkip': false,  // 46410: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: play(null) + stopFinish > jumpWords(-1) + loopSkip': false,  // 46436: event should have been triggerd but was NOT
		'skip-combos: play(null) + stopFinish > jumpWords(0) + loopSkip': false,  // 46462: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: play(null) + stopFinish > jumpWords(4) + loopSkip': false,  // 46488: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: play(null) + stopFinish > jumpSentences(-1) + loopSkip': false,  // 46592: event should have been triggerd but was NOT
		'skip-combos: play(null) + stopFinish > jumpSentences(0) + loopSkip': false,  // 46618: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: play(null) + stopFinish > jumpSentences(1) + loopSkip': false,  // 46644: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: play(null) + stopFinish > jumpSentences(3) + loopSkip': false,  // 46670: event should not have been triggerd but WAS
		'skip-combos: play(null) + stopFinish > nextWord(null) + loopSkip': false,  // 46722: event should not have been triggerd but WAS
		'skip-combos: play(null) + stopFinish > nextSentence(null) + loopSkip': false,  // 46748: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: play(null) + stopFinish > prevWord(null) + loopSkip': false,  // 46774: event should have been triggerd but was NOT
		'skip-combos: play(null) + stopFinish > prevSentence(null) + loopSkip': false,  // 46800: event should have been triggerd but was NOT
		'skip-combos: play(null) + done > rewind(null) + loopSkip': false,  // 47060: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: play(null) + done > fastForward(null) + loopSkip': false,  // 47086: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: play(null) + done > once([0,0,-2]) + loopSkip': false,  // 47112: event should have been triggerd but was NOT
		'skip-combos: play(null) + done > once([0,0,2]) + loopSkip': false,  // 47138: event should not have been triggerd but WAS
		'skip-combos: play(null) + done > current(null) + loopSkip': false,  // 47164: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: play(null) + done > jumpWords(-3) + loopSkip': false,  // 47346: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: play(null) + done > jumpWords(-1) + loopSkip': false,  // 47372: event should have been triggerd but was NOT
		'skip-combos: play(null) + done > jumpWords(0) + loopSkip': false,  // 47398: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: play(null) + done > jumpWords(4) + loopSkip': false,  // 47424: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: play(null) + done > jumpSentences(-1) + loopSkip': false,  // 47528: event should have been triggerd but was NOT
		'skip-combos: play(null) + done > jumpSentences(0) + loopSkip': false,  // 47554: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: play(null) + done > jumpSentences(1) + loopSkip': false,  // 47580: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: play(null) + done > jumpSentences(3) + loopSkip': false,  // 47606: event should not have been triggerd but WAS
		'skip-combos: play(null) + done > nextWord(null) + loopSkip': false,  // 47658: event should not have been triggerd but WAS
		'skip-combos: play(null) + done > nextSentence(null) + loopSkip': false,  // 47684: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: play(null) + done > prevWord(null) + loopSkip': false,  // 47710: event should have been triggerd but was NOT
		'skip-combos: play(null) + done > prevSentence(null) + loopSkip': false,  // 47736: event should have been triggerd but was NOT
		'skip-combos: play(null) + loopSkip > play(null) + progress': false,  // 47836: 'progress' expected [0.08333333333333333,0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1]
		'skip-combos: play(null) + loopSkip > play(null) + loopSkip': false,  // 47840: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?"]
		'skip-combos: play(null) + loopSkip > toggle(null) + playBegin': false,  // 47841: event should have been triggerd but was NOT
		'skip-combos: play(null) + loopSkip > toggle(null) + playFinish': false,  // 47842: event should have been triggerd but was NOT
		'skip-combos: play(null) + loopSkip > toggle(null) + pauseBegin': false,  // 47847: event should not have been triggerd but WAS
		'skip-combos: play(null) + loopSkip > toggle(null) + pauseFinish': false,  // 47848: event should not have been triggerd but WAS
		'skip-combos: play(null) + loopSkip > toggle(null) + loopBegin': false,  // 47859: event should have been triggerd but was NOT
		'skip-combos: play(null) + loopSkip > toggle(null) + loopFinish': false,  // 47860: event should have been triggerd but was NOT
		'skip-combos: play(null) + loopSkip > toggle(null) + newWordFragment': false,  // 47861: event should have been triggerd but was NOT
		'skip-combos: play(null) + loopSkip > toggle(null) + progress': false,  // 47862: event should have been triggerd but was NOT
		'skip-combos: play(null) + loopSkip > toggle(null) + stopBegin': false,  // 47863: event should have been triggerd but was NOT
		'skip-combos: play(null) + loopSkip > toggle(null) + stopFinish': false,  // 47864: event should have been triggerd but was NOT
		'skip-combos: play(null) + loopSkip > toggle(null) + done': false,  // 47865: event should have been triggerd but was NOT
		'skip-combos: play(null) + loopSkip > toggle(null) + loopSkip': false,  // 47866: event should have been triggerd but was NOT
		'skip-combos: play(null) + loopSkip > revert(null) + playBegin': false,  // 47945: event should not have been triggerd but WAS
		'skip-combos: play(null) + loopSkip > revert(null) + playFinish': false,  // 47946: event should not have been triggerd but WAS
		'skip-combos: play(null) + loopSkip > revert(null) + pauseBegin': false,  // 47951: event should have been triggerd but was NOT
		'skip-combos: play(null) + loopSkip > revert(null) + pauseFinish': false,  // 47952: event should have been triggerd but was NOT

		// toggle(null)
		'skip-combos: toggle(null) + playBegin > play(null) + loopSkip': false,  // 3848: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?"]
		'skip-combos: toggle(null) + playBegin > toggle(null) + loopSkip': false,  // 3874: event should have been triggerd but was NOT
		'skip-combos: toggle(null) + playFinish > play(null) + loopSkip': false,  // 4784: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?"]
		'skip-combos: toggle(null) + playFinish > toggle(null) + loopSkip': false,  // 4810: event should have been triggerd but was NOT
		'skip-combos: toggle(null) + loopBegin > play(null) + loopSkip': false,  // 16952: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?"]
		'skip-combos: toggle(null) + loopBegin > toggle(null) + loopSkip': false,  // 16978: event should have been triggerd but was NOT
		'skip-combos: toggle(null) + loopFinish > play(null) + loopSkip': false,  // 17888: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?"]
		'skip-combos: toggle(null) + loopFinish > toggle(null) + loopSkip': false,  // 17914: event should have been triggerd but was NOT
		'skip-combos: toggle(null) + newWordFragment > play(null) + loopSkip': false,  // 18824: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?"]
		'skip-combos: toggle(null) + newWordFragment > toggle(null) + loopSkip': false,  // 18850: event should have been triggerd but was NOT
		'skip-combos: toggle(null) + newWordFragment > fastForward(null) + newWordFragment': false,  // 19001: frags expected ["you","brave","I","back.","Why,","oh"], but got ["brave","I","back.","Why,","oh"]
		'skip-combos: toggle(null) + newWordFragment > fastForward(null) + progress': false,  // 19002: 'progress' expected [0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1]
		'skip-combos: toggle(null) + newWordFragment > once([0,0,2]) + newWordFragment': false,  // 19053: frags expected ["you"], but got ["brave"]
		'skip-combos: toggle(null) + newWordFragment > once([0,0,2]) + progress': false,  // 19054: 'progress' expected [0.16666666666666666], but got [0.25]
		'skip-combos: toggle(null) + newWordFragment > current(null) + progress': false,  // 19080: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.16666666666666666]
		'skip-combos: toggle(null) + newWordFragment > current(null) + loopSkip': false,  // 19084: event should have been triggerd but was NOT
		'skip-combos: toggle(null) + newWordFragment > jumpWords(0) + progress': false,  // 19314: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.16666666666666666]
		'skip-combos: toggle(null) + newWordFragment > jumpWords(0) + loopSkip': false,  // 19318: event should have been triggerd but was NOT
		'skip-combos: toggle(null) + newWordFragment > jumpWords(4) + progress': false,  // 19340: 'progress' expected [0.4166666666666667,0.5], but got [0.5]
		'skip-combos: toggle(null) + newWordFragment > jumpWords(4) + loopSkip': false,  // 19344: event should have been triggerd but was NOT
		'skip-combos: toggle(null) + newWordFragment > jumpSentences(0) + progress': false,  // 19470: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.16666666666666666]
		'skip-combos: toggle(null) + newWordFragment > jumpSentences(0) + loopSkip': false,  // 19474: event should have been triggerd but was NOT
		'skip-combos: toggle(null) + newWordFragment > nextWord(null) + newWordFragment': false,  // 19573: frags expected ["you"], but got ["brave"]
		'skip-combos: toggle(null) + newWordFragment > nextWord(null) + progress': false,  // 19574: 'progress' expected [0.16666666666666666], but got [0.25]
		'skip-combos: toggle(null) + progress > play(null) + loopSkip': false,  // 19760: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?"]
		'skip-combos: toggle(null) + progress > toggle(null) + loopSkip': false,  // 19786: event should have been triggerd but was NOT
		'skip-combos: toggle(null) + stopBegin > rewind(null) + loopSkip': false,  // 20852: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: toggle(null) + stopBegin > fastForward(null) + loopSkip': false,  // 20878: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: toggle(null) + stopBegin > once([0,0,-2]) + loopSkip': false,  // 20904: event should have been triggerd but was NOT
		'skip-combos: toggle(null) + stopBegin > once([0,0,2]) + loopSkip': false,  // 20930: event should not have been triggerd but WAS
		'skip-combos: toggle(null) + stopBegin > current(null) + loopSkip': false,  // 20956: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: toggle(null) + stopBegin > jumpWords(-3) + loopSkip': false,  // 21138: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: toggle(null) + stopBegin > jumpWords(-1) + loopSkip': false,  // 21164: event should have been triggerd but was NOT
		'skip-combos: toggle(null) + stopBegin > jumpWords(0) + loopSkip': false,  // 21190: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: toggle(null) + stopBegin > jumpWords(4) + loopSkip': false,  // 21216: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: toggle(null) + stopBegin > jumpSentences(-1) + loopSkip': false,  // 21320: event should have been triggerd but was NOT
		'skip-combos: toggle(null) + stopBegin > jumpSentences(0) + loopSkip': false,  // 21346: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: toggle(null) + stopBegin > jumpSentences(1) + loopSkip': false,  // 21372: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: toggle(null) + stopBegin > jumpSentences(3) + loopSkip': false,  // 21398: event should not have been triggerd but WAS
		'skip-combos: toggle(null) + stopBegin > nextWord(null) + loopSkip': false,  // 21450: event should not have been triggerd but WAS
		'skip-combos: toggle(null) + stopBegin > nextSentence(null) + loopSkip': false,  // 21476: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: toggle(null) + stopBegin > prevWord(null) + loopSkip': false,  // 21502: event should have been triggerd but was NOT
		'skip-combos: toggle(null) + stopBegin > prevSentence(null) + loopSkip': false,  // 21528: event should have been triggerd but was NOT
		'skip-combos: toggle(null) + stopFinish > rewind(null) + loopSkip': false,  // 21788: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: toggle(null) + stopFinish > fastForward(null) + loopSkip': false,  // 21814: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: toggle(null) + stopFinish > once([0,0,-2]) + loopSkip': false,  // 21840: event should have been triggerd but was NOT
		'skip-combos: toggle(null) + stopFinish > once([0,0,2]) + loopSkip': false,  // 21866: event should not have been triggerd but WAS
		'skip-combos: toggle(null) + stopFinish > current(null) + loopSkip': false,  // 21892: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: toggle(null) + stopFinish > jumpWords(-3) + loopSkip': false,  // 22074: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: toggle(null) + stopFinish > jumpWords(-1) + loopSkip': false,  // 22100: event should have been triggerd but was NOT
		'skip-combos: toggle(null) + stopFinish > jumpWords(0) + loopSkip': false,  // 22126: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: toggle(null) + stopFinish > jumpWords(4) + loopSkip': false,  // 22152: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: toggle(null) + stopFinish > jumpSentences(-1) + loopSkip': false,  // 22256: event should have been triggerd but was NOT
		'skip-combos: toggle(null) + stopFinish > jumpSentences(0) + loopSkip': false,  // 22282: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: toggle(null) + stopFinish > jumpSentences(1) + loopSkip': false,  // 22308: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: toggle(null) + stopFinish > jumpSentences(3) + loopSkip': false,  // 22334: event should not have been triggerd but WAS
		'skip-combos: toggle(null) + stopFinish > nextWord(null) + loopSkip': false,  // 22386: event should not have been triggerd but WAS
		'skip-combos: toggle(null) + stopFinish > nextSentence(null) + loopSkip': false,  // 22412: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: toggle(null) + stopFinish > prevWord(null) + loopSkip': false,  // 22438: event should have been triggerd but was NOT
		'skip-combos: toggle(null) + stopFinish > prevSentence(null) + loopSkip': false,  // 22464: event should have been triggerd but was NOT
		'skip-combos: toggle(null) + done > rewind(null) + loopSkip': false,  // 22724: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: toggle(null) + done > fastForward(null) + loopSkip': false,  // 22750: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: toggle(null) + done > once([0,0,-2]) + loopSkip': false,  // 22776: event should have been triggerd but was NOT
		'skip-combos: toggle(null) + done > once([0,0,2]) + loopSkip': false,  // 22802: event should not have been triggerd but WAS
		'skip-combos: toggle(null) + done > current(null) + loopSkip': false,  // 22828: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: toggle(null) + done > jumpWords(-3) + loopSkip': false,  // 23010: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: toggle(null) + done > jumpWords(-1) + loopSkip': false,  // 23036: event should have been triggerd but was NOT
		'skip-combos: toggle(null) + done > jumpWords(0) + loopSkip': false,  // 23062: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: toggle(null) + done > jumpWords(4) + loopSkip': false,  // 23088: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: toggle(null) + done > jumpSentences(-1) + loopSkip': false,  // 23192: event should have been triggerd but was NOT
		'skip-combos: toggle(null) + done > jumpSentences(0) + loopSkip': false,  // 23218: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: toggle(null) + done > jumpSentences(1) + loopSkip': false,  // 23244: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: toggle(null) + done > jumpSentences(3) + loopSkip': false,  // 23270: event should not have been triggerd but WAS
		'skip-combos: toggle(null) + done > nextWord(null) + loopSkip': false,  // 23322: event should not have been triggerd but WAS
		'skip-combos: toggle(null) + done > nextSentence(null) + loopSkip': false,  // 23348: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: toggle(null) + done > prevWord(null) + loopSkip': false,  // 23374: event should have been triggerd but was NOT
		'skip-combos: toggle(null) + done > prevSentence(null) + loopSkip': false,  // 23400: event should have been triggerd but was NOT
		'skip-combos: toggle(null) + loopSkip > play(null) + progress': false,  // 23500: 'progress' expected [0.08333333333333333,0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1]
		'skip-combos: toggle(null) + loopSkip > play(null) + loopSkip': false,  // 23504: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?"]
		'skip-combos: toggle(null) + loopSkip > toggle(null) + playBegin': false,  // 23505: event should have been triggerd but was NOT
		'skip-combos: toggle(null) + loopSkip > toggle(null) + playFinish': false,  // 23506: event should have been triggerd but was NOT
		'skip-combos: toggle(null) + loopSkip > toggle(null) + pauseBegin': false,  // 23511: event should not have been triggerd but WAS
		'skip-combos: toggle(null) + loopSkip > toggle(null) + pauseFinish': false,  // 23512: event should not have been triggerd but WAS
		'skip-combos: toggle(null) + loopSkip > toggle(null) + loopBegin': false,  // 23523: event should have been triggerd but was NOT
		'skip-combos: toggle(null) + loopSkip > toggle(null) + loopFinish': false,  // 23524: event should have been triggerd but was NOT
		'skip-combos: toggle(null) + loopSkip > toggle(null) + newWordFragment': false,  // 23525: event should have been triggerd but was NOT
		'skip-combos: toggle(null) + loopSkip > toggle(null) + progress': false,  // 23526: event should have been triggerd but was NOT
		'skip-combos: toggle(null) + loopSkip > toggle(null) + stopBegin': false,  // 23527: event should have been triggerd but was NOT
		'skip-combos: toggle(null) + loopSkip > toggle(null) + stopFinish': false,  // 23528: event should have been triggerd but was NOT
		'skip-combos: toggle(null) + loopSkip > toggle(null) + done': false,  // 23529: event should have been triggerd but was NOT
		'skip-combos: toggle(null) + loopSkip > toggle(null) + loopSkip': false,  // 23530: event should have been triggerd but was NOT
		'skip-combos: toggle(null) + loopSkip > revert(null) + playBegin': false,  // 23609: event should not have been triggerd but WAS
		'skip-combos: toggle(null) + loopSkip > revert(null) + playFinish': false,  // 23610: event should not have been triggerd but WAS
		'skip-combos: toggle(null) + loopSkip > revert(null) + pauseBegin': false,  // 23615: event should have been triggerd but was NOT
		'skip-combos: toggle(null) + loopSkip > revert(null) + pauseFinish': false,  // 23616: event should have been triggerd but was NOT

		// pause(null)
		// stop(null)
		// close(null)
		// revert(null)

		// rewind(null)
		'skip-combos: rewind(null) + loopSkip > play(null) + playBegin': false,  // 47815: event should have been triggerd but was NOT
		'skip-combos: rewind(null) + loopSkip > play(null) + playFinish': false,  // 47816: event should have been triggerd but was NOT
		'skip-combos: rewind(null) + loopSkip > play(null) + restartBegin': false,  // 47819: event should not have been triggerd but WAS
		'skip-combos: rewind(null) + loopSkip > play(null) + restartFinish': false,  // 47820: event should not have been triggerd but WAS
		'skip-combos: rewind(null) + loopSkip > toggle(null) + playBegin': false,  // 47841: event should have been triggerd but was NOT
		'skip-combos: rewind(null) + loopSkip > toggle(null) + playFinish': false,  // 47842: event should have been triggerd but was NOT
		'skip-combos: rewind(null) + loopSkip > toggle(null) + restartBegin': false,  // 47845: event should not have been triggerd but WAS
		'skip-combos: rewind(null) + loopSkip > toggle(null) + restartFinish': false,  // 47846: event should not have been triggerd but WAS

		// fastForward(null)
		'skip-combos: fastForward(null) + fastForwardBegin > play(null) + loopSkip': false,  // 15080: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?","wattlebird?"]
		'skip-combos: fastForward(null) + fastForwardBegin > toggle(null) + loopSkip': false,  // 15106: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + fastForwardBegin > current(null) + loopSkip': false,  // 15340: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + fastForwardBegin > jumpWords(0) + loopSkip': false,  // 15574: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + fastForwardBegin > jumpWords(4) + loopSkip': false,  // 15600: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + fastForwardBegin > jumpSentences(0) + loopSkip': false,  // 15730: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + fastForwardFinish > play(null) + loopSkip': false,  // 16016: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?","wattlebird?"]
		'skip-combos: fastForward(null) + fastForwardFinish > toggle(null) + loopSkip': false,  // 16042: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + fastForwardFinish > current(null) + loopSkip': false,  // 16276: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + fastForwardFinish > jumpWords(0) + loopSkip': false,  // 16510: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + fastForwardFinish > jumpWords(4) + loopSkip': false,  // 16536: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + fastForwardFinish > jumpSentences(0) + loopSkip': false,  // 16666: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + loopBegin > play(null) + loopSkip': false,  // 16952: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?","wattlebird?"]
		'skip-combos: fastForward(null) + loopBegin > toggle(null) + loopSkip': false,  // 16978: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + loopBegin > current(null) + loopSkip': false,  // 17212: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + loopBegin > jumpWords(0) + loopSkip': false,  // 17446: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + loopBegin > jumpWords(4) + loopSkip': false,  // 17472: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + loopBegin > jumpSentences(0) + loopSkip': false,  // 17602: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + loopFinish > play(null) + loopSkip': false,  // 17888: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?","wattlebird?"]
		'skip-combos: fastForward(null) + loopFinish > toggle(null) + loopSkip': false,  // 17914: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + loopFinish > current(null) + loopSkip': false,  // 18148: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + loopFinish > jumpWords(0) + loopSkip': false,  // 18382: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + loopFinish > jumpWords(4) + loopSkip': false,  // 18408: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + loopFinish > jumpSentences(0) + loopSkip': false,  // 18538: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + newWordFragment > play(null) + loopSkip': false,  // 18824: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?","wattlebird?"]
		'skip-combos: fastForward(null) + newWordFragment > toggle(null) + loopSkip': false,  // 18850: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + newWordFragment > current(null) + loopSkip': false,  // 19084: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + newWordFragment > jumpWords(0) + loopSkip': false,  // 19318: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + newWordFragment > jumpWords(4) + loopSkip': false,  // 19344: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + newWordFragment > jumpSentences(0) + loopSkip': false,  // 19474: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + progress > play(null) + loopSkip': false,  // 19760: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?","wattlebird?"]
		'skip-combos: fastForward(null) + progress > toggle(null) + loopSkip': false,  // 19786: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + progress > current(null) + loopSkip': false,  // 20020: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + progress > jumpWords(0) + loopSkip': false,  // 20254: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + progress > jumpWords(4) + loopSkip': false,  // 20280: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + progress > jumpSentences(0) + loopSkip': false,  // 20410: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + stopBegin > rewind(null) + loopSkip': false,  // 20852: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: fastForward(null) + stopBegin > fastForward(null) + loopSkip': false,  // 20878: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: fastForward(null) + stopBegin > once([0,0,-2]) + loopSkip': false,  // 20904: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + stopBegin > once([0,0,2]) + loopSkip': false,  // 20930: event should not have been triggerd but WAS
		'skip-combos: fastForward(null) + stopBegin > current(null) + loopSkip': false,  // 20956: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: fastForward(null) + stopBegin > jumpWords(-3) + loopSkip': false,  // 21138: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: fastForward(null) + stopBegin > jumpWords(-1) + loopSkip': false,  // 21164: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + stopBegin > jumpWords(0) + loopSkip': false,  // 21190: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: fastForward(null) + stopBegin > jumpWords(4) + loopSkip': false,  // 21216: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: fastForward(null) + stopBegin > jumpSentences(-1) + loopSkip': false,  // 21320: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + stopBegin > jumpSentences(0) + loopSkip': false,  // 21346: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: fastForward(null) + stopBegin > jumpSentences(1) + loopSkip': false,  // 21372: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: fastForward(null) + stopBegin > jumpSentences(3) + loopSkip': false,  // 21398: event should not have been triggerd but WAS
		'skip-combos: fastForward(null) + stopBegin > nextWord(null) + loopSkip': false,  // 21450: event should not have been triggerd but WAS
		'skip-combos: fastForward(null) + stopBegin > nextSentence(null) + loopSkip': false,  // 21476: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: fastForward(null) + stopBegin > prevWord(null) + loopSkip': false,  // 21502: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + stopBegin > prevSentence(null) + loopSkip': false,  // 21528: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + stopFinish > rewind(null) + loopSkip': false,  // 21788: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: fastForward(null) + stopFinish > fastForward(null) + loopSkip': false,  // 21814: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: fastForward(null) + stopFinish > once([0,0,-2]) + loopSkip': false,  // 21840: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + stopFinish > once([0,0,2]) + loopSkip': false,  // 21866: event should not have been triggerd but WAS
		'skip-combos: fastForward(null) + stopFinish > current(null) + loopSkip': false,  // 21892: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: fastForward(null) + stopFinish > jumpWords(-3) + loopSkip': false,  // 22074: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: fastForward(null) + stopFinish > jumpWords(-1) + loopSkip': false,  // 22100: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + stopFinish > jumpWords(0) + loopSkip': false,  // 22126: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: fastForward(null) + stopFinish > jumpWords(4) + loopSkip': false,  // 22152: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: fastForward(null) + stopFinish > jumpSentences(-1) + loopSkip': false,  // 22256: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + stopFinish > jumpSentences(0) + loopSkip': false,  // 22282: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: fastForward(null) + stopFinish > jumpSentences(1) + loopSkip': false,  // 22308: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: fastForward(null) + stopFinish > jumpSentences(3) + loopSkip': false,  // 22334: event should not have been triggerd but WAS
		'skip-combos: fastForward(null) + stopFinish > nextWord(null) + loopSkip': false,  // 22386: event should not have been triggerd but WAS
		'skip-combos: fastForward(null) + stopFinish > nextSentence(null) + loopSkip': false,  // 22412: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: fastForward(null) + stopFinish > prevWord(null) + loopSkip': false,  // 22438: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + stopFinish > prevSentence(null) + loopSkip': false,  // 22464: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + done > rewind(null) + loopSkip': false,  // 22724: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: fastForward(null) + done > fastForward(null) + loopSkip': false,  // 22750: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: fastForward(null) + done > once([0,0,-2]) + loopSkip': false,  // 22776: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + done > once([0,0,2]) + loopSkip': false,  // 22802: event should not have been triggerd but WAS
		'skip-combos: fastForward(null) + done > current(null) + loopSkip': false,  // 22828: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: fastForward(null) + done > jumpWords(-3) + loopSkip': false,  // 23010: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: fastForward(null) + done > jumpWords(-1) + loopSkip': false,  // 23036: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + done > jumpWords(0) + loopSkip': false,  // 23062: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: fastForward(null) + done > jumpWords(4) + loopSkip': false,  // 23088: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: fastForward(null) + done > jumpSentences(-1) + loopSkip': false,  // 23192: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + done > jumpSentences(0) + loopSkip': false,  // 23218: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: fastForward(null) + done > jumpSentences(1) + loopSkip': false,  // 23244: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: fastForward(null) + done > jumpSentences(3) + loopSkip': false,  // 23270: event should not have been triggerd but WAS
		'skip-combos: fastForward(null) + done > nextWord(null) + loopSkip': false,  // 23322: event should not have been triggerd but WAS
		'skip-combos: fastForward(null) + done > nextSentence(null) + loopSkip': false,  // 23348: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: fastForward(null) + done > prevWord(null) + loopSkip': false,  // 23374: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + done > prevSentence(null) + loopSkip': false,  // 23400: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + loopSkip > play(null) + newWordFragment': false,  // 23499: frags expected ["you","brave","I","back.","Why,","oh"], but got ["I","back.","Why,","oh"]
		'skip-combos: fastForward(null) + loopSkip > play(null) + progress': false,  // 23500: 'progress' expected [0.08333333333333333,0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1,1]
		'skip-combos: fastForward(null) + loopSkip > play(null) + loopSkip': false,  // 23504: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?","wattlebird?"]
		'skip-combos: fastForward(null) + loopSkip > toggle(null) + playBegin': false,  // 23505: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + loopSkip > toggle(null) + playFinish': false,  // 23506: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + loopSkip > toggle(null) + pauseBegin': false,  // 23511: event should not have been triggerd but WAS
		'skip-combos: fastForward(null) + loopSkip > toggle(null) + pauseFinish': false,  // 23512: event should not have been triggerd but WAS
		'skip-combos: fastForward(null) + loopSkip > toggle(null) + revertBegin': false,  // 23517: event should not have been triggerd but WAS
		'skip-combos: fastForward(null) + loopSkip > toggle(null) + revertFinish': false,  // 23518: event should not have been triggerd but WAS
		'skip-combos: fastForward(null) + loopSkip > toggle(null) + loopBegin': false,  // 23523: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + loopSkip > toggle(null) + loopFinish': false,  // 23524: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + loopSkip > toggle(null) + newWordFragment': false,  // 23525: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + loopSkip > toggle(null) + progress': false,  // 23526: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + loopSkip > toggle(null) + stopBegin': false,  // 23527: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + loopSkip > toggle(null) + stopFinish': false,  // 23528: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + loopSkip > toggle(null) + done': false,  // 23529: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + loopSkip > toggle(null) + loopSkip': false,  // 23530: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + loopSkip > rewind(null) + newWordFragment': false,  // 23655: event should not have been triggerd but WAS
		'skip-combos: fastForward(null) + loopSkip > rewind(null) + progress': false,  // 23656: 'progress' expected [0.08333333333333333], but got [0.25,0.16666666666666666,0.08333333333333333]
		'skip-combos: fastForward(null) + loopSkip > fastForward(null) + newWordFragment': false,  // 23681: frags expected ["you","brave","I","back.","Why,","oh"], but got ["I","back.","Why,","oh"]
		'skip-combos: fastForward(null) + loopSkip > fastForward(null) + progress': false,  // 23682: 'progress' expected [0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1]
		'skip-combos: fastForward(null) + loopSkip > fastForward(null) + loopSkip': false,  // 23686: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["Delirious,","come","\n","wattlebird?"]
		'skip-combos: fastForward(null) + loopSkip > once([0,0,-2]) + newWordFragment': false,  // 23707: event should not have been triggerd but WAS
		'skip-combos: fastForward(null) + loopSkip > once([0,0,-2]) + progress': false,  // 23708: 'progress' expected [0.08333333333333333], but got [0.25]
		'skip-combos: fastForward(null) + loopSkip > once([0,0,-2]) + stopBegin': false,  // 23709: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + loopSkip > once([0,0,-2]) + stopFinish': false,  // 23710: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + loopSkip > once([0,0,-2]) + done': false,  // 23711: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + loopSkip > once([0,0,-2]) + loopSkip': false,  // 23712: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + loopSkip > once([0,0,2]) + newWordFragment': false,  // 23733: frags expected ["you"], but got ["I"]
		'skip-combos: fastForward(null) + loopSkip > once([0,0,2]) + progress': false,  // 23734: 'progress' expected [0.16666666666666666], but got [0.4166666666666667,0.5]
		'skip-combos: fastForward(null) + loopSkip > once([0,0,2]) + loopSkip': false,  // 23738: event should not have been triggerd but WAS
		'skip-combos: fastForward(null) + loopSkip > current(null) + newWordFragment': false,  // 23759: frags expected ["you"], but got ["I"]
		'skip-combos: fastForward(null) + loopSkip > current(null) + progress': false,  // 23760: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.3333333333333333,0.4166666666666667,0.5]
		'skip-combos: fastForward(null) + loopSkip > current(null) + loopSkip': false,  // 23764: frags expected ["Victorious,"], but got ["flag.","Delirious,"]
		'skip-combos: fastForward(null) + loopSkip > jumpWords(-1) + newWordFragment': false,  // 23967: event should not have been triggerd but WAS
		'skip-combos: fastForward(null) + loopSkip > jumpWords(-1) + progress': false,  // 23968: 'progress' expected [0.08333333333333333], but got [0.25]
		'skip-combos: fastForward(null) + loopSkip > jumpWords(-1) + stopBegin': false,  // 23969: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + loopSkip > jumpWords(-1) + stopFinish': false,  // 23970: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + loopSkip > jumpWords(-1) + done': false,  // 23971: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + loopSkip > jumpWords(-1) + loopSkip': false,  // 23972: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + loopSkip > jumpWords(0) + newWordFragment': false,  // 23993: frags expected ["you"], but got ["I"]
		'skip-combos: fastForward(null) + loopSkip > jumpWords(0) + progress': false,  // 23994: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.3333333333333333,0.4166666666666667,0.5]
		'skip-combos: fastForward(null) + loopSkip > jumpWords(0) + loopSkip': false,  // 23998: frags expected ["Victorious,"], but got ["flag.","Delirious,"]
		'skip-combos: fastForward(null) + loopSkip > jumpWords(4) + newWordFragment': false,  // 24019: frags expected ["I"], but got ["back."]
		'skip-combos: fastForward(null) + loopSkip > jumpWords(4) + progress': false,  // 24020: 'progress' expected [0.4166666666666667,0.5], but got [0.6666666666666666]
		'skip-combos: fastForward(null) + loopSkip > jumpWords(4) + loopSkip': false,  // 24024: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + loopSkip > jumpSentences(0) + newWordFragment': false,  // 24149: frags expected ["you"], but got ["I"]
		'skip-combos: fastForward(null) + loopSkip > jumpSentences(0) + progress': false,  // 24150: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.3333333333333333,0.4166666666666667,0.5]
		'skip-combos: fastForward(null) + loopSkip > jumpSentences(0) + loopSkip': false,  // 24154: frags expected ["Victorious,"], but got ["flag.","Delirious,"]
		'skip-combos: fastForward(null) + loopSkip > nextWord(null) + newWordFragment': false,  // 24253: frags expected ["you"], but got ["I"]
		'skip-combos: fastForward(null) + loopSkip > nextWord(null) + progress': false,  // 24254: 'progress' expected [0.16666666666666666], but got [0.4166666666666667,0.5]
		'skip-combos: fastForward(null) + loopSkip > nextWord(null) + loopSkip': false,  // 24258: event should not have been triggerd but WAS
		'skip-combos: fastForward(null) + loopSkip > prevWord(null) + newWordFragment': false,  // 24305: event should not have been triggerd but WAS
		'skip-combos: fastForward(null) + loopSkip > prevWord(null) + progress': false,  // 24306: 'progress' expected [0.08333333333333333], but got [0.25]
		'skip-combos: fastForward(null) + loopSkip > prevWord(null) + stopBegin': false,  // 24307: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + loopSkip > prevWord(null) + stopFinish': false,  // 24308: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + loopSkip > prevWord(null) + done': false,  // 24309: event should have been triggerd but was NOT
		'skip-combos: fastForward(null) + loopSkip > prevWord(null) + loopSkip': false,  // 24310: event should have been triggerd but was NOT

		// once([0,0,-2])
		'skip-combos: once([0,0,-2]) + loopSkip > play(null) + playBegin': false,  // 47815: event should have been triggerd but was NOT
		'skip-combos: once([0,0,-2]) + loopSkip > play(null) + playFinish': false,  // 47816: event should have been triggerd but was NOT
		'skip-combos: once([0,0,-2]) + loopSkip > play(null) + restartBegin': false,  // 47819: event should not have been triggerd but WAS
		'skip-combos: once([0,0,-2]) + loopSkip > play(null) + restartFinish': false,  // 47820: event should not have been triggerd but WAS
		'skip-combos: once([0,0,-2]) + loopSkip > toggle(null) + playBegin': false,  // 47841: event should have been triggerd but was NOT
		'skip-combos: once([0,0,-2]) + loopSkip > toggle(null) + playFinish': false,  // 47842: event should have been triggerd but was NOT
		'skip-combos: once([0,0,-2]) + loopSkip > toggle(null) + restartBegin': false,  // 47845: event should not have been triggerd but WAS
		'skip-combos: once([0,0,-2]) + loopSkip > toggle(null) + restartFinish': false,  // 47846: event should not have been triggerd but WAS

		// once(0,0,2)
		'skip-combos: once([0,0,2]) + onceBegin > play(null) + loopSkip': false,  // 9464: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?"]
		'skip-combos: once([0,0,2]) + onceBegin > toggle(null) + loopSkip': false,  // 9490: event should have been triggerd but was NOT
		'skip-combos: once([0,0,2]) + onceBegin > current(null) + loopSkip': false,  // 9724: event should have been triggerd but was NOT
		'skip-combos: once([0,0,2]) + onceBegin > jumpWords(0) + loopSkip': false,  // 9958: event should have been triggerd but was NOT
		'skip-combos: once([0,0,2]) + onceBegin > jumpWords(4) + loopSkip': false,  // 9984: event should have been triggerd but was NOT
		'skip-combos: once([0,0,2]) + onceBegin > jumpSentences(0) + loopSkip': false,  // 10114: event should have been triggerd but was NOT
		'skip-combos: once([0,0,2]) + onceFinish > play(null) + loopSkip': false,  // 10400: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?"]
		'skip-combos: once([0,0,2]) + onceFinish > toggle(null) + loopSkip': false,  // 10426: event should have been triggerd but was NOT
		'skip-combos: once([0,0,2]) + onceFinish > current(null) + loopSkip': false,  // 10660: event should have been triggerd but was NOT
		'skip-combos: once([0,0,2]) + onceFinish > jumpWords(0) + loopSkip': false,  // 10894: event should have been triggerd but was NOT
		'skip-combos: once([0,0,2]) + onceFinish > jumpWords(4) + loopSkip': false,  // 10920: event should have been triggerd but was NOT
		'skip-combos: once([0,0,2]) + onceFinish > jumpSentences(0) + loopSkip': false,  // 11050: event should have been triggerd but was NOT
		'skip-combos: once([0,0,2]) + loopBegin > play(null) + loopSkip': false,  // 16952: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?"]
		'skip-combos: once([0,0,2]) + loopBegin > toggle(null) + loopSkip': false,  // 16978: event should have been triggerd but was NOT
		'skip-combos: once([0,0,2]) + loopBegin > current(null) + loopSkip': false,  // 17212: event should have been triggerd but was NOT
		'skip-combos: once([0,0,2]) + loopBegin > jumpWords(0) + loopSkip': false,  // 17446: event should have been triggerd but was NOT
		'skip-combos: once([0,0,2]) + loopBegin > jumpWords(4) + loopSkip': false,  // 17472: event should have been triggerd but was NOT
		'skip-combos: once([0,0,2]) + loopBegin > jumpSentences(0) + loopSkip': false,  // 17602: event should have been triggerd but was NOT
		'skip-combos: once([0,0,2]) + loopFinish > play(null) + loopSkip': false,  // 17888: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?"]
		'skip-combos: once([0,0,2]) + loopFinish > toggle(null) + loopSkip': false,  // 17914: event should have been triggerd but was NOT
		'skip-combos: once([0,0,2]) + loopFinish > current(null) + loopSkip': false,  // 18148: event should have been triggerd but was NOT
		'skip-combos: once([0,0,2]) + loopFinish > jumpWords(0) + loopSkip': false,  // 18382: event should have been triggerd but was NOT
		'skip-combos: once([0,0,2]) + loopFinish > jumpWords(4) + loopSkip': false,  // 18408: event should have been triggerd but was NOT
		'skip-combos: once([0,0,2]) + loopFinish > jumpSentences(0) + loopSkip': false,  // 18538: event should have been triggerd but was NOT
		'skip-combos: once([0,0,2]) + newWordFragment > play(null) + loopSkip': false,  // 18824: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?"]
		'skip-combos: once([0,0,2]) + newWordFragment > toggle(null) + loopSkip': false,  // 18850: event should have been triggerd but was NOT
		'skip-combos: once([0,0,2]) + newWordFragment > current(null) + loopSkip': false,  // 19084: event should have been triggerd but was NOT
		'skip-combos: once([0,0,2]) + newWordFragment > jumpWords(0) + loopSkip': false,  // 19318: event should have been triggerd but was NOT
		'skip-combos: once([0,0,2]) + newWordFragment > jumpWords(4) + loopSkip': false,  // 19344: event should have been triggerd but was NOT
		'skip-combos: once([0,0,2]) + newWordFragment > jumpSentences(0) + loopSkip': false,  // 19474: event should have been triggerd but was NOT
		'skip-combos: once([0,0,2]) + progress > play(null) + loopSkip': false,  // 19760: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?"]
		'skip-combos: once([0,0,2]) + progress > toggle(null) + loopSkip': false,  // 19786: event should have been triggerd but was NOT
		'skip-combos: once([0,0,2]) + progress > current(null) + loopSkip': false,  // 20020: event should have been triggerd but was NOT
		'skip-combos: once([0,0,2]) + progress > jumpWords(0) + loopSkip': false,  // 20254: event should have been triggerd but was NOT
		'skip-combos: once([0,0,2]) + progress > jumpWords(4) + loopSkip': false,  // 20280: event should have been triggerd but was NOT
		'skip-combos: once([0,0,2]) + progress > jumpSentences(0) + loopSkip': false,  // 20410: event should have been triggerd but was NOT

		// current(null)
		'skip-combos: current(null) + onceBegin > toggle(null) + loopSkip': false,  // 33826: event should have been triggerd but was NOT
		'skip-combos: current(null) + onceFinish > toggle(null) + loopSkip': false,  // 34762: event should have been triggerd but was NOT
		'skip-combos: current(null) + loopBegin > toggle(null) + loopSkip': false,  // 41314: event should have been triggerd but was NOT
		'skip-combos: current(null) + loopFinish > toggle(null) + loopSkip': false,  // 42250: event should have been triggerd but was NOT
		'skip-combos: current(null) + newWordFragment > play(null) + progress': false,  // 43156: 'progress' expected [0.08333333333333333,0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1]
		'skip-combos: current(null) + newWordFragment > play(null) + loopSkip': false,  // 43160: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?"]
		'skip-combos: current(null) + newWordFragment > toggle(null) + loopSkip': false,  // 43186: event should have been triggerd but was NOT
		'skip-combos: current(null) + newWordFragment > fastForward(null) + newWordFragment': false,  // 43337: frags expected ["you","brave","I","back.","Why,","oh"], but got ["brave","I","back.","Why,","oh"]
		'skip-combos: current(null) + newWordFragment > fastForward(null) + progress': false,  // 43338: 'progress' expected [0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1]
		'skip-combos: current(null) + newWordFragment > once([0,0,2]) + newWordFragment': false,  // 43389: frags expected ["you"], but got ["brave"]
		'skip-combos: current(null) + newWordFragment > once([0,0,2]) + progress': false,  // 43390: 'progress' expected [0.16666666666666666], but got [0.25]
		'skip-combos: current(null) + newWordFragment > current(null) + progress': false,  // 43416: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.16666666666666666]
		'skip-combos: current(null) + newWordFragment > current(null) + loopSkip': false,  // 43420: event should have been triggerd but was NOT
		'skip-combos: current(null) + newWordFragment > jumpWords(0) + progress': false,  // 43650: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.16666666666666666]
		'skip-combos: current(null) + newWordFragment > jumpWords(0) + loopSkip': false,  // 43654: event should have been triggerd but was NOT
		'skip-combos: current(null) + newWordFragment > jumpWords(4) + progress': false,  // 43676: 'progress' expected [0.4166666666666667,0.5], but got [0.5]
		'skip-combos: current(null) + newWordFragment > jumpWords(4) + loopSkip': false,  // 43680: event should have been triggerd but was NOT
		'skip-combos: current(null) + newWordFragment > jumpSentences(0) + progress': false,  // 43806: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.16666666666666666]
		'skip-combos: current(null) + newWordFragment > jumpSentences(0) + loopSkip': false,  // 43810: event should have been triggerd but was NOT
		'skip-combos: current(null) + newWordFragment > nextWord(null) + newWordFragment': false,  // 43909: frags expected ["you"], but got ["brave"]
		'skip-combos: current(null) + newWordFragment > nextWord(null) + progress': false,  // 43910: 'progress' expected [0.16666666666666666], but got [0.25]
		'skip-combos: current(null) + progress > toggle(null) + loopSkip': false,  // 44122: event should have been triggerd but was NOT
		'skip-combos: current(null) + loopSkip > toggle(null) + playBegin': false,  // 47841: event should have been triggerd but was NOT
		'skip-combos: current(null) + loopSkip > toggle(null) + playFinish': false,  // 47842: event should have been triggerd but was NOT
		'skip-combos: current(null) + loopSkip > toggle(null) + pauseBegin': false,  // 47847: event should not have been triggerd but WAS
		'skip-combos: current(null) + loopSkip > toggle(null) + pauseFinish': false,  // 47848: event should not have been triggerd but WAS
		'skip-combos: current(null) + loopSkip > toggle(null) + revertBegin': false,  // 47853: event should not have been triggerd but WAS
		'skip-combos: current(null) + loopSkip > toggle(null) + revertFinish': false,  // 47854: event should not have been triggerd but WAS
		'skip-combos: current(null) + loopSkip > toggle(null) + loopBegin': false,  // 47859: event should have been triggerd but was NOT
		'skip-combos: current(null) + loopSkip > toggle(null) + loopFinish': false,  // 47860: event should have been triggerd but was NOT
		'skip-combos: current(null) + loopSkip > toggle(null) + newWordFragment': false,  // 47861: event should have been triggerd but was NOT
		'skip-combos: current(null) + loopSkip > toggle(null) + progress': false,  // 47862: event should have been triggerd but was NOT
		'skip-combos: current(null) + loopSkip > toggle(null) + stopBegin': false,  // 47863: event should have been triggerd but was NOT
		'skip-combos: current(null) + loopSkip > toggle(null) + stopFinish': false,  // 47864: event should have been triggerd but was NOT
		'skip-combos: current(null) + loopSkip > toggle(null) + done': false,  // 47865: event should have been triggerd but was NOT
		'skip-combos: current(null) + loopSkip > toggle(null) + loopSkip': false,  // 47866: event should have been triggerd but was NOT

		// jumpTo(-3)
		'skip-combos: jumpTo(-3) + loopSkip > play(null) + playBegin': false,  // 23479: event should have been triggerd but was NOT
		'skip-combos: jumpTo(-3) + loopSkip > play(null) + playFinish': false,  // 23480: event should have been triggerd but was NOT
		'skip-combos: jumpTo(-3) + loopSkip > play(null) + restartBegin': false,  // 23483: event should not have been triggerd but WAS
		'skip-combos: jumpTo(-3) + loopSkip > play(null) + restartFinish': false,  // 23484: event should not have been triggerd but WAS
		'skip-combos: jumpTo(-3) + loopSkip > toggle(null) + playBegin': false,  // 23505: event should have been triggerd but was NOT
		'skip-combos: jumpTo(-3) + loopSkip > toggle(null) + playFinish': false,  // 23506: event should have been triggerd but was NOT
		'skip-combos: jumpTo(-3) + loopSkip > toggle(null) + restartBegin': false,  // 23509: event should not have been triggerd but WAS
		'skip-combos: jumpTo(-3) + loopSkip > toggle(null) + restartFinish': false,  // 23510: event should not have been triggerd but WAS

		// jumpTo(-1)
		'skip-combos: jumpTo(-1) + loopSkip > play(null) + playBegin': false,  // 47815: event should have been triggerd but was NOT
		'skip-combos: jumpTo(-1) + loopSkip > play(null) + playFinish': false,  // 47816: event should have been triggerd but was NOT
		'skip-combos: jumpTo(-1) + loopSkip > play(null) + restartBegin': false,  // 47819: event should not have been triggerd but WAS
		'skip-combos: jumpTo(-1) + loopSkip > play(null) + restartFinish': false,  // 47820: event should not have been triggerd but WAS
		'skip-combos: jumpTo(-1) + loopSkip > toggle(null) + playBegin': false,  // 47841: event should have been triggerd but was NOT
		'skip-combos: jumpTo(-1) + loopSkip > toggle(null) + playFinish': false,  // 47842: event should have been triggerd but was NOT
		'skip-combos: jumpTo(-1) + loopSkip > toggle(null) + restartBegin': false,  // 47845: event should not have been triggerd but WAS
		'skip-combos: jumpTo(-1) + loopSkip > toggle(null) + restartFinish': false,  // 47846: event should not have been triggerd but WAS

		// jumpTo(0)
		'skip-combos: jumpTo(0) + onceBegin > toggle(null) + loopSkip': false,  // 9490: event should have been triggerd but was NOT
		'skip-combos: jumpTo(0) + onceFinish > toggle(null) + loopSkip': false,  // 10426: event should have been triggerd but was NOT
		'skip-combos: jumpTo(0) + loopBegin > toggle(null) + loopSkip': false,  // 16978: event should have been triggerd but was NOT
		'skip-combos: jumpTo(0) + loopFinish > toggle(null) + loopSkip': false,  // 17914: event should have been triggerd but was NOT
		'skip-combos: jumpTo(0) + newWordFragment > play(null) + progress': false,  // 18820: 'progress' expected [0.08333333333333333,0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1]
		'skip-combos: jumpTo(0) + newWordFragment > play(null) + loopSkip': false,  // 18824: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?"]
		'skip-combos: jumpTo(0) + newWordFragment > toggle(null) + loopSkip': false,  // 18850: event should have been triggerd but was NOT
		'skip-combos: jumpTo(0) + newWordFragment > fastForward(null) + newWordFragment': false,  // 19001: frags expected ["you","brave","I","back.","Why,","oh"], but got ["brave","I","back.","Why,","oh"]
		'skip-combos: jumpTo(0) + newWordFragment > fastForward(null) + progress': false,  // 19002: 'progress' expected [0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1]
		'skip-combos: jumpTo(0) + newWordFragment > once([0,0,2]) + newWordFragment': false,  // 19053: frags expected ["you"], but got ["brave"]
		'skip-combos: jumpTo(0) + newWordFragment > once([0,0,2]) + progress': false,  // 19054: 'progress' expected [0.16666666666666666], but got [0.25]
		'skip-combos: jumpTo(0) + newWordFragment > current(null) + progress': false,  // 19080: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.16666666666666666]
		'skip-combos: jumpTo(0) + newWordFragment > current(null) + loopSkip': false,  // 19084: event should have been triggerd but was NOT
		'skip-combos: jumpTo(0) + newWordFragment > jumpWords(0) + progress': false,  // 19314: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.16666666666666666]
		'skip-combos: jumpTo(0) + newWordFragment > jumpWords(0) + loopSkip': false,  // 19318: event should have been triggerd but was NOT
		'skip-combos: jumpTo(0) + newWordFragment > jumpWords(4) + progress': false,  // 19340: 'progress' expected [0.4166666666666667,0.5], but got [0.5]
		'skip-combos: jumpTo(0) + newWordFragment > jumpWords(4) + loopSkip': false,  // 19344: event should have been triggerd but was NOT
		'skip-combos: jumpTo(0) + newWordFragment > jumpSentences(0) + progress': false,  // 19470: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.16666666666666666]
		'skip-combos: jumpTo(0) + newWordFragment > jumpSentences(0) + loopSkip': false,  // 19474: event should have been triggerd but was NOT
		'skip-combos: jumpTo(0) + newWordFragment > nextWord(null) + newWordFragment': false,  // 19573: frags expected ["you"], but got ["brave"]
		'skip-combos: jumpTo(0) + newWordFragment > nextWord(null) + progress': false,  // 19574: 'progress' expected [0.16666666666666666], but got [0.25]
		'skip-combos: jumpTo(0) + progress > toggle(null) + loopSkip': false,  // 19786: event should have been triggerd but was NOT
		'skip-combos: jumpTo(0) + loopSkip > toggle(null) + playBegin': false,  // 23505: event should have been triggerd but was NOT
		'skip-combos: jumpTo(0) + loopSkip > toggle(null) + playFinish': false,  // 23506: event should have been triggerd but was NOT
		'skip-combos: jumpTo(0) + loopSkip > toggle(null) + pauseBegin': false,  // 23511: event should not have been triggerd but WAS
		'skip-combos: jumpTo(0) + loopSkip > toggle(null) + pauseFinish': false,  // 23512: event should not have been triggerd but WAS
		'skip-combos: jumpTo(0) + loopSkip > toggle(null) + revertBegin': false,  // 23517: event should not have been triggerd but WAS
		'skip-combos: jumpTo(0) + loopSkip > toggle(null) + revertFinish': false,  // 23518: event should not have been triggerd but WAS
		'skip-combos: jumpTo(0) + loopSkip > toggle(null) + loopBegin': false,  // 23523: event should have been triggerd but was NOT
		'skip-combos: jumpTo(0) + loopSkip > toggle(null) + loopFinish': false,  // 23524: event should have been triggerd but was NOT
		'skip-combos: jumpTo(0) + loopSkip > toggle(null) + newWordFragment': false,  // 23525: event should have been triggerd but was NOT
		'skip-combos: jumpTo(0) + loopSkip > toggle(null) + progress': false,  // 23526: event should have been triggerd but was NOT
		'skip-combos: jumpTo(0) + loopSkip > toggle(null) + stopBegin': false,  // 23527: event should have been triggerd but was NOT
		'skip-combos: jumpTo(0) + loopSkip > toggle(null) + stopFinish': false,  // 23528: event should have been triggerd but was NOT
		'skip-combos: jumpTo(0) + loopSkip > toggle(null) + done': false,  // 23529: event should have been triggerd but was NOT
		'skip-combos: jumpTo(0) + loopSkip > toggle(null) + loopSkip': false,  // 23530: event should have been triggerd but was NOT

		// jumpTo(4)
		'skip-combos: jumpTo(4) + onceBegin > play(null) + newWordFragment': false,  // 33795: frags expected ["you","brave","I","back.","Why,","oh"], but got ["I","back.","Why,","oh"]
		'skip-combos: jumpTo(4) + onceBegin > play(null) + progress': false,  // 33796: 'progress' expected [0.08333333333333333,0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1]
		'skip-combos: jumpTo(4) + onceBegin > play(null) + loopSkip': false,  // 33800: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["Delirious,","come","\n","wattlebird?"]
		'skip-combos: jumpTo(4) + onceBegin > toggle(null) + playBegin': false,  // 33801: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceBegin > toggle(null) + playFinish': false,  // 33802: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceBegin > toggle(null) + pauseBegin': false,  // 33807: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + onceBegin > toggle(null) + pauseFinish': false,  // 33808: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + onceBegin > toggle(null) + revertBegin': false,  // 33813: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + onceBegin > toggle(null) + revertFinish': false,  // 33814: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + onceBegin > toggle(null) + loopBegin': false,  // 33819: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceBegin > toggle(null) + loopFinish': false,  // 33820: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceBegin > toggle(null) + newWordFragment': false,  // 33821: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceBegin > toggle(null) + progress': false,  // 33822: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceBegin > toggle(null) + stopBegin': false,  // 33823: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceBegin > toggle(null) + stopFinish': false,  // 33824: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceBegin > toggle(null) + done': false,  // 33825: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceBegin > toggle(null) + loopSkip': false,  // 33826: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceBegin > rewind(null) + newWordFragment': false,  // 33951: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + onceBegin > rewind(null) + progress': false,  // 33952: 'progress' expected [0.08333333333333333], but got [0.3333333333333333,0.25,0.16666666666666666,0.08333333333333333]
		'skip-combos: jumpTo(4) + onceBegin > rewind(null) + loopSkip': false,  // 33956: frags expected ["Victorious,"], but got ["flag.","Victorious,"]
		'skip-combos: jumpTo(4) + onceBegin > fastForward(null) + newWordFragment': false,  // 33977: frags expected ["you","brave","I","back.","Why,","oh"], but got ["I","back.","Why,","oh"]
		'skip-combos: jumpTo(4) + onceBegin > fastForward(null) + progress': false,  // 33978: 'progress' expected [0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1]
		'skip-combos: jumpTo(4) + onceBegin > fastForward(null) + loopSkip': false,  // 33982: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["come","\n","wattlebird?"]
		'skip-combos: jumpTo(4) + onceBegin > once([0,0,-2]) + newWordFragment': false,  // 34003: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + onceBegin > once([0,0,-2]) + progress': false,  // 34004: 'progress' expected [0.08333333333333333], but got [0.3333333333333333,0.25]
		'skip-combos: jumpTo(4) + onceBegin > once([0,0,-2]) + stopBegin': false,  // 34005: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceBegin > once([0,0,-2]) + stopFinish': false,  // 34006: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceBegin > once([0,0,-2]) + done': false,  // 34007: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceBegin > once([0,0,-2]) + loopSkip': false,  // 34008: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpTo(4) + onceBegin > once([0,0,2]) + newWordFragment': false,  // 34029: frags expected ["you"], but got ["I"]
		'skip-combos: jumpTo(4) + onceBegin > once([0,0,2]) + progress': false,  // 34030: 'progress' expected [0.16666666666666666], but got [0.5]
		'skip-combos: jumpTo(4) + onceBegin > current(null) + newWordFragment': false,  // 34055: frags expected ["you"], but got ["I"]
		'skip-combos: jumpTo(4) + onceBegin > current(null) + progress': false,  // 34056: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.4166666666666667,0.5]
		'skip-combos: jumpTo(4) + onceBegin > current(null) + loopSkip': false,  // 34060: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpTo(4) + onceBegin > jumpWords(-3) + newWordFragment': false,  // 34237: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + onceBegin > jumpWords(-3) + progress': false,  // 34238: 'progress' expected [0.08333333333333333], but got [0.16666666666666666]
		'skip-combos: jumpTo(4) + onceBegin > jumpWords(-3) + stopBegin': false,  // 34239: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceBegin > jumpWords(-3) + stopFinish': false,  // 34240: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceBegin > jumpWords(-3) + done': false,  // 34241: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceBegin > jumpWords(-3) + loopSkip': false,  // 34242: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceBegin > jumpWords(-1) + newWordFragment': false,  // 34263: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + onceBegin > jumpWords(-1) + progress': false,  // 34264: 'progress' expected [0.08333333333333333], but got [0.3333333333333333,0.25]
		'skip-combos: jumpTo(4) + onceBegin > jumpWords(-1) + stopBegin': false,  // 34265: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceBegin > jumpWords(-1) + stopFinish': false,  // 34266: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceBegin > jumpWords(-1) + done': false,  // 34267: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceBegin > jumpWords(-1) + loopSkip': false,  // 34268: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpTo(4) + onceBegin > jumpWords(0) + newWordFragment': false,  // 34289: frags expected ["you"], but got ["I"]
		'skip-combos: jumpTo(4) + onceBegin > jumpWords(0) + progress': false,  // 34290: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.4166666666666667,0.5]
		'skip-combos: jumpTo(4) + onceBegin > jumpWords(0) + loopSkip': false,  // 34294: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpTo(4) + onceBegin > jumpWords(4) + newWordFragment': false,  // 34315: frags expected ["I"], but got ["Why,"]
		'skip-combos: jumpTo(4) + onceBegin > jumpWords(4) + progress': false,  // 34316: 'progress' expected [0.4166666666666667,0.5], but got [0.75,0.8333333333333334]
		'skip-combos: jumpTo(4) + onceBegin > jumpWords(4) + loopSkip': false,  // 34320: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpTo(4) + onceBegin > jumpSentences(0) + newWordFragment': false,  // 34445: frags expected ["you"], but got ["I"]
		'skip-combos: jumpTo(4) + onceBegin > jumpSentences(0) + progress': false,  // 34446: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.4166666666666667,0.5]
		'skip-combos: jumpTo(4) + onceBegin > jumpSentences(0) + loopSkip': false,  // 34450: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpTo(4) + onceBegin > jumpSentences(1) + newWordFragment': false,  // 34471: frags expected ["I"], but got ["Why,"]
		'skip-combos: jumpTo(4) + onceBegin > jumpSentences(1) + progress': false,  // 34472: 'progress' expected [0.4166666666666667,0.5], but got [0.75,0.8333333333333334]
		'skip-combos: jumpTo(4) + onceBegin > jumpSentences(1) + loopSkip': false,  // 34476: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpTo(4) + onceBegin > jumpSentences(3) + newWordFragment': false,  // 34497: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceBegin > jumpSentences(3) + progress': false,  // 34498: 'progress' expected [0.8333333333333334], but got [1]
		'skip-combos: jumpTo(4) + onceBegin > jumpSentences(3) + stopBegin': false,  // 34499: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + onceBegin > jumpSentences(3) + stopFinish': false,  // 34500: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + onceBegin > jumpSentences(3) + done': false,  // 34501: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + onceBegin > jumpSentences(3) + loopSkip': false,  // 34502: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + onceBegin > nextWord(null) + newWordFragment': false,  // 34549: frags expected ["you"], but got ["I"]
		'skip-combos: jumpTo(4) + onceBegin > nextWord(null) + progress': false,  // 34550: 'progress' expected [0.16666666666666666], but got [0.5]
		'skip-combos: jumpTo(4) + onceBegin > nextSentence(null) + newWordFragment': false,  // 34575: frags expected ["I"], but got ["Why,"]
		'skip-combos: jumpTo(4) + onceBegin > nextSentence(null) + progress': false,  // 34576: 'progress' expected [0.4166666666666667,0.5], but got [0.75,0.8333333333333334]
		'skip-combos: jumpTo(4) + onceBegin > nextSentence(null) + loopSkip': false,  // 34580: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpTo(4) + onceBegin > prevWord(null) + newWordFragment': false,  // 34601: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + onceBegin > prevWord(null) + progress': false,  // 34602: 'progress' expected [0.08333333333333333], but got [0.3333333333333333,0.25]
		'skip-combos: jumpTo(4) + onceBegin > prevWord(null) + stopBegin': false,  // 34603: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceBegin > prevWord(null) + stopFinish': false,  // 34604: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceBegin > prevWord(null) + done': false,  // 34605: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceBegin > prevWord(null) + loopSkip': false,  // 34606: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpTo(4) + onceFinish > play(null) + newWordFragment': false,  // 34731: frags expected ["you","brave","I","back.","Why,","oh"], but got ["I","back.","Why,","oh"]
		'skip-combos: jumpTo(4) + onceFinish > play(null) + progress': false,  // 34732: 'progress' expected [0.08333333333333333,0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1]
		'skip-combos: jumpTo(4) + onceFinish > play(null) + loopSkip': false,  // 34736: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["Delirious,","come","\n","wattlebird?"]
		'skip-combos: jumpTo(4) + onceFinish > toggle(null) + playBegin': false,  // 34737: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceFinish > toggle(null) + playFinish': false,  // 34738: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceFinish > toggle(null) + pauseBegin': false,  // 34743: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + onceFinish > toggle(null) + pauseFinish': false,  // 34744: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + onceFinish > toggle(null) + revertBegin': false,  // 34749: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + onceFinish > toggle(null) + revertFinish': false,  // 34750: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + onceFinish > toggle(null) + loopBegin': false,  // 34755: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceFinish > toggle(null) + loopFinish': false,  // 34756: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceFinish > toggle(null) + newWordFragment': false,  // 34757: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceFinish > toggle(null) + progress': false,  // 34758: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceFinish > toggle(null) + stopBegin': false,  // 34759: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceFinish > toggle(null) + stopFinish': false,  // 34760: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceFinish > toggle(null) + done': false,  // 34761: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceFinish > toggle(null) + loopSkip': false,  // 34762: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceFinish > rewind(null) + newWordFragment': false,  // 34887: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + onceFinish > rewind(null) + progress': false,  // 34888: 'progress' expected [0.08333333333333333], but got [0.3333333333333333,0.25,0.16666666666666666,0.08333333333333333]
		'skip-combos: jumpTo(4) + onceFinish > rewind(null) + loopSkip': false,  // 34892: frags expected ["Victorious,"], but got ["flag.","Victorious,"]
		'skip-combos: jumpTo(4) + onceFinish > fastForward(null) + newWordFragment': false,  // 34913: frags expected ["you","brave","I","back.","Why,","oh"], but got ["I","back.","Why,","oh"]
		'skip-combos: jumpTo(4) + onceFinish > fastForward(null) + progress': false,  // 34914: 'progress' expected [0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1]
		'skip-combos: jumpTo(4) + onceFinish > fastForward(null) + loopSkip': false,  // 34918: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["come","\n","wattlebird?"]
		'skip-combos: jumpTo(4) + onceFinish > once([0,0,-2]) + newWordFragment': false,  // 34939: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + onceFinish > once([0,0,-2]) + progress': false,  // 34940: 'progress' expected [0.08333333333333333], but got [0.3333333333333333,0.25]
		'skip-combos: jumpTo(4) + onceFinish > once([0,0,-2]) + stopBegin': false,  // 34941: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceFinish > once([0,0,-2]) + stopFinish': false,  // 34942: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceFinish > once([0,0,-2]) + done': false,  // 34943: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceFinish > once([0,0,-2]) + loopSkip': false,  // 34944: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpTo(4) + onceFinish > once([0,0,2]) + newWordFragment': false,  // 34965: frags expected ["you"], but got ["I"]
		'skip-combos: jumpTo(4) + onceFinish > once([0,0,2]) + progress': false,  // 34966: 'progress' expected [0.16666666666666666], but got [0.5]
		'skip-combos: jumpTo(4) + onceFinish > current(null) + newWordFragment': false,  // 34991: frags expected ["you"], but got ["I"]
		'skip-combos: jumpTo(4) + onceFinish > current(null) + progress': false,  // 34992: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.4166666666666667,0.5]
		'skip-combos: jumpTo(4) + onceFinish > current(null) + loopSkip': false,  // 34996: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpTo(4) + onceFinish > jumpWords(-3) + newWordFragment': false,  // 35173: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + onceFinish > jumpWords(-3) + progress': false,  // 35174: 'progress' expected [0.08333333333333333], but got [0.16666666666666666]
		'skip-combos: jumpTo(4) + onceFinish > jumpWords(-3) + stopBegin': false,  // 35175: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceFinish > jumpWords(-3) + stopFinish': false,  // 35176: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceFinish > jumpWords(-3) + done': false,  // 35177: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceFinish > jumpWords(-3) + loopSkip': false,  // 35178: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceFinish > jumpWords(-1) + newWordFragment': false,  // 35199: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + onceFinish > jumpWords(-1) + progress': false,  // 35200: 'progress' expected [0.08333333333333333], but got [0.3333333333333333,0.25]
		'skip-combos: jumpTo(4) + onceFinish > jumpWords(-1) + stopBegin': false,  // 35201: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceFinish > jumpWords(-1) + stopFinish': false,  // 35202: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceFinish > jumpWords(-1) + done': false,  // 35203: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceFinish > jumpWords(-1) + loopSkip': false,  // 35204: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpTo(4) + onceFinish > jumpWords(0) + newWordFragment': false,  // 35225: frags expected ["you"], but got ["I"]
		'skip-combos: jumpTo(4) + onceFinish > jumpWords(0) + progress': false,  // 35226: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.4166666666666667,0.5]
		'skip-combos: jumpTo(4) + onceFinish > jumpWords(0) + loopSkip': false,  // 35230: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpTo(4) + onceFinish > jumpWords(4) + newWordFragment': false,  // 35251: frags expected ["I"], but got ["Why,"]
		'skip-combos: jumpTo(4) + onceFinish > jumpWords(4) + progress': false,  // 35252: 'progress' expected [0.4166666666666667,0.5], but got [0.75,0.8333333333333334]
		'skip-combos: jumpTo(4) + onceFinish > jumpWords(4) + loopSkip': false,  // 35256: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpTo(4) + onceFinish > jumpSentences(0) + newWordFragment': false,  // 35381: frags expected ["you"], but got ["I"]
		'skip-combos: jumpTo(4) + onceFinish > jumpSentences(0) + progress': false,  // 35382: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.4166666666666667,0.5]
		'skip-combos: jumpTo(4) + onceFinish > jumpSentences(0) + loopSkip': false,  // 35386: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpTo(4) + onceFinish > jumpSentences(1) + newWordFragment': false,  // 35407: frags expected ["I"], but got ["Why,"]
		'skip-combos: jumpTo(4) + onceFinish > jumpSentences(1) + progress': false,  // 35408: 'progress' expected [0.4166666666666667,0.5], but got [0.75,0.8333333333333334]
		'skip-combos: jumpTo(4) + onceFinish > jumpSentences(1) + loopSkip': false,  // 35412: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpTo(4) + onceFinish > jumpSentences(3) + newWordFragment': false,  // 35433: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceFinish > jumpSentences(3) + progress': false,  // 35434: 'progress' expected [0.8333333333333334], but got [1]
		'skip-combos: jumpTo(4) + onceFinish > jumpSentences(3) + stopBegin': false,  // 35435: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + onceFinish > jumpSentences(3) + stopFinish': false,  // 35436: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + onceFinish > jumpSentences(3) + done': false,  // 35437: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + onceFinish > jumpSentences(3) + loopSkip': false,  // 35438: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + onceFinish > nextWord(null) + newWordFragment': false,  // 35485: frags expected ["you"], but got ["I"]
		'skip-combos: jumpTo(4) + onceFinish > nextWord(null) + progress': false,  // 35486: 'progress' expected [0.16666666666666666], but got [0.5]
		'skip-combos: jumpTo(4) + onceFinish > nextSentence(null) + newWordFragment': false,  // 35511: frags expected ["I"], but got ["Why,"]
		'skip-combos: jumpTo(4) + onceFinish > nextSentence(null) + progress': false,  // 35512: 'progress' expected [0.4166666666666667,0.5], but got [0.75,0.8333333333333334]
		'skip-combos: jumpTo(4) + onceFinish > nextSentence(null) + loopSkip': false,  // 35516: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpTo(4) + onceFinish > prevWord(null) + newWordFragment': false,  // 35537: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + onceFinish > prevWord(null) + progress': false,  // 35538: 'progress' expected [0.08333333333333333], but got [0.3333333333333333,0.25]
		'skip-combos: jumpTo(4) + onceFinish > prevWord(null) + stopBegin': false,  // 35539: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceFinish > prevWord(null) + stopFinish': false,  // 35540: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceFinish > prevWord(null) + done': false,  // 35541: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + onceFinish > prevWord(null) + loopSkip': false,  // 35542: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpTo(4) + loopBegin > play(null) + newWordFragment': false,  // 41283: frags expected ["you","brave","I","back.","Why,","oh"], but got ["I","back.","Why,","oh"]
		'skip-combos: jumpTo(4) + loopBegin > play(null) + progress': false,  // 41284: 'progress' expected [0.08333333333333333,0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1]
		'skip-combos: jumpTo(4) + loopBegin > play(null) + loopSkip': false,  // 41288: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["Delirious,","come","\n","wattlebird?"]
		'skip-combos: jumpTo(4) + loopBegin > toggle(null) + playBegin': false,  // 41289: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopBegin > toggle(null) + playFinish': false,  // 41290: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopBegin > toggle(null) + pauseBegin': false,  // 41295: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopBegin > toggle(null) + pauseFinish': false,  // 41296: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopBegin > toggle(null) + revertBegin': false,  // 41301: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopBegin > toggle(null) + revertFinish': false,  // 41302: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopBegin > toggle(null) + loopBegin': false,  // 41307: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopBegin > toggle(null) + loopFinish': false,  // 41308: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopBegin > toggle(null) + newWordFragment': false,  // 41309: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopBegin > toggle(null) + progress': false,  // 41310: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopBegin > toggle(null) + stopBegin': false,  // 41311: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopBegin > toggle(null) + stopFinish': false,  // 41312: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopBegin > toggle(null) + done': false,  // 41313: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopBegin > toggle(null) + loopSkip': false,  // 41314: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopBegin > rewind(null) + newWordFragment': false,  // 41439: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopBegin > rewind(null) + progress': false,  // 41440: 'progress' expected [0.08333333333333333], but got [0.3333333333333333,0.25,0.16666666666666666,0.08333333333333333]
		'skip-combos: jumpTo(4) + loopBegin > rewind(null) + loopSkip': false,  // 41444: frags expected ["Victorious,"], but got ["flag.","Victorious,"]
		'skip-combos: jumpTo(4) + loopBegin > fastForward(null) + newWordFragment': false,  // 41465: frags expected ["you","brave","I","back.","Why,","oh"], but got ["I","back.","Why,","oh"]
		'skip-combos: jumpTo(4) + loopBegin > fastForward(null) + progress': false,  // 41466: 'progress' expected [0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1]
		'skip-combos: jumpTo(4) + loopBegin > fastForward(null) + loopSkip': false,  // 41470: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["come","\n","wattlebird?"]
		'skip-combos: jumpTo(4) + loopBegin > once([0,0,-2]) + newWordFragment': false,  // 41491: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopBegin > once([0,0,-2]) + progress': false,  // 41492: 'progress' expected [0.08333333333333333], but got [0.3333333333333333,0.25]
		'skip-combos: jumpTo(4) + loopBegin > once([0,0,-2]) + stopBegin': false,  // 41493: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopBegin > once([0,0,-2]) + stopFinish': false,  // 41494: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopBegin > once([0,0,-2]) + done': false,  // 41495: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopBegin > once([0,0,-2]) + loopSkip': false,  // 41496: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpTo(4) + loopBegin > once([0,0,2]) + newWordFragment': false,  // 41517: frags expected ["you"], but got ["I"]
		'skip-combos: jumpTo(4) + loopBegin > once([0,0,2]) + progress': false,  // 41518: 'progress' expected [0.16666666666666666], but got [0.5]
		'skip-combos: jumpTo(4) + loopBegin > current(null) + newWordFragment': false,  // 41543: frags expected ["you"], but got ["I"]
		'skip-combos: jumpTo(4) + loopBegin > current(null) + progress': false,  // 41544: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.4166666666666667,0.5]
		'skip-combos: jumpTo(4) + loopBegin > current(null) + loopSkip': false,  // 41548: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpTo(4) + loopBegin > jumpWords(-3) + newWordFragment': false,  // 41725: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopBegin > jumpWords(-3) + progress': false,  // 41726: 'progress' expected [0.08333333333333333], but got [0.16666666666666666]
		'skip-combos: jumpTo(4) + loopBegin > jumpWords(-3) + stopBegin': false,  // 41727: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopBegin > jumpWords(-3) + stopFinish': false,  // 41728: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopBegin > jumpWords(-3) + done': false,  // 41729: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopBegin > jumpWords(-3) + loopSkip': false,  // 41730: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopBegin > jumpWords(-1) + newWordFragment': false,  // 41751: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopBegin > jumpWords(-1) + progress': false,  // 41752: 'progress' expected [0.08333333333333333], but got [0.3333333333333333,0.25]
		'skip-combos: jumpTo(4) + loopBegin > jumpWords(-1) + stopBegin': false,  // 41753: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopBegin > jumpWords(-1) + stopFinish': false,  // 41754: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopBegin > jumpWords(-1) + done': false,  // 41755: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopBegin > jumpWords(-1) + loopSkip': false,  // 41756: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpTo(4) + loopBegin > jumpWords(0) + newWordFragment': false,  // 41777: frags expected ["you"], but got ["I"]
		'skip-combos: jumpTo(4) + loopBegin > jumpWords(0) + progress': false,  // 41778: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.4166666666666667,0.5]
		'skip-combos: jumpTo(4) + loopBegin > jumpWords(0) + loopSkip': false,  // 41782: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpTo(4) + loopBegin > jumpWords(4) + newWordFragment': false,  // 41803: frags expected ["I"], but got ["Why,"]
		'skip-combos: jumpTo(4) + loopBegin > jumpWords(4) + progress': false,  // 41804: 'progress' expected [0.4166666666666667,0.5], but got [0.75,0.8333333333333334]
		'skip-combos: jumpTo(4) + loopBegin > jumpWords(4) + loopSkip': false,  // 41808: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpTo(4) + loopBegin > jumpSentences(0) + newWordFragment': false,  // 41933: frags expected ["you"], but got ["I"]
		'skip-combos: jumpTo(4) + loopBegin > jumpSentences(0) + progress': false,  // 41934: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.4166666666666667,0.5]
		'skip-combos: jumpTo(4) + loopBegin > jumpSentences(0) + loopSkip': false,  // 41938: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpTo(4) + loopBegin > jumpSentences(1) + newWordFragment': false,  // 41959: frags expected ["I"], but got ["Why,"]
		'skip-combos: jumpTo(4) + loopBegin > jumpSentences(1) + progress': false,  // 41960: 'progress' expected [0.4166666666666667,0.5], but got [0.75,0.8333333333333334]
		'skip-combos: jumpTo(4) + loopBegin > jumpSentences(1) + loopSkip': false,  // 41964: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpTo(4) + loopBegin > jumpSentences(3) + newWordFragment': false,  // 41985: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopBegin > jumpSentences(3) + progress': false,  // 41986: 'progress' expected [0.8333333333333334], but got [1]
		'skip-combos: jumpTo(4) + loopBegin > jumpSentences(3) + stopBegin': false,  // 41987: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopBegin > jumpSentences(3) + stopFinish': false,  // 41988: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopBegin > jumpSentences(3) + done': false,  // 41989: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopBegin > jumpSentences(3) + loopSkip': false,  // 41990: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopBegin > nextWord(null) + newWordFragment': false,  // 42037: frags expected ["you"], but got ["I"]
		'skip-combos: jumpTo(4) + loopBegin > nextWord(null) + progress': false,  // 42038: 'progress' expected [0.16666666666666666], but got [0.5]
		'skip-combos: jumpTo(4) + loopBegin > nextSentence(null) + newWordFragment': false,  // 42063: frags expected ["I"], but got ["Why,"]
		'skip-combos: jumpTo(4) + loopBegin > nextSentence(null) + progress': false,  // 42064: 'progress' expected [0.4166666666666667,0.5], but got [0.75,0.8333333333333334]
		'skip-combos: jumpTo(4) + loopBegin > nextSentence(null) + loopSkip': false,  // 42068: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpTo(4) + loopBegin > prevWord(null) + newWordFragment': false,  // 42089: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopBegin > prevWord(null) + progress': false,  // 42090: 'progress' expected [0.08333333333333333], but got [0.3333333333333333,0.25]
		'skip-combos: jumpTo(4) + loopBegin > prevWord(null) + stopBegin': false,  // 42091: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopBegin > prevWord(null) + stopFinish': false,  // 42092: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopBegin > prevWord(null) + done': false,  // 42093: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopBegin > prevWord(null) + loopSkip': false,  // 42094: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpTo(4) + loopFinish > play(null) + newWordFragment': false,  // 42219: frags expected ["you","brave","I","back.","Why,","oh"], but got ["I","back.","Why,","oh"]
		'skip-combos: jumpTo(4) + loopFinish > play(null) + progress': false,  // 42220: 'progress' expected [0.08333333333333333,0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1]
		'skip-combos: jumpTo(4) + loopFinish > play(null) + loopSkip': false,  // 42224: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["Delirious,","come","\n","wattlebird?"]
		'skip-combos: jumpTo(4) + loopFinish > toggle(null) + playBegin': false,  // 42225: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopFinish > toggle(null) + playFinish': false,  // 42226: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopFinish > toggle(null) + pauseBegin': false,  // 42231: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopFinish > toggle(null) + pauseFinish': false,  // 42232: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopFinish > toggle(null) + revertBegin': false,  // 42237: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopFinish > toggle(null) + revertFinish': false,  // 42238: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopFinish > toggle(null) + loopBegin': false,  // 42243: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopFinish > toggle(null) + loopFinish': false,  // 42244: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopFinish > toggle(null) + newWordFragment': false,  // 42245: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopFinish > toggle(null) + progress': false,  // 42246: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopFinish > toggle(null) + stopBegin': false,  // 42247: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopFinish > toggle(null) + stopFinish': false,  // 42248: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopFinish > toggle(null) + done': false,  // 42249: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopFinish > toggle(null) + loopSkip': false,  // 42250: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopFinish > rewind(null) + newWordFragment': false,  // 42375: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopFinish > rewind(null) + progress': false,  // 42376: 'progress' expected [0.08333333333333333], but got [0.3333333333333333,0.25,0.16666666666666666,0.08333333333333333]
		'skip-combos: jumpTo(4) + loopFinish > rewind(null) + loopSkip': false,  // 42380: frags expected ["Victorious,"], but got ["flag.","Victorious,"]
		'skip-combos: jumpTo(4) + loopFinish > fastForward(null) + newWordFragment': false,  // 42401: frags expected ["you","brave","I","back.","Why,","oh"], but got ["I","back.","Why,","oh"]
		'skip-combos: jumpTo(4) + loopFinish > fastForward(null) + progress': false,  // 42402: 'progress' expected [0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1]
		'skip-combos: jumpTo(4) + loopFinish > fastForward(null) + loopSkip': false,  // 42406: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["come","\n","wattlebird?"]
		'skip-combos: jumpTo(4) + loopFinish > once([0,0,-2]) + newWordFragment': false,  // 42427: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopFinish > once([0,0,-2]) + progress': false,  // 42428: 'progress' expected [0.08333333333333333], but got [0.3333333333333333,0.25]
		'skip-combos: jumpTo(4) + loopFinish > once([0,0,-2]) + stopBegin': false,  // 42429: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopFinish > once([0,0,-2]) + stopFinish': false,  // 42430: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopFinish > once([0,0,-2]) + done': false,  // 42431: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopFinish > once([0,0,-2]) + loopSkip': false,  // 42432: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpTo(4) + loopFinish > once([0,0,2]) + newWordFragment': false,  // 42453: frags expected ["you"], but got ["I"]
		'skip-combos: jumpTo(4) + loopFinish > once([0,0,2]) + progress': false,  // 42454: 'progress' expected [0.16666666666666666], but got [0.5]
		'skip-combos: jumpTo(4) + loopFinish > current(null) + newWordFragment': false,  // 42479: frags expected ["you"], but got ["I"]
		'skip-combos: jumpTo(4) + loopFinish > current(null) + progress': false,  // 42480: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.4166666666666667,0.5]
		'skip-combos: jumpTo(4) + loopFinish > current(null) + loopSkip': false,  // 42484: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpTo(4) + loopFinish > jumpWords(-3) + newWordFragment': false,  // 42661: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopFinish > jumpWords(-3) + progress': false,  // 42662: 'progress' expected [0.08333333333333333], but got [0.16666666666666666]
		'skip-combos: jumpTo(4) + loopFinish > jumpWords(-3) + stopBegin': false,  // 42663: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopFinish > jumpWords(-3) + stopFinish': false,  // 42664: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopFinish > jumpWords(-3) + done': false,  // 42665: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopFinish > jumpWords(-3) + loopSkip': false,  // 42666: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopFinish > jumpWords(-1) + newWordFragment': false,  // 42687: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopFinish > jumpWords(-1) + progress': false,  // 42688: 'progress' expected [0.08333333333333333], but got [0.3333333333333333,0.25]
		'skip-combos: jumpTo(4) + loopFinish > jumpWords(-1) + stopBegin': false,  // 42689: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopFinish > jumpWords(-1) + stopFinish': false,  // 42690: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopFinish > jumpWords(-1) + done': false,  // 42691: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopFinish > jumpWords(-1) + loopSkip': false,  // 42692: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpTo(4) + loopFinish > jumpWords(0) + newWordFragment': false,  // 42713: frags expected ["you"], but got ["I"]
		'skip-combos: jumpTo(4) + loopFinish > jumpWords(0) + progress': false,  // 42714: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.4166666666666667,0.5]
		'skip-combos: jumpTo(4) + loopFinish > jumpWords(0) + loopSkip': false,  // 42718: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpTo(4) + loopFinish > jumpWords(4) + newWordFragment': false,  // 42739: frags expected ["I"], but got ["Why,"]
		'skip-combos: jumpTo(4) + loopFinish > jumpWords(4) + progress': false,  // 42740: 'progress' expected [0.4166666666666667,0.5], but got [0.75,0.8333333333333334]
		'skip-combos: jumpTo(4) + loopFinish > jumpWords(4) + loopSkip': false,  // 42744: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpTo(4) + loopFinish > jumpSentences(0) + newWordFragment': false,  // 42869: frags expected ["you"], but got ["I"]
		'skip-combos: jumpTo(4) + loopFinish > jumpSentences(0) + progress': false,  // 42870: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.4166666666666667,0.5]
		'skip-combos: jumpTo(4) + loopFinish > jumpSentences(0) + loopSkip': false,  // 42874: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpTo(4) + loopFinish > jumpSentences(1) + newWordFragment': false,  // 42895: frags expected ["I"], but got ["Why,"]
		'skip-combos: jumpTo(4) + loopFinish > jumpSentences(1) + progress': false,  // 42896: 'progress' expected [0.4166666666666667,0.5], but got [0.75,0.8333333333333334]
		'skip-combos: jumpTo(4) + loopFinish > jumpSentences(1) + loopSkip': false,  // 42900: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpTo(4) + loopFinish > jumpSentences(3) + newWordFragment': false,  // 42921: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopFinish > jumpSentences(3) + progress': false,  // 42922: 'progress' expected [0.8333333333333334], but got [1]
		'skip-combos: jumpTo(4) + loopFinish > jumpSentences(3) + stopBegin': false,  // 42923: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopFinish > jumpSentences(3) + stopFinish': false,  // 42924: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopFinish > jumpSentences(3) + done': false,  // 42925: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopFinish > jumpSentences(3) + loopSkip': false,  // 42926: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopFinish > nextWord(null) + newWordFragment': false,  // 42973: frags expected ["you"], but got ["I"]
		'skip-combos: jumpTo(4) + loopFinish > nextWord(null) + progress': false,  // 42974: 'progress' expected [0.16666666666666666], but got [0.5]
		'skip-combos: jumpTo(4) + loopFinish > nextSentence(null) + newWordFragment': false,  // 42999: frags expected ["I"], but got ["Why,"]
		'skip-combos: jumpTo(4) + loopFinish > nextSentence(null) + progress': false,  // 43000: 'progress' expected [0.4166666666666667,0.5], but got [0.75,0.8333333333333334]
		'skip-combos: jumpTo(4) + loopFinish > nextSentence(null) + loopSkip': false,  // 43004: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpTo(4) + loopFinish > prevWord(null) + newWordFragment': false,  // 43025: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopFinish > prevWord(null) + progress': false,  // 43026: 'progress' expected [0.08333333333333333], but got [0.3333333333333333,0.25]
		'skip-combos: jumpTo(4) + loopFinish > prevWord(null) + stopBegin': false,  // 43027: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopFinish > prevWord(null) + stopFinish': false,  // 43028: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopFinish > prevWord(null) + done': false,  // 43029: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopFinish > prevWord(null) + loopSkip': false,  // 43030: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpTo(4) + newWordFragment > play(null) + newWordFragment': false,  // 43155: frags expected ["you","brave","I","back.","Why,","oh"], but got ["I","back.","Why,","oh"]
		'skip-combos: jumpTo(4) + newWordFragment > play(null) + progress': false,  // 43156: 'progress' expected [0.08333333333333333,0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1]
		'skip-combos: jumpTo(4) + newWordFragment > play(null) + loopSkip': false,  // 43160: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["come","\n","wattlebird?"]
		'skip-combos: jumpTo(4) + newWordFragment > toggle(null) + playBegin': false,  // 43161: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + newWordFragment > toggle(null) + playFinish': false,  // 43162: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + newWordFragment > toggle(null) + pauseBegin': false,  // 43167: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + newWordFragment > toggle(null) + pauseFinish': false,  // 43168: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + newWordFragment > toggle(null) + revertBegin': false,  // 43173: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + newWordFragment > toggle(null) + revertFinish': false,  // 43174: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + newWordFragment > toggle(null) + loopBegin': false,  // 43179: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + newWordFragment > toggle(null) + loopFinish': false,  // 43180: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + newWordFragment > toggle(null) + newWordFragment': false,  // 43181: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + newWordFragment > toggle(null) + progress': false,  // 43182: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + newWordFragment > toggle(null) + stopBegin': false,  // 43183: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + newWordFragment > toggle(null) + stopFinish': false,  // 43184: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + newWordFragment > toggle(null) + done': false,  // 43185: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + newWordFragment > toggle(null) + loopSkip': false,  // 43186: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + newWordFragment > rewind(null) + newWordFragment': false,  // 43311: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + newWordFragment > rewind(null) + progress': false,  // 43312: 'progress' expected [0.08333333333333333], but got [0.4166666666666667,0.3333333333333333,0.25,0.16666666666666666,0.08333333333333333]
		'skip-combos: jumpTo(4) + newWordFragment > rewind(null) + loopSkip': false,  // 43316: frags expected ["Victorious,"], but got ["Delirious,","flag.","Victorious,"]
		'skip-combos: jumpTo(4) + newWordFragment > fastForward(null) + newWordFragment': false,  // 43337: frags expected ["you","brave","I","back.","Why,","oh"], but got ["back.","Why,","oh"]
		'skip-combos: jumpTo(4) + newWordFragment > fastForward(null) + progress': false,  // 43338: 'progress' expected [0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1]
		'skip-combos: jumpTo(4) + newWordFragment > fastForward(null) + loopSkip': false,  // 43342: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["come","\n","wattlebird?"]
		'skip-combos: jumpTo(4) + newWordFragment > once([0,0,-2]) + newWordFragment': false,  // 43363: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + newWordFragment > once([0,0,-2]) + progress': false,  // 43364: 'progress' expected [0.08333333333333333], but got [0.4166666666666667,0.3333333333333333,0.25]
		'skip-combos: jumpTo(4) + newWordFragment > once([0,0,-2]) + stopBegin': false,  // 43365: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + newWordFragment > once([0,0,-2]) + stopFinish': false,  // 43366: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + newWordFragment > once([0,0,-2]) + done': false,  // 43367: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + newWordFragment > once([0,0,-2]) + loopSkip': false,  // 43368: frags expected ["Victorious,"], but got ["Delirious,","flag."]
		'skip-combos: jumpTo(4) + newWordFragment > once([0,0,2]) + newWordFragment': false,  // 43389: frags expected ["you"], but got ["back."]
		'skip-combos: jumpTo(4) + newWordFragment > once([0,0,2]) + progress': false,  // 43390: 'progress' expected [0.16666666666666666], but got [0.5833333333333334,0.6666666666666666]
		'skip-combos: jumpTo(4) + newWordFragment > once([0,0,2]) + loopSkip': false,  // 43394: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + newWordFragment > current(null) + newWordFragment': false,  // 43415: frags expected ["you"], but got ["I"]
		'skip-combos: jumpTo(4) + newWordFragment > current(null) + progress': false,  // 43416: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.5]
		'skip-combos: jumpTo(4) + newWordFragment > current(null) + loopSkip': false,  // 43420: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + newWordFragment > jumpWords(-3) + newWordFragment': false,  // 43597: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + newWordFragment > jumpWords(-3) + progress': false,  // 43598: 'progress' expected [0.08333333333333333], but got [0.25]
		'skip-combos: jumpTo(4) + newWordFragment > jumpWords(-3) + stopBegin': false,  // 43599: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + newWordFragment > jumpWords(-3) + stopFinish': false,  // 43600: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + newWordFragment > jumpWords(-3) + done': false,  // 43601: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + newWordFragment > jumpWords(-3) + loopSkip': false,  // 43602: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + newWordFragment > jumpWords(-1) + newWordFragment': false,  // 43623: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + newWordFragment > jumpWords(-1) + progress': false,  // 43624: 'progress' expected [0.08333333333333333], but got [0.4166666666666667,0.3333333333333333,0.25]
		'skip-combos: jumpTo(4) + newWordFragment > jumpWords(-1) + stopBegin': false,  // 43625: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + newWordFragment > jumpWords(-1) + stopFinish': false,  // 43626: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + newWordFragment > jumpWords(-1) + done': false,  // 43627: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + newWordFragment > jumpWords(-1) + loopSkip': false,  // 43628: frags expected ["Victorious,"], but got ["Delirious,","flag."]
		'skip-combos: jumpTo(4) + newWordFragment > jumpWords(0) + newWordFragment': false,  // 43649: frags expected ["you"], but got ["I"]
		'skip-combos: jumpTo(4) + newWordFragment > jumpWords(0) + progress': false,  // 43650: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.5]
		'skip-combos: jumpTo(4) + newWordFragment > jumpWords(0) + loopSkip': false,  // 43654: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + newWordFragment > jumpWords(4) + newWordFragment': false,  // 43675: frags expected ["I"], but got ["Why,"]
		'skip-combos: jumpTo(4) + newWordFragment > jumpWords(4) + progress': false,  // 43676: 'progress' expected [0.4166666666666667,0.5], but got [0.8333333333333334]
		'skip-combos: jumpTo(4) + newWordFragment > jumpWords(4) + loopSkip': false,  // 43680: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + newWordFragment > jumpSentences(-1) + newWordFragment': false,  // 43779: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + newWordFragment > jumpSentences(-1) + progress': false,  // 43780: 'progress' expected [0.08333333333333333], but got [0.4166666666666667,0.3333333333333333,0.25]
		'skip-combos: jumpTo(4) + newWordFragment > jumpSentences(-1) + stopBegin': false,  // 43781: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + newWordFragment > jumpSentences(-1) + stopFinish': false,  // 43782: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + newWordFragment > jumpSentences(-1) + done': false,  // 43783: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + newWordFragment > jumpSentences(-1) + loopSkip': false,  // 43784: frags expected ["Victorious,"], but got ["Delirious,","flag."]
		'skip-combos: jumpTo(4) + newWordFragment > jumpSentences(0) + newWordFragment': false,  // 43805: frags expected ["you"], but got ["I"]
		'skip-combos: jumpTo(4) + newWordFragment > jumpSentences(0) + progress': false,  // 43806: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.5]
		'skip-combos: jumpTo(4) + newWordFragment > jumpSentences(0) + loopSkip': false,  // 43810: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + newWordFragment > jumpSentences(1) + newWordFragment': false,  // 43831: frags expected ["I"], but got ["Why,"]
		'skip-combos: jumpTo(4) + newWordFragment > jumpSentences(1) + progress': false,  // 43832: 'progress' expected [0.4166666666666667,0.5], but got [0.75,0.8333333333333334]
		'skip-combos: jumpTo(4) + newWordFragment > jumpSentences(1) + loopSkip': false,  // 43836: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpTo(4) + newWordFragment > jumpSentences(3) + newWordFragment': false,  // 43857: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + newWordFragment > jumpSentences(3) + progress': false,  // 43858: 'progress' expected [0.8333333333333334], but got [1]
		'skip-combos: jumpTo(4) + newWordFragment > jumpSentences(3) + stopBegin': false,  // 43859: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + newWordFragment > jumpSentences(3) + stopFinish': false,  // 43860: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + newWordFragment > jumpSentences(3) + done': false,  // 43861: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + newWordFragment > jumpSentences(3) + loopSkip': false,  // 43862: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + newWordFragment > nextWord(null) + newWordFragment': false,  // 43909: frags expected ["you"], but got ["back."]
		'skip-combos: jumpTo(4) + newWordFragment > nextWord(null) + progress': false,  // 43910: 'progress' expected [0.16666666666666666], but got [0.5833333333333334,0.6666666666666666]
		'skip-combos: jumpTo(4) + newWordFragment > nextWord(null) + loopSkip': false,  // 43914: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + newWordFragment > nextSentence(null) + newWordFragment': false,  // 43935: frags expected ["I"], but got ["Why,"]
		'skip-combos: jumpTo(4) + newWordFragment > nextSentence(null) + progress': false,  // 43936: 'progress' expected [0.4166666666666667,0.5], but got [0.75,0.8333333333333334]
		'skip-combos: jumpTo(4) + newWordFragment > nextSentence(null) + loopSkip': false,  // 43940: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpTo(4) + newWordFragment > prevWord(null) + newWordFragment': false,  // 43961: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + newWordFragment > prevWord(null) + progress': false,  // 43962: 'progress' expected [0.08333333333333333], but got [0.4166666666666667,0.3333333333333333,0.25]
		'skip-combos: jumpTo(4) + newWordFragment > prevWord(null) + stopBegin': false,  // 43963: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + newWordFragment > prevWord(null) + stopFinish': false,  // 43964: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + newWordFragment > prevWord(null) + done': false,  // 43965: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + newWordFragment > prevWord(null) + loopSkip': false,  // 43966: frags expected ["Victorious,"], but got ["Delirious,","flag."]
		'skip-combos: jumpTo(4) + newWordFragment > prevSentence(null) + newWordFragment': false,  // 43987: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + newWordFragment > prevSentence(null) + progress': false,  // 43988: 'progress' expected [0.08333333333333333], but got [0.4166666666666667,0.3333333333333333,0.25]
		'skip-combos: jumpTo(4) + newWordFragment > prevSentence(null) + stopBegin': false,  // 43989: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + newWordFragment > prevSentence(null) + stopFinish': false,  // 43990: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + newWordFragment > prevSentence(null) + done': false,  // 43991: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + newWordFragment > prevSentence(null) + loopSkip': false,  // 43992: frags expected ["Victorious,"], but got ["Delirious,","flag."]
		'skip-combos: jumpTo(4) + progress > play(null) + newWordFragment': false,  // 44091: frags expected ["you","brave","I","back.","Why,","oh"], but got ["I","back.","Why,","oh"]
		'skip-combos: jumpTo(4) + progress > play(null) + progress': false,  // 44092: 'progress' expected [0.08333333333333333,0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1]
		'skip-combos: jumpTo(4) + progress > play(null) + loopSkip': false,  // 44096: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["Delirious,","come","\n","wattlebird?"]
		'skip-combos: jumpTo(4) + progress > toggle(null) + playBegin': false,  // 44097: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + progress > toggle(null) + playFinish': false,  // 44098: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + progress > toggle(null) + pauseBegin': false,  // 44103: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + progress > toggle(null) + pauseFinish': false,  // 44104: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + progress > toggle(null) + revertBegin': false,  // 44109: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + progress > toggle(null) + revertFinish': false,  // 44110: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + progress > toggle(null) + loopBegin': false,  // 44115: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + progress > toggle(null) + loopFinish': false,  // 44116: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + progress > toggle(null) + newWordFragment': false,  // 44117: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + progress > toggle(null) + progress': false,  // 44118: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + progress > toggle(null) + stopBegin': false,  // 44119: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + progress > toggle(null) + stopFinish': false,  // 44120: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + progress > toggle(null) + done': false,  // 44121: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + progress > toggle(null) + loopSkip': false,  // 44122: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + progress > rewind(null) + newWordFragment': false,  // 44247: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + progress > rewind(null) + progress': false,  // 44248: 'progress' expected [0.08333333333333333], but got [0.3333333333333333,0.25,0.16666666666666666,0.08333333333333333]
		'skip-combos: jumpTo(4) + progress > rewind(null) + loopSkip': false,  // 44252: frags expected ["Victorious,"], but got ["flag.","Victorious,"]
		'skip-combos: jumpTo(4) + progress > fastForward(null) + newWordFragment': false,  // 44273: frags expected ["you","brave","I","back.","Why,","oh"], but got ["I","back.","Why,","oh"]
		'skip-combos: jumpTo(4) + progress > fastForward(null) + progress': false,  // 44274: 'progress' expected [0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1]
		'skip-combos: jumpTo(4) + progress > fastForward(null) + loopSkip': false,  // 44278: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["come","\n","wattlebird?"]
		'skip-combos: jumpTo(4) + progress > once([0,0,-2]) + newWordFragment': false,  // 44299: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + progress > once([0,0,-2]) + progress': false,  // 44300: 'progress' expected [0.08333333333333333], but got [0.3333333333333333,0.25]
		'skip-combos: jumpTo(4) + progress > once([0,0,-2]) + stopBegin': false,  // 44301: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + progress > once([0,0,-2]) + stopFinish': false,  // 44302: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + progress > once([0,0,-2]) + done': false,  // 44303: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + progress > once([0,0,-2]) + loopSkip': false,  // 44304: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpTo(4) + progress > once([0,0,2]) + newWordFragment': false,  // 44325: frags expected ["you"], but got ["I"]
		'skip-combos: jumpTo(4) + progress > once([0,0,2]) + progress': false,  // 44326: 'progress' expected [0.16666666666666666], but got [0.5]
		'skip-combos: jumpTo(4) + progress > current(null) + newWordFragment': false,  // 44351: frags expected ["you"], but got ["I"]
		'skip-combos: jumpTo(4) + progress > current(null) + progress': false,  // 44352: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.4166666666666667,0.5]
		'skip-combos: jumpTo(4) + progress > current(null) + loopSkip': false,  // 44356: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpTo(4) + progress > jumpWords(-3) + newWordFragment': false,  // 44533: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + progress > jumpWords(-3) + progress': false,  // 44534: 'progress' expected [0.08333333333333333], but got [0.16666666666666666]
		'skip-combos: jumpTo(4) + progress > jumpWords(-3) + stopBegin': false,  // 44535: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + progress > jumpWords(-3) + stopFinish': false,  // 44536: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + progress > jumpWords(-3) + done': false,  // 44537: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + progress > jumpWords(-3) + loopSkip': false,  // 44538: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + progress > jumpWords(-1) + newWordFragment': false,  // 44559: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + progress > jumpWords(-1) + progress': false,  // 44560: 'progress' expected [0.08333333333333333], but got [0.3333333333333333,0.25]
		'skip-combos: jumpTo(4) + progress > jumpWords(-1) + stopBegin': false,  // 44561: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + progress > jumpWords(-1) + stopFinish': false,  // 44562: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + progress > jumpWords(-1) + done': false,  // 44563: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + progress > jumpWords(-1) + loopSkip': false,  // 44564: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpTo(4) + progress > jumpWords(0) + newWordFragment': false,  // 44585: frags expected ["you"], but got ["I"]
		'skip-combos: jumpTo(4) + progress > jumpWords(0) + progress': false,  // 44586: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.4166666666666667,0.5]
		'skip-combos: jumpTo(4) + progress > jumpWords(0) + loopSkip': false,  // 44590: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpTo(4) + progress > jumpWords(4) + newWordFragment': false,  // 44611: frags expected ["I"], but got ["Why,"]
		'skip-combos: jumpTo(4) + progress > jumpWords(4) + progress': false,  // 44612: 'progress' expected [0.4166666666666667,0.5], but got [0.75,0.8333333333333334]
		'skip-combos: jumpTo(4) + progress > jumpWords(4) + loopSkip': false,  // 44616: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpTo(4) + progress > jumpSentences(0) + newWordFragment': false,  // 44741: frags expected ["you"], but got ["I"]
		'skip-combos: jumpTo(4) + progress > jumpSentences(0) + progress': false,  // 44742: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.4166666666666667,0.5]
		'skip-combos: jumpTo(4) + progress > jumpSentences(0) + loopSkip': false,  // 44746: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpTo(4) + progress > jumpSentences(1) + newWordFragment': false,  // 44767: frags expected ["I"], but got ["Why,"]
		'skip-combos: jumpTo(4) + progress > jumpSentences(1) + progress': false,  // 44768: 'progress' expected [0.4166666666666667,0.5], but got [0.75,0.8333333333333334]
		'skip-combos: jumpTo(4) + progress > jumpSentences(1) + loopSkip': false,  // 44772: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpTo(4) + progress > jumpSentences(3) + newWordFragment': false,  // 44793: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + progress > jumpSentences(3) + progress': false,  // 44794: 'progress' expected [0.8333333333333334], but got [1]
		'skip-combos: jumpTo(4) + progress > jumpSentences(3) + stopBegin': false,  // 44795: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + progress > jumpSentences(3) + stopFinish': false,  // 44796: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + progress > jumpSentences(3) + done': false,  // 44797: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + progress > jumpSentences(3) + loopSkip': false,  // 44798: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + progress > nextWord(null) + newWordFragment': false,  // 44845: frags expected ["you"], but got ["I"]
		'skip-combos: jumpTo(4) + progress > nextWord(null) + progress': false,  // 44846: 'progress' expected [0.16666666666666666], but got [0.5]
		'skip-combos: jumpTo(4) + progress > nextSentence(null) + newWordFragment': false,  // 44871: frags expected ["I"], but got ["Why,"]
		'skip-combos: jumpTo(4) + progress > nextSentence(null) + progress': false,  // 44872: 'progress' expected [0.4166666666666667,0.5], but got [0.75,0.8333333333333334]
		'skip-combos: jumpTo(4) + progress > nextSentence(null) + loopSkip': false,  // 44876: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpTo(4) + progress > prevWord(null) + newWordFragment': false,  // 44897: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + progress > prevWord(null) + progress': false,  // 44898: 'progress' expected [0.08333333333333333], but got [0.3333333333333333,0.25]
		'skip-combos: jumpTo(4) + progress > prevWord(null) + stopBegin': false,  // 44899: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + progress > prevWord(null) + stopFinish': false,  // 44900: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + progress > prevWord(null) + done': false,  // 44901: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + progress > prevWord(null) + loopSkip': false,  // 44902: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpTo(4) + loopSkip > play(null) + newWordFragment': false,  // 47835: frags expected ["you","brave","I","back.","Why,","oh"], but got ["I","back.","Why,","oh"]
		'skip-combos: jumpTo(4) + loopSkip > play(null) + progress': false,  // 47836: 'progress' expected [0.08333333333333333,0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1]
		'skip-combos: jumpTo(4) + loopSkip > play(null) + loopSkip': false,  // 47840: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["Delirious,","come","\n","wattlebird?"]
		'skip-combos: jumpTo(4) + loopSkip > toggle(null) + playBegin': false,  // 47841: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopSkip > toggle(null) + playFinish': false,  // 47842: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopSkip > toggle(null) + pauseBegin': false,  // 47847: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopSkip > toggle(null) + pauseFinish': false,  // 47848: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopSkip > toggle(null) + revertBegin': false,  // 47853: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopSkip > toggle(null) + revertFinish': false,  // 47854: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopSkip > toggle(null) + loopBegin': false,  // 47859: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopSkip > toggle(null) + loopFinish': false,  // 47860: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopSkip > toggle(null) + newWordFragment': false,  // 47861: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopSkip > toggle(null) + progress': false,  // 47862: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopSkip > toggle(null) + stopBegin': false,  // 47863: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopSkip > toggle(null) + stopFinish': false,  // 47864: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopSkip > toggle(null) + done': false,  // 47865: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopSkip > toggle(null) + loopSkip': false,  // 47866: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopSkip > rewind(null) + newWordFragment': false,  // 47991: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopSkip > rewind(null) + progress': false,  // 47992: 'progress' expected [0.08333333333333333], but got [0.3333333333333333,0.25,0.16666666666666666,0.08333333333333333]
		'skip-combos: jumpTo(4) + loopSkip > rewind(null) + loopSkip': false,  // 47996: frags expected ["Victorious,"], but got ["flag.","Victorious,"]
		'skip-combos: jumpTo(4) + loopSkip > fastForward(null) + newWordFragment': false,  // 48017: frags expected ["you","brave","I","back.","Why,","oh"], but got ["I","back.","Why,","oh"]
		'skip-combos: jumpTo(4) + loopSkip > fastForward(null) + progress': false,  // 48018: 'progress' expected [0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1]
		'skip-combos: jumpTo(4) + loopSkip > fastForward(null) + loopSkip': false,  // 48022: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["come","\n","wattlebird?"]
		'skip-combos: jumpTo(4) + loopSkip > once([0,0,-2]) + newWordFragment': false,  // 48043: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopSkip > once([0,0,-2]) + progress': false,  // 48044: 'progress' expected [0.08333333333333333], but got [0.3333333333333333,0.25]
		'skip-combos: jumpTo(4) + loopSkip > once([0,0,-2]) + stopBegin': false,  // 48045: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopSkip > once([0,0,-2]) + stopFinish': false,  // 48046: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopSkip > once([0,0,-2]) + done': false,  // 48047: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopSkip > once([0,0,-2]) + loopSkip': false,  // 48048: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpTo(4) + loopSkip > once([0,0,2]) + newWordFragment': false,  // 48069: frags expected ["you"], but got ["I"]
		'skip-combos: jumpTo(4) + loopSkip > once([0,0,2]) + progress': false,  // 48070: 'progress' expected [0.16666666666666666], but got [0.5]
		'skip-combos: jumpTo(4) + loopSkip > current(null) + newWordFragment': false,  // 48095: frags expected ["you"], but got ["I"]
		'skip-combos: jumpTo(4) + loopSkip > current(null) + progress': false,  // 48096: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.4166666666666667,0.5]
		'skip-combos: jumpTo(4) + loopSkip > current(null) + loopSkip': false,  // 48100: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpTo(4) + loopSkip > jumpWords(-3) + newWordFragment': false,  // 48277: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopSkip > jumpWords(-3) + progress': false,  // 48278: 'progress' expected [0.08333333333333333], but got [0.16666666666666666]
		'skip-combos: jumpTo(4) + loopSkip > jumpWords(-3) + stopBegin': false,  // 48279: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopSkip > jumpWords(-3) + stopFinish': false,  // 48280: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopSkip > jumpWords(-3) + done': false,  // 48281: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopSkip > jumpWords(-3) + loopSkip': false,  // 48282: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopSkip > jumpWords(-1) + newWordFragment': false,  // 48303: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopSkip > jumpWords(-1) + progress': false,  // 48304: 'progress' expected [0.08333333333333333], but got [0.3333333333333333,0.25]
		'skip-combos: jumpTo(4) + loopSkip > jumpWords(-1) + stopBegin': false,  // 48305: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopSkip > jumpWords(-1) + stopFinish': false,  // 48306: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopSkip > jumpWords(-1) + done': false,  // 48307: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopSkip > jumpWords(-1) + loopSkip': false,  // 48308: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpTo(4) + loopSkip > jumpWords(0) + newWordFragment': false,  // 48329: frags expected ["you"], but got ["I"]
		'skip-combos: jumpTo(4) + loopSkip > jumpWords(0) + progress': false,  // 48330: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.4166666666666667,0.5]
		'skip-combos: jumpTo(4) + loopSkip > jumpWords(0) + loopSkip': false,  // 48334: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpTo(4) + loopSkip > jumpWords(4) + newWordFragment': false,  // 48355: frags expected ["I"], but got ["Why,"]
		'skip-combos: jumpTo(4) + loopSkip > jumpWords(4) + progress': false,  // 48356: 'progress' expected [0.4166666666666667,0.5], but got [0.75,0.8333333333333334]
		'skip-combos: jumpTo(4) + loopSkip > jumpWords(4) + loopSkip': false,  // 48360: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpTo(4) + loopSkip > jumpSentences(0) + newWordFragment': false,  // 48485: frags expected ["you"], but got ["I"]
		'skip-combos: jumpTo(4) + loopSkip > jumpSentences(0) + progress': false,  // 48486: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.4166666666666667,0.5]
		'skip-combos: jumpTo(4) + loopSkip > jumpSentences(0) + loopSkip': false,  // 48490: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpTo(4) + loopSkip > jumpSentences(1) + newWordFragment': false,  // 48511: frags expected ["I"], but got ["Why,"]
		'skip-combos: jumpTo(4) + loopSkip > jumpSentences(1) + progress': false,  // 48512: 'progress' expected [0.4166666666666667,0.5], but got [0.75,0.8333333333333334]
		'skip-combos: jumpTo(4) + loopSkip > jumpSentences(1) + loopSkip': false,  // 48516: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpTo(4) + loopSkip > jumpSentences(3) + newWordFragment': false,  // 48537: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopSkip > jumpSentences(3) + progress': false,  // 48538: 'progress' expected [0.8333333333333334], but got [1]
		'skip-combos: jumpTo(4) + loopSkip > jumpSentences(3) + stopBegin': false,  // 48539: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopSkip > jumpSentences(3) + stopFinish': false,  // 48540: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopSkip > jumpSentences(3) + done': false,  // 48541: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopSkip > jumpSentences(3) + loopSkip': false,  // 48542: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopSkip > nextWord(null) + newWordFragment': false,  // 48589: frags expected ["you"], but got ["I"]
		'skip-combos: jumpTo(4) + loopSkip > nextWord(null) + progress': false,  // 48590: 'progress' expected [0.16666666666666666], but got [0.5]
		'skip-combos: jumpTo(4) + loopSkip > nextSentence(null) + newWordFragment': false,  // 48615: frags expected ["I"], but got ["Why,"]
		'skip-combos: jumpTo(4) + loopSkip > nextSentence(null) + progress': false,  // 48616: 'progress' expected [0.4166666666666667,0.5], but got [0.75,0.8333333333333334]
		'skip-combos: jumpTo(4) + loopSkip > nextSentence(null) + loopSkip': false,  // 48620: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpTo(4) + loopSkip > prevWord(null) + newWordFragment': false,  // 48641: event should not have been triggerd but WAS
		'skip-combos: jumpTo(4) + loopSkip > prevWord(null) + progress': false,  // 48642: 'progress' expected [0.08333333333333333], but got [0.3333333333333333,0.25]
		'skip-combos: jumpTo(4) + loopSkip > prevWord(null) + stopBegin': false,  // 48643: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopSkip > prevWord(null) + stopFinish': false,  // 48644: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopSkip > prevWord(null) + done': false,  // 48645: event should have been triggerd but was NOT
		'skip-combos: jumpTo(4) + loopSkip > prevWord(null) + loopSkip': false,  // 48646: frags expected ["Victorious,"], but got ["flag."]

		// jumpTo(11)
		'skip-combos: jumpTo(11) + onceBegin > rewind(null) + loopSkip': false,  // 9620: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpTo(11) + onceBegin > fastForward(null) + loopSkip': false,  // 9646: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + onceBegin > once([0,0,-2]) + loopSkip': false,  // 9672: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + onceBegin > once([0,0,2]) + loopSkip': false,  // 9698: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + onceBegin > current(null) + loopSkip': false,  // 9724: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + onceBegin > jumpWords(-3) + loopSkip': false,  // 9906: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpTo(11) + onceBegin > jumpWords(-1) + loopSkip': false,  // 9932: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + onceBegin > jumpWords(0) + loopSkip': false,  // 9958: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + onceBegin > jumpWords(4) + loopSkip': false,  // 9984: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + onceBegin > jumpSentences(-1) + loopSkip': false,  // 10088: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + onceBegin > jumpSentences(0) + loopSkip': false,  // 10114: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + onceBegin > jumpSentences(1) + loopSkip': false,  // 10140: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + onceBegin > jumpSentences(3) + loopSkip': false,  // 10166: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + onceBegin > nextWord(null) + loopSkip': false,  // 10218: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + onceBegin > nextSentence(null) + loopSkip': false,  // 10244: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + onceBegin > prevWord(null) + loopSkip': false,  // 10270: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + onceBegin > prevSentence(null) + loopSkip': false,  // 10296: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + onceFinish > rewind(null) + loopSkip': false,  // 10556: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpTo(11) + onceFinish > fastForward(null) + loopSkip': false,  // 10582: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + onceFinish > once([0,0,-2]) + loopSkip': false,  // 10608: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + onceFinish > once([0,0,2]) + loopSkip': false,  // 10634: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + onceFinish > current(null) + loopSkip': false,  // 10660: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + onceFinish > jumpWords(-3) + loopSkip': false,  // 10842: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpTo(11) + onceFinish > jumpWords(-1) + loopSkip': false,  // 10868: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + onceFinish > jumpWords(0) + loopSkip': false,  // 10894: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + onceFinish > jumpWords(4) + loopSkip': false,  // 10920: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + onceFinish > jumpSentences(-1) + loopSkip': false,  // 11024: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + onceFinish > jumpSentences(0) + loopSkip': false,  // 11050: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + onceFinish > jumpSentences(1) + loopSkip': false,  // 11076: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + onceFinish > jumpSentences(3) + loopSkip': false,  // 11102: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + onceFinish > nextWord(null) + loopSkip': false,  // 11154: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + onceFinish > nextSentence(null) + loopSkip': false,  // 11180: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + onceFinish > prevWord(null) + loopSkip': false,  // 11206: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + onceFinish > prevSentence(null) + loopSkip': false,  // 11232: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopBegin > rewind(null) + loopSkip': false,  // 17108: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpTo(11) + loopBegin > fastForward(null) + loopSkip': false,  // 17134: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + loopBegin > once([0,0,-2]) + loopSkip': false,  // 17160: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopBegin > once([0,0,2]) + loopSkip': false,  // 17186: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopBegin > current(null) + loopSkip': false,  // 17212: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + loopBegin > jumpWords(-3) + loopSkip': false,  // 17394: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpTo(11) + loopBegin > jumpWords(-1) + loopSkip': false,  // 17420: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopBegin > jumpWords(0) + loopSkip': false,  // 17446: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + loopBegin > jumpWords(4) + loopSkip': false,  // 17472: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + loopBegin > jumpSentences(-1) + loopSkip': false,  // 17576: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopBegin > jumpSentences(0) + loopSkip': false,  // 17602: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + loopBegin > jumpSentences(1) + loopSkip': false,  // 17628: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + loopBegin > jumpSentences(3) + loopSkip': false,  // 17654: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopBegin > nextWord(null) + loopSkip': false,  // 17706: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopBegin > nextSentence(null) + loopSkip': false,  // 17732: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + loopBegin > prevWord(null) + loopSkip': false,  // 17758: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopBegin > prevSentence(null) + loopSkip': false,  // 17784: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopFinish > rewind(null) + loopSkip': false,  // 18044: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpTo(11) + loopFinish > fastForward(null) + loopSkip': false,  // 18070: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + loopFinish > once([0,0,-2]) + loopSkip': false,  // 18096: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopFinish > once([0,0,2]) + loopSkip': false,  // 18122: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopFinish > current(null) + loopSkip': false,  // 18148: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + loopFinish > jumpWords(-3) + loopSkip': false,  // 18330: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpTo(11) + loopFinish > jumpWords(-1) + loopSkip': false,  // 18356: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopFinish > jumpWords(0) + loopSkip': false,  // 18382: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + loopFinish > jumpWords(4) + loopSkip': false,  // 18408: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + loopFinish > jumpSentences(-1) + loopSkip': false,  // 18512: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopFinish > jumpSentences(0) + loopSkip': false,  // 18538: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + loopFinish > jumpSentences(1) + loopSkip': false,  // 18564: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + loopFinish > jumpSentences(3) + loopSkip': false,  // 18590: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopFinish > nextWord(null) + loopSkip': false,  // 18642: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopFinish > nextSentence(null) + loopSkip': false,  // 18668: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + loopFinish > prevWord(null) + loopSkip': false,  // 18694: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopFinish > prevSentence(null) + loopSkip': false,  // 18720: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + progress > rewind(null) + loopSkip': false,  // 19916: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpTo(11) + progress > fastForward(null) + loopSkip': false,  // 19942: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + progress > once([0,0,-2]) + loopSkip': false,  // 19968: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + progress > once([0,0,2]) + loopSkip': false,  // 19994: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + progress > current(null) + loopSkip': false,  // 20020: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + progress > jumpWords(-3) + loopSkip': false,  // 20202: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpTo(11) + progress > jumpWords(-1) + loopSkip': false,  // 20228: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + progress > jumpWords(0) + loopSkip': false,  // 20254: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + progress > jumpWords(4) + loopSkip': false,  // 20280: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + progress > jumpSentences(-1) + loopSkip': false,  // 20384: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + progress > jumpSentences(0) + loopSkip': false,  // 20410: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + progress > jumpSentences(1) + loopSkip': false,  // 20436: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + progress > jumpSentences(3) + loopSkip': false,  // 20462: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + progress > nextWord(null) + loopSkip': false,  // 20514: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + progress > nextSentence(null) + loopSkip': false,  // 20540: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + progress > prevWord(null) + loopSkip': false,  // 20566: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + progress > prevSentence(null) + loopSkip': false,  // 20592: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + stopBegin > rewind(null) + loopSkip': false,  // 20852: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpTo(11) + stopBegin > fastForward(null) + loopSkip': false,  // 20878: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + stopBegin > once([0,0,-2]) + loopSkip': false,  // 20904: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + stopBegin > once([0,0,2]) + loopSkip': false,  // 20930: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + stopBegin > current(null) + loopSkip': false,  // 20956: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + stopBegin > jumpWords(-3) + loopSkip': false,  // 21138: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpTo(11) + stopBegin > jumpWords(-1) + loopSkip': false,  // 21164: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + stopBegin > jumpWords(0) + loopSkip': false,  // 21190: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + stopBegin > jumpWords(4) + loopSkip': false,  // 21216: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + stopBegin > jumpSentences(-1) + loopSkip': false,  // 21320: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + stopBegin > jumpSentences(0) + loopSkip': false,  // 21346: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + stopBegin > jumpSentences(1) + loopSkip': false,  // 21372: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + stopBegin > jumpSentences(3) + loopSkip': false,  // 21398: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + stopBegin > nextWord(null) + loopSkip': false,  // 21450: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + stopBegin > nextSentence(null) + loopSkip': false,  // 21476: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + stopBegin > prevWord(null) + loopSkip': false,  // 21502: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + stopBegin > prevSentence(null) + loopSkip': false,  // 21528: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + stopFinish > rewind(null) + loopSkip': false,  // 21788: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpTo(11) + stopFinish > fastForward(null) + loopSkip': false,  // 21814: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + stopFinish > once([0,0,-2]) + loopSkip': false,  // 21840: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + stopFinish > once([0,0,2]) + loopSkip': false,  // 21866: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + stopFinish > current(null) + loopSkip': false,  // 21892: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + stopFinish > jumpWords(-3) + loopSkip': false,  // 22074: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpTo(11) + stopFinish > jumpWords(-1) + loopSkip': false,  // 22100: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + stopFinish > jumpWords(0) + loopSkip': false,  // 22126: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + stopFinish > jumpWords(4) + loopSkip': false,  // 22152: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + stopFinish > jumpSentences(-1) + loopSkip': false,  // 22256: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + stopFinish > jumpSentences(0) + loopSkip': false,  // 22282: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + stopFinish > jumpSentences(1) + loopSkip': false,  // 22308: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + stopFinish > jumpSentences(3) + loopSkip': false,  // 22334: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + stopFinish > nextWord(null) + loopSkip': false,  // 22386: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + stopFinish > nextSentence(null) + loopSkip': false,  // 22412: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + stopFinish > prevWord(null) + loopSkip': false,  // 22438: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + stopFinish > prevSentence(null) + loopSkip': false,  // 22464: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + done > rewind(null) + loopSkip': false,  // 22724: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpTo(11) + done > fastForward(null) + loopSkip': false,  // 22750: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + done > once([0,0,-2]) + loopSkip': false,  // 22776: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + done > once([0,0,2]) + loopSkip': false,  // 22802: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + done > current(null) + loopSkip': false,  // 22828: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + done > jumpWords(-3) + loopSkip': false,  // 23010: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpTo(11) + done > jumpWords(-1) + loopSkip': false,  // 23036: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + done > jumpWords(0) + loopSkip': false,  // 23062: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + done > jumpWords(4) + loopSkip': false,  // 23088: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + done > jumpSentences(-1) + loopSkip': false,  // 23192: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + done > jumpSentences(0) + loopSkip': false,  // 23218: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + done > jumpSentences(1) + loopSkip': false,  // 23244: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + done > jumpSentences(3) + loopSkip': false,  // 23270: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + done > nextWord(null) + loopSkip': false,  // 23322: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + done > nextSentence(null) + loopSkip': false,  // 23348: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + done > prevWord(null) + loopSkip': false,  // 23374: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + done > prevSentence(null) + loopSkip': false,  // 23400: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopSkip > play(null) + playBegin': false,  // 23479: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopSkip > play(null) + playFinish': false,  // 23480: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopSkip > play(null) + restartBegin': false,  // 23483: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > play(null) + restartFinish': false,  // 23484: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > toggle(null) + playBegin': false,  // 23505: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopSkip > toggle(null) + playFinish': false,  // 23506: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopSkip > toggle(null) + restartBegin': false,  // 23509: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > toggle(null) + restartFinish': false,  // 23510: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > rewind(null) + newWordFragment': false,  // 23655: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > rewind(null) + progress': false,  // 23656: 'progress' expected [0.08333333333333333], but got [0.9166666666666666,0.8333333333333334,0.75,0.6666666666666666,0.5833333333333334,0.5,0.4166666666666667,0.3333333333333333,0.25,0.16666666666666666,0.08333333333333333]
		'skip-combos: jumpTo(11) + loopSkip > rewind(null) + loopSkip': false,  // 23660: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpTo(11) + loopSkip > fastForward(null) + newWordFragment': false,  // 23681: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopSkip > fastForward(null) + progress': false,  // 23682: 'progress' expected [0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [1]
		'skip-combos: jumpTo(11) + loopSkip > fastForward(null) + loopSkip': false,  // 23686: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + loopSkip > once([0,0,-2]) + newWordFragment': false,  // 23707: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > once([0,0,-2]) + progress': false,  // 23708: 'progress' expected [0.08333333333333333], but got [0.9166666666666666]
		'skip-combos: jumpTo(11) + loopSkip > once([0,0,-2]) + stopBegin': false,  // 23709: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopSkip > once([0,0,-2]) + stopFinish': false,  // 23710: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopSkip > once([0,0,-2]) + done': false,  // 23711: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopSkip > once([0,0,-2]) + loopSkip': false,  // 23712: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopSkip > once([0,0,2]) + newWordFragment': false,  // 23733: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopSkip > once([0,0,2]) + progress': false,  // 23734: 'progress' expected [0.16666666666666666], but got [1]
		'skip-combos: jumpTo(11) + loopSkip > once([0,0,2]) + stopBegin': false,  // 23735: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > once([0,0,2]) + stopFinish': false,  // 23736: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > once([0,0,2]) + done': false,  // 23737: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > once([0,0,2]) + loopSkip': false,  // 23738: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > current(null) + newWordFragment': false,  // 23759: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopSkip > current(null) + progress': false,  // 23760: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [1]
		'skip-combos: jumpTo(11) + loopSkip > current(null) + stopBegin': false,  // 23761: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > current(null) + stopFinish': false,  // 23762: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > current(null) + done': false,  // 23763: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > current(null) + loopSkip': false,  // 23764: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + loopSkip > jumpWords(-3) + newWordFragment': false,  // 23941: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > jumpWords(-3) + progress': false,  // 23942: 'progress' expected [0.08333333333333333], but got [0.75,0.6666666666666666]
		'skip-combos: jumpTo(11) + loopSkip > jumpWords(-3) + stopBegin': false,  // 23943: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopSkip > jumpWords(-3) + stopFinish': false,  // 23944: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopSkip > jumpWords(-3) + done': false,  // 23945: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopSkip > jumpWords(-3) + loopSkip': false,  // 23946: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpTo(11) + loopSkip > jumpWords(-1) + newWordFragment': false,  // 23967: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > jumpWords(-1) + progress': false,  // 23968: 'progress' expected [0.08333333333333333], but got [0.9166666666666666]
		'skip-combos: jumpTo(11) + loopSkip > jumpWords(-1) + stopBegin': false,  // 23969: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopSkip > jumpWords(-1) + stopFinish': false,  // 23970: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopSkip > jumpWords(-1) + done': false,  // 23971: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopSkip > jumpWords(-1) + loopSkip': false,  // 23972: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopSkip > jumpWords(0) + newWordFragment': false,  // 23993: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopSkip > jumpWords(0) + progress': false,  // 23994: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [1]
		'skip-combos: jumpTo(11) + loopSkip > jumpWords(0) + stopBegin': false,  // 23995: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > jumpWords(0) + stopFinish': false,  // 23996: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > jumpWords(0) + done': false,  // 23997: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > jumpWords(0) + loopSkip': false,  // 23998: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + loopSkip > jumpWords(4) + newWordFragment': false,  // 24019: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopSkip > jumpWords(4) + progress': false,  // 24020: 'progress' expected [0.4166666666666667,0.5], but got [1]
		'skip-combos: jumpTo(11) + loopSkip > jumpWords(4) + stopBegin': false,  // 24021: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > jumpWords(4) + stopFinish': false,  // 24022: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > jumpWords(4) + done': false,  // 24023: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > jumpWords(4) + loopSkip': false,  // 24024: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + loopSkip > jumpSentences(-1) + newWordFragment': false,  // 24123: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > jumpSentences(-1) + progress': false,  // 24124: 'progress' expected [0.08333333333333333], but got [0.8333333333333334]
		'skip-combos: jumpTo(11) + loopSkip > jumpSentences(-1) + stopBegin': false,  // 24125: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopSkip > jumpSentences(-1) + stopFinish': false,  // 24126: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopSkip > jumpSentences(-1) + done': false,  // 24127: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopSkip > jumpSentences(-1) + loopSkip': false,  // 24128: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopSkip > jumpSentences(0) + newWordFragment': false,  // 24149: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopSkip > jumpSentences(0) + progress': false,  // 24150: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [1]
		'skip-combos: jumpTo(11) + loopSkip > jumpSentences(0) + stopBegin': false,  // 24151: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > jumpSentences(0) + stopFinish': false,  // 24152: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > jumpSentences(0) + done': false,  // 24153: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > jumpSentences(0) + loopSkip': false,  // 24154: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + loopSkip > jumpSentences(1) + newWordFragment': false,  // 24175: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopSkip > jumpSentences(1) + progress': false,  // 24176: 'progress' expected [0.4166666666666667,0.5], but got [1]
		'skip-combos: jumpTo(11) + loopSkip > jumpSentences(1) + stopBegin': false,  // 24177: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > jumpSentences(1) + stopFinish': false,  // 24178: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > jumpSentences(1) + done': false,  // 24179: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > jumpSentences(1) + loopSkip': false,  // 24180: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + loopSkip > jumpSentences(3) + newWordFragment': false,  // 24201: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopSkip > jumpSentences(3) + progress': false,  // 24202: 'progress' expected [0.8333333333333334], but got [1]
		'skip-combos: jumpTo(11) + loopSkip > jumpSentences(3) + stopBegin': false,  // 24203: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > jumpSentences(3) + stopFinish': false,  // 24204: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > jumpSentences(3) + done': false,  // 24205: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > jumpSentences(3) + loopSkip': false,  // 24206: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > nextWord(null) + newWordFragment': false,  // 24253: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopSkip > nextWord(null) + progress': false,  // 24254: 'progress' expected [0.16666666666666666], but got [1]
		'skip-combos: jumpTo(11) + loopSkip > nextWord(null) + stopBegin': false,  // 24255: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > nextWord(null) + stopFinish': false,  // 24256: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > nextWord(null) + done': false,  // 24257: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > nextWord(null) + loopSkip': false,  // 24258: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > nextSentence(null) + newWordFragment': false,  // 24279: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopSkip > nextSentence(null) + progress': false,  // 24280: 'progress' expected [0.4166666666666667,0.5], but got [1]
		'skip-combos: jumpTo(11) + loopSkip > nextSentence(null) + stopBegin': false,  // 24281: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > nextSentence(null) + stopFinish': false,  // 24282: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > nextSentence(null) + done': false,  // 24283: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > nextSentence(null) + loopSkip': false,  // 24284: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(11) + loopSkip > prevWord(null) + newWordFragment': false,  // 24305: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > prevWord(null) + progress': false,  // 24306: 'progress' expected [0.08333333333333333], but got [0.9166666666666666]
		'skip-combos: jumpTo(11) + loopSkip > prevWord(null) + stopBegin': false,  // 24307: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopSkip > prevWord(null) + stopFinish': false,  // 24308: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopSkip > prevWord(null) + done': false,  // 24309: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopSkip > prevWord(null) + loopSkip': false,  // 24310: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopSkip > prevSentence(null) + newWordFragment': false,  // 24331: event should not have been triggerd but WAS
		'skip-combos: jumpTo(11) + loopSkip > prevSentence(null) + progress': false,  // 24332: 'progress' expected [0.08333333333333333], but got [0.8333333333333334]
		'skip-combos: jumpTo(11) + loopSkip > prevSentence(null) + stopBegin': false,  // 24333: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopSkip > prevSentence(null) + stopFinish': false,  // 24334: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopSkip > prevSentence(null) + done': false,  // 24335: event should have been triggerd but was NOT
		'skip-combos: jumpTo(11) + loopSkip > prevSentence(null) + loopSkip': false,  // 24336: event should have been triggerd but was NOT

		// jumpTo(100)
		'skip-combos: jumpTo(100) + onceBegin > rewind(null) + loopSkip': false,  // 33956: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpTo(100) + onceBegin > fastForward(null) + loopSkip': false,  // 33982: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + onceBegin > once([0,0,-2]) + loopSkip': false,  // 34008: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + onceBegin > once([0,0,2]) + loopSkip': false,  // 34034: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + onceBegin > current(null) + loopSkip': false,  // 34060: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + onceBegin > jumpWords(-3) + loopSkip': false,  // 34242: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpTo(100) + onceBegin > jumpWords(-1) + loopSkip': false,  // 34268: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + onceBegin > jumpWords(0) + loopSkip': false,  // 34294: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + onceBegin > jumpWords(4) + loopSkip': false,  // 34320: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + onceBegin > jumpSentences(-1) + loopSkip': false,  // 34424: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + onceBegin > jumpSentences(0) + loopSkip': false,  // 34450: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + onceBegin > jumpSentences(1) + loopSkip': false,  // 34476: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + onceBegin > jumpSentences(3) + loopSkip': false,  // 34502: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + onceBegin > nextWord(null) + loopSkip': false,  // 34554: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + onceBegin > nextSentence(null) + loopSkip': false,  // 34580: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + onceBegin > prevWord(null) + loopSkip': false,  // 34606: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + onceBegin > prevSentence(null) + loopSkip': false,  // 34632: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + onceFinish > rewind(null) + loopSkip': false,  // 34892: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpTo(100) + onceFinish > fastForward(null) + loopSkip': false,  // 34918: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + onceFinish > once([0,0,-2]) + loopSkip': false,  // 34944: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + onceFinish > once([0,0,2]) + loopSkip': false,  // 34970: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + onceFinish > current(null) + loopSkip': false,  // 34996: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + onceFinish > jumpWords(-3) + loopSkip': false,  // 35178: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpTo(100) + onceFinish > jumpWords(-1) + loopSkip': false,  // 35204: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + onceFinish > jumpWords(0) + loopSkip': false,  // 35230: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + onceFinish > jumpWords(4) + loopSkip': false,  // 35256: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + onceFinish > jumpSentences(-1) + loopSkip': false,  // 35360: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + onceFinish > jumpSentences(0) + loopSkip': false,  // 35386: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + onceFinish > jumpSentences(1) + loopSkip': false,  // 35412: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + onceFinish > jumpSentences(3) + loopSkip': false,  // 35438: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + onceFinish > nextWord(null) + loopSkip': false,  // 35490: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + onceFinish > nextSentence(null) + loopSkip': false,  // 35516: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + onceFinish > prevWord(null) + loopSkip': false,  // 35542: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + onceFinish > prevSentence(null) + loopSkip': false,  // 35568: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopBegin > rewind(null) + loopSkip': false,  // 41444: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpTo(100) + loopBegin > fastForward(null) + loopSkip': false,  // 41470: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + loopBegin > once([0,0,-2]) + loopSkip': false,  // 41496: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopBegin > once([0,0,2]) + loopSkip': false,  // 41522: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopBegin > current(null) + loopSkip': false,  // 41548: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + loopBegin > jumpWords(-3) + loopSkip': false,  // 41730: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpTo(100) + loopBegin > jumpWords(-1) + loopSkip': false,  // 41756: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopBegin > jumpWords(0) + loopSkip': false,  // 41782: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + loopBegin > jumpWords(4) + loopSkip': false,  // 41808: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + loopBegin > jumpSentences(-1) + loopSkip': false,  // 41912: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopBegin > jumpSentences(0) + loopSkip': false,  // 41938: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + loopBegin > jumpSentences(1) + loopSkip': false,  // 41964: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + loopBegin > jumpSentences(3) + loopSkip': false,  // 41990: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopBegin > nextWord(null) + loopSkip': false,  // 42042: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopBegin > nextSentence(null) + loopSkip': false,  // 42068: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + loopBegin > prevWord(null) + loopSkip': false,  // 42094: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopBegin > prevSentence(null) + loopSkip': false,  // 42120: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopFinish > rewind(null) + loopSkip': false,  // 42380: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpTo(100) + loopFinish > fastForward(null) + loopSkip': false,  // 42406: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + loopFinish > once([0,0,-2]) + loopSkip': false,  // 42432: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopFinish > once([0,0,2]) + loopSkip': false,  // 42458: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopFinish > current(null) + loopSkip': false,  // 42484: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + loopFinish > jumpWords(-3) + loopSkip': false,  // 42666: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpTo(100) + loopFinish > jumpWords(-1) + loopSkip': false,  // 42692: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopFinish > jumpWords(0) + loopSkip': false,  // 42718: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + loopFinish > jumpWords(4) + loopSkip': false,  // 42744: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + loopFinish > jumpSentences(-1) + loopSkip': false,  // 42848: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopFinish > jumpSentences(0) + loopSkip': false,  // 42874: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + loopFinish > jumpSentences(1) + loopSkip': false,  // 42900: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + loopFinish > jumpSentences(3) + loopSkip': false,  // 42926: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopFinish > nextWord(null) + loopSkip': false,  // 42978: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopFinish > nextSentence(null) + loopSkip': false,  // 43004: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + loopFinish > prevWord(null) + loopSkip': false,  // 43030: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopFinish > prevSentence(null) + loopSkip': false,  // 43056: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + progress > rewind(null) + loopSkip': false,  // 44252: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpTo(100) + progress > fastForward(null) + loopSkip': false,  // 44278: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + progress > once([0,0,-2]) + loopSkip': false,  // 44304: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + progress > once([0,0,2]) + loopSkip': false,  // 44330: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + progress > current(null) + loopSkip': false,  // 44356: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + progress > jumpWords(-3) + loopSkip': false,  // 44538: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpTo(100) + progress > jumpWords(-1) + loopSkip': false,  // 44564: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + progress > jumpWords(0) + loopSkip': false,  // 44590: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + progress > jumpWords(4) + loopSkip': false,  // 44616: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + progress > jumpSentences(-1) + loopSkip': false,  // 44720: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + progress > jumpSentences(0) + loopSkip': false,  // 44746: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + progress > jumpSentences(1) + loopSkip': false,  // 44772: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + progress > jumpSentences(3) + loopSkip': false,  // 44798: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + progress > nextWord(null) + loopSkip': false,  // 44850: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + progress > nextSentence(null) + loopSkip': false,  // 44876: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + progress > prevWord(null) + loopSkip': false,  // 44902: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + progress > prevSentence(null) + loopSkip': false,  // 44928: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + stopBegin > rewind(null) + loopSkip': false,  // 45188: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpTo(100) + stopBegin > fastForward(null) + loopSkip': false,  // 45214: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + stopBegin > once([0,0,-2]) + loopSkip': false,  // 45240: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + stopBegin > once([0,0,2]) + loopSkip': false,  // 45266: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + stopBegin > current(null) + loopSkip': false,  // 45292: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + stopBegin > jumpWords(-3) + loopSkip': false,  // 45474: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpTo(100) + stopBegin > jumpWords(-1) + loopSkip': false,  // 45500: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + stopBegin > jumpWords(0) + loopSkip': false,  // 45526: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + stopBegin > jumpWords(4) + loopSkip': false,  // 45552: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + stopBegin > jumpSentences(-1) + loopSkip': false,  // 45656: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + stopBegin > jumpSentences(0) + loopSkip': false,  // 45682: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + stopBegin > jumpSentences(1) + loopSkip': false,  // 45708: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + stopBegin > jumpSentences(3) + loopSkip': false,  // 45734: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + stopBegin > nextWord(null) + loopSkip': false,  // 45786: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + stopBegin > nextSentence(null) + loopSkip': false,  // 45812: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + stopBegin > prevWord(null) + loopSkip': false,  // 45838: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + stopBegin > prevSentence(null) + loopSkip': false,  // 45864: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + stopFinish > rewind(null) + loopSkip': false,  // 46124: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpTo(100) + stopFinish > fastForward(null) + loopSkip': false,  // 46150: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + stopFinish > once([0,0,-2]) + loopSkip': false,  // 46176: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + stopFinish > once([0,0,2]) + loopSkip': false,  // 46202: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + stopFinish > current(null) + loopSkip': false,  // 46228: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + stopFinish > jumpWords(-3) + loopSkip': false,  // 46410: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpTo(100) + stopFinish > jumpWords(-1) + loopSkip': false,  // 46436: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + stopFinish > jumpWords(0) + loopSkip': false,  // 46462: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + stopFinish > jumpWords(4) + loopSkip': false,  // 46488: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + stopFinish > jumpSentences(-1) + loopSkip': false,  // 46592: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + stopFinish > jumpSentences(0) + loopSkip': false,  // 46618: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + stopFinish > jumpSentences(1) + loopSkip': false,  // 46644: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + stopFinish > jumpSentences(3) + loopSkip': false,  // 46670: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + stopFinish > nextWord(null) + loopSkip': false,  // 46722: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + stopFinish > nextSentence(null) + loopSkip': false,  // 46748: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + stopFinish > prevWord(null) + loopSkip': false,  // 46774: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + stopFinish > prevSentence(null) + loopSkip': false,  // 46800: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + done > rewind(null) + loopSkip': false,  // 47060: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpTo(100) + done > fastForward(null) + loopSkip': false,  // 47086: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + done > once([0,0,-2]) + loopSkip': false,  // 47112: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + done > once([0,0,2]) + loopSkip': false,  // 47138: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + done > current(null) + loopSkip': false,  // 47164: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + done > jumpWords(-3) + loopSkip': false,  // 47346: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpTo(100) + done > jumpWords(-1) + loopSkip': false,  // 47372: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + done > jumpWords(0) + loopSkip': false,  // 47398: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + done > jumpWords(4) + loopSkip': false,  // 47424: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + done > jumpSentences(-1) + loopSkip': false,  // 47528: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + done > jumpSentences(0) + loopSkip': false,  // 47554: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + done > jumpSentences(1) + loopSkip': false,  // 47580: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + done > jumpSentences(3) + loopSkip': false,  // 47606: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + done > nextWord(null) + loopSkip': false,  // 47658: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + done > nextSentence(null) + loopSkip': false,  // 47684: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + done > prevWord(null) + loopSkip': false,  // 47710: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + done > prevSentence(null) + loopSkip': false,  // 47736: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopSkip > play(null) + playBegin': false,  // 47815: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopSkip > play(null) + playFinish': false,  // 47816: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopSkip > play(null) + restartBegin': false,  // 47819: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > play(null) + restartFinish': false,  // 47820: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > toggle(null) + playBegin': false,  // 47841: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopSkip > toggle(null) + playFinish': false,  // 47842: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopSkip > toggle(null) + restartBegin': false,  // 47845: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > toggle(null) + restartFinish': false,  // 47846: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > rewind(null) + newWordFragment': false,  // 47991: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > rewind(null) + progress': false,  // 47992: 'progress' expected [0.08333333333333333], but got [0.9166666666666666,0.8333333333333334,0.75,0.6666666666666666,0.5833333333333334,0.5,0.4166666666666667,0.3333333333333333,0.25,0.16666666666666666,0.08333333333333333]
		'skip-combos: jumpTo(100) + loopSkip > rewind(null) + loopSkip': false,  // 47996: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpTo(100) + loopSkip > fastForward(null) + newWordFragment': false,  // 48017: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopSkip > fastForward(null) + progress': false,  // 48018: 'progress' expected [0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [1]
		'skip-combos: jumpTo(100) + loopSkip > fastForward(null) + loopSkip': false,  // 48022: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + loopSkip > once([0,0,-2]) + newWordFragment': false,  // 48043: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > once([0,0,-2]) + progress': false,  // 48044: 'progress' expected [0.08333333333333333], but got [0.9166666666666666]
		'skip-combos: jumpTo(100) + loopSkip > once([0,0,-2]) + stopBegin': false,  // 48045: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopSkip > once([0,0,-2]) + stopFinish': false,  // 48046: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopSkip > once([0,0,-2]) + done': false,  // 48047: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopSkip > once([0,0,-2]) + loopSkip': false,  // 48048: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopSkip > once([0,0,2]) + newWordFragment': false,  // 48069: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopSkip > once([0,0,2]) + progress': false,  // 48070: 'progress' expected [0.16666666666666666], but got [1]
		'skip-combos: jumpTo(100) + loopSkip > once([0,0,2]) + stopBegin': false,  // 48071: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > once([0,0,2]) + stopFinish': false,  // 48072: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > once([0,0,2]) + done': false,  // 48073: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > once([0,0,2]) + loopSkip': false,  // 48074: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > current(null) + newWordFragment': false,  // 48095: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopSkip > current(null) + progress': false,  // 48096: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [1]
		'skip-combos: jumpTo(100) + loopSkip > current(null) + stopBegin': false,  // 48097: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > current(null) + stopFinish': false,  // 48098: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > current(null) + done': false,  // 48099: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > current(null) + loopSkip': false,  // 48100: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + loopSkip > jumpWords(-3) + newWordFragment': false,  // 48277: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > jumpWords(-3) + progress': false,  // 48278: 'progress' expected [0.08333333333333333], but got [0.75,0.6666666666666666]
		'skip-combos: jumpTo(100) + loopSkip > jumpWords(-3) + stopBegin': false,  // 48279: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopSkip > jumpWords(-3) + stopFinish': false,  // 48280: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopSkip > jumpWords(-3) + done': false,  // 48281: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopSkip > jumpWords(-3) + loopSkip': false,  // 48282: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpTo(100) + loopSkip > jumpWords(-1) + newWordFragment': false,  // 48303: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > jumpWords(-1) + progress': false,  // 48304: 'progress' expected [0.08333333333333333], but got [0.9166666666666666]
		'skip-combos: jumpTo(100) + loopSkip > jumpWords(-1) + stopBegin': false,  // 48305: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopSkip > jumpWords(-1) + stopFinish': false,  // 48306: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopSkip > jumpWords(-1) + done': false,  // 48307: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopSkip > jumpWords(-1) + loopSkip': false,  // 48308: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopSkip > jumpWords(0) + newWordFragment': false,  // 48329: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopSkip > jumpWords(0) + progress': false,  // 48330: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [1]
		'skip-combos: jumpTo(100) + loopSkip > jumpWords(0) + stopBegin': false,  // 48331: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > jumpWords(0) + stopFinish': false,  // 48332: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > jumpWords(0) + done': false,  // 48333: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > jumpWords(0) + loopSkip': false,  // 48334: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + loopSkip > jumpWords(4) + newWordFragment': false,  // 48355: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopSkip > jumpWords(4) + progress': false,  // 48356: 'progress' expected [0.4166666666666667,0.5], but got [1]
		'skip-combos: jumpTo(100) + loopSkip > jumpWords(4) + stopBegin': false,  // 48357: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > jumpWords(4) + stopFinish': false,  // 48358: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > jumpWords(4) + done': false,  // 48359: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > jumpWords(4) + loopSkip': false,  // 48360: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + loopSkip > jumpSentences(-1) + newWordFragment': false,  // 48459: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > jumpSentences(-1) + progress': false,  // 48460: 'progress' expected [0.08333333333333333], but got [0.8333333333333334]
		'skip-combos: jumpTo(100) + loopSkip > jumpSentences(-1) + stopBegin': false,  // 48461: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopSkip > jumpSentences(-1) + stopFinish': false,  // 48462: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopSkip > jumpSentences(-1) + done': false,  // 48463: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopSkip > jumpSentences(-1) + loopSkip': false,  // 48464: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopSkip > jumpSentences(0) + newWordFragment': false,  // 48485: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopSkip > jumpSentences(0) + progress': false,  // 48486: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [1]
		'skip-combos: jumpTo(100) + loopSkip > jumpSentences(0) + stopBegin': false,  // 48487: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > jumpSentences(0) + stopFinish': false,  // 48488: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > jumpSentences(0) + done': false,  // 48489: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > jumpSentences(0) + loopSkip': false,  // 48490: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + loopSkip > jumpSentences(1) + newWordFragment': false,  // 48511: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopSkip > jumpSentences(1) + progress': false,  // 48512: 'progress' expected [0.4166666666666667,0.5], but got [1]
		'skip-combos: jumpTo(100) + loopSkip > jumpSentences(1) + stopBegin': false,  // 48513: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > jumpSentences(1) + stopFinish': false,  // 48514: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > jumpSentences(1) + done': false,  // 48515: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > jumpSentences(1) + loopSkip': false,  // 48516: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + loopSkip > jumpSentences(3) + newWordFragment': false,  // 48537: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopSkip > jumpSentences(3) + progress': false,  // 48538: 'progress' expected [0.8333333333333334], but got [1]
		'skip-combos: jumpTo(100) + loopSkip > jumpSentences(3) + stopBegin': false,  // 48539: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > jumpSentences(3) + stopFinish': false,  // 48540: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > jumpSentences(3) + done': false,  // 48541: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > jumpSentences(3) + loopSkip': false,  // 48542: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > nextWord(null) + newWordFragment': false,  // 48589: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopSkip > nextWord(null) + progress': false,  // 48590: 'progress' expected [0.16666666666666666], but got [1]
		'skip-combos: jumpTo(100) + loopSkip > nextWord(null) + stopBegin': false,  // 48591: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > nextWord(null) + stopFinish': false,  // 48592: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > nextWord(null) + done': false,  // 48593: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > nextWord(null) + loopSkip': false,  // 48594: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > nextSentence(null) + newWordFragment': false,  // 48615: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopSkip > nextSentence(null) + progress': false,  // 48616: 'progress' expected [0.4166666666666667,0.5], but got [1]
		'skip-combos: jumpTo(100) + loopSkip > nextSentence(null) + stopBegin': false,  // 48617: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > nextSentence(null) + stopFinish': false,  // 48618: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > nextSentence(null) + done': false,  // 48619: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > nextSentence(null) + loopSkip': false,  // 48620: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpTo(100) + loopSkip > prevWord(null) + newWordFragment': false,  // 48641: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > prevWord(null) + progress': false,  // 48642: 'progress' expected [0.08333333333333333], but got [0.9166666666666666]
		'skip-combos: jumpTo(100) + loopSkip > prevWord(null) + stopBegin': false,  // 48643: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopSkip > prevWord(null) + stopFinish': false,  // 48644: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopSkip > prevWord(null) + done': false,  // 48645: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopSkip > prevWord(null) + loopSkip': false,  // 48646: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopSkip > prevSentence(null) + newWordFragment': false,  // 48667: event should not have been triggerd but WAS
		'skip-combos: jumpTo(100) + loopSkip > prevSentence(null) + progress': false,  // 48668: 'progress' expected [0.08333333333333333], but got [0.8333333333333334]
		'skip-combos: jumpTo(100) + loopSkip > prevSentence(null) + stopBegin': false,  // 48669: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopSkip > prevSentence(null) + stopFinish': false,  // 48670: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopSkip > prevSentence(null) + done': false,  // 48671: event should have been triggerd but was NOT
		'skip-combos: jumpTo(100) + loopSkip > prevSentence(null) + loopSkip': false,  // 48672: event should have been triggerd but was NOT

		// jumpWords(-3)
		'skip-combos: jumpWords(-3) + loopSkip > play(null) + playBegin': false,  // 23479: event should have been triggerd but was NOT
		'skip-combos: jumpWords(-3) + loopSkip > play(null) + playFinish': false,  // 23480: event should have been triggerd but was NOT
		'skip-combos: jumpWords(-3) + loopSkip > play(null) + restartBegin': false,  // 23483: event should not have been triggerd but WAS
		'skip-combos: jumpWords(-3) + loopSkip > play(null) + restartFinish': false,  // 23484: event should not have been triggerd but WAS
		'skip-combos: jumpWords(-3) + loopSkip > toggle(null) + playBegin': false,  // 23505: event should have been triggerd but was NOT
		'skip-combos: jumpWords(-3) + loopSkip > toggle(null) + playFinish': false,  // 23506: event should have been triggerd but was NOT
		'skip-combos: jumpWords(-3) + loopSkip > toggle(null) + restartBegin': false,  // 23509: event should not have been triggerd but WAS
		'skip-combos: jumpWords(-3) + loopSkip > toggle(null) + restartFinish': false,  // 23510: event should not have been triggerd but WAS

		// jumpWords(-1)
		'skip-combos: jumpWords(-1) + loopSkip > play(null) + playBegin': false,  // 47815: event should have been triggerd but was NOT
		'skip-combos: jumpWords(-1) + loopSkip > play(null) + playFinish': false,  // 47816: event should have been triggerd but was NOT
		'skip-combos: jumpWords(-1) + loopSkip > play(null) + restartBegin': false,  // 47819: event should not have been triggerd but WAS
		'skip-combos: jumpWords(-1) + loopSkip > play(null) + restartFinish': false,  // 47820: event should not have been triggerd but WAS
		'skip-combos: jumpWords(-1) + loopSkip > toggle(null) + playBegin': false,  // 47841: event should have been triggerd but was NOT
		'skip-combos: jumpWords(-1) + loopSkip > toggle(null) + playFinish': false,  // 47842: event should have been triggerd but was NOT
		'skip-combos: jumpWords(-1) + loopSkip > toggle(null) + restartBegin': false,  // 47845: event should not have been triggerd but WAS
		'skip-combos: jumpWords(-1) + loopSkip > toggle(null) + restartFinish': false,  // 47846: event should not have been triggerd but WAS

		// jumpWords(0)
		'skip-combos: jumpWords(0) + onceBegin > toggle(null) + loopSkip': false,  // 9490: event should have been triggerd but was NOT
		'skip-combos: jumpWords(0) + onceFinish > toggle(null) + loopSkip': false,  // 10426: event should have been triggerd but was NOT
		'skip-combos: jumpWords(0) + loopBegin > toggle(null) + loopSkip': false,  // 16978: event should have been triggerd but was NOT
		'skip-combos: jumpWords(0) + loopFinish > toggle(null) + loopSkip': false,  // 17914: event should have been triggerd but was NOT
		'skip-combos: jumpWords(0) + newWordFragment > play(null) + progress': false,  // 18820: 'progress' expected [0.08333333333333333,0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1]
		'skip-combos: jumpWords(0) + newWordFragment > play(null) + loopSkip': false,  // 18824: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?"]
		'skip-combos: jumpWords(0) + newWordFragment > toggle(null) + loopSkip': false,  // 18850: event should have been triggerd but was NOT
		'skip-combos: jumpWords(0) + newWordFragment > fastForward(null) + newWordFragment': false,  // 19001: frags expected ["you","brave","I","back.","Why,","oh"], but got ["brave","I","back.","Why,","oh"]
		'skip-combos: jumpWords(0) + newWordFragment > fastForward(null) + progress': false,  // 19002: 'progress' expected [0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1]
		'skip-combos: jumpWords(0) + newWordFragment > once([0,0,2]) + newWordFragment': false,  // 19053: frags expected ["you"], but got ["brave"]
		'skip-combos: jumpWords(0) + newWordFragment > once([0,0,2]) + progress': false,  // 19054: 'progress' expected [0.16666666666666666], but got [0.25]
		'skip-combos: jumpWords(0) + newWordFragment > current(null) + progress': false,  // 19080: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.16666666666666666]
		'skip-combos: jumpWords(0) + newWordFragment > current(null) + loopSkip': false,  // 19084: event should have been triggerd but was NOT
		'skip-combos: jumpWords(0) + newWordFragment > jumpWords(0) + progress': false,  // 19314: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.16666666666666666]
		'skip-combos: jumpWords(0) + newWordFragment > jumpWords(0) + loopSkip': false,  // 19318: event should have been triggerd but was NOT
		'skip-combos: jumpWords(0) + newWordFragment > jumpWords(4) + progress': false,  // 19340: 'progress' expected [0.4166666666666667,0.5], but got [0.5]
		'skip-combos: jumpWords(0) + newWordFragment > jumpWords(4) + loopSkip': false,  // 19344: event should have been triggerd but was NOT
		'skip-combos: jumpWords(0) + newWordFragment > jumpSentences(0) + progress': false,  // 19470: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.16666666666666666]
		'skip-combos: jumpWords(0) + newWordFragment > jumpSentences(0) + loopSkip': false,  // 19474: event should have been triggerd but was NOT
		'skip-combos: jumpWords(0) + newWordFragment > nextWord(null) + newWordFragment': false,  // 19573: frags expected ["you"], but got ["brave"]
		'skip-combos: jumpWords(0) + newWordFragment > nextWord(null) + progress': false,  // 19574: 'progress' expected [0.16666666666666666], but got [0.25]
		'skip-combos: jumpWords(0) + progress > toggle(null) + loopSkip': false,  // 19786: event should have been triggerd but was NOT
		'skip-combos: jumpWords(0) + loopSkip > toggle(null) + playBegin': false,  // 23505: event should have been triggerd but was NOT
		'skip-combos: jumpWords(0) + loopSkip > toggle(null) + playFinish': false,  // 23506: event should have been triggerd but was NOT
		'skip-combos: jumpWords(0) + loopSkip > toggle(null) + pauseBegin': false,  // 23511: event should not have been triggerd but WAS
		'skip-combos: jumpWords(0) + loopSkip > toggle(null) + pauseFinish': false,  // 23512: event should not have been triggerd but WAS
		'skip-combos: jumpWords(0) + loopSkip > toggle(null) + revertBegin': false,  // 23517: event should not have been triggerd but WAS
		'skip-combos: jumpWords(0) + loopSkip > toggle(null) + revertFinish': false,  // 23518: event should not have been triggerd but WAS
		'skip-combos: jumpWords(0) + loopSkip > toggle(null) + loopBegin': false,  // 23523: event should have been triggerd but was NOT
		'skip-combos: jumpWords(0) + loopSkip > toggle(null) + loopFinish': false,  // 23524: event should have been triggerd but was NOT
		'skip-combos: jumpWords(0) + loopSkip > toggle(null) + newWordFragment': false,  // 23525: event should have been triggerd but was NOT
		'skip-combos: jumpWords(0) + loopSkip > toggle(null) + progress': false,  // 23526: event should have been triggerd but was NOT
		'skip-combos: jumpWords(0) + loopSkip > toggle(null) + stopBegin': false,  // 23527: event should have been triggerd but was NOT
		'skip-combos: jumpWords(0) + loopSkip > toggle(null) + stopFinish': false,  // 23528: event should have been triggerd but was NOT
		'skip-combos: jumpWords(0) + loopSkip > toggle(null) + done': false,  // 23529: event should have been triggerd but was NOT
		'skip-combos: jumpWords(0) + loopSkip > toggle(null) + loopSkip': false,  // 23530: event should have been triggerd but was NOT

		// jumpWords(4)
		'skip-combos: jumpWords(4) + onceBegin > play(null) + loopSkip': false,  // 33800: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["Delirious,","come","\n","wattlebird?"]
		'skip-combos: jumpWords(4) + onceBegin > toggle(null) + loopSkip': false,  // 33826: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + onceBegin > rewind(null) + loopSkip': false,  // 33956: frags expected ["Victorious,"], but got ["flag.","Victorious,"]
		'skip-combos: jumpWords(4) + onceBegin > fastForward(null) + loopSkip': false,  // 33982: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["come","\n","wattlebird?"]
		'skip-combos: jumpWords(4) + onceBegin > once([0,0,-2]) + loopSkip': false,  // 34008: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpWords(4) + onceBegin > current(null) + loopSkip': false,  // 34060: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpWords(4) + onceBegin > jumpWords(-3) + loopSkip': false,  // 34242: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + onceBegin > jumpWords(-1) + loopSkip': false,  // 34268: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpWords(4) + onceBegin > jumpWords(0) + loopSkip': false,  // 34294: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpWords(4) + onceBegin > jumpWords(4) + loopSkip': false,  // 34320: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpWords(4) + onceBegin > jumpSentences(0) + loopSkip': false,  // 34450: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpWords(4) + onceBegin > jumpSentences(1) + loopSkip': false,  // 34476: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpWords(4) + onceBegin > jumpSentences(3) + loopSkip': false,  // 34502: event should not have been triggerd but WAS
		'skip-combos: jumpWords(4) + onceBegin > nextSentence(null) + loopSkip': false,  // 34580: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpWords(4) + onceBegin > prevWord(null) + loopSkip': false,  // 34606: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpWords(4) + onceFinish > play(null) + loopSkip': false,  // 34736: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["Delirious,","come","\n","wattlebird?"]
		'skip-combos: jumpWords(4) + onceFinish > toggle(null) + loopSkip': false,  // 34762: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + onceFinish > rewind(null) + loopSkip': false,  // 34892: frags expected ["Victorious,"], but got ["flag.","Victorious,"]
		'skip-combos: jumpWords(4) + onceFinish > fastForward(null) + loopSkip': false,  // 34918: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["come","\n","wattlebird?"]
		'skip-combos: jumpWords(4) + onceFinish > once([0,0,-2]) + loopSkip': false,  // 34944: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpWords(4) + onceFinish > current(null) + loopSkip': false,  // 34996: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpWords(4) + onceFinish > jumpWords(-3) + loopSkip': false,  // 35178: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + onceFinish > jumpWords(-1) + loopSkip': false,  // 35204: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpWords(4) + onceFinish > jumpWords(0) + loopSkip': false,  // 35230: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpWords(4) + onceFinish > jumpWords(4) + loopSkip': false,  // 35256: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpWords(4) + onceFinish > jumpSentences(0) + loopSkip': false,  // 35386: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpWords(4) + onceFinish > jumpSentences(1) + loopSkip': false,  // 35412: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpWords(4) + onceFinish > jumpSentences(3) + loopSkip': false,  // 35438: event should not have been triggerd but WAS
		'skip-combos: jumpWords(4) + onceFinish > nextSentence(null) + loopSkip': false,  // 35516: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpWords(4) + onceFinish > prevWord(null) + loopSkip': false,  // 35542: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpWords(4) + loopBegin > play(null) + loopSkip': false,  // 41288: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["Delirious,","come","\n","wattlebird?"]
		'skip-combos: jumpWords(4) + loopBegin > toggle(null) + loopSkip': false,  // 41314: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + loopBegin > rewind(null) + loopSkip': false,  // 41444: frags expected ["Victorious,"], but got ["flag.","Victorious,"]
		'skip-combos: jumpWords(4) + loopBegin > fastForward(null) + loopSkip': false,  // 41470: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["come","\n","wattlebird?"]
		'skip-combos: jumpWords(4) + loopBegin > once([0,0,-2]) + loopSkip': false,  // 41496: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpWords(4) + loopBegin > current(null) + loopSkip': false,  // 41548: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpWords(4) + loopBegin > jumpWords(-3) + loopSkip': false,  // 41730: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + loopBegin > jumpWords(-1) + loopSkip': false,  // 41756: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpWords(4) + loopBegin > jumpWords(0) + loopSkip': false,  // 41782: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpWords(4) + loopBegin > jumpWords(4) + loopSkip': false,  // 41808: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpWords(4) + loopBegin > jumpSentences(0) + loopSkip': false,  // 41938: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpWords(4) + loopBegin > jumpSentences(1) + loopSkip': false,  // 41964: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpWords(4) + loopBegin > jumpSentences(3) + loopSkip': false,  // 41990: event should not have been triggerd but WAS
		'skip-combos: jumpWords(4) + loopBegin > nextSentence(null) + loopSkip': false,  // 42068: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpWords(4) + loopBegin > prevWord(null) + loopSkip': false,  // 42094: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpWords(4) + loopFinish > play(null) + loopSkip': false,  // 42224: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["Delirious,","come","\n","wattlebird?"]
		'skip-combos: jumpWords(4) + loopFinish > toggle(null) + loopSkip': false,  // 42250: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + loopFinish > rewind(null) + loopSkip': false,  // 42380: frags expected ["Victorious,"], but got ["flag.","Victorious,"]
		'skip-combos: jumpWords(4) + loopFinish > fastForward(null) + loopSkip': false,  // 42406: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["come","\n","wattlebird?"]
		'skip-combos: jumpWords(4) + loopFinish > once([0,0,-2]) + loopSkip': false,  // 42432: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpWords(4) + loopFinish > current(null) + loopSkip': false,  // 42484: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpWords(4) + loopFinish > jumpWords(-3) + loopSkip': false,  // 42666: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + loopFinish > jumpWords(-1) + loopSkip': false,  // 42692: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpWords(4) + loopFinish > jumpWords(0) + loopSkip': false,  // 42718: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpWords(4) + loopFinish > jumpWords(4) + loopSkip': false,  // 42744: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpWords(4) + loopFinish > jumpSentences(0) + loopSkip': false,  // 42874: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpWords(4) + loopFinish > jumpSentences(1) + loopSkip': false,  // 42900: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpWords(4) + loopFinish > jumpSentences(3) + loopSkip': false,  // 42926: event should not have been triggerd but WAS
		'skip-combos: jumpWords(4) + loopFinish > nextSentence(null) + loopSkip': false,  // 43004: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpWords(4) + loopFinish > prevWord(null) + loopSkip': false,  // 43030: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpWords(4) + newWordFragment > play(null) + loopSkip': false,  // 43160: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["come","\n","wattlebird?"]
		'skip-combos: jumpWords(4) + newWordFragment > toggle(null) + loopSkip': false,  // 43186: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + newWordFragment > rewind(null) + loopSkip': false,  // 43316: frags expected ["Victorious,"], but got ["Delirious,","flag.","Victorious,"]
		'skip-combos: jumpWords(4) + newWordFragment > fastForward(null) + loopSkip': false,  // 43342: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["come","\n","wattlebird?"]
		'skip-combos: jumpWords(4) + newWordFragment > once([0,0,-2]) + loopSkip': false,  // 43368: frags expected ["Victorious,"], but got ["Delirious,","flag."]
		'skip-combos: jumpWords(4) + newWordFragment > once([0,0,2]) + loopSkip': false,  // 43394: event should not have been triggerd but WAS
		'skip-combos: jumpWords(4) + newWordFragment > current(null) + loopSkip': false,  // 43420: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + newWordFragment > jumpWords(-3) + loopSkip': false,  // 43602: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + newWordFragment > jumpWords(-1) + loopSkip': false,  // 43628: frags expected ["Victorious,"], but got ["Delirious,","flag."]
		'skip-combos: jumpWords(4) + newWordFragment > jumpWords(0) + loopSkip': false,  // 43654: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + newWordFragment > jumpWords(4) + loopSkip': false,  // 43680: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + newWordFragment > jumpSentences(-1) + newWordFragment': false,  // 43779: event should not have been triggerd but WAS
		'skip-combos: jumpWords(4) + newWordFragment > jumpSentences(-1) + progress': false,  // 43780: 'progress' expected [0.08333333333333333], but got [0.4166666666666667,0.3333333333333333,0.25]
		'skip-combos: jumpWords(4) + newWordFragment > jumpSentences(-1) + stopBegin': false,  // 43781: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + newWordFragment > jumpSentences(-1) + stopFinish': false,  // 43782: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + newWordFragment > jumpSentences(-1) + done': false,  // 43783: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + newWordFragment > jumpSentences(-1) + loopSkip': false,  // 43784: frags expected ["Victorious,"], but got ["Delirious,","flag."]
		'skip-combos: jumpWords(4) + newWordFragment > jumpSentences(0) + loopSkip': false,  // 43810: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + newWordFragment > jumpSentences(1) + loopSkip': false,  // 43836: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpWords(4) + newWordFragment > jumpSentences(3) + loopSkip': false,  // 43862: event should not have been triggerd but WAS
		'skip-combos: jumpWords(4) + newWordFragment > nextWord(null) + loopSkip': false,  // 43914: event should not have been triggerd but WAS
		'skip-combos: jumpWords(4) + newWordFragment > nextSentence(null) + loopSkip': false,  // 43940: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpWords(4) + newWordFragment > prevWord(null) + loopSkip': false,  // 43966: frags expected ["Victorious,"], but got ["Delirious,","flag."]
		'skip-combos: jumpWords(4) + newWordFragment > prevSentence(null) + newWordFragment': false,  // 43987: event should not have been triggerd but WAS
		'skip-combos: jumpWords(4) + newWordFragment > prevSentence(null) + progress': false,  // 43988: 'progress' expected [0.08333333333333333], but got [0.4166666666666667,0.3333333333333333,0.25]
		'skip-combos: jumpWords(4) + newWordFragment > prevSentence(null) + stopBegin': false,  // 43989: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + newWordFragment > prevSentence(null) + stopFinish': false,  // 43990: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + newWordFragment > prevSentence(null) + done': false,  // 43991: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + newWordFragment > prevSentence(null) + loopSkip': false,  // 43992: frags expected ["Victorious,"], but got ["Delirious,","flag."]
		'skip-combos: jumpWords(4) + progress > play(null) + loopSkip': false,  // 44096: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["Delirious,","come","\n","wattlebird?"]
		'skip-combos: jumpWords(4) + progress > toggle(null) + loopSkip': false,  // 44122: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + progress > rewind(null) + loopSkip': false,  // 44252: frags expected ["Victorious,"], but got ["flag.","Victorious,"]
		'skip-combos: jumpWords(4) + progress > fastForward(null) + loopSkip': false,  // 44278: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["come","\n","wattlebird?"]
		'skip-combos: jumpWords(4) + progress > once([0,0,-2]) + loopSkip': false,  // 44304: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpWords(4) + progress > current(null) + loopSkip': false,  // 44356: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpWords(4) + progress > jumpWords(-3) + loopSkip': false,  // 44538: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + progress > jumpWords(-1) + loopSkip': false,  // 44564: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpWords(4) + progress > jumpWords(0) + loopSkip': false,  // 44590: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpWords(4) + progress > jumpWords(4) + loopSkip': false,  // 44616: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpWords(4) + progress > jumpSentences(0) + loopSkip': false,  // 44746: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpWords(4) + progress > jumpSentences(1) + loopSkip': false,  // 44772: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpWords(4) + progress > jumpSentences(3) + loopSkip': false,  // 44798: event should not have been triggerd but WAS
		'skip-combos: jumpWords(4) + progress > nextSentence(null) + loopSkip': false,  // 44876: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpWords(4) + progress > prevWord(null) + loopSkip': false,  // 44902: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpWords(4) + loopSkip > play(null) + newWordFragment': false,  // 47835: frags expected ["you","brave","I","back.","Why,","oh"], but got ["I","back.","Why,","oh"]
		'skip-combos: jumpWords(4) + loopSkip > play(null) + progress': false,  // 47836: 'progress' expected [0.08333333333333333,0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1]
		'skip-combos: jumpWords(4) + loopSkip > play(null) + loopSkip': false,  // 47840: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["Delirious,","come","\n","wattlebird?"]
		'skip-combos: jumpWords(4) + loopSkip > toggle(null) + playBegin': false,  // 47841: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + loopSkip > toggle(null) + playFinish': false,  // 47842: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + loopSkip > toggle(null) + pauseBegin': false,  // 47847: event should not have been triggerd but WAS
		'skip-combos: jumpWords(4) + loopSkip > toggle(null) + pauseFinish': false,  // 47848: event should not have been triggerd but WAS
		'skip-combos: jumpWords(4) + loopSkip > toggle(null) + revertBegin': false,  // 47853: event should not have been triggerd but WAS
		'skip-combos: jumpWords(4) + loopSkip > toggle(null) + revertFinish': false,  // 47854: event should not have been triggerd but WAS
		'skip-combos: jumpWords(4) + loopSkip > toggle(null) + loopBegin': false,  // 47859: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + loopSkip > toggle(null) + loopFinish': false,  // 47860: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + loopSkip > toggle(null) + newWordFragment': false,  // 47861: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + loopSkip > toggle(null) + progress': false,  // 47862: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + loopSkip > toggle(null) + stopBegin': false,  // 47863: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + loopSkip > toggle(null) + stopFinish': false,  // 47864: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + loopSkip > toggle(null) + done': false,  // 47865: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + loopSkip > toggle(null) + loopSkip': false,  // 47866: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + loopSkip > rewind(null) + newWordFragment': false,  // 47991: event should not have been triggerd but WAS
		'skip-combos: jumpWords(4) + loopSkip > rewind(null) + progress': false,  // 47992: 'progress' expected [0.08333333333333333], but got [0.3333333333333333,0.25,0.16666666666666666,0.08333333333333333]
		'skip-combos: jumpWords(4) + loopSkip > rewind(null) + loopSkip': false,  // 47996: frags expected ["Victorious,"], but got ["flag.","Victorious,"]
		'skip-combos: jumpWords(4) + loopSkip > fastForward(null) + newWordFragment': false,  // 48017: frags expected ["you","brave","I","back.","Why,","oh"], but got ["I","back.","Why,","oh"]
		'skip-combos: jumpWords(4) + loopSkip > fastForward(null) + progress': false,  // 48018: 'progress' expected [0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1]
		'skip-combos: jumpWords(4) + loopSkip > fastForward(null) + loopSkip': false,  // 48022: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["come","\n","wattlebird?"]
		'skip-combos: jumpWords(4) + loopSkip > once([0,0,-2]) + newWordFragment': false,  // 48043: event should not have been triggerd but WAS
		'skip-combos: jumpWords(4) + loopSkip > once([0,0,-2]) + progress': false,  // 48044: 'progress' expected [0.08333333333333333], but got [0.3333333333333333,0.25]
		'skip-combos: jumpWords(4) + loopSkip > once([0,0,-2]) + stopBegin': false,  // 48045: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + loopSkip > once([0,0,-2]) + stopFinish': false,  // 48046: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + loopSkip > once([0,0,-2]) + done': false,  // 48047: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + loopSkip > once([0,0,-2]) + loopSkip': false,  // 48048: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpWords(4) + loopSkip > once([0,0,2]) + newWordFragment': false,  // 48069: frags expected ["you"], but got ["I"]
		'skip-combos: jumpWords(4) + loopSkip > once([0,0,2]) + progress': false,  // 48070: 'progress' expected [0.16666666666666666], but got [0.5]
		'skip-combos: jumpWords(4) + loopSkip > current(null) + newWordFragment': false,  // 48095: frags expected ["you"], but got ["I"]
		'skip-combos: jumpWords(4) + loopSkip > current(null) + progress': false,  // 48096: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.4166666666666667,0.5]
		'skip-combos: jumpWords(4) + loopSkip > current(null) + loopSkip': false,  // 48100: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpWords(4) + loopSkip > jumpWords(-3) + newWordFragment': false,  // 48277: event should not have been triggerd but WAS
		'skip-combos: jumpWords(4) + loopSkip > jumpWords(-3) + progress': false,  // 48278: 'progress' expected [0.08333333333333333], but got [0.16666666666666666]
		'skip-combos: jumpWords(4) + loopSkip > jumpWords(-3) + stopBegin': false,  // 48279: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + loopSkip > jumpWords(-3) + stopFinish': false,  // 48280: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + loopSkip > jumpWords(-3) + done': false,  // 48281: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + loopSkip > jumpWords(-3) + loopSkip': false,  // 48282: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + loopSkip > jumpWords(-1) + newWordFragment': false,  // 48303: event should not have been triggerd but WAS
		'skip-combos: jumpWords(4) + loopSkip > jumpWords(-1) + progress': false,  // 48304: 'progress' expected [0.08333333333333333], but got [0.3333333333333333,0.25]
		'skip-combos: jumpWords(4) + loopSkip > jumpWords(-1) + stopBegin': false,  // 48305: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + loopSkip > jumpWords(-1) + stopFinish': false,  // 48306: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + loopSkip > jumpWords(-1) + done': false,  // 48307: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + loopSkip > jumpWords(-1) + loopSkip': false,  // 48308: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpWords(4) + loopSkip > jumpWords(0) + newWordFragment': false,  // 48329: frags expected ["you"], but got ["I"]
		'skip-combos: jumpWords(4) + loopSkip > jumpWords(0) + progress': false,  // 48330: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.4166666666666667,0.5]
		'skip-combos: jumpWords(4) + loopSkip > jumpWords(0) + loopSkip': false,  // 48334: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpWords(4) + loopSkip > jumpWords(4) + newWordFragment': false,  // 48355: frags expected ["I"], but got ["Why,"]
		'skip-combos: jumpWords(4) + loopSkip > jumpWords(4) + progress': false,  // 48356: 'progress' expected [0.4166666666666667,0.5], but got [0.75,0.8333333333333334]
		'skip-combos: jumpWords(4) + loopSkip > jumpWords(4) + loopSkip': false,  // 48360: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpWords(4) + loopSkip > jumpSentences(0) + newWordFragment': false,  // 48485: frags expected ["you"], but got ["I"]
		'skip-combos: jumpWords(4) + loopSkip > jumpSentences(0) + progress': false,  // 48486: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.4166666666666667,0.5]
		'skip-combos: jumpWords(4) + loopSkip > jumpSentences(0) + loopSkip': false,  // 48490: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpWords(4) + loopSkip > jumpSentences(1) + newWordFragment': false,  // 48511: frags expected ["I"], but got ["Why,"]
		'skip-combos: jumpWords(4) + loopSkip > jumpSentences(1) + progress': false,  // 48512: 'progress' expected [0.4166666666666667,0.5], but got [0.75,0.8333333333333334]
		'skip-combos: jumpWords(4) + loopSkip > jumpSentences(1) + loopSkip': false,  // 48516: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpWords(4) + loopSkip > jumpSentences(3) + newWordFragment': false,  // 48537: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + loopSkip > jumpSentences(3) + progress': false,  // 48538: 'progress' expected [0.8333333333333334], but got [1]
		'skip-combos: jumpWords(4) + loopSkip > jumpSentences(3) + stopBegin': false,  // 48539: event should not have been triggerd but WAS
		'skip-combos: jumpWords(4) + loopSkip > jumpSentences(3) + stopFinish': false,  // 48540: event should not have been triggerd but WAS
		'skip-combos: jumpWords(4) + loopSkip > jumpSentences(3) + done': false,  // 48541: event should not have been triggerd but WAS
		'skip-combos: jumpWords(4) + loopSkip > jumpSentences(3) + loopSkip': false,  // 48542: event should not have been triggerd but WAS
		'skip-combos: jumpWords(4) + loopSkip > nextWord(null) + newWordFragment': false,  // 48589: frags expected ["you"], but got ["I"]
		'skip-combos: jumpWords(4) + loopSkip > nextWord(null) + progress': false,  // 48590: 'progress' expected [0.16666666666666666], but got [0.5]
		'skip-combos: jumpWords(4) + loopSkip > nextSentence(null) + newWordFragment': false,  // 48615: frags expected ["I"], but got ["Why,"]
		'skip-combos: jumpWords(4) + loopSkip > nextSentence(null) + progress': false,  // 48616: 'progress' expected [0.4166666666666667,0.5], but got [0.75,0.8333333333333334]
		'skip-combos: jumpWords(4) + loopSkip > nextSentence(null) + loopSkip': false,  // 48620: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpWords(4) + loopSkip > prevWord(null) + newWordFragment': false,  // 48641: event should not have been triggerd but WAS
		'skip-combos: jumpWords(4) + loopSkip > prevWord(null) + progress': false,  // 48642: 'progress' expected [0.08333333333333333], but got [0.3333333333333333,0.25]
		'skip-combos: jumpWords(4) + loopSkip > prevWord(null) + stopBegin': false,  // 48643: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + loopSkip > prevWord(null) + stopFinish': false,  // 48644: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + loopSkip > prevWord(null) + done': false,  // 48645: event should have been triggerd but was NOT
		'skip-combos: jumpWords(4) + loopSkip > prevWord(null) + loopSkip': false,  // 48646: frags expected ["Victorious,"], but got ["flag."]

		// jumpWords(11)
		'skip-combos: jumpWords(11) + onceBegin > rewind(null) + loopSkip': false,  // 9620: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpWords(11) + onceBegin > fastForward(null) + loopSkip': false,  // 9646: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + onceBegin > once([0,0,-2]) + loopSkip': false,  // 9672: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + onceBegin > once([0,0,2]) + loopSkip': false,  // 9698: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + onceBegin > current(null) + loopSkip': false,  // 9724: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + onceBegin > jumpWords(-3) + loopSkip': false,  // 9906: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpWords(11) + onceBegin > jumpWords(-1) + loopSkip': false,  // 9932: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + onceBegin > jumpWords(0) + loopSkip': false,  // 9958: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + onceBegin > jumpWords(4) + loopSkip': false,  // 9984: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + onceBegin > jumpSentences(-1) + loopSkip': false,  // 10088: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + onceBegin > jumpSentences(0) + loopSkip': false,  // 10114: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + onceBegin > jumpSentences(1) + loopSkip': false,  // 10140: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + onceBegin > jumpSentences(3) + loopSkip': false,  // 10166: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + onceBegin > nextWord(null) + loopSkip': false,  // 10218: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + onceBegin > nextSentence(null) + loopSkip': false,  // 10244: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + onceBegin > prevWord(null) + loopSkip': false,  // 10270: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + onceBegin > prevSentence(null) + loopSkip': false,  // 10296: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + onceFinish > rewind(null) + loopSkip': false,  // 10556: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpWords(11) + onceFinish > fastForward(null) + loopSkip': false,  // 10582: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + onceFinish > once([0,0,-2]) + loopSkip': false,  // 10608: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + onceFinish > once([0,0,2]) + loopSkip': false,  // 10634: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + onceFinish > current(null) + loopSkip': false,  // 10660: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + onceFinish > jumpWords(-3) + loopSkip': false,  // 10842: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpWords(11) + onceFinish > jumpWords(-1) + loopSkip': false,  // 10868: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + onceFinish > jumpWords(0) + loopSkip': false,  // 10894: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + onceFinish > jumpWords(4) + loopSkip': false,  // 10920: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + onceFinish > jumpSentences(-1) + loopSkip': false,  // 11024: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + onceFinish > jumpSentences(0) + loopSkip': false,  // 11050: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + onceFinish > jumpSentences(1) + loopSkip': false,  // 11076: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + onceFinish > jumpSentences(3) + loopSkip': false,  // 11102: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + onceFinish > nextWord(null) + loopSkip': false,  // 11154: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + onceFinish > nextSentence(null) + loopSkip': false,  // 11180: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + onceFinish > prevWord(null) + loopSkip': false,  // 11206: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + onceFinish > prevSentence(null) + loopSkip': false,  // 11232: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopBegin > rewind(null) + loopSkip': false,  // 17108: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpWords(11) + loopBegin > fastForward(null) + loopSkip': false,  // 17134: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + loopBegin > once([0,0,-2]) + loopSkip': false,  // 17160: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopBegin > once([0,0,2]) + loopSkip': false,  // 17186: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopBegin > current(null) + loopSkip': false,  // 17212: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + loopBegin > jumpWords(-3) + loopSkip': false,  // 17394: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpWords(11) + loopBegin > jumpWords(-1) + loopSkip': false,  // 17420: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopBegin > jumpWords(0) + loopSkip': false,  // 17446: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + loopBegin > jumpWords(4) + loopSkip': false,  // 17472: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + loopBegin > jumpSentences(-1) + loopSkip': false,  // 17576: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopBegin > jumpSentences(0) + loopSkip': false,  // 17602: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + loopBegin > jumpSentences(1) + loopSkip': false,  // 17628: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + loopBegin > jumpSentences(3) + loopSkip': false,  // 17654: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopBegin > nextWord(null) + loopSkip': false,  // 17706: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopBegin > nextSentence(null) + loopSkip': false,  // 17732: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + loopBegin > prevWord(null) + loopSkip': false,  // 17758: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopBegin > prevSentence(null) + loopSkip': false,  // 17784: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopFinish > rewind(null) + loopSkip': false,  // 18044: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpWords(11) + loopFinish > fastForward(null) + loopSkip': false,  // 18070: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + loopFinish > once([0,0,-2]) + loopSkip': false,  // 18096: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopFinish > once([0,0,2]) + loopSkip': false,  // 18122: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopFinish > current(null) + loopSkip': false,  // 18148: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + loopFinish > jumpWords(-3) + loopSkip': false,  // 18330: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpWords(11) + loopFinish > jumpWords(-1) + loopSkip': false,  // 18356: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopFinish > jumpWords(0) + loopSkip': false,  // 18382: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + loopFinish > jumpWords(4) + loopSkip': false,  // 18408: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + loopFinish > jumpSentences(-1) + loopSkip': false,  // 18512: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopFinish > jumpSentences(0) + loopSkip': false,  // 18538: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + loopFinish > jumpSentences(1) + loopSkip': false,  // 18564: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + loopFinish > jumpSentences(3) + loopSkip': false,  // 18590: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopFinish > nextWord(null) + loopSkip': false,  // 18642: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopFinish > nextSentence(null) + loopSkip': false,  // 18668: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + loopFinish > prevWord(null) + loopSkip': false,  // 18694: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopFinish > prevSentence(null) + loopSkip': false,  // 18720: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + progress > rewind(null) + loopSkip': false,  // 19916: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpWords(11) + progress > fastForward(null) + loopSkip': false,  // 19942: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + progress > once([0,0,-2]) + loopSkip': false,  // 19968: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + progress > once([0,0,2]) + loopSkip': false,  // 19994: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + progress > current(null) + loopSkip': false,  // 20020: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + progress > jumpWords(-3) + loopSkip': false,  // 20202: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpWords(11) + progress > jumpWords(-1) + loopSkip': false,  // 20228: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + progress > jumpWords(0) + loopSkip': false,  // 20254: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + progress > jumpWords(4) + loopSkip': false,  // 20280: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + progress > jumpSentences(-1) + loopSkip': false,  // 20384: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + progress > jumpSentences(0) + loopSkip': false,  // 20410: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + progress > jumpSentences(1) + loopSkip': false,  // 20436: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + progress > jumpSentences(3) + loopSkip': false,  // 20462: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + progress > nextWord(null) + loopSkip': false,  // 20514: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + progress > nextSentence(null) + loopSkip': false,  // 20540: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + progress > prevWord(null) + loopSkip': false,  // 20566: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + progress > prevSentence(null) + loopSkip': false,  // 20592: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + stopBegin > rewind(null) + loopSkip': false,  // 20852: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpWords(11) + stopBegin > fastForward(null) + loopSkip': false,  // 20878: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + stopBegin > once([0,0,-2]) + loopSkip': false,  // 20904: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + stopBegin > once([0,0,2]) + loopSkip': false,  // 20930: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + stopBegin > current(null) + loopSkip': false,  // 20956: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + stopBegin > jumpWords(-3) + loopSkip': false,  // 21138: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpWords(11) + stopBegin > jumpWords(-1) + loopSkip': false,  // 21164: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + stopBegin > jumpWords(0) + loopSkip': false,  // 21190: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + stopBegin > jumpWords(4) + loopSkip': false,  // 21216: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + stopBegin > jumpSentences(-1) + loopSkip': false,  // 21320: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + stopBegin > jumpSentences(0) + loopSkip': false,  // 21346: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + stopBegin > jumpSentences(1) + loopSkip': false,  // 21372: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + stopBegin > jumpSentences(3) + loopSkip': false,  // 21398: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + stopBegin > nextWord(null) + loopSkip': false,  // 21450: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + stopBegin > nextSentence(null) + loopSkip': false,  // 21476: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + stopBegin > prevWord(null) + loopSkip': false,  // 21502: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + stopBegin > prevSentence(null) + loopSkip': false,  // 21528: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + stopFinish > rewind(null) + loopSkip': false,  // 21788: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpWords(11) + stopFinish > fastForward(null) + loopSkip': false,  // 21814: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + stopFinish > once([0,0,-2]) + loopSkip': false,  // 21840: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + stopFinish > once([0,0,2]) + loopSkip': false,  // 21866: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + stopFinish > current(null) + loopSkip': false,  // 21892: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + stopFinish > jumpWords(-3) + loopSkip': false,  // 22074: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpWords(11) + stopFinish > jumpWords(-1) + loopSkip': false,  // 22100: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + stopFinish > jumpWords(0) + loopSkip': false,  // 22126: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + stopFinish > jumpWords(4) + loopSkip': false,  // 22152: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + stopFinish > jumpSentences(-1) + loopSkip': false,  // 22256: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + stopFinish > jumpSentences(0) + loopSkip': false,  // 22282: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + stopFinish > jumpSentences(1) + loopSkip': false,  // 22308: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + stopFinish > jumpSentences(3) + loopSkip': false,  // 22334: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + stopFinish > nextWord(null) + loopSkip': false,  // 22386: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + stopFinish > nextSentence(null) + loopSkip': false,  // 22412: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + stopFinish > prevWord(null) + loopSkip': false,  // 22438: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + stopFinish > prevSentence(null) + loopSkip': false,  // 22464: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + done > rewind(null) + loopSkip': false,  // 22724: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpWords(11) + done > fastForward(null) + loopSkip': false,  // 22750: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + done > once([0,0,-2]) + loopSkip': false,  // 22776: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + done > once([0,0,2]) + loopSkip': false,  // 22802: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + done > current(null) + loopSkip': false,  // 22828: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + done > jumpWords(-3) + loopSkip': false,  // 23010: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpWords(11) + done > jumpWords(-1) + loopSkip': false,  // 23036: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + done > jumpWords(0) + loopSkip': false,  // 23062: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + done > jumpWords(4) + loopSkip': false,  // 23088: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + done > jumpSentences(-1) + loopSkip': false,  // 23192: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + done > jumpSentences(0) + loopSkip': false,  // 23218: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + done > jumpSentences(1) + loopSkip': false,  // 23244: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + done > jumpSentences(3) + loopSkip': false,  // 23270: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + done > nextWord(null) + loopSkip': false,  // 23322: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + done > nextSentence(null) + loopSkip': false,  // 23348: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + done > prevWord(null) + loopSkip': false,  // 23374: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + done > prevSentence(null) + loopSkip': false,  // 23400: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopSkip > play(null) + playBegin': false,  // 23479: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopSkip > play(null) + playFinish': false,  // 23480: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopSkip > play(null) + restartBegin': false,  // 23483: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > play(null) + restartFinish': false,  // 23484: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > toggle(null) + playBegin': false,  // 23505: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopSkip > toggle(null) + playFinish': false,  // 23506: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopSkip > toggle(null) + restartBegin': false,  // 23509: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > toggle(null) + restartFinish': false,  // 23510: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > rewind(null) + newWordFragment': false,  // 23655: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > rewind(null) + progress': false,  // 23656: 'progress' expected [0.08333333333333333], but got [0.9166666666666666,0.8333333333333334,0.75,0.6666666666666666,0.5833333333333334,0.5,0.4166666666666667,0.3333333333333333,0.25,0.16666666666666666,0.08333333333333333]
		'skip-combos: jumpWords(11) + loopSkip > rewind(null) + loopSkip': false,  // 23660: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpWords(11) + loopSkip > fastForward(null) + newWordFragment': false,  // 23681: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopSkip > fastForward(null) + progress': false,  // 23682: 'progress' expected [0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [1]
		'skip-combos: jumpWords(11) + loopSkip > fastForward(null) + loopSkip': false,  // 23686: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + loopSkip > once([0,0,-2]) + newWordFragment': false,  // 23707: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > once([0,0,-2]) + progress': false,  // 23708: 'progress' expected [0.08333333333333333], but got [0.9166666666666666]
		'skip-combos: jumpWords(11) + loopSkip > once([0,0,-2]) + stopBegin': false,  // 23709: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopSkip > once([0,0,-2]) + stopFinish': false,  // 23710: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopSkip > once([0,0,-2]) + done': false,  // 23711: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopSkip > once([0,0,-2]) + loopSkip': false,  // 23712: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopSkip > once([0,0,2]) + newWordFragment': false,  // 23733: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopSkip > once([0,0,2]) + progress': false,  // 23734: 'progress' expected [0.16666666666666666], but got [1]
		'skip-combos: jumpWords(11) + loopSkip > once([0,0,2]) + stopBegin': false,  // 23735: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > once([0,0,2]) + stopFinish': false,  // 23736: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > once([0,0,2]) + done': false,  // 23737: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > once([0,0,2]) + loopSkip': false,  // 23738: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > current(null) + newWordFragment': false,  // 23759: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopSkip > current(null) + progress': false,  // 23760: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [1]
		'skip-combos: jumpWords(11) + loopSkip > current(null) + stopBegin': false,  // 23761: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > current(null) + stopFinish': false,  // 23762: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > current(null) + done': false,  // 23763: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > current(null) + loopSkip': false,  // 23764: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + loopSkip > jumpWords(-3) + newWordFragment': false,  // 23941: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > jumpWords(-3) + progress': false,  // 23942: 'progress' expected [0.08333333333333333], but got [0.75,0.6666666666666666]
		'skip-combos: jumpWords(11) + loopSkip > jumpWords(-3) + stopBegin': false,  // 23943: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopSkip > jumpWords(-3) + stopFinish': false,  // 23944: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopSkip > jumpWords(-3) + done': false,  // 23945: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopSkip > jumpWords(-3) + loopSkip': false,  // 23946: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpWords(11) + loopSkip > jumpWords(-1) + newWordFragment': false,  // 23967: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > jumpWords(-1) + progress': false,  // 23968: 'progress' expected [0.08333333333333333], but got [0.9166666666666666]
		'skip-combos: jumpWords(11) + loopSkip > jumpWords(-1) + stopBegin': false,  // 23969: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopSkip > jumpWords(-1) + stopFinish': false,  // 23970: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopSkip > jumpWords(-1) + done': false,  // 23971: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopSkip > jumpWords(-1) + loopSkip': false,  // 23972: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopSkip > jumpWords(0) + newWordFragment': false,  // 23993: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopSkip > jumpWords(0) + progress': false,  // 23994: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [1]
		'skip-combos: jumpWords(11) + loopSkip > jumpWords(0) + stopBegin': false,  // 23995: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > jumpWords(0) + stopFinish': false,  // 23996: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > jumpWords(0) + done': false,  // 23997: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > jumpWords(0) + loopSkip': false,  // 23998: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + loopSkip > jumpWords(4) + newWordFragment': false,  // 24019: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopSkip > jumpWords(4) + progress': false,  // 24020: 'progress' expected [0.4166666666666667,0.5], but got [1]
		'skip-combos: jumpWords(11) + loopSkip > jumpWords(4) + stopBegin': false,  // 24021: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > jumpWords(4) + stopFinish': false,  // 24022: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > jumpWords(4) + done': false,  // 24023: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > jumpWords(4) + loopSkip': false,  // 24024: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + loopSkip > jumpSentences(-1) + newWordFragment': false,  // 24123: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > jumpSentences(-1) + progress': false,  // 24124: 'progress' expected [0.08333333333333333], but got [0.8333333333333334]
		'skip-combos: jumpWords(11) + loopSkip > jumpSentences(-1) + stopBegin': false,  // 24125: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopSkip > jumpSentences(-1) + stopFinish': false,  // 24126: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopSkip > jumpSentences(-1) + done': false,  // 24127: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopSkip > jumpSentences(-1) + loopSkip': false,  // 24128: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopSkip > jumpSentences(0) + newWordFragment': false,  // 24149: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopSkip > jumpSentences(0) + progress': false,  // 24150: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [1]
		'skip-combos: jumpWords(11) + loopSkip > jumpSentences(0) + stopBegin': false,  // 24151: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > jumpSentences(0) + stopFinish': false,  // 24152: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > jumpSentences(0) + done': false,  // 24153: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > jumpSentences(0) + loopSkip': false,  // 24154: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + loopSkip > jumpSentences(1) + newWordFragment': false,  // 24175: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopSkip > jumpSentences(1) + progress': false,  // 24176: 'progress' expected [0.4166666666666667,0.5], but got [1]
		'skip-combos: jumpWords(11) + loopSkip > jumpSentences(1) + stopBegin': false,  // 24177: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > jumpSentences(1) + stopFinish': false,  // 24178: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > jumpSentences(1) + done': false,  // 24179: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > jumpSentences(1) + loopSkip': false,  // 24180: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + loopSkip > jumpSentences(3) + newWordFragment': false,  // 24201: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopSkip > jumpSentences(3) + progress': false,  // 24202: 'progress' expected [0.8333333333333334], but got [1]
		'skip-combos: jumpWords(11) + loopSkip > jumpSentences(3) + stopBegin': false,  // 24203: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > jumpSentences(3) + stopFinish': false,  // 24204: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > jumpSentences(3) + done': false,  // 24205: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > jumpSentences(3) + loopSkip': false,  // 24206: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > nextWord(null) + newWordFragment': false,  // 24253: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopSkip > nextWord(null) + progress': false,  // 24254: 'progress' expected [0.16666666666666666], but got [1]
		'skip-combos: jumpWords(11) + loopSkip > nextWord(null) + stopBegin': false,  // 24255: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > nextWord(null) + stopFinish': false,  // 24256: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > nextWord(null) + done': false,  // 24257: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > nextWord(null) + loopSkip': false,  // 24258: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > nextSentence(null) + newWordFragment': false,  // 24279: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopSkip > nextSentence(null) + progress': false,  // 24280: 'progress' expected [0.4166666666666667,0.5], but got [1]
		'skip-combos: jumpWords(11) + loopSkip > nextSentence(null) + stopBegin': false,  // 24281: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > nextSentence(null) + stopFinish': false,  // 24282: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > nextSentence(null) + done': false,  // 24283: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > nextSentence(null) + loopSkip': false,  // 24284: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(11) + loopSkip > prevWord(null) + newWordFragment': false,  // 24305: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > prevWord(null) + progress': false,  // 24306: 'progress' expected [0.08333333333333333], but got [0.9166666666666666]
		'skip-combos: jumpWords(11) + loopSkip > prevWord(null) + stopBegin': false,  // 24307: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopSkip > prevWord(null) + stopFinish': false,  // 24308: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopSkip > prevWord(null) + done': false,  // 24309: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopSkip > prevWord(null) + loopSkip': false,  // 24310: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopSkip > prevSentence(null) + newWordFragment': false,  // 24331: event should not have been triggerd but WAS
		'skip-combos: jumpWords(11) + loopSkip > prevSentence(null) + progress': false,  // 24332: 'progress' expected [0.08333333333333333], but got [0.8333333333333334]
		'skip-combos: jumpWords(11) + loopSkip > prevSentence(null) + stopBegin': false,  // 24333: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopSkip > prevSentence(null) + stopFinish': false,  // 24334: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopSkip > prevSentence(null) + done': false,  // 24335: event should have been triggerd but was NOT
		'skip-combos: jumpWords(11) + loopSkip > prevSentence(null) + loopSkip': false,  // 24336: event should have been triggerd but was NOT

		// jumpWords(100)
		'skip-combos: jumpWords(100) + onceBegin > rewind(null) + loopSkip': false,  // 33956: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpWords(100) + onceBegin > fastForward(null) + loopSkip': false,  // 33982: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + onceBegin > once([0,0,-2]) + loopSkip': false,  // 34008: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + onceBegin > once([0,0,2]) + loopSkip': false,  // 34034: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + onceBegin > current(null) + loopSkip': false,  // 34060: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + onceBegin > jumpWords(-3) + loopSkip': false,  // 34242: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpWords(100) + onceBegin > jumpWords(-1) + loopSkip': false,  // 34268: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + onceBegin > jumpWords(0) + loopSkip': false,  // 34294: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + onceBegin > jumpWords(4) + loopSkip': false,  // 34320: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + onceBegin > jumpSentences(-1) + loopSkip': false,  // 34424: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + onceBegin > jumpSentences(0) + loopSkip': false,  // 34450: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + onceBegin > jumpSentences(1) + loopSkip': false,  // 34476: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + onceBegin > jumpSentences(3) + loopSkip': false,  // 34502: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + onceBegin > nextWord(null) + loopSkip': false,  // 34554: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + onceBegin > nextSentence(null) + loopSkip': false,  // 34580: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + onceBegin > prevWord(null) + loopSkip': false,  // 34606: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + onceBegin > prevSentence(null) + loopSkip': false,  // 34632: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + onceFinish > rewind(null) + loopSkip': false,  // 34892: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpWords(100) + onceFinish > fastForward(null) + loopSkip': false,  // 34918: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + onceFinish > once([0,0,-2]) + loopSkip': false,  // 34944: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + onceFinish > once([0,0,2]) + loopSkip': false,  // 34970: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + onceFinish > current(null) + loopSkip': false,  // 34996: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + onceFinish > jumpWords(-3) + loopSkip': false,  // 35178: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpWords(100) + onceFinish > jumpWords(-1) + loopSkip': false,  // 35204: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + onceFinish > jumpWords(0) + loopSkip': false,  // 35230: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + onceFinish > jumpWords(4) + loopSkip': false,  // 35256: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + onceFinish > jumpSentences(-1) + loopSkip': false,  // 35360: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + onceFinish > jumpSentences(0) + loopSkip': false,  // 35386: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + onceFinish > jumpSentences(1) + loopSkip': false,  // 35412: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + onceFinish > jumpSentences(3) + loopSkip': false,  // 35438: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + onceFinish > nextWord(null) + loopSkip': false,  // 35490: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + onceFinish > nextSentence(null) + loopSkip': false,  // 35516: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + onceFinish > prevWord(null) + loopSkip': false,  // 35542: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + onceFinish > prevSentence(null) + loopSkip': false,  // 35568: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopBegin > rewind(null) + loopSkip': false,  // 41444: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpWords(100) + loopBegin > fastForward(null) + loopSkip': false,  // 41470: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + loopBegin > once([0,0,-2]) + loopSkip': false,  // 41496: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopBegin > once([0,0,2]) + loopSkip': false,  // 41522: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopBegin > current(null) + loopSkip': false,  // 41548: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + loopBegin > jumpWords(-3) + loopSkip': false,  // 41730: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpWords(100) + loopBegin > jumpWords(-1) + loopSkip': false,  // 41756: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopBegin > jumpWords(0) + loopSkip': false,  // 41782: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + loopBegin > jumpWords(4) + loopSkip': false,  // 41808: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + loopBegin > jumpSentences(-1) + loopSkip': false,  // 41912: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopBegin > jumpSentences(0) + loopSkip': false,  // 41938: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + loopBegin > jumpSentences(1) + loopSkip': false,  // 41964: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + loopBegin > jumpSentences(3) + loopSkip': false,  // 41990: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopBegin > nextWord(null) + loopSkip': false,  // 42042: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopBegin > nextSentence(null) + loopSkip': false,  // 42068: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + loopBegin > prevWord(null) + loopSkip': false,  // 42094: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopBegin > prevSentence(null) + loopSkip': false,  // 42120: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopFinish > rewind(null) + loopSkip': false,  // 42380: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpWords(100) + loopFinish > fastForward(null) + loopSkip': false,  // 42406: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + loopFinish > once([0,0,-2]) + loopSkip': false,  // 42432: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopFinish > once([0,0,2]) + loopSkip': false,  // 42458: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopFinish > current(null) + loopSkip': false,  // 42484: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + loopFinish > jumpWords(-3) + loopSkip': false,  // 42666: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpWords(100) + loopFinish > jumpWords(-1) + loopSkip': false,  // 42692: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopFinish > jumpWords(0) + loopSkip': false,  // 42718: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + loopFinish > jumpWords(4) + loopSkip': false,  // 42744: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + loopFinish > jumpSentences(-1) + loopSkip': false,  // 42848: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopFinish > jumpSentences(0) + loopSkip': false,  // 42874: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + loopFinish > jumpSentences(1) + loopSkip': false,  // 42900: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + loopFinish > jumpSentences(3) + loopSkip': false,  // 42926: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopFinish > nextWord(null) + loopSkip': false,  // 42978: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopFinish > nextSentence(null) + loopSkip': false,  // 43004: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + loopFinish > prevWord(null) + loopSkip': false,  // 43030: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopFinish > prevSentence(null) + loopSkip': false,  // 43056: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + progress > rewind(null) + loopSkip': false,  // 44252: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpWords(100) + progress > fastForward(null) + loopSkip': false,  // 44278: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + progress > once([0,0,-2]) + loopSkip': false,  // 44304: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + progress > once([0,0,2]) + loopSkip': false,  // 44330: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + progress > current(null) + loopSkip': false,  // 44356: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + progress > jumpWords(-3) + loopSkip': false,  // 44538: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpWords(100) + progress > jumpWords(-1) + loopSkip': false,  // 44564: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + progress > jumpWords(0) + loopSkip': false,  // 44590: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + progress > jumpWords(4) + loopSkip': false,  // 44616: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + progress > jumpSentences(-1) + loopSkip': false,  // 44720: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + progress > jumpSentences(0) + loopSkip': false,  // 44746: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + progress > jumpSentences(1) + loopSkip': false,  // 44772: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + progress > jumpSentences(3) + loopSkip': false,  // 44798: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + progress > nextWord(null) + loopSkip': false,  // 44850: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + progress > nextSentence(null) + loopSkip': false,  // 44876: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + progress > prevWord(null) + loopSkip': false,  // 44902: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + progress > prevSentence(null) + loopSkip': false,  // 44928: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + stopBegin > rewind(null) + loopSkip': false,  // 45188: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpWords(100) + stopBegin > fastForward(null) + loopSkip': false,  // 45214: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + stopBegin > once([0,0,-2]) + loopSkip': false,  // 45240: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + stopBegin > once([0,0,2]) + loopSkip': false,  // 45266: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + stopBegin > current(null) + loopSkip': false,  // 45292: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + stopBegin > jumpWords(-3) + loopSkip': false,  // 45474: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpWords(100) + stopBegin > jumpWords(-1) + loopSkip': false,  // 45500: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + stopBegin > jumpWords(0) + loopSkip': false,  // 45526: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + stopBegin > jumpWords(4) + loopSkip': false,  // 45552: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + stopBegin > jumpSentences(-1) + loopSkip': false,  // 45656: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + stopBegin > jumpSentences(0) + loopSkip': false,  // 45682: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + stopBegin > jumpSentences(1) + loopSkip': false,  // 45708: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + stopBegin > jumpSentences(3) + loopSkip': false,  // 45734: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + stopBegin > nextWord(null) + loopSkip': false,  // 45786: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + stopBegin > nextSentence(null) + loopSkip': false,  // 45812: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + stopBegin > prevWord(null) + loopSkip': false,  // 45838: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + stopBegin > prevSentence(null) + loopSkip': false,  // 45864: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + stopFinish > rewind(null) + loopSkip': false,  // 46124: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpWords(100) + stopFinish > fastForward(null) + loopSkip': false,  // 46150: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + stopFinish > once([0,0,-2]) + loopSkip': false,  // 46176: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + stopFinish > once([0,0,2]) + loopSkip': false,  // 46202: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + stopFinish > current(null) + loopSkip': false,  // 46228: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + stopFinish > jumpWords(-3) + loopSkip': false,  // 46410: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpWords(100) + stopFinish > jumpWords(-1) + loopSkip': false,  // 46436: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + stopFinish > jumpWords(0) + loopSkip': false,  // 46462: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + stopFinish > jumpWords(4) + loopSkip': false,  // 46488: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + stopFinish > jumpSentences(-1) + loopSkip': false,  // 46592: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + stopFinish > jumpSentences(0) + loopSkip': false,  // 46618: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + stopFinish > jumpSentences(1) + loopSkip': false,  // 46644: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + stopFinish > jumpSentences(3) + loopSkip': false,  // 46670: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + stopFinish > nextWord(null) + loopSkip': false,  // 46722: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + stopFinish > nextSentence(null) + loopSkip': false,  // 46748: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + stopFinish > prevWord(null) + loopSkip': false,  // 46774: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + stopFinish > prevSentence(null) + loopSkip': false,  // 46800: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + done > rewind(null) + loopSkip': false,  // 47060: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpWords(100) + done > fastForward(null) + loopSkip': false,  // 47086: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + done > once([0,0,-2]) + loopSkip': false,  // 47112: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + done > once([0,0,2]) + loopSkip': false,  // 47138: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + done > current(null) + loopSkip': false,  // 47164: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + done > jumpWords(-3) + loopSkip': false,  // 47346: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpWords(100) + done > jumpWords(-1) + loopSkip': false,  // 47372: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + done > jumpWords(0) + loopSkip': false,  // 47398: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + done > jumpWords(4) + loopSkip': false,  // 47424: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + done > jumpSentences(-1) + loopSkip': false,  // 47528: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + done > jumpSentences(0) + loopSkip': false,  // 47554: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + done > jumpSentences(1) + loopSkip': false,  // 47580: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + done > jumpSentences(3) + loopSkip': false,  // 47606: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + done > nextWord(null) + loopSkip': false,  // 47658: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + done > nextSentence(null) + loopSkip': false,  // 47684: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + done > prevWord(null) + loopSkip': false,  // 47710: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + done > prevSentence(null) + loopSkip': false,  // 47736: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopSkip > play(null) + playBegin': false,  // 47815: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopSkip > play(null) + playFinish': false,  // 47816: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopSkip > play(null) + restartBegin': false,  // 47819: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > play(null) + restartFinish': false,  // 47820: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > toggle(null) + playBegin': false,  // 47841: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopSkip > toggle(null) + playFinish': false,  // 47842: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopSkip > toggle(null) + restartBegin': false,  // 47845: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > toggle(null) + restartFinish': false,  // 47846: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > rewind(null) + newWordFragment': false,  // 47991: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > rewind(null) + progress': false,  // 47992: 'progress' expected [0.08333333333333333], but got [0.9166666666666666,0.8333333333333334,0.75,0.6666666666666666,0.5833333333333334,0.5,0.4166666666666667,0.3333333333333333,0.25,0.16666666666666666,0.08333333333333333]
		'skip-combos: jumpWords(100) + loopSkip > rewind(null) + loopSkip': false,  // 47996: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpWords(100) + loopSkip > fastForward(null) + newWordFragment': false,  // 48017: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopSkip > fastForward(null) + progress': false,  // 48018: 'progress' expected [0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [1]
		'skip-combos: jumpWords(100) + loopSkip > fastForward(null) + loopSkip': false,  // 48022: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + loopSkip > once([0,0,-2]) + newWordFragment': false,  // 48043: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > once([0,0,-2]) + progress': false,  // 48044: 'progress' expected [0.08333333333333333], but got [0.9166666666666666]
		'skip-combos: jumpWords(100) + loopSkip > once([0,0,-2]) + stopBegin': false,  // 48045: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopSkip > once([0,0,-2]) + stopFinish': false,  // 48046: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopSkip > once([0,0,-2]) + done': false,  // 48047: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopSkip > once([0,0,-2]) + loopSkip': false,  // 48048: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopSkip > once([0,0,2]) + newWordFragment': false,  // 48069: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopSkip > once([0,0,2]) + progress': false,  // 48070: 'progress' expected [0.16666666666666666], but got [1]
		'skip-combos: jumpWords(100) + loopSkip > once([0,0,2]) + stopBegin': false,  // 48071: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > once([0,0,2]) + stopFinish': false,  // 48072: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > once([0,0,2]) + done': false,  // 48073: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > once([0,0,2]) + loopSkip': false,  // 48074: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > current(null) + newWordFragment': false,  // 48095: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopSkip > current(null) + progress': false,  // 48096: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [1]
		'skip-combos: jumpWords(100) + loopSkip > current(null) + stopBegin': false,  // 48097: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > current(null) + stopFinish': false,  // 48098: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > current(null) + done': false,  // 48099: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > current(null) + loopSkip': false,  // 48100: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + loopSkip > jumpWords(-3) + newWordFragment': false,  // 48277: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > jumpWords(-3) + progress': false,  // 48278: 'progress' expected [0.08333333333333333], but got [0.75,0.6666666666666666]
		'skip-combos: jumpWords(100) + loopSkip > jumpWords(-3) + stopBegin': false,  // 48279: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopSkip > jumpWords(-3) + stopFinish': false,  // 48280: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopSkip > jumpWords(-3) + done': false,  // 48281: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopSkip > jumpWords(-3) + loopSkip': false,  // 48282: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpWords(100) + loopSkip > jumpWords(-1) + newWordFragment': false,  // 48303: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > jumpWords(-1) + progress': false,  // 48304: 'progress' expected [0.08333333333333333], but got [0.9166666666666666]
		'skip-combos: jumpWords(100) + loopSkip > jumpWords(-1) + stopBegin': false,  // 48305: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopSkip > jumpWords(-1) + stopFinish': false,  // 48306: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopSkip > jumpWords(-1) + done': false,  // 48307: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopSkip > jumpWords(-1) + loopSkip': false,  // 48308: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopSkip > jumpWords(0) + newWordFragment': false,  // 48329: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopSkip > jumpWords(0) + progress': false,  // 48330: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [1]
		'skip-combos: jumpWords(100) + loopSkip > jumpWords(0) + stopBegin': false,  // 48331: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > jumpWords(0) + stopFinish': false,  // 48332: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > jumpWords(0) + done': false,  // 48333: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > jumpWords(0) + loopSkip': false,  // 48334: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + loopSkip > jumpWords(4) + newWordFragment': false,  // 48355: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopSkip > jumpWords(4) + progress': false,  // 48356: 'progress' expected [0.4166666666666667,0.5], but got [1]
		'skip-combos: jumpWords(100) + loopSkip > jumpWords(4) + stopBegin': false,  // 48357: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > jumpWords(4) + stopFinish': false,  // 48358: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > jumpWords(4) + done': false,  // 48359: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > jumpWords(4) + loopSkip': false,  // 48360: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + loopSkip > jumpSentences(-1) + newWordFragment': false,  // 48459: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > jumpSentences(-1) + progress': false,  // 48460: 'progress' expected [0.08333333333333333], but got [0.8333333333333334]
		'skip-combos: jumpWords(100) + loopSkip > jumpSentences(-1) + stopBegin': false,  // 48461: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopSkip > jumpSentences(-1) + stopFinish': false,  // 48462: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopSkip > jumpSentences(-1) + done': false,  // 48463: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopSkip > jumpSentences(-1) + loopSkip': false,  // 48464: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopSkip > jumpSentences(0) + newWordFragment': false,  // 48485: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopSkip > jumpSentences(0) + progress': false,  // 48486: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [1]
		'skip-combos: jumpWords(100) + loopSkip > jumpSentences(0) + stopBegin': false,  // 48487: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > jumpSentences(0) + stopFinish': false,  // 48488: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > jumpSentences(0) + done': false,  // 48489: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > jumpSentences(0) + loopSkip': false,  // 48490: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + loopSkip > jumpSentences(1) + newWordFragment': false,  // 48511: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopSkip > jumpSentences(1) + progress': false,  // 48512: 'progress' expected [0.4166666666666667,0.5], but got [1]
		'skip-combos: jumpWords(100) + loopSkip > jumpSentences(1) + stopBegin': false,  // 48513: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > jumpSentences(1) + stopFinish': false,  // 48514: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > jumpSentences(1) + done': false,  // 48515: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > jumpSentences(1) + loopSkip': false,  // 48516: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + loopSkip > jumpSentences(3) + newWordFragment': false,  // 48537: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopSkip > jumpSentences(3) + progress': false,  // 48538: 'progress' expected [0.8333333333333334], but got [1]
		'skip-combos: jumpWords(100) + loopSkip > jumpSentences(3) + stopBegin': false,  // 48539: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > jumpSentences(3) + stopFinish': false,  // 48540: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > jumpSentences(3) + done': false,  // 48541: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > jumpSentences(3) + loopSkip': false,  // 48542: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > nextWord(null) + newWordFragment': false,  // 48589: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopSkip > nextWord(null) + progress': false,  // 48590: 'progress' expected [0.16666666666666666], but got [1]
		'skip-combos: jumpWords(100) + loopSkip > nextWord(null) + stopBegin': false,  // 48591: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > nextWord(null) + stopFinish': false,  // 48592: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > nextWord(null) + done': false,  // 48593: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > nextWord(null) + loopSkip': false,  // 48594: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > nextSentence(null) + newWordFragment': false,  // 48615: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopSkip > nextSentence(null) + progress': false,  // 48616: 'progress' expected [0.4166666666666667,0.5], but got [1]
		'skip-combos: jumpWords(100) + loopSkip > nextSentence(null) + stopBegin': false,  // 48617: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > nextSentence(null) + stopFinish': false,  // 48618: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > nextSentence(null) + done': false,  // 48619: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > nextSentence(null) + loopSkip': false,  // 48620: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpWords(100) + loopSkip > prevWord(null) + newWordFragment': false,  // 48641: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > prevWord(null) + progress': false,  // 48642: 'progress' expected [0.08333333333333333], but got [0.9166666666666666]
		'skip-combos: jumpWords(100) + loopSkip > prevWord(null) + stopBegin': false,  // 48643: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopSkip > prevWord(null) + stopFinish': false,  // 48644: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopSkip > prevWord(null) + done': false,  // 48645: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopSkip > prevWord(null) + loopSkip': false,  // 48646: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopSkip > prevSentence(null) + newWordFragment': false,  // 48667: event should not have been triggerd but WAS
		'skip-combos: jumpWords(100) + loopSkip > prevSentence(null) + progress': false,  // 48668: 'progress' expected [0.08333333333333333], but got [0.8333333333333334]
		'skip-combos: jumpWords(100) + loopSkip > prevSentence(null) + stopBegin': false,  // 48669: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopSkip > prevSentence(null) + stopFinish': false,  // 48670: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopSkip > prevSentence(null) + done': false,  // 48671: event should have been triggerd but was NOT
		'skip-combos: jumpWords(100) + loopSkip > prevSentence(null) + loopSkip': false,  // 48672: event should have been triggerd but was NOT

		// jumpSentences(-3)
		'skip-combos: jumpSentences(-3) + loopSkip > play(null) + playBegin': false,  // 23479: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(-3) + loopSkip > play(null) + playFinish': false,  // 23480: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(-3) + loopSkip > play(null) + restartBegin': false,  // 23483: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(-3) + loopSkip > play(null) + restartFinish': false,  // 23484: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(-3) + loopSkip > toggle(null) + playBegin': false,  // 23505: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(-3) + loopSkip > toggle(null) + playFinish': false,  // 23506: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(-3) + loopSkip > toggle(null) + restartBegin': false,  // 23509: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(-3) + loopSkip > toggle(null) + restartFinish': false,  // 23510: event should not have been triggerd but WAS

		// jumpSentences(-1)
		'skip-combos: jumpSentences(-1) + loopSkip > play(null) + playBegin': false,  // 47815: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(-1) + loopSkip > play(null) + playFinish': false,  // 47816: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(-1) + loopSkip > play(null) + restartBegin': false,  // 47819: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(-1) + loopSkip > play(null) + restartFinish': false,  // 47820: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(-1) + loopSkip > toggle(null) + playBegin': false,  // 47841: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(-1) + loopSkip > toggle(null) + playFinish': false,  // 47842: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(-1) + loopSkip > toggle(null) + restartBegin': false,  // 47845: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(-1) + loopSkip > toggle(null) + restartFinish': false,  // 47846: event should not have been triggerd but WAS

		// jumpSentences(0)
		'skip-combos: jumpSentences(0) + onceBegin > toggle(null) + loopSkip': false,  // 9490: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(0) + onceFinish > toggle(null) + loopSkip': false,  // 10426: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(0) + loopBegin > toggle(null) + loopSkip': false,  // 16978: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(0) + loopFinish > toggle(null) + loopSkip': false,  // 17914: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(0) + newWordFragment > play(null) + progress': false,  // 18820: 'progress' expected [0.08333333333333333,0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1]
		'skip-combos: jumpSentences(0) + newWordFragment > play(null) + loopSkip': false,  // 18824: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?"]
		'skip-combos: jumpSentences(0) + newWordFragment > toggle(null) + loopSkip': false,  // 18850: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(0) + newWordFragment > fastForward(null) + newWordFragment': false,  // 19001: frags expected ["you","brave","I","back.","Why,","oh"], but got ["brave","I","back.","Why,","oh"]
		'skip-combos: jumpSentences(0) + newWordFragment > fastForward(null) + progress': false,  // 19002: 'progress' expected [0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1]
		'skip-combos: jumpSentences(0) + newWordFragment > once([0,0,2]) + newWordFragment': false,  // 19053: frags expected ["you"], but got ["brave"]
		'skip-combos: jumpSentences(0) + newWordFragment > once([0,0,2]) + progress': false,  // 19054: 'progress' expected [0.16666666666666666], but got [0.25]
		'skip-combos: jumpSentences(0) + newWordFragment > current(null) + progress': false,  // 19080: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.16666666666666666]
		'skip-combos: jumpSentences(0) + newWordFragment > current(null) + loopSkip': false,  // 19084: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(0) + newWordFragment > jumpWords(0) + progress': false,  // 19314: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.16666666666666666]
		'skip-combos: jumpSentences(0) + newWordFragment > jumpWords(0) + loopSkip': false,  // 19318: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(0) + newWordFragment > jumpWords(4) + progress': false,  // 19340: 'progress' expected [0.4166666666666667,0.5], but got [0.5]
		'skip-combos: jumpSentences(0) + newWordFragment > jumpWords(4) + loopSkip': false,  // 19344: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(0) + newWordFragment > jumpSentences(0) + progress': false,  // 19470: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.16666666666666666]
		'skip-combos: jumpSentences(0) + newWordFragment > jumpSentences(0) + loopSkip': false,  // 19474: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(0) + newWordFragment > nextWord(null) + newWordFragment': false,  // 19573: frags expected ["you"], but got ["brave"]
		'skip-combos: jumpSentences(0) + newWordFragment > nextWord(null) + progress': false,  // 19574: 'progress' expected [0.16666666666666666], but got [0.25]
		'skip-combos: jumpSentences(0) + progress > toggle(null) + loopSkip': false,  // 19786: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(0) + loopSkip > toggle(null) + playBegin': false,  // 23505: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(0) + loopSkip > toggle(null) + playFinish': false,  // 23506: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(0) + loopSkip > toggle(null) + pauseBegin': false,  // 23511: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(0) + loopSkip > toggle(null) + pauseFinish': false,  // 23512: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(0) + loopSkip > toggle(null) + revertBegin': false,  // 23517: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(0) + loopSkip > toggle(null) + revertFinish': false,  // 23518: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(0) + loopSkip > toggle(null) + loopBegin': false,  // 23523: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(0) + loopSkip > toggle(null) + loopFinish': false,  // 23524: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(0) + loopSkip > toggle(null) + newWordFragment': false,  // 23525: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(0) + loopSkip > toggle(null) + progress': false,  // 23526: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(0) + loopSkip > toggle(null) + stopBegin': false,  // 23527: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(0) + loopSkip > toggle(null) + stopFinish': false,  // 23528: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(0) + loopSkip > toggle(null) + done': false,  // 23529: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(0) + loopSkip > toggle(null) + loopSkip': false,  // 23530: event should have been triggerd but was NOT

		// jumpSentences(1)
		'skip-combos: jumpSentences(1) + onceBegin > play(null) + loopSkip': false,  // 33800: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["Delirious,","come","\n","wattlebird?"]
		'skip-combos: jumpSentences(1) + onceBegin > toggle(null) + loopSkip': false,  // 33826: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + onceBegin > rewind(null) + loopSkip': false,  // 33956: frags expected ["Victorious,"], but got ["flag.","Victorious,"]
		'skip-combos: jumpSentences(1) + onceBegin > fastForward(null) + loopSkip': false,  // 33982: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["come","\n","wattlebird?"]
		'skip-combos: jumpSentences(1) + onceBegin > once([0,0,-2]) + loopSkip': false,  // 34008: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpSentences(1) + onceBegin > current(null) + loopSkip': false,  // 34060: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpSentences(1) + onceBegin > jumpWords(-3) + loopSkip': false,  // 34242: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + onceBegin > jumpWords(-1) + loopSkip': false,  // 34268: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpSentences(1) + onceBegin > jumpWords(0) + loopSkip': false,  // 34294: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpSentences(1) + onceBegin > jumpWords(4) + loopSkip': false,  // 34320: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpSentences(1) + onceBegin > jumpSentences(0) + loopSkip': false,  // 34450: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpSentences(1) + onceBegin > jumpSentences(1) + loopSkip': false,  // 34476: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpSentences(1) + onceBegin > jumpSentences(3) + loopSkip': false,  // 34502: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(1) + onceBegin > nextSentence(null) + loopSkip': false,  // 34580: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpSentences(1) + onceBegin > prevWord(null) + loopSkip': false,  // 34606: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpSentences(1) + onceFinish > play(null) + loopSkip': false,  // 34736: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["Delirious,","come","\n","wattlebird?"]
		'skip-combos: jumpSentences(1) + onceFinish > toggle(null) + loopSkip': false,  // 34762: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + onceFinish > rewind(null) + loopSkip': false,  // 34892: frags expected ["Victorious,"], but got ["flag.","Victorious,"]
		'skip-combos: jumpSentences(1) + onceFinish > fastForward(null) + loopSkip': false,  // 34918: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["come","\n","wattlebird?"]
		'skip-combos: jumpSentences(1) + onceFinish > once([0,0,-2]) + loopSkip': false,  // 34944: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpSentences(1) + onceFinish > current(null) + loopSkip': false,  // 34996: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpSentences(1) + onceFinish > jumpWords(-3) + loopSkip': false,  // 35178: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + onceFinish > jumpWords(-1) + loopSkip': false,  // 35204: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpSentences(1) + onceFinish > jumpWords(0) + loopSkip': false,  // 35230: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpSentences(1) + onceFinish > jumpWords(4) + loopSkip': false,  // 35256: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpSentences(1) + onceFinish > jumpSentences(0) + loopSkip': false,  // 35386: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpSentences(1) + onceFinish > jumpSentences(1) + loopSkip': false,  // 35412: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpSentences(1) + onceFinish > jumpSentences(3) + loopSkip': false,  // 35438: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(1) + onceFinish > nextSentence(null) + loopSkip': false,  // 35516: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpSentences(1) + onceFinish > prevWord(null) + loopSkip': false,  // 35542: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpSentences(1) + loopBegin > play(null) + loopSkip': false,  // 41288: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["Delirious,","come","\n","wattlebird?"]
		'skip-combos: jumpSentences(1) + loopBegin > toggle(null) + loopSkip': false,  // 41314: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + loopBegin > rewind(null) + loopSkip': false,  // 41444: frags expected ["Victorious,"], but got ["flag.","Victorious,"]
		'skip-combos: jumpSentences(1) + loopBegin > fastForward(null) + loopSkip': false,  // 41470: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["come","\n","wattlebird?"]
		'skip-combos: jumpSentences(1) + loopBegin > once([0,0,-2]) + loopSkip': false,  // 41496: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpSentences(1) + loopBegin > current(null) + loopSkip': false,  // 41548: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpSentences(1) + loopBegin > jumpWords(-3) + loopSkip': false,  // 41730: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + loopBegin > jumpWords(-1) + loopSkip': false,  // 41756: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpSentences(1) + loopBegin > jumpWords(0) + loopSkip': false,  // 41782: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpSentences(1) + loopBegin > jumpWords(4) + loopSkip': false,  // 41808: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpSentences(1) + loopBegin > jumpSentences(0) + loopSkip': false,  // 41938: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpSentences(1) + loopBegin > jumpSentences(1) + loopSkip': false,  // 41964: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpSentences(1) + loopBegin > jumpSentences(3) + loopSkip': false,  // 41990: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(1) + loopBegin > nextSentence(null) + loopSkip': false,  // 42068: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpSentences(1) + loopBegin > prevWord(null) + loopSkip': false,  // 42094: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpSentences(1) + loopFinish > play(null) + loopSkip': false,  // 42224: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["Delirious,","come","\n","wattlebird?"]
		'skip-combos: jumpSentences(1) + loopFinish > toggle(null) + loopSkip': false,  // 42250: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + loopFinish > rewind(null) + loopSkip': false,  // 42380: frags expected ["Victorious,"], but got ["flag.","Victorious,"]
		'skip-combos: jumpSentences(1) + loopFinish > fastForward(null) + loopSkip': false,  // 42406: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["come","\n","wattlebird?"]
		'skip-combos: jumpSentences(1) + loopFinish > once([0,0,-2]) + loopSkip': false,  // 42432: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpSentences(1) + loopFinish > current(null) + loopSkip': false,  // 42484: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpSentences(1) + loopFinish > jumpWords(-3) + loopSkip': false,  // 42666: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + loopFinish > jumpWords(-1) + loopSkip': false,  // 42692: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpSentences(1) + loopFinish > jumpWords(0) + loopSkip': false,  // 42718: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpSentences(1) + loopFinish > jumpWords(4) + loopSkip': false,  // 42744: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpSentences(1) + loopFinish > jumpSentences(0) + loopSkip': false,  // 42874: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpSentences(1) + loopFinish > jumpSentences(1) + loopSkip': false,  // 42900: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpSentences(1) + loopFinish > jumpSentences(3) + loopSkip': false,  // 42926: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(1) + loopFinish > nextSentence(null) + loopSkip': false,  // 43004: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpSentences(1) + loopFinish > prevWord(null) + loopSkip': false,  // 43030: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpSentences(1) + newWordFragment > play(null) + loopSkip': false,  // 43160: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["come","\n","wattlebird?"]
		'skip-combos: jumpSentences(1) + newWordFragment > toggle(null) + loopSkip': false,  // 43186: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + newWordFragment > rewind(null) + loopSkip': false,  // 43316: frags expected ["Victorious,"], but got ["Delirious,","flag.","Victorious,"]
		'skip-combos: jumpSentences(1) + newWordFragment > fastForward(null) + loopSkip': false,  // 43342: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["come","\n","wattlebird?"]
		'skip-combos: jumpSentences(1) + newWordFragment > once([0,0,-2]) + loopSkip': false,  // 43368: frags expected ["Victorious,"], but got ["Delirious,","flag."]
		'skip-combos: jumpSentences(1) + newWordFragment > once([0,0,2]) + loopSkip': false,  // 43394: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(1) + newWordFragment > current(null) + loopSkip': false,  // 43420: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + newWordFragment > jumpWords(-3) + loopSkip': false,  // 43602: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + newWordFragment > jumpWords(-1) + loopSkip': false,  // 43628: frags expected ["Victorious,"], but got ["Delirious,","flag."]
		'skip-combos: jumpSentences(1) + newWordFragment > jumpWords(0) + loopSkip': false,  // 43654: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + newWordFragment > jumpWords(4) + loopSkip': false,  // 43680: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + newWordFragment > jumpSentences(-1) + newWordFragment': false,  // 43779: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(1) + newWordFragment > jumpSentences(-1) + progress': false,  // 43780: 'progress' expected [0.08333333333333333], but got [0.4166666666666667,0.3333333333333333,0.25]
		'skip-combos: jumpSentences(1) + newWordFragment > jumpSentences(-1) + stopBegin': false,  // 43781: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + newWordFragment > jumpSentences(-1) + stopFinish': false,  // 43782: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + newWordFragment > jumpSentences(-1) + done': false,  // 43783: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + newWordFragment > jumpSentences(-1) + loopSkip': false,  // 43784: frags expected ["Victorious,"], but got ["Delirious,","flag."]
		'skip-combos: jumpSentences(1) + newWordFragment > jumpSentences(0) + loopSkip': false,  // 43810: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + newWordFragment > jumpSentences(1) + loopSkip': false,  // 43836: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpSentences(1) + newWordFragment > jumpSentences(3) + loopSkip': false,  // 43862: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(1) + newWordFragment > nextWord(null) + loopSkip': false,  // 43914: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(1) + newWordFragment > nextSentence(null) + loopSkip': false,  // 43940: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpSentences(1) + newWordFragment > prevWord(null) + loopSkip': false,  // 43966: frags expected ["Victorious,"], but got ["Delirious,","flag."]
		'skip-combos: jumpSentences(1) + newWordFragment > prevSentence(null) + newWordFragment': false,  // 43987: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(1) + newWordFragment > prevSentence(null) + progress': false,  // 43988: 'progress' expected [0.08333333333333333], but got [0.4166666666666667,0.3333333333333333,0.25]
		'skip-combos: jumpSentences(1) + newWordFragment > prevSentence(null) + stopBegin': false,  // 43989: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + newWordFragment > prevSentence(null) + stopFinish': false,  // 43990: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + newWordFragment > prevSentence(null) + done': false,  // 43991: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + newWordFragment > prevSentence(null) + loopSkip': false,  // 43992: frags expected ["Victorious,"], but got ["Delirious,","flag."]
		'skip-combos: jumpSentences(1) + progress > play(null) + loopSkip': false,  // 44096: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["Delirious,","come","\n","wattlebird?"]
		'skip-combos: jumpSentences(1) + progress > toggle(null) + loopSkip': false,  // 44122: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + progress > rewind(null) + loopSkip': false,  // 44252: frags expected ["Victorious,"], but got ["flag.","Victorious,"]
		'skip-combos: jumpSentences(1) + progress > fastForward(null) + loopSkip': false,  // 44278: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["come","\n","wattlebird?"]
		'skip-combos: jumpSentences(1) + progress > once([0,0,-2]) + loopSkip': false,  // 44304: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpSentences(1) + progress > current(null) + loopSkip': false,  // 44356: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpSentences(1) + progress > jumpWords(-3) + loopSkip': false,  // 44538: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + progress > jumpWords(-1) + loopSkip': false,  // 44564: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpSentences(1) + progress > jumpWords(0) + loopSkip': false,  // 44590: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpSentences(1) + progress > jumpWords(4) + loopSkip': false,  // 44616: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpSentences(1) + progress > jumpSentences(0) + loopSkip': false,  // 44746: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpSentences(1) + progress > jumpSentences(1) + loopSkip': false,  // 44772: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpSentences(1) + progress > jumpSentences(3) + loopSkip': false,  // 44798: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(1) + progress > nextSentence(null) + loopSkip': false,  // 44876: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpSentences(1) + progress > prevWord(null) + loopSkip': false,  // 44902: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpSentences(1) + loopSkip > play(null) + newWordFragment': false,  // 47835: frags expected ["you","brave","I","back.","Why,","oh"], but got ["I","back.","Why,","oh"]
		'skip-combos: jumpSentences(1) + loopSkip > play(null) + progress': false,  // 47836: 'progress' expected [0.08333333333333333,0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1]
		'skip-combos: jumpSentences(1) + loopSkip > play(null) + loopSkip': false,  // 47840: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["Delirious,","come","\n","wattlebird?"]
		'skip-combos: jumpSentences(1) + loopSkip > toggle(null) + playBegin': false,  // 47841: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + loopSkip > toggle(null) + playFinish': false,  // 47842: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + loopSkip > toggle(null) + pauseBegin': false,  // 47847: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(1) + loopSkip > toggle(null) + pauseFinish': false,  // 47848: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(1) + loopSkip > toggle(null) + revertBegin': false,  // 47853: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(1) + loopSkip > toggle(null) + revertFinish': false,  // 47854: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(1) + loopSkip > toggle(null) + loopBegin': false,  // 47859: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + loopSkip > toggle(null) + loopFinish': false,  // 47860: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + loopSkip > toggle(null) + newWordFragment': false,  // 47861: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + loopSkip > toggle(null) + progress': false,  // 47862: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + loopSkip > toggle(null) + stopBegin': false,  // 47863: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + loopSkip > toggle(null) + stopFinish': false,  // 47864: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + loopSkip > toggle(null) + done': false,  // 47865: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + loopSkip > toggle(null) + loopSkip': false,  // 47866: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + loopSkip > rewind(null) + newWordFragment': false,  // 47991: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(1) + loopSkip > rewind(null) + progress': false,  // 47992: 'progress' expected [0.08333333333333333], but got [0.3333333333333333,0.25,0.16666666666666666,0.08333333333333333]
		'skip-combos: jumpSentences(1) + loopSkip > rewind(null) + loopSkip': false,  // 47996: frags expected ["Victorious,"], but got ["flag.","Victorious,"]
		'skip-combos: jumpSentences(1) + loopSkip > fastForward(null) + newWordFragment': false,  // 48017: frags expected ["you","brave","I","back.","Why,","oh"], but got ["I","back.","Why,","oh"]
		'skip-combos: jumpSentences(1) + loopSkip > fastForward(null) + progress': false,  // 48018: 'progress' expected [0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1]
		'skip-combos: jumpSentences(1) + loopSkip > fastForward(null) + loopSkip': false,  // 48022: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["come","\n","wattlebird?"]
		'skip-combos: jumpSentences(1) + loopSkip > once([0,0,-2]) + newWordFragment': false,  // 48043: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(1) + loopSkip > once([0,0,-2]) + progress': false,  // 48044: 'progress' expected [0.08333333333333333], but got [0.3333333333333333,0.25]
		'skip-combos: jumpSentences(1) + loopSkip > once([0,0,-2]) + stopBegin': false,  // 48045: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + loopSkip > once([0,0,-2]) + stopFinish': false,  // 48046: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + loopSkip > once([0,0,-2]) + done': false,  // 48047: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + loopSkip > once([0,0,-2]) + loopSkip': false,  // 48048: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpSentences(1) + loopSkip > once([0,0,2]) + newWordFragment': false,  // 48069: frags expected ["you"], but got ["I"]
		'skip-combos: jumpSentences(1) + loopSkip > once([0,0,2]) + progress': false,  // 48070: 'progress' expected [0.16666666666666666], but got [0.5]
		'skip-combos: jumpSentences(1) + loopSkip > current(null) + newWordFragment': false,  // 48095: frags expected ["you"], but got ["I"]
		'skip-combos: jumpSentences(1) + loopSkip > current(null) + progress': false,  // 48096: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.4166666666666667,0.5]
		'skip-combos: jumpSentences(1) + loopSkip > current(null) + loopSkip': false,  // 48100: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpSentences(1) + loopSkip > jumpWords(-3) + newWordFragment': false,  // 48277: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(1) + loopSkip > jumpWords(-3) + progress': false,  // 48278: 'progress' expected [0.08333333333333333], but got [0.16666666666666666]
		'skip-combos: jumpSentences(1) + loopSkip > jumpWords(-3) + stopBegin': false,  // 48279: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + loopSkip > jumpWords(-3) + stopFinish': false,  // 48280: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + loopSkip > jumpWords(-3) + done': false,  // 48281: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + loopSkip > jumpWords(-3) + loopSkip': false,  // 48282: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + loopSkip > jumpWords(-1) + newWordFragment': false,  // 48303: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(1) + loopSkip > jumpWords(-1) + progress': false,  // 48304: 'progress' expected [0.08333333333333333], but got [0.3333333333333333,0.25]
		'skip-combos: jumpSentences(1) + loopSkip > jumpWords(-1) + stopBegin': false,  // 48305: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + loopSkip > jumpWords(-1) + stopFinish': false,  // 48306: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + loopSkip > jumpWords(-1) + done': false,  // 48307: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + loopSkip > jumpWords(-1) + loopSkip': false,  // 48308: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: jumpSentences(1) + loopSkip > jumpWords(0) + newWordFragment': false,  // 48329: frags expected ["you"], but got ["I"]
		'skip-combos: jumpSentences(1) + loopSkip > jumpWords(0) + progress': false,  // 48330: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.4166666666666667,0.5]
		'skip-combos: jumpSentences(1) + loopSkip > jumpWords(0) + loopSkip': false,  // 48334: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpSentences(1) + loopSkip > jumpWords(4) + newWordFragment': false,  // 48355: frags expected ["I"], but got ["Why,"]
		'skip-combos: jumpSentences(1) + loopSkip > jumpWords(4) + progress': false,  // 48356: 'progress' expected [0.4166666666666667,0.5], but got [0.75,0.8333333333333334]
		'skip-combos: jumpSentences(1) + loopSkip > jumpWords(4) + loopSkip': false,  // 48360: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpSentences(1) + loopSkip > jumpSentences(0) + newWordFragment': false,  // 48485: frags expected ["you"], but got ["I"]
		'skip-combos: jumpSentences(1) + loopSkip > jumpSentences(0) + progress': false,  // 48486: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.4166666666666667,0.5]
		'skip-combos: jumpSentences(1) + loopSkip > jumpSentences(0) + loopSkip': false,  // 48490: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: jumpSentences(1) + loopSkip > jumpSentences(1) + newWordFragment': false,  // 48511: frags expected ["I"], but got ["Why,"]
		'skip-combos: jumpSentences(1) + loopSkip > jumpSentences(1) + progress': false,  // 48512: 'progress' expected [0.4166666666666667,0.5], but got [0.75,0.8333333333333334]
		'skip-combos: jumpSentences(1) + loopSkip > jumpSentences(1) + loopSkip': false,  // 48516: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpSentences(1) + loopSkip > jumpSentences(3) + newWordFragment': false,  // 48537: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + loopSkip > jumpSentences(3) + progress': false,  // 48538: 'progress' expected [0.8333333333333334], but got [1]
		'skip-combos: jumpSentences(1) + loopSkip > jumpSentences(3) + stopBegin': false,  // 48539: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(1) + loopSkip > jumpSentences(3) + stopFinish': false,  // 48540: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(1) + loopSkip > jumpSentences(3) + done': false,  // 48541: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(1) + loopSkip > jumpSentences(3) + loopSkip': false,  // 48542: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(1) + loopSkip > nextWord(null) + newWordFragment': false,  // 48589: frags expected ["you"], but got ["I"]
		'skip-combos: jumpSentences(1) + loopSkip > nextWord(null) + progress': false,  // 48590: 'progress' expected [0.16666666666666666], but got [0.5]
		'skip-combos: jumpSentences(1) + loopSkip > nextSentence(null) + newWordFragment': false,  // 48615: frags expected ["I"], but got ["Why,"]
		'skip-combos: jumpSentences(1) + loopSkip > nextSentence(null) + progress': false,  // 48616: 'progress' expected [0.4166666666666667,0.5], but got [0.75,0.8333333333333334]
		'skip-combos: jumpSentences(1) + loopSkip > nextSentence(null) + loopSkip': false,  // 48620: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: jumpSentences(1) + loopSkip > prevWord(null) + newWordFragment': false,  // 48641: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(1) + loopSkip > prevWord(null) + progress': false,  // 48642: 'progress' expected [0.08333333333333333], but got [0.3333333333333333,0.25]
		'skip-combos: jumpSentences(1) + loopSkip > prevWord(null) + stopBegin': false,  // 48643: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + loopSkip > prevWord(null) + stopFinish': false,  // 48644: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + loopSkip > prevWord(null) + done': false,  // 48645: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(1) + loopSkip > prevWord(null) + loopSkip': false,  // 48646: frags expected ["Victorious,"], but got ["flag."]

		// jumpSentences(3)
		'skip-combos: jumpSentences(3) + onceBegin > play(null) + loopSkip': false,  // 9464: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(3) + onceBegin > toggle(null) + loopSkip': false,  // 9490: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(3) + onceBegin > rewind(null) + loopSkip': false,  // 9620: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpSentences(3) + onceBegin > fastForward(null) + loopSkip': false,  // 9646: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(3) + onceBegin > once([0,0,-2]) + loopSkip': false,  // 9672: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpSentences(3) + onceBegin > current(null) + loopSkip': false,  // 9724: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(3) + onceBegin > jumpWords(-3) + loopSkip': false,  // 9906: frags expected ["Victorious,"], but got ["come"]
		'skip-combos: jumpSentences(3) + onceBegin > jumpWords(-1) + loopSkip': false,  // 9932: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpSentences(3) + onceBegin > jumpWords(0) + loopSkip': false,  // 9958: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(3) + onceBegin > jumpWords(4) + loopSkip': false,  // 9984: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(3) + onceBegin > jumpSentences(-1) + loopSkip': false,  // 10088: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpSentences(3) + onceBegin > jumpSentences(0) + loopSkip': false,  // 10114: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(3) + onceBegin > jumpSentences(1) + loopSkip': false,  // 10140: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(3) + onceBegin > jumpSentences(3) + loopSkip': false,  // 10166: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(3) + onceBegin > nextSentence(null) + loopSkip': false,  // 10244: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(3) + onceBegin > prevWord(null) + loopSkip': false,  // 10270: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpSentences(3) + onceBegin > prevSentence(null) + loopSkip': false,  // 10296: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpSentences(3) + onceFinish > play(null) + loopSkip': false,  // 10400: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(3) + onceFinish > toggle(null) + loopSkip': false,  // 10426: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(3) + onceFinish > rewind(null) + loopSkip': false,  // 10556: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpSentences(3) + onceFinish > fastForward(null) + loopSkip': false,  // 10582: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(3) + onceFinish > once([0,0,-2]) + loopSkip': false,  // 10608: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpSentences(3) + onceFinish > current(null) + loopSkip': false,  // 10660: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(3) + onceFinish > jumpWords(-3) + loopSkip': false,  // 10842: frags expected ["Victorious,"], but got ["come"]
		'skip-combos: jumpSentences(3) + onceFinish > jumpWords(-1) + loopSkip': false,  // 10868: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpSentences(3) + onceFinish > jumpWords(0) + loopSkip': false,  // 10894: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(3) + onceFinish > jumpWords(4) + loopSkip': false,  // 10920: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(3) + onceFinish > jumpSentences(-1) + loopSkip': false,  // 11024: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpSentences(3) + onceFinish > jumpSentences(0) + loopSkip': false,  // 11050: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(3) + onceFinish > jumpSentences(1) + loopSkip': false,  // 11076: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(3) + onceFinish > jumpSentences(3) + loopSkip': false,  // 11102: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(3) + onceFinish > nextSentence(null) + loopSkip': false,  // 11180: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(3) + onceFinish > prevWord(null) + loopSkip': false,  // 11206: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpSentences(3) + onceFinish > prevSentence(null) + loopSkip': false,  // 11232: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpSentences(3) + loopBegin > play(null) + loopSkip': false,  // 16952: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(3) + loopBegin > toggle(null) + loopSkip': false,  // 16978: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(3) + loopBegin > rewind(null) + loopSkip': false,  // 17108: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpSentences(3) + loopBegin > fastForward(null) + loopSkip': false,  // 17134: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(3) + loopBegin > once([0,0,-2]) + loopSkip': false,  // 17160: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpSentences(3) + loopBegin > current(null) + loopSkip': false,  // 17212: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(3) + loopBegin > jumpWords(-3) + loopSkip': false,  // 17394: frags expected ["Victorious,"], but got ["come"]
		'skip-combos: jumpSentences(3) + loopBegin > jumpWords(-1) + loopSkip': false,  // 17420: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpSentences(3) + loopBegin > jumpWords(0) + loopSkip': false,  // 17446: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(3) + loopBegin > jumpWords(4) + loopSkip': false,  // 17472: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(3) + loopBegin > jumpSentences(-1) + loopSkip': false,  // 17576: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpSentences(3) + loopBegin > jumpSentences(0) + loopSkip': false,  // 17602: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(3) + loopBegin > jumpSentences(1) + loopSkip': false,  // 17628: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(3) + loopBegin > jumpSentences(3) + loopSkip': false,  // 17654: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(3) + loopBegin > nextSentence(null) + loopSkip': false,  // 17732: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(3) + loopBegin > prevWord(null) + loopSkip': false,  // 17758: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpSentences(3) + loopBegin > prevSentence(null) + loopSkip': false,  // 17784: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpSentences(3) + loopFinish > play(null) + loopSkip': false,  // 17888: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(3) + loopFinish > toggle(null) + loopSkip': false,  // 17914: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(3) + loopFinish > rewind(null) + loopSkip': false,  // 18044: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpSentences(3) + loopFinish > fastForward(null) + loopSkip': false,  // 18070: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(3) + loopFinish > once([0,0,-2]) + loopSkip': false,  // 18096: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpSentences(3) + loopFinish > current(null) + loopSkip': false,  // 18148: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(3) + loopFinish > jumpWords(-3) + loopSkip': false,  // 18330: frags expected ["Victorious,"], but got ["come"]
		'skip-combos: jumpSentences(3) + loopFinish > jumpWords(-1) + loopSkip': false,  // 18356: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpSentences(3) + loopFinish > jumpWords(0) + loopSkip': false,  // 18382: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(3) + loopFinish > jumpWords(4) + loopSkip': false,  // 18408: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(3) + loopFinish > jumpSentences(-1) + loopSkip': false,  // 18512: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpSentences(3) + loopFinish > jumpSentences(0) + loopSkip': false,  // 18538: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(3) + loopFinish > jumpSentences(1) + loopSkip': false,  // 18564: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(3) + loopFinish > jumpSentences(3) + loopSkip': false,  // 18590: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(3) + loopFinish > nextSentence(null) + loopSkip': false,  // 18668: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(3) + loopFinish > prevWord(null) + loopSkip': false,  // 18694: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpSentences(3) + loopFinish > prevSentence(null) + loopSkip': false,  // 18720: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpSentences(3) + newWordFragment > play(null) + loopSkip': false,  // 18824: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(3) + newWordFragment > toggle(null) + loopSkip': false,  // 18850: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(3) + newWordFragment > rewind(null) + loopSkip': false,  // 18980: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpSentences(3) + newWordFragment > fastForward(null) + loopSkip': false,  // 19006: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(3) + newWordFragment > once([0,0,-2]) + loopSkip': false,  // 19032: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpSentences(3) + newWordFragment > current(null) + loopSkip': false,  // 19084: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(3) + newWordFragment > jumpWords(-3) + loopSkip': false,  // 19266: frags expected ["Victorious,"], but got ["come"]
		'skip-combos: jumpSentences(3) + newWordFragment > jumpWords(-1) + loopSkip': false,  // 19292: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpSentences(3) + newWordFragment > jumpWords(0) + loopSkip': false,  // 19318: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(3) + newWordFragment > jumpWords(4) + loopSkip': false,  // 19344: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(3) + newWordFragment > jumpSentences(-1) + loopSkip': false,  // 19448: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpSentences(3) + newWordFragment > jumpSentences(0) + loopSkip': false,  // 19474: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(3) + newWordFragment > jumpSentences(1) + loopSkip': false,  // 19500: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(3) + newWordFragment > jumpSentences(3) + loopSkip': false,  // 19526: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(3) + newWordFragment > nextSentence(null) + loopSkip': false,  // 19604: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(3) + newWordFragment > prevWord(null) + loopSkip': false,  // 19630: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpSentences(3) + newWordFragment > prevSentence(null) + loopSkip': false,  // 19656: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpSentences(3) + progress > play(null) + loopSkip': false,  // 19760: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(3) + progress > toggle(null) + loopSkip': false,  // 19786: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(3) + progress > rewind(null) + loopSkip': false,  // 19916: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpSentences(3) + progress > fastForward(null) + loopSkip': false,  // 19942: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(3) + progress > once([0,0,-2]) + loopSkip': false,  // 19968: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpSentences(3) + progress > current(null) + loopSkip': false,  // 20020: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(3) + progress > jumpWords(-3) + loopSkip': false,  // 20202: frags expected ["Victorious,"], but got ["come"]
		'skip-combos: jumpSentences(3) + progress > jumpWords(-1) + loopSkip': false,  // 20228: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpSentences(3) + progress > jumpWords(0) + loopSkip': false,  // 20254: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(3) + progress > jumpWords(4) + loopSkip': false,  // 20280: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(3) + progress > jumpSentences(-1) + loopSkip': false,  // 20384: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpSentences(3) + progress > jumpSentences(0) + loopSkip': false,  // 20410: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(3) + progress > jumpSentences(1) + loopSkip': false,  // 20436: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(3) + progress > jumpSentences(3) + loopSkip': false,  // 20462: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(3) + progress > nextSentence(null) + loopSkip': false,  // 20540: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(3) + progress > prevWord(null) + loopSkip': false,  // 20566: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpSentences(3) + progress > prevSentence(null) + loopSkip': false,  // 20592: frags expected ["Victorious,"], but got ["\n"]

		// jumpSentences(100)
		'skip-combos: jumpSentences(100) + onceBegin > rewind(null) + loopSkip': false,  // 33956: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpSentences(100) + onceBegin > fastForward(null) + loopSkip': false,  // 33982: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + onceBegin > once([0,0,-2]) + loopSkip': false,  // 34008: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + onceBegin > once([0,0,2]) + loopSkip': false,  // 34034: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + onceBegin > current(null) + loopSkip': false,  // 34060: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + onceBegin > jumpWords(-3) + loopSkip': false,  // 34242: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpSentences(100) + onceBegin > jumpWords(-1) + loopSkip': false,  // 34268: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + onceBegin > jumpWords(0) + loopSkip': false,  // 34294: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + onceBegin > jumpWords(4) + loopSkip': false,  // 34320: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + onceBegin > jumpSentences(-1) + loopSkip': false,  // 34424: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + onceBegin > jumpSentences(0) + loopSkip': false,  // 34450: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + onceBegin > jumpSentences(1) + loopSkip': false,  // 34476: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + onceBegin > jumpSentences(3) + loopSkip': false,  // 34502: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + onceBegin > nextWord(null) + loopSkip': false,  // 34554: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + onceBegin > nextSentence(null) + loopSkip': false,  // 34580: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + onceBegin > prevWord(null) + loopSkip': false,  // 34606: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + onceBegin > prevSentence(null) + loopSkip': false,  // 34632: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + onceFinish > rewind(null) + loopSkip': false,  // 34892: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpSentences(100) + onceFinish > fastForward(null) + loopSkip': false,  // 34918: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + onceFinish > once([0,0,-2]) + loopSkip': false,  // 34944: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + onceFinish > once([0,0,2]) + loopSkip': false,  // 34970: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + onceFinish > current(null) + loopSkip': false,  // 34996: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + onceFinish > jumpWords(-3) + loopSkip': false,  // 35178: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpSentences(100) + onceFinish > jumpWords(-1) + loopSkip': false,  // 35204: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + onceFinish > jumpWords(0) + loopSkip': false,  // 35230: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + onceFinish > jumpWords(4) + loopSkip': false,  // 35256: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + onceFinish > jumpSentences(-1) + loopSkip': false,  // 35360: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + onceFinish > jumpSentences(0) + loopSkip': false,  // 35386: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + onceFinish > jumpSentences(1) + loopSkip': false,  // 35412: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + onceFinish > jumpSentences(3) + loopSkip': false,  // 35438: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + onceFinish > nextWord(null) + loopSkip': false,  // 35490: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + onceFinish > nextSentence(null) + loopSkip': false,  // 35516: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + onceFinish > prevWord(null) + loopSkip': false,  // 35542: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + onceFinish > prevSentence(null) + loopSkip': false,  // 35568: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopBegin > rewind(null) + loopSkip': false,  // 41444: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpSentences(100) + loopBegin > fastForward(null) + loopSkip': false,  // 41470: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + loopBegin > once([0,0,-2]) + loopSkip': false,  // 41496: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopBegin > once([0,0,2]) + loopSkip': false,  // 41522: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopBegin > current(null) + loopSkip': false,  // 41548: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + loopBegin > jumpWords(-3) + loopSkip': false,  // 41730: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpSentences(100) + loopBegin > jumpWords(-1) + loopSkip': false,  // 41756: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopBegin > jumpWords(0) + loopSkip': false,  // 41782: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + loopBegin > jumpWords(4) + loopSkip': false,  // 41808: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + loopBegin > jumpSentences(-1) + loopSkip': false,  // 41912: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopBegin > jumpSentences(0) + loopSkip': false,  // 41938: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + loopBegin > jumpSentences(1) + loopSkip': false,  // 41964: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + loopBegin > jumpSentences(3) + loopSkip': false,  // 41990: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopBegin > nextWord(null) + loopSkip': false,  // 42042: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopBegin > nextSentence(null) + loopSkip': false,  // 42068: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + loopBegin > prevWord(null) + loopSkip': false,  // 42094: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopBegin > prevSentence(null) + loopSkip': false,  // 42120: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopFinish > rewind(null) + loopSkip': false,  // 42380: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpSentences(100) + loopFinish > fastForward(null) + loopSkip': false,  // 42406: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + loopFinish > once([0,0,-2]) + loopSkip': false,  // 42432: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopFinish > once([0,0,2]) + loopSkip': false,  // 42458: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopFinish > current(null) + loopSkip': false,  // 42484: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + loopFinish > jumpWords(-3) + loopSkip': false,  // 42666: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpSentences(100) + loopFinish > jumpWords(-1) + loopSkip': false,  // 42692: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopFinish > jumpWords(0) + loopSkip': false,  // 42718: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + loopFinish > jumpWords(4) + loopSkip': false,  // 42744: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + loopFinish > jumpSentences(-1) + loopSkip': false,  // 42848: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopFinish > jumpSentences(0) + loopSkip': false,  // 42874: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + loopFinish > jumpSentences(1) + loopSkip': false,  // 42900: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + loopFinish > jumpSentences(3) + loopSkip': false,  // 42926: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopFinish > nextWord(null) + loopSkip': false,  // 42978: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopFinish > nextSentence(null) + loopSkip': false,  // 43004: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + loopFinish > prevWord(null) + loopSkip': false,  // 43030: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopFinish > prevSentence(null) + loopSkip': false,  // 43056: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + progress > rewind(null) + loopSkip': false,  // 44252: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpSentences(100) + progress > fastForward(null) + loopSkip': false,  // 44278: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + progress > once([0,0,-2]) + loopSkip': false,  // 44304: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + progress > once([0,0,2]) + loopSkip': false,  // 44330: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + progress > current(null) + loopSkip': false,  // 44356: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + progress > jumpWords(-3) + loopSkip': false,  // 44538: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpSentences(100) + progress > jumpWords(-1) + loopSkip': false,  // 44564: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + progress > jumpWords(0) + loopSkip': false,  // 44590: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + progress > jumpWords(4) + loopSkip': false,  // 44616: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + progress > jumpSentences(-1) + loopSkip': false,  // 44720: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + progress > jumpSentences(0) + loopSkip': false,  // 44746: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + progress > jumpSentences(1) + loopSkip': false,  // 44772: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + progress > jumpSentences(3) + loopSkip': false,  // 44798: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + progress > nextWord(null) + loopSkip': false,  // 44850: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + progress > nextSentence(null) + loopSkip': false,  // 44876: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + progress > prevWord(null) + loopSkip': false,  // 44902: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + progress > prevSentence(null) + loopSkip': false,  // 44928: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + stopBegin > rewind(null) + loopSkip': false,  // 45188: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpSentences(100) + stopBegin > fastForward(null) + loopSkip': false,  // 45214: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + stopBegin > once([0,0,-2]) + loopSkip': false,  // 45240: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + stopBegin > once([0,0,2]) + loopSkip': false,  // 45266: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + stopBegin > current(null) + loopSkip': false,  // 45292: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + stopBegin > jumpWords(-3) + loopSkip': false,  // 45474: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpSentences(100) + stopBegin > jumpWords(-1) + loopSkip': false,  // 45500: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + stopBegin > jumpWords(0) + loopSkip': false,  // 45526: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + stopBegin > jumpWords(4) + loopSkip': false,  // 45552: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + stopBegin > jumpSentences(-1) + loopSkip': false,  // 45656: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + stopBegin > jumpSentences(0) + loopSkip': false,  // 45682: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + stopBegin > jumpSentences(1) + loopSkip': false,  // 45708: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + stopBegin > jumpSentences(3) + loopSkip': false,  // 45734: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + stopBegin > nextWord(null) + loopSkip': false,  // 45786: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + stopBegin > nextSentence(null) + loopSkip': false,  // 45812: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + stopBegin > prevWord(null) + loopSkip': false,  // 45838: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + stopBegin > prevSentence(null) + loopSkip': false,  // 45864: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + stopFinish > rewind(null) + loopSkip': false,  // 46124: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpSentences(100) + stopFinish > fastForward(null) + loopSkip': false,  // 46150: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + stopFinish > once([0,0,-2]) + loopSkip': false,  // 46176: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + stopFinish > once([0,0,2]) + loopSkip': false,  // 46202: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + stopFinish > current(null) + loopSkip': false,  // 46228: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + stopFinish > jumpWords(-3) + loopSkip': false,  // 46410: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpSentences(100) + stopFinish > jumpWords(-1) + loopSkip': false,  // 46436: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + stopFinish > jumpWords(0) + loopSkip': false,  // 46462: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + stopFinish > jumpWords(4) + loopSkip': false,  // 46488: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + stopFinish > jumpSentences(-1) + loopSkip': false,  // 46592: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + stopFinish > jumpSentences(0) + loopSkip': false,  // 46618: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + stopFinish > jumpSentences(1) + loopSkip': false,  // 46644: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + stopFinish > jumpSentences(3) + loopSkip': false,  // 46670: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + stopFinish > nextWord(null) + loopSkip': false,  // 46722: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + stopFinish > nextSentence(null) + loopSkip': false,  // 46748: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + stopFinish > prevWord(null) + loopSkip': false,  // 46774: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + stopFinish > prevSentence(null) + loopSkip': false,  // 46800: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + done > rewind(null) + loopSkip': false,  // 47060: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpSentences(100) + done > fastForward(null) + loopSkip': false,  // 47086: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + done > once([0,0,-2]) + loopSkip': false,  // 47112: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + done > once([0,0,2]) + loopSkip': false,  // 47138: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + done > current(null) + loopSkip': false,  // 47164: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + done > jumpWords(-3) + loopSkip': false,  // 47346: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpSentences(100) + done > jumpWords(-1) + loopSkip': false,  // 47372: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + done > jumpWords(0) + loopSkip': false,  // 47398: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + done > jumpWords(4) + loopSkip': false,  // 47424: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + done > jumpSentences(-1) + loopSkip': false,  // 47528: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + done > jumpSentences(0) + loopSkip': false,  // 47554: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + done > jumpSentences(1) + loopSkip': false,  // 47580: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + done > jumpSentences(3) + loopSkip': false,  // 47606: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + done > nextWord(null) + loopSkip': false,  // 47658: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + done > nextSentence(null) + loopSkip': false,  // 47684: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + done > prevWord(null) + loopSkip': false,  // 47710: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + done > prevSentence(null) + loopSkip': false,  // 47736: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopSkip > play(null) + playBegin': false,  // 47815: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopSkip > play(null) + playFinish': false,  // 47816: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopSkip > play(null) + restartBegin': false,  // 47819: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > play(null) + restartFinish': false,  // 47820: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > toggle(null) + playBegin': false,  // 47841: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopSkip > toggle(null) + playFinish': false,  // 47842: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopSkip > toggle(null) + restartBegin': false,  // 47845: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > toggle(null) + restartFinish': false,  // 47846: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > rewind(null) + newWordFragment': false,  // 47991: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > rewind(null) + progress': false,  // 47992: 'progress' expected [0.08333333333333333], but got [0.9166666666666666,0.8333333333333334,0.75,0.6666666666666666,0.5833333333333334,0.5,0.4166666666666667,0.3333333333333333,0.25,0.16666666666666666,0.08333333333333333]
		'skip-combos: jumpSentences(100) + loopSkip > rewind(null) + loopSkip': false,  // 47996: frags expected ["Victorious,"], but got ["\n","come","Delirious,","flag.","Victorious,"]
		'skip-combos: jumpSentences(100) + loopSkip > fastForward(null) + newWordFragment': false,  // 48017: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopSkip > fastForward(null) + progress': false,  // 48018: 'progress' expected [0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [1]
		'skip-combos: jumpSentences(100) + loopSkip > fastForward(null) + loopSkip': false,  // 48022: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + loopSkip > once([0,0,-2]) + newWordFragment': false,  // 48043: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > once([0,0,-2]) + progress': false,  // 48044: 'progress' expected [0.08333333333333333], but got [0.9166666666666666]
		'skip-combos: jumpSentences(100) + loopSkip > once([0,0,-2]) + stopBegin': false,  // 48045: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopSkip > once([0,0,-2]) + stopFinish': false,  // 48046: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopSkip > once([0,0,-2]) + done': false,  // 48047: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopSkip > once([0,0,-2]) + loopSkip': false,  // 48048: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopSkip > once([0,0,2]) + newWordFragment': false,  // 48069: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopSkip > once([0,0,2]) + progress': false,  // 48070: 'progress' expected [0.16666666666666666], but got [1]
		'skip-combos: jumpSentences(100) + loopSkip > once([0,0,2]) + stopBegin': false,  // 48071: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > once([0,0,2]) + stopFinish': false,  // 48072: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > once([0,0,2]) + done': false,  // 48073: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > once([0,0,2]) + loopSkip': false,  // 48074: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > current(null) + newWordFragment': false,  // 48095: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopSkip > current(null) + progress': false,  // 48096: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [1]
		'skip-combos: jumpSentences(100) + loopSkip > current(null) + stopBegin': false,  // 48097: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > current(null) + stopFinish': false,  // 48098: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > current(null) + done': false,  // 48099: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > current(null) + loopSkip': false,  // 48100: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + loopSkip > jumpWords(-3) + newWordFragment': false,  // 48277: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > jumpWords(-3) + progress': false,  // 48278: 'progress' expected [0.08333333333333333], but got [0.75,0.6666666666666666]
		'skip-combos: jumpSentences(100) + loopSkip > jumpWords(-3) + stopBegin': false,  // 48279: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopSkip > jumpWords(-3) + stopFinish': false,  // 48280: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopSkip > jumpWords(-3) + done': false,  // 48281: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopSkip > jumpWords(-3) + loopSkip': false,  // 48282: frags expected ["Victorious,"], but got ["\n"]
		'skip-combos: jumpSentences(100) + loopSkip > jumpWords(-1) + newWordFragment': false,  // 48303: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > jumpWords(-1) + progress': false,  // 48304: 'progress' expected [0.08333333333333333], but got [0.9166666666666666]
		'skip-combos: jumpSentences(100) + loopSkip > jumpWords(-1) + stopBegin': false,  // 48305: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopSkip > jumpWords(-1) + stopFinish': false,  // 48306: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopSkip > jumpWords(-1) + done': false,  // 48307: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopSkip > jumpWords(-1) + loopSkip': false,  // 48308: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopSkip > jumpWords(0) + newWordFragment': false,  // 48329: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopSkip > jumpWords(0) + progress': false,  // 48330: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [1]
		'skip-combos: jumpSentences(100) + loopSkip > jumpWords(0) + stopBegin': false,  // 48331: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > jumpWords(0) + stopFinish': false,  // 48332: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > jumpWords(0) + done': false,  // 48333: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > jumpWords(0) + loopSkip': false,  // 48334: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + loopSkip > jumpWords(4) + newWordFragment': false,  // 48355: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopSkip > jumpWords(4) + progress': false,  // 48356: 'progress' expected [0.4166666666666667,0.5], but got [1]
		'skip-combos: jumpSentences(100) + loopSkip > jumpWords(4) + stopBegin': false,  // 48357: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > jumpWords(4) + stopFinish': false,  // 48358: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > jumpWords(4) + done': false,  // 48359: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > jumpWords(4) + loopSkip': false,  // 48360: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + loopSkip > jumpSentences(-1) + newWordFragment': false,  // 48459: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > jumpSentences(-1) + progress': false,  // 48460: 'progress' expected [0.08333333333333333], but got [0.8333333333333334]
		'skip-combos: jumpSentences(100) + loopSkip > jumpSentences(-1) + stopBegin': false,  // 48461: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopSkip > jumpSentences(-1) + stopFinish': false,  // 48462: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopSkip > jumpSentences(-1) + done': false,  // 48463: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopSkip > jumpSentences(-1) + loopSkip': false,  // 48464: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopSkip > jumpSentences(0) + newWordFragment': false,  // 48485: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopSkip > jumpSentences(0) + progress': false,  // 48486: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [1]
		'skip-combos: jumpSentences(100) + loopSkip > jumpSentences(0) + stopBegin': false,  // 48487: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > jumpSentences(0) + stopFinish': false,  // 48488: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > jumpSentences(0) + done': false,  // 48489: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > jumpSentences(0) + loopSkip': false,  // 48490: frags expected ["Victorious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + loopSkip > jumpSentences(1) + newWordFragment': false,  // 48511: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopSkip > jumpSentences(1) + progress': false,  // 48512: 'progress' expected [0.4166666666666667,0.5], but got [1]
		'skip-combos: jumpSentences(100) + loopSkip > jumpSentences(1) + stopBegin': false,  // 48513: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > jumpSentences(1) + stopFinish': false,  // 48514: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > jumpSentences(1) + done': false,  // 48515: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > jumpSentences(1) + loopSkip': false,  // 48516: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + loopSkip > jumpSentences(3) + newWordFragment': false,  // 48537: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopSkip > jumpSentences(3) + progress': false,  // 48538: 'progress' expected [0.8333333333333334], but got [1]
		'skip-combos: jumpSentences(100) + loopSkip > jumpSentences(3) + stopBegin': false,  // 48539: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > jumpSentences(3) + stopFinish': false,  // 48540: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > jumpSentences(3) + done': false,  // 48541: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > jumpSentences(3) + loopSkip': false,  // 48542: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > nextWord(null) + newWordFragment': false,  // 48589: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopSkip > nextWord(null) + progress': false,  // 48590: 'progress' expected [0.16666666666666666], but got [1]
		'skip-combos: jumpSentences(100) + loopSkip > nextWord(null) + stopBegin': false,  // 48591: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > nextWord(null) + stopFinish': false,  // 48592: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > nextWord(null) + done': false,  // 48593: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > nextWord(null) + loopSkip': false,  // 48594: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > nextSentence(null) + newWordFragment': false,  // 48615: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopSkip > nextSentence(null) + progress': false,  // 48616: 'progress' expected [0.4166666666666667,0.5], but got [1]
		'skip-combos: jumpSentences(100) + loopSkip > nextSentence(null) + stopBegin': false,  // 48617: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > nextSentence(null) + stopFinish': false,  // 48618: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > nextSentence(null) + done': false,  // 48619: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > nextSentence(null) + loopSkip': false,  // 48620: frags expected ["Delirious,"], but got ["wattlebird?"]
		'skip-combos: jumpSentences(100) + loopSkip > prevWord(null) + newWordFragment': false,  // 48641: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > prevWord(null) + progress': false,  // 48642: 'progress' expected [0.08333333333333333], but got [0.9166666666666666]
		'skip-combos: jumpSentences(100) + loopSkip > prevWord(null) + stopBegin': false,  // 48643: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopSkip > prevWord(null) + stopFinish': false,  // 48644: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopSkip > prevWord(null) + done': false,  // 48645: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopSkip > prevWord(null) + loopSkip': false,  // 48646: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopSkip > prevSentence(null) + newWordFragment': false,  // 48667: event should not have been triggerd but WAS
		'skip-combos: jumpSentences(100) + loopSkip > prevSentence(null) + progress': false,  // 48668: 'progress' expected [0.08333333333333333], but got [0.8333333333333334]
		'skip-combos: jumpSentences(100) + loopSkip > prevSentence(null) + stopBegin': false,  // 48669: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopSkip > prevSentence(null) + stopFinish': false,  // 48670: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopSkip > prevSentence(null) + done': false,  // 48671: event should have been triggerd but was NOT
		'skip-combos: jumpSentences(100) + loopSkip > prevSentence(null) + loopSkip': false,  // 48672: event should have been triggerd but was NOT

		// nextWord(null)
		'skip-combos: nextWord(null) + onceBegin > play(null) + loopSkip': false,  // 9464: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?"]
		'skip-combos: nextWord(null) + onceBegin > toggle(null) + loopSkip': false,  // 9490: event should have been triggerd but was NOT
		'skip-combos: nextWord(null) + onceBegin > current(null) + loopSkip': false,  // 9724: event should have been triggerd but was NOT
		'skip-combos: nextWord(null) + onceBegin > jumpWords(0) + loopSkip': false,  // 9958: event should have been triggerd but was NOT
		'skip-combos: nextWord(null) + onceBegin > jumpWords(4) + loopSkip': false,  // 9984: event should have been triggerd but was NOT
		'skip-combos: nextWord(null) + onceBegin > jumpSentences(0) + loopSkip': false,  // 10114: event should have been triggerd but was NOT
		'skip-combos: nextWord(null) + onceFinish > play(null) + loopSkip': false,  // 10400: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?"]
		'skip-combos: nextWord(null) + onceFinish > toggle(null) + loopSkip': false,  // 10426: event should have been triggerd but was NOT
		'skip-combos: nextWord(null) + onceFinish > current(null) + loopSkip': false,  // 10660: event should have been triggerd but was NOT
		'skip-combos: nextWord(null) + onceFinish > jumpWords(0) + loopSkip': false,  // 10894: event should have been triggerd but was NOT
		'skip-combos: nextWord(null) + onceFinish > jumpWords(4) + loopSkip': false,  // 10920: event should have been triggerd but was NOT
		'skip-combos: nextWord(null) + onceFinish > jumpSentences(0) + loopSkip': false,  // 11050: event should have been triggerd but was NOT
		'skip-combos: nextWord(null) + loopBegin > play(null) + loopSkip': false,  // 16952: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?"]
		'skip-combos: nextWord(null) + loopBegin > toggle(null) + loopSkip': false,  // 16978: event should have been triggerd but was NOT
		'skip-combos: nextWord(null) + loopBegin > current(null) + loopSkip': false,  // 17212: event should have been triggerd but was NOT
		'skip-combos: nextWord(null) + loopBegin > jumpWords(0) + loopSkip': false,  // 17446: event should have been triggerd but was NOT
		'skip-combos: nextWord(null) + loopBegin > jumpWords(4) + loopSkip': false,  // 17472: event should have been triggerd but was NOT
		'skip-combos: nextWord(null) + loopBegin > jumpSentences(0) + loopSkip': false,  // 17602: event should have been triggerd but was NOT
		'skip-combos: nextWord(null) + loopFinish > play(null) + loopSkip': false,  // 17888: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?"]
		'skip-combos: nextWord(null) + loopFinish > toggle(null) + loopSkip': false,  // 17914: event should have been triggerd but was NOT
		'skip-combos: nextWord(null) + loopFinish > current(null) + loopSkip': false,  // 18148: event should have been triggerd but was NOT
		'skip-combos: nextWord(null) + loopFinish > jumpWords(0) + loopSkip': false,  // 18382: event should have been triggerd but was NOT
		'skip-combos: nextWord(null) + loopFinish > jumpWords(4) + loopSkip': false,  // 18408: event should have been triggerd but was NOT
		'skip-combos: nextWord(null) + loopFinish > jumpSentences(0) + loopSkip': false,  // 18538: event should have been triggerd but was NOT
		'skip-combos: nextWord(null) + newWordFragment > play(null) + loopSkip': false,  // 18824: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?"]
		'skip-combos: nextWord(null) + newWordFragment > toggle(null) + loopSkip': false,  // 18850: event should have been triggerd but was NOT
		'skip-combos: nextWord(null) + newWordFragment > current(null) + loopSkip': false,  // 19084: event should have been triggerd but was NOT
		'skip-combos: nextWord(null) + newWordFragment > jumpWords(0) + loopSkip': false,  // 19318: event should have been triggerd but was NOT
		'skip-combos: nextWord(null) + newWordFragment > jumpWords(4) + loopSkip': false,  // 19344: event should have been triggerd but was NOT
		'skip-combos: nextWord(null) + newWordFragment > jumpSentences(0) + loopSkip': false,  // 19474: event should have been triggerd but was NOT
		'skip-combos: nextWord(null) + progress > play(null) + loopSkip': false,  // 19760: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["flag.","Delirious,","come","\n","wattlebird?"]
		'skip-combos: nextWord(null) + progress > toggle(null) + loopSkip': false,  // 19786: event should have been triggerd but was NOT
		'skip-combos: nextWord(null) + progress > current(null) + loopSkip': false,  // 20020: event should have been triggerd but was NOT
		'skip-combos: nextWord(null) + progress > jumpWords(0) + loopSkip': false,  // 20254: event should have been triggerd but was NOT
		'skip-combos: nextWord(null) + progress > jumpWords(4) + loopSkip': false,  // 20280: event should have been triggerd but was NOT
		'skip-combos: nextWord(null) + progress > jumpSentences(0) + loopSkip': false,  // 20410: event should have been triggerd but was NOT

		// nextSentence(null)
		'skip-combos: nextSentence(null) + onceBegin > play(null) + loopSkip': false,  // 33800: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["Delirious,","come","\n","wattlebird?"]
		'skip-combos: nextSentence(null) + onceBegin > toggle(null) + loopSkip': false,  // 33826: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + onceBegin > rewind(null) + loopSkip': false,  // 33956: frags expected ["Victorious,"], but got ["flag.","Victorious,"]
		'skip-combos: nextSentence(null) + onceBegin > fastForward(null) + loopSkip': false,  // 33982: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["come","\n","wattlebird?"]
		'skip-combos: nextSentence(null) + onceBegin > once([0,0,-2]) + loopSkip': false,  // 34008: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: nextSentence(null) + onceBegin > current(null) + loopSkip': false,  // 34060: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: nextSentence(null) + onceBegin > jumpWords(-3) + loopSkip': false,  // 34242: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + onceBegin > jumpWords(-1) + loopSkip': false,  // 34268: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: nextSentence(null) + onceBegin > jumpWords(0) + loopSkip': false,  // 34294: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: nextSentence(null) + onceBegin > jumpWords(4) + loopSkip': false,  // 34320: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: nextSentence(null) + onceBegin > jumpSentences(0) + loopSkip': false,  // 34450: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: nextSentence(null) + onceBegin > jumpSentences(1) + loopSkip': false,  // 34476: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: nextSentence(null) + onceBegin > jumpSentences(3) + loopSkip': false,  // 34502: event should not have been triggerd but WAS
		'skip-combos: nextSentence(null) + onceBegin > nextSentence(null) + loopSkip': false,  // 34580: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: nextSentence(null) + onceBegin > prevWord(null) + loopSkip': false,  // 34606: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: nextSentence(null) + onceFinish > play(null) + loopSkip': false,  // 34736: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["Delirious,","come","\n","wattlebird?"]
		'skip-combos: nextSentence(null) + onceFinish > toggle(null) + loopSkip': false,  // 34762: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + onceFinish > rewind(null) + loopSkip': false,  // 34892: frags expected ["Victorious,"], but got ["flag.","Victorious,"]
		'skip-combos: nextSentence(null) + onceFinish > fastForward(null) + loopSkip': false,  // 34918: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["come","\n","wattlebird?"]
		'skip-combos: nextSentence(null) + onceFinish > once([0,0,-2]) + loopSkip': false,  // 34944: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: nextSentence(null) + onceFinish > current(null) + loopSkip': false,  // 34996: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: nextSentence(null) + onceFinish > jumpWords(-3) + loopSkip': false,  // 35178: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + onceFinish > jumpWords(-1) + loopSkip': false,  // 35204: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: nextSentence(null) + onceFinish > jumpWords(0) + loopSkip': false,  // 35230: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: nextSentence(null) + onceFinish > jumpWords(4) + loopSkip': false,  // 35256: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: nextSentence(null) + onceFinish > jumpSentences(0) + loopSkip': false,  // 35386: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: nextSentence(null) + onceFinish > jumpSentences(1) + loopSkip': false,  // 35412: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: nextSentence(null) + onceFinish > jumpSentences(3) + loopSkip': false,  // 35438: event should not have been triggerd but WAS
		'skip-combos: nextSentence(null) + onceFinish > nextSentence(null) + loopSkip': false,  // 35516: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: nextSentence(null) + onceFinish > prevWord(null) + loopSkip': false,  // 35542: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: nextSentence(null) + loopBegin > play(null) + loopSkip': false,  // 41288: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["Delirious,","come","\n","wattlebird?"]
		'skip-combos: nextSentence(null) + loopBegin > toggle(null) + loopSkip': false,  // 41314: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + loopBegin > rewind(null) + loopSkip': false,  // 41444: frags expected ["Victorious,"], but got ["flag.","Victorious,"]
		'skip-combos: nextSentence(null) + loopBegin > fastForward(null) + loopSkip': false,  // 41470: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["come","\n","wattlebird?"]
		'skip-combos: nextSentence(null) + loopBegin > once([0,0,-2]) + loopSkip': false,  // 41496: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: nextSentence(null) + loopBegin > current(null) + loopSkip': false,  // 41548: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: nextSentence(null) + loopBegin > jumpWords(-3) + loopSkip': false,  // 41730: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + loopBegin > jumpWords(-1) + loopSkip': false,  // 41756: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: nextSentence(null) + loopBegin > jumpWords(0) + loopSkip': false,  // 41782: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: nextSentence(null) + loopBegin > jumpWords(4) + loopSkip': false,  // 41808: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: nextSentence(null) + loopBegin > jumpSentences(0) + loopSkip': false,  // 41938: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: nextSentence(null) + loopBegin > jumpSentences(1) + loopSkip': false,  // 41964: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: nextSentence(null) + loopBegin > jumpSentences(3) + loopSkip': false,  // 41990: event should not have been triggerd but WAS
		'skip-combos: nextSentence(null) + loopBegin > nextSentence(null) + loopSkip': false,  // 42068: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: nextSentence(null) + loopBegin > prevWord(null) + loopSkip': false,  // 42094: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: nextSentence(null) + loopFinish > play(null) + loopSkip': false,  // 42224: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["Delirious,","come","\n","wattlebird?"]
		'skip-combos: nextSentence(null) + loopFinish > toggle(null) + loopSkip': false,  // 42250: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + loopFinish > rewind(null) + loopSkip': false,  // 42380: frags expected ["Victorious,"], but got ["flag.","Victorious,"]
		'skip-combos: nextSentence(null) + loopFinish > fastForward(null) + loopSkip': false,  // 42406: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["come","\n","wattlebird?"]
		'skip-combos: nextSentence(null) + loopFinish > once([0,0,-2]) + loopSkip': false,  // 42432: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: nextSentence(null) + loopFinish > current(null) + loopSkip': false,  // 42484: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: nextSentence(null) + loopFinish > jumpWords(-3) + loopSkip': false,  // 42666: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + loopFinish > jumpWords(-1) + loopSkip': false,  // 42692: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: nextSentence(null) + loopFinish > jumpWords(0) + loopSkip': false,  // 42718: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: nextSentence(null) + loopFinish > jumpWords(4) + loopSkip': false,  // 42744: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: nextSentence(null) + loopFinish > jumpSentences(0) + loopSkip': false,  // 42874: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: nextSentence(null) + loopFinish > jumpSentences(1) + loopSkip': false,  // 42900: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: nextSentence(null) + loopFinish > jumpSentences(3) + loopSkip': false,  // 42926: event should not have been triggerd but WAS
		'skip-combos: nextSentence(null) + loopFinish > nextSentence(null) + loopSkip': false,  // 43004: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: nextSentence(null) + loopFinish > prevWord(null) + loopSkip': false,  // 43030: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: nextSentence(null) + newWordFragment > play(null) + loopSkip': false,  // 43160: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["come","\n","wattlebird?"]
		'skip-combos: nextSentence(null) + newWordFragment > toggle(null) + loopSkip': false,  // 43186: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + newWordFragment > rewind(null) + loopSkip': false,  // 43316: frags expected ["Victorious,"], but got ["Delirious,","flag.","Victorious,"]
		'skip-combos: nextSentence(null) + newWordFragment > fastForward(null) + loopSkip': false,  // 43342: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["come","\n","wattlebird?"]
		'skip-combos: nextSentence(null) + newWordFragment > once([0,0,-2]) + loopSkip': false,  // 43368: frags expected ["Victorious,"], but got ["Delirious,","flag."]
		'skip-combos: nextSentence(null) + newWordFragment > once([0,0,2]) + loopSkip': false,  // 43394: event should not have been triggerd but WAS
		'skip-combos: nextSentence(null) + newWordFragment > current(null) + loopSkip': false,  // 43420: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + newWordFragment > jumpWords(-3) + loopSkip': false,  // 43602: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + newWordFragment > jumpWords(-1) + loopSkip': false,  // 43628: frags expected ["Victorious,"], but got ["Delirious,","flag."]
		'skip-combos: nextSentence(null) + newWordFragment > jumpWords(0) + loopSkip': false,  // 43654: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + newWordFragment > jumpWords(4) + loopSkip': false,  // 43680: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + newWordFragment > jumpSentences(-1) + newWordFragment': false,  // 43779: event should not have been triggerd but WAS
		'skip-combos: nextSentence(null) + newWordFragment > jumpSentences(-1) + progress': false,  // 43780: 'progress' expected [0.08333333333333333], but got [0.4166666666666667,0.3333333333333333,0.25]
		'skip-combos: nextSentence(null) + newWordFragment > jumpSentences(-1) + stopBegin': false,  // 43781: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + newWordFragment > jumpSentences(-1) + stopFinish': false,  // 43782: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + newWordFragment > jumpSentences(-1) + done': false,  // 43783: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + newWordFragment > jumpSentences(-1) + loopSkip': false,  // 43784: frags expected ["Victorious,"], but got ["Delirious,","flag."]
		'skip-combos: nextSentence(null) + newWordFragment > jumpSentences(0) + loopSkip': false,  // 43810: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + newWordFragment > jumpSentences(1) + loopSkip': false,  // 43836: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: nextSentence(null) + newWordFragment > jumpSentences(3) + loopSkip': false,  // 43862: event should not have been triggerd but WAS
		'skip-combos: nextSentence(null) + newWordFragment > nextWord(null) + loopSkip': false,  // 43914: event should not have been triggerd but WAS
		'skip-combos: nextSentence(null) + newWordFragment > nextSentence(null) + loopSkip': false,  // 43940: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: nextSentence(null) + newWordFragment > prevWord(null) + loopSkip': false,  // 43966: frags expected ["Victorious,"], but got ["Delirious,","flag."]
		'skip-combos: nextSentence(null) + newWordFragment > prevSentence(null) + newWordFragment': false,  // 43987: event should not have been triggerd but WAS
		'skip-combos: nextSentence(null) + newWordFragment > prevSentence(null) + progress': false,  // 43988: 'progress' expected [0.08333333333333333], but got [0.4166666666666667,0.3333333333333333,0.25]
		'skip-combos: nextSentence(null) + newWordFragment > prevSentence(null) + stopBegin': false,  // 43989: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + newWordFragment > prevSentence(null) + stopFinish': false,  // 43990: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + newWordFragment > prevSentence(null) + done': false,  // 43991: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + newWordFragment > prevSentence(null) + loopSkip': false,  // 43992: frags expected ["Victorious,"], but got ["Delirious,","flag."]
		'skip-combos: nextSentence(null) + progress > play(null) + loopSkip': false,  // 44096: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["Delirious,","come","\n","wattlebird?"]
		'skip-combos: nextSentence(null) + progress > toggle(null) + loopSkip': false,  // 44122: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + progress > rewind(null) + loopSkip': false,  // 44252: frags expected ["Victorious,"], but got ["flag.","Victorious,"]
		'skip-combos: nextSentence(null) + progress > fastForward(null) + loopSkip': false,  // 44278: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["come","\n","wattlebird?"]
		'skip-combos: nextSentence(null) + progress > once([0,0,-2]) + loopSkip': false,  // 44304: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: nextSentence(null) + progress > current(null) + loopSkip': false,  // 44356: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: nextSentence(null) + progress > jumpWords(-3) + loopSkip': false,  // 44538: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + progress > jumpWords(-1) + loopSkip': false,  // 44564: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: nextSentence(null) + progress > jumpWords(0) + loopSkip': false,  // 44590: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: nextSentence(null) + progress > jumpWords(4) + loopSkip': false,  // 44616: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: nextSentence(null) + progress > jumpSentences(0) + loopSkip': false,  // 44746: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: nextSentence(null) + progress > jumpSentences(1) + loopSkip': false,  // 44772: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: nextSentence(null) + progress > jumpSentences(3) + loopSkip': false,  // 44798: event should not have been triggerd but WAS
		'skip-combos: nextSentence(null) + progress > nextSentence(null) + loopSkip': false,  // 44876: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: nextSentence(null) + progress > prevWord(null) + loopSkip': false,  // 44902: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: nextSentence(null) + loopSkip > play(null) + newWordFragment': false,  // 47835: frags expected ["you","brave","I","back.","Why,","oh"], but got ["I","back.","Why,","oh"]
		'skip-combos: nextSentence(null) + loopSkip > play(null) + progress': false,  // 47836: 'progress' expected [0.08333333333333333,0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1]
		'skip-combos: nextSentence(null) + loopSkip > play(null) + loopSkip': false,  // 47840: frags expected ["Victorious,","flag.","Delirious,","come","\n","wattlebird?"], but got ["Delirious,","come","\n","wattlebird?"]
		'skip-combos: nextSentence(null) + loopSkip > toggle(null) + playBegin': false,  // 47841: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + loopSkip > toggle(null) + playFinish': false,  // 47842: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + loopSkip > toggle(null) + pauseBegin': false,  // 47847: event should not have been triggerd but WAS
		'skip-combos: nextSentence(null) + loopSkip > toggle(null) + pauseFinish': false,  // 47848: event should not have been triggerd but WAS
		'skip-combos: nextSentence(null) + loopSkip > toggle(null) + revertBegin': false,  // 47853: event should not have been triggerd but WAS
		'skip-combos: nextSentence(null) + loopSkip > toggle(null) + revertFinish': false,  // 47854: event should not have been triggerd but WAS
		'skip-combos: nextSentence(null) + loopSkip > toggle(null) + loopBegin': false,  // 47859: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + loopSkip > toggle(null) + loopFinish': false,  // 47860: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + loopSkip > toggle(null) + newWordFragment': false,  // 47861: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + loopSkip > toggle(null) + progress': false,  // 47862: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + loopSkip > toggle(null) + stopBegin': false,  // 47863: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + loopSkip > toggle(null) + stopFinish': false,  // 47864: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + loopSkip > toggle(null) + done': false,  // 47865: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + loopSkip > toggle(null) + loopSkip': false,  // 47866: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + loopSkip > rewind(null) + newWordFragment': false,  // 47991: event should not have been triggerd but WAS
		'skip-combos: nextSentence(null) + loopSkip > rewind(null) + progress': false,  // 47992: 'progress' expected [0.08333333333333333], but got [0.3333333333333333,0.25,0.16666666666666666,0.08333333333333333]
		// False negative somehow. Needed more time?
		// 'skip-combos: nextSentence(null) + loopSkip > rewind(null) + stopFinish': false,  // 47994: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + loopSkip > rewind(null) + loopSkip': false,  // 47996: frags expected ["Victorious,"], but got ["flag.","Victorious,"]
		'skip-combos: nextSentence(null) + loopSkip > fastForward(null) + newWordFragment': false,  // 48017: frags expected ["you","brave","I","back.","Why,","oh"], but got ["I","back.","Why,","oh"]
		'skip-combos: nextSentence(null) + loopSkip > fastForward(null) + progress': false,  // 48018: 'progress' expected [0.16666666666666666,0.25,0.3333333333333333,0.4166666666666667,0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1], but got [0.5,0.5833333333333334,0.6666666666666666,0.75,0.8333333333333334,0.9166666666666666,1]
		'skip-combos: nextSentence(null) + loopSkip > fastForward(null) + loopSkip': false,  // 48022: frags expected ["flag.","Delirious,","come","\n","wattlebird?"], but got ["come","\n","wattlebird?"]
		'skip-combos: nextSentence(null) + loopSkip > once([0,0,-2]) + newWordFragment': false,  // 48043: event should not have been triggerd but WAS
		'skip-combos: nextSentence(null) + loopSkip > once([0,0,-2]) + progress': false,  // 48044: 'progress' expected [0.08333333333333333], but got [0.3333333333333333,0.25]
		'skip-combos: nextSentence(null) + loopSkip > once([0,0,-2]) + stopBegin': false,  // 48045: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + loopSkip > once([0,0,-2]) + stopFinish': false,  // 48046: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + loopSkip > once([0,0,-2]) + done': false,  // 48047: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + loopSkip > once([0,0,-2]) + loopSkip': false,  // 48048: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: nextSentence(null) + loopSkip > once([0,0,2]) + newWordFragment': false,  // 48069: frags expected ["you"], but got ["I"]
		'skip-combos: nextSentence(null) + loopSkip > once([0,0,2]) + progress': false,  // 48070: 'progress' expected [0.16666666666666666], but got [0.5]
		'skip-combos: nextSentence(null) + loopSkip > current(null) + newWordFragment': false,  // 48095: frags expected ["you"], but got ["I"]
		'skip-combos: nextSentence(null) + loopSkip > current(null) + progress': false,  // 48096: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.4166666666666667,0.5]
		'skip-combos: nextSentence(null) + loopSkip > current(null) + loopSkip': false,  // 48100: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: nextSentence(null) + loopSkip > jumpWords(-3) + newWordFragment': false,  // 48277: event should not have been triggerd but WAS
		'skip-combos: nextSentence(null) + loopSkip > jumpWords(-3) + progress': false,  // 48278: 'progress' expected [0.08333333333333333], but got [0.16666666666666666]
		'skip-combos: nextSentence(null) + loopSkip > jumpWords(-3) + stopBegin': false,  // 48279: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + loopSkip > jumpWords(-3) + stopFinish': false,  // 48280: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + loopSkip > jumpWords(-3) + done': false,  // 48281: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + loopSkip > jumpWords(-3) + loopSkip': false,  // 48282: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + loopSkip > jumpWords(-1) + newWordFragment': false,  // 48303: event should not have been triggerd but WAS
		'skip-combos: nextSentence(null) + loopSkip > jumpWords(-1) + progress': false,  // 48304: 'progress' expected [0.08333333333333333], but got [0.3333333333333333,0.25]
		'skip-combos: nextSentence(null) + loopSkip > jumpWords(-1) + stopBegin': false,  // 48305: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + loopSkip > jumpWords(-1) + stopFinish': false,  // 48306: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + loopSkip > jumpWords(-1) + done': false,  // 48307: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + loopSkip > jumpWords(-1) + loopSkip': false,  // 48308: frags expected ["Victorious,"], but got ["flag."]
		'skip-combos: nextSentence(null) + loopSkip > jumpWords(0) + newWordFragment': false,  // 48329: frags expected ["you"], but got ["I"]
		'skip-combos: nextSentence(null) + loopSkip > jumpWords(0) + progress': false,  // 48330: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.4166666666666667,0.5]
		'skip-combos: nextSentence(null) + loopSkip > jumpWords(0) + loopSkip': false,  // 48334: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: nextSentence(null) + loopSkip > jumpWords(4) + newWordFragment': false,  // 48355: frags expected ["I"], but got ["Why,"]
		'skip-combos: nextSentence(null) + loopSkip > jumpWords(4) + progress': false,  // 48356: 'progress' expected [0.4166666666666667,0.5], but got [0.75,0.8333333333333334]
		'skip-combos: nextSentence(null) + loopSkip > jumpWords(4) + loopSkip': false,  // 48360: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: nextSentence(null) + loopSkip > jumpSentences(0) + newWordFragment': false,  // 48485: frags expected ["you"], but got ["I"]
		'skip-combos: nextSentence(null) + loopSkip > jumpSentences(0) + progress': false,  // 48486: 'progress' expected [0.08333333333333333,0.16666666666666666], but got [0.4166666666666667,0.5]
		'skip-combos: nextSentence(null) + loopSkip > jumpSentences(0) + loopSkip': false,  // 48490: frags expected ["Victorious,"], but got ["Delirious,"]
		'skip-combos: nextSentence(null) + loopSkip > jumpSentences(1) + newWordFragment': false,  // 48511: frags expected ["I"], but got ["Why,"]
		'skip-combos: nextSentence(null) + loopSkip > jumpSentences(1) + progress': false,  // 48512: 'progress' expected [0.4166666666666667,0.5], but got [0.75,0.8333333333333334]
		'skip-combos: nextSentence(null) + loopSkip > jumpSentences(1) + loopSkip': false,  // 48516: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: nextSentence(null) + loopSkip > jumpSentences(3) + newWordFragment': false,  // 48537: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + loopSkip > jumpSentences(3) + progress': false,  // 48538: 'progress' expected [0.8333333333333334], but got [1]
		'skip-combos: nextSentence(null) + loopSkip > jumpSentences(3) + stopBegin': false,  // 48539: event should not have been triggerd but WAS
		'skip-combos: nextSentence(null) + loopSkip > jumpSentences(3) + stopFinish': false,  // 48540: event should not have been triggerd but WAS
		'skip-combos: nextSentence(null) + loopSkip > jumpSentences(3) + done': false,  // 48541: event should not have been triggerd but WAS
		'skip-combos: nextSentence(null) + loopSkip > jumpSentences(3) + loopSkip': false,  // 48542: event should not have been triggerd but WAS
		'skip-combos: nextSentence(null) + loopSkip > nextWord(null) + newWordFragment': false,  // 48589: frags expected ["you"], but got ["I"]
		'skip-combos: nextSentence(null) + loopSkip > nextWord(null) + progress': false,  // 48590: 'progress' expected [0.16666666666666666], but got [0.5]
		'skip-combos: nextSentence(null) + loopSkip > nextSentence(null) + newWordFragment': false,  // 48615: frags expected ["I"], but got ["Why,"]
		'skip-combos: nextSentence(null) + loopSkip > nextSentence(null) + progress': false,  // 48616: 'progress' expected [0.4166666666666667,0.5], but got [0.75,0.8333333333333334]
		'skip-combos: nextSentence(null) + loopSkip > nextSentence(null) + loopSkip': false,  // 48620: frags expected ["Delirious,"], but got ["\n"]
		'skip-combos: nextSentence(null) + loopSkip > prevWord(null) + newWordFragment': false,  // 48641: event should not have been triggerd but WAS
		'skip-combos: nextSentence(null) + loopSkip > prevWord(null) + progress': false,  // 48642: 'progress' expected [0.08333333333333333], but got [0.3333333333333333,0.25]
		'skip-combos: nextSentence(null) + loopSkip > prevWord(null) + stopBegin': false,  // 48643: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + loopSkip > prevWord(null) + stopFinish': false,  // 48644: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + loopSkip > prevWord(null) + done': false,  // 48645: event should have been triggerd but was NOT
		'skip-combos: nextSentence(null) + loopSkip > prevWord(null) + loopSkip': false,  // 48646: frags expected ["Victorious,"], but got ["flag."]

		// prevWord(null)
		'skip-combos: prevWord(null) + loopSkip > play(null) + playBegin': false,  // 23479: event should have been triggerd but was NOT
		'skip-combos: prevWord(null) + loopSkip > play(null) + playFinish': false,  // 23480: event should have been triggerd but was NOT
		'skip-combos: prevWord(null) + loopSkip > play(null) + restartBegin': false,  // 23483: event should not have been triggerd but WAS
		'skip-combos: prevWord(null) + loopSkip > play(null) + restartFinish': false,  // 23484: event should not have been triggerd but WAS
		'skip-combos: prevWord(null) + loopSkip > toggle(null) + playBegin': false,  // 23505: event should have been triggerd but was NOT
		'skip-combos: prevWord(null) + loopSkip > toggle(null) + playFinish': false,  // 23506: event should have been triggerd but was NOT
		'skip-combos: prevWord(null) + loopSkip > toggle(null) + restartBegin': false,  // 23509: event should not have been triggerd but WAS
		'skip-combos: prevWord(null) + loopSkip > toggle(null) + restartFinish': false,  // 23510: event should not have been triggerd but WAS

		// prevSentence(null)
		'skip-combos: prevSentence(null) + loopSkip > play(null) + playBegin': false,  // 47815: event should have been triggerd but was NOT
		'skip-combos: prevSentence(null) + loopSkip > play(null) + playFinish': false,  // 47816: event should have been triggerd but was NOT
		'skip-combos: prevSentence(null) + loopSkip > play(null) + restartBegin': false,  // 47819: event should not have been triggerd but WAS
		'skip-combos: prevSentence(null) + loopSkip > play(null) + restartFinish': false,  // 47820: event should not have been triggerd but WAS
		'skip-combos: prevSentence(null) + loopSkip > toggle(null) + playBegin': false,  // 47841: event should have been triggerd but was NOT
		'skip-combos: prevSentence(null) + loopSkip > toggle(null) + playFinish': false,  // 47842: event should have been triggerd but was NOT
		'skip-combos: prevSentence(null) + loopSkip > toggle(null) + restartBegin': false,  // 47845: event should not have been triggerd but WAS
		'skip-combos: prevSentence(null) + loopSkip > toggle(null) + restartFinish': false,  // 47846: event should not have been triggerd but WAS

		// Different than regular combos:
		// 'skip-combos: restart(null) + restartBegin > play(null) + newWordFragment': false,  // 1896: ?? (skip results are different than combos)
		// 'skip-combos: restart(null) + restartFinish > play(null) + newWordFragment': false,  // 2796: ?? (skip results are different than combos)
		// 'skip-combos: restart(null) + loopBegin > play(null) + newWordFragment': false,  // 16296: ?? (skip results are different than combos)
		// 'skip-combos: restart(null) + loopFinish > play(null) + newWordFragment': false,  // 17196: ?? (skip results are different than combos)
		// 'skip-combos: restart(null) + progress > play(null) + newWordFragment': false,  // 18996: ?? (skip results are different than combos)
		// 'skip-combos: play(null) + playBegin > play(null) + newWordFragment': false,  // 3696: ?? (skip results are different than combos)
		// 'skip-combos: play(null) + playFinish > play(null) + newWordFragment': false,  // 4596: ?? (skip results are different than combos)
		// 'skip-combos: play(null) + loopBegin > play(null) + newWordFragment': false,  // 16296: ?? (skip results are different than combos)
		// 'skip-combos: play(null) + loopFinish > play(null) + newWordFragment': false,  // 17196: ?? (skip results are different than combos)
		// 'skip-combos: play(null) + progress > play(null) + newWordFragment': false,  // 18996: ?? (skip results are different than combos)
		// 'skip-combos: toggle(null) + playBegin > play(null) + newWordFragment': false,  // 3696: ?? (skip results are different than combos)
		// 'skip-combos: toggle(null) + playFinish > play(null) + newWordFragment': false,  // 4596: ?? (skip results are different than combos)
		// 'skip-combos: toggle(null) + loopBegin > play(null) + newWordFragment': false,  // 16296: ?? (skip results are different than combos)
		// 'skip-combos: toggle(null) + loopFinish > play(null) + newWordFragment': false,  // 17196: ?? (skip results are different than combos)
		// 'skip-combos: toggle(null) + progress > play(null) + newWordFragment': false,  // 18996: ?? (skip results are different than combos)
		// 'skip-combos: fastForward(null) + fastForwardBegin > play(null) + newWordFragment': false,  // 14496: ?? (skip results are different than combos)
		// 'skip-combos: fastForward(null) + fastForwardBegin > current(null) + newWordFragment': false,  // 14746: ?? (skip results are different than combos)
		// 'skip-combos: fastForward(null) + fastForwardBegin > jumpWords(0) + newWordFragment': false,  // 14971: ?? (skip results are different than combos)
		// 'skip-combos: fastForward(null) + fastForwardBegin > jumpWords(4) + newWordFragment': false,  // 14996: ?? (skip results are different than combos)
		// 'skip-combos: fastForward(null) + fastForwardBegin > jumpSentences(0) + newWordFragment': false,  // 15121: ?? (skip results are different than combos)
		// 'skip-combos: fastForward(null) + fastForwardFinish > play(null) + newWordFragment': false,  // 15396: ?? (skip results are different than combos)
		// 'skip-combos: fastForward(null) + fastForwardFinish > current(null) + newWordFragment': false,  // 15646: ?? (skip results are different than combos)
		// 'skip-combos: fastForward(null) + fastForwardFinish > jumpWords(0) + newWordFragment': false,  // 15871: ?? (skip results are different than combos)
		// 'skip-combos: fastForward(null) + fastForwardFinish > jumpWords(4) + newWordFragment': false,  // 15896: ?? (skip results are different than combos)
		// 'skip-combos: fastForward(null) + fastForwardFinish > jumpSentences(0) + newWordFragment': false,  // 16021: ?? (skip results are different than combos)
		// 'skip-combos: fastForward(null) + loopBegin > play(null) + newWordFragment': false,  // 16296: ?? (skip results are different than combos)
		// 'skip-combos: fastForward(null) + loopBegin > current(null) + newWordFragment': false,  // 16546: ?? (skip results are different than combos)
		// 'skip-combos: fastForward(null) + loopBegin > jumpWords(0) + newWordFragment': false,  // 16771: ?? (skip results are different than combos)
		// 'skip-combos: fastForward(null) + loopBegin > jumpWords(4) + newWordFragment': false,  // 16796: ?? (skip results are different than combos)
		// 'skip-combos: fastForward(null) + loopBegin > jumpSentences(0) + newWordFragment': false,  // 16921: ?? (skip results are different than combos)
		// 'skip-combos: fastForward(null) + loopFinish > play(null) + newWordFragment': false,  // 17196: ?? (skip results are different than combos)
		// 'skip-combos: fastForward(null) + loopFinish > current(null) + newWordFragment': false,  // 17446: ?? (skip results are different than combos)
		// 'skip-combos: fastForward(null) + loopFinish > jumpWords(0) + newWordFragment': false,  // 17671: ?? (skip results are different than combos)
		// 'skip-combos: fastForward(null) + loopFinish > jumpWords(4) + newWordFragment': false,  // 17696: ?? (skip results are different than combos)
		// 'skip-combos: fastForward(null) + loopFinish > jumpSentences(0) + newWordFragment': false,  // 17821: ?? (skip results are different than combos)
		// 'skip-combos: fastForward(null) + newWordFragment > play(null) + newWordFragment': false,  // 18096: ?? (skip results are different than combos)
		// 'skip-combos: fastForward(null) + newWordFragment > current(null) + newWordFragment': false,  // 18346: ?? (skip results are different than combos)
		// 'skip-combos: fastForward(null) + newWordFragment > jumpWords(0) + newWordFragment': false,  // 18571: ?? (skip results are different than combos)
		// 'skip-combos: fastForward(null) + newWordFragment > jumpWords(4) + newWordFragment': false,  // 18596: ?? (skip results are different than combos)
		// 'skip-combos: fastForward(null) + newWordFragment > jumpSentences(0) + newWordFragment': false,  // 18721: ?? (skip results are different than combos)
		// 'skip-combos: fastForward(null) + progress > play(null) + newWordFragment': false,  // 18996: ?? (skip results are different than combos)
		// 'skip-combos: fastForward(null) + progress > current(null) + newWordFragment': false,  // 19246: ?? (skip results are different than combos)
		// 'skip-combos: fastForward(null) + progress > jumpWords(0) + newWordFragment': false,  // 19471: ?? (skip results are different than combos)
		// 'skip-combos: fastForward(null) + progress > jumpWords(4) + newWordFragment': false,  // 19496: ?? (skip results are different than combos)
		// 'skip-combos: fastForward(null) + progress > jumpSentences(0) + newWordFragment': false,  // 19621: ?? (skip results are different than combos)
		// 'skip-combos: once([0,0,2]) + onceBegin > play(null) + newWordFragment': false,  // 9096: ?? (skip results are different than combos)
		// 'skip-combos: once([0,0,2]) + onceBegin > current(null) + newWordFragment': false,  // 9346: ?? (skip results are different than combos)
		// 'skip-combos: once([0,0,2]) + onceBegin > jumpWords(0) + newWordFragment': false,  // 9571: ?? (skip results are different than combos)
		// 'skip-combos: once([0,0,2]) + onceBegin > jumpWords(4) + newWordFragment': false,  // 9596: ?? (skip results are different than combos)
		// 'skip-combos: once([0,0,2]) + onceBegin > jumpSentences(0) + newWordFragment': false,  // 9721: ?? (skip results are different than combos)
		// 'skip-combos: once([0,0,2]) + onceFinish > play(null) + newWordFragment': false,  // 9996: ?? (skip results are different than combos)
		// 'skip-combos: once([0,0,2]) + onceFinish > current(null) + newWordFragment': false,  // 10246: ?? (skip results are different than combos)
		// 'skip-combos: once([0,0,2]) + onceFinish > jumpWords(0) + newWordFragment': false,  // 10471: ?? (skip results are different than combos)
		// 'skip-combos: once([0,0,2]) + onceFinish > jumpWords(4) + newWordFragment': false,  // 10496: ?? (skip results are different than combos)
		// 'skip-combos: once([0,0,2]) + onceFinish > jumpSentences(0) + newWordFragment': false,  // 10621: ?? (skip results are different than combos)
		// 'skip-combos: once([0,0,2]) + loopBegin > play(null) + newWordFragment': false,  // 16296: ?? (skip results are different than combos)
		// 'skip-combos: once([0,0,2]) + loopBegin > current(null) + newWordFragment': false,  // 16546: ?? (skip results are different than combos)
		// 'skip-combos: once([0,0,2]) + loopBegin > jumpWords(0) + newWordFragment': false,  // 16771: ?? (skip results are different than combos)
		// 'skip-combos: once([0,0,2]) + loopBegin > jumpWords(4) + newWordFragment': false,  // 16796: ?? (skip results are different than combos)
		// 'skip-combos: once([0,0,2]) + loopBegin > jumpSentences(0) + newWordFragment': false,  // 16921: ?? (skip results are different than combos)
		// 'skip-combos: once([0,0,2]) + loopFinish > play(null) + newWordFragment': false,  // 17196: ?? (skip results are different than combos)
		// 'skip-combos: once([0,0,2]) + loopFinish > current(null) + newWordFragment': false,  // 17446: ?? (skip results are different than combos)
		// 'skip-combos: once([0,0,2]) + loopFinish > jumpWords(0) + newWordFragment': false,  // 17671: ?? (skip results are different than combos)
		// 'skip-combos: once([0,0,2]) + loopFinish > jumpWords(4) + newWordFragment': false,  // 17696: ?? (skip results are different than combos)
		// 'skip-combos: once([0,0,2]) + loopFinish > jumpSentences(0) + newWordFragment': false,  // 17821: ?? (skip results are different than combos)
		// 'skip-combos: once([0,0,2]) + newWordFragment > play(null) + newWordFragment': false,  // 18096: ?? (skip results are different than combos)
		// 'skip-combos: once([0,0,2]) + newWordFragment > current(null) + newWordFragment': false,  // 18346: ?? (skip results are different than combos)
		// 'skip-combos: once([0,0,2]) + newWordFragment > jumpWords(0) + newWordFragment': false,  // 18571: ?? (skip results are different than combos)
		// 'skip-combos: once([0,0,2]) + newWordFragment > jumpWords(4) + newWordFragment': false,  // 18596: ?? (skip results are different than combos)
		// 'skip-combos: once([0,0,2]) + newWordFragment > jumpSentences(0) + newWordFragment': false,  // 18721: ?? (skip results are different than combos)
		// 'skip-combos: once([0,0,2]) + progress > play(null) + newWordFragment': false,  // 18996: ?? (skip results are different than combos)
		// 'skip-combos: once([0,0,2]) + progress > current(null) + newWordFragment': false,  // 19246: ?? (skip results are different than combos)
		// 'skip-combos: once([0,0,2]) + progress > jumpWords(0) + newWordFragment': false,  // 19471: ?? (skip results are different than combos)
		// 'skip-combos: once([0,0,2]) + progress > jumpWords(4) + newWordFragment': false,  // 19496: ?? (skip results are different than combos)
		// 'skip-combos: once([0,0,2]) + progress > jumpSentences(0) + newWordFragment': false,  // 19621: ?? (skip results are different than combos)
		// 'skip-combos: nextWord(null) + onceBegin > play(null) + newWordFragment': false,  // 9096: ?? (skip results are different than combos)
		// 'skip-combos: nextWord(null) + onceBegin > current(null) + newWordFragment': false,  // 9346: ?? (skip results are different than combos)
		// 'skip-combos: nextWord(null) + onceBegin > jumpWords(0) + newWordFragment': false,  // 9571: ?? (skip results are different than combos)
		// 'skip-combos: nextWord(null) + onceBegin > jumpWords(4) + newWordFragment': false,  // 9596: ?? (skip results are different than combos)
		// 'skip-combos: nextWord(null) + onceBegin > jumpSentences(0) + newWordFragment': false,  // 9721: ?? (skip results are different than combos)
		// 'skip-combos: nextWord(null) + onceFinish > play(null) + newWordFragment': false,  // 9996: ?? (skip results are different than combos)
		// 'skip-combos: nextWord(null) + onceFinish > current(null) + newWordFragment': false,  // 10246: ?? (skip results are different than combos)
		// 'skip-combos: nextWord(null) + onceFinish > jumpWords(0) + newWordFragment': false,  // 10471: ?? (skip results are different than combos)
		// 'skip-combos: nextWord(null) + onceFinish > jumpWords(4) + newWordFragment': false,  // 10496: ?? (skip results are different than combos)
		// 'skip-combos: nextWord(null) + onceFinish > jumpSentences(0) + newWordFragment': false,  // 10621: ?? (skip results are different than combos)
		// 'skip-combos: nextWord(null) + loopBegin > play(null) + newWordFragment': false,  // 16296: ?? (skip results are different than combos)
		// 'skip-combos: nextWord(null) + loopBegin > current(null) + newWordFragment': false,  // 16546: ?? (skip results are different than combos)
		// 'skip-combos: nextWord(null) + loopBegin > jumpWords(0) + newWordFragment': false,  // 16771: ?? (skip results are different than combos)
		// 'skip-combos: nextWord(null) + loopBegin > jumpWords(4) + newWordFragment': false,  // 16796: ?? (skip results are different than combos)
		// 'skip-combos: nextWord(null) + loopBegin > jumpSentences(0) + newWordFragment': false,  // 16921: ?? (skip results are different than combos)
		// 'skip-combos: nextWord(null) + loopFinish > play(null) + newWordFragment': false,  // 17196: ?? (skip results are different than combos)
		// 'skip-combos: nextWord(null) + loopFinish > current(null) + newWordFragment': false,  // 17446: ?? (skip results are different than combos)
		// 'skip-combos: nextWord(null) + loopFinish > jumpWords(0) + newWordFragment': false,  // 17671: ?? (skip results are different than combos)
		// 'skip-combos: nextWord(null) + loopFinish > jumpWords(4) + newWordFragment': false,  // 17696: ?? (skip results are different than combos)
		// 'skip-combos: nextWord(null) + loopFinish > jumpSentences(0) + newWordFragment': false,  // 17821: ?? (skip results are different than combos)
		// 'skip-combos: nextWord(null) + newWordFragment > play(null) + newWordFragment': false,  // 18096: ?? (skip results are different than combos)
		// 'skip-combos: nextWord(null) + newWordFragment > current(null) + newWordFragment': false,  // 18346: ?? (skip results are different than combos)
		// 'skip-combos: nextWord(null) + newWordFragment > jumpWords(0) + newWordFragment': false,  // 18571: ?? (skip results are different than combos)
		// 'skip-combos: nextWord(null) + newWordFragment > jumpWords(4) + newWordFragment': false,  // 18596: ?? (skip results are different than combos)
		// 'skip-combos: nextWord(null) + newWordFragment > jumpSentences(0) + newWordFragment': false,  // 18721: ?? (skip results are different than combos)
		// 'skip-combos: nextWord(null) + progress > play(null) + newWordFragment': false,  // 18996: ?? (skip results are different than combos)
		// 'skip-combos: nextWord(null) + progress > current(null) + newWordFragment': false,  // 19246: ?? (skip results are different than combos)
		// 'skip-combos: nextWord(null) + progress > jumpWords(0) + newWordFragment': false,  // 19471: ?? (skip results are different than combos)
		// 'skip-combos: nextWord(null) + progress > jumpWords(4) + newWordFragment': false,  // 19496: ?? (skip results are different than combos)
		// 'skip-combos: nextWord(null) + progress > jumpSentences(0) + newWordFragment': false,  // 19621: ?? (skip results are different than combos)

	}  // end asts.expectedFailures{}

	return asts;
};  // End MakeAltAsserts()

