
jasmine.playbackExpectedFailures = [
// No expected failures until I've dealt with them
  // 'When Playback calls , for a test-test, `.play()` with null then, on \'resetFinish\', calls `.play()` with null and we collect data on \'playBegin\' it should return...',
  // 'When Playback calls , for a test-test, `.play()` with null then, on \'resetFinish\', calls `.play()` with null and we collect data on \'playFinish\' it should return...',
  // 'When Playback calls , for a test-test, `.play()` with null then, on \'resetFinish\', calls `.play()` with null and we collect data on \'restartBegin\' it should return...',
  // 'When Playback calls , for a test-test, `.play()` with null then, on \'resetFinish\', calls `.play()` with null and we collect data on \'restartBegin\' it should return...',
  // 'When Playback calls , for a test-test, `.play()` with null then, on \'resetFinish\', calls `.play()` with null and we collect data on \'restartFinish\' it should return...',
  // 'When Playback calls , for a test-test, `.play()` with null then, on \'resetFinish\', calls `.play()` with null and we collect data on \'restartFinish\' it should return...',
  // 'When Playback calls , for a test-test, `.play()` with null then, on \'restartBegin\', calls `.play()` with null and we collect data on \'playBegin\' it should return...',
  // 'When Playback calls , for a test-test, `.play()` with null then, on \'restartBegin\', calls `.play()` with null and we collect data on \'playFinish\' it should return...',
  // 'When Playback calls , for a test-test, `.play()` with null then, on \'restartBegin\', calls `.play()` with null and we collect data on \'restartBegin\' it should return...',
  // 'When Playback calls , for a test-test, `.play()` with null then, on \'restartBegin\', calls `.play()` with null and we collect data on \'restartBegin\' it should return...',
  // 'When Playback calls , for a test-test, `.play()` with null then, on \'restartBegin\', calls `.play()` with null and we collect data on \'restartFinish\' it should return...',
  // 'When Playback calls , for a test-test, `.play()` with null then, on \'restartBegin\', calls `.play()` with null and we collect data on \'restartFinish\' it should return...',
  // 'When Playback calls , for a test-test, `.reset()` with null then, on \'resetBegin\', calls `.play()` with null and we collect data on \'playBegin\' it should return...',
  // 'When Playback calls , for a test-test, `.reset()` with null then, on \'resetBegin\', calls `.play()` with null and we collect data on \'playFinish\' it should return...',
  // 'When Playback calls , for a test-test, `.reset()` with null then, on \'resetBegin\', calls `.play()` with null and we collect data on \'restartBegin\' it should return...',
  // 'When Playback calls , for a test-test, `.reset()` with null then, on \'resetBegin\', calls `.play()` with null and we collect data on \'restartBegin\' it should return...',
  // 'When Playback calls , for a test-test, `.reset()` with null then, on \'resetBegin\', calls `.play()` with null and we collect data on \'restartFinish\' it should return...',
  // 'When Playback calls , for a test-test, `.reset()` with null then, on \'resetBegin\', calls `.play()` with null and we collect data on \'restartFinish\' it should return...',
  // 'When Playback calls , for a test-test, `.reset()` with null then, on \'resetFinish\', calls `.play()` with null and we collect data on \'playBegin\' it should return...',
  // 'When Playback calls , for a test-test, `.reset()` with null then, on \'resetFinish\', calls `.play()` with null and we collect data on \'playFinish\' it should return...',
  // 'When Playback calls , for a test-test, `.reset()` with null then, on \'resetFinish\', calls `.play()` with null and we collect data on \'restartBegin\' it should return...',
  // 'When Playback calls , for a test-test, `.reset()` with null then, on \'resetFinish\', calls `.play()` with null and we collect data on \'restartBegin\' it should return...',
  // 'When Playback calls , for a test-test, `.reset()` with null then, on \'resetFinish\', calls `.play()` with null and we collect data on \'restartFinish\' it should return...',
  // 'When Playback calls , for a test-test, `.reset()` with null then, on \'resetFinish\', calls `.play()` with null and we collect data on \'restartFinish\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'playBegin\', calls `.play()` with null and we collect data on \'playBegin\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'playBegin\', calls `.play()` with null and we collect data on \'playFinish\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'playBegin\', calls `.play()` with null and we collect data on \'restartBegin\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'playBegin\', calls `.play()` with null and we collect data on \'restartBegin\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'playBegin\', calls `.play()` with null and we collect data on \'restartFinish\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'playBegin\', calls `.play()` with null and we collect data on \'restartFinish\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'playBegin\', calls `.play()` with null and we collect data on \'playBegin\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'playBegin\', calls `.play()` with null and we collect data on \'playFinish\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'playBegin\', calls `.play()` with null and we collect data on \'restartBegin\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'playBegin\', calls `.play()` with null and we collect data on \'restartBegin\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'playBegin\', calls `.play()` with null and we collect data on \'restartFinish\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'playBegin\', calls `.play()` with null and we collect data on \'restartFinish\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'playBegin\', calls `.play()` with null and we collect data on \'playBegin\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'playBegin\', calls `.play()` with null and we collect data on \'playFinish\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'playBegin\', calls `.play()` with null and we collect data on \'restartBegin\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'playBegin\', calls `.play()` with null and we collect data on \'restartBegin\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'playBegin\', calls `.play()` with null and we collect data on \'restartFinish\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'playBegin\', calls `.play()` with null and we collect data on \'restartFinish\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'playBegin\', calls `.play()` with null and we collect data on \'playBegin\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'playBegin\', calls `.play()` with null and we collect data on \'playFinish\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'playBegin\', calls `.play()` with null and we collect data on \'restartBegin\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'playBegin\', calls `.play()` with null and we collect data on \'restartBegin\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'playBegin\', calls `.play()` with null and we collect data on \'restartFinish\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'playBegin\', calls `.play()` with null and we collect data on \'restartFinish\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'resetFinish\', calls `.play()` with null and we collect data on \'playBegin\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'resetFinish\', calls `.play()` with null and we collect data on \'playFinish\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'resetFinish\', calls `.play()` with null and we collect data on \'restartBegin\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'resetFinish\', calls `.play()` with null and we collect data on \'restartBegin\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'resetFinish\', calls `.play()` with null and we collect data on \'restartFinish\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'resetFinish\', calls `.play()` with null and we collect data on \'restartFinish\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'restartBegin\', calls `.play()` with null and we collect data on \'playBegin\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'restartBegin\', calls `.play()` with null and we collect data on \'playFinish\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'restartBegin\', calls `.play()` with null and we collect data on \'restartBegin\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'restartBegin\', calls `.play()` with null and we collect data on \'restartBegin\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'restartBegin\', calls `.play()` with null and we collect data on \'restartFinish\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'restartBegin\', calls `.play()` with null and we collect data on \'restartFinish\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'restartBegin\', calls `.play()` with null and we collect data on \'playBegin\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'restartBegin\', calls `.play()` with null and we collect data on \'playFinish\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'restartBegin\', calls `.play()` with null and we collect data on \'restartBegin\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'restartBegin\', calls `.play()` with null and we collect data on \'restartBegin\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'restartBegin\', calls `.play()` with null and we collect data on \'restartFinish\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'restartBegin\', calls `.play()` with null and we collect data on \'restartFinish\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'restartFinish\', calls `.play()` with null and we collect data on \'playBegin\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'restartFinish\', calls `.play()` with null and we collect data on \'playFinish\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'restartFinish\', calls `.play()` with null and we collect data on \'restartBegin\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'restartFinish\', calls `.play()` with null and we collect data on \'restartBegin\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'restartFinish\', calls `.play()` with null and we collect data on \'restartFinish\' it should return...',
  // 'When Playback calls , for a test-test, `.restart()` with null then, on \'restartFinish\', calls `.play()` with null and we collect data on \'restartFinish\' it should return...' 
  ];
